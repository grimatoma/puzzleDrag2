"use strict";
(() => {
  // src/textures/seasonal/tree/oak.ts
  function groundShadow(ctx, rx = 16, alpha = 0.22) {
    ctx.fillStyle = `rgba(0,0,0,${alpha})`;
    ctx.beginPath();
    ctx.ellipse(0, 22, rx, 4, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function canopyBlob(ctx, x, y, r, colors, alpha = 1) {
    const [dark, mid, light] = colors;
    ctx.save();
    ctx.globalAlpha = alpha;
    ctx.fillStyle = dark;
    ctx.beginPath();
    ctx.ellipse(x, y + r * 0.18, r, r * 0.92, 0, 0, Math.PI * 2);
    ctx.fill();
    const grad = ctx.createRadialGradient(x - r * 0.4, y - r * 0.4, r * 0.2, x, y, r);
    grad.addColorStop(0, light);
    grad.addColorStop(0.55, mid);
    grad.addColorStop(1, dark);
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.ellipse(x, y, r * 0.96, r * 0.86, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = light;
    ctx.globalAlpha = alpha * 0.7;
    ctx.beginPath();
    ctx.ellipse(x - r * 0.38, y - r * 0.42, r * 0.34, r * 0.28, -0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function trunk(ctx, wet = false) {
    const bark = wet ? "#3c2c1c" : "#6b4a26";
    const barkLight = wet ? "#5a4530" : "#8a6336";
    const barkDark = wet ? "#241a10" : "#4a3318";
    ctx.fillStyle = barkDark;
    ctx.beginPath();
    ctx.moveTo(-5.5, 20);
    ctx.quadraticCurveTo(-4, 6, -3, -2);
    ctx.lineTo(3, -2);
    ctx.quadraticCurveTo(4, 6, 5.5, 20);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = bark;
    ctx.beginPath();
    ctx.moveTo(-4.6, 20);
    ctx.quadraticCurveTo(-3.2, 6, -2.4, -2);
    ctx.lineTo(2.4, -2);
    ctx.quadraticCurveTo(3.2, 6, 4.6, 20);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = barkLight;
    ctx.lineWidth = 1.2;
    ctx.beginPath();
    ctx.moveTo(-4, 19);
    ctx.quadraticCurveTo(-2.8, 6, -2, -1);
    ctx.stroke();
    ctx.fillStyle = barkDark;
    ctx.beginPath();
    ctx.moveTo(-5.5, 20);
    ctx.quadraticCurveTo(-9, 19, -11, 20.5);
    ctx.quadraticCurveTo(-7, 20.5, -4.5, 19.5);
    ctx.closePath();
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(5.5, 20);
    ctx.quadraticCurveTo(9, 19, 11, 20.5);
    ctx.quadraticCurveTo(7, 20.5, 4.5, 19.5);
    ctx.closePath();
    ctx.fill();
  }
  var BRANCHES = [
    // [ctrlX, ctrlY, tipX, tipY, width]
    [-6, -6, -13, -16, 2.6],
    [-2, -10, -6, -22, 2.2],
    [3, -10, 6, -23, 2.4],
    [7, -6, 14, -15, 2.6],
    [0, -8, 1, -24, 2],
    [-9, -3, -16, -8, 2],
    [9, -3, 17, -7, 2]
  ];
  function branchSilhouette(ctx, color, sway, snow) {
    ctx.lineCap = "round";
    BRANCHES.forEach(([cx, cy, tx, ty, w], i) => {
      const s = sway * (0.4 + Math.abs(tx) / 24);
      ctx.strokeStyle = color;
      ctx.lineWidth = w;
      ctx.beginPath();
      ctx.moveTo(0, -2);
      ctx.quadraticCurveTo(cx + s * 0.5, cy, tx + s, ty);
      ctx.stroke();
      ctx.lineWidth = w * 0.55;
      ctx.beginPath();
      ctx.moveTo(cx + s * 0.5, cy);
      ctx.lineTo(cx + s * 0.5 + (tx < 0 ? -4 : 4), cy - 5);
      ctx.stroke();
      if (snow) {
        ctx.fillStyle = "#f4f8ff";
        ctx.beginPath();
        ctx.ellipse(tx + s, ty - 0.5, 2.6 + i % 2 * 0.6, 1.5, tx < 0 ? -0.5 : 0.5, 0, Math.PI * 2);
        ctx.fill();
      }
    });
    ctx.lineCap = "butt";
  }
  var SPRING_CLUSTERS = [
    // [x, y, r]
    [-12, -15, 4],
    [-5, -20, 4.5],
    [4, -21, 4.5],
    [13, -14, 4],
    [0, -10, 3.5],
    [-15, -8, 3],
    [16, -7, 3]
  ];
  function springOak(ctx, sway, budPulse) {
    groundShadow(ctx, 14, 0.2);
    trunk(ctx, false);
    branchSilhouette(ctx, "#5a3d20", sway, false);
    const fresh = ["#5a7d24", "#86b53a", "#c0e26a"];
    SPRING_CLUSTERS.forEach(([x, y, r]) => {
      const s = sway * (0.3 + Math.abs(x) / 28);
      canopyBlob(ctx, x + s, y, r, fresh, 0.9);
    });
    ctx.fillStyle = "#a6e06a";
    SPRING_CLUSTERS.forEach(([x, y, r], i) => {
      const s = sway * (0.3 + Math.abs(x) / 28);
      for (let k = 0; k < 3; k++) {
        const a = i * 1.7 + k * 2.1;
        ctx.beginPath();
        ctx.ellipse(x + s + Math.cos(a) * r, y + Math.sin(a) * r, 1.4, 2.2, a, 0, Math.PI * 2);
        ctx.fill();
      }
    });
    const buds = [
      [-8, -18],
      [6, -19],
      [-2, -14],
      [11, -12],
      [-13, -11]
    ];
    buds.forEach(([bx, by], i) => {
      const a = 0.5 + 0.45 * (0.5 + 0.5 * Math.sin(budPulse + i * 1.3));
      ctx.fillStyle = `rgba(232,224,150,${a})`;
      ctx.beginPath();
      ctx.arc(bx + sway * 0.2, by, 1.5, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function drawOakSpring(ctx) {
    springOak(ctx, 0, 0.6);
  }
  function animOakSpring(ctx, t) {
    const sway = Math.sin(t * 1.3) * 1.6;
    springOak(ctx, sway, t * 2.4);
  }
  var SUMMER_BLOBS = [
    // [x, y, r]
    [0, -16, 11],
    [-9, -13, 7.5],
    [9, -13, 7.5],
    [-6, -21, 6.5],
    [6, -21, 6.5],
    [0, -24, 6],
    [-13, -9, 5.5],
    [13, -9, 5.5],
    [0, -10, 7]
  ];
  function summerOak(ctx, sway, shimmer) {
    groundShadow(ctx, 16, 0.22);
    trunk(ctx, false);
    const green = ["#2f5418", "#4f8a2a", "#86bf48"];
    SUMMER_BLOBS.forEach(([x, y, r], i) => {
      const s = sway * (0.25 + Math.abs(x) / 26) + Math.sin(shimmer + i) * 0.4;
      canopyBlob(ctx, x + s, y, r, green, 1);
    });
    ctx.fillStyle = `rgba(190,230,140,${0.4 + 0.3 * (0.5 + 0.5 * Math.sin(shimmer))})`;
    [[-7, -22, 2], [-2, -19, 1.6], [-11, -14, 1.8]].forEach(([fx, fy, r]) => {
      ctx.beginPath();
      ctx.arc(fx + sway * 0.3, fy, r, 0, Math.PI * 2);
      ctx.fill();
    });
    const flx = 15 + Math.sin(shimmer * 0.8) * 1.5;
    const fly = -6 + Math.cos(shimmer * 1.1) * 1.2;
    ctx.fillStyle = "#4f8a2a";
    ctx.save();
    ctx.translate(flx, fly);
    ctx.rotate(Math.sin(shimmer) * 0.4);
    ctx.beginPath();
    ctx.ellipse(0, 0, 1.6, 2.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function drawOakSummer(ctx) {
    summerOak(ctx, 0, 0);
  }
  function animOakSummer(ctx, t) {
    const sway = Math.sin(t * 1.3) * 1.4;
    summerOak(ctx, sway, t * 1.9);
  }
  var AUTUMN_BLOBS = [
    [0, -16, 10.5, 0.5],
    [-9, -13, 7, 0.2],
    [9, -13, 7, 0.8],
    [-6, -21, 6, 0.4],
    [6, -21, 6, 0.7],
    [0, -24, 5.5, 0.3],
    [-13, -9, 5, 0.9],
    [13, -9, 5, 0.1],
    [0, -10, 6.5, 0.6]
  ];
  function autumnColors(hue) {
    const stops = [
      [0, [216, 168, 48]],
      // gold
      [0.5, [196, 96, 24]],
      // orange
      [1, [150, 52, 28]]
      // russet
    ];
    function pick(h) {
      let lo = stops[0];
      let hi = stops[stops.length - 1];
      for (let i = 0; i < stops.length - 1; i++) {
        if (h >= stops[i][0] && h <= stops[i + 1][0]) {
          lo = stops[i];
          hi = stops[i + 1];
          break;
        }
      }
      const span = hi[0] - lo[0] || 1;
      const f = (h - lo[0]) / span;
      return [
        lo[1][0] + (hi[1][0] - lo[1][0]) * f,
        lo[1][1] + (hi[1][1] - lo[1][1]) * f,
        lo[1][2] + (hi[1][2] - lo[1][2]) * f
      ];
    }
    const [r, g, b] = pick(Math.max(0, Math.min(1, hue)));
    const dark = `rgb(${Math.round(r * 0.55)},${Math.round(g * 0.45)},${Math.round(b * 0.4)})`;
    const mid = `rgb(${Math.round(r)},${Math.round(g)},${Math.round(b)})`;
    const light = `rgb(${Math.min(255, Math.round(r * 1.25))},${Math.min(255, Math.round(g * 1.25))},${Math.min(255, Math.round(b * 1.2))})`;
    return [dark, mid, light];
  }
  function acorns(ctx) {
    const spots = [[-4, -9], [7, -7]];
    spots.forEach(([ax, ay]) => {
      ctx.fillStyle = "#6b4710";
      ctx.beginPath();
      ctx.ellipse(ax, ay, 2.2, 1.4, 0, Math.PI, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "#b5762a";
      ctx.beginPath();
      ctx.ellipse(ax, ay + 1.6, 1.8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,235,180,0.5)";
      ctx.beginPath();
      ctx.arc(ax - 0.6, ay + 1, 0.6, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function autumnOak(ctx, sway, fall) {
    groundShadow(ctx, 15, 0.22);
    trunk(ctx, false);
    AUTUMN_BLOBS.forEach(([x, y, r, hue], i) => {
      if (i === 5) return;
      const s = sway * (0.25 + Math.abs(x) / 26);
      canopyBlob(ctx, x + s, y, r, autumnColors(hue), 0.95);
    });
    acorns(ctx);
    const leaves = [
      // [startX, hue, phase, swayAmp]
      [-6, 0.3, 0, 6],
      [9, 0.8, 0.5, 5]
    ];
    leaves.forEach(([sx, hue, phase, amp]) => {
      const prog = ((fall + phase) % 1 + 1) % 1;
      const ly = -6 + prog * 26;
      const lx = sx + Math.sin(prog * Math.PI * 2 + phase * 6) * amp;
      const [, mid] = autumnColors(hue);
      ctx.fillStyle = mid;
      ctx.save();
      ctx.translate(lx, ly);
      ctx.rotate(prog * Math.PI * 3 + phase * 4);
      ctx.beginPath();
      ctx.ellipse(0, 0, 1.7, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    });
    ctx.fillStyle = autumnColors(0.6)[1];
    [[-7, 21], [5, 22]].forEach(([fx, fy]) => {
      ctx.beginPath();
      ctx.ellipse(fx, fy, 2.4, 1.3, 0.4, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function drawOakAutumn(ctx) {
    autumnOak(ctx, 0, 0.25);
  }
  function animOakAutumn(ctx, t) {
    const sway = Math.sin(t * 1.1) * 1.2;
    const fall = t * 0.28 % 1;
    autumnOak(ctx, sway, fall);
  }
  function winterOak(ctx, sway, flakes, sheen2) {
    groundShadow(ctx, 16, 0.18);
    const snow = ctx.createLinearGradient(0, 16, 0, 24);
    snow.addColorStop(0, "#eef4fb");
    snow.addColorStop(1, "#c2d2e4");
    ctx.fillStyle = snow;
    ctx.beginPath();
    ctx.ellipse(0, 20, 18, 6, 0, 0, Math.PI * 2);
    ctx.fill();
    trunk(ctx, true);
    branchSilhouette(ctx, "#2e2114", sway, true);
    ctx.fillStyle = `rgba(200,224,255,${0.16 + sheen2 * 0.16})`;
    ctx.beginPath();
    ctx.ellipse(-3, 19, 10, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "#ffffff";
    flakes.forEach(([fx, fy, r]) => {
      ctx.globalAlpha = 0.85;
      ctx.beginPath();
      ctx.arc(fx, fy, r, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.globalAlpha = 1;
  }
  function drawOakWinter(ctx) {
    winterOak(
      ctx,
      0,
      [
        [-9, -14, 1.3],
        [6, -4, 1.1],
        [12, -18, 1],
        [-3, 6, 1.2]
      ],
      0.4
    );
  }
  function animOakWinter(ctx, t) {
    const sway = Math.sin(t * 0.9) * 1;
    const span = 36;
    const seeds = [
      [-9, 1.3, 0],
      [6, 1.1, 0.45],
      [12, 1, 0.7],
      [-3, 1.2, 0.25]
    ];
    const flakes = seeds.map(([fx, r, phase]) => {
      const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
      const fy = -24 + prog * span;
      const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
      return [driftX, fy, r];
    });
    const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.7);
    winterOak(ctx, sway, flakes, sheen2);
  }
  function lerp(a, b, f) {
    return a + (b - a) * f;
  }
  function springToSummer(ctx, p) {
    const q = Math.max(0, Math.min(1, p));
    groundShadow(ctx, lerp(14, 16, q), 0.21);
    trunk(ctx, false);
    branchSilhouette(ctx, "#5a3d20", 0, false);
    const fresh = ["#5a7d24", "#86b53a", "#c0e26a"];
    SPRING_CLUSTERS.forEach(([x, y, r]) => {
      const a = 0.9 * (1 - q);
      if (a > 0.02) canopyBlob(ctx, x, y, lerp(r, r * 0.8, q), fresh, a);
    });
    const green = ["#2f5418", "#4f8a2a", "#86bf48"];
    SUMMER_BLOBS.forEach(([x, y, r], i) => {
      const grow = Math.max(0, q * 1.15 - i * 0.015);
      const rr = r * Math.min(1, grow);
      if (rr > 0.5) canopyBlob(ctx, x, y, rr, green, Math.min(1, q + 0.15));
    });
    const buds = [
      [-8, -18],
      [6, -19],
      [-2, -14],
      [11, -12],
      [-13, -11]
    ];
    const budA = Math.max(0, 0.6 * (1 - q * 1.6));
    if (budA > 0.02) {
      buds.forEach(([bx, by]) => {
        ctx.fillStyle = `rgba(232,224,150,${budA})`;
        ctx.beginPath();
        ctx.arc(bx, by, 1.4, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    ctx.globalAlpha = 1;
  }
  function summerToAutumn(ctx, p) {
    const q = Math.max(0, Math.min(1, p));
    groundShadow(ctx, lerp(16, 15, q), 0.22);
    trunk(ctx, false);
    SUMMER_BLOBS.forEach(([x, y, r], i) => {
      const aBlob = AUTUMN_BLOBS[Math.min(i, AUTUMN_BLOBS.length - 1)];
      const [adark, amid, alight] = autumnColors(aBlob[3]);
      const greenCols = ["#2f5418", "#4f8a2a", "#86bf48"];
      const autumnCols = [adark, amid, alight];
      const rr = i === 5 ? r * lerp(1, 0.15, q) : r;
      if (rr > 0.5) {
        canopyBlob(ctx, x, y, rr, greenCols, 1);
        if (q > 0.01) canopyBlob(ctx, x, y, rr, autumnCols, q);
      }
    });
    if (q > 0.5) {
      ctx.save();
      ctx.globalAlpha = (q - 0.5) * 2;
      acorns(ctx);
      ctx.restore();
    }
    if (q > 0.65) {
      const d = (q - 0.65) / 0.35;
      const leaves = [
        [-6, -6, 0.4],
        [9, -8, 0.8]
      ];
      leaves.forEach(([sx, sy, hue]) => {
        const lx = sx + d * 2;
        const ly = sy + d * 5;
        ctx.fillStyle = autumnColors(hue)[1];
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(d * 1.4);
        ctx.globalAlpha = 1 - d * 0.3;
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.7, 3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      });
    }
    ctx.globalAlpha = 1;
  }
  function autumnToWinter(ctx, p) {
    const q = Math.max(0, Math.min(1, p));
    const shed = Math.min(1, q / 0.7);
    const snowAmt = Math.max(0, (q - 0.4) / 0.6);
    groundShadow(ctx, lerp(15, 16, q), lerp(0.22, 0.18, q));
    if (snowAmt > 0.01) {
      const snow = ctx.createLinearGradient(0, 16, 0, 24);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c2d2e4");
      ctx.save();
      ctx.globalAlpha = snowAmt;
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 20, 18, 6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    trunk(ctx, false);
    if (snowAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = snowAmt;
      trunk(ctx, true);
      ctx.restore();
    }
    branchSilhouette(ctx, "#3a2a18", 0, false);
    AUTUMN_BLOBS.forEach(([x, y, r, hue], i) => {
      if (i === 5) return;
      const a = 0.95 * (1 - shed);
      if (a > 0.03) {
        const drop = shed * (8 + Math.abs(x) * 0.3);
        canopyBlob(ctx, x, y + drop, r * (1 - shed * 0.4), autumnColors(hue), a);
      }
    });
    if (shed > 0.05 && shed < 0.98) {
      const leaves = [
        [-6, 0.3, 0, 6],
        [9, 0.8, 0.4, 5],
        [2, 0.6, 0.7, 5]
      ];
      leaves.forEach(([sx, hue, phase, amp]) => {
        const prog = Math.min(1, shed + phase * 0.3);
        const ly = -8 + prog * 30;
        const lx = sx + Math.sin(prog * Math.PI * 2 + phase * 6) * amp;
        ctx.fillStyle = autumnColors(hue)[1];
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(prog * Math.PI * 3 + phase * 4);
        ctx.globalAlpha = 1 - prog * 0.5;
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.7, 3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      });
    }
    if (snowAmt > 0.02) {
      ctx.save();
      ctx.globalAlpha = snowAmt;
      BRANCHES.forEach(([_cx, _cy, tx, ty], i) => {
        ctx.fillStyle = "#f4f8ff";
        ctx.beginPath();
        ctx.ellipse(tx, ty - 0.5, 2.6 + i % 2 * 0.6, 1.5, tx < 0 ? -0.5 : 0.5, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.fillStyle = `rgba(200,224,255,${0.16 * snowAmt})`;
      ctx.beginPath();
      ctx.ellipse(-3, 19, 10, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.globalAlpha = 1;
  }
  var VARIANTS = {
    Spring: { draw: drawOakSpring, anim: animOakSpring },
    Summer: { draw: drawOakSummer, anim: animOakSummer },
    Autumn: { draw: drawOakAutumn, anim: animOakAutumn },
    Winter: { draw: drawOakWinter, anim: animOakWinter }
  };
  var TRANSITIONS = {
    0: springToSummer,
    1: summerToAutumn,
    2: autumnToWinter
  };

  // src/textures/seasonal/tree/birch.ts
  function clamp01(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp2(a, b, f) {
    return a + (b - a) * f;
  }
  function lerp3(a, b, f) {
    return [lerp2(a[0], b[0], f), lerp2(a[1], b[1], f), lerp2(a[2], b[2], f)];
  }
  function rgb(c, alpha = 1) {
    const r = Math.round(clamp01(c[0] / 255) * 255);
    const g = Math.round(clamp01(c[1] / 255) * 255);
    const b = Math.round(clamp01(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${alpha})`;
  }
  function scale3(c, k) {
    return [c[0] * k, c[1] * k, c[2] * k];
  }
  var smoother = (x) => {
    const c = clamp01(x);
    return c * c * c * (c * (6 * c - 15) + 10);
  };
  function bobAt(t, A = 0.6, w = 1.2) {
    return A * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP = {
    Spring: {
      foliageDark: [78, 122, 52],
      foliageMid: [134, 188, 78],
      foliageLight: [196, 226, 132],
      bark: [238, 240, 236],
      barkShade: [196, 202, 200],
      lenticel: [70, 70, 66],
      padGrass: [126, 196, 86],
      soil: [104, 78, 44],
      outlineTint: [44, 50, 38],
      lightTint: [210, 235, 230],
      // cool-bright
      leafDensity: 0.6,
      catkinAmt: 0.85,
      frostAmt: 0,
      branchSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.5,
      fallenLeafAmt: 0,
      lightStrength: 0.22,
      shadowStrength: 0.2
    },
    Summer: {
      foliageDark: [42, 92, 32],
      foliageMid: [70, 142, 44],
      foliageLight: [148, 200, 86],
      bark: [240, 242, 238],
      barkShade: [194, 200, 196],
      lenticel: [64, 64, 60],
      padGrass: [70, 158, 64],
      soil: [96, 70, 40],
      outlineTint: [36, 48, 30],
      lightTint: [255, 244, 206],
      // warm
      leafDensity: 1,
      catkinAmt: 0,
      frostAmt: 0,
      branchSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.24,
      shadowStrength: 0.26
    },
    Autumn: {
      foliageDark: [150, 92, 24],
      foliageMid: [224, 168, 44],
      foliageLight: [248, 214, 96],
      bark: [232, 230, 220],
      barkShade: [188, 184, 172],
      lenticel: [72, 60, 44],
      padGrass: [150, 144, 86],
      soil: [110, 80, 44],
      outlineTint: [54, 42, 26],
      lightTint: [250, 220, 168],
      // low amber
      leafDensity: 0.78,
      catkinAmt: 0,
      frostAmt: 0,
      branchSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.6,
      lightStrength: 0.26,
      shadowStrength: 0.22
    },
    Winter: {
      foliageDark: [120, 132, 120],
      foliageMid: [160, 172, 160],
      foliageLight: [200, 210, 204],
      bark: [236, 240, 244],
      barkShade: [188, 198, 206],
      lenticel: [58, 60, 64],
      padGrass: [196, 210, 224],
      soil: [120, 126, 136],
      outlineTint: [46, 52, 62],
      lightTint: [206, 222, 240],
      // cool blue-grey
      leafDensity: 0,
      catkinAmt: 0,
      frostAmt: 0.8,
      branchSnowAmt: 0.85,
      padSnowAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.16
    }
  };
  var BRANCHES2 = [
    [-5, -6, -13, -15, 2],
    [-2, -12, -7, -22, 1.7],
    [3, -12, 7, -23, 1.8],
    [6, -6, 13, -14, 2],
    [0, -10, 1, -25, 1.8],
    [-8, -2, -15, -7, 1.6],
    [8, -2, 16, -6, 1.6]
  ];
  var CLUSTERS = [
    [-12, -14, 5],
    [-5, -20, 5.6],
    [4, -21, 5.6],
    [13, -13, 5],
    [0, -9, 4.4],
    [-15, -7, 3.8],
    [16, -6, 3.8],
    [-3, -17, 4.6],
    [7, -16, 4.6]
  ];
  function padEllipse(ctx, p) {
    ctx.fillStyle = `rgba(0,0,0,${0.24 * p.shadowStrength + 0.04})`;
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb(scale3(p.padGrass, 0.6), 1);
    ctx.beginPath();
    ctx.ellipse(0, 20.4, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb(p.padGrass, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt < 0.6) {
      ctx.fillStyle = rgb(scale3(p.padGrass, 1.12), 1 - p.padSnowAmt);
      const tufts = 11;
      for (let i = 0; i < tufts; i++) {
        const a = i / tufts * Math.PI * 2;
        const tx = Math.cos(a) * 17;
        const ty = 19 + Math.sin(a) * 4.7;
        if (ty < 17) continue;
        ctx.beginPath();
        ctx.ellipse(tx, ty, 2, 1.3, a, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.fillStyle = rgb(p.soil, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19.4, 6.5, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.blossomAmt > 0.02) {
      const spots = [
        [-12, 20],
        [-6, 21.6],
        [8, 21],
        [13, 19.6],
        [2, 22],
        [-2, 19.6]
      ];
      spots.forEach(([sx, sy], i) => {
        const a = p.blossomAmt * (i % 2 === 0 ? 0.9 : 0.7);
        ctx.fillStyle = `rgba(255,236,246,${a})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 1.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(248,210,90,${a})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 0.45, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.fallenLeafAmt > 0.02) {
      const leaves = [
        [-8, 20.4, 0.5],
        [6, 21.4, -0.4],
        [-1, 22, 0.2],
        [11, 20, 0.8]
      ];
      leaves.forEach(([lx, ly, rot], i) => {
        if (i / leaves.length > p.fallenLeafAmt + 0.05) return;
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb(p.foliageMid, Math.min(1, p.fallenLeafAmt + 0.3));
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.4, 1.3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb(p.outlineTint, 0.4);
        ctx.lineWidth = 0.5;
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.padSnowAmt > 0.02) {
      const snow = ctx.createLinearGradient(0, 15, 0, 23);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c6d6e6");
      ctx.save();
      ctx.globalAlpha = p.padSnowAmt;
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = `rgba(255,255,255,${0.5 * p.frostAmt + 0.3})`;
        const sparkle = [
          [-10, 18],
          [-4, 20],
          [3, 19],
          [9, 20.5],
          [13, 18.5],
          [-13, 19.5]
        ];
        sparkle.forEach(([sx, sy], i) => {
          if (i / sparkle.length > p.frostAmt) return;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
    }
  }
  function trunk2(ctx, p) {
    ctx.fillStyle = rgb(p.outlineTint, 0.9);
    ctx.beginPath();
    ctx.moveTo(-4, 19);
    ctx.quadraticCurveTo(-3, 4, -2.4, -4);
    ctx.lineTo(2.4, -4);
    ctx.quadraticCurveTo(3, 4, 4, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb(p.barkShade, 1);
    ctx.beginPath();
    ctx.moveTo(-3.2, 19);
    ctx.quadraticCurveTo(-2.4, 4, -1.9, -4);
    ctx.lineTo(1.9, -4);
    ctx.quadraticCurveTo(2.4, 4, 3.2, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb(p.bark, 1);
    ctx.beginPath();
    ctx.moveTo(-3.2, 19);
    ctx.quadraticCurveTo(-2.4, 4, -1.9, -4);
    ctx.lineTo(0.6, -4);
    ctx.quadraticCurveTo(0.2, 6, -0.4, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb(scale3(p.bark, 0.86), 1);
    ctx.beginPath();
    ctx.moveTo(-3.2, 19);
    ctx.quadraticCurveTo(-6.5, 18.4, -8, 19.6);
    ctx.quadraticCurveTo(-5, 19.6, -2.8, 18.6);
    ctx.closePath();
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(3.2, 19);
    ctx.quadraticCurveTo(6.5, 18.4, 8, 19.6);
    ctx.quadraticCurveTo(5, 19.6, 2.8, 18.6);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb(p.lenticel, 0.9);
    ctx.lineWidth = 1;
    ctx.lineCap = "round";
    const marks = [
      [-1.4, 15, 2.2],
      [0.8, 11, 1.6],
      [-1, 7, 2],
      [1, 3, 1.4],
      [-0.8, -0.5, 1.8]
    ];
    marks.forEach(([mx, my, w]) => {
      ctx.beginPath();
      ctx.moveTo(mx - w / 2, my);
      ctx.lineTo(mx + w / 2, my + 0.4);
      ctx.stroke();
    });
    ctx.lineCap = "butt";
  }
  function branches(ctx, p, sway) {
    ctx.lineCap = "round";
    BRANCHES2.forEach(([cx, cy, tx, ty, w], i) => {
      const s = sway * (0.4 + Math.abs(tx) / 24);
      ctx.strokeStyle = rgb(scale3(p.bark, 0.9), 1);
      ctx.lineWidth = w;
      ctx.beginPath();
      ctx.moveTo(0, -3);
      ctx.quadraticCurveTo(cx + s * 0.5, cy, tx + s, ty);
      ctx.stroke();
      ctx.strokeStyle = rgb(p.outlineTint, 0.5);
      ctx.lineWidth = w * 0.45;
      ctx.beginPath();
      ctx.moveTo(0, -3);
      ctx.quadraticCurveTo(cx + s * 0.5 + 0.5, cy + 0.6, tx + s + 0.4, ty + 0.6);
      ctx.stroke();
      ctx.strokeStyle = rgb(scale3(p.bark, 0.85), 1);
      ctx.lineWidth = w * 0.5;
      ctx.beginPath();
      ctx.moveTo(cx + s * 0.5, cy);
      ctx.lineTo(cx + s * 0.5 + (tx < 0 ? -4 : 4), cy - 5);
      ctx.stroke();
      if (p.branchSnowAmt > 0.02 && ty < -8) {
        ctx.strokeStyle = `rgba(244,248,255,${p.branchSnowAmt})`;
        ctx.lineWidth = w * 0.8;
        ctx.beginPath();
        ctx.moveTo(0, -4);
        ctx.quadraticCurveTo(cx + s * 0.5, cy - 1, tx + s, ty - 1);
        ctx.stroke();
        ctx.fillStyle = `rgba(244,248,255,${p.branchSnowAmt})`;
        ctx.beginPath();
        ctx.ellipse(tx + s, ty - 0.8, 2.2 + i % 2 * 0.5, 1.3, tx < 0 ? -0.5 : 0.5, 0, Math.PI * 2);
        ctx.fill();
      }
    });
    ctx.lineCap = "butt";
  }
  function canopyBlob2(ctx, x, y, r, p, alpha) {
    if (r < 0.4 || alpha < 0.02) return;
    ctx.save();
    ctx.globalAlpha = alpha;
    ctx.fillStyle = rgb(p.foliageDark, 1);
    ctx.beginPath();
    ctx.ellipse(x, y + r * 0.18, r, r * 0.9, 0, 0, Math.PI * 2);
    ctx.fill();
    const grad = ctx.createRadialGradient(x - r * 0.4, y - r * 0.4, r * 0.2, x, y, r);
    grad.addColorStop(0, rgb(p.foliageLight, 1));
    grad.addColorStop(0.55, rgb(p.foliageMid, 1));
    grad.addColorStop(1, rgb(p.foliageDark, 1));
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.ellipse(x, y, r * 0.95, r * 0.84, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.globalAlpha = alpha * 0.7;
    ctx.fillStyle = rgb(p.foliageLight, 1);
    ctx.beginPath();
    ctx.ellipse(x - r * 0.38, y - r * 0.42, r * 0.32, r * 0.26, -0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function canopy(ctx, p, sway, shimmer) {
    const d = clamp01(p.leafDensity);
    if (d < 0.03) return;
    CLUSTERS.forEach(([x, y, r], i) => {
      const threshold = i / CLUSTERS.length;
      const grow = clamp01((d - threshold * 0.55) / 0.45);
      if (grow < 0.04) return;
      const s = sway * (0.3 + Math.abs(x) / 28) + Math.sin(shimmer + i) * 0.35 * d;
      canopyBlob2(ctx, x + s, y, r * grow, p, 0.9);
    });
    const fleckAlpha = 0.5 * d;
    if (fleckAlpha > 0.04) {
      ctx.fillStyle = rgb(p.foliageLight, fleckAlpha);
      CLUSTERS.forEach(([x, y, r], i) => {
        const threshold = i / CLUSTERS.length;
        const grow = clamp01((d - threshold * 0.55) / 0.45);
        if (grow < 0.2) return;
        const s = sway * (0.3 + Math.abs(x) / 28);
        for (let k = 0; k < 2; k++) {
          const a = i * 1.7 + k * 2.3 + shimmer * 0.2;
          ctx.beginPath();
          ctx.ellipse(x + s + Math.cos(a) * r, y + Math.sin(a) * r, 1.2, 1.9, a, 0, Math.PI * 2);
          ctx.fill();
        }
      });
    }
  }
  function catkins(ctx, p, sway) {
    if (p.catkinAmt < 0.02) return;
    const anchors = [
      [-9, -16],
      [4, -18],
      [-3, -13],
      [11, -11],
      [8, -19],
      [-13, -9]
    ];
    anchors.forEach(([ax, ay], i) => {
      if (i / anchors.length > p.catkinAmt + 0.1) return;
      const dx = sway * 0.6 + Math.sin(i * 1.3) * 0.4;
      ctx.strokeStyle = `rgba(214,206,150,${0.85 * p.catkinAmt})`;
      ctx.lineWidth = 1.6;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(ax, ay);
      ctx.quadraticCurveTo(ax + dx * 0.5, ay + 3, ax + dx, ay + 6);
      ctx.stroke();
      ctx.lineCap = "butt";
      ctx.fillStyle = `rgba(238,232,180,${0.9 * p.catkinAmt})`;
      ctx.beginPath();
      ctx.ellipse(ax + dx, ay + 6, 1, 1.6, 0, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function paint(ctx, p, bob, sway = 0, shimmer = 0) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      padEllipse(ctx, p);
      ctx.save();
      ctx.translate(0, -bob);
      trunk2(ctx, p);
      branches(ctx, p, sway);
      canopy(ctx, p, sway, shimmer);
      catkins(ctx, p, sway);
      ctx.restore();
      if (p.lightStrength > 0.01) {
        const lg = ctx.createRadialGradient(-8, -16, 4, -2, -8, 34);
        lg.addColorStop(0, rgb(p.lightTint, p.lightStrength));
        lg.addColorStop(1, rgb(p.lightTint, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.ellipse(-2, -8, 26, 28, 0, 0, Math.PI * 2);
        ctx.fill();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function lerpP(a, b, f) {
    return {
      foliageDark: lerp3(a.foliageDark, b.foliageDark, f),
      foliageMid: lerp3(a.foliageMid, b.foliageMid, f),
      foliageLight: lerp3(a.foliageLight, b.foliageLight, f),
      bark: lerp3(a.bark, b.bark, f),
      barkShade: lerp3(a.barkShade, b.barkShade, f),
      lenticel: lerp3(a.lenticel, b.lenticel, f),
      padGrass: lerp3(a.padGrass, b.padGrass, f),
      soil: lerp3(a.soil, b.soil, f),
      outlineTint: lerp3(a.outlineTint, b.outlineTint, f),
      lightTint: lerp3(a.lightTint, b.lightTint, f),
      leafDensity: lerp2(a.leafDensity, b.leafDensity, f),
      catkinAmt: lerp2(a.catkinAmt, b.catkinAmt, f),
      frostAmt: lerp2(a.frostAmt, b.frostAmt, f),
      branchSnowAmt: lerp2(a.branchSnowAmt, b.branchSnowAmt, f),
      padSnowAmt: lerp2(a.padSnowAmt, b.padSnowAmt, f),
      blossomAmt: lerp2(a.blossomAmt, b.blossomAmt, f),
      fallenLeafAmt: lerp2(a.fallenLeafAmt, b.fallenLeafAmt, f),
      lightStrength: lerp2(a.lightStrength, b.lightStrength, f),
      shadowStrength: lerp2(a.shadowStrength, b.shadowStrength, f)
    };
  }
  function makeDraw(season) {
    return (ctx) => paint(ctx, SP[season], 0, 0, 0);
  }
  function animSpring(ctx, t) {
    const bob = bobAt(t, 0.5, 1.3);
    const sway = Math.sin(t * 1.4) * 1.4;
    paint(ctx, SP.Spring, bob, sway, t * 2.4);
  }
  function animSummer(ctx, t) {
    const bob = bobAt(t, 0.5, 1.2);
    const sway = Math.sin(t * 1.3) * 1.3;
    paint(ctx, SP.Summer, bob, sway, t * 1.9);
  }
  function animAutumn(ctx, t) {
    const bob = bobAt(t, 0.5, 1.1);
    const sway = Math.sin(t * 1.1) * 1.1;
    paint(ctx, SP.Autumn, bob, sway, t * 1.6);
    ctx.save();
    try {
      const p = SP.Autumn;
      const leaves = [
        [-6, 0, 0.55],
        // [startX, phase, hueBlend]
        [9, 0.5, 0.35]
      ];
      leaves.forEach(([sx, phase, hb]) => {
        const prog = ((t * 0.28 + phase) % 1 + 1) % 1;
        const ly = -8 + prog * 28;
        const lx = sx + Math.sin(prog * Math.PI * 2 + phase * 6) * 6;
        const col = lerp3(p.foliageMid, p.foliageLight, hb);
        ctx.fillStyle = rgb(col, 1 - prog * 0.4);
        ctx.translate(lx, ly);
        ctx.rotate(prog * Math.PI * 3 + phase * 4);
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.6, 2.9, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.setTransform(1, 0, 0, 1, 0, 0);
      });
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animWinter(ctx, t) {
    const bob = bobAt(t, 0.35, 0.9);
    const sway = Math.sin(t * 0.9) * 0.9;
    paint(ctx, SP.Winter, bob, sway, 0);
    ctx.save();
    try {
      const span = 36;
      const seeds = [
        [-9, 1.2, 0],
        [7, 1, 0.5],
        [13, 0.9, 0.7]
      ];
      seeds.forEach(([fx, r, phase]) => {
        const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
        const fy = -24 + prog * span;
        const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
        ctx.fillStyle = "rgba(255,255,255,0.85)";
        ctx.beginPath();
        ctx.arc(driftX, fy, r, 0, Math.PI * 2);
        ctx.fill();
      });
      const sheen2 = 0.12 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.7));
      ctx.fillStyle = `rgba(200,224,255,${sheen2})`;
      ctx.beginPath();
      ctx.ellipse(-3, 18.6, 11, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function makeTransition(from, to) {
    return (ctx, pp) => {
      const f = smoother(clamp01(pp));
      paint(ctx, lerpP(SP[from], SP[to], f), 0, 0, 0);
    };
  }
  var VARIANTS2 = {
    Spring: { draw: makeDraw("Spring"), anim: animSpring },
    Summer: { draw: makeDraw("Summer"), anim: animSummer },
    Autumn: { draw: makeDraw("Autumn"), anim: animAutumn },
    Winter: { draw: makeDraw("Winter"), anim: animWinter }
  };
  var TRANSITIONS2 = {
    0: makeTransition("Spring", "Summer"),
    1: makeTransition("Summer", "Autumn"),
    2: makeTransition("Autumn", "Winter")
  };

  // src/textures/seasonal/tree/cypress.ts
  function clamp012(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp4(a, b, f) {
    return a + (b - a) * f;
  }
  function lerp32(a, b, f) {
    return [lerp4(a[0], b[0], f), lerp4(a[1], b[1], f), lerp4(a[2], b[2], f)];
  }
  function rgb2(c, alpha = 1) {
    const r = Math.round(clamp012(c[0] / 255) * 255);
    const g = Math.round(clamp012(c[1] / 255) * 255);
    const b = Math.round(clamp012(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp012(alpha)})`;
  }
  function scale32(c, k) {
    return [c[0] * k, c[1] * k, c[2] * k];
  }
  var smoother2 = (x) => {
    const c = clamp012(x);
    return c * c * c * (c * (6 * c - 15) + 10);
  };
  function bobAt2(t, A = 0.5, w = 1.2) {
    return A * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP2 = {
    Spring: {
      foliageDark: [30, 78, 44],
      foliageMid: [52, 116, 60],
      foliageLight: [108, 168, 92],
      trunk: [120, 88, 56],
      trunkShade: [86, 60, 38],
      padGrass: [126, 196, 86],
      soil: [104, 78, 44],
      outlineTint: [22, 48, 30],
      lightTint: [214, 238, 230],
      // cool-bright dewy
      leafDensity: 0.96,
      greenTint: 1,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.2
    },
    Summer: {
      foliageDark: [18, 62, 34],
      foliageMid: [30, 96, 46],
      foliageLight: [78, 142, 70],
      trunk: [116, 84, 52],
      trunkShade: [82, 56, 34],
      padGrass: [70, 158, 64],
      soil: [96, 70, 40],
      outlineTint: [14, 40, 24],
      lightTint: [255, 244, 206],
      // warm peak
      leafDensity: 1,
      greenTint: 0.6,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.24,
      shadowStrength: 0.28
    },
    Autumn: {
      foliageDark: [54, 70, 38],
      foliageMid: [86, 104, 56],
      foliageLight: [136, 150, 90],
      trunk: [108, 78, 46],
      trunkShade: [76, 52, 30],
      padGrass: [150, 144, 86],
      soil: [110, 80, 44],
      outlineTint: [40, 46, 26],
      lightTint: [250, 220, 168],
      // low amber
      leafDensity: 0.94,
      greenTint: 0.3,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.6,
      lightStrength: 0.26,
      shadowStrength: 0.22
    },
    Winter: {
      foliageDark: [26, 60, 42],
      foliageMid: [40, 86, 54],
      foliageLight: [86, 130, 92],
      trunk: [104, 84, 64],
      trunkShade: [74, 58, 42],
      padGrass: [196, 210, 224],
      soil: [120, 126, 136],
      outlineTint: [28, 46, 40],
      lightTint: [206, 222, 240],
      // cool blue-grey
      leafDensity: 0.96,
      // evergreen — stays full under the snow
      greenTint: 0.4,
      frostAmt: 0.8,
      snowCapAmt: 0.9,
      padSnowAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.16
    }
  };
  var SPIRE_TOP = -23;
  var SPIRE_BASE = 13;
  var TIERS = [
    [11, 6.8],
    [7, 6.6],
    [3, 6.2],
    [-1, 5.6],
    [-5, 4.9],
    [-9, 4.1],
    [-13, 3.2],
    [-17, 2.3],
    [-20.5, 1.4]
  ];
  function padEllipse2(ctx, p) {
    ctx.fillStyle = `rgba(0,0,0,${0.24 * p.shadowStrength + 0.04})`;
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb2(scale32(p.padGrass, 0.6), 1);
    ctx.beginPath();
    ctx.ellipse(0, 20.4, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb2(p.padGrass, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt < 0.6) {
      ctx.fillStyle = rgb2(scale32(p.padGrass, 1.12), 1 - p.padSnowAmt);
      const tufts = 11;
      for (let i = 0; i < tufts; i++) {
        const a = i / tufts * Math.PI * 2;
        const tx = Math.cos(a) * 17;
        const ty = 19 + Math.sin(a) * 4.7;
        if (ty < 17) continue;
        ctx.beginPath();
        ctx.ellipse(tx, ty, 2, 1.3, a, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.fillStyle = rgb2(p.soil, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19.4, 6, 2.3, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.blossomAmt > 0.02) {
      const spots = [
        [-12, 20],
        [-6, 21.6],
        [8, 21],
        [13, 19.6],
        [2, 22]
      ];
      spots.forEach(([sx, sy], i) => {
        const a = p.blossomAmt * (i % 2 === 0 ? 0.9 : 0.7);
        ctx.fillStyle = `rgba(255,236,246,${clamp012(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 1.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(248,210,90,${clamp012(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 0.45, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.fallenLeafAmt > 0.02) {
      const leaves = [
        [-9, 20.4, 0.5],
        [7, 21.4, -0.4],
        [-1, 22, 0.2],
        [12, 20, 0.8]
      ];
      leaves.forEach(([lx, ly, rot], i) => {
        if (i / leaves.length > p.fallenLeafAmt + 0.05) return;
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb2([196, 120, 40], clamp012(p.fallenLeafAmt + 0.3));
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.4, 1.3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb2(p.outlineTint, 0.4);
        ctx.lineWidth = 0.5;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.padSnowAmt > 0.02) {
      const snow = ctx.createLinearGradient(0, 15, 0, 23);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c6d6e6");
      ctx.save();
      ctx.globalAlpha = clamp012(p.padSnowAmt);
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = `rgba(255,255,255,${clamp012(0.5 * p.frostAmt + 0.3)})`;
        const sparkle = [
          [-10, 18],
          [-4, 20],
          [3, 19],
          [9, 20.5],
          [13, 18.5],
          [-13, 19.5]
        ];
        sparkle.forEach(([sx, sy], i) => {
          if (i / sparkle.length > p.frostAmt) return;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
    }
  }
  function trunkBase(ctx, p) {
    ctx.fillStyle = rgb2(p.outlineTint, 0.9);
    ctx.beginPath();
    ctx.moveTo(-3.2, 19);
    ctx.lineTo(-2.4, 11);
    ctx.lineTo(2.4, 11);
    ctx.lineTo(3.2, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb2(p.trunkShade, 1);
    ctx.beginPath();
    ctx.moveTo(-2.5, 19);
    ctx.lineTo(-1.9, 11.4);
    ctx.lineTo(2.5, 11.4);
    ctx.lineTo(2.5, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb2(p.trunk, 1);
    ctx.beginPath();
    ctx.moveTo(-2.5, 19);
    ctx.lineTo(-1.9, 11.4);
    ctx.lineTo(0.6, 11.4);
    ctx.lineTo(0.2, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb2(scale32(p.trunk, 0.86), 1);
    ctx.beginPath();
    ctx.moveTo(-2.5, 19);
    ctx.quadraticCurveTo(-5.5, 18.6, -7, 19.6);
    ctx.quadraticCurveTo(-4, 19.4, -2, 18.6);
    ctx.closePath();
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(2.5, 19);
    ctx.quadraticCurveTo(5.5, 18.6, 7, 19.6);
    ctx.quadraticCurveTo(4, 19.4, 2, 18.6);
    ctx.closePath();
    ctx.fill();
  }
  function foliageTier(ctx, cy, hw, p, sway) {
    if (hw < 0.4) return;
    const s = sway * (0.4 + (SPIRE_BASE - cy) / 40);
    ctx.fillStyle = rgb2(p.foliageDark, 1);
    ctx.beginPath();
    ctx.moveTo(-hw + s, cy + 1.4);
    ctx.quadraticCurveTo(s, cy + 4, hw + s, cy + 1.4);
    ctx.quadraticCurveTo(hw * 0.5 + s, cy + 2.2, s, cy + 1);
    ctx.quadraticCurveTo(-hw * 0.5 + s, cy + 2.2, -hw + s, cy + 1.4);
    ctx.closePath();
    ctx.fill();
    const grad = ctx.createLinearGradient(-hw + s, cy - 3, hw + s, cy + 2);
    grad.addColorStop(0, rgb2(p.foliageLight, 1));
    grad.addColorStop(0.5, rgb2(p.foliageMid, 1));
    grad.addColorStop(1, rgb2(p.foliageDark, 1));
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.moveTo(-hw + s, cy + 1.2);
    ctx.quadraticCurveTo(-hw * 0.7 + s, cy - 3.4, s, cy - 3);
    ctx.quadraticCurveTo(hw * 0.7 + s, cy - 3.4, hw + s, cy + 1.2);
    ctx.quadraticCurveTo(hw * 0.5 + s, cy + 2.4, s, cy + 2);
    ctx.quadraticCurveTo(-hw * 0.5 + s, cy + 2.4, -hw + s, cy + 1.2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb2(p.foliageLight, 0.75);
    ctx.beginPath();
    ctx.ellipse(-hw * 0.42 + s, cy - 1.6, hw * 0.34, 1.3, -0.4, 0, Math.PI * 2);
    ctx.fill();
  }
  function spire(ctx, p, sway, shimmer) {
    const d = clamp012(p.leafDensity);
    TIERS.forEach(([cy, hw], i) => {
      const w = hw * (0.86 + 0.14 * d);
      const sh = Math.sin(shimmer + i * 0.8) * 0.3 * d;
      foliageTier(ctx, cy, w, p, sway + sh);
    });
    ctx.fillStyle = rgb2(p.foliageLight, 0.4 * d);
    TIERS.forEach(([cy, hw], i) => {
      if (i % 2 === 1) return;
      const s = sway * 0.5;
      ctx.beginPath();
      ctx.ellipse(-hw * 0.6 + s, cy - 1, 1, 1.8, -0.5, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function snowCaps(ctx, p, sway) {
    if (p.snowCapAmt < 0.02) return;
    const a = clamp012(p.snowCapAmt);
    TIERS.forEach(([cy, hw], i) => {
      if (i % 2 === 1) return;
      const s = sway * (0.4 + (SPIRE_BASE - cy) / 40);
      ctx.fillStyle = `rgba(244,248,255,${a})`;
      ctx.beginPath();
      ctx.moveTo(-hw * 0.7 + s, cy - 1.4);
      ctx.quadraticCurveTo(-hw * 0.4 + s, cy - 3.4, s, cy - 3);
      ctx.quadraticCurveTo(hw * 0.4 + s, cy - 3.4, hw * 0.7 + s, cy - 1.4);
      ctx.quadraticCurveTo(hw * 0.3 + s, cy - 2.2, s, cy - 1.9);
      ctx.quadraticCurveTo(-hw * 0.3 + s, cy - 2.2, -hw * 0.7 + s, cy - 1.4);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = `rgba(206,222,240,${a * 0.5})`;
      ctx.beginPath();
      ctx.ellipse(s, cy - 1.5, hw * 0.5, 0.9, 0, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.fillStyle = `rgba(248,251,255,${a})`;
    ctx.beginPath();
    ctx.ellipse(sway * 0.7, SPIRE_TOP + 0.6, 1.8, 1.4, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function paint2(ctx, p, bob, sway = 0, shimmer = 0) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      padEllipse2(ctx, p);
      ctx.save();
      ctx.translate(0, -bob);
      trunkBase(ctx, p);
      spire(ctx, p, sway, shimmer);
      snowCaps(ctx, p, sway);
      ctx.restore();
      if (p.lightStrength > 0.01) {
        const lg = ctx.createRadialGradient(-7, -16, 4, -2, -8, 34);
        lg.addColorStop(0, rgb2(p.lightTint, p.lightStrength));
        lg.addColorStop(1, rgb2(p.lightTint, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.ellipse(-2, -6, 24, 30, 0, 0, Math.PI * 2);
        ctx.fill();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function lerpP2(a, b, f) {
    return {
      foliageDark: lerp32(a.foliageDark, b.foliageDark, f),
      foliageMid: lerp32(a.foliageMid, b.foliageMid, f),
      foliageLight: lerp32(a.foliageLight, b.foliageLight, f),
      trunk: lerp32(a.trunk, b.trunk, f),
      trunkShade: lerp32(a.trunkShade, b.trunkShade, f),
      padGrass: lerp32(a.padGrass, b.padGrass, f),
      soil: lerp32(a.soil, b.soil, f),
      outlineTint: lerp32(a.outlineTint, b.outlineTint, f),
      lightTint: lerp32(a.lightTint, b.lightTint, f),
      leafDensity: lerp4(a.leafDensity, b.leafDensity, f),
      greenTint: lerp4(a.greenTint, b.greenTint, f),
      frostAmt: lerp4(a.frostAmt, b.frostAmt, f),
      snowCapAmt: lerp4(a.snowCapAmt, b.snowCapAmt, f),
      padSnowAmt: lerp4(a.padSnowAmt, b.padSnowAmt, f),
      blossomAmt: lerp4(a.blossomAmt, b.blossomAmt, f),
      fallenLeafAmt: lerp4(a.fallenLeafAmt, b.fallenLeafAmt, f),
      lightStrength: lerp4(a.lightStrength, b.lightStrength, f),
      shadowStrength: lerp4(a.shadowStrength, b.shadowStrength, f)
    };
  }
  function makeDraw2(season) {
    return (ctx) => paint2(ctx, SP2[season], 0, 0, 0);
  }
  function animSpring2(ctx, t) {
    const bob = bobAt2(t, 0.45, 1.3);
    const sway = Math.sin(t * 1.4) * 1;
    paint2(ctx, SP2.Spring, bob, sway, t * 2.2);
    ctx.save();
    try {
      const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
      const gy = -4 - bob + Math.sin(t * 1) * 4;
      ctx.fillStyle = `rgba(255,255,255,${clamp012(g)})`;
      ctx.beginPath();
      ctx.arc(-3.2, gy, 1 + g * 0.7, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animSummer2(ctx, t) {
    const bob = bobAt2(t, 0.5, 1.2);
    const sway = Math.sin(t * 1.2) * 0.9;
    paint2(ctx, SP2.Summer, bob, sway, t * 2);
    ctx.save();
    try {
      const spots = [
        [-3, -2, 0],
        [3, -10, 1.4],
        [-2, -16, 2.6]
      ];
      spots.forEach(([sx, sy, ph]) => {
        const g = 0.1 + 0.16 * (0.5 + 0.5 * Math.sin(t * 3 + ph));
        ctx.fillStyle = `rgba(220,250,210,${clamp012(g)})`;
        ctx.beginPath();
        ctx.arc(sx, sy - bob, 0.9, 0, Math.PI * 2);
        ctx.fill();
      });
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animAutumn2(ctx, t) {
    const bob = bobAt2(t, 0.4, 1);
    const sway = Math.sin(t * 0.9) * 0.8;
    paint2(ctx, SP2.Autumn, bob, sway, t * 1.4);
    ctx.save();
    try {
      const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
      ctx.fillStyle = `rgba(250,224,170,${clamp012(s)})`;
      ctx.beginPath();
      ctx.ellipse(-2, -6 - bob, 5, 8, -0.1, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animWinter2(ctx, t) {
    const bob = bobAt2(t, 0.32, 0.9);
    const sway = Math.sin(t * 0.8) * 0.7;
    paint2(ctx, SP2.Winter, bob, sway, 0);
    ctx.save();
    try {
      const span = 36;
      const seeds = [
        [-7, 1.2, 0],
        [6, 1, 0.5],
        [10, 0.9, 0.75]
      ];
      seeds.forEach(([fx, r, phase]) => {
        const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
        const fy = -24 + prog * span;
        const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
        ctx.fillStyle = "rgba(255,255,255,0.85)";
        ctx.beginPath();
        ctx.arc(driftX, fy, r, 0, Math.PI * 2);
        ctx.fill();
      });
      const sheen2 = 0.12 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.7));
      ctx.fillStyle = `rgba(200,224,255,${clamp012(sheen2)})`;
      ctx.beginPath();
      ctx.ellipse(-3, 18.6, 11, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function makeTransition2(from, to) {
    return (ctx, pp) => {
      const f = smoother2(clamp012(pp));
      paint2(ctx, lerpP2(SP2[from], SP2[to], f), 0, 0, 0);
    };
  }
  var VARIANTS3 = {
    Spring: { draw: makeDraw2("Spring"), anim: animSpring2 },
    Summer: { draw: makeDraw2("Summer"), anim: animSummer2 },
    Autumn: { draw: makeDraw2("Autumn"), anim: animAutumn2 },
    Winter: { draw: makeDraw2("Winter"), anim: animWinter2 }
  };
  var TRANSITIONS3 = {
    0: makeTransition2("Spring", "Summer"),
    1: makeTransition2("Summer", "Autumn"),
    2: makeTransition2("Autumn", "Winter")
  };

  // src/textures/seasonal/tree/fir.ts
  function clamp013(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp5(a, b, f) {
    return a + (b - a) * f;
  }
  function lerp33(a, b, f) {
    return [lerp5(a[0], b[0], f), lerp5(a[1], b[1], f), lerp5(a[2], b[2], f)];
  }
  function scale33(c, k) {
    return [c[0] * k, c[1] * k, c[2] * k];
  }
  function rgb3(c, alpha = 1) {
    const r = Math.round(clamp013(c[0] / 255) * 255);
    const g = Math.round(clamp013(c[1] / 255) * 255);
    const b = Math.round(clamp013(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp013(alpha)})`;
  }
  var smoother3 = (x) => {
    const c = clamp013(x);
    return c * c * c * (c * (6 * c - 15) + 10);
  };
  function bobAt3(t, A = 0.55, w = 1.2) {
    return A * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP3 = {
    // Spring — bright new needle tips at branch ends; dewy lime pad + blossom.
    Spring: {
      needleDark: [28, 74, 44],
      needleMid: [44, 108, 60],
      needleLight: [98, 156, 84],
      tipHighlight: [176, 214, 120],
      // fresh lime new growth at the tips
      trunk: [118, 84, 50],
      trunkShade: [86, 60, 36],
      padGrass: [126, 200, 88],
      soil: [104, 78, 44],
      outlineTint: [22, 46, 30],
      lightTint: [214, 238, 230],
      // cool-bright
      leafDensity: 1,
      tipFreshAmt: 0.9,
      coneAmt: 0,
      frostAmt: 0,
      snowLoadAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.2
    },
    // Summer — PEAK: deep saturated evergreen green; warm light, strong shadow.
    Summer: {
      needleDark: [20, 66, 38],
      needleMid: [30, 98, 50],
      needleLight: [78, 146, 70],
      tipHighlight: [104, 166, 84],
      trunk: [110, 78, 46],
      trunkShade: [78, 54, 32],
      padGrass: [70, 158, 64],
      soil: [96, 70, 40],
      outlineTint: [16, 44, 26],
      lightTint: [255, 244, 206],
      // warm
      leafDensity: 1,
      tipFreshAmt: 0,
      coneAmt: 0,
      frostAmt: 0,
      snowLoadAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.24,
      shadowStrength: 0.28
    },
    // Autumn — dustier, slightly olive evergreen green; a couple of small brown
    // cones; olive-tan pad with a few fallen leaves.
    Autumn: {
      needleDark: [40, 70, 44],
      needleMid: [62, 100, 56],
      needleLight: [108, 144, 80],
      tipHighlight: [122, 144, 82],
      trunk: [108, 76, 44],
      trunkShade: [78, 54, 30],
      padGrass: [150, 144, 86],
      soil: [110, 80, 44],
      outlineTint: [34, 50, 30],
      lightTint: [250, 220, 168],
      // low amber
      leafDensity: 1,
      tipFreshAmt: 0,
      coneAmt: 0.85,
      frostAmt: 0,
      snowLoadAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.6,
      lightStrength: 0.26,
      shadowStrength: 0.22
    },
    // Winter — boughs HEAVY WITH SNOW; cool blue-grey light. Green still shows
    // beneath the thick snow load. Snowy pad + frost. NO white-out.
    Winter: {
      needleDark: [34, 64, 50],
      needleMid: [46, 88, 60],
      needleLight: [86, 124, 92],
      tipHighlight: [120, 150, 120],
      trunk: [98, 74, 52],
      trunkShade: [70, 52, 38],
      padGrass: [196, 210, 224],
      soil: [120, 126, 136],
      outlineTint: [30, 46, 40],
      lightTint: [206, 222, 240],
      // cool blue-grey
      leafDensity: 1,
      tipFreshAmt: 0,
      coneAmt: 0,
      frostAmt: 0.8,
      snowLoadAmt: 0.92,
      // thick snow resting on the tiers
      padSnowAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.16
    }
  };
  var TIERS2 = [
    [13.5, 16, 6],
    // bottom (widest) skirt
    [6, 12.6, 5.4],
    [-1, 9.4, 4.8],
    [-8.5, 6.2, 4.2]
    // upper skirt; apex spike rises above this
  ];
  var APEX_Y = -23.5;
  var TRUNK_TOP = 15.5;
  var TRUNK_BOT = 19;
  function padEllipse3(ctx, p) {
    ctx.fillStyle = `rgba(0,0,0,${0.24 * p.shadowStrength + 0.04})`;
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb3(scale33(p.padGrass, 0.6), 1);
    ctx.beginPath();
    ctx.ellipse(0, 20.4, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb3(p.padGrass, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt < 0.6) {
      ctx.fillStyle = rgb3(scale33(p.padGrass, 1.12), 1 - p.padSnowAmt);
      const tufts = 11;
      for (let i = 0; i < tufts; i++) {
        const a = i / tufts * Math.PI * 2;
        const tx = Math.cos(a) * 17;
        const ty = 19 + Math.sin(a) * 4.7;
        if (ty < 17) continue;
        ctx.beginPath();
        ctx.ellipse(tx, ty, 2, 1.3, a, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.fillStyle = rgb3(p.soil, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19.4, 6.5, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.blossomAmt > 0.02) {
      const spots = [
        [-12, 20],
        [-6, 21.6],
        [9, 21],
        [13, 19.6],
        [2, 22],
        [-2, 19.6]
      ];
      spots.forEach(([sx, sy], i) => {
        const a = p.blossomAmt * (i % 2 === 0 ? 0.9 : 0.7);
        ctx.fillStyle = `rgba(255,236,246,${clamp013(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 1.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(248,210,90,${clamp013(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 0.45, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.fallenLeafAmt > 0.02) {
      const leaves = [
        [-9, 20.4, 0.5, [196, 120, 40]],
        [7, 21.4, -0.4, [176, 72, 32]],
        [-1, 22, 0.2, [182, 132, 46]]
      ];
      leaves.forEach(([lx, ly, rot, col], i) => {
        if (i / leaves.length > p.fallenLeafAmt + 0.05) return;
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb3(col, Math.min(1, p.fallenLeafAmt + 0.3));
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb3(p.outlineTint, 0.4);
        ctx.lineWidth = 0.5;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.padSnowAmt > 0.02) {
      const snow = ctx.createLinearGradient(0, 15, 0, 23);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c6d6e6");
      ctx.save();
      ctx.globalAlpha = clamp013(p.padSnowAmt);
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = `rgba(255,255,255,${clamp013(0.5 * p.frostAmt + 0.3)})`;
        const sparkle = [
          [-10, 18],
          [-4, 20],
          [3, 19],
          [9, 20.5],
          [13, 18.5],
          [-13, 19.5]
        ];
        sparkle.forEach(([sx, sy], i) => {
          if (i / sparkle.length > p.frostAmt) return;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
    }
  }
  function trunk3(ctx, p) {
    ctx.fillStyle = rgb3(p.outlineTint, 0.9);
    ctx.beginPath();
    ctx.moveTo(-3.2, TRUNK_BOT);
    ctx.lineTo(-2.4, TRUNK_TOP);
    ctx.lineTo(2.4, TRUNK_TOP);
    ctx.lineTo(3.2, TRUNK_BOT);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb3(p.trunkShade, 1);
    ctx.beginPath();
    ctx.moveTo(-2.4, TRUNK_BOT);
    ctx.lineTo(-1.8, TRUNK_TOP);
    ctx.lineTo(2, TRUNK_TOP);
    ctx.lineTo(2.6, TRUNK_BOT);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb3(p.trunk, 1);
    ctx.beginPath();
    ctx.moveTo(-2.4, TRUNK_BOT);
    ctx.lineTo(-1.8, TRUNK_TOP);
    ctx.lineTo(0.3, TRUNK_TOP);
    ctx.lineTo(-0.2, TRUNK_BOT);
    ctx.closePath();
    ctx.fill();
  }
  function needleTier(ctx, cy, half, depth, p, sway) {
    const topY = cy - depth * 0.7;
    const botY = cy + depth;
    const s = sway * (0.4 + half / 18);
    const tierPath = (insetW, insetD) => {
      const h = Math.max(0, half - insetW);
      const d = Math.max(0, depth - insetD);
      ctx.beginPath();
      ctx.moveTo(-h + s, topY + insetD * 0.5);
      const pts = 5;
      for (let i = 0; i <= pts; i++) {
        const f = i / pts;
        const x = lerp5(-h, h, f) + s * (1 - Math.abs(f - 0.5) * 0.6);
        const dip = botY + insetD * 0.4 - Math.sin(f * Math.PI) * d * 0.25;
        const pointY = i % 1 === 0 ? dip : dip;
        ctx.quadraticCurveTo(
          lerp5(-h, h, (i - 0.5) / pts) + s,
          cy + d * 0.9,
          x,
          pointY
        );
      }
      ctx.lineTo(h + s, topY + insetD * 0.5);
      ctx.quadraticCurveTo(s, topY - d * 0.5 + insetD * 0.5, -h + s, topY + insetD * 0.5);
      ctx.closePath();
    };
    ctx.fillStyle = rgb3(p.needleDark, 1);
    tierPath(0, 0);
    ctx.fill();
    ctx.fillStyle = rgb3(p.needleMid, 1);
    tierPath(1.4, 1.2);
    ctx.fill();
    ctx.save();
    tierPath(1.4, 1.2);
    ctx.clip();
    const lg = ctx.createLinearGradient(-half + s, topY - depth, half + s, botY);
    lg.addColorStop(0, rgb3(p.needleLight, 1));
    lg.addColorStop(0.45, rgb3(p.needleMid, 1));
    lg.addColorStop(1, rgb3(p.needleDark, 0));
    ctx.fillStyle = lg;
    ctx.globalAlpha = 0.85;
    ctx.beginPath();
    ctx.ellipse(-half * 0.3 + s, cy - depth * 0.2, half * 0.9, depth * 1.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.strokeStyle = rgb3(p.needleDark, 0.85);
    ctx.lineWidth = 1;
    ctx.lineCap = "round";
    const rim = 7;
    for (let i = 0; i <= rim; i++) {
      const f = i / rim;
      const x = lerp5(-half + 1, half - 1, f) + s;
      const y0 = cy + depth * (0.35 + Math.sin(f * Math.PI) * 0.18);
      const droop = botY - Math.sin(f * Math.PI) * depth * 0.2;
      ctx.beginPath();
      ctx.moveTo(x, y0);
      ctx.lineTo(x - s * 0.1, droop);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    if (p.tipFreshAmt > 0.02) {
      ctx.fillStyle = rgb3(p.tipHighlight, 0.9 * p.tipFreshAmt);
      for (let i = 0; i <= rim; i++) {
        const f = i / rim;
        const x = lerp5(-half + 1, half - 1, f) + s;
        const droop = botY - Math.sin(f * Math.PI) * depth * 0.2;
        ctx.beginPath();
        ctx.arc(x - s * 0.1, droop, 1, 0, Math.PI * 2);
        ctx.fill();
      }
    }
  }
  function cone(ctx, x, y, p, a) {
    ctx.save();
    ctx.globalAlpha = clamp013(a);
    ctx.fillStyle = rgb3([110, 72, 36], 1);
    ctx.beginPath();
    ctx.moveTo(x, y - 3);
    ctx.quadraticCurveTo(x + 2.2, y - 1, x + 1.6, y + 2.4);
    ctx.quadraticCurveTo(x, y + 3.4, x - 1.6, y + 2.4);
    ctx.quadraticCurveTo(x - 2.2, y - 1, x, y - 3);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb3([158, 112, 64], 0.9);
    for (let r = 0; r < 3; r++) {
      ctx.beginPath();
      ctx.ellipse(x, y - 1.6 + r * 1.6, 1.3 - r * 0.1, 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  }
  function tierSnow(ctx, cy, half, depth, p, sway) {
    if (p.snowLoadAmt < 0.02) return;
    const a = p.snowLoadAmt;
    const s = sway * (0.4 + half / 18);
    const topY = cy - depth * 0.7;
    const h = half - 1.2;
    ctx.save();
    ctx.fillStyle = `rgba(246,251,255,${clamp013(0.95 * a)})`;
    ctx.beginPath();
    ctx.moveTo(-h + s, topY + 0.5);
    ctx.quadraticCurveTo(s, topY - depth * 0.45, h + s, topY + 0.5);
    const lumps = 4;
    for (let i = 0; i <= lumps; i++) {
      const f = 1 - i / lumps;
      const x = lerp5(-h, h, f) + s;
      const lumpY = topY + 2.6 + Math.sin(f * Math.PI) * depth * 0.5 * a;
      ctx.quadraticCurveTo(x + 1.4, lumpY + 1.2, x, lumpY);
    }
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = `rgba(205,222,242,${clamp013(0.5 * a)})`;
    ctx.beginPath();
    ctx.ellipse(s, topY + 1.4, h * 0.7, 1.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint3(ctx, p, bob, sway = 0) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      padEllipse3(ctx, p);
      ctx.save();
      ctx.translate(0, -bob);
      trunk3(ctx, p);
      const apexS = sway * 0.9;
      ctx.fillStyle = rgb3(p.needleDark, 1);
      ctx.beginPath();
      ctx.moveTo(-6.2, -8.5);
      ctx.quadraticCurveTo(-2 + apexS, APEX_Y + 4, apexS, APEX_Y);
      ctx.quadraticCurveTo(2 + apexS, APEX_Y + 4, 6.2, -8.5);
      ctx.quadraticCurveTo(0, -6, -6.2, -8.5);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb3(p.needleMid, 1);
      ctx.beginPath();
      ctx.moveTo(-4.6, -9);
      ctx.quadraticCurveTo(-1.4 + apexS, APEX_Y + 4.5, apexS, APEX_Y + 1);
      ctx.quadraticCurveTo(1.4 + apexS, APEX_Y + 4.5, 4.6, -9);
      ctx.quadraticCurveTo(0, -7, -4.6, -9);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb3(p.needleLight, 0.75);
      ctx.beginPath();
      ctx.moveTo(-3.6, -9.2);
      ctx.quadraticCurveTo(-1.2 + apexS, APEX_Y + 6, apexS - 0.6, APEX_Y + 2);
      ctx.quadraticCurveTo(-1, -12, -3.6, -9.2);
      ctx.closePath();
      ctx.fill();
      TIERS2.forEach(([cy, half, depth]) => {
        needleTier(ctx, cy, half, depth, p, sway);
      });
      if (p.coneAmt > 0.02) {
        cone(ctx, 6.5, 2, p, p.coneAmt);
        cone(ctx, -5.5, -3.5, p, p.coneAmt * 0.9);
      }
      if (p.snowLoadAmt > 0.02) {
        TIERS2.forEach(([cy, half, depth]) => {
          tierSnow(ctx, cy, half, depth, p, sway);
        });
        const a = p.snowLoadAmt;
        ctx.fillStyle = `rgba(246,251,255,${clamp013(0.95 * a)})`;
        ctx.beginPath();
        ctx.moveTo(-3.4, APEX_Y + 6);
        ctx.quadraticCurveTo(apexS, APEX_Y - 1.5, 3.4, APEX_Y + 6);
        ctx.quadraticCurveTo(apexS, APEX_Y + 4, -3.4, APEX_Y + 6);
        ctx.closePath();
        ctx.fill();
      }
      ctx.restore();
      if (p.lightStrength > 0.01) {
        const lg = ctx.createRadialGradient(-8, -16, 4, -2, -6, 36);
        lg.addColorStop(0, rgb3(p.lightTint, p.lightStrength));
        lg.addColorStop(1, rgb3(p.lightTint, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.ellipse(-2, -6, 26, 30, 0, 0, Math.PI * 2);
        ctx.fill();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function lerpP3(a, b, f) {
    return {
      needleDark: lerp33(a.needleDark, b.needleDark, f),
      needleMid: lerp33(a.needleMid, b.needleMid, f),
      needleLight: lerp33(a.needleLight, b.needleLight, f),
      tipHighlight: lerp33(a.tipHighlight, b.tipHighlight, f),
      trunk: lerp33(a.trunk, b.trunk, f),
      trunkShade: lerp33(a.trunkShade, b.trunkShade, f),
      padGrass: lerp33(a.padGrass, b.padGrass, f),
      soil: lerp33(a.soil, b.soil, f),
      outlineTint: lerp33(a.outlineTint, b.outlineTint, f),
      lightTint: lerp33(a.lightTint, b.lightTint, f),
      leafDensity: lerp5(a.leafDensity, b.leafDensity, f),
      tipFreshAmt: lerp5(a.tipFreshAmt, b.tipFreshAmt, f),
      coneAmt: lerp5(a.coneAmt, b.coneAmt, f),
      frostAmt: lerp5(a.frostAmt, b.frostAmt, f),
      snowLoadAmt: lerp5(a.snowLoadAmt, b.snowLoadAmt, f),
      padSnowAmt: lerp5(a.padSnowAmt, b.padSnowAmt, f),
      blossomAmt: lerp5(a.blossomAmt, b.blossomAmt, f),
      fallenLeafAmt: lerp5(a.fallenLeafAmt, b.fallenLeafAmt, f),
      lightStrength: lerp5(a.lightStrength, b.lightStrength, f),
      shadowStrength: lerp5(a.shadowStrength, b.shadowStrength, f)
    };
  }
  function makeDraw3(season) {
    return (ctx) => paint3(ctx, SP3[season], 0, 0);
  }
  function animSpring3(ctx, t) {
    const bob = bobAt3(t, 0.5, 1.3);
    const sway = Math.sin(t * 1.3) * 1;
    paint3(ctx, SP3.Spring, bob, sway);
    ctx.save();
    try {
      const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
      ctx.fillStyle = `rgba(255,255,255,${clamp013(g)})`;
      const spots = [[-7, 3], [6, -2], [-3, 9]];
      spots.forEach(([sx, sy], i) => {
        const gy = sy - bob + Math.sin(t * 1.1 + i) * 0.8;
        ctx.beginPath();
        ctx.arc(sx + sway * 0.5, gy, 0.9 + g * 0.6, 0, Math.PI * 2);
        ctx.fill();
      });
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animSummer3(ctx, t) {
    const bob = bobAt3(t, 0.5, 1.2);
    const sway = Math.sin(t * 1.25) * 1.1;
    paint3(ctx, SP3.Summer, bob, sway);
    ctx.save();
    try {
      const prog = t * 0.35 % 1;
      const sy = -16 + prog * 28;
      const s = 0.1 + 0.14 * Math.sin(prog * Math.PI);
      ctx.fillStyle = `rgba(206,236,170,${clamp013(s)})`;
      ctx.beginPath();
      ctx.ellipse(-3 + sway * 0.5, sy - bob, 7, 3.2, -0.15, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animAutumn3(ctx, t) {
    const bob = bobAt3(t, 0.45, 1);
    const sway = Math.sin(t * 0.95) * 0.8;
    paint3(ctx, SP3.Autumn, bob, sway);
  }
  function animWinter3(ctx, t) {
    const bob = bobAt3(t, 0.32, 0.9);
    const sway = Math.sin(t * 0.85) * 0.6;
    paint3(ctx, SP3.Winter, bob, sway);
    ctx.save();
    try {
      const span = 36;
      const seeds = [
        [-9, 1.2, 0],
        [7, 1, 0.5],
        [13, 0.9, 0.7],
        [-2, 1.1, 0.25]
      ];
      seeds.forEach(([fx, r, phase]) => {
        const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
        const fy = -24 + prog * span;
        const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
        ctx.fillStyle = `rgba(255,255,255,${clamp013(0.4 + 0.45 * Math.sin(prog * Math.PI))})`;
        ctx.beginPath();
        ctx.arc(driftX, fy, r, 0, Math.PI * 2);
        ctx.fill();
      });
      const sheen2 = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.7));
      ctx.fillStyle = `rgba(200,224,255,${clamp013(sheen2)})`;
      ctx.beginPath();
      ctx.ellipse(-3, 18.6, 11, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function makeTransition3(from, to) {
    return (ctx, pp) => {
      const f = smoother3(clamp013(pp));
      paint3(ctx, lerpP3(SP3[from], SP3[to], f), 0, 0);
    };
  }
  var VARIANTS4 = {
    Spring: { draw: makeDraw3("Spring"), anim: animSpring3 },
    Summer: { draw: makeDraw3("Summer"), anim: animSummer3 },
    Autumn: { draw: makeDraw3("Autumn"), anim: animAutumn3 },
    Winter: { draw: makeDraw3("Winter"), anim: animWinter3 }
  };
  var TRANSITIONS4 = {
    0: makeTransition3("Spring", "Summer"),
    1: makeTransition3("Summer", "Autumn"),
    2: makeTransition3("Autumn", "Winter")
  };

  // src/textures/seasonal/types.ts
  var SEASON_NAMES = ["Spring", "Summer", "Autumn", "Winter"];

  // src/textures/seasonal/tree/palm.ts
  function clamp014(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother4 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp6(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB(a, b, t) {
    return [lerp6(a[0], b[0], t), lerp6(a[1], b[1], t), lerp6(a[2], b[2], t)];
  }
  function rgb4(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp014(a)})`;
  }
  function lerpP4(a, b, t) {
    return {
      frondLight: lerpRGB(a.frondLight, b.frondLight, t),
      frondMid: lerpRGB(a.frondMid, b.frondMid, t),
      frondDark: lerpRGB(a.frondDark, b.frondDark, t),
      frondTip: lerpRGB(a.frondTip, b.frondTip, t),
      trunkLight: lerpRGB(a.trunkLight, b.trunkLight, t),
      trunkDark: lerpRGB(a.trunkDark, b.trunkDark, t),
      coconut: lerpRGB(a.coconut, b.coconut, t),
      padGrass: lerpRGB(a.padGrass, b.padGrass, t),
      padDark: lerpRGB(a.padDark, b.padDark, t),
      soil: lerpRGB(a.soil, b.soil, t),
      outline: lerpRGB(a.outline, b.outline, t),
      light: lerpRGB(a.light, b.light, t),
      lightAmt: lerp6(a.lightAmt, b.lightAmt, t),
      vigor: lerp6(a.vigor, b.vigor, t),
      gloss: lerp6(a.gloss, b.gloss, t),
      tipBrown: lerp6(a.tipBrown, b.tipBrown, t),
      droop: lerp6(a.droop, b.droop, t),
      frostAmt: lerp6(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp6(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp6(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp6(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp6(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP(p) {
    return {
      ...p,
      lightAmt: clamp014(p.lightAmt),
      vigor: clamp014(p.vigor),
      gloss: clamp014(p.gloss),
      tipBrown: clamp014(p.tipBrown),
      droop: clamp014(p.droop),
      frostAmt: clamp014(p.frostAmt),
      snowCapAmt: clamp014(p.snowCapAmt),
      padSnowAmt: clamp014(p.padSnowAmt),
      blossomAmt: clamp014(p.blossomAmt),
      fallenLeafAmt: clamp014(p.fallenLeafAmt)
    };
  }
  var SP4 = {
    // Spring — fresh dewy lime-green fronds; bright lime dewy pad + blossom.
    Spring: {
      frondLight: [168, 224, 104],
      frondMid: [104, 182, 74],
      frondDark: [56, 122, 52],
      frondTip: [150, 210, 96],
      trunkLight: [156, 116, 72],
      trunkDark: [96, 66, 38],
      coconut: [118, 84, 48],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [40, 60, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      vigor: 0.6,
      gloss: 0.28,
      tipBrown: 0,
      droop: 0.08,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — lush vivid PEAK green fronds; saturated mid-green pad; warm light.
    Summer: {
      frondLight: [142, 214, 78],
      frondMid: [70, 162, 56],
      frondDark: [34, 104, 42],
      frondTip: [108, 190, 64],
      trunkLight: [164, 120, 72],
      trunkDark: [100, 66, 36],
      coconut: [110, 76, 42],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [28, 58, 26],
      light: [255, 240, 206],
      lightAmt: 0.18,
      vigor: 1,
      gloss: 0.7,
      tipBrown: 0,
      droop: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fronds still green but the LOWER tips brown (olive-tan); rust pad.
    Autumn: {
      frondLight: [134, 184, 80],
      frondMid: [86, 144, 58],
      frondDark: [52, 96, 40],
      frondTip: [176, 128, 56],
      // olive-tan the lower tips fade toward
      trunkLight: [150, 108, 64],
      trunkDark: [92, 60, 32],
      coconut: [104, 70, 38],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [46, 50, 26],
      light: [248, 210, 150],
      lightAmt: 0.2,
      vigor: 0.7,
      gloss: 0.4,
      tipBrown: 0.85,
      droop: 0.55,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — fronds STILL GREEN under a light snow dusting + frost; snowy pad,
    // cool light. No white-out: the green stays clearly visible underneath.
    Winter: {
      frondLight: [122, 168, 96],
      frondMid: [74, 132, 70],
      frondDark: [44, 92, 58],
      frondTip: [108, 156, 96],
      trunkLight: [140, 112, 84],
      trunkDark: [88, 64, 46],
      coconut: [104, 80, 58],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [44, 50, 46],
      light: [206, 226, 252],
      lightAmt: 0.3,
      vigor: 0.5,
      gloss: 0.22,
      tipBrown: 0.1,
      droop: 0.35,
      frostAmt: 0.7,
      snowCapAmt: 0.7,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var TRUNK_BASE_X = 0;
  var TRUNK_BASE_Y = 18;
  var TRUNK_TOP_X = 6;
  var TRUNK_TOP_Y = -10;
  var TRUNK_CTRL_X = -2;
  var TRUNK_CTRL_Y = 4;
  var TRUNK_HALF = 3.4;
  var SEGMENTS = 6;
  var FRONDS = [
    { ang: -1.32, len: 21, lower: 1 },
    // far left, sweeps down-left (lower)
    { ang: -0.86, len: 23, lower: 0.45 },
    // upper-left
    { ang: -0.34, len: 20, lower: 0 },
    // top-left
    { ang: 0.16, len: 20, lower: 0 },
    // top-right
    { ang: 0.74, len: 23, lower: 0.45 },
    // upper-right
    { ang: 1.3, len: 21, lower: 1 }
    // far right, sweeps down-right (lower)
  ];
  function trunkPath(ctx, bob) {
    const bx = TRUNK_BASE_X;
    const by = TRUNK_BASE_Y;
    const tx = TRUNK_TOP_X;
    const ty = TRUNK_TOP_Y + bob;
    const cx = TRUNK_CTRL_X;
    const cy = TRUNK_CTRL_Y + bob;
    const wB = TRUNK_HALF;
    const wT = TRUNK_HALF * 0.62;
    ctx.beginPath();
    ctx.moveTo(bx - wB, by);
    ctx.quadraticCurveTo(cx - wB, cy, tx - wT, ty);
    ctx.lineTo(tx + wT, ty);
    ctx.quadraticCurveTo(cx + wB, cy, bx + wB, by);
    ctx.closePath();
  }
  function trunkPoint(u, bob) {
    const bx = TRUNK_BASE_X;
    const by = TRUNK_BASE_Y;
    const tx = TRUNK_TOP_X;
    const ty = TRUNK_TOP_Y + bob;
    const cx = TRUNK_CTRL_X;
    const cy = TRUNK_CTRL_Y + bob;
    const mt = 1 - u;
    const x = mt * mt * bx + 2 * mt * u * cx + u * u * tx;
    const y = mt * mt * by + 2 * mt * u * cy + u * u * ty;
    return [x, y];
  }
  function drawFrond(ctx, ax, ay, ang, len, sway, extraDroop, p, tipMix) {
    const dirX = Math.sin(ang);
    const dirY = -Math.cos(ang);
    const midX = ax + dirX * len * 0.55;
    const midY = ay + dirY * len * 0.55 - 2;
    const droopFall = 5 + extraDroop * 9;
    const tipX = ax + dirX * len + sway;
    const tipY = ay + dirY * len + droopFall;
    const tipCol = lerpRGB(p.frondMid, p.frondTip, clamp014(tipMix));
    ctx.strokeStyle = rgb4(p.frondDark);
    ctx.lineWidth = 3.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(ax, ay);
    ctx.quadraticCurveTo(midX, midY, tipX, tipY);
    ctx.stroke();
    ctx.strokeStyle = rgb4(p.frondMid);
    ctx.lineWidth = 2;
    ctx.beginPath();
    ctx.moveTo(ax, ay);
    ctx.quadraticCurveTo(midX, midY, tipX, tipY);
    ctx.stroke();
    const N = 7;
    for (let i = 1; i <= N; i++) {
      const u = i / (N + 1);
      const mt = 1 - u;
      const rx = mt * mt * ax + 2 * mt * u * midX + u * u * tipX;
      const ry = mt * mt * ay + 2 * mt * u * midY + u * u * tipY;
      const tgX = 2 * mt * (midX - ax) + 2 * u * (tipX - midX);
      const tgY = 2 * mt * (midY - ay) + 2 * u * (tipY - midY);
      const tl = Math.hypot(tgX, tgY) || 1;
      const pxn = -tgY / tl;
      const pyn = tgX / tl;
      const leafLen = (1 - u) * 4.2 + 1.4;
      const lc = u > 0.55 ? lerpRGB(p.frondMid, tipCol, (u - 0.55) / 0.45) : p.frondMid;
      ctx.strokeStyle = rgb4(lc);
      ctx.lineWidth = 1.1;
      [1, -1].forEach((side) => {
        ctx.beginPath();
        ctx.moveTo(rx, ry);
        ctx.lineTo(rx + pxn * side * leafLen + dirX * 1.2, ry + pyn * side * leafLen + 2.2);
        ctx.stroke();
      });
    }
    ctx.strokeStyle = rgba(p.frondLight, 0.7);
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(ax, ay - 0.4);
    ctx.quadraticCurveTo(midX - 0.6, midY - 1.2, tipX - sway * 0.3, tipY - 1.2);
    ctx.stroke();
    ctx.lineCap = "butt";
  }
  function drawCrown(ctx, bob, swayOf, p) {
    const [ax, ay] = [TRUNK_TOP_X, TRUNK_TOP_Y + bob];
    const order = [0, 5, 1, 4, 2, 3];
    order.forEach((idx) => {
      const f = FRONDS[idx];
      const extraDroop = f.lower * p.droop;
      const tipMix = f.lower * p.tipBrown;
      drawFrond(ctx, ax, ay, f.ang, f.len, swayOf(idx), extraDroop, p, tipMix);
    });
    ctx.fillStyle = rgb4(p.frondDark);
    ctx.beginPath();
    ctx.ellipse(ax, ay - 1, 3.2, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function drawCoconuts(ctx, bob, p) {
    const [ax, ay] = [TRUNK_TOP_X, TRUNK_TOP_Y + bob];
    const nuts = [
      [ax - 3.2, ay + 3.4, 2.6],
      [ax + 0.4, ay + 4.6, 2.8],
      [ax - 1.6, ay + 5.6, 2.3]
    ];
    nuts.forEach(([nx, ny, r]) => {
      ctx.fillStyle = rgb4(p.outline);
      ctx.beginPath();
      ctx.arc(nx, ny, r + 0.7, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb4(p.coconut);
      ctx.beginPath();
      ctx.arc(nx, ny, r, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba(lerpRGB(p.coconut, [255, 240, 210], 0.45), 0.7);
      ctx.beginPath();
      ctx.arc(nx - r * 0.35, ny - r * 0.35, r * 0.5, 0, Math.PI * 2);
      ctx.fill();
    });
  }
  function paint4(ctx, raw, bob) {
    const p = clampP(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb4(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb4(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb4(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 150, 60]],
          [12, 18.6, 0.7, [176, 120, 44]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.4, 1.5, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba([90, 64, 24], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3.2, 0);
          ctx.lineTo(3.2, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb4(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, TRUNK_BASE_Y + 1.5, 7, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(3.5, TRUNK_BASE_Y + 2, 12, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.lineWidth = 1.8;
      trunkPath(ctx, bob);
      ctx.fillStyle = rgb4(p.outline);
      ctx.strokeStyle = rgb4(p.outline);
      ctx.fill();
      ctx.stroke();
      ctx.restore();
      ctx.save();
      trunkPath(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb4(p.trunkDark);
      ctx.fillRect(-8, TRUNK_TOP_Y + bob - 4, 22, TRUNK_BASE_Y - TRUNK_TOP_Y + 12);
      const tg = ctx.createLinearGradient(-TRUNK_HALF, 0, TRUNK_HALF + 4, 0);
      tg.addColorStop(0, rgb4(p.trunkLight));
      tg.addColorStop(0.6, rgb4(p.trunkLight));
      tg.addColorStop(1, rgb4(p.trunkDark));
      ctx.fillStyle = tg;
      ctx.globalAlpha = 0.92;
      trunkPath(ctx, bob);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba(p.trunkDark, 0.9);
      ctx.lineWidth = 1.3;
      for (let s = 1; s <= SEGMENTS; s++) {
        const u = s / (SEGMENTS + 1);
        const [px, py] = trunkPoint(u, bob);
        const w = lerp6(TRUNK_HALF, TRUNK_HALF * 0.62, u) + 1.6;
        ctx.beginPath();
        ctx.moveTo(px - w, py + 1.2);
        ctx.quadraticCurveTo(px, py + 2.6, px + w, py + 0.6);
        ctx.stroke();
      }
      ctx.restore();
      drawCrown(ctx, bob, () => 0, p);
      drawCoconuts(ctx, bob, p);
      if (p.gloss > 0.02) {
        const [ax, ay] = [TRUNK_TOP_X, TRUNK_TOP_Y + bob];
        ctx.strokeStyle = rgba(p.frondLight, 0.28 + 0.4 * p.gloss);
        ctx.lineWidth = 1;
        [-0.34, 0.16].forEach((ang) => {
          const dirX = Math.sin(ang);
          const dirY = -Math.cos(ang);
          ctx.beginPath();
          ctx.moveTo(ax + dirX * 4, ay + dirY * 4 - 1);
          ctx.lineTo(ax + dirX * 14, ay + dirY * 14 - 3);
          ctx.stroke();
        });
      }
      if (p.frostAmt > 0.02) {
        const [ax, ay] = [TRUNK_TOP_X, TRUNK_TOP_Y + bob];
        ctx.fillStyle = rgba([225, 240, 255], 0.6 * p.frostAmt);
        const speck = [
          [ax - 12, ay - 8],
          [ax - 6, ay - 12],
          [ax + 2, ay - 13],
          [ax + 10, ay - 9],
          [ax + 14, ay - 3],
          [ax - 15, ay - 2],
          [ax - 2, ay - 6],
          [ax + 6, ay - 5]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        const [ax, ay] = [TRUNK_TOP_X, TRUNK_TOP_Y + bob];
        ctx.fillStyle = rgba([246, 251, 255], 0.9 * a);
        const caps = [
          [ax - 6, ay - 6, 3.6, 1.5],
          [ax + 4, ay - 7, 3.8, 1.6],
          [ax - 1, ay - 2, 3.2, 1.4]
        ];
        caps.forEach(([cx, cy, rx, ry]) => {
          ctx.beginPath();
          ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba(p.light, p.lightAmt));
        lg.addColorStop(1, rgba(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt4(t, amp = 0.8, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw(season) {
    return (ctx) => paint4(ctx, SP4[season], 0);
  }
  function paintWithSway(ctx, season, t, bob) {
    paint4(ctx, SP4[season], bob);
    const p = clampP(SP4[season]);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      const swayOf = (i) => {
        return Math.sin(t * 1.3 + i * 0.7) * 1.6 + Math.sin(t * 0.8) * 0.8;
      };
      drawCrown(ctx, bob, swayOf, p);
      drawCoconuts(ctx, bob, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function anim(season) {
    return (ctx, t) => {
      const bob = bobAt4(t);
      paintWithSway(ctx, season, t, bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -10 + bob + Math.sin(t * 1.1) * 1;
          ctx.beginPath();
          ctx.arc(-2, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const ang = 0.16;
          const dirX = Math.sin(ang);
          const dirY = -Math.cos(ang);
          const r = 4 + prog * 14;
          const gx = TRUNK_TOP_X + dirX * r;
          const gy = TRUNK_TOP_Y + bob + dirY * r - 2;
          ctx.fillStyle = "rgba(232,255,200,0.7)";
          ctx.beginPath();
          ctx.ellipse(gx, gy, 1.6, 1, ang, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const f = Math.sin(t * 3);
          const ang = 1.3;
          const dirX = Math.sin(ang);
          const dirY = -Math.cos(ang);
          const len = 21;
          const tx = TRUNK_TOP_X + dirX * len + f * 1.4;
          const ty = TRUNK_TOP_Y + bob + dirY * len + 11 + Math.abs(f) * 0.6;
          ctx.fillStyle = "rgba(184,132,60,0.85)";
          ctx.beginPath();
          ctx.ellipse(tx, ty, 1.6, 1, ang + f * 0.3, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [12, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(TRUNK_TOP_X, TRUNK_TOP_Y + bob - 4, 12, 6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition4(fromIdx) {
    const from = SP4[SEASON_NAMES[fromIdx]];
    const to = SP4[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother4(clamp014(pp));
      paint4(ctx, lerpP4(from, to, k), 0);
    };
  }
  var springToSummer2 = makeTransition4(0);
  var summerToAutumn2 = makeTransition4(1);
  var autumnToWinter2 = makeTransition4(2);
  var VARIANTS5 = {
    Spring: { draw: draw("Spring"), anim: anim("Spring") },
    Summer: { draw: draw("Summer"), anim: anim("Summer") },
    Autumn: { draw: draw("Autumn"), anim: anim("Autumn") },
    Winter: { draw: draw("Winter"), anim: anim("Winter") }
  };
  var TRANSITIONS5 = {
    0: springToSummer2,
    1: summerToAutumn2,
    2: autumnToWinter2
  };

  // src/textures/seasonal/tree/willow.ts
  function clamp015(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp7(a, b, f) {
    return a + (b - a) * f;
  }
  function lerp34(a, b, f) {
    return [lerp7(a[0], b[0], f), lerp7(a[1], b[1], f), lerp7(a[2], b[2], f)];
  }
  function rgb5(c, alpha = 1) {
    const r = Math.round(clamp015(c[0] / 255) * 255);
    const g = Math.round(clamp015(c[1] / 255) * 255);
    const b = Math.round(clamp015(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp015(alpha)})`;
  }
  function scale34(c, k) {
    return [c[0] * k, c[1] * k, c[2] * k];
  }
  var smoother5 = (x) => {
    const c = clamp015(x);
    return c * c * c * (c * (6 * c - 15) + 10);
  };
  function bobAt5(t, A = 0.5, w = 1.1) {
    return A * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP5 = {
    // Spring — pale fresh-green new fronds; airy dewy lime pad + blossom.
    Spring: {
      frondDark: [86, 134, 52],
      frondMid: [140, 196, 80],
      frondLight: [200, 230, 130],
      bark: [120, 92, 58],
      barkShade: [90, 66, 40],
      barkLine: [62, 44, 26],
      padGrass: [126, 200, 86],
      soil: [104, 78, 44],
      outlineTint: [44, 50, 34],
      lightTint: [214, 238, 228],
      // cool-bright
      leafDensity: 0.72,
      catkinAmt: 0.85,
      frostAmt: 0,
      crownSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.55,
      fallenLeafAmt: 0,
      lightStrength: 0.22,
      shadowStrength: 0.2
    },
    // Summer — PEAK: full lush green cascade, densest; warm light, strong shadow.
    Summer: {
      frondDark: [48, 102, 36],
      frondMid: [82, 156, 56],
      frondLight: [150, 202, 92],
      bark: [114, 84, 52],
      barkShade: [84, 60, 36],
      barkLine: [56, 40, 24],
      padGrass: [70, 162, 64],
      soil: [96, 70, 40],
      outlineTint: [36, 48, 30],
      lightTint: [255, 244, 206],
      // warm
      leafDensity: 1,
      catkinAmt: 0,
      frostAmt: 0,
      crownSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.24,
      shadowStrength: 0.28
    },
    // Autumn — pale gold-yellow trailing strands; olive-tan pad; fallen leaves.
    Autumn: {
      frondDark: [168, 124, 36],
      frondMid: [224, 184, 64],
      frondLight: [248, 222, 120],
      bark: [110, 80, 48],
      barkShade: [82, 58, 34],
      barkLine: [58, 42, 26],
      padGrass: [150, 144, 86],
      soil: [110, 80, 44],
      outlineTint: [54, 44, 26],
      lightTint: [250, 220, 162],
      // low amber
      leafDensity: 0.82,
      catkinAmt: 0,
      frostAmt: 0,
      crownSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.6,
      lightStrength: 0.26,
      shadowStrength: 0.22
    },
    // Winter — sparse bare drooping strands w/ frost; snow cap on crown + trunk.
    Winter: {
      frondDark: [120, 124, 116],
      frondMid: [156, 162, 154],
      frondLight: [196, 204, 200],
      bark: [108, 92, 78],
      barkShade: [80, 66, 56],
      barkLine: [56, 48, 44],
      padGrass: [196, 210, 224],
      soil: [120, 126, 136],
      outlineTint: [48, 52, 60],
      lightTint: [206, 222, 240],
      // cool blue-grey
      leafDensity: 0.22,
      // sparse bare drooping strands (still reads as a willow)
      catkinAmt: 0,
      frostAmt: 0.8,
      crownSnowAmt: 0.85,
      padSnowAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      lightStrength: 0.2,
      shadowStrength: 0.16
    }
  };
  var STRANDS = [
    [-16.5, -8, 22, 1.3],
    [-13, -11, 25, 1.15],
    [-9.5, -12.5, 27, 1],
    [-6, -13.2, 28, 0.9],
    [-2.5, -13.5, 29, 0.82],
    [1, -13.5, 29, 0.82],
    [4.5, -13, 28, 0.9],
    [8, -12, 27, 1],
    [11.5, -10.5, 25, 1.15],
    [15, -7.5, 22, 1.3],
    // a few inner/front strands for layered depth
    [-7.5, -10, 24, 0.7],
    [-1, -10.5, 25, 0.65],
    [5.5, -10, 24, 0.7]
  ];
  var CROWN = [
    [-9, -13, 6],
    [-2, -16, 6.6],
    [5, -15, 6.4],
    [11, -12, 5.6],
    [-13, -10, 5],
    [13, -9, 4.8],
    [0, -11, 5.4]
  ];
  function padEllipse4(ctx, p) {
    ctx.fillStyle = `rgba(0,0,0,${0.24 * p.shadowStrength + 0.04})`;
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb5(scale34(p.padGrass, 0.6), 1);
    ctx.beginPath();
    ctx.ellipse(0, 20.4, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb5(p.padGrass, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt < 0.6) {
      ctx.fillStyle = rgb5(scale34(p.padGrass, 1.12), 1 - p.padSnowAmt);
      const tufts = 11;
      for (let i = 0; i < tufts; i++) {
        const a = i / tufts * Math.PI * 2;
        const tx = Math.cos(a) * 17;
        const ty = 19 + Math.sin(a) * 4.7;
        if (ty < 17) continue;
        ctx.beginPath();
        ctx.ellipse(tx, ty, 2, 1.3, a, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.fillStyle = rgb5(p.soil, 1);
    ctx.beginPath();
    ctx.ellipse(0, 19.4, 6.5, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.blossomAmt > 0.02) {
      const spots = [
        [-12, 20],
        [-6, 21.6],
        [8, 21],
        [13, 19.6],
        [2, 22],
        [-2, 19.6]
      ];
      spots.forEach(([sx, sy], i) => {
        const a = p.blossomAmt * (i % 2 === 0 ? 0.9 : 0.7);
        ctx.fillStyle = `rgba(255,236,246,${clamp015(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 1.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = `rgba(248,210,90,${clamp015(a)})`;
        ctx.beginPath();
        ctx.arc(sx, sy, 0.45, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.fallenLeafAmt > 0.02) {
      const leaves = [
        [-8, 20.4, 0.5],
        [6, 21.4, -0.4],
        [-1, 22, 0.2],
        [11, 20, 0.8]
      ];
      leaves.forEach(([lx, ly, rot], i) => {
        if (i / leaves.length > p.fallenLeafAmt + 0.05) return;
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb5(p.frondMid, Math.min(1, p.fallenLeafAmt + 0.3));
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.1, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb5(p.outlineTint, 0.4);
        ctx.lineWidth = 0.5;
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.padSnowAmt > 0.02) {
      const snow = ctx.createLinearGradient(0, 15, 0, 23);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c6d6e6");
      ctx.save();
      ctx.globalAlpha = clamp015(p.padSnowAmt);
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = `rgba(255,255,255,${clamp015(0.5 * p.frostAmt + 0.3)})`;
        const sparkle = [
          [-10, 18],
          [-4, 20],
          [3, 19],
          [9, 20.5],
          [13, 18.5],
          [-13, 19.5]
        ];
        sparkle.forEach(([sx, sy], i) => {
          if (i / sparkle.length > p.frostAmt) return;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
    }
  }
  function trunk4(ctx, p) {
    ctx.fillStyle = rgb5(p.outlineTint, 0.9);
    ctx.beginPath();
    ctx.moveTo(-5.6, 19.5);
    ctx.quadraticCurveTo(-5, 8, -4.2, -2);
    ctx.quadraticCurveTo(-2.2, -6.5, 0, -6.6);
    ctx.quadraticCurveTo(2.2, -6.5, 4.2, -2);
    ctx.quadraticCurveTo(5, 8, 5.6, 19.5);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb5(p.barkShade, 1);
    ctx.beginPath();
    ctx.moveTo(-4.6, 19);
    ctx.quadraticCurveTo(-4, 8, -3.3, -2);
    ctx.quadraticCurveTo(-1.6, -5.6, 0, -5.6);
    ctx.quadraticCurveTo(1.6, -5.6, 3.3, -2);
    ctx.quadraticCurveTo(4, 8, 4.6, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb5(p.bark, 1);
    ctx.beginPath();
    ctx.moveTo(-4.6, 19);
    ctx.quadraticCurveTo(-4, 8, -3.3, -2);
    ctx.quadraticCurveTo(-1.6, -5.6, 0.4, -5.6);
    ctx.quadraticCurveTo(0.1, 6, -0.6, 19);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb5(scale34(p.bark, 0.84), 1);
    ctx.beginPath();
    ctx.moveTo(-4.6, 19);
    ctx.quadraticCurveTo(-8.5, 18.2, -10.5, 19.8);
    ctx.quadraticCurveTo(-6.5, 19.6, -3.8, 18.4);
    ctx.closePath();
    ctx.fill();
    ctx.beginPath();
    ctx.moveTo(4.6, 19);
    ctx.quadraticCurveTo(8.5, 18.2, 10.5, 19.8);
    ctx.quadraticCurveTo(6.5, 19.6, 3.8, 18.4);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb5(p.barkLine, 0.85);
    ctx.lineWidth = 1;
    ctx.lineCap = "round";
    const furrows = [
      [-2.6, 0.2],
      [-0.4, -0.2],
      [2, 0.4]
    ];
    furrows.forEach(([fx, lean]) => {
      ctx.beginPath();
      ctx.moveTo(fx, -4.5);
      ctx.quadraticCurveTo(fx + lean, 8, fx + lean * 1.6, 18);
      ctx.stroke();
    });
    ctx.lineCap = "butt";
    if (p.crownSnowAmt > 0.02) {
      ctx.fillStyle = `rgba(244,248,255,${clamp015(0.9 * p.crownSnowAmt)})`;
      ctx.beginPath();
      ctx.ellipse(0, -5.4, 4.4, 1.6, 0, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function crownBlob(ctx, x, y, r, p, alpha) {
    if (r < 0.4 || alpha < 0.02) return;
    ctx.save();
    ctx.globalAlpha = clamp015(alpha);
    ctx.fillStyle = rgb5(p.frondDark, 1);
    ctx.beginPath();
    ctx.ellipse(x, y + r * 0.2, r, r * 0.92, 0, 0, Math.PI * 2);
    ctx.fill();
    const grad = ctx.createRadialGradient(x - r * 0.4, y - r * 0.4, r * 0.2, x, y, r);
    grad.addColorStop(0, rgb5(p.frondLight, 1));
    grad.addColorStop(0.55, rgb5(p.frondMid, 1));
    grad.addColorStop(1, rgb5(p.frondDark, 1));
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.ellipse(x, y, r * 0.96, r * 0.82, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.globalAlpha = clamp015(alpha * 0.7);
    ctx.fillStyle = rgb5(p.frondLight, 1);
    ctx.beginPath();
    ctx.ellipse(x - r * 0.38, y - r * 0.42, r * 0.32, r * 0.26, -0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function crown(ctx, p, sway, shimmer) {
    const d = clamp015(p.leafDensity);
    const domeA = 0.45 + 0.55 * d;
    CROWN.forEach(([x, y, r], i) => {
      const s = sway * (0.15 + Math.abs(x) / 40) + Math.sin(shimmer + i) * 0.25 * d;
      crownBlob(ctx, x + s, y, r * (0.62 + 0.38 * d), p, domeA);
    });
  }
  function strands(ctx, p, sway, shimmer) {
    const d = clamp015(p.leafDensity);
    ctx.lineCap = "round";
    STRANDS.forEach(([ax, ay, len, baseSway], i) => {
      const threshold = i % 10 / 10;
      const grow = clamp015((d - threshold * 0.45) / 0.55);
      if (grow < 0.04) return;
      const L = len * (0.62 + 0.38 * grow);
      const tipY = ay + L;
      const phase = i * 0.7;
      const drift = sway * baseSway + Math.sin(shimmer * 0.8 + phase) * 0.6 * d;
      const midX = ax + drift * 0.45;
      const tipX = ax + drift;
      ctx.strokeStyle = rgb5(p.frondDark, 0.9 * grow);
      ctx.lineWidth = 2.4 * (0.5 + 0.5 * grow);
      ctx.beginPath();
      ctx.moveTo(ax, ay);
      ctx.quadraticCurveTo(midX, ay + L * 0.55, tipX, tipY);
      ctx.stroke();
      const col = i % 3 === 0 ? p.frondLight : p.frondMid;
      ctx.strokeStyle = rgb5(col, (0.78 + 0.2 * grow) * (0.4 + 0.6 * grow));
      ctx.lineWidth = 1.3 * (0.5 + 0.5 * grow);
      ctx.beginPath();
      ctx.moveTo(ax, ay);
      ctx.quadraticCurveTo(midX, ay + L * 0.55, tipX, tipY);
      ctx.stroke();
      if (grow > 0.35) {
        ctx.fillStyle = rgb5(p.frondLight, 0.55 * grow);
        for (let k = 1; k <= 3; k++) {
          const f = k / 4;
          const fx = lerp7(ax, tipX, f) + Math.sin(phase + k) * 0.6;
          const fy = ay + L * f;
          ctx.beginPath();
          ctx.ellipse(fx, fy, 0.9, 1.8, drift * 0.05, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      if (p.frostAmt > 0.02 && grow < 0.7) {
        ctx.fillStyle = `rgba(228,240,255,${clamp015(0.6 * p.frostAmt)})`;
        const fy = ay + L * 0.78;
        ctx.beginPath();
        ctx.arc(tipX + (midX - tipX) * 0.1, fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    });
    ctx.lineCap = "butt";
    if (p.catkinAmt > 0.02) {
      const anchors = [
        [-10, -12],
        [-3, -15],
        [4, -14],
        [10, -11],
        [0, -13],
        [-6, -10]
      ];
      anchors.forEach(([cx, cy], i) => {
        if (i / anchors.length > p.catkinAmt + 0.1) return;
        const dx = sway * 0.4 + Math.sin(i * 1.3) * 0.4;
        ctx.fillStyle = `rgba(232,232,176,${clamp015(0.85 * p.catkinAmt)})`;
        ctx.beginPath();
        ctx.ellipse(cx + dx, cy, 1, 1.7, 0, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.crownSnowAmt > 0.02) {
      ctx.fillStyle = `rgba(244,248,255,${clamp015(0.92 * p.crownSnowAmt)})`;
      const caps = [
        [-7, -17, 5.2],
        [2, -19, 5.6],
        [9, -15, 4.6]
      ];
      caps.forEach(([cx, cy, cr]) => {
        ctx.beginPath();
        ctx.ellipse(cx, cy, cr, cr * 0.5, 0, Math.PI, Math.PI * 2);
        ctx.fill();
      });
    }
  }
  function paint5(ctx, p, bob, sway = 0, shimmer = 0) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      padEllipse4(ctx, p);
      ctx.save();
      ctx.translate(0, -bob);
      trunk4(ctx, p);
      crown(ctx, p, sway, shimmer);
      strands(ctx, p, sway, shimmer);
      ctx.restore();
      if (p.lightStrength > 0.01) {
        const lg = ctx.createRadialGradient(-8, -16, 4, -2, -8, 34);
        lg.addColorStop(0, rgb5(p.lightTint, p.lightStrength));
        lg.addColorStop(1, rgb5(p.lightTint, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.ellipse(-2, -6, 27, 30, 0, 0, Math.PI * 2);
        ctx.fill();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function lerpP5(a, b, f) {
    return {
      frondDark: lerp34(a.frondDark, b.frondDark, f),
      frondMid: lerp34(a.frondMid, b.frondMid, f),
      frondLight: lerp34(a.frondLight, b.frondLight, f),
      bark: lerp34(a.bark, b.bark, f),
      barkShade: lerp34(a.barkShade, b.barkShade, f),
      barkLine: lerp34(a.barkLine, b.barkLine, f),
      padGrass: lerp34(a.padGrass, b.padGrass, f),
      soil: lerp34(a.soil, b.soil, f),
      outlineTint: lerp34(a.outlineTint, b.outlineTint, f),
      lightTint: lerp34(a.lightTint, b.lightTint, f),
      leafDensity: lerp7(a.leafDensity, b.leafDensity, f),
      catkinAmt: lerp7(a.catkinAmt, b.catkinAmt, f),
      frostAmt: lerp7(a.frostAmt, b.frostAmt, f),
      crownSnowAmt: lerp7(a.crownSnowAmt, b.crownSnowAmt, f),
      padSnowAmt: lerp7(a.padSnowAmt, b.padSnowAmt, f),
      blossomAmt: lerp7(a.blossomAmt, b.blossomAmt, f),
      fallenLeafAmt: lerp7(a.fallenLeafAmt, b.fallenLeafAmt, f),
      lightStrength: lerp7(a.lightStrength, b.lightStrength, f),
      shadowStrength: lerp7(a.shadowStrength, b.shadowStrength, f)
    };
  }
  function makeDraw4(season) {
    return (ctx) => paint5(ctx, SP5[season], 0, 0, 0);
  }
  function animSpring4(ctx, t) {
    const bob = bobAt5(t, 0.45, 1.2);
    const sway = Math.sin(t * 1.3) * 2.2;
    paint5(ctx, SP5.Spring, bob, sway, t * 2.4);
  }
  function animSummer4(ctx, t) {
    const bob = bobAt5(t, 0.45, 1.1);
    const sway = Math.sin(t * 1.2) * 2.4;
    paint5(ctx, SP5.Summer, bob, sway, t * 1.9);
  }
  function animAutumn4(ctx, t) {
    const bob = bobAt5(t, 0.45, 1.05);
    const sway = Math.sin(t * 1) * 2;
    paint5(ctx, SP5.Autumn, bob, sway, t * 1.6);
    ctx.save();
    try {
      const p = SP5.Autumn;
      const leaves = [
        [-6, 0, 0.55],
        // [startX, phase, hueBlend]
        [9, 0.5, 0.35]
      ];
      leaves.forEach(([sx, phase, hb]) => {
        const prog = ((t * 0.26 + phase) % 1 + 1) % 1;
        const ly = -6 + prog * 28;
        const lx = sx + Math.sin(prog * Math.PI * 2 + phase * 6) * 6;
        const col = lerp34(p.frondMid, p.frondLight, hb);
        ctx.fillStyle = rgb5(col, 1 - prog * 0.4);
        ctx.translate(lx, ly);
        ctx.rotate(prog * Math.PI * 2.5 + phase * 4);
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.1, 3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.setTransform(1, 0, 0, 1, 0, 0);
      });
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function animWinter4(ctx, t) {
    const bob = bobAt5(t, 0.32, 0.9);
    const sway = Math.sin(t * 0.85) * 1.5;
    paint5(ctx, SP5.Winter, bob, sway, 0);
    ctx.save();
    try {
      const span = 36;
      const seeds = [
        [-9, 1.2, 0],
        [7, 1, 0.5],
        [13, 0.9, 0.7]
      ];
      seeds.forEach(([fx, r, phase]) => {
        const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
        const fy = -24 + prog * span;
        const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
        ctx.fillStyle = "rgba(255,255,255,0.85)";
        ctx.beginPath();
        ctx.arc(driftX, fy, r, 0, Math.PI * 2);
        ctx.fill();
      });
      const sheen2 = 0.12 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.7));
      ctx.fillStyle = `rgba(200,224,255,${sheen2})`;
      ctx.beginPath();
      ctx.ellipse(-3, 18.6, 11, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function makeTransition5(from, to) {
    return (ctx, pp) => {
      const f = smoother5(clamp015(pp));
      paint5(ctx, lerpP5(SP5[from], SP5[to], f), 0, 0, 0);
    };
  }
  var VARIANTS6 = {
    Spring: { draw: makeDraw4("Spring"), anim: animSpring4 },
    Summer: { draw: makeDraw4("Summer"), anim: animSummer4 },
    Autumn: { draw: makeDraw4("Autumn"), anim: animAutumn4 },
    Winter: { draw: makeDraw4("Winter"), anim: animWinter4 }
  };
  var TRANSITIONS6 = {
    0: makeTransition5("Spring", "Summer"),
    1: makeTransition5("Summer", "Autumn"),
    2: makeTransition5("Autumn", "Winter")
  };

  // src/textures/seasonal/fruit/apple.ts
  function groundShadow2(ctx, rx = 14, alpha = 0.22) {
    ctx.fillStyle = `rgba(0,0,0,${alpha})`;
    ctx.beginPath();
    ctx.ellipse(0, 22, rx, 4, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function twig(ctx, lean, dead) {
    const darkBark = dead ? "#3a2b1c" : "#5a3d1e";
    const liteBark = dead ? "#5c4630" : "#8a5e2c";
    ctx.lineCap = "round";
    ctx.strokeStyle = darkBark;
    ctx.lineWidth = 4.2;
    ctx.beginPath();
    ctx.moveTo(-2, 22);
    ctx.quadraticCurveTo(2, 4, 8 + lean, -14);
    ctx.stroke();
    ctx.strokeStyle = liteBark;
    ctx.lineWidth = 1.8;
    ctx.beginPath();
    ctx.moveTo(-2, 22);
    ctx.quadraticCurveTo(2, 4, 8 + lean, -14);
    ctx.stroke();
    ctx.strokeStyle = darkBark;
    ctx.lineWidth = 3.2;
    ctx.beginPath();
    ctx.moveTo(1, 7);
    ctx.quadraticCurveTo(-7, -2, -9 + lean * 0.4, -10);
    ctx.stroke();
    ctx.strokeStyle = liteBark;
    ctx.lineWidth = 1.3;
    ctx.beginPath();
    ctx.moveTo(1, 7);
    ctx.quadraticCurveTo(-7, -2, -9 + lean * 0.4, -10);
    ctx.stroke();
    ctx.lineCap = "butt";
  }
  function leaf(ctx, x, y, angle, len, dark, lite) {
    ctx.save();
    ctx.translate(x, y);
    ctx.rotate(angle);
    ctx.fillStyle = dark;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(len * 0.5, -len * 0.42, len, 0);
    ctx.quadraticCurveTo(len * 0.5, len * 0.42, 0, 0);
    ctx.fill();
    ctx.fillStyle = lite;
    ctx.beginPath();
    ctx.moveTo(len * 0.1, 0);
    ctx.quadraticCurveTo(len * 0.5, -len * 0.3, len * 0.86, 0);
    ctx.quadraticCurveTo(len * 0.5, len * 0.3, len * 0.1, 0);
    ctx.fill();
    ctx.strokeStyle = dark;
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    ctx.moveTo(len * 0.08, 0);
    ctx.lineTo(len * 0.9, 0);
    ctx.stroke();
    ctx.restore();
  }
  function apple(ctx, x, y, r, skin, blush, glint, withStem) {
    ctx.save();
    ctx.translate(x, y);
    const grad = ctx.createRadialGradient(-r * 0.35, -r * 0.4, r * 0.15, 0, 0, r * 1.25);
    grad.addColorStop(0, skin.hi);
    grad.addColorStop(0.55, skin.mid);
    grad.addColorStop(1, skin.lo);
    ctx.fillStyle = grad;
    ctx.beginPath();
    ctx.moveTo(0, -r * 0.78);
    ctx.bezierCurveTo(-r * 0.7, -r * 1.05, -r * 1.18, -r * 0.4, -r * 1.05, r * 0.18);
    ctx.bezierCurveTo(-r * 0.95, r * 0.95, -r * 0.35, r * 1.15, 0, r * 0.95);
    ctx.bezierCurveTo(r * 0.35, r * 1.15, r * 0.95, r * 0.95, r * 1.05, r * 0.18);
    ctx.bezierCurveTo(r * 1.18, -r * 0.4, r * 0.7, -r * 1.05, 0, -r * 0.78);
    ctx.fill();
    if (blush.amt > 0.01) {
      ctx.save();
      ctx.globalAlpha = Math.min(1, blush.amt) * 0.8;
      const bg = ctx.createRadialGradient(r * 0.35, r * 0.1, 0, r * 0.35, r * 0.1, r * 0.85);
      bg.addColorStop(0, blush.color);
      bg.addColorStop(1, "rgba(0,0,0,0)");
      ctx.fillStyle = bg;
      ctx.beginPath();
      ctx.ellipse(r * 0.3, r * 0.05, r * 0.75, r * 0.85, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = "rgba(0,0,0,0.22)";
    ctx.beginPath();
    ctx.ellipse(0, -r * 0.72, r * 0.22, r * 0.16, 0, 0, Math.PI * 2);
    ctx.fill();
    if (withStem) {
      ctx.strokeStyle = "#5a3d1e";
      ctx.lineWidth = 1.6;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(0, -r * 0.75);
      ctx.quadraticCurveTo(r * 0.18, -r * 1.2, r * 0.45, -r * 1.35);
      ctx.stroke();
      ctx.lineCap = "butt";
      leaf(ctx, r * 0.4, -r * 1.28, -0.5, r * 0.7, "#3f6b1c", "#6fae35");
    }
    const gx = -r * 0.4 + Math.sin(glint * Math.PI * 2) * r * 0.12;
    const gy = -r * 0.45;
    const ga = 0.5 + 0.35 * (0.5 + 0.5 * Math.sin(glint * Math.PI * 2));
    ctx.fillStyle = `rgba(255,255,255,${ga})`;
    ctx.beginPath();
    ctx.ellipse(gx, gy, r * 0.26, r * 0.34, -0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = `rgba(255,255,255,${Math.min(1, ga + 0.25)})`;
    ctx.beginPath();
    ctx.arc(gx - r * 0.1, gy - r * 0.1, r * 0.09, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function blossom(ctx, x, y, r, open, alpha) {
    if (open <= 0.01 || alpha <= 0.01) return;
    ctx.save();
    ctx.translate(x, y);
    ctx.globalAlpha = Math.min(1, alpha);
    const pr = r * open;
    for (let i = 0; i < 5; i++) {
      const a = i / 5 * Math.PI * 2 - Math.PI / 2;
      ctx.save();
      ctx.rotate(a);
      const pg = ctx.createLinearGradient(0, 0, 0, -pr);
      pg.addColorStop(0, "#f7c9d6");
      pg.addColorStop(1, "#fff4f7");
      ctx.fillStyle = pg;
      ctx.beginPath();
      ctx.ellipse(0, -pr * 0.55, pr * 0.42, pr * 0.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = "#f2c84a";
    ctx.beginPath();
    ctx.arc(0, 0, r * 0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "#d99a1f";
    ctx.beginPath();
    ctx.arc(0, 0, r * 0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  var LEAF_GREEN = { dark: "#2f5916", lite: "#6fae35" };
  var LEAF_GOLD = { dark: "#9a6a16", lite: "#e0a73a" };
  var GREEN_SKIN = { hi: "#cfe88a", mid: "#7fb53a", lo: "#3f6b1c" };
  var RED_SKIN = { hi: "#ff7a5e", mid: "#d6322a", lo: "#7c1410" };
  var DULL_RED_SKIN = { hi: "#a8584e", mid: "#8a2f2a", lo: "#521012" };
  function appleSpring(ctx, sway, glint) {
    groundShadow2(ctx, 12, 0.2);
    twig(ctx, sway * 0.4, false);
    leaf(ctx, 6, -2, -0.9 + sway * 0.04, 11, LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, -6, -1, 3.3 - sway * 0.04, 9, LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, 9, -12, -1.5, 8, LEAF_GREEN.dark, LEAF_GREEN.lite);
    ctx.fillStyle = "#7fb53a";
    ctx.beginPath();
    ctx.arc(-8 + sway * 0.3, -9, 3, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "rgba(255,255,255,0.4)";
    ctx.beginPath();
    ctx.arc(-9 + sway * 0.3, -10, 1, 0, Math.PI * 2);
    ctx.fill();
    blossom(ctx, 0 + sway, 0, 8, 1, 1);
    blossom(ctx, 11 + sway * 1.2, -10, 5, 1, 0.95);
    const ga = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(glint));
    ctx.fillStyle = `rgba(255,255,255,${ga})`;
    ctx.beginPath();
    ctx.arc(-2 + sway, -2, 1.6, 0, Math.PI * 2);
    ctx.fill();
  }
  function drawAppleSpring(ctx) {
    appleSpring(ctx, 0, 0.3);
  }
  function animAppleSpring(ctx, t) {
    const sway = Math.sin(t * 1.3) * 1.8;
    appleSpring(ctx, sway, t * 2.4);
  }
  function appleSummer(ctx, bob, glint) {
    groundShadow2(ctx, 12, 0.2);
    twig(ctx, bob * 0.3, false);
    leaf(ctx, 7, -10, -0.7, 12, LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, -7, -2, 3.2, 10, LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, 11, -1, -0.2, 9, LEAF_GREEN.dark, LEAF_GREEN.lite);
    apple(ctx, 9, -11, 5, GREEN_SKIN, { color: "#cfe88a", amt: 0 }, (glint + 0.3) % 1, false);
    apple(ctx, -1, 2 + bob, 10, GREEN_SKIN, { color: "#e8f0a0", amt: 0.25 }, glint, true);
  }
  function drawAppleSummer(ctx) {
    appleSummer(ctx, 0, 0.3);
  }
  function animAppleSummer(ctx, t) {
    const bob = Math.sin(t * 1.3) * 1.6;
    const glint = t * 0.45 % 1;
    appleSummer(ctx, bob, glint);
  }
  function appleAutumn(ctx, bob, glint, leafFall) {
    groundShadow2(ctx, 13, 0.22);
    twig(ctx, bob * 0.3, false);
    leaf(ctx, 7, -10, -0.7, 12, LEAF_GOLD.dark, LEAF_GOLD.lite);
    leaf(ctx, -7, -2, 3.2, 10, LEAF_GOLD.dark, LEAF_GOLD.lite);
    apple(ctx, 10, -10, 5.5, RED_SKIN, { color: "#ffcf6a", amt: 0.5 }, (glint + 0.4) % 1, false);
    apple(ctx, -1, 3 + bob, 11, RED_SKIN, { color: "#ffd166", amt: 0.7 }, glint, true);
    leaf(ctx, leafFall.x, leafFall.y, leafFall.rot, 9, LEAF_GOLD.dark, LEAF_GOLD.lite);
  }
  function drawAppleAutumn(ctx) {
    appleAutumn(ctx, 0, 0.3, { x: 14, y: 8, rot: 0.6 });
  }
  function animAppleAutumn(ctx, t) {
    const bob = Math.sin(t * 1) * 1.7;
    const glint = t * 0.5 % 1;
    const prog = (t / 4 % 1 + 1) % 1;
    const leafFall = {
      x: 12 + Math.sin(prog * Math.PI * 2) * 4,
      y: -12 + prog * 30,
      rot: 0.4 + prog * Math.PI * 2
    };
    appleAutumn(ctx, bob, glint, leafFall);
  }
  function appleWinter(ctx, flakes, sheen2, sway) {
    groundShadow2(ctx, 14, 0.18);
    const snow = ctx.createLinearGradient(0, 16, 0, 24);
    snow.addColorStop(0, "#eef4fb");
    snow.addColorStop(1, "#c2d2e4");
    ctx.fillStyle = snow;
    ctx.beginPath();
    ctx.ellipse(0, 21, 16, 5.5, 0, 0, Math.PI * 2);
    ctx.fill();
    twig(ctx, sway * 0.3, true);
    ctx.fillStyle = "#f4f8ff";
    ctx.beginPath();
    ctx.ellipse(8 + sway * 0.3, -14, 3.4, 1.8, -0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.ellipse(2, 4, 2.6, 1.4, 0.2, 0, Math.PI * 2);
    ctx.fill();
    apple(ctx, -1 + sway * 0.5, 3, 10, DULL_RED_SKIN, { color: "#7c4a30", amt: 0.35 }, 0.5, false);
    ctx.strokeStyle = "rgba(60,16,18,0.5)";
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    ctx.moveTo(-5 + sway * 0.5, 0);
    ctx.quadraticCurveTo(-1 + sway * 0.5, 4, 4 + sway * 0.5, 1);
    ctx.stroke();
    ctx.fillStyle = "rgba(244,248,255,0.85)";
    ctx.beginPath();
    ctx.ellipse(-1 + sway * 0.5, -5, 6, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = `rgba(200,224,255,${0.3 + sheen2 * 0.25})`;
    ctx.beginPath();
    ctx.arc(-4 + sway * 0.5, -1, 1.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "#ffffff";
    flakes.forEach(([fx, fy, r]) => {
      ctx.globalAlpha = 0.85;
      ctx.beginPath();
      ctx.arc(fx, fy, r, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.globalAlpha = 1;
  }
  function drawAppleWinter(ctx) {
    appleWinter(
      ctx,
      [
        [-8, -6, 1.4],
        [6, 2, 1.1],
        [11, -12, 1],
        [-3, 10, 1.2]
      ],
      0.4,
      0
    );
  }
  function animAppleWinter(ctx, t) {
    const span = 30;
    const seeds = [
      [-8, 1.4, 0],
      [6, 1.1, 0.45],
      [11, 1, 0.7],
      [-3, 1.2, 0.25]
    ];
    const flakes = seeds.map(([fx, r, phase]) => {
      const prog = ((t / 3.2 + phase) % 1 + 1) % 1;
      const fy = -22 + prog * span;
      const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
      return [driftX, fy, r];
    });
    const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
    const sway = Math.sin(t * 0.7) * 0.8;
    appleWinter(ctx, flakes, sheen2, sway);
  }
  function lerp8(a, b, p) {
    return a + (b - a) * p;
  }
  function clamp016(p) {
    return p < 0 ? 0 : p > 1 ? 1 : p;
  }
  function mixHex(c1, c2, p) {
    const a = parseInt(c1.slice(1), 16);
    const b = parseInt(c2.slice(1), 16);
    const r = Math.round(lerp8(a >> 16 & 255, b >> 16 & 255, p));
    const g = Math.round(lerp8(a >> 8 & 255, b >> 8 & 255, p));
    const bl = Math.round(lerp8(a & 255, b & 255, p));
    return `rgb(${r},${g},${bl})`;
  }
  function mixSkin(s1, s2, p) {
    return {
      hi: mixHex(s1.hi, s2.hi, p),
      mid: mixHex(s1.mid, s2.mid, p),
      lo: mixHex(s1.lo, s2.lo, p)
    };
  }
  function springToSummer3(ctx, p) {
    const q = clamp016(p);
    groundShadow2(ctx, 12, 0.2);
    twig(ctx, 0, false);
    leaf(ctx, 7, -10, -0.7, lerp8(11, 12, q), LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, -7, -2, 3.2, lerp8(9, 10, q), LEAF_GREEN.dark, LEAF_GREEN.lite);
    leaf(ctx, 11, -1, -0.2, lerp8(8, 9, q), LEAF_GREEN.dark, LEAF_GREEN.lite);
    if (q > 0.4) {
      apple(ctx, 9, -11, lerp8(2, 5, (q - 0.4) / 0.6), GREEN_SKIN, { color: "#cfe88a", amt: 0 }, 0.3, false);
    }
    const r = lerp8(3, 10, q);
    const fy = lerp8(-2, 2, q);
    apple(ctx, lerp8(-3, -1, q), fy, r, GREEN_SKIN, { color: "#e8f0a0", amt: lerp8(0, 0.25, q) }, 0.3, q > 0.3);
    const fade = clamp016(1 - q / 0.7);
    if (fade > 0.01) {
      const fall = q * 14;
      blossom(ctx, 2, 0 + fall, 8, fade, fade);
      blossom(ctx, 11, -10 + fall * 0.8, 5, fade, fade * 0.9);
    }
  }
  function summerToAutumn3(ctx, p) {
    const q = clamp016(p);
    groundShadow2(ctx, lerp8(12, 13, q), lerp8(0.2, 0.22, q));
    twig(ctx, 0, false);
    const ld = mixHex(LEAF_GREEN.dark, LEAF_GOLD.dark, q);
    const ll = mixHex(LEAF_GREEN.lite, LEAF_GOLD.lite, q);
    leaf(ctx, 7, -10, -0.7, 12, ld, ll);
    leaf(ctx, -7, -2, 3.2, 10, ld, ll);
    leaf(ctx, 11, -1, -0.2, lerp8(9, 0.01, q), ld, ll);
    const skin2 = mixSkin(GREEN_SKIN, RED_SKIN, q);
    apple(ctx, 10, -10, lerp8(5, 5.5, q), skin2, { color: mixHex("#cfe88a", "#ffcf6a", q), amt: lerp8(0, 0.5, q) }, 0.3, false);
    const skin = mixSkin(GREEN_SKIN, RED_SKIN, q);
    const blushColor = mixHex("#e8f0a0", "#ffd166", q);
    apple(ctx, -1, lerp8(2, 3, q), lerp8(10, 11, q), skin, { color: blushColor, amt: lerp8(0.25, 0.7, q) }, 0.3, true);
  }
  function autumnToWinter3(ctx, p) {
    const q = clamp016(p);
    groundShadow2(ctx, 14, lerp8(0.22, 0.18, q));
    const snowAmt = clamp016((q - 0.5) / 0.5);
    if (snowAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = snowAmt;
      const snow = ctx.createLinearGradient(0, 16, 0, 24);
      snow.addColorStop(0, "#eef4fb");
      snow.addColorStop(1, "#c2d2e4");
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 21, lerp8(8, 16, snowAmt), lerp8(3, 5.5, snowAmt), 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    twig(ctx, 0, q > 0.5);
    const leafLife = clamp016(1 - q / 0.5);
    if (leafLife > 0.01) {
      ctx.save();
      ctx.globalAlpha = leafLife;
      const drop = (1 - leafLife) * 16;
      leaf(ctx, 7, -10 + drop, -0.7 + (1 - leafLife), 12 * leafLife + 2, LEAF_GOLD.dark, LEAF_GOLD.lite);
      leaf(ctx, -7, -2 + drop, 3.2 + (1 - leafLife), 10 * leafLife + 2, LEAF_GOLD.dark, LEAF_GOLD.lite);
      ctx.restore();
    }
    if (q < 0.6) {
      const a2 = clamp016(1 - q / 0.6);
      ctx.save();
      ctx.globalAlpha = a2;
      apple(ctx, 10, -10, 5.5, mixSkin(RED_SKIN, DULL_RED_SKIN, q), { color: "#7c4a30", amt: 0.4 }, 0.5, false);
      ctx.restore();
    }
    const skin = mixSkin(RED_SKIN, DULL_RED_SKIN, q);
    apple(ctx, -1, 3, lerp8(11, 10, q), skin, { color: mixHex("#ffd166", "#7c4a30", q), amt: lerp8(0.7, 0.35, q) }, 0.5, q < 0.5);
    if (snowAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = snowAmt;
      ctx.fillStyle = "#f4f8ff";
      ctx.beginPath();
      ctx.ellipse(8, -14, 3.4 * snowAmt, 1.8 * snowAmt, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(244,248,255,0.85)";
      ctx.beginPath();
      ctx.ellipse(-1, -5, lerp8(3, 6, snowAmt), lerp8(1.4, 2.6, snowAmt), 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.globalAlpha = 1;
  }
  var VARIANTS7 = {
    Spring: { draw: drawAppleSpring, anim: animAppleSpring },
    Summer: { draw: drawAppleSummer, anim: animAppleSummer },
    Autumn: { draw: drawAppleAutumn, anim: animAppleAutumn },
    Winter: { draw: drawAppleWinter, anim: animAppleWinter }
  };
  var TRANSITIONS7 = {
    0: springToSummer3,
    1: summerToAutumn3,
    2: autumnToWinter3
  };

  // src/textures/seasonal/fruit/pear.ts
  var clamp017 = (x) => x < 0 ? 0 : x > 1 ? 1 : x;
  var rgb6 = (c, a = 1) => `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${a})`;
  var lerp9 = (a, b, t) => a + (b - a) * t;
  var lerpRGB2 = (a, b, t) => [
    lerp9(a[0], b[0], t),
    lerp9(a[1], b[1], t),
    lerp9(a[2], b[2], t)
  ];
  var smoother6 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerpP6(a, b, t) {
    return {
      skinTop: lerpRGB2(a.skinTop, b.skinTop, t),
      skinMid: lerpRGB2(a.skinMid, b.skinMid, t),
      skinBot: lerpRGB2(a.skinBot, b.skinBot, t),
      outline: lerpRGB2(a.outline, b.outline, t),
      leaf: lerpRGB2(a.leaf, b.leaf, t),
      padGrass: lerpRGB2(a.padGrass, b.padGrass, t),
      padGrassDark: lerpRGB2(a.padGrassDark, b.padGrassDark, t),
      soil: lerpRGB2(a.soil, b.soil, t),
      light: lerpRGB2(a.light, b.light, t),
      lightAmt: lerp9(a.lightAmt, b.lightAmt, t),
      shadowAmt: lerp9(a.shadowAmt, b.shadowAmt, t),
      ripeness: lerp9(a.ripeness, b.ripeness, t),
      gloss: lerp9(a.gloss, b.gloss, t),
      blush: lerp9(a.blush, b.blush, t),
      freckleAmt: lerp9(a.freckleAmt, b.freckleAmt, t),
      frostAmt: lerp9(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp9(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp9(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp9(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp9(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  var rand = (n) => {
    const s = Math.sin(n * 127.1 + 0.5) * 43758.5453;
    return s - Math.floor(s);
  };
  function pearPath(ctx, s) {
    const topX = 0;
    const topY = -11 * s;
    ctx.beginPath();
    ctx.moveTo(topX, topY);
    ctx.bezierCurveTo(5.5 * s, -9 * s, 6.5 * s, -2 * s, 8 * s, 4 * s);
    ctx.bezierCurveTo(10.5 * s, 9 * s, 9.5 * s, 17 * s, 0, 18 * s);
    ctx.bezierCurveTo(-9.5 * s, 17 * s, -10.5 * s, 9 * s, -8 * s, 4 * s);
    ctx.bezierCurveTo(-6.5 * s, -2 * s, -5.5 * s, -9 * s, topX, topY);
    ctx.closePath();
  }
  function paint6(ctx, pp, bob) {
    const p = {
      ...pp,
      lightAmt: clamp017(pp.lightAmt),
      shadowAmt: clamp017(pp.shadowAmt),
      ripeness: clamp017(pp.ripeness),
      gloss: clamp017(pp.gloss),
      blush: clamp017(pp.blush),
      freckleAmt: clamp017(pp.freckleAmt),
      frostAmt: clamp017(pp.frostAmt),
      snowCapAmt: clamp017(pp.snowCapAmt),
      padSnowAmt: clamp017(pp.padSnowAmt),
      blossomAmt: clamp017(pp.blossomAmt),
      fallenLeafAmt: clamp017(pp.fallenLeafAmt)
    };
    const s = 0.92 + p.ripeness * 0.12;
    ctx.save();
    ctx.fillStyle = rgb6([0, 0, 0], 0.1 + p.shadowAmt * 0.16);
    ctx.beginPath();
    ctx.ellipse(3, 21, 15, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb6(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    const padGrad = ctx.createLinearGradient(0, 15, 0, 23);
    padGrad.addColorStop(0, rgb6(p.padGrass));
    padGrad.addColorStop(1, rgb6(p.padGrassDark));
    ctx.fillStyle = padGrad;
    ctx.beginPath();
    ctx.ellipse(0, 18.5, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb6(p.padGrass);
    ctx.lineWidth = 1.3;
    ctx.lineCap = "round";
    for (let i = 0; i < 11; i++) {
      const tx = -16 + i * 3.2;
      const ty = 15.4 + Math.abs(tx) * 0.06;
      ctx.beginPath();
      ctx.moveTo(tx, ty + 1.4);
      ctx.lineTo(tx + (i % 2 ? 1.1 : -1.1), ty - 2.2);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    if (p.blossomAmt > 0.01) {
      for (let i = 0; i < 5; i++) {
        const bx = -13 + rand(i + 1) * 26;
        const by = 17 + rand(i + 21) * 4;
        ctx.fillStyle = rgb6([255, 244, 250], p.blossomAmt * 0.85);
        ctx.beginPath();
        ctx.ellipse(bx, by, 1.7, 1.2, rand(i + 7) * 3, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb6([255, 214, 230], p.blossomAmt * 0.7);
        ctx.beginPath();
        ctx.arc(bx, by, 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 0.01) {
      const leaves = [
        [-11, 19, -0.5, [196, 120, 36]],
        [9, 20.5, 0.7, [176, 92, 28]]
      ];
      leaves.forEach(([lx, ly, rot, col]) => {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb6(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.7, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb6([110, 64, 18], p.fallenLeafAmt * 0.8);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-3, 0);
        ctx.lineTo(3, 0);
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.padSnowAmt > 0.01) {
      ctx.fillStyle = rgb6([238, 245, 252], 0.55 + p.padSnowAmt * 0.4);
      ctx.beginPath();
      ctx.ellipse(0, 18, 17 * p.padSnowAmt + 4, 4.6 * p.padSnowAmt + 1.2, 0, 0, Math.PI * 2);
      ctx.fill();
      for (let i = 0; i < 6; i++) {
        const sx = -14 + rand(i + 31) * 28;
        const sy = 16.5 + rand(i + 41) * 4;
        ctx.fillStyle = rgb6([255, 255, 255], p.padSnowAmt * (0.4 + rand(i + 51) * 0.5));
        ctx.beginPath();
        ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const cy = -2 + bob;
    ctx.save();
    ctx.translate(0, cy);
    ctx.save();
    ctx.translate(0, -cy);
    ctx.fillStyle = rgb6([0, 0, 0], 0.12 + p.shadowAmt * 0.14);
    ctx.beginPath();
    ctx.ellipse(2.5, 17.5, 9 * s, 3.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.strokeStyle = rgb6([96, 64, 30]);
    ctx.lineWidth = 2.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(0.5 * s, -10 * s);
    ctx.quadraticCurveTo(1.6 * s, -14 * s, 2.6 * s, -16.5 * s);
    ctx.stroke();
    ctx.strokeStyle = rgb6([138, 96, 50]);
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(0.5 * s, -10 * s);
    ctx.quadraticCurveTo(1.6 * s, -14 * s, 2.6 * s, -16.5 * s);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.save();
    ctx.translate(-1.5 * s, -13.5 * s);
    ctx.rotate(-0.5);
    ctx.fillStyle = rgb6(p.leaf);
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-5.5 * s, -1.5 * s, -8 * s, -5.5 * s);
    ctx.quadraticCurveTo(-3.5 * s, -3 * s, 0, 0);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb6([from3(p.leaf, -50)[0], from3(p.leaf, -50)[1], from3(p.leaf, -50)[2]]);
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-4 * s, -2.4 * s, -7 * s, -5 * s);
    ctx.stroke();
    ctx.restore();
    ctx.save();
    pearPath(ctx, s);
    ctx.fillStyle = rgb6(p.outline);
    ctx.shadowColor = "transparent";
    ctx.fill();
    ctx.restore();
    ctx.save();
    pearPath(ctx, s * 0.93);
    ctx.clip();
    const bodyGrad = ctx.createLinearGradient(-7 * s, -10 * s, 8 * s, 16 * s);
    bodyGrad.addColorStop(0, rgb6(p.skinTop));
    bodyGrad.addColorStop(0.5, rgb6(p.skinMid));
    bodyGrad.addColorStop(1, rgb6(p.skinBot));
    ctx.fillStyle = bodyGrad;
    ctx.fillRect(-14, -20, 28, 42);
    if (p.blush > 0.01) {
      const bg = ctx.createRadialGradient(-4 * s, 8 * s, 1, -4 * s, 8 * s, 8 * s);
      bg.addColorStop(0, rgb6([240, 120, 96], p.blush * 0.55));
      bg.addColorStop(1, rgb6([240, 120, 96], 0));
      ctx.fillStyle = bg;
      ctx.fillRect(-14, -20, 28, 42);
    }
    if (p.freckleAmt > 0.01) {
      for (let i = 0; i < 8; i++) {
        const fx = -5 + rand(i + 61) * 10;
        const fy = 0 + rand(i + 71) * 14;
        ctx.fillStyle = rgb6([150, 96, 30], p.freckleAmt * 0.5);
        ctx.beginPath();
        ctx.arc(fx * s, fy * s - 4, 0.5, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.gloss > 0.01) {
      ctx.fillStyle = rgb6([255, 255, 255], p.gloss * 0.6);
      ctx.beginPath();
      ctx.ellipse(-4 * s, -3 * s, 2.2 * s, 4.2 * s, -0.35, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb6([255, 255, 255], p.gloss * 0.35);
      ctx.beginPath();
      ctx.ellipse(-5.4 * s, 4 * s, 1, 2.2, -0.3, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.frostAmt > 0.01) {
      ctx.fillStyle = rgb6([214, 230, 244], p.frostAmt * 0.4);
      ctx.fillRect(-14, -20, 28, 42);
      for (let i = 0; i < 10; i++) {
        const fx = -7 + rand(i + 81) * 14;
        const fy = -8 + rand(i + 91) * 24;
        ctx.fillStyle = rgb6([255, 255, 255], p.frostAmt * (0.3 + rand(i + 101) * 0.4));
        ctx.beginPath();
        ctx.arc(fx * s, fy * s, 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.restore();
    if (p.snowCapAmt > 0.01) {
      ctx.fillStyle = rgb6([244, 249, 255], 0.85);
      ctx.save();
      pearPath(ctx, s * 0.93);
      ctx.clip();
      ctx.beginPath();
      ctx.ellipse(-1 * s, -8 * s, 6.5 * s * p.snowCapAmt + 1, 3.2 * p.snowCapAmt + 0.6, -0.18, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.fillStyle = rgb6([248, 251, 255], 0.9);
      ctx.beginPath();
      ctx.arc(2.6 * s, -16.5 * s, 1.6 * p.snowCapAmt + 0.5, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  function from3(c, d) {
    return [
      Math.max(0, Math.min(255, c[0] + d)),
      Math.max(0, Math.min(255, c[1] + d)),
      Math.max(0, Math.min(255, c[2] + d))
    ];
  }
  var SP6 = {
    // Spring — small-ish, green unripe, matte; bright dewy lime pad + blossom.
    Spring: {
      skinTop: [178, 214, 120],
      skinMid: [138, 184, 84],
      skinBot: [92, 134, 54],
      outline: [58, 92, 38],
      leaf: [120, 196, 86],
      padGrass: [134, 206, 96],
      padGrassDark: [86, 156, 70],
      soil: [120, 92, 56],
      light: [236, 248, 224],
      lightAmt: 0.5,
      shadowAmt: 0.35,
      ripeness: 0.25,
      gloss: 0.1,
      blush: 0,
      freckleAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK: ripe yellow-green, warm blush, gentle gloss; saturated pad.
    Summer: {
      skinTop: [232, 230, 138],
      skinMid: [196, 206, 96],
      skinBot: [142, 158, 58],
      outline: [86, 96, 36],
      leaf: [86, 162, 64],
      padGrass: [104, 178, 78],
      padGrassDark: [64, 130, 56],
      soil: [110, 82, 48],
      light: [255, 248, 214],
      lightAmt: 0.65,
      shadowAmt: 0.55,
      ripeness: 1,
      gloss: 0.55,
      blush: 0.7,
      freckleAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — golden-ripe warm amber, faint freckles; leaf turning, olive-tan pad.
    Autumn: {
      skinTop: [244, 206, 116],
      skinMid: [224, 162, 64],
      skinBot: [166, 104, 34],
      outline: [104, 64, 22],
      leaf: [196, 138, 52],
      padGrass: [150, 150, 84],
      padGrassDark: [108, 104, 60],
      soil: [108, 78, 44],
      light: [250, 224, 168],
      lightAmt: 0.45,
      shadowAmt: 0.42,
      ripeness: 0.9,
      gloss: 0.2,
      blush: 0.3,
      freckleAmt: 0.7,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.8
    },
    // Winter — pale frost dusting (cool), fruit visible; snow cap + pad snow.
    Winter: {
      skinTop: [214, 214, 188],
      skinMid: [184, 184, 156],
      skinBot: [134, 138, 124],
      outline: [86, 90, 92],
      leaf: [150, 158, 140],
      padGrass: [196, 210, 218],
      padGrassDark: [150, 170, 186],
      soil: [120, 110, 102],
      light: [222, 234, 248],
      lightAmt: 0.5,
      shadowAmt: 0.3,
      ripeness: 0.75,
      gloss: 0.18,
      blush: 0,
      freckleAmt: 0,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var BOB_W = 1.5;
  var BOB_A = 1.1;
  var bobAt6 = (t) => -BOB_A * (1 - Math.cos(BOB_W * t)) * 0.5;
  function summerGlint(ctx, t, bob) {
    const prog = t * 0.35 % 1;
    const gx = -5 + prog * 9;
    const gy = -8 + prog * 18 + bob;
    ctx.save();
    ctx.globalCompositeOperation = "lighter";
    ctx.fillStyle = "rgba(255,255,240,0.5)";
    ctx.beginPath();
    ctx.ellipse(gx, gy, 1.8, 3.4, -0.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  function autumnLeafFlutter(ctx, t, bob) {
    const flut = Math.sin(t * 2.6) * 0.16 + Math.sin(t * 4.1) * 0.06;
    ctx.save();
    ctx.translate(-1.5, -15.5 + bob);
    ctx.rotate(-0.5 + flut);
    ctx.fillStyle = rgb6([210, 150, 58], 0.85);
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-5, -1.4, -7.4, -5.2);
    ctx.quadraticCurveTo(-3.2, -2.8, 0, 0);
    ctx.closePath();
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  function winterSnow(ctx, t) {
    const seeds = [
      [-7, 0, 0.7],
      [6, 0.5, 0.6],
      [1, 0.25, 0.55]
    ];
    ctx.save();
    seeds.forEach(([fx, phase, r]) => {
      const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
      const fy = -20 + prog * 40;
      const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
      ctx.fillStyle = rgb6([255, 255, 255], 0.5 + 0.4 * Math.sin(prog * Math.PI));
      ctx.beginPath();
      ctx.arc(dx, fy, r, 0, Math.PI * 2);
      ctx.fill();
    });
    const sheen2 = 0.08 + 0.06 * (0.5 + 0.5 * Math.sin(t * 0.9));
    ctx.fillStyle = rgb6([210, 228, 246], sheen2);
    ctx.beginPath();
    ctx.ellipse(-3, 0, 6, 9, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  function springDew(ctx, t, bob) {
    const sh = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
    ctx.save();
    ctx.fillStyle = rgb6([255, 255, 255], sh);
    ctx.beginPath();
    ctx.arc(-4.5, -2 + bob, 1 + sh, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.globalAlpha = 1;
  }
  var draw2 = (season) => (ctx) => {
    paint6(ctx, SP6[season], 0);
  };
  var anim2 = (season) => (ctx, t) => {
    const bob = bobAt6(t);
    paint6(ctx, SP6[season], bob);
    if (season === "Spring") springDew(ctx, t, bob);
    else if (season === "Summer") summerGlint(ctx, t, bob);
    else if (season === "Autumn") autumnLeafFlutter(ctx, t, bob);
    else if (season === "Winter") winterSnow(ctx, t);
  };
  var springToSummer4 = (ctx, pp) => {
    const u = smoother6(clamp017(pp));
    paint6(ctx, lerpP6(SP6.Spring, SP6.Summer, u), 0);
  };
  var summerToAutumn4 = (ctx, pp) => {
    const u = smoother6(clamp017(pp));
    paint6(ctx, lerpP6(SP6.Summer, SP6.Autumn, u), 0);
  };
  var autumnToWinter4 = (ctx, pp) => {
    const u = smoother6(clamp017(pp));
    paint6(ctx, lerpP6(SP6.Autumn, SP6.Winter, u), 0);
  };
  var VARIANTS8 = {
    Spring: { draw: draw2("Spring"), anim: anim2("Spring") },
    Summer: { draw: draw2("Summer"), anim: anim2("Summer") },
    Autumn: { draw: draw2("Autumn"), anim: anim2("Autumn") },
    Winter: { draw: draw2("Winter"), anim: anim2("Winter") }
  };
  var TRANSITIONS8 = {
    0: springToSummer4,
    1: summerToAutumn4,
    2: autumnToWinter4
  };

  // src/textures/seasonal/fruit/lemon.ts
  function clamp018(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function rgb7(c, a = 1) {
    const r = Math.round(clamp018(c[0] / 255) * 255);
    const g = Math.round(clamp018(c[1] / 255) * 255);
    const b = Math.round(clamp018(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${a})`;
  }
  function mix(a, b, t) {
    const k = clamp018(t);
    return [
      a[0] + (b[0] - a[0]) * k,
      a[1] + (b[1] - a[1]) * k,
      a[2] + (b[2] - a[2]) * k
    ];
  }
  var SP7 = {
    // Fresh pastel, cool-bright light, dewy lime pad, tiny blossom.
    Spring: {
      skinDark: [150, 168, 60],
      skinMid: [196, 214, 96],
      skinLight: [232, 244, 160],
      leaf: [108, 196, 84],
      leafVein: [70, 142, 56],
      padGrass: [150, 222, 110],
      padShade: [96, 168, 78],
      soil: [96, 70, 42],
      outline: [92, 96, 48],
      lightTint: [222, 240, 255],
      lightAmt: 0.16,
      ripeness: 0.28,
      gloss: 0.18,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0,
      shadowAmt: 0.2
    },
    // Peak: richest saturated yellow, glossy, warm light, strong shadow.
    Summer: {
      skinDark: [212, 158, 22],
      skinMid: [248, 208, 44],
      skinLight: [255, 244, 150],
      leaf: [70, 156, 56],
      leafVein: [44, 110, 40],
      padGrass: [86, 174, 66],
      padShade: [54, 122, 48],
      soil: [104, 74, 40],
      outline: [120, 84, 18],
      lightTint: [255, 244, 198],
      lightAmt: 0.22,
      ripeness: 0.72,
      gloss: 0.62,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      shadowAmt: 0.3
    },
    // Gold/rust, fully ripe waxy, amber low light, olive-tan pad, fallen leaves.
    Autumn: {
      skinDark: [196, 140, 18],
      skinMid: [240, 192, 40],
      skinLight: [252, 226, 122],
      leaf: [196, 150, 52],
      leafVein: [140, 96, 30],
      padGrass: [150, 146, 78],
      padShade: [104, 100, 54],
      soil: [96, 64, 32],
      outline: [110, 72, 16],
      lightTint: [248, 206, 130],
      lightAmt: 0.24,
      ripeness: 0.92,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.8,
      shadowAmt: 0.26
    },
    // Cool blue-grey light, pad snow + frost, snow cap + frost dusting on skin.
    Winter: {
      skinDark: [198, 168, 64],
      skinMid: [236, 206, 96],
      skinLight: [250, 232, 158],
      leaf: [120, 150, 110],
      leafVein: [84, 112, 84],
      padGrass: [196, 214, 224],
      padShade: [150, 174, 196],
      soil: [88, 92, 104],
      outline: [86, 96, 116],
      lightTint: [196, 220, 255],
      lightAmt: 0.26,
      ripeness: 0.82,
      gloss: 0.3,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      shadowAmt: 0.18
    }
  };
  function lerpN(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpP7(a, b, t) {
    const k = clamp018(t);
    return {
      skinDark: mix(a.skinDark, b.skinDark, k),
      skinMid: mix(a.skinMid, b.skinMid, k),
      skinLight: mix(a.skinLight, b.skinLight, k),
      leaf: mix(a.leaf, b.leaf, k),
      leafVein: mix(a.leafVein, b.leafVein, k),
      padGrass: mix(a.padGrass, b.padGrass, k),
      padShade: mix(a.padShade, b.padShade, k),
      soil: mix(a.soil, b.soil, k),
      outline: mix(a.outline, b.outline, k),
      lightTint: mix(a.lightTint, b.lightTint, k),
      lightAmt: lerpN(a.lightAmt, b.lightAmt, k),
      ripeness: lerpN(a.ripeness, b.ripeness, k),
      gloss: lerpN(a.gloss, b.gloss, k),
      frostAmt: lerpN(a.frostAmt, b.frostAmt, k),
      snowCapAmt: lerpN(a.snowCapAmt, b.snowCapAmt, k),
      padSnowAmt: lerpN(a.padSnowAmt, b.padSnowAmt, k),
      blossomAmt: lerpN(a.blossomAmt, b.blossomAmt, k),
      fallenLeafAmt: lerpN(a.fallenLeafAmt, b.fallenLeafAmt, k),
      shadowAmt: lerpN(a.shadowAmt, b.shadowAmt, k)
    };
  }
  var BODY_CX = 0;
  var BODY_CY = 6;
  var BODY_RX = 13;
  var BODY_RY = 15;
  var TILT = -0.12;
  var NUB_TOP = [
    BODY_CX + Math.sin(TILT) * (BODY_RY + 1.5),
    BODY_CY - Math.cos(TILT) * (BODY_RY + 1.5)
  ];
  var NUB_BOT = [
    BODY_CX - Math.sin(TILT) * (BODY_RY + 1.5),
    BODY_CY + Math.cos(TILT) * (BODY_RY + 1.5)
  ];
  var DIMPLES = [
    [-6, -4],
    [-2, -7],
    [3, -5],
    [7, -1],
    [-8, 2],
    [-3, 1],
    [2, 3],
    [6, 4],
    [-5, 8],
    [0, 9],
    [5, 9],
    [-9, -2],
    [9, 2],
    [-1, -3],
    [4, 7]
  ];
  function paint7(ctx, p, bob) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineCap = "round";
      ctx.lineJoin = "round";
      drawPad(ctx, p);
      ctx.save();
      ctx.translate(0, -bob);
      drawLemon(ctx, p);
      ctx.restore();
      drawAmbient(ctx, p);
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function drawPad(ctx, p) {
    ctx.fillStyle = rgb7(p.padShade, 0.35 * p.shadowAmt + 0.18);
    ctx.beginPath();
    ctx.ellipse(2.5, 21, 15, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(p.padShade);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb7(mix(p.padGrass, p.padShade, 0.4));
    ctx.lineWidth = 1.4;
    for (let i = 0; i < 13; i++) {
      const a = Math.PI * (0.04 + i / 12 * 0.92);
      const ex = Math.cos(a) * 18;
      const ey = 19 + Math.sin(a) * 5.4;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex - 0.6, ey + 2.2);
      ctx.stroke();
    }
    ctx.fillStyle = rgb7(mix(p.padGrass, [255, 255, 255], 0.28), 0.5);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(p.soil, 0.5);
    ctx.beginPath();
    ctx.ellipse(0, 18.5, 9, 2.2, 0, 0, Math.PI * 2);
    ctx.fill();
    drawPadDressing(ctx, p);
  }
  function drawPadDressing(ctx, p) {
    if (p.blossomAmt > 0.02) {
      const a = clamp018(p.blossomAmt);
      ctx.save();
      ctx.globalAlpha = a;
      ctx.translate(-12, 17.5);
      for (let i = 0; i < 5; i++) {
        const ang = i / 5 * Math.PI * 2;
        ctx.fillStyle = "rgba(255,236,246,1)";
        ctx.beginPath();
        ctx.ellipse(Math.cos(ang) * 2.4, Math.sin(ang) * 1.7, 1.9, 1.4, ang, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.fillStyle = "rgba(255,206,86,1)";
      ctx.beginPath();
      ctx.arc(0, 0, 1.4, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.globalAlpha = 1;
    }
    if (p.fallenLeafAmt > 0.02) {
      const a = clamp018(p.fallenLeafAmt);
      const leaves = [
        [-11, 19.5, 0.5, [206, 120, 42]],
        [12, 18.5, -0.7, [190, 92, 36]]
      ];
      leaves.forEach(([lx, ly, rot, col]) => {
        ctx.save();
        ctx.globalAlpha = a;
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb7(col);
        ctx.beginPath();
        ctx.ellipse(0, 0, 4, 2.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb7(mix(col, [80, 40, 16], 0.5));
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(-4, 0);
        ctx.lineTo(4, 0);
        ctx.stroke();
        ctx.restore();
      });
      ctx.globalAlpha = 1;
    }
    if (p.padSnowAmt > 0.02) {
      const a = clamp018(p.padSnowAmt);
      ctx.save();
      ctx.globalAlpha = a;
      const g = ctx.createLinearGradient(0, 14, 0, 24);
      g.addColorStop(0, "rgba(244,248,255,1)");
      g.addColorStop(1, "rgba(206,222,240,1)");
      ctx.fillStyle = g;
      ctx.beginPath();
      ctx.ellipse(0, 18.5, 17, 5, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,1)";
      [[-10, 18], [-3, 20], [6, 17.5], [11, 19.5]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.9, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
      ctx.globalAlpha = 1;
    }
  }
  function drawLemon(ctx, p) {
    ctx.fillStyle = rgb7(p.outline, 0.9);
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX + 1.4, BODY_RY + 1.4, TILT, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.ellipse(NUB_TOP[0], NUB_TOP[1], 3.2, 2.6, TILT, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.ellipse(NUB_BOT[0], NUB_BOT[1], 2.8, 2.4, TILT, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(p.skinDark);
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX, BODY_RY, TILT, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX, BODY_RY, TILT, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb7(p.skinMid);
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX, BODY_RY, TILT, 0, Math.PI * 2);
    ctx.fill();
    const lg = ctx.createRadialGradient(-5, BODY_CY - 6, 2, -3, BODY_CY - 3, BODY_RX + 4);
    lg.addColorStop(0, rgb7(p.skinLight, 0.95));
    lg.addColorStop(0.55, rgb7(p.skinMid, 0));
    lg.addColorStop(1, rgb7(p.skinMid, 0));
    ctx.fillStyle = lg;
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX, BODY_RY, TILT, 0, Math.PI * 2);
    ctx.fill();
    const sg = ctx.createRadialGradient(6, BODY_CY + 7, 2, 6, BODY_CY + 7, BODY_RX + 6);
    sg.addColorStop(0, rgb7(p.skinDark, 0.45));
    sg.addColorStop(0.7, rgb7(p.skinDark, 0));
    ctx.fillStyle = sg;
    ctx.beginPath();
    ctx.ellipse(BODY_CX, BODY_CY, BODY_RX, BODY_RY, TILT, 0, Math.PI * 2);
    ctx.fill();
    const dimC = 0.18 + p.ripeness * 0.32;
    DIMPLES.forEach(([dx, dy]) => {
      ctx.fillStyle = rgb7(p.skinDark, dimC * 0.7);
      ctx.beginPath();
      ctx.arc(dx, BODY_CY + dy, 0.95, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb7(p.skinLight, dimC * 0.55);
      ctx.beginPath();
      ctx.arc(dx - 0.7, BODY_CY + dy - 0.7, 0.6, 0, Math.PI * 2);
      ctx.fill();
    });
    if (p.frostAmt > 0.01) {
      const fa = clamp018(p.frostAmt);
      ctx.fillStyle = `rgba(212,232,255,${0.34 * fa})`;
      ctx.beginPath();
      ctx.ellipse(-2, BODY_CY - 5, BODY_RX - 2, BODY_RY - 4, TILT, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = `rgba(255,255,255,${0.7 * fa})`;
      DIMPLES.forEach(([dx, dy], i) => {
        if (i % 2 === 0 && dy < 4) {
          ctx.beginPath();
          ctx.arc(dx, BODY_CY + dy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        }
      });
    }
    const glossA = 0.25 + p.gloss * 0.6;
    ctx.fillStyle = `rgba(255,255,255,${glossA})`;
    ctx.beginPath();
    ctx.ellipse(-5, BODY_CY - 6, 3.6 - p.gloss * 1.2, 6 - p.gloss * 2, TILT - 0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.snowCapAmt > 0.01) {
      const sa = clamp018(p.snowCapAmt);
      ctx.fillStyle = `rgba(248,252,255,${0.95 * sa})`;
      ctx.beginPath();
      ctx.ellipse(-3.5, BODY_CY - 10, 8.5 * sa + 2, 4.5 * sa + 1.5, TILT - 0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = `rgba(210,226,244,${0.6 * sa})`;
      ctx.beginPath();
      ctx.ellipse(-2, BODY_CY - 8, 7 * sa + 1.5, 2.4, TILT, 0, Math.PI * 2);
      ctx.fill();
    }
    drawLeaf(ctx, p);
  }
  function drawLeaf(ctx, p) {
    ctx.save();
    ctx.translate(NUB_TOP[0] + 1, NUB_TOP[1] - 1);
    ctx.rotate(-0.5);
    ctx.fillStyle = rgb7(mix(p.leaf, p.leafVein, 0.5));
    ctx.beginPath();
    ctx.ellipse(5.5, 0, 7, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(p.leaf);
    ctx.beginPath();
    ctx.ellipse(5, -0.4, 6.4, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb7(mix(p.leaf, [255, 255, 255], 0.4), 0.6);
    ctx.beginPath();
    ctx.ellipse(3.5, -1, 2.8, 1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb7(p.leafVein);
    ctx.lineWidth = 0.9;
    ctx.beginPath();
    ctx.moveTo(-0.5, 0);
    ctx.lineTo(11, -0.6);
    ctx.stroke();
    ctx.restore();
  }
  function drawAmbient(ctx, p) {
    if (p.lightAmt <= 0) return;
    const g = ctx.createRadialGradient(-8, -6, 4, 0, 4, 34);
    g.addColorStop(0, rgb7(p.lightTint, p.lightAmt));
    g.addColorStop(1, rgb7(p.lightTint, 0));
    ctx.fillStyle = g;
    ctx.fillRect(-24, -24, 48, 48);
  }
  function bobAt7(t, amp = 1.1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function microMotion(ctx, season, t) {
    switch (season) {
      case "Spring": {
        const a = 0.18 + 0.22 * (0.5 + 0.5 * Math.sin(t * 2.2));
        ctx.fillStyle = `rgba(255,255,255,${a})`;
        ctx.beginPath();
        ctx.arc(-4, BODY_CY - 4, 1.6 + a, 0, Math.PI * 2);
        ctx.fill();
        break;
      }
      case "Summer": {
        const prog = t * 0.35 % 1;
        const gx = -7 + prog * 14;
        const gy = BODY_CY - 8 + prog * 14;
        ctx.fillStyle = "rgba(255,255,255,0.8)";
        ctx.beginPath();
        ctx.ellipse(gx, gy, 1.8, 3, -0.6, 0, Math.PI * 2);
        ctx.fill();
        break;
      }
      case "Autumn": {
        const fl = Math.sin(t * 2.6) * 1.2;
        ctx.save();
        ctx.translate(NUB_TOP[0] + 9, NUB_TOP[1] - 5);
        ctx.rotate(fl * 0.12);
        ctx.fillStyle = "rgba(212,150,60,0.7)";
        ctx.beginPath();
        ctx.ellipse(fl * 0.4, 0, 2.4, 1, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        break;
      }
      case "Winter": {
        const seeds = [
          [-9, 0],
          [-2, 0.4],
          [6, 0.7],
          [11, 0.25]
        ];
        ctx.fillStyle = "rgba(255,255,255,0.9)";
        seeds.forEach(([fx, ph]) => {
          const prog = ((t / 3.4 + ph) % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const dx = fx + Math.sin(prog * Math.PI * 2 + ph * 6) * 2.4;
          ctx.beginPath();
          ctx.arc(dx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
        });
        const sheen2 = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.9));
        ctx.fillStyle = `rgba(200,224,255,${sheen2})`;
        ctx.beginPath();
        ctx.ellipse(-3, BODY_CY - 4, 7, 3, TILT, 0, Math.PI * 2);
        ctx.fill();
        break;
      }
    }
  }
  var smoother7 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function makeDraw5(season) {
    return (ctx) => paint7(ctx, SP7[season], 0);
  }
  function makeAnim(season) {
    return (ctx, t) => {
      paint7(ctx, SP7[season], bobAt7(t));
      ctx.save();
      try {
        ctx.translate(0, -bobAt7(t));
        microMotion(ctx, season, t);
      } catch {
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition6(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother7(clamp018(pp));
      paint7(ctx, lerpP7(SP7[from], SP7[to], k), 0);
    };
  }
  var VARIANTS9 = {
    Spring: { draw: makeDraw5("Spring"), anim: makeAnim("Spring") },
    Summer: { draw: makeDraw5("Summer"), anim: makeAnim("Summer") },
    Autumn: { draw: makeDraw5("Autumn"), anim: makeAnim("Autumn") },
    Winter: { draw: makeDraw5("Winter"), anim: makeAnim("Winter") }
  };
  var TRANSITIONS9 = {
    0: makeTransition6(0),
    // Spring → Summer
    1: makeTransition6(1),
    // Summer → Autumn
    2: makeTransition6(2)
    // Autumn → Winter
  };

  // src/textures/seasonal/fruit/blackberry.ts
  function clamp019(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother8 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp10(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB3(a, b, t) {
    return [lerp10(a[0], b[0], t), lerp10(a[1], b[1], t), lerp10(a[2], b[2], t)];
  }
  function rgb8(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba2(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp019(a)})`;
  }
  function lerpP8(a, b, t) {
    return {
      berryLight: lerpRGB3(a.berryLight, b.berryLight, t),
      berryMid: lerpRGB3(a.berryMid, b.berryMid, t),
      berryDark: lerpRGB3(a.berryDark, b.berryDark, t),
      leafLight: lerpRGB3(a.leafLight, b.leafLight, t),
      leafMid: lerpRGB3(a.leafMid, b.leafMid, t),
      leafDark: lerpRGB3(a.leafDark, b.leafDark, t),
      sprig: lerpRGB3(a.sprig, b.sprig, t),
      padGrass: lerpRGB3(a.padGrass, b.padGrass, t),
      padDark: lerpRGB3(a.padDark, b.padDark, t),
      soil: lerpRGB3(a.soil, b.soil, t),
      outline: lerpRGB3(a.outline, b.outline, t),
      light: lerpRGB3(a.light, b.light, t),
      lightAmt: lerp10(a.lightAmt, b.lightAmt, t),
      ripeness: lerp10(a.ripeness, b.ripeness, t),
      gloss: lerp10(a.gloss, b.gloss, t),
      sunken: lerp10(a.sunken, b.sunken, t),
      frostAmt: lerp10(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp10(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp10(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp10(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp10(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP2(p) {
    return {
      ...p,
      lightAmt: clamp019(p.lightAmt),
      ripeness: clamp019(p.ripeness),
      gloss: clamp019(p.gloss),
      sunken: clamp019(p.sunken),
      frostAmt: clamp019(p.frostAmt),
      snowCapAmt: clamp019(p.snowCapAmt),
      padSnowAmt: clamp019(p.padSnowAmt),
      blossomAmt: clamp019(p.blossomAmt),
      fallenLeafAmt: clamp019(p.fallenLeafAmt)
    };
  }
  var SP8 = {
    // Spring — fresh pastel; UNRIPE green/pale-red berries, matte; bright lime
    // dewy pad + tiny blossom; cool-bright light.
    Spring: {
      berryLight: [186, 214, 120],
      berryMid: [132, 176, 78],
      berryDark: [92, 128, 60],
      leafLight: [150, 210, 96],
      leafMid: [96, 168, 64],
      leafDark: [52, 112, 44],
      sprig: [120, 150, 70],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [44, 60, 34],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.1,
      gloss: 0.12,
      sunken: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK. Plump GLOSSY BLACK ripe berries with tiny highlights;
    // saturated mid-green leaf + pad; warm light, strong gloss.
    Summer: {
      berryLight: [86, 60, 96],
      berryMid: [40, 26, 50],
      berryDark: [16, 10, 22],
      leafLight: [120, 196, 78],
      leafMid: [70, 150, 54],
      leafDark: [38, 98, 40],
      sprig: [110, 86, 50],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [16, 10, 22],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 1,
      gloss: 0.95,
      sunken: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — OVERRIPE very dark berries (a touch sunken/matte); the leaf
    // reddening; olive-tan browning pad with a couple of fallen leaves.
    Autumn: {
      berryLight: [62, 40, 60],
      berryMid: [30, 18, 32],
      berryDark: [14, 8, 16],
      leafLight: [206, 120, 56],
      leafMid: [168, 78, 40],
      leafDark: [104, 42, 28],
      sprig: [128, 92, 48],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [24, 14, 22],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.3,
      sunken: 0.85,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; FROST-DUSTED dark berries + small snow cap,
    // still clearly visible; snowy pad. The berries keep their dark colour under.
    Winter: {
      berryLight: [78, 66, 96],
      berryMid: [42, 34, 56],
      berryDark: [22, 18, 32],
      leafLight: [128, 150, 150],
      leafMid: [92, 116, 120],
      leafDark: [60, 82, 92],
      sprig: [112, 110, 104],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [40, 36, 52],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.95,
      gloss: 0.22,
      sunken: 0.25,
      frostAmt: 0.72,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var BERRIES = [
    [-6.2, 5, 5.2],
    // lower-left
    [5.4, 6.2, 5],
    // lower-right
    [-0.6, 8.6, 4.6],
    // bottom-centre (tucked low)
    [-3, -1, 4.8],
    // upper-left
    [4, -1.4, 4.4]
    // upper-right (hero, catches most light)
  ];
  var DRUPELETS = [
    [0, 0, 0.42],
    [0, -0.62, 0.36],
    [0.58, -0.3, 0.34],
    [0.58, 0.34, 0.34],
    [0, 0.64, 0.34],
    [-0.58, 0.34, 0.34],
    [-0.58, -0.3, 0.34]
  ];
  function drawBerry(ctx, cx, cy, r, p) {
    ctx.fillStyle = rgb8(p.outline);
    ctx.beginPath();
    ctx.arc(cx, cy, r + 0.9, 0, Math.PI * 2);
    ctx.fill();
    const bodyMid = lerpRGB3(p.berryMid, p.berryDark, 0.55 * p.sunken);
    for (const [dx, dy, dr] of DRUPELETS) {
      const px = cx + dx * r;
      const py = cy + dy * r;
      const pr = dr * r;
      ctx.fillStyle = rgb8(p.berryDark);
      ctx.beginPath();
      ctx.arc(px, py, pr, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb8(bodyMid);
      ctx.beginPath();
      ctx.arc(px, py, pr * 0.86, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb8(p.berryLight);
      ctx.globalAlpha = 0.85;
      ctx.beginPath();
      ctx.arc(px - pr * 0.3, py - pr * 0.34, pr * 0.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba2([255, 255, 255], 0.25 + 0.65 * p.gloss);
        ctx.beginPath();
        ctx.arc(px - pr * 0.36, py - pr * 0.42, pr * 0.2 + 0.3, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.fillStyle = rgba2(p.berryDark, 0.4);
    ctx.beginPath();
    ctx.ellipse(cx, cy + r * 0.55, r * 0.7, r * 0.4, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.frostAmt > 0.02) {
      ctx.fillStyle = rgba2([214, 232, 250], 0.3 * p.frostAmt);
      ctx.beginPath();
      ctx.arc(cx - r * 0.2, cy - r * 0.25, r * 0.78, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba2([238, 248, 255], 0.75 * p.frostAmt);
      for (const [dx, dy] of [[-0.4, -0.5], [0.3, -0.4], [-0.1, 0.2], [0.5, 0.1]]) {
        ctx.beginPath();
        ctx.arc(cx + dx * r, cy + dy * r, 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
    }
  }
  function drawLeaf2(ctx, p) {
    ctx.save();
    ctx.translate(7.5, -3.5);
    ctx.rotate(-0.5);
    ctx.fillStyle = rgb8(p.outline);
    ctx.beginPath();
    ctx.moveTo(-7.5, 0);
    ctx.quadraticCurveTo(-2, -5.2, 7, -1.6);
    ctx.quadraticCurveTo(-1.5, 0.8, -7.5, 0);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb8(p.leafMid);
    ctx.beginPath();
    ctx.moveTo(-6.6, 0);
    ctx.quadraticCurveTo(-2, -4.5, 6.2, -1.5);
    ctx.quadraticCurveTo(-1.5, 0.4, -6.6, 0);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgba2(p.leafLight, 0.8);
    ctx.beginPath();
    ctx.moveTo(-5.6, -0.6);
    ctx.quadraticCurveTo(-1.5, -4, 5.4, -1.6);
    ctx.quadraticCurveTo(-0.5, -1.2, -5.6, -0.6);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb8(p.leafDark);
    ctx.lineWidth = 0.9;
    ctx.beginPath();
    ctx.moveTo(-6, -0.2);
    ctx.quadraticCurveTo(-1, -1.4, 6, -1.5);
    ctx.stroke();
    ctx.restore();
  }
  function paint8(ctx, raw, bob) {
    const p = clampP2(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba2(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb8(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb8(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb8(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba2([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba2([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba2([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba2([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba2(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba2([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba2(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba2([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb8(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, 14 + bob + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba2(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(2.5, 14 + bob + 2, 10, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.translate(0, bob);
      ctx.strokeStyle = rgb8(p.outline);
      ctx.lineWidth = 3.2;
      ctx.beginPath();
      ctx.moveTo(0.5, 13.5);
      ctx.quadraticCurveTo(2.5, 8, 3.5, 2);
      ctx.stroke();
      ctx.strokeStyle = rgb8(p.sprig);
      ctx.lineWidth = 1.8;
      ctx.beginPath();
      ctx.moveTo(0.5, 13.5);
      ctx.quadraticCurveTo(2.5, 8, 3.5, 2);
      ctx.stroke();
      drawLeaf2(ctx, p);
      const order = [3, 0, 2, 1, 4];
      for (const idx of order) {
        const b = BERRIES[idx];
        drawBerry(ctx, b[0], b[1], b[2], p);
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba2([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-7, -3.4);
        ctx.quadraticCurveTo(-2, -7.6, 1, -6.4);
        ctx.quadraticCurveTo(5, -8, 8, -3.6);
        ctx.quadraticCurveTo(4, -2.6, 1.5, -3.8);
        ctx.quadraticCurveTo(-2, -2.4, -7, -3.4);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba2([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, -3.4, 6.4, 1.5, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.restore();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba2(p.light, p.lightAmt));
        lg.addColorStop(1, rgba2(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt8(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw3(season) {
    return (ctx) => paint8(ctx, SP8[season], 0);
  }
  function anim3(season) {
    return (ctx, t) => {
      const bob = bobAt8(t);
      paint8(ctx, SP8[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -1 + bob + Math.sin(t * 1.1) * 1.2;
          ctx.beginPath();
          ctx.arc(2.5, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const gx = lerp10(-7, 7, prog);
          const gy = 2 + bob + Math.sin(prog * Math.PI) * -3;
          ctx.fillStyle = "rgba(255,255,255,0.85)";
          ctx.beginPath();
          ctx.ellipse(gx, gy, 1.5, 1.9, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.ellipse(gx - 2, gy + 1.4, 0.9, 1.2, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.16 * (0.5 + 0.5 * Math.sin(t * 1.6));
          const flx = Math.sin(t * 1.6) * 1.1;
          ctx.fillStyle = `rgba(255,220,170,${s})`;
          ctx.beginPath();
          ctx.ellipse(7.5 + flx, -4 + bob, 4.6, 2.4, -0.5, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(0, 3 + bob, 7, 4, -0.1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition7(fromIdx) {
    const from = SP8[SEASON_NAMES[fromIdx]];
    const to = SP8[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother8(clamp019(pp));
      paint8(ctx, lerpP8(from, to, k), 0);
    };
  }
  var springToSummer5 = makeTransition7(0);
  var summerToAutumn5 = makeTransition7(1);
  var autumnToWinter5 = makeTransition7(2);
  var VARIANTS10 = {
    Spring: { draw: draw3("Spring"), anim: anim3("Spring") },
    Summer: { draw: draw3("Summer"), anim: anim3("Summer") },
    Autumn: { draw: draw3("Autumn"), anim: anim3("Autumn") },
    Winter: { draw: draw3("Winter"), anim: anim3("Winter") }
  };
  var TRANSITIONS10 = {
    0: springToSummer5,
    1: summerToAutumn5,
    2: autumnToWinter5
  };

  // src/textures/seasonal/fruit/coconut.ts
  function clamp0110(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother9 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp11(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB4(a, b, t) {
    return [lerp11(a[0], b[0], t), lerp11(a[1], b[1], t), lerp11(a[2], b[2], t)];
  }
  function rgb9(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba3(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0110(a)})`;
  }
  function lerpP9(a, b, t) {
    return {
      huskLight: lerpRGB4(a.huskLight, b.huskLight, t),
      huskMid: lerpRGB4(a.huskMid, b.huskMid, t),
      huskDark: lerpRGB4(a.huskDark, b.huskDark, t),
      fibre: lerpRGB4(a.fibre, b.fibre, t),
      eye: lerpRGB4(a.eye, b.eye, t),
      tuftLight: lerpRGB4(a.tuftLight, b.tuftLight, t),
      tuftDark: lerpRGB4(a.tuftDark, b.tuftDark, t),
      padGrass: lerpRGB4(a.padGrass, b.padGrass, t),
      padDark: lerpRGB4(a.padDark, b.padDark, t),
      soil: lerpRGB4(a.soil, b.soil, t),
      outline: lerpRGB4(a.outline, b.outline, t),
      light: lerpRGB4(a.light, b.light, t),
      lightAmt: lerp11(a.lightAmt, b.lightAmt, t),
      ripeness: lerp11(a.ripeness, b.ripeness, t),
      gloss: lerp11(a.gloss, b.gloss, t),
      frostAmt: lerp11(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp11(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp11(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp11(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp11(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP3(p) {
    return {
      ...p,
      lightAmt: clamp0110(p.lightAmt),
      ripeness: clamp0110(p.ripeness),
      gloss: clamp0110(p.gloss),
      frostAmt: clamp0110(p.frostAmt),
      snowCapAmt: clamp0110(p.snowCapAmt),
      padSnowAmt: clamp0110(p.padSnowAmt),
      blossomAmt: clamp0110(p.blossomAmt),
      fallenLeafAmt: clamp0110(p.fallenLeafAmt)
    };
  }
  var SP9 = {
    // Spring — young green coconut: husk still green-tinged, dewy lime pad + blossom.
    Spring: {
      huskLight: [156, 178, 102],
      huskMid: [118, 142, 70],
      huskDark: [80, 100, 46],
      fibre: [96, 116, 56],
      eye: [70, 70, 40],
      tuftLight: [150, 216, 96],
      tuftDark: [76, 144, 54],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [48, 56, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.18,
      gloss: 0.2,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — maturing green-brown husk at PEAK saturation; mid-green pad, soft sheen.
    Summer: {
      huskLight: [186, 150, 86],
      huskMid: [150, 110, 58],
      huskDark: [104, 70, 36],
      fibre: [122, 86, 44],
      eye: [58, 40, 24],
      tuftLight: [140, 210, 78],
      tuftDark: [64, 138, 46],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [62, 40, 22],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.6,
      gloss: 0.85,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — full ripe brown fibrous coconut; olive-tan pad, fallen leaves.
    Autumn: {
      huskLight: [168, 122, 70],
      huskMid: [128, 84, 44],
      huskDark: [86, 52, 28],
      fibre: [104, 64, 32],
      eye: [50, 32, 20],
      tuftLight: [150, 150, 80],
      tuftDark: [104, 110, 52],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [56, 34, 20],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; brown coconut CLEARLY visible under snow cap + frost.
    Winter: {
      huskLight: [156, 118, 80],
      huskMid: [120, 86, 56],
      huskDark: [82, 56, 38],
      fibre: [104, 74, 48],
      eye: [54, 38, 28],
      tuftLight: [128, 168, 120],
      tuftDark: [82, 124, 80],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [52, 40, 34],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.9,
      gloss: 0.25,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var COCO_CX = 0;
  var COCO_CY = 7;
  var COCO_RX = 13.5;
  var COCO_RY = 14;
  var TUFT_X = -2;
  var TUFT_Y = COCO_CY - COCO_RY;
  var EYES = [
    [-4.4, COCO_CY - 2.4, 1.7],
    [4.2, COCO_CY - 2.6, 1.7],
    [-0.2, COCO_CY + 1.6, 1.9]
  ];
  function coconutBodyPath(ctx, bob) {
    ctx.beginPath();
    ctx.ellipse(COCO_CX, COCO_CY + bob, COCO_RX, COCO_RY, 0, 0, Math.PI * 2);
    ctx.closePath();
  }
  function drawTuft(ctx, p, bob, sway) {
    const baseX = COCO_CX + TUFT_X;
    const baseY = TUFT_Y + bob + 1.5;
    const blades = [
      [-3.6, -7.5],
      [-1.2, -10.5],
      [1.4, -9.5],
      [3.6, -6.5]
    ];
    blades.forEach(([dx, dy], i) => {
      const tipX = baseX + dx + sway * (0.6 + i * 0.18);
      const tipY = baseY + dy;
      ctx.strokeStyle = rgb9(p.tuftDark);
      ctx.lineWidth = 2.6;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(baseX + dx * 0.3, baseY);
      ctx.quadraticCurveTo(baseX + dx * 0.8 + sway * 0.4, baseY + dy * 0.5, tipX, tipY);
      ctx.stroke();
      ctx.strokeStyle = rgb9(p.tuftLight);
      ctx.lineWidth = 1.3;
      ctx.beginPath();
      ctx.moveTo(baseX + dx * 0.3, baseY);
      ctx.quadraticCurveTo(baseX + dx * 0.8 + sway * 0.4, baseY + dy * 0.5, tipX, tipY);
      ctx.stroke();
    });
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb9(p.tuftDark);
    ctx.beginPath();
    ctx.ellipse(baseX, baseY + 0.4, 3, 2, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function paint9(ctx, raw, bob, tuftSway = 0) {
    const p = clampP3(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba3(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb9(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb9(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb9(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba3([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba3([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba3([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba3([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba3(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba3([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba3(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba3([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb9(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, COCO_CY + COCO_RY + bob + 0.5, 9, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba3(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(3, COCO_CY + COCO_RY + bob + 1, 12, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      coconutBodyPath(ctx, bob);
      ctx.lineWidth = 2.4;
      ctx.strokeStyle = rgb9(p.outline);
      ctx.stroke();
      ctx.fillStyle = rgb9(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      coconutBodyPath(ctx, bob);
      ctx.clip();
      const cy = COCO_CY + bob;
      ctx.fillStyle = rgb9(p.huskMid);
      ctx.fillRect(COCO_CX - COCO_RX - 3, cy - COCO_RY - 3, (COCO_RX + 3) * 2, (COCO_RY + 3) * 2);
      const litGrad = ctx.createRadialGradient(
        COCO_CX - 5,
        cy - 6,
        2,
        COCO_CX - 2,
        cy - 1,
        COCO_RX + 6
      );
      litGrad.addColorStop(0, rgb9(p.huskLight));
      litGrad.addColorStop(0.5, rgb9(p.huskMid));
      litGrad.addColorStop(1, rgb9(p.huskDark));
      ctx.fillStyle = litGrad;
      ctx.beginPath();
      ctx.ellipse(COCO_CX, cy, COCO_RX, COCO_RY, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba3(p.huskDark, 0.55);
      ctx.beginPath();
      ctx.ellipse(COCO_CX + 4, cy + 4, COCO_RX * 0.82, COCO_RY * 0.78, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.lineCap = "round";
      for (let i = 0; i < 34; i++) {
        const ang = i * 2.39996 % (Math.PI * 2);
        const rad = i * 0.61803 % 1 * 0.86 + 0.06;
        const sx = COCO_CX + Math.cos(ang) * COCO_RX * rad;
        const sy = cy + Math.sin(ang) * COCO_RY * rad;
        const dir = sx < COCO_CX ? -1 : 1;
        ctx.strokeStyle = rgba3(p.fibre, 0.5 + 0.25 * (0.5 + 0.5 * Math.sin(i)));
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(sx, sy - 1.6);
        ctx.quadraticCurveTo(sx + dir * 0.6, sy, sx + dir * 0.4, sy + 2.2);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba3(p.huskLight, 0.4);
      ctx.lineWidth = 0.8;
      for (let i = 0; i < 10; i++) {
        const sx = COCO_CX - 6 + i % 3 * 3 - 1;
        const sy = cy - 6 + Math.floor(i / 3) * 3;
        ctx.beginPath();
        ctx.moveTo(sx, sy - 1.4);
        ctx.lineTo(sx - 0.6, sy + 1.8);
        ctx.stroke();
      }
      EYES.forEach(([ex, ey, er]) => {
        const y = ey + bob;
        ctx.fillStyle = rgba3(p.huskDark, 0.7);
        ctx.beginPath();
        ctx.ellipse(ex, y, er + 0.8, er + 0.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb9(p.eye);
        ctx.beginPath();
        ctx.ellipse(ex, y, er, er, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba3(p.huskLight, 0.35);
        ctx.beginPath();
        ctx.arc(ex - er * 0.4, y - er * 0.4, er * 0.4, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba3([255, 255, 255], 0.12 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(COCO_CX - 5.5, cy - 6.5, 3.4, 5.2, -0.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba3([255, 255, 255], 0.08 + 0.22 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(COCO_CX + 1, cy - 8, 1.6, 2.6, -0.2, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba3([210, 230, 250], 0.24 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(COCO_CX - 1, cy - 6, COCO_RX * 0.9, COCO_RY * 0.5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba3([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-8, cy - 7],
          [-3, cy - 9],
          [3, cy - 8],
          [8, cy - 5],
          [-6, cy - 2],
          [5, cy - 1],
          [0, cy - 4]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.lineCap = "butt";
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        const cyTop = cy - COCO_RY;
        ctx.fillStyle = rgba3([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(COCO_CX - COCO_RX * 0.7, cyTop + 5);
        ctx.quadraticCurveTo(COCO_CX - 5, cyTop - 2, COCO_CX, cyTop - 1);
        ctx.quadraticCurveTo(COCO_CX + 5, cyTop - 2, COCO_CX + COCO_RX * 0.7, cyTop + 5);
        ctx.quadraticCurveTo(COCO_CX + 7, cyTop + 8, COCO_CX + 2, cyTop + 6);
        ctx.quadraticCurveTo(COCO_CX, cyTop + 8.5, COCO_CX - 2, cyTop + 6);
        ctx.quadraticCurveTo(COCO_CX - 7, cyTop + 8, COCO_CX - COCO_RX * 0.7, cyTop + 5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba3([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(COCO_CX, cyTop + 5.4, COCO_RX * 0.6, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      drawTuft(ctx, p, bob, tuftSway);
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba3(p.light, p.lightAmt));
        lg.addColorStop(1, rgba3(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt9(t, amp = 0.9, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw4(season) {
    return (ctx) => paint9(ctx, SP9[season], 0, 0);
  }
  function anim4(season) {
    return (ctx, t) => {
      const bob = bobAt9(t);
      const tuftSway = Math.sin(t * 1.7) * 1.2 + Math.sin(t * 1.7 + 1) * 0.5;
      paint9(ctx, SP9[season], bob, tuftSway);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const cy = COCO_CY + bob;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = cy - 6 + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-5.5, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const gy = lerp11(cy - COCO_RY + 4, cy + COCO_RY - 4, prog);
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(-5, gy, 1.5, 2.8, -0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.34)";
          ctx.beginPath();
          ctx.ellipse(0.5, gy * 0.96, 1, 1.9, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(-4, cy - 5, 5, 3.6, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, cy - 4, 6, 4.4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition8(fromIdx) {
    const from = SP9[SEASON_NAMES[fromIdx]];
    const to = SP9[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother9(clamp0110(pp));
      paint9(ctx, lerpP9(from, to, k), 0, 0);
    };
  }
  var springToSummer6 = makeTransition8(0);
  var summerToAutumn6 = makeTransition8(1);
  var autumnToWinter6 = makeTransition8(2);
  var VARIANTS11 = {
    Spring: { draw: draw4("Spring"), anim: anim4("Spring") },
    Summer: { draw: draw4("Summer"), anim: anim4("Summer") },
    Autumn: { draw: draw4("Autumn"), anim: anim4("Autumn") },
    Winter: { draw: draw4("Winter"), anim: anim4("Winter") }
  };
  var TRANSITIONS11 = {
    0: springToSummer6,
    1: summerToAutumn6,
    2: autumnToWinter6
  };

  // src/textures/seasonal/fruit/goldenApple.ts
  function clamp0111(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother10 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp12(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB5(a, b, t) {
    return [lerp12(a[0], b[0], t), lerp12(a[1], b[1], t), lerp12(a[2], b[2], t)];
  }
  function rgb10(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba4(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0111(a)})`;
  }
  function lerpP10(a, b, t) {
    return {
      skinLight: lerpRGB5(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB5(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB5(a.skinDark, b.skinDark, t),
      stem: lerpRGB5(a.stem, b.stem, t),
      leaf: lerpRGB5(a.leaf, b.leaf, t),
      padGrass: lerpRGB5(a.padGrass, b.padGrass, t),
      padDark: lerpRGB5(a.padDark, b.padDark, t),
      soil: lerpRGB5(a.soil, b.soil, t),
      outline: lerpRGB5(a.outline, b.outline, t),
      light: lerpRGB5(a.light, b.light, t),
      lightAmt: lerp12(a.lightAmt, b.lightAmt, t),
      ripeness: lerp12(a.ripeness, b.ripeness, t),
      gloss: lerp12(a.gloss, b.gloss, t),
      frostAmt: lerp12(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp12(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp12(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp12(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp12(a.fallenLeafAmt, b.fallenLeafAmt, t),
      leafTurn: lerp12(a.leafTurn, b.leafTurn, t)
    };
  }
  function clampP4(p) {
    return {
      ...p,
      lightAmt: clamp0111(p.lightAmt),
      ripeness: clamp0111(p.ripeness),
      gloss: clamp0111(p.gloss),
      frostAmt: clamp0111(p.frostAmt),
      snowCapAmt: clamp0111(p.snowCapAmt),
      padSnowAmt: clamp0111(p.padSnowAmt),
      blossomAmt: clamp0111(p.blossomAmt),
      fallenLeafAmt: clamp0111(p.fallenLeafAmt),
      leafTurn: clamp0111(p.leafTurn)
    };
  }
  var SP10 = {
    // Spring — a little pale, gold-green tinged young gold; dewy lime pad + blossom.
    Spring: {
      skinLight: [248, 232, 150],
      skinMid: [224, 192, 88],
      skinDark: [176, 142, 52],
      stem: [120, 92, 50],
      leaf: [134, 196, 92],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [86, 64, 24],
      light: [234, 244, 222],
      lightAmt: 0.16,
      ripeness: 0.35,
      gloss: 0.45,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0,
      leafTurn: 0
    },
    // Summer — bright polished PEAK gold; saturated mid-green pad; strong sheen.
    Summer: {
      skinLight: [255, 234, 130],
      skinMid: [246, 196, 58],
      skinDark: [196, 142, 30],
      stem: [112, 82, 42],
      leaf: [86, 170, 70],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [96, 62, 16],
      light: [255, 240, 200],
      lightAmt: 0.18,
      ripeness: 0.7,
      gloss: 1,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      leafTurn: 0
    },
    // Autumn — rich deep glowing-gold; olive-tan pad; fallen leaves; leaf turning amber.
    Autumn: {
      skinLight: [248, 214, 104],
      skinMid: [224, 168, 44],
      skinDark: [160, 110, 24],
      stem: [110, 76, 36],
      leaf: [206, 146, 48],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [82, 52, 14],
      light: [248, 208, 142],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.6,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85,
      leafTurn: 0.9
    },
    // Winter — frost-rimmed gold + small snow cap; gold still glowing through cool
    // light; snowy pad. The apple stays CLEARLY gold underneath the frost.
    Winter: {
      skinLight: [240, 222, 152],
      skinMid: [214, 176, 78],
      skinDark: [152, 120, 56],
      stem: [104, 86, 64],
      leaf: [128, 150, 120],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [70, 56, 44],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.8,
      gloss: 0.4,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      leafTurn: 0
    }
  };
  var APP_TOP = -8;
  var APP_BOT = 17;
  var APP_HALF = 13;
  function appleBodyPath(ctx, bob) {
    const t = APP_TOP + bob;
    const b = APP_BOT + bob;
    const h = APP_HALF;
    const midY = lerp12(t, b, 0.42);
    ctx.beginPath();
    ctx.moveTo(0, t + 1.4);
    ctx.quadraticCurveTo(3.2, t - 2.2, 6.6, t - 1.4);
    ctx.quadraticCurveTo(h * 0.92, t + 1, h, midY - 2);
    ctx.quadraticCurveTo(h + 1.2, midY + 5, h * 0.74, b - 4);
    ctx.quadraticCurveTo(h * 0.5, b + 1.6, 4.2, b - 1.2);
    ctx.quadraticCurveTo(0, b + 1, -4.2, b - 1.2);
    ctx.quadraticCurveTo(-h * 0.5, b + 1.6, -h * 0.74, b - 4);
    ctx.quadraticCurveTo(-h - 1.2, midY + 5, -h, midY - 2);
    ctx.quadraticCurveTo(-h * 0.92, t + 1, -6.6, t - 1.4);
    ctx.quadraticCurveTo(-3.2, t - 2.2, 0, t + 1.4);
    ctx.closePath();
  }
  function paint10(ctx, raw, bob) {
    const p = clampP4(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba4(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb10(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb10(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb10(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba4([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba4([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba4([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba4([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba4(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba4([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba4(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba4([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb10(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, APP_BOT + bob + 1.5, 9, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba4(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, APP_BOT + bob + 2, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      const top = APP_TOP + bob;
      const bot = APP_BOT + bob;
      const midY = lerp12(top, bot, 0.42);
      appleBodyPath(ctx, bob);
      ctx.fillStyle = rgb10(p.outline);
      ctx.fill();
      ctx.save();
      appleBodyPath(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb10(p.skinMid);
      ctx.fillRect(-APP_HALF - 3, top - 6, (APP_HALF + 3) * 2, bot - top + 12);
      const bodyGrad = ctx.createRadialGradient(-5, top + 4, 1.5, -1, midY, APP_HALF + 7);
      bodyGrad.addColorStop(0, rgb10(p.skinLight));
      bodyGrad.addColorStop(0.5, rgb10(p.skinMid));
      bodyGrad.addColorStop(1, rgb10(p.skinDark));
      ctx.fillStyle = bodyGrad;
      ctx.globalAlpha = 0.95;
      ctx.beginPath();
      ctx.ellipse(-1, midY, APP_HALF + 3, (bot - top) * 0.66, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.fillStyle = rgba4(p.skinDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(5, bot - 5, APP_HALF * 0.8, (bot - top) * 0.42, -0.3, 0, Math.PI * 2);
      ctx.fill();
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba4([255, 250, 220], 0.18 + 0.34 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-5, lerp12(top, bot, 0.3), 5.6, 3.4, -0.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba4([255, 255, 244], 0.4 + 0.5 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-5.5, top + 3.5, 1.9, 2.6, -0.4, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba4([255, 248, 214], 0.22 + 0.3 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(6.5, bot - 6, 1.1, 1.8, 0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba4([210, 230, 250], 0.24 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp12(top, bot, 0.28), APP_HALF, (bot - top) * 0.32, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba4([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-8, top + 3],
          [-3, top + 2],
          [3, top + 3.5],
          [8, top + 5],
          [-6, lerp12(top, bot, 0.4)],
          [5, lerp12(top, bot, 0.46)],
          [0, lerp12(top, bot, 0.32)]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba4([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-APP_HALF * 0.66, top + 1.5);
        ctx.quadraticCurveTo(-4, top - 4.6, 0, top + 0.6);
        ctx.quadraticCurveTo(4, top - 4.6, APP_HALF * 0.66, top + 1.5);
        ctx.quadraticCurveTo(6, top + 4, 2, top + 2.6);
        ctx.quadraticCurveTo(0, top + 4.4, -2, top + 2.6);
        ctx.quadraticCurveTo(-6, top + 4, -APP_HALF * 0.66, top + 1.5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba4([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 2.4, APP_HALF * 0.58, 1.6, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const stemBaseY = top + 0.5;
      ctx.strokeStyle = rgb10(p.outline);
      ctx.lineWidth = 3.4;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(1.2, stemBaseY - 4, 2.2, stemBaseY - 7);
      ctx.stroke();
      ctx.strokeStyle = rgb10(p.stem);
      ctx.lineWidth = 2;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(1.2, stemBaseY - 4, 2.2, stemBaseY - 7);
      ctx.stroke();
      const leafCol = lerpRGB5(p.leaf, [210, 150, 56], clamp0111(p.leafTurn));
      ctx.save();
      ctx.translate(-1, stemBaseY - 5.5);
      ctx.rotate(-0.7);
      ctx.fillStyle = rgb10(p.outline);
      ctx.beginPath();
      ctx.ellipse(-3.2, -0.4, 4.4, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb10(leafCol);
      ctx.beginPath();
      ctx.ellipse(-3.2, -0.6, 3.8, 1.9, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba4(p.outline, 0.6);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(-6.6, -0.5);
      ctx.lineTo(0.4, -0.7);
      ctx.stroke();
      ctx.restore();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba4(p.light, p.lightAmt));
        lg.addColorStop(1, rgba4(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt10(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw5(season) {
    return (ctx) => paint10(ctx, SP10[season], 0);
  }
  function anim5(season) {
    return (ctx, t) => {
      const bob = bobAt10(t);
      paint10(ctx, SP10[season], bob);
      const top = APP_TOP + bob;
      const bot = APP_BOT + bob;
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        {
          const prog = t * 0.4 % 1;
          const gx = lerp12(-6.5, 6.5, prog);
          const gy = lerp12(top + 3, bot - 5, prog);
          const edge = Math.sin(prog * Math.PI);
          ctx.fillStyle = `rgba(255,252,228,${0.5 * edge})`;
          ctx.beginPath();
          ctx.ellipse(gx, gy, 1.7, 2.7, -0.4, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(255,245,200,${0.22 * edge})`;
          ctx.beginPath();
          ctx.ellipse(gx + 1.2, gy + 0.6, 3.4, 2, -0.3, 0, Math.PI * 2);
          ctx.fill();
        }
        if (season === "Spring") {
          const g = 0.22 + 0.26 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = top + 4 + Math.sin(t * 1.1) * 1;
          ctx.beginPath();
          ctx.arc(-5.5, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const fl = Math.sin(t * 2.6) * 0.16;
          ctx.translate(-1, top - 5 + bob * 0);
          ctx.rotate(-0.7 + fl);
          const amber = [212, 150, 54];
          ctx.fillStyle = rgba4(amber, 0.5 + 0.2 * (0.5 + 0.5 * Math.sin(t * 2.6)));
          ctx.beginPath();
          ctx.ellipse(-3.2, -0.6, 3.6, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Winter") {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, lerp12(top, bot, 0.42), 6, 4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition9(fromIdx) {
    const from = SP10[SEASON_NAMES[fromIdx]];
    const to = SP10[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother10(clamp0111(pp));
      paint10(ctx, lerpP10(from, to, k), 0);
    };
  }
  var springToSummer7 = makeTransition9(0);
  var summerToAutumn7 = makeTransition9(1);
  var autumnToWinter7 = makeTransition9(2);
  var VARIANTS12 = {
    Spring: { draw: draw5("Spring"), anim: anim5("Spring") },
    Summer: { draw: draw5("Summer"), anim: anim5("Summer") },
    Autumn: { draw: draw5("Autumn"), anim: anim5("Autumn") },
    Winter: { draw: draw5("Winter"), anim: anim5("Winter") }
  };
  var TRANSITIONS12 = {
    0: springToSummer7,
    1: summerToAutumn7,
    2: autumnToWinter7
  };

  // src/textures/seasonal/fruit/jackfruit.ts
  function clamp0112(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother11 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp13(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB6(a, b, t) {
    return [lerp13(a[0], b[0], t), lerp13(a[1], b[1], t), lerp13(a[2], b[2], t)];
  }
  function rgb11(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba5(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0112(a)})`;
  }
  function lerpP11(a, b, t) {
    return {
      skinLight: lerpRGB6(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB6(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB6(a.skinDark, b.skinDark, t),
      knobTip: lerpRGB6(a.knobTip, b.knobTip, t),
      stem: lerpRGB6(a.stem, b.stem, t),
      padGrass: lerpRGB6(a.padGrass, b.padGrass, t),
      padDark: lerpRGB6(a.padDark, b.padDark, t),
      soil: lerpRGB6(a.soil, b.soil, t),
      outline: lerpRGB6(a.outline, b.outline, t),
      light: lerpRGB6(a.light, b.light, t),
      lightAmt: lerp13(a.lightAmt, b.lightAmt, t),
      ripeness: lerp13(a.ripeness, b.ripeness, t),
      gloss: lerp13(a.gloss, b.gloss, t),
      frostAmt: lerp13(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp13(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp13(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp13(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp13(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP5(p) {
    return {
      ...p,
      lightAmt: clamp0112(p.lightAmt),
      ripeness: clamp0112(p.ripeness),
      gloss: clamp0112(p.gloss),
      frostAmt: clamp0112(p.frostAmt),
      snowCapAmt: clamp0112(p.snowCapAmt),
      padSnowAmt: clamp0112(p.padSnowAmt),
      blossomAmt: clamp0112(p.blossomAmt),
      fallenLeafAmt: clamp0112(p.fallenLeafAmt)
    };
  }
  var SP11 = {
    // Spring — small green knobbly fruit (still green/unripe); dewy lime pad +
    // a tiny blossom. Cool-bright light. (Same silhouette; colour reads young.)
    Spring: {
      skinLight: [156, 200, 96],
      skinMid: [104, 160, 60],
      skinDark: [56, 104, 42],
      knobTip: [186, 216, 120],
      stem: [108, 86, 44],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [42, 64, 32],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.15,
      gloss: 0.2,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — large ripe green-gold fruit (PEAK, richest/most-saturated);
    // mid-green pad, warm light, strong sheen across the knobs.
    Summer: {
      skinLight: [206, 214, 104],
      skinMid: [156, 178, 60],
      skinDark: [98, 124, 42],
      knobTip: [226, 226, 130],
      stem: [120, 90, 44],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [62, 78, 30],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.7,
      gloss: 0.9,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — yellow-brown ripe fruit (gold/rust); olive-tan pad, a couple of
    // fallen leaves; low amber light.
    Autumn: {
      skinLight: [216, 184, 92],
      skinMid: [180, 138, 58],
      skinDark: [120, 86, 40],
      knobTip: [228, 198, 116],
      stem: [110, 78, 38],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [78, 56, 28],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.5,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — green-gold still visible under a snow cap + frost on the bumps;
    // snowy pad, cool blue-grey light. No white-out: the skin stays readable.
    Winter: {
      skinLight: [172, 182, 110],
      skinMid: [128, 142, 72],
      skinDark: [86, 100, 56],
      knobTip: [196, 200, 138],
      stem: [104, 88, 64],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [54, 56, 50],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.85,
      gloss: 0.3,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var JF_TOP = -10;
  var JF_BOT = 17;
  var JF_HALF = 13.5;
  var JF_CY = 4.5;
  var KNOBS = (() => {
    const out = [];
    const rows = [0.12, 0.3, 0.5, 0.7, 0.87];
    rows.forEach((v, ri) => {
      const widthAt = Math.sin(Math.PI * (0.18 + 0.64 * v));
      const count = 5 + ri % 2;
      const off = ri % 2 * 0.5;
      for (let i = 0; i < count; i++) {
        const u = ((i + off) / (count - 1) - 0.5) * 2 * widthAt;
        out.push({ u, v, r: 1.9 - 0.4 * Math.abs(u) * 0.6 + (v > 0.6 ? 0.3 : 0) });
      }
    });
    return out;
  })();
  function knobXY(k, bob) {
    const top = JF_TOP + bob;
    const bot = JF_BOT + bob;
    const y = lerp13(top, bot, k.v);
    const taper = Math.sin(Math.PI * (0.12 + 0.76 * k.v));
    const x = k.u * JF_HALF * 0.78 * taper;
    return [x, y];
  }
  function jackfruitBodyPath(ctx, bob) {
    const cy = JF_CY + bob;
    const rx = JF_HALF;
    const ry = (JF_BOT - JF_TOP) / 2;
    const STEPS = 44;
    ctx.beginPath();
    for (let i = 0; i <= STEPS; i++) {
      const a = i / STEPS * Math.PI * 2 - Math.PI / 2;
      const ripple = 1 + 0.05 * Math.sin(a * 9) + 0.03 * Math.sin(a * 15 + 1.3);
      const x = Math.cos(a) * rx * ripple;
      const y = cy + Math.sin(a) * ry * ripple;
      if (i === 0) ctx.moveTo(x, y);
      else ctx.lineTo(x, y);
    }
    ctx.closePath();
  }
  function paint11(ctx, raw, bob) {
    const p = clampP5(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba5(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb11(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb11(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb11(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba5([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba5([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba5([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba5([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba5(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba5([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba5(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba5([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb11(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, JF_BOT + bob + 1.5, 11, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba5(p.outline, 0.3);
      ctx.beginPath();
      ctx.ellipse(3, JF_BOT + bob + 2.2, 13, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      jackfruitBodyPath(ctx, bob);
      ctx.fillStyle = rgb11(p.outline);
      ctx.fill();
      ctx.save();
      jackfruitBodyPath(ctx, bob);
      ctx.clip();
      const top = JF_TOP + bob;
      const bot = JF_BOT + bob;
      const cy = JF_CY + bob;
      ctx.fillStyle = rgb11(p.skinMid);
      ctx.fillRect(-JF_HALF - 3, top - 8, (JF_HALF + 3) * 2, bot - top + 16);
      const litGrad = ctx.createLinearGradient(-JF_HALF, top - 2, JF_HALF, bot);
      litGrad.addColorStop(0, rgb11(p.skinLight));
      litGrad.addColorStop(0.5, rgb11(p.skinMid));
      litGrad.addColorStop(1, rgb11(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-2, cy - 1, JF_HALF + 2, (bot - top) * 0.55, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      KNOBS.forEach((k) => {
        const [kx, ky] = knobXY(k, bob);
        ctx.fillStyle = rgba5(p.skinDark, 0.55);
        ctx.beginPath();
        ctx.ellipse(kx + 0.5, ky + 0.6, k.r + 0.7, k.r + 0.5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb11(p.skinMid);
        ctx.beginPath();
        ctx.ellipse(kx, ky, k.r, k.r * 0.92, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba5(p.skinLight, 0.85);
        ctx.beginPath();
        ctx.ellipse(kx - 0.5, ky - 0.5, k.r * 0.62, k.r * 0.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba5(p.knobTip, 0.9);
        ctx.beginPath();
        ctx.arc(kx - 0.6, ky - 0.6, k.r * 0.28, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.fillStyle = rgba5(p.skinDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(1, bot - 3, JF_HALF * 0.86, 5, 0, 0, Math.PI * 2);
      ctx.fill();
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba5([255, 255, 255], 0.1 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-4.5, cy - 5, JF_HALF * 0.5, (bot - top) * 0.26, -0.35, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba5([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, cy - 6, JF_HALF, (bot - top) * 0.3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba5([236, 246, 255], 0.75 * p.frostAmt);
        KNOBS.forEach((k) => {
          if (k.v > 0.42) return;
          const [kx, ky] = knobXY(k, bob);
          ctx.beginPath();
          ctx.arc(kx - 0.5, ky - 0.7, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba5([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-JF_HALF * 0.78, top + 5);
        ctx.quadraticCurveTo(-6, top - 2, 0, top - 1.5);
        ctx.quadraticCurveTo(6, top - 2, JF_HALF * 0.78, top + 5);
        ctx.quadraticCurveTo(8, top + 8.5, 3, top + 6);
        ctx.quadraticCurveTo(0, top + 8.5, -3, top + 6);
        ctx.quadraticCurveTo(-8, top + 8.5, -JF_HALF * 0.78, top + 5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba5([206, 223, 243], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 5, JF_HALF * 0.6, 2.2, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const stemBaseY = top + 1;
      ctx.strokeStyle = rgb11(p.outline);
      ctx.lineWidth = 5.4;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(0.6, stemBaseY - 4, 1.2, stemBaseY - 7);
      ctx.stroke();
      ctx.strokeStyle = rgb11(p.stem);
      ctx.lineWidth = 3.6;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(0.6, stemBaseY - 4, 1.2, stemBaseY - 7);
      ctx.stroke();
      ctx.strokeStyle = rgba5([220, 200, 150], 0.5);
      ctx.lineWidth = 1.1;
      ctx.beginPath();
      ctx.moveTo(-1, stemBaseY - 1);
      ctx.quadraticCurveTo(-0.6, stemBaseY - 4, 0.2, stemBaseY - 6.5);
      ctx.stroke();
      if (p.snowCapAmt > 0.02) {
        ctx.fillStyle = rgba5([246, 251, 255], 0.95 * p.snowCapAmt);
        ctx.beginPath();
        ctx.ellipse(1.2, stemBaseY - 7.2, 3, 1.8, -0.2, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba5(p.light, p.lightAmt));
        lg.addColorStop(1, rgba5(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt11(t, amp = 0.7, w = 1.3) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw6(season) {
    return (ctx) => paint11(ctx, SP11[season], 0);
  }
  function anim6(season) {
    return (ctx, t) => {
      const bob = bobAt11(t);
      paint11(ctx, SP11[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.1));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -3 + bob + Math.sin(t * 1) * 1.4;
          ctx.beginPath();
          ctx.arc(-5, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const top = JF_TOP + bob;
          const bot = JF_BOT + bob;
          const gy = lerp13(top + 2, bot - 4, prog);
          ctx.fillStyle = "rgba(255,255,255,0.5)";
          ctx.beginPath();
          ctx.ellipse(-5, gy, 4.2, 2.2, -0.35, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.25)";
          ctx.beginPath();
          ctx.ellipse(2, gy + 1, 2.6, 1.6, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(-4, -3 + bob, 5, 3.4, -0.25, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 0 + bob, 6.5, 4.4, -0.25, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition10(fromIdx) {
    const from = SP11[SEASON_NAMES[fromIdx]];
    const to = SP11[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother11(clamp0112(pp));
      paint11(ctx, lerpP11(from, to, k), 0);
    };
  }
  var springToSummer8 = makeTransition10(0);
  var summerToAutumn8 = makeTransition10(1);
  var autumnToWinter8 = makeTransition10(2);
  var VARIANTS13 = {
    Spring: { draw: draw6("Spring"), anim: anim6("Spring") },
    Summer: { draw: draw6("Summer"), anim: anim6("Summer") },
    Autumn: { draw: draw6("Autumn"), anim: anim6("Autumn") },
    Winter: { draw: draw6("Winter"), anim: anim6("Winter") }
  };
  var TRANSITIONS13 = {
    0: springToSummer8,
    1: summerToAutumn8,
    2: autumnToWinter8
  };

  // src/textures/seasonal/fruit/rambutan.ts
  function clamp0113(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother12 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp14(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB7(a, b, t) {
    return [lerp14(a[0], b[0], t), lerp14(a[1], b[1], t), lerp14(a[2], b[2], t)];
  }
  function rgb12(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba6(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0113(a)})`;
  }
  function lerpP12(a, b, t) {
    return {
      skinLight: lerpRGB7(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB7(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB7(a.skinDark, b.skinDark, t),
      spineLight: lerpRGB7(a.spineLight, b.spineLight, t),
      spineDark: lerpRGB7(a.spineDark, b.spineDark, t),
      leaf: lerpRGB7(a.leaf, b.leaf, t),
      stem: lerpRGB7(a.stem, b.stem, t),
      padGrass: lerpRGB7(a.padGrass, b.padGrass, t),
      padDark: lerpRGB7(a.padDark, b.padDark, t),
      soil: lerpRGB7(a.soil, b.soil, t),
      outline: lerpRGB7(a.outline, b.outline, t),
      light: lerpRGB7(a.light, b.light, t),
      lightAmt: lerp14(a.lightAmt, b.lightAmt, t),
      ripeness: lerp14(a.ripeness, b.ripeness, t),
      gloss: lerp14(a.gloss, b.gloss, t),
      spineTipGreen: lerp14(a.spineTipGreen, b.spineTipGreen, t),
      frostAmt: lerp14(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp14(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp14(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp14(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp14(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP6(p) {
    return {
      ...p,
      lightAmt: clamp0113(p.lightAmt),
      ripeness: clamp0113(p.ripeness),
      gloss: clamp0113(p.gloss),
      spineTipGreen: clamp0113(p.spineTipGreen),
      frostAmt: clamp0113(p.frostAmt),
      snowCapAmt: clamp0113(p.snowCapAmt),
      padSnowAmt: clamp0113(p.padSnowAmt),
      blossomAmt: clamp0113(p.blossomAmt),
      fallenLeafAmt: clamp0113(p.fallenLeafAmt)
    };
  }
  var SP12 = {
    // Spring — green spiky young fruit, hairs greenish; dewy lime pad + blossom.
    Spring: {
      skinLight: [168, 214, 104],
      skinMid: [112, 174, 70],
      skinDark: [64, 120, 50],
      spineLight: [186, 222, 120],
      spineDark: [96, 150, 64],
      leaf: [108, 168, 70],
      stem: [126, 110, 70],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [44, 70, 34],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.12,
      gloss: 0.2,
      spineTipGreen: 0.9,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full ripe RED hairy fruit with green-tipped spines (PEAK); mid-green pad.
    Summer: {
      skinLight: [240, 78, 64],
      skinMid: [206, 36, 40],
      skinDark: [142, 18, 30],
      spineLight: [232, 86, 70],
      spineDark: [150, 26, 32],
      leaf: [86, 150, 56],
      stem: [120, 92, 56],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [82, 16, 24],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.8,
      gloss: 0.9,
      spineTipGreen: 0.55,
      // green-tipped spines at peak
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — deep crimson overripe fruit, spines darkening; olive-tan pad + leaves.
    Autumn: {
      skinLight: [186, 52, 48],
      skinMid: [150, 28, 36],
      skinDark: [92, 18, 28],
      spineLight: [156, 56, 50],
      spineDark: [96, 30, 30],
      leaf: [150, 120, 60],
      stem: [110, 82, 48],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [66, 16, 22],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.42,
      spineTipGreen: 0.12,
      // spines darkening, little green left
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — snow-dusted dull-red fruit + frost on spines; red still visible,
    // snowy pad, cool light.
    Winter: {
      skinLight: [180, 86, 78],
      skinMid: [144, 52, 56],
      skinDark: [92, 36, 46],
      spineLight: [176, 100, 96],
      spineDark: [110, 56, 60],
      leaf: [118, 130, 96],
      stem: [110, 96, 80],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [54, 38, 50],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.85,
      gloss: 0.22,
      spineTipGreen: 0.05,
      frostAmt: 0.72,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var FRUIT = [
    { cx: -6.2, cy: 8, rx: 7.4, ry: 8.6, rot: -0.16, spines: 17 },
    { cx: 6, cy: 9, rx: 7, ry: 8.2, rot: 0.18, spines: 16 },
    { cx: 0.4, cy: 1.6, rx: 6.6, ry: 7.8, rot: 0.02, spines: 16 }
  ];
  function jit(i) {
    const s = Math.sin(i * 12.9898 + 4.1) * 43758.5453;
    return s - Math.floor(s);
  }
  function drawSpines(ctx, f, bob, seed, p, pass) {
    const cx = f.cx;
    const cy = f.cy + bob;
    const n = f.spines;
    for (let i = 0; i < n; i++) {
      const base = i / n * Math.PI * 2 + f.rot;
      const ang = base + (jit(i + seed) - 0.5) * 0.18;
      const downness = Math.cos(ang);
      if (downness > 0.55) continue;
      const len = 3.4 + jit(i * 3 + seed) * 2.2;
      const rx0 = cx + Math.sin(ang) * f.rx * 0.92;
      const ry0 = cy - Math.cos(ang) * f.ry * 0.92;
      const curl = (jit(i * 5 + seed) - 0.5) * 1.8 - 0.6;
      const tipX = cx + Math.sin(ang) * (f.rx + len);
      const tipY = cy - Math.cos(ang) * (f.ry + len);
      const ctrlX = cx + Math.sin(ang) * (f.rx + len * 0.5) + Math.cos(ang) * curl;
      const ctrlY = cy - Math.cos(ang) * (f.ry + len * 0.5) + Math.sin(ang) * curl;
      ctx.beginPath();
      ctx.moveTo(rx0, ry0);
      ctx.quadraticCurveTo(ctrlX, ctrlY, tipX, tipY);
      if (pass === "outline") {
        ctx.strokeStyle = rgb12(p.outline);
        ctx.lineWidth = 2.4;
      } else {
        const upperLeft = -Math.sin(ang) * 0.5 - Math.cos(ang) * 0.5;
        const litMix = clamp0113(0.4 + upperLeft * 0.5);
        const tipCol = lerpRGB7(p.spineDark, p.spineLight, litMix);
        const greened = lerpRGB7(tipCol, [120, 180, 70], p.spineTipGreen * 0.6);
        ctx.strokeStyle = rgb12(greened);
        ctx.lineWidth = 1.5;
      }
      ctx.stroke();
    }
  }
  function fruitPath(ctx, f, bob, grow = 0) {
    ctx.beginPath();
    ctx.ellipse(f.cx, f.cy + bob, f.rx + grow, f.ry + grow, f.rot, 0, Math.PI * 2);
  }
  function drawFruitBody(ctx, f, bob, p) {
    const cy = f.cy + bob;
    ctx.fillStyle = rgb12(p.skinMid);
    fruitPath(ctx, f, bob);
    ctx.fill();
    ctx.save();
    fruitPath(ctx, f, bob);
    ctx.clip();
    const g = ctx.createLinearGradient(f.cx - f.rx, cy - f.ry, f.cx + f.rx, cy + f.ry);
    g.addColorStop(0, rgb12(p.skinLight));
    g.addColorStop(0.5, rgb12(p.skinMid));
    g.addColorStop(1, rgb12(p.skinDark));
    ctx.fillStyle = g;
    ctx.globalAlpha = 0.92;
    ctx.beginPath();
    ctx.ellipse(f.cx - 1, cy - 0.5, f.rx + 1, f.ry + 1, f.rot, 0, Math.PI * 2);
    ctx.fill();
    ctx.globalAlpha = 1;
    ctx.fillStyle = rgba6(p.skinDark, 0.5);
    ctx.beginPath();
    ctx.ellipse(f.cx + 1.4, cy + f.ry * 0.42, f.rx * 0.86, f.ry * 0.5, f.rot, 0, Math.PI * 2);
    ctx.fill();
    if (p.gloss > 0.02) {
      ctx.fillStyle = rgba6([255, 255, 255], 0.16 + 0.5 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(f.cx - f.rx * 0.42, cy - f.ry * 0.4, 1.5, 2.6, f.rot - 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.frostAmt > 0.02) {
      ctx.fillStyle = rgba6([232, 244, 255], 0.6 * p.frostAmt);
      for (let k = 0; k < 4; k++) {
        const a = -0.9 + k * 0.5;
        ctx.beginPath();
        ctx.arc(f.cx + Math.sin(a) * f.rx * 0.5, cy - Math.cos(a) * f.ry * 0.5 - 1, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.restore();
  }
  function paint12(ctx, raw, bob) {
    const p = clampP6(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba6(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb12(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb12(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb12(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba6([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba6([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba6([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba6([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy2]) => {
          ctx.beginPath();
          ctx.arc(sx, sy2, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba6(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba6([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba6(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba6([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb12(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, 16 + bob + 1.5, 10, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba6(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, 16 + bob + 2, 12, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      const sy = -3 + bob;
      ctx.strokeStyle = rgb12(p.outline);
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(1.5, 1.5 + bob);
      ctx.quadraticCurveTo(3.5, sy - 2, 5.5, sy - 7);
      ctx.stroke();
      ctx.strokeStyle = rgb12(p.stem);
      ctx.lineWidth = 1.7;
      ctx.beginPath();
      ctx.moveTo(1.5, 1.5 + bob);
      ctx.quadraticCurveTo(3.5, sy - 2, 5.5, sy - 7);
      ctx.stroke();
      ctx.save();
      ctx.translate(5.5, sy - 7);
      ctx.rotate(0.5);
      fruitLeaf(ctx, p);
      ctx.restore();
      const order = [2, 0, 1];
      order.forEach((idx) => {
        const f = FRUIT[idx];
        fruitPath(ctx, f, bob, 1.1);
        ctx.fillStyle = rgb12(p.outline);
        ctx.fill();
        drawSpines(ctx, f, bob, idx * 7 + 1, p, "outline");
        drawFruitBody(ctx, f, bob, p);
        drawSpines(ctx, f, bob, idx * 7 + 1, p, "fill");
      });
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba6([246, 251, 255], 0.95 * a);
        [FRUIT[0], FRUIT[1], FRUIT[2]].forEach((f) => {
          const cy = f.cy + bob;
          ctx.beginPath();
          ctx.moveTo(f.cx - f.rx * 0.7, cy - f.ry * 0.6);
          ctx.quadraticCurveTo(f.cx - 1, cy - f.ry - 2, f.cx, cy - f.ry - 1.4);
          ctx.quadraticCurveTo(f.cx + 1, cy - f.ry - 2, f.cx + f.rx * 0.7, cy - f.ry * 0.6);
          ctx.quadraticCurveTo(f.cx + 2, cy - f.ry * 0.3, f.cx, cy - f.ry * 0.5);
          ctx.quadraticCurveTo(f.cx - 2, cy - f.ry * 0.3, f.cx - f.rx * 0.7, cy - f.ry * 0.6);
          ctx.closePath();
          ctx.fill();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba6(p.light, p.lightAmt));
        lg.addColorStop(1, rgba6(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function fruitLeaf(ctx, p) {
    ctx.fillStyle = rgb12(p.outline);
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-4.4, -3.6, -1, -9.6);
    ctx.quadraticCurveTo(2.4, -4.6, 0, 0);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb12(p.leaf);
    ctx.beginPath();
    ctx.moveTo(0, -0.6);
    ctx.quadraticCurveTo(-3.4, -3.6, -1, -8.8);
    ctx.quadraticCurveTo(1.8, -4.4, 0, -0.6);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgba6(p.outline, 0.6);
    ctx.lineWidth = 0.7;
    ctx.beginPath();
    ctx.moveTo(-0.3, -1);
    ctx.quadraticCurveTo(-1.4, -4.4, -1, -8.4);
    ctx.stroke();
  }
  function bobAt12(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw7(season) {
    return (ctx) => paint12(ctx, SP12[season], 0);
  }
  function anim7(season) {
    return (ctx, t) => {
      const bob = bobAt12(t);
      paint12(ctx, SP12[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const quiver = 0.06 + 0.05 * (0.5 + 0.5 * Math.sin(t * 3.4));
        ctx.strokeStyle = `rgba(255,255,255,${quiver})`;
        ctx.lineWidth = 0.8;
        FRUIT.forEach((f, idx) => {
          const cy = f.cy + bob;
          const a = idx * 1.3 - 0.8 + Math.sin(t * 2.6 + idx) * 0.05;
          ctx.beginPath();
          ctx.moveTo(f.cx + Math.sin(a) * f.rx, cy - Math.cos(a) * f.ry);
          ctx.lineTo(f.cx + Math.sin(a) * (f.rx + 4), cy - Math.cos(a) * (f.ry + 4));
          ctx.stroke();
        });
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = 2 + bob + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-6, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.3 + 0.4 * (0.5 + 0.5 * Math.sin(t * 1.8));
          ctx.fillStyle = `rgba(255,255,255,${s})`;
          ctx.beginPath();
          ctx.ellipse(-9, 4 + bob, 1.3, 2, -0.3, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,228,190,${s})`;
          ctx.beginPath();
          ctx.ellipse(-5, 3 + bob, 5.2, 3.6, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 5 + bob, 7, 5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition11(fromIdx) {
    const from = SP12[SEASON_NAMES[fromIdx]];
    const to = SP12[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother12(clamp0113(pp));
      paint12(ctx, lerpP12(from, to, k), 0);
    };
  }
  var springToSummer9 = makeTransition11(0);
  var summerToAutumn9 = makeTransition11(1);
  var autumnToWinter9 = makeTransition11(2);
  var VARIANTS14 = {
    Spring: { draw: draw7("Spring"), anim: anim7("Spring") },
    Summer: { draw: draw7("Summer"), anim: anim7("Summer") },
    Autumn: { draw: draw7("Autumn"), anim: anim7("Autumn") },
    Winter: { draw: draw7("Winter"), anim: anim7("Winter") }
  };
  var TRANSITIONS14 = {
    0: springToSummer9,
    1: summerToAutumn9,
    2: autumnToWinter9
  };

  // src/textures/seasonal/fruit/starfruit.ts
  function clamp0114(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother13 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp15(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB8(a, b, t) {
    return [lerp15(a[0], b[0], t), lerp15(a[1], b[1], t), lerp15(a[2], b[2], t)];
  }
  function rgb13(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba7(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0114(a)})`;
  }
  function lerpP13(a, b, t) {
    return {
      skinLight: lerpRGB8(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB8(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB8(a.skinDark, b.skinDark, t),
      edge: lerpRGB8(a.edge, b.edge, t),
      leaf: lerpRGB8(a.leaf, b.leaf, t),
      padGrass: lerpRGB8(a.padGrass, b.padGrass, t),
      padDark: lerpRGB8(a.padDark, b.padDark, t),
      soil: lerpRGB8(a.soil, b.soil, t),
      outline: lerpRGB8(a.outline, b.outline, t),
      light: lerpRGB8(a.light, b.light, t),
      lightAmt: lerp15(a.lightAmt, b.lightAmt, t),
      ripeness: lerp15(a.ripeness, b.ripeness, t),
      gloss: lerp15(a.gloss, b.gloss, t),
      edgeBrown: lerp15(a.edgeBrown, b.edgeBrown, t),
      frostAmt: lerp15(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp15(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp15(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp15(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp15(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP7(p) {
    return {
      ...p,
      lightAmt: clamp0114(p.lightAmt),
      ripeness: clamp0114(p.ripeness),
      gloss: clamp0114(p.gloss),
      edgeBrown: clamp0114(p.edgeBrown),
      frostAmt: clamp0114(p.frostAmt),
      snowCapAmt: clamp0114(p.snowCapAmt),
      padSnowAmt: clamp0114(p.padSnowAmt),
      blossomAmt: clamp0114(p.blossomAmt),
      fallenLeafAmt: clamp0114(p.fallenLeafAmt)
    };
  }
  var SP13 = {
    // Spring — unripe GREEN starfruit, crisp ridges; bright lime dewy pad + blossom.
    Spring: {
      skinLight: [186, 224, 110],
      skinMid: [126, 188, 70],
      skinDark: [78, 140, 52],
      edge: [206, 234, 138],
      leaf: [104, 168, 58],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [50, 86, 36],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.2,
      gloss: 0.3,
      edgeBrown: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK: bright ripe YELLOW star, translucent edges; warm light, gloss.
    Summer: {
      skinLight: [255, 240, 120],
      skinMid: [248, 208, 52],
      skinDark: [206, 158, 30],
      edge: [255, 248, 170],
      leaf: [86, 158, 50],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [150, 110, 24],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.9,
      gloss: 0.95,
      edgeBrown: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — amber-ripe star, ridge edges browning; gold/rust pad + fallen leaves.
    Autumn: {
      skinLight: [248, 206, 96],
      skinMid: [228, 162, 46],
      skinDark: [176, 112, 32],
      edge: [220, 168, 86],
      leaf: [148, 130, 56],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [120, 78, 28],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.45,
      edgeBrown: 0.7,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; frost-dusted YELLOW star still visible, snow
    // cap on the top ridge; snowy pad.
    Winter: {
      skinLight: [236, 220, 132],
      skinMid: [214, 188, 76],
      skinDark: [158, 136, 64],
      edge: [228, 224, 176],
      leaf: [124, 142, 96],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [86, 78, 56],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.8,
      gloss: 0.25,
      edgeBrown: 0.15,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var SF_TOP = -16;
  var SF_BOT = 16;
  var SF_HALF = 11;
  var SF_MIDY = 1;
  var _RIDGES = [-SF_HALF, -5.4, 0, 5.4, SF_HALF];
  function starfruitBodyPath(ctx, bob) {
    const t = SF_TOP + bob;
    const b = SF_BOT + bob;
    const my = SF_MIDY + bob;
    const h = SF_HALF;
    ctx.beginPath();
    ctx.moveTo(0, t);
    ctx.quadraticCurveTo(h * 0.55, t + 4, h * 0.72, lerp15(t, my, 0.5));
    ctx.quadraticCurveTo(h + 1.4, my - 4, h, my);
    ctx.quadraticCurveTo(h + 1.4, my + 5, h * 0.7, lerp15(my, b, 0.55));
    ctx.quadraticCurveTo(h * 0.5, b - 2, b * 0 + 0, b);
    ctx.quadraticCurveTo(-h * 0.5, b - 2, -h * 0.7, lerp15(my, b, 0.55));
    ctx.quadraticCurveTo(-h - 1.4, my + 5, -h, my);
    ctx.quadraticCurveTo(-h - 1.4, my - 4, -h * 0.72, lerp15(t, my, 0.5));
    ctx.quadraticCurveTo(-h * 0.55, t + 4, 0, t);
    ctx.closePath();
  }
  function paint13(ctx, raw, bob) {
    const p = clampP7(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba7(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb13(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb13(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb13(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba7([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba7([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba7([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba7([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba7(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba7([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba7(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba7([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb13(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, SF_BOT + bob + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba7(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, SF_BOT + bob + 2, 10, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const top = SF_TOP + bob;
      const bot = SF_BOT + bob;
      const my = SF_MIDY + bob;
      starfruitBodyPath(ctx, bob);
      ctx.fillStyle = rgb13(p.outline);
      ctx.fill();
      ctx.save();
      starfruitBodyPath(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb13(p.skinMid);
      ctx.fillRect(-SF_HALF - 3, top - 4, (SF_HALF + 3) * 2, bot - top + 8);
      const litGrad = ctx.createLinearGradient(-SF_HALF, top, SF_HALF, bot);
      litGrad.addColorStop(0, rgb13(p.skinLight));
      litGrad.addColorStop(0.45, rgb13(p.skinMid));
      litGrad.addColorStop(1, rgb13(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.9;
      ctx.beginPath();
      ctx.ellipse(-1.5, lerp15(top, bot, 0.5), SF_HALF + 1, (bot - top) * 0.52, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba7(p.skinDark, 0.85);
      ctx.lineWidth = 2;
      [-2.7, 2.7].forEach((vx) => {
        ctx.beginPath();
        ctx.moveTo(vx * 0.4, top + 3);
        ctx.quadraticCurveTo(vx, my, vx * 0.4, bot - 3);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba7(p.skinDark, 0.55);
      ctx.lineWidth = 1.5;
      [-8, 8].forEach((vx) => {
        ctx.beginPath();
        ctx.moveTo(vx * 0.5, top + 5);
        ctx.quadraticCurveTo(vx, my, vx * 0.5, bot - 5);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba7(p.skinLight, 0.7);
      ctx.lineWidth = 1.8;
      [0].forEach((cx) => {
        ctx.beginPath();
        ctx.moveTo(cx, top + 2);
        ctx.quadraticCurveTo(cx - 0.6, my, cx, bot - 2);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba7(p.skinLight, 0.45);
      ctx.lineWidth = 1.3;
      [-5.4, 5.4].forEach((cx) => {
        ctx.beginPath();
        ctx.moveTo(cx * 0.5, top + 4);
        ctx.quadraticCurveTo(cx, my, cx * 0.5, bot - 4);
        ctx.stroke();
      });
      {
        const edgeCol = lerpRGB8(p.edge, [150, 104, 48], p.edgeBrown);
        const ea = 0.3 + 0.4 * p.ripeness;
        ctx.fillStyle = rgba7(edgeCol, ea);
        [[-1, my], [1, my]].forEach(([sgn]) => {
          const xx = sgn * (SF_HALF - 1);
          ctx.beginPath();
          ctx.ellipse(xx, my, 2.2, (bot - top) * 0.34, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba7([255, 255, 255], 0.16 + 0.6 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-3.5, lerp15(top, bot, 0.4), 1.4, (bot - top) * 0.3, -0.08, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba7([255, 255, 255], 0.1 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(0.8, lerp15(top, bot, 0.34), 0.9, (bot - top) * 0.22, -0.04, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.edgeBrown > 0.02) {
        ctx.fillStyle = rgba7([138, 92, 40], 0.6 * p.edgeBrown);
        const browns = [
          [-SF_HALF + 1, my - 6],
          [-SF_HALF + 1.5, my + 5],
          [SF_HALF - 1, my - 4],
          [SF_HALF - 1.5, my + 6],
          [0, bot - 4]
        ];
        browns.forEach(([bx, by]) => {
          ctx.beginPath();
          ctx.ellipse(bx, by, 1.3, 2, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba7([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp15(top, my, 0.5), SF_HALF, (my - top) * 0.7, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba7([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-7, top + 4],
          [-2, top + 2],
          [3, top + 4],
          [7, top + 6],
          [-5, my - 2],
          [5, my],
          [0, my - 4],
          [-3, my + 6],
          [4, my + 8]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba7([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-5.5, top + 5);
        ctx.quadraticCurveTo(-3, top - 2, 0, top + 0.5);
        ctx.quadraticCurveTo(3, top - 2, 5.5, top + 5);
        ctx.quadraticCurveTo(2.5, top + 7, 0, top + 5.5);
        ctx.quadraticCurveTo(-2.5, top + 7, -5.5, top + 5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba7([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 4.6, 4.4, 1.5, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const stemBaseY = top + 0.5;
      ctx.strokeStyle = rgb13(p.outline);
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(0.6, stemBaseY - 3.5, 2.2, stemBaseY - 5);
      ctx.stroke();
      ctx.strokeStyle = rgb13(p.leaf);
      ctx.lineWidth = 1.6;
      ctx.beginPath();
      ctx.moveTo(0, stemBaseY);
      ctx.quadraticCurveTo(0.6, stemBaseY - 3.5, 2.2, stemBaseY - 5);
      ctx.stroke();
      ctx.fillStyle = rgb13(p.leaf);
      ctx.beginPath();
      ctx.moveTo(2, stemBaseY - 4.5);
      ctx.quadraticCurveTo(7.5, stemBaseY - 9, 9.5, stemBaseY - 4.5);
      ctx.quadraticCurveTo(6, stemBaseY - 3.5, 2, stemBaseY - 4.5);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb13(p.outline);
      ctx.lineWidth = 0.9;
      ctx.stroke();
      ctx.strokeStyle = rgba7(p.skinLight, 0.45);
      ctx.lineWidth = 0.8;
      ctx.beginPath();
      ctx.moveTo(2.6, stemBaseY - 4.6);
      ctx.quadraticCurveTo(6, stemBaseY - 6, 9, stemBaseY - 4.8);
      ctx.stroke();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba7(p.light, p.lightAmt));
        lg.addColorStop(1, rgba7(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt13(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw8(season) {
    return (ctx) => paint13(ctx, SP13[season], 0);
  }
  function anim8(season) {
    return (ctx, t) => {
      const bob = bobAt13(t);
      paint13(ctx, SP13[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        {
          const top = SF_TOP + bob;
          const stemBaseY = top + 0.5;
          const flut = Math.sin(t * 3.1) * 0.12 + Math.sin(t * 1.7) * 0.05;
          ctx.save();
          ctx.translate(2, stemBaseY - 4.5);
          ctx.rotate(flut);
          ctx.fillStyle = rgb13(SP13[season].leaf);
          ctx.beginPath();
          ctx.moveTo(0, 0);
          ctx.quadraticCurveTo(5.5, -4.5, 7.5, 0);
          ctx.quadraticCurveTo(4, 1, 0, 0);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = rgb13(SP13[season].outline);
          ctx.lineWidth = 0.9;
          ctx.stroke();
          ctx.restore();
        }
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -2 + bob + Math.sin(t * 1.1) * 1.6;
          ctx.beginPath();
          ctx.arc(-4, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const top = SF_TOP + bob;
          const bot = SF_BOT + bob;
          const gy = lerp15(top + 2, bot - 2, prog);
          ctx.fillStyle = "rgba(255,255,255,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, gy, 1.4, 2.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.ellipse(-3.5, gy * 0.97, 0.9, 1.9, -0.05, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.12 + 0.16 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,228,170,${s})`;
          ctx.beginPath();
          ctx.ellipse(-3, 0 + bob, 4.5, 5.5, -0.15, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.12 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-2, 0 + bob, 5, 6, -0.15, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition12(fromIdx) {
    const from = SP13[SEASON_NAMES[fromIdx]];
    const to = SP13[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother13(clamp0114(pp));
      paint13(ctx, lerpP13(from, to, k), 0);
    };
  }
  var springToSummer10 = makeTransition12(0);
  var summerToAutumn10 = makeTransition12(1);
  var autumnToWinter10 = makeTransition12(2);
  var VARIANTS15 = {
    Spring: { draw: draw8("Spring"), anim: anim8("Spring") },
    Summer: { draw: draw8("Summer"), anim: anim8("Summer") },
    Autumn: { draw: draw8("Autumn"), anim: anim8("Autumn") },
    Winter: { draw: draw8("Winter"), anim: anim8("Winter") }
  };
  var TRANSITIONS15 = {
    0: springToSummer10,
    1: summerToAutumn10,
    2: autumnToWinter10
  };

  // src/textures/seasonal/grain/corn.ts
  var SP14 = {
    // Spring — fresh lightly-desaturated pastel; husk tight & fully closed,
    // fresh green, a little dew; bright lime dewy pad + a pale blossom.
    Spring: {
      shadowAmt: 0.5,
      padGrass: [142, 214, 96],
      padGrassDark: [86, 150, 60],
      padSnowAmt: 0,
      blossomAmt: 1,
      fallenLeafAmt: 0,
      huskLight: [126, 196, 92],
      huskDark: [62, 116, 50],
      huskBrownAmt: 0,
      huskOpen: 0,
      kernel: [232, 214, 120],
      kernelDeep: [190, 162, 80],
      kernelDent: 0,
      silk: [206, 224, 150],
      outline: [44, 70, 36],
      frostAmt: 0,
      snowCapAmt: 0,
      gloss: 0.5
    },
    // Summer — richest, most-saturated palette (PEAK); husk peels open to show
    // plump bright-yellow kernels in neat rows, golden silk, vivid green husk.
    Summer: {
      shadowAmt: 0.7,
      padGrass: [104, 184, 70],
      padGrassDark: [62, 126, 46],
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      huskLight: [108, 184, 66],
      huskDark: [48, 104, 40],
      huskBrownAmt: 0,
      huskOpen: 1,
      kernel: [255, 214, 64],
      kernelDeep: [206, 150, 28],
      kernelDent: 0,
      silk: [240, 214, 120],
      outline: [40, 66, 32],
      frostAmt: 0,
      snowCapAmt: 0,
      gloss: 0.8
    },
    // Autumn — gold/orange/rust; husk drying, curling & browning at edges,
    // kernels deeper gold & a touch dented; silk rust-brown; fallen leaves.
    Autumn: {
      shadowAmt: 0.55,
      padGrass: [158, 150, 78],
      padGrassDark: [112, 96, 50],
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 1,
      huskLight: [176, 150, 72],
      huskDark: [104, 76, 32],
      huskBrownAmt: 1,
      huskOpen: 0.9,
      kernel: [232, 168, 44],
      kernelDeep: [168, 108, 24],
      kernelDent: 1,
      silk: [168, 108, 56],
      outline: [70, 48, 24],
      frostAmt: 0,
      snowCapAmt: 0,
      gloss: 0.45
    },
    // Winter — cool blue-grey light; husk muted green-grey, kernels still
    // visible; snow cap + frost dusting on upward surfaces; pad snow-covered.
    Winter: {
      shadowAmt: 0.4,
      padGrass: [120, 138, 122],
      padGrassDark: [86, 104, 96],
      padSnowAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      huskLight: [120, 150, 110],
      huskDark: [70, 96, 76],
      huskBrownAmt: 0.2,
      huskOpen: 0.55,
      kernel: [214, 188, 110],
      kernelDeep: [156, 132, 78],
      kernelDent: 0.4,
      silk: [196, 200, 198],
      outline: [54, 66, 60],
      frostAmt: 1,
      snowCapAmt: 1,
      gloss: 0.35
    }
  };
  function clamp0115(x) {
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function rgb14([r, g, b], a = 1) {
    return `rgba(${Math.round(r)},${Math.round(g)},${Math.round(b)},${a})`;
  }
  function lerp16(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB9(a, b, t) {
    return [lerp16(a[0], b[0], t), lerp16(a[1], b[1], t), lerp16(a[2], b[2], t)];
  }
  function lerpP14(a, b, t) {
    return {
      shadowAmt: lerp16(a.shadowAmt, b.shadowAmt, t),
      padGrass: lerpRGB9(a.padGrass, b.padGrass, t),
      padGrassDark: lerpRGB9(a.padGrassDark, b.padGrassDark, t),
      padSnowAmt: lerp16(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp16(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp16(a.fallenLeafAmt, b.fallenLeafAmt, t),
      huskLight: lerpRGB9(a.huskLight, b.huskLight, t),
      huskDark: lerpRGB9(a.huskDark, b.huskDark, t),
      huskBrownAmt: lerp16(a.huskBrownAmt, b.huskBrownAmt, t),
      huskOpen: lerp16(a.huskOpen, b.huskOpen, t),
      kernel: lerpRGB9(a.kernel, b.kernel, t),
      kernelDeep: lerpRGB9(a.kernelDeep, b.kernelDeep, t),
      kernelDent: lerp16(a.kernelDent, b.kernelDent, t),
      silk: lerpRGB9(a.silk, b.silk, t),
      outline: lerpRGB9(a.outline, b.outline, t),
      frostAmt: lerp16(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp16(a.snowCapAmt, b.snowCapAmt, t),
      gloss: lerp16(a.gloss, b.gloss, t)
    };
  }
  function clampP8(p) {
    return {
      ...p,
      shadowAmt: clamp0115(p.shadowAmt),
      padSnowAmt: clamp0115(p.padSnowAmt),
      blossomAmt: clamp0115(p.blossomAmt),
      fallenLeafAmt: clamp0115(p.fallenLeafAmt),
      huskBrownAmt: clamp0115(p.huskBrownAmt),
      huskOpen: clamp0115(p.huskOpen),
      kernelDent: clamp0115(p.kernelDent),
      frostAmt: clamp0115(p.frostAmt),
      snowCapAmt: clamp0115(p.snowCapAmt),
      gloss: clamp0115(p.gloss)
    };
  }
  var smoother14 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  var COB_TOP = -22;
  var COB_BOT = 14;
  var COB_HALF = 8.5;
  function cobBodyPath(ctx) {
    ctx.beginPath();
    ctx.moveTo(0, COB_TOP);
    ctx.bezierCurveTo(COB_HALF, COB_TOP + 6, COB_HALF, -2, COB_HALF * 0.86, 6);
    ctx.bezierCurveTo(COB_HALF * 0.7, COB_BOT - 2, 4, COB_BOT, 0, COB_BOT);
    ctx.bezierCurveTo(-4, COB_BOT, -COB_HALF * 0.7, COB_BOT - 2, -COB_HALF * 0.86, 6);
    ctx.bezierCurveTo(-COB_HALF, -2, -COB_HALF, COB_TOP + 6, 0, COB_TOP);
    ctx.closePath();
  }
  function huskBackPath(ctx) {
    ctx.beginPath();
    ctx.moveTo(-COB_HALF, COB_TOP + 7);
    ctx.quadraticCurveTo(-COB_HALF - 1, 4, -5, COB_BOT + 1);
    ctx.lineTo(5, COB_BOT + 1);
    ctx.quadraticCurveTo(COB_HALF + 1, 4, COB_HALF, COB_TOP + 7);
    ctx.quadraticCurveTo(0, COB_TOP + 1, -COB_HALF, COB_TOP + 7);
    ctx.closePath();
  }
  function contactShadow(ctx, p) {
    ctx.save();
    ctx.fillStyle = `rgba(0,0,0,${0.26 * p.shadowAmt})`;
    ctx.beginPath();
    ctx.ellipse(3, 21, 15, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function pad(ctx, p) {
    ctx.fillStyle = rgb14(p.padGrassDark);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb14(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb14(p.padGrassDark);
    ctx.lineWidth = 1.3;
    ctx.lineCap = "round";
    for (let i = 0; i < 11; i++) {
      const a = i / 11 * Math.PI * 2;
      const ex = Math.cos(a) * 17.2;
      const ey = 19 + Math.sin(a) * 4.6;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + Math.cos(a) * 1.8, ey + Math.sin(a) * 1.4 - 1.4);
      ctx.stroke();
    }
    ctx.strokeStyle = rgb14(lerpRGB9(p.padGrass, [255, 255, 255], 0.25));
    for (let i = 0; i < 8; i++) {
      const a = i / 8 * Math.PI * 2 + 0.3;
      const ex = Math.cos(a) * 12;
      const ey = 18 + Math.sin(a) * 3.2;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + 0.6, ey - 2);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    if (p.padSnowAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = p.padSnowAmt;
      const snow = ctx.createLinearGradient(0, 14, 0, 23);
      snow.addColorStop(0, "#f3f7fd");
      snow.addColorStop(1, "#cbd8e6");
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, 19, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.9)";
      const specks = [
        [-10, 18],
        [-3, 20],
        [6, 18.5],
        [12, 19.5],
        [2, 17.5]
      ];
      specks.forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
    }
  }
  function blossom2(ctx, p) {
    if (p.blossomAmt < 0.02) return;
    ctx.save();
    ctx.globalAlpha = p.blossomAmt;
    ctx.translate(-12, 19);
    ctx.fillStyle = "rgba(255,240,248,0.96)";
    for (let i = 0; i < 5; i++) {
      const a = i / 5 * Math.PI * 2;
      ctx.beginPath();
      ctx.ellipse(Math.cos(a) * 2.4, Math.sin(a) * 1.4, 1.7, 1.1, a, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = "rgba(248,206,96,0.95)";
    ctx.beginPath();
    ctx.arc(0, 0, 1.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function fallenLeaves(ctx, p) {
    if (p.fallenLeafAmt < 0.02) return;
    ctx.save();
    ctx.globalAlpha = p.fallenLeafAmt;
    const leaves = [
      [-12, 20, -0.5, "#c8772b"],
      [11, 20.5, 0.7, "#b8531f"]
    ];
    leaves.forEach(([lx, ly, rot, col]) => {
      ctx.save();
      ctx.translate(lx, ly);
      ctx.rotate(rot);
      ctx.fillStyle = col;
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.4, 1.7, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = "rgba(90,48,16,0.7)";
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(-3.2, 0);
      ctx.lineTo(3.2, 0);
      ctx.stroke();
      ctx.restore();
    });
    ctx.restore();
  }
  function cobKernels(ctx, p) {
    ctx.save();
    cobBodyPath(ctx);
    ctx.clip();
    ctx.fillStyle = rgb14(p.kernelDeep);
    cobBodyPath(ctx);
    ctx.fill();
    const dentPush = p.kernelDent * 0.6;
    for (let row = 0; row < 9; row++) {
      const y = COB_TOP + 4 + row * 3.6;
      const halfAtRow = COB_HALF * 0.82 * (1 - Math.abs((y + 4) / 30) * 0.18);
      const cols = 5;
      for (let c = 0; c < cols; c++) {
        const fx = (c / (cols - 1) - 0.5) * 2 * (halfAtRow - 1.4);
        const sx = fx + (row % 2 === 0 ? 0 : (halfAtRow - 1.4) / (cols - 1));
        if (Math.abs(sx) > halfAtRow - 0.6) continue;
        const kr = 1.9 - dentPush;
        const face = lerpRGB9(p.kernel, p.kernelDeep, dentPush * 0.5);
        ctx.fillStyle = rgb14(face);
        ctx.beginPath();
        ctx.ellipse(sx, y, kr, kr * 1.15, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb14(lerpRGB9(face, [255, 255, 255], 0.4 * p.gloss));
        ctx.beginPath();
        ctx.ellipse(sx - 0.5, y - 0.6, kr * 0.5, kr * 0.55, 0, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.restore();
    ctx.strokeStyle = rgb14(p.outline, 0.85);
    ctx.lineWidth = 1.1;
    cobBodyPath(ctx);
    ctx.stroke();
  }
  function silkTuft(ctx, p, sway) {
    ctx.save();
    ctx.translate(0, COB_TOP);
    ctx.strokeStyle = rgb14(p.silk, 0.95);
    ctx.lineWidth = 1.1;
    ctx.lineCap = "round";
    const strands2 = [
      [-4, -0.5],
      [-2, -0.2],
      [0, 0],
      [2, 0.2],
      [4, 0.5]
    ];
    strands2.forEach(([sx, lean], i) => {
      const tipX = sx + lean * 6 + sway * (0.4 + i * 0.12);
      const tipY = -7 - Math.abs(sx) * 0.3;
      ctx.beginPath();
      ctx.moveTo(sx * 0.4, 0);
      ctx.quadraticCurveTo(sx * 0.7 + sway * 0.3, -3.5, tipX, tipY);
      ctx.stroke();
    });
    ctx.strokeStyle = rgb14(lerpRGB9(p.silk, [255, 255, 255], 0.4), 0.8);
    ctx.lineWidth = 0.7;
    strands2.forEach(([sx, lean], i) => {
      const tipX = sx + lean * 6 + sway * (0.4 + i * 0.12);
      const tipY = -7 - Math.abs(sx) * 0.3;
      ctx.beginPath();
      ctx.moveTo(sx * 0.55 + sway * 0.2, -3.5);
      ctx.lineTo(tipX, tipY);
      ctx.stroke();
    });
    ctx.lineCap = "butt";
    ctx.restore();
  }
  function husk(ctx, p, leafFlutter) {
    const light = lerpRGB9(p.huskLight, [150, 104, 44], p.huskBrownAmt * 0.5);
    const dark = lerpRGB9(p.huskDark, [96, 64, 28], p.huskBrownAmt * 0.6);
    ctx.fillStyle = rgb14(dark);
    huskBackPath(ctx);
    ctx.fill();
    const leaves = [
      [-1, -7.5, 1],
      [-1, -4, 0.3],
      [1, 7.5, 1],
      [1, 4, 0.3]
    ];
    leaves.forEach(([side, baseX, fw]) => {
      const open = p.huskOpen * (0.45 + 0.55 * Math.abs(baseX) / 7.5);
      const tipX = baseX + side * (2 + open * 9);
      const tipY = COB_TOP + 5 - open * 7 + leafFlutter * fw;
      const midX = baseX + side * (1 + open * 4);
      const midY = -4;
      ctx.fillStyle = rgb14(dark);
      ctx.beginPath();
      ctx.moveTo(baseX, COB_BOT);
      ctx.quadraticCurveTo(midX - side * 3, midY, tipX, tipY);
      ctx.quadraticCurveTo(midX + side * 1.5, midY + 3, baseX + side * 2, COB_BOT);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb14(light);
      ctx.beginPath();
      ctx.moveTo(baseX + side * 0.6, COB_BOT - 1);
      ctx.quadraticCurveTo(midX - side * 2, midY, tipX - side * 1.2, tipY + 1.2);
      ctx.quadraticCurveTo(midX + side * 1, midY + 2.5, baseX + side * 1.8, COB_BOT - 1);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb14(dark, 0.7);
      ctx.lineWidth = 0.8;
      ctx.beginPath();
      ctx.moveTo(baseX + side * 1, COB_BOT - 1);
      ctx.quadraticCurveTo(midX, midY, tipX - side * 0.8, tipY + 0.8);
      ctx.stroke();
      if (p.huskBrownAmt > 0.05) {
        ctx.fillStyle = rgb14([120, 78, 32], p.huskBrownAmt);
        ctx.beginPath();
        ctx.ellipse(tipX, tipY, 1.6, 1.1, side * 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
    });
    ctx.strokeStyle = rgb14(p.outline, 0.85);
    ctx.lineWidth = 1.1;
    ctx.beginPath();
    ctx.moveTo(-COB_HALF, COB_TOP + 7);
    ctx.quadraticCurveTo(-COB_HALF - 1, 4, -5, COB_BOT + 1);
    ctx.lineTo(5, COB_BOT + 1);
    ctx.quadraticCurveTo(COB_HALF + 1, 4, COB_HALF, COB_TOP + 7);
    ctx.stroke();
  }
  function frostAndSnow(ctx, p) {
    if (p.snowCapAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = p.snowCapAmt;
      const cap2 = ctx.createLinearGradient(0, COB_TOP - 3, 0, COB_TOP + 6);
      cap2.addColorStop(0, "#ffffff");
      cap2.addColorStop(1, "#dbe6f2");
      ctx.fillStyle = cap2;
      ctx.beginPath();
      ctx.moveTo(-COB_HALF + 1, COB_TOP + 7);
      ctx.quadraticCurveTo(-6, COB_TOP - 4, 0, COB_TOP - 3);
      ctx.quadraticCurveTo(6, COB_TOP - 4, COB_HALF - 1, COB_TOP + 7);
      ctx.quadraticCurveTo(0, COB_TOP + 3, -COB_HALF + 1, COB_TOP + 7);
      ctx.closePath();
      ctx.fill();
      ctx.restore();
    }
    if (p.frostAmt > 0.01) {
      ctx.save();
      ctx.globalAlpha = 0.7 * p.frostAmt;
      ctx.fillStyle = "#eaf3ff";
      const specks = [
        [-5, -14],
        [-2, -8],
        [-6, -2],
        [-3, 3],
        [3, -12],
        [5, -5],
        [1, -16],
        [-4, 8]
      ];
      specks.forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
    }
  }
  function sheen(ctx, p) {
    if (p.gloss < 0.02) return;
    ctx.save();
    cobBodyPath(ctx);
    ctx.clip();
    const g = ctx.createLinearGradient(-COB_HALF, COB_TOP, COB_HALF * 0.4, COB_BOT);
    g.addColorStop(0, `rgba(255,255,255,${0.22 * p.gloss})`);
    g.addColorStop(0.5, "rgba(255,255,255,0)");
    ctx.fillStyle = g;
    cobBodyPath(ctx);
    ctx.fill();
    ctx.restore();
  }
  function paint14(ctx, pRaw, bob, micro) {
    const p = clampP8(pRaw);
    const m = micro ?? {};
    contactShadow(ctx, p);
    pad(ctx, p);
    blossom2(ctx, p);
    fallenLeaves(ctx, p);
    ctx.save();
    ctx.translate(0, bob);
    husk(ctx, p, m.leafFlutter ?? 0);
    cobKernels(ctx, p);
    sheen(ctx, p);
    if (m.glint !== void 0 && p.huskOpen > 0.2) {
      ctx.save();
      cobBodyPath(ctx);
      ctx.clip();
      const gy = COB_TOP + 4 + m.glint * 28;
      const grad = ctx.createRadialGradient(-1, gy, 0, -1, gy, 5);
      grad.addColorStop(0, "rgba(255,252,220,0.7)");
      grad.addColorStop(1, "rgba(255,252,220,0)");
      ctx.fillStyle = grad;
      ctx.beginPath();
      ctx.ellipse(-1, gy, 5, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    silkTuft(ctx, p, m.silkSway ?? 0);
    frostAndSnow(ctx, p);
    if (m.coldSheen !== void 0 && m.coldSheen > 0.01) {
      ctx.save();
      cobBodyPath(ctx);
      ctx.clip();
      ctx.globalAlpha = 0.12 * m.coldSheen;
      ctx.fillStyle = "#cfe6ff";
      cobBodyPath(ctx);
      ctx.fill();
      ctx.restore();
    }
    if (m.dew !== void 0 && m.dew > 0.01) {
      ctx.save();
      ctx.globalAlpha = m.dew;
      ctx.fillStyle = "rgba(255,255,255,0.85)";
      ctx.beginPath();
      ctx.arc(-3, -6, 0.9 + 0.5 * m.dew, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(2, 2, 0.7, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.restore();
    if (m.extraFlakes) {
      ctx.save();
      ctx.fillStyle = "#ffffff";
      m.extraFlakes.forEach(([fx, fy, r]) => {
        ctx.globalAlpha = 0.85;
        ctx.beginPath();
        ctx.arc(fx, fy, r, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
    }
    ctx.globalAlpha = 1;
  }
  function bobAt14(t) {
    const A = 1.4;
    const w = 1.5;
    return -A * (1 - Math.cos(w * t)) * 0.5;
  }
  function drawCornSpring(ctx) {
    paint14(ctx, SP14.Spring, 0);
  }
  function drawCornSummer(ctx) {
    paint14(ctx, SP14.Summer, 0);
  }
  function drawCornAutumn(ctx) {
    paint14(ctx, SP14.Autumn, 0);
  }
  function drawCornWinter(ctx) {
    paint14(ctx, SP14.Winter, 0);
  }
  function animCornSpring(ctx, t) {
    const dew = 0.35 + 0.35 * (0.5 + 0.5 * Math.sin(t * 1.8));
    paint14(ctx, SP14.Spring, bobAt14(t), {
      silkSway: Math.sin(t * 1.4) * 0.8,
      dew
    });
  }
  function animCornSummer(ctx, t) {
    const glint = t * 0.45 % 1;
    paint14(ctx, SP14.Summer, bobAt14(t), {
      silkSway: Math.sin(t * 1.3) * 1,
      glint
    });
  }
  function animCornAutumn(ctx, t) {
    const leafFlutter = Math.sin(t * 2.2) * 1.4;
    paint14(ctx, SP14.Autumn, bobAt14(t), {
      leafFlutter,
      silkSway: Math.sin(t * 1.1) * 0.7
    });
  }
  function animCornWinter(ctx, t) {
    const seeds = [
      [-6, 1.1, 0],
      [7, 0.9, 0.5]
    ];
    const span = 36;
    const flakes = seeds.map(([fx, r, phase]) => {
      const prog = ((t / 3.6 + phase) % 1 + 1) % 1;
      const fy = -22 + prog * span;
      const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
      return [driftX, fy, r];
    });
    const coldSheen = 0.5 + 0.5 * Math.sin(t * 0.8);
    paint14(ctx, SP14.Winter, bobAt14(t), {
      silkSway: Math.sin(t * 0.9) * 0.5,
      extraFlakes: flakes,
      coldSheen
    });
  }
  function makeTransition13(from, to) {
    return (ctx, pp) => {
      const t = smoother14(clamp0115(pp));
      paint14(ctx, lerpP14(SP14[from], SP14[to], t), 0);
    };
  }
  var springToSummer11 = makeTransition13("Spring", "Summer");
  var summerToAutumn11 = makeTransition13("Summer", "Autumn");
  var autumnToWinter11 = makeTransition13("Autumn", "Winter");
  var VARIANTS16 = {
    Spring: { draw: drawCornSpring, anim: animCornSpring },
    Summer: { draw: drawCornSummer, anim: animCornSummer },
    Autumn: { draw: drawCornAutumn, anim: animCornAutumn },
    Winter: { draw: drawCornWinter, anim: animCornWinter }
  };
  var TRANSITIONS16 = {
    0: springToSummer11,
    1: summerToAutumn11,
    2: autumnToWinter11
  };

  // src/textures/seasonal/veg/pepper.ts
  function clamp0116(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother15 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp17(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB10(a, b, t) {
    return [lerp17(a[0], b[0], t), lerp17(a[1], b[1], t), lerp17(a[2], b[2], t)];
  }
  function rgb15(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba8(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0116(a)})`;
  }
  function lerpP15(a, b, t) {
    return {
      skinLight: lerpRGB10(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB10(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB10(a.skinDark, b.skinDark, t),
      stem: lerpRGB10(a.stem, b.stem, t),
      padGrass: lerpRGB10(a.padGrass, b.padGrass, t),
      padDark: lerpRGB10(a.padDark, b.padDark, t),
      soil: lerpRGB10(a.soil, b.soil, t),
      outline: lerpRGB10(a.outline, b.outline, t),
      light: lerpRGB10(a.light, b.light, t),
      lightAmt: lerp17(a.lightAmt, b.lightAmt, t),
      ripeness: lerp17(a.ripeness, b.ripeness, t),
      gloss: lerp17(a.gloss, b.gloss, t),
      frostAmt: lerp17(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp17(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp17(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp17(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp17(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP9(p) {
    return {
      ...p,
      lightAmt: clamp0116(p.lightAmt),
      ripeness: clamp0116(p.ripeness),
      gloss: clamp0116(p.gloss),
      frostAmt: clamp0116(p.frostAmt),
      snowCapAmt: clamp0116(p.snowCapAmt),
      padSnowAmt: clamp0116(p.padSnowAmt),
      blossomAmt: clamp0116(p.blossomAmt),
      fallenLeafAmt: clamp0116(p.fallenLeafAmt)
    };
  }
  var SP15 = {
    // Spring — fresh pastel; green unripe pepper; bright lime dewy pad + blossom.
    Spring: {
      skinLight: [150, 210, 96],
      skinMid: [96, 170, 64],
      skinDark: [54, 116, 44],
      stem: [104, 158, 58],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [40, 64, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.15,
      gloss: 0.18,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — richest saturated peak; full glossy RED; warm light, strong gloss.
    Summer: {
      skinLight: [248, 96, 70],
      skinMid: [214, 46, 40],
      skinDark: [150, 22, 28],
      stem: [86, 150, 50],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [86, 18, 22],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.75,
      gloss: 0.95,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — deeper darker ripe red; gold/rust pad; a couple fallen leaves.
    Autumn: {
      skinLight: [212, 76, 52],
      skinMid: [172, 38, 34],
      skinDark: [108, 22, 26],
      stem: [122, 124, 56],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [70, 18, 20],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.45,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; frost-dusted red pepper, snow cap, pad snow.
    Winter: {
      skinLight: [196, 84, 70],
      skinMid: [158, 48, 50],
      skinDark: [98, 34, 44],
      stem: [108, 122, 86],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [56, 40, 52],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.9,
      gloss: 0.25,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var PEP_TOP = -10;
  var PEP_BOT = 16;
  var PEP_HALF = 13;
  var LOBES = [-8.5, -2.8, 3.2, 9];
  function pepperBodyPath(ctx, bob) {
    const t = PEP_TOP + bob;
    const b = PEP_BOT + bob;
    const h = PEP_HALF;
    ctx.beginPath();
    ctx.moveTo(-h * 0.78, t + 2);
    ctx.quadraticCurveTo(-h * 0.7, t - 4, -3.4, t - 5.4);
    ctx.quadraticCurveTo(0, t - 6.6, 3.4, t - 5.4);
    ctx.quadraticCurveTo(h * 0.7, t - 4, h * 0.78, t + 2);
    ctx.quadraticCurveTo(h + 1, lerp17(t, b, 0.45), h * 0.92, b - 5);
    ctx.quadraticCurveTo(h * 0.82, b + 1.5, 9, b - 1);
    ctx.quadraticCurveTo(6.6, b + 2.4, 3.2, b - 0.5);
    ctx.quadraticCurveTo(0.3, b + 2.6, -2.8, b - 0.5);
    ctx.quadraticCurveTo(-6, b + 2.4, -8.5, b - 1);
    ctx.quadraticCurveTo(-h * 0.82, b + 1.5, -h * 0.92, b - 5);
    ctx.quadraticCurveTo(-h - 1, lerp17(t, b, 0.45), -h * 0.78, t + 2);
    ctx.closePath();
  }
  function paint15(ctx, raw, bob) {
    const p = clampP9(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba8(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb15(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb15(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb15(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba8([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba8([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba8([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba8([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba8(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba8([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba8(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba8([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb15(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, PEP_BOT + bob + 1.5, 9, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba8(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, PEP_BOT + bob + 2, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      pepperBodyPath(ctx, bob);
      ctx.fillStyle = rgb15(p.outline);
      ctx.fill();
      ctx.save();
      pepperBodyPath(ctx, bob);
      ctx.clip();
      const top = PEP_TOP + bob;
      const bot = PEP_BOT + bob;
      ctx.fillStyle = rgb15(p.skinMid);
      ctx.fillRect(-PEP_HALF - 3, top - 8, (PEP_HALF + 3) * 2, bot - top + 14);
      const litGrad = ctx.createLinearGradient(-PEP_HALF, top - 4, PEP_HALF, bot);
      litGrad.addColorStop(0, rgb15(p.skinLight));
      litGrad.addColorStop(0.45, rgb15(p.skinMid));
      litGrad.addColorStop(1, rgb15(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.9;
      ctx.beginPath();
      ctx.ellipse(-2, lerp17(top, bot, 0.42), PEP_HALF + 2, (bot - top) * 0.62, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba8(p.skinDark, 0.85);
      ctx.lineWidth = 2.2;
      LOBES.forEach((lx, i) => {
        if (i === 0) return;
        const cx = (LOBES[i - 1] + lx) / 2;
        ctx.beginPath();
        ctx.moveTo(cx + 1.4, top + 1);
        ctx.quadraticCurveTo(cx, lerp17(top, bot, 0.55), cx, bot - 1.5);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba8(p.skinLight, 0.5);
      ctx.lineWidth = 1.3;
      [-6.4, 0.2, 6.2].forEach((cx) => {
        ctx.beginPath();
        ctx.moveTo(cx - 1.2, top + 1);
        ctx.quadraticCurveTo(cx - 1.6, lerp17(top, bot, 0.5), cx - 1.4, bot - 3);
        ctx.stroke();
      });
      ctx.fillStyle = rgba8(p.skinDark, 0.55);
      LOBES.forEach((lx) => {
        ctx.beginPath();
        ctx.ellipse(lx, bot - 2.5, 3.2, 3.6, 0, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba8([255, 255, 255], 0.16 + 0.6 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-5.5, lerp17(top, bot, 0.34), 1.7, (bot - top) * 0.28, -0.12, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba8([255, 255, 255], 0.1 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(1.5, lerp17(top, bot, 0.3), 1, (bot - top) * 0.22, -0.05, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba8([210, 230, 250], 0.28 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp17(top, bot, 0.28), PEP_HALF, (bot - top) * 0.3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba8([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-8, top + 2],
          [-3, top + 1],
          [3, top + 2.5],
          [8, top + 3],
          [-6, lerp17(top, bot, 0.4)],
          [5, lerp17(top, bot, 0.45)],
          [0, lerp17(top, bot, 0.3)]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba8([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-PEP_HALF * 0.7, top + 1);
        ctx.quadraticCurveTo(-4, top - 6, 0, top - 4.5);
        ctx.quadraticCurveTo(4, top - 6, PEP_HALF * 0.7, top + 1);
        ctx.quadraticCurveTo(6, top + 3.5, 2, top + 2);
        ctx.quadraticCurveTo(0, top + 4, -2, top + 2);
        ctx.quadraticCurveTo(-6, top + 3.5, -PEP_HALF * 0.7, top + 1);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba8([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 1.6, PEP_HALF * 0.62, 1.6, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const stemBaseY = top - 3.5;
      ctx.fillStyle = rgb15(p.stem);
      ctx.beginPath();
      ctx.moveTo(-5.4, stemBaseY + 2);
      ctx.quadraticCurveTo(-3, stemBaseY - 1, -1.4, stemBaseY + 1.4);
      ctx.quadraticCurveTo(0, stemBaseY - 1, 1.4, stemBaseY + 1.4);
      ctx.quadraticCurveTo(3, stemBaseY - 1, 5.4, stemBaseY + 2);
      ctx.quadraticCurveTo(2.5, stemBaseY + 3.2, 0, stemBaseY + 2.6);
      ctx.quadraticCurveTo(-2.5, stemBaseY + 3.2, -5.4, stemBaseY + 2);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb15(p.outline);
      ctx.lineWidth = 1.1;
      ctx.stroke();
      ctx.strokeStyle = rgb15(p.outline);
      ctx.lineWidth = 4.2;
      ctx.beginPath();
      ctx.moveTo(-0.3, stemBaseY + 0.5);
      ctx.quadraticCurveTo(-0.8, stemBaseY - 5, 0.6, stemBaseY - 8);
      ctx.stroke();
      ctx.strokeStyle = rgb15(p.stem);
      ctx.lineWidth = 2.6;
      ctx.beginPath();
      ctx.moveTo(-0.3, stemBaseY + 0.5);
      ctx.quadraticCurveTo(-0.8, stemBaseY - 5, 0.6, stemBaseY - 8);
      ctx.stroke();
      ctx.strokeStyle = rgba8(p.skinLight, 0.4);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(-1.1, stemBaseY - 1);
      ctx.quadraticCurveTo(-1.4, stemBaseY - 5, -0.4, stemBaseY - 7.5);
      ctx.stroke();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        ctx.fillStyle = rgba8(p.light, p.lightAmt);
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba8(p.light, p.lightAmt));
        lg.addColorStop(1, rgba8(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt15(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw9(season) {
    return (ctx) => paint15(ctx, SP15[season], 0);
  }
  function anim9(season) {
    return (ctx, t) => {
      const bob = bobAt15(t);
      paint15(ctx, SP15[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -2 + bob + Math.sin(t * 1.1) * 1.2;
          ctx.beginPath();
          ctx.arc(-5, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const top = PEP_TOP + bob;
          const bot = PEP_BOT + bob;
          const gy = lerp17(top + 1, bot - 2, prog);
          ctx.fillStyle = "rgba(255,255,255,0.85)";
          ctx.beginPath();
          ctx.ellipse(-5, gy, 1.5, 2.6, -0.1, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.ellipse(1.5, gy * 0.96, 1, 1.9, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.12 + 0.16 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(-4, -4 + bob, 4.5, 3.2, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.12 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 0 + bob, 6, 4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition14(fromIdx) {
    const from = SP15[SEASON_NAMES[fromIdx]];
    const to = SP15[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother15(clamp0116(pp));
      paint15(ctx, lerpP15(from, to, k), 0);
    };
  }
  var springToSummer12 = makeTransition14(0);
  var summerToAutumn12 = makeTransition14(1);
  var autumnToWinter12 = makeTransition14(2);
  var VARIANTS17 = {
    Spring: { draw: draw9("Spring"), anim: anim9("Spring") },
    Summer: { draw: draw9("Summer"), anim: anim9("Summer") },
    Autumn: { draw: draw9("Autumn"), anim: anim9("Autumn") },
    Winter: { draw: draw9("Winter"), anim: anim9("Winter") }
  };
  var TRANSITIONS17 = {
    0: springToSummer12,
    1: summerToAutumn12,
    2: autumnToWinter12
  };

  // src/textures/seasonal/veg/mushroom.ts
  function clamp0117(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  var smoother16 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp18(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB11(a, b, t) {
    return [lerp18(a[0], b[0], t), lerp18(a[1], b[1], t), lerp18(a[2], b[2], t)];
  }
  function rgb16(c, alpha = 1) {
    const r = Math.round(c[0]);
    const g = Math.round(c[1]);
    const b = Math.round(c[2]);
    return alpha >= 1 ? `rgb(${r},${g},${b})` : `rgba(${r},${g},${b},${alpha})`;
  }
  function mix2(c, target, k) {
    return lerpRGB11(c, target, clamp0117(k));
  }
  function shade(c, k) {
    return k >= 0 ? mix2(c, [255, 255, 255], k) : mix2(c, [0, 0, 0], -k);
  }
  var SPRING = {
    cap: [223, 74, 64],
    // fresh bright red
    spot: [255, 250, 240],
    stem: [246, 238, 220],
    grass: [150, 214, 96],
    // bright lime dewy
    soil: [92, 70, 44],
    outline: [74, 48, 40],
    light: [214, 240, 255],
    // cool-bright
    lightAmt: 0.32,
    capOpenAmount: 0.18,
    // small button caps
    gloss: 0.32,
    droop: 0,
    frostAmt: 0,
    snowCapAmt: 0,
    padSnowAmt: 0,
    blossomAmt: 0.85,
    fallenLeafAmt: 0
  };
  var SUMMER = {
    cap: [214, 44, 40],
    // vivid saturated red
    spot: [255, 248, 232],
    stem: [240, 230, 206],
    grass: [86, 168, 64],
    // saturated mid-green
    soil: [82, 60, 36],
    outline: [66, 40, 32],
    light: [255, 246, 214],
    // warm
    lightAmt: 0.4,
    capOpenAmount: 0.6,
    // full rounded domes
    gloss: 0.62,
    droop: 0,
    frostAmt: 0,
    snowCapAmt: 0,
    padSnowAmt: 0,
    blossomAmt: 0,
    fallenLeafAmt: 0
  };
  var AUTUMN = {
    cap: [176, 40, 36],
    // deep mature red
    spot: [238, 222, 188],
    // ageing cream
    stem: [226, 210, 178],
    grass: [150, 142, 78],
    // olive-tan
    soil: [86, 58, 30],
    outline: [70, 42, 28],
    light: [255, 224, 168],
    // low amber
    lightAmt: 0.42,
    capOpenAmount: 1,
    // peak size, edges flattening
    gloss: 0.34,
    droop: 0.06,
    frostAmt: 0,
    snowCapAmt: 0,
    padSnowAmt: 0,
    blossomAmt: 0,
    fallenLeafAmt: 0.9
  };
  var WINTER = {
    cap: [168, 76, 70],
    // muted but clearly red
    spot: [240, 240, 244],
    stem: [222, 220, 222],
    grass: [126, 150, 150],
    // cool grey-green under snow
    soil: [78, 76, 86],
    outline: [64, 60, 72],
    light: [206, 224, 248],
    // cool blue-grey
    lightAmt: 0.46,
    capOpenAmount: 0.78,
    // mature, drooping a touch
    gloss: 0.22,
    droop: 0.22,
    frostAmt: 0.85,
    snowCapAmt: 0.7,
    padSnowAmt: 0.92,
    blossomAmt: 0,
    fallenLeafAmt: 0
  };
  var SP16 = {
    Spring: SPRING,
    Summer: SUMMER,
    Autumn: AUTUMN,
    Winter: WINTER
  };
  function lerpP16(a, b, t) {
    return {
      cap: lerpRGB11(a.cap, b.cap, t),
      spot: lerpRGB11(a.spot, b.spot, t),
      stem: lerpRGB11(a.stem, b.stem, t),
      grass: lerpRGB11(a.grass, b.grass, t),
      soil: lerpRGB11(a.soil, b.soil, t),
      outline: lerpRGB11(a.outline, b.outline, t),
      light: lerpRGB11(a.light, b.light, t),
      lightAmt: lerp18(a.lightAmt, b.lightAmt, t),
      capOpenAmount: lerp18(a.capOpenAmount, b.capOpenAmount, t),
      gloss: lerp18(a.gloss, b.gloss, t),
      droop: lerp18(a.droop, b.droop, t),
      frostAmt: lerp18(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp18(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp18(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp18(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp18(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function paintPad(ctx, p) {
    const cy = 19;
    ctx.fillStyle = "rgba(0,0,0,0.2)";
    ctx.beginPath();
    ctx.ellipse(3, cy + 2.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb16(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, cy + 1.4, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb16(p.grass);
    ctx.beginPath();
    ctx.ellipse(0, cy - 0.2, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb16(shade(p.grass, 0.22));
    ctx.beginPath();
    ctx.ellipse(-2, cy - 1.4, 13, 3.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb16(shade(p.grass, -0.18));
    ctx.lineWidth = 1.2;
    ctx.lineCap = "round";
    for (let i = 0; i < 11; i++) {
      const a = -0.18 + i / 10 * (Math.PI + 0.36);
      const ex = Math.cos(a) * 17.5;
      const ey = cy - 0.2 + Math.sin(a) * 4.8;
      const lean = ex < 0 ? -1 : 1;
      ctx.strokeStyle = rgb16(shade(p.grass, i % 2 ? 0.18 : -0.14));
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + lean * 1.2, ey - 2.6);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    if (p.blossomAmt > 0.01) {
      const a = clamp0117(p.blossomAmt);
      const flowers = [
        [-11, 18, 1],
        [12, 20, 0.85]
      ];
      flowers.forEach(([fx, fy, s]) => {
        ctx.globalAlpha = a;
        ctx.fillStyle = "rgba(255,255,255,0.95)";
        for (let k = 0; k < 5; k++) {
          const ang = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.ellipse(fx + Math.cos(ang) * 1.6 * s, fy + Math.sin(ang) * 1.6 * s, 1 * s, 1 * s, 0, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = "rgba(255,214,84,0.95)";
        ctx.beginPath();
        ctx.arc(fx, fy, 0.9 * s, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.globalAlpha = 1;
    }
    if (p.fallenLeafAmt > 0.01) {
      const a = clamp0117(p.fallenLeafAmt);
      const leaves = [
        [-12, 20, 0.5, [196, 120, 40]],
        [11, 21, -0.7, [168, 70, 36]]
      ];
      leaves.forEach(([lx, ly, rot, col]) => {
        ctx.save();
        ctx.globalAlpha = a;
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb16(col);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb16(shade(col, -0.3));
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-3.2, 0);
        ctx.lineTo(3.2, 0);
        ctx.stroke();
        ctx.restore();
      });
      ctx.globalAlpha = 1;
    }
    if (p.padSnowAmt > 0.01) {
      const a = clamp0117(p.padSnowAmt);
      ctx.globalAlpha = a;
      const snow = ctx.createLinearGradient(0, cy - 4, 0, cy + 4);
      snow.addColorStop(0, "rgba(244,248,255,1)");
      snow.addColorStop(1, "rgba(206,222,240,1)");
      ctx.fillStyle = snow;
      ctx.beginPath();
      ctx.ellipse(0, cy - 0.6, 16.5 * (0.6 + 0.4 * a), 4.4 * (0.6 + 0.4 * a), 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
    }
  }
  function paintMushroom(ctx, p, bx, by, scale, sparkle) {
    const open = clamp0117(p.capOpenAmount);
    const capW = (6 + open * 4) * scale;
    const capH = (5.6 - open * 1.4) * scale;
    const droop = p.droop * 2.4 * scale;
    const stemH = (9 - open * 1) * scale;
    const stemW = (2.6 + open * 0.4) * scale;
    const capCy = by - stemH;
    const stemGrad = ctx.createLinearGradient(bx - stemW, 0, bx + stemW, 0);
    stemGrad.addColorStop(0, rgb16(shade(p.stem, -0.22)));
    stemGrad.addColorStop(0.45, rgb16(p.stem));
    stemGrad.addColorStop(1, rgb16(shade(p.stem, 0.12)));
    ctx.fillStyle = stemGrad;
    ctx.beginPath();
    ctx.moveTo(bx - stemW, by);
    ctx.quadraticCurveTo(bx - stemW * 0.7, by - stemH * 0.5, bx - stemW * 0.85, capCy + capH * 0.3);
    ctx.lineTo(bx + stemW * 0.85, capCy + capH * 0.3);
    ctx.quadraticCurveTo(bx + stemW * 0.7, by - stemH * 0.5, bx + stemW, by);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb16(p.outline, 0.9);
    ctx.lineWidth = 1;
    ctx.stroke();
    ctx.save();
    ctx.translate(bx, capCy);
    ctx.fillStyle = rgb16(shade(p.cap, -0.42));
    ctx.beginPath();
    ctx.ellipse(0, droop + capH * 0.35, capW + 0.8, capH + 0.8, 0, Math.PI, Math.PI * 2);
    ctx.fill();
    const capGrad = ctx.createLinearGradient(-capW, -capH, capW, capH);
    capGrad.addColorStop(0, rgb16(shade(p.cap, 0.28)));
    capGrad.addColorStop(0.55, rgb16(p.cap));
    capGrad.addColorStop(1, rgb16(shade(p.cap, -0.3)));
    ctx.fillStyle = capGrad;
    ctx.beginPath();
    ctx.ellipse(0, droop, capW, capH, 0, Math.PI, Math.PI * 2);
    ctx.ellipse(0, droop, capW, capH * 0.4, 0, 0, Math.PI);
    ctx.fill();
    ctx.strokeStyle = rgb16(p.outline);
    ctx.lineWidth = 1.1;
    ctx.beginPath();
    ctx.ellipse(0, droop, capW, capH, 0, Math.PI, Math.PI * 2);
    ctx.stroke();
    if (p.gloss > 0.01) {
      ctx.globalAlpha = clamp0117(p.gloss);
      ctx.fillStyle = "rgba(255,255,255,0.85)";
      ctx.beginPath();
      ctx.ellipse(-capW * 0.38, droop - capH * 0.42, capW * 0.34, capH * 0.3, -0.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
    }
    const spots = [
      [-capW * 0.45, droop - capH * 0.3, 0.9],
      [capW * 0.1, droop - capH * 0.55, 1],
      [capW * 0.5, droop - capH * 0.2, 0.85],
      [-capW * 0.1, droop - capH * 0.08, 0.8],
      [capW * 0.32, droop - capH * 0.66, 0.6]
    ];
    spots.forEach(([sx, sy, sr], i) => {
      const r = sr * 1.5 * scale;
      ctx.fillStyle = rgb16(p.spot);
      ctx.beginPath();
      ctx.ellipse(sx, sy, r, r * 0.86, 0, 0, Math.PI * 2);
      ctx.fill();
      if (sparkle > 0.01 && i % 2 === 0) {
        ctx.globalAlpha = clamp0117(sparkle);
        ctx.fillStyle = "rgba(255,255,255,0.9)";
        ctx.beginPath();
        ctx.arc(sx - r * 0.3, sy - r * 0.3, r * 0.4, 0, Math.PI * 2);
        ctx.fill();
        ctx.globalAlpha = 1;
      }
    });
    if (p.frostAmt > 0.01) {
      const f = clamp0117(p.frostAmt);
      ctx.globalAlpha = 0.5 * f;
      ctx.strokeStyle = "rgba(226,240,255,1)";
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.ellipse(0, droop + capH * 0.15, capW * 0.96, capH * 0.92, 0, Math.PI * 0.05, Math.PI * 0.95);
      ctx.stroke();
      ctx.fillStyle = "rgba(240,248,255,1)";
      const frost = [
        [-capW * 0.6, droop - capH * 0.1],
        [capW * 0.55, droop - capH * 0.35],
        [-capW * 0.2, droop - capH * 0.5]
      ];
      frost.forEach(([fx, fy]) => {
        ctx.globalAlpha = f * (0.4 + 0.5 * sparkle + 0.3);
        ctx.beginPath();
        ctx.arc(fx, fy, 0.7 * scale, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.globalAlpha = 1;
    }
    if (p.snowCapAmt > 0.01) {
      const s = clamp0117(p.snowCapAmt);
      ctx.globalAlpha = s;
      ctx.fillStyle = "rgba(248,251,255,1)";
      ctx.beginPath();
      ctx.ellipse(-capW * 0.12, droop - capH * 0.62, capW * 0.62 * s + capW * 0.18, capH * 0.5 * s + 0.6, -0.18, Math.PI, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(-capW * 0.4, droop - capH * 0.42, 1.4 * scale, 1 * scale, 0, 0, Math.PI * 2);
      ctx.ellipse(capW * 0.2, droop - capH * 0.5, 1.6 * scale, 1.1 * scale, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
    }
    ctx.restore();
  }
  function paint16(ctx, p, bob) {
    ctx.save();
    try {
      paintPad(ctx, p);
      const baseY = 18 + bob;
      paintMushroom(ctx, p, -8.5, baseY - 1.5, 0.62, 0);
      paintMushroom(ctx, p, 9.5, baseY - 0.5, 0.7, 0);
      paintMushroom(ctx, p, 0.5, baseY + 0.5, 1, 0);
      if (p.lightAmt > 1e-3) {
        const g = ctx.createRadialGradient(-8, -8, 2, 0, 0, 34);
        g.addColorStop(0, rgb16(p.light, 0.16 * p.lightAmt));
        g.addColorStop(1, rgb16(p.light, 0));
        ctx.fillStyle = g;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  var drawFor = (season) => (ctx) => paint16(ctx, SP16[season], 0);
  function bobAt16(t, amp, w) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var animFor = (season) => (ctx, t) => {
    const bob = -bobAt16(t, 0.9, 1.5);
    ctx.save();
    try {
      paintPad(ctx, SP16[season]);
      const p = SP16[season];
      const baseY = 18 + bob;
      paintMushroom(ctx, p, -8.5, baseY - 1.5, 0.62, 0);
      paintMushroom(ctx, p, 9.5, baseY - 0.5, 0.7, 0);
      let sparkle = 0;
      if (season === "Spring") sparkle = 0.4 + 0.4 * (0.5 - 0.5 * Math.cos(t * 2.2));
      if (season === "Summer") sparkle = 0.5 * (0.5 - 0.5 * Math.cos(t * 1.8));
      if (season === "Winter") sparkle = 0.45 * (0.5 - 0.5 * Math.cos(t * 1.4));
      paintMushroom(ctx, p, 0.5, baseY + 0.5, 1, sparkle);
      if (season === "Autumn") {
        const rock = Math.sin(t * 1.2) * 0.25;
        ctx.save();
        ctx.globalAlpha = clamp0117(p.fallenLeafAmt);
        ctx.translate(-12, 20);
        ctx.rotate(0.5 + rock);
        ctx.fillStyle = "rgb(196,120,40)";
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        ctx.globalAlpha = 1;
      }
      if (season === "Winter") {
        const seeds = [
          [-7, 1, 0],
          [8, 0.8, 0.5]
        ];
        ctx.fillStyle = "#ffffff";
        seeds.forEach(([fx, r, phase]) => {
          const prog = ((t / 3.4 + phase) % 1 + 1) % 1;
          const fy = -18 + prog * 32;
          const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
          ctx.globalAlpha = 0.85 * Math.sin(prog * Math.PI);
          ctx.beginPath();
          ctx.arc(dx, fy, r, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.globalAlpha = 1;
      }
      if (p.lightAmt > 1e-3) {
        const g = ctx.createRadialGradient(-8, -8, 2, 0, 0, 34);
        g.addColorStop(0, rgb16(p.light, 0.16 * p.lightAmt));
        g.addColorStop(1, rgb16(p.light, 0));
        ctx.fillStyle = g;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  };
  var springToSummer13 = (ctx, pp) => paint16(ctx, lerpP16(SP16.Spring, SP16.Summer, smoother16(clamp0117(pp))), 0);
  var summerToAutumn13 = (ctx, pp) => paint16(ctx, lerpP16(SP16.Summer, SP16.Autumn, smoother16(clamp0117(pp))), 0);
  var autumnToWinter13 = (ctx, pp) => paint16(ctx, lerpP16(SP16.Autumn, SP16.Winter, smoother16(clamp0117(pp))), 0);
  var VARIANTS18 = {
    Spring: { draw: drawFor("Spring"), anim: animFor("Spring") },
    Summer: { draw: drawFor("Summer"), anim: animFor("Summer") },
    Autumn: { draw: drawFor("Autumn"), anim: animFor("Autumn") },
    Winter: { draw: drawFor("Winter"), anim: animFor("Winter") }
  };
  var TRANSITIONS18 = {
    0: springToSummer13,
    1: summerToAutumn13,
    2: autumnToWinter13
  };

  // src/textures/seasonal/veg/beet.ts
  function clamp0118(x) {
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp19(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB12(a, b, t) {
    return [lerp19(a[0], b[0], t), lerp19(a[1], b[1], t), lerp19(a[2], b[2], t)];
  }
  function rgb17(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba9(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0118(a)})`;
  }
  function smoother17(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt17(t, amp, w) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP17 = {
    Spring: {
      rootHi: [216, 96, 120],
      rootMid: [180, 56, 84],
      rootShade: [126, 32, 58],
      leafHi: [150, 206, 96],
      leafMid: [86, 156, 58],
      veins: [196, 70, 96],
      padGrass: [138, 206, 92],
      padShade: [80, 140, 60],
      soil: [92, 64, 38],
      outline: [70, 30, 40],
      light: [225, 240, 255],
      ripeness: 0.32,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0,
      leafYellow: 0,
      gloss: 0.3,
      lightAmt: 0.28
    },
    Summer: {
      rootHi: [214, 70, 96],
      rootMid: [168, 36, 64],
      rootShade: [104, 18, 42],
      leafHi: [120, 184, 70],
      leafMid: [54, 122, 44],
      veins: [186, 48, 72],
      padGrass: [88, 174, 70],
      padShade: [48, 116, 50],
      soil: [82, 54, 30],
      outline: [56, 22, 32],
      light: [255, 244, 210],
      ripeness: 0.62,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      leafYellow: 0,
      gloss: 0.85,
      lightAmt: 0.34
    },
    Autumn: {
      rootHi: [186, 60, 84],
      rootMid: [138, 30, 56],
      rootShade: [82, 16, 36],
      leafHi: [150, 158, 70],
      leafMid: [78, 104, 44],
      veins: [168, 56, 56],
      padGrass: [150, 154, 84],
      padShade: [104, 100, 56],
      soil: [88, 58, 32],
      outline: [54, 28, 24],
      light: [255, 214, 150],
      ripeness: 0.92,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.8,
      leafYellow: 0.7,
      gloss: 0.4,
      lightAmt: 0.32
    },
    Winter: {
      rootHi: [176, 92, 110],
      rootMid: [134, 46, 70],
      rootShade: [86, 28, 48],
      leafHi: [120, 158, 118],
      leafMid: [70, 110, 80],
      veins: [150, 78, 86],
      padGrass: [186, 204, 214],
      padShade: [134, 158, 178],
      soil: [96, 84, 78],
      outline: [60, 50, 60],
      light: [206, 226, 255],
      ripeness: 0.78,
      frostAmt: 0.62,
      snowCapAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      leafYellow: 0.12,
      gloss: 0.4,
      lightAmt: 0.3
    }
  };
  function lerpP17(a, b, t) {
    return {
      rootHi: lerpRGB12(a.rootHi, b.rootHi, t),
      rootMid: lerpRGB12(a.rootMid, b.rootMid, t),
      rootShade: lerpRGB12(a.rootShade, b.rootShade, t),
      leafHi: lerpRGB12(a.leafHi, b.leafHi, t),
      leafMid: lerpRGB12(a.leafMid, b.leafMid, t),
      veins: lerpRGB12(a.veins, b.veins, t),
      padGrass: lerpRGB12(a.padGrass, b.padGrass, t),
      padShade: lerpRGB12(a.padShade, b.padShade, t),
      soil: lerpRGB12(a.soil, b.soil, t),
      outline: lerpRGB12(a.outline, b.outline, t),
      light: lerpRGB12(a.light, b.light, t),
      ripeness: lerp19(a.ripeness, b.ripeness, t),
      frostAmt: lerp19(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp19(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp19(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp19(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp19(a.fallenLeafAmt, b.fallenLeafAmt, t),
      leafYellow: lerp19(a.leafYellow, b.leafYellow, t),
      gloss: lerp19(a.gloss, b.gloss, t),
      lightAmt: lerp19(a.lightAmt, b.lightAmt, t)
    };
  }
  function paint17(ctx, p, bob) {
    ctx.save();
    ctx.globalAlpha = 1;
    const outline = rgb17(p.outline);
    ctx.fillStyle = "rgba(0,0,0,0.20)";
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb17(p.padShade);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb17(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb17(p.padGrass);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    for (let i = 0; i < 11; i++) {
      const a = Math.PI + i / 10 * Math.PI;
      const ex = Math.cos(a) * 17.4;
      const ey = 19 + Math.sin(a) * 5;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + Math.cos(a) * 0.6, ey - 2.1 - i % 2 * 0.7);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgba9(p.light, 0.18 * p.lightAmt + 0.06);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 11, 2.6, -0.2, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 0.01) {
      ctx.fillStyle = rgba9([244, 250, 255], 0.9 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.2, 17 * (0.7 + 0.3 * p.padSnowAmt), 4.4 * p.padSnowAmt + 1.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba9([255, 255, 255], 0.85 * p.padSnowAmt);
      [[-9, 18], [6, 19.5], [12, 17.5], [-2, 20]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    ctx.save();
    ctx.translate(0, bob);
    const ripe = clamp0118(p.ripeness);
    const bw = 12 + ripe * 1.8;
    const bh = 11 + ripe * 1.6;
    const cy = 9;
    const tipY = cy + bh + 6.2;
    ctx.fillStyle = rgb17(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, cy + bh * 0.4, bw * 0.9, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    function bulbPath2() {
      ctx.beginPath();
      ctx.moveTo(-bw, cy - bh * 0.1);
      ctx.bezierCurveTo(-bw, cy - bh, -bw * 0.4, cy - bh * 1.25, 0, cy - bh * 1.2);
      ctx.bezierCurveTo(bw * 0.4, cy - bh * 1.25, bw, cy - bh, bw, cy - bh * 0.1);
      ctx.bezierCurveTo(bw, cy + bh * 0.7, bw * 0.45, cy + bh * 0.95, 2, tipY - 2);
      ctx.lineTo(0.6, tipY);
      ctx.lineTo(-0.6, tipY - 0.4);
      ctx.bezierCurveTo(-bw * 0.45, cy + bh * 0.95, -bw, cy + bh * 0.7, -bw, cy - bh * 0.1);
      ctx.closePath();
    }
    bulbPath2();
    ctx.fillStyle = rgb17(p.rootShade);
    ctx.fill();
    ctx.save();
    ctx.translate(-0.5, -0.6);
    bulbPath2();
    ctx.fillStyle = rgb17(p.rootMid);
    ctx.fill();
    ctx.restore();
    ctx.save();
    bulbPath2();
    ctx.clip();
    ctx.fillStyle = rgb17(p.rootHi);
    ctx.beginPath();
    ctx.ellipse(-bw * 0.32, cy - bh * 0.45, bw * 0.7, bh * 0.85, -0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgba9(p.rootShade, 0.35);
    ctx.lineWidth = 1;
    for (let r = 0; r < 2; r++) {
      const ry = cy + bh * (0.15 + r * 0.35);
      ctx.beginPath();
      ctx.ellipse(0, ry, bw * (0.7 - r * 0.18), 2.2, 0, 0.1 * Math.PI, 0.9 * Math.PI);
      ctx.stroke();
    }
    ctx.restore();
    ctx.fillStyle = rgba9([255, 240, 244], 0.5 * p.gloss + 0.12);
    ctx.beginPath();
    ctx.ellipse(-bw * 0.38, cy - bh * 0.55, bw * 0.22, bh * 0.32, -0.5, 0, Math.PI * 2);
    ctx.fill();
    bulbPath2();
    ctx.strokeStyle = rgba9(p.outline, 0.55);
    ctx.lineWidth = 1.4;
    ctx.stroke();
    ctx.strokeStyle = outline;
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(0, tipY - 1);
    ctx.quadraticCurveTo(1.6, tipY + 3, 0.4, tipY + 6);
    ctx.stroke();
    ctx.lineCap = "butt";
    const crownY = cy - bh * 1.18;
    const leaves = [
      [-5.4, -8, -18.5],
      [-2.6, -4, -22],
      [0, 0, -23.5],
      [2.6, 4, -22],
      [5.4, 8, -18.5]
    ];
    leaves.forEach(([bx, lean, ty], i) => {
      const baseX = bx * 0.5;
      const baseY = crownY + 1;
      const cpx = bx + lean * 0.4;
      const cpy = crownY - 6;
      const tipX = bx + lean;
      const tipY2 = ty;
      ctx.strokeStyle = outline;
      ctx.lineWidth = 3.4;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(cpx * 0.6, (baseY + cpy) * 0.5, cpx, cpy);
      ctx.stroke();
      ctx.strokeStyle = rgb17(p.veins);
      ctx.lineWidth = 1.8;
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(cpx * 0.6, (baseY + cpy) * 0.5, cpx, cpy);
      ctx.stroke();
      const drawBlade = (color, inset) => {
        ctx.fillStyle = color;
        ctx.beginPath();
        ctx.moveTo(cpx, cpy + inset);
        ctx.quadraticCurveTo(
          cpx - 4.2 + inset * 0.4,
          (cpy + tipY2) * 0.5,
          tipX,
          tipY2 + inset
        );
        ctx.quadraticCurveTo(
          cpx + 4.2 - inset * 0.4,
          (cpy + tipY2) * 0.5,
          cpx,
          cpy + inset
        );
        ctx.closePath();
        ctx.fill();
      };
      drawBlade(outline, 0);
      drawBlade(rgb17(p.leafMid), 1);
      ctx.save();
      ctx.globalAlpha = 0.85;
      drawBlade(rgb17(p.leafHi), 2.6);
      ctx.restore();
      if (p.leafYellow > 0.02) {
        ctx.fillStyle = rgba9([214, 196, 92], 0.7 * p.leafYellow);
        ctx.beginPath();
        ctx.ellipse(tipX, tipY2 + 1.5, 2.4, 4, lean * 0.05, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.strokeStyle = rgba9(p.veins, 0.8);
      ctx.lineWidth = 1;
      ctx.beginPath();
      ctx.moveTo(cpx, cpy + 1);
      ctx.quadraticCurveTo((cpx + tipX) * 0.5 - 1, (cpy + tipY2) * 0.5, tipX, tipY2 + 1.5);
      ctx.stroke();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba9([226, 240, 255], 0.4 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse((cpx + tipX) * 0.5, (cpy + tipY2) * 0.5, 2, 4.2, lean * 0.05, 0, Math.PI * 2);
        ctx.fill();
      }
      void i;
    });
    ctx.lineCap = "butt";
    if (p.frostAmt > 0.02) {
      ctx.save();
      bulbPath2();
      ctx.clip();
      ctx.fillStyle = rgba9([220, 236, 255], 0.45 * p.frostAmt);
      ctx.beginPath();
      ctx.ellipse(-bw * 0.1, cy - bh * 0.6, bw * 0.85, bh * 0.5, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba9([255, 255, 255], 0.8 * p.frostAmt);
      [[-4, cy - bh * 0.7], [3, cy - bh * 0.5], [-1, cy - bh * 0.2]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
    }
    if (p.snowCapAmt > 0.02) {
      ctx.fillStyle = rgba9([248, 252, 255], 0.92 * p.snowCapAmt);
      ctx.beginPath();
      ctx.ellipse(0, crownY - 16, 6.5 * p.snowCapAmt + 1.5, 2.6 * p.snowCapAmt + 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(-3, crownY - 13, 2.4 * p.snowCapAmt + 0.6, 1.6, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.blossomAmt > 0.02) {
      const petal2 = rgba9([255, 224, 236], 0.9 * p.blossomAmt);
      const bxC = -12.5;
      const byC = 17;
      ctx.fillStyle = petal2;
      for (let k = 0; k < 5; k++) {
        const a = k / 5 * Math.PI * 2;
        ctx.beginPath();
        ctx.ellipse(bxC + Math.cos(a) * 1.7, byC + Math.sin(a) * 1.1, 1.3, 1, a, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.fillStyle = rgba9([255, 214, 96], p.blossomAmt);
      ctx.beginPath();
      ctx.arc(bxC, byC, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.fallenLeafAmt > 0.02) {
      const leafCol = rgba9([196, 132, 52], 0.95 * p.fallenLeafAmt);
      const edgeCol = rgba9([120, 70, 24], 0.9 * p.fallenLeafAmt);
      const fallen = [
        [11.5, 18.5, 0.5],
        [-13, 19.5, -0.7]
      ];
      fallen.forEach(([fx, fy, rot]) => {
        ctx.save();
        ctx.translate(fx, fy);
        ctx.rotate(rot);
        ctx.fillStyle = leafCol;
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = edgeCol;
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(-3.2, 0);
        ctx.lineTo(3.2, 0);
        ctx.stroke();
        ctx.restore();
      });
    }
    if (p.lightAmt > 1e-3) {
      ctx.fillStyle = rgba9(p.light, 0.16 * p.lightAmt);
      ctx.beginPath();
      ctx.ellipse(-6, 0, 22, 26, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.globalAlpha = 1;
    ctx.restore();
  }
  function draw10(season) {
    return (ctx) => paint17(ctx, SP17[season], 0);
  }
  function anim10(season) {
    return (ctx, t) => {
      const base = SP17[season];
      const bob = bobAt17(t, 0.7, 1.4);
      const p = base;
      let p2 = p;
      if (season === "Summer") {
        const pulse = (1 - Math.cos(t * 2)) * 0.5;
        p2 = { ...base, gloss: clamp0118(base.gloss + pulse * 0.18) };
      } else if (season === "Autumn") {
        const flutter = (1 - Math.cos(t * 1.6)) * 0.5;
        p2 = { ...base, leafYellow: clamp0118(base.leafYellow + flutter * 0.18) };
      } else if (season === "Winter") {
        const sheen2 = (1 - Math.cos(t * 0.9)) * 0.5;
        p2 = { ...base, frostAmt: clamp0118(base.frostAmt + sheen2 * 0.12) };
      } else if (season === "Spring") {
        const dew = (1 - Math.cos(t * 1.8)) * 0.5;
        p2 = { ...base, lightAmt: clamp0118(base.lightAmt + dew * 0.12) };
      }
      paint17(ctx, p2, bob);
      if (season === "Winter") {
        ctx.save();
        ctx.fillStyle = "rgba(255,255,255,0.85)";
        const seeds = [
          [-9, 0.9, 0],
          [5, 0.8, 0.4],
          [12, 0.7, 0.7],
          [-3, 0.85, 0.22]
        ];
        seeds.forEach(([fx, r, phase]) => {
          const prog = ((t / 3.4 + phase) % 1 + 1) % 1;
          const fy = -22 + prog * 40;
          const driftX = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
          ctx.globalAlpha = 0.8 * (1 - prog * 0.3);
          ctx.beginPath();
          ctx.arc(driftX, fy, r, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  var FROM = ["Spring", "Summer", "Autumn", "Winter"];
  function transition(fromIdx) {
    const a = SP17[FROM[fromIdx]];
    const b = SP17[FROM[fromIdx + 1]];
    return (ctx, pp) => paint17(ctx, lerpP17(a, b, smoother17(clamp0118(pp))), 0);
  }
  var VARIANTS19 = {
    Spring: { draw: draw10("Spring"), anim: anim10("Spring") },
    Summer: { draw: draw10("Summer"), anim: anim10("Summer") },
    Autumn: { draw: draw10("Autumn"), anim: anim10("Autumn") },
    Winter: { draw: draw10("Winter"), anim: anim10("Winter") }
  };
  var TRANSITIONS19 = {
    0: transition(0),
    // Spring → Summer
    1: transition(1),
    // Summer → Autumn
    2: transition(2)
    // Autumn → Winter
  };

  // src/textures/seasonal/veg/broccoli.ts
  function clamp0119(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother18 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp20(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB13(a, b, t) {
    return [lerp20(a[0], b[0], t), lerp20(a[1], b[1], t), lerp20(a[2], b[2], t)];
  }
  function rgb18(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba10(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0119(a)})`;
  }
  function lerpP18(a, b, t) {
    return {
      budLight: lerpRGB13(a.budLight, b.budLight, t),
      budMid: lerpRGB13(a.budMid, b.budMid, t),
      budDark: lerpRGB13(a.budDark, b.budDark, t),
      budCrown: lerpRGB13(a.budCrown, b.budCrown, t),
      stalkLight: lerpRGB13(a.stalkLight, b.stalkLight, t),
      stalkMid: lerpRGB13(a.stalkMid, b.stalkMid, t),
      stalkDark: lerpRGB13(a.stalkDark, b.stalkDark, t),
      padGrass: lerpRGB13(a.padGrass, b.padGrass, t),
      padDark: lerpRGB13(a.padDark, b.padDark, t),
      soil: lerpRGB13(a.soil, b.soil, t),
      outline: lerpRGB13(a.outline, b.outline, t),
      light: lerpRGB13(a.light, b.light, t),
      lightAmt: lerp20(a.lightAmt, b.lightAmt, t),
      ripeness: lerp20(a.ripeness, b.ripeness, t),
      gloss: lerp20(a.gloss, b.gloss, t),
      loosen: lerp20(a.loosen, b.loosen, t),
      frostAmt: lerp20(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp20(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp20(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp20(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp20(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP10(p) {
    return {
      ...p,
      lightAmt: clamp0119(p.lightAmt),
      ripeness: clamp0119(p.ripeness),
      gloss: clamp0119(p.gloss),
      loosen: clamp0119(p.loosen),
      frostAmt: clamp0119(p.frostAmt),
      snowCapAmt: clamp0119(p.snowCapAmt),
      padSnowAmt: clamp0119(p.padSnowAmt),
      blossomAmt: clamp0119(p.blossomAmt),
      fallenLeafAmt: clamp0119(p.fallenLeafAmt)
    };
  }
  var SP18 = {
    // Spring — small young head, looser & a touch lighter green; dewy lime pad +
    // blossom. Cool-bright light. Palette stays blue-green (just younger/lighter).
    Spring: {
      budLight: [128, 196, 132],
      budMid: [74, 152, 104],
      budDark: [40, 100, 74],
      budCrown: [150, 206, 132],
      stalkLight: [196, 222, 168],
      stalkMid: [158, 196, 132],
      stalkDark: [110, 152, 96],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [30, 64, 48],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.2,
      gloss: 0.2,
      loosen: 0.42,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK: full TIGHT dense blue-green head; richest saturation, mid-green
    // pad, warm light, strong soft sheen over the packed buds.
    Summer: {
      budLight: [96, 178, 128],
      budMid: [44, 132, 92],
      budDark: [20, 84, 62],
      budCrown: [70, 158, 110],
      stalkLight: [206, 226, 168],
      stalkMid: [168, 200, 128],
      stalkDark: [116, 156, 92],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [16, 58, 44],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.85,
      gloss: 0.6,
      loosen: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — head loosening: buds opening & yellowing at the crown; deeper olive
    // blue-green body; olive-tan pad, a couple fallen leaves. Low amber light.
    Autumn: {
      budLight: [120, 170, 104],
      budMid: [70, 124, 78],
      budDark: [38, 78, 52],
      budCrown: [206, 200, 96],
      // yellowing open crown
      stalkLight: [196, 208, 140],
      stalkMid: [158, 178, 108],
      stalkDark: [108, 132, 76],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [44, 56, 36],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.3,
      loosen: 0.85,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; frost-dusted blue-green head + small snow cap,
    // still clearly broccoli; snowy pad. Tight head again under the snow.
    Winter: {
      budLight: [108, 168, 140],
      budMid: [54, 116, 100],
      budDark: [28, 74, 68],
      budCrown: [96, 158, 134],
      stalkLight: [196, 214, 196],
      stalkMid: [156, 184, 168],
      stalkDark: [108, 144, 134],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [38, 56, 60],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.8,
      gloss: 0.22,
      loosen: 0.12,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var STALK_BOT = 17;
  var STALK_TOP = -1;
  var STALK_HALF = 4.2;
  var DOME_CY = -7;
  var DOME_RX = 14;
  var DOME_RY = 11;
  var BUDS = [
    // crown row (top of the dome)
    [-7, -8.2, 3],
    [-2.4, -9.4, 3.2],
    [2.6, -9.2, 3.2],
    [7.2, -8, 3],
    // upper-mid row
    [-11, -5, 3],
    [-5.6, -5.4, 3.3],
    [0, -6, 3.4],
    [5.6, -5.4, 3.3],
    [11, -5, 3],
    // mid row
    [-12.6, -0.6, 3],
    [-7.4, -1, 3.3],
    [-2, -1.4, 3.4],
    [3.4, -1.2, 3.4],
    [8.4, -0.8, 3.3],
    [12.8, -0.6, 3],
    // lower row (skirt of the dome over the stalk shoulders)
    [-9.6, 3.2, 2.8],
    [-4.2, 3, 3],
    [1.2, 2.8, 3],
    [6.6, 3, 2.9],
    [10.6, 3.4, 2.6]
  ];
  var CROWN_IDX = [0, 1, 2, 3, 5, 6, 7];
  function domePath(ctx, bob) {
    const cy = DOME_CY + bob;
    ctx.beginPath();
    ctx.moveTo(-DOME_RX, cy + DOME_RY * 0.45);
    ctx.quadraticCurveTo(-DOME_RX, cy - DOME_RY * 0.9, -DOME_RX * 0.45, cy - DOME_RY);
    ctx.quadraticCurveTo(0, cy - DOME_RY * 1.18, DOME_RX * 0.45, cy - DOME_RY);
    ctx.quadraticCurveTo(DOME_RX, cy - DOME_RY * 0.9, DOME_RX, cy + DOME_RY * 0.45);
    ctx.quadraticCurveTo(DOME_RX * 0.8, cy + DOME_RY * 0.95, DOME_RX * 0.36, cy + DOME_RY * 0.92);
    ctx.quadraticCurveTo(0, cy + DOME_RY * 0.86, -DOME_RX * 0.36, cy + DOME_RY * 0.92);
    ctx.quadraticCurveTo(-DOME_RX * 0.8, cy + DOME_RY * 0.95, -DOME_RX, cy + DOME_RY * 0.45);
    ctx.closePath();
  }
  function stalkPath(ctx, bob) {
    const b = STALK_BOT + bob;
    const t = STALK_TOP + bob;
    const h = STALK_HALF;
    ctx.beginPath();
    ctx.moveTo(-h - 1.4, b);
    ctx.quadraticCurveTo(-h - 0.4, b - 6, -h, lerp20(t, b, 0.4));
    ctx.quadraticCurveTo(-h - 1.6, t + 5, -h - 4.2, t + 1.5);
    ctx.quadraticCurveTo(-h - 1.2, t + 2.5, -h * 0.5, t + 0.5);
    ctx.quadraticCurveTo(0, t - 1, h * 0.5, t + 0.5);
    ctx.quadraticCurveTo(h + 1.2, t + 2.5, h + 4.2, t + 1.5);
    ctx.quadraticCurveTo(h + 1.6, t + 5, h, lerp20(t, b, 0.4));
    ctx.quadraticCurveTo(h + 0.4, b - 6, h + 1.4, b);
    ctx.quadraticCurveTo(0, b + 2.4, -h - 1.4, b);
    ctx.closePath();
  }
  function paint18(ctx, raw, bob) {
    const p = clampP10(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba10(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb18(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb18(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb18(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba10([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba10([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba10([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba10([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba10(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba10([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba10(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba10([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb18(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, STALK_BOT + bob + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba10(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, STALK_BOT + bob + 2, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      stalkPath(ctx, bob);
      ctx.fillStyle = rgb18(p.outline);
      ctx.fill();
      ctx.save();
      stalkPath(ctx, bob);
      ctx.clip();
      const sGrad = ctx.createLinearGradient(-STALK_HALF - 4, 0, STALK_HALF + 4, 0);
      sGrad.addColorStop(0, rgb18(p.stalkLight));
      sGrad.addColorStop(0.5, rgb18(p.stalkMid));
      sGrad.addColorStop(1, rgb18(p.stalkDark));
      ctx.fillStyle = sGrad;
      ctx.fillRect(-STALK_HALF - 6, STALK_TOP + bob - 4, (STALK_HALF + 6) * 2, STALK_BOT - STALK_TOP + 10);
      ctx.strokeStyle = rgba10(p.stalkDark, 0.6);
      ctx.lineWidth = 1.1;
      [-1.6, 1.4].forEach((gx) => {
        ctx.beginPath();
        ctx.moveTo(gx, STALK_TOP + bob + 2);
        ctx.lineTo(gx + 0.4, STALK_BOT + bob - 1);
        ctx.stroke();
      });
      ctx.restore();
      domePath(ctx, bob);
      ctx.fillStyle = rgb18(p.outline);
      ctx.fill();
      ctx.save();
      domePath(ctx, bob);
      ctx.clip();
      const cy = DOME_CY + bob;
      ctx.fillStyle = rgb18(p.budMid);
      ctx.fillRect(-DOME_RX - 2, cy - DOME_RY - 4, (DOME_RX + 2) * 2, DOME_RY * 2 + 10);
      const dGrad = ctx.createLinearGradient(0, cy - DOME_RY, 0, cy + DOME_RY);
      dGrad.addColorStop(0, rgb18(p.budLight));
      dGrad.addColorStop(0.5, rgb18(p.budMid));
      dGrad.addColorStop(1, rgb18(p.budDark));
      ctx.globalAlpha = 0.7;
      ctx.fillStyle = dGrad;
      ctx.fillRect(-DOME_RX - 2, cy - DOME_RY - 4, (DOME_RX + 2) * 2, DOME_RY * 2 + 10);
      ctx.globalAlpha = 1;
      BUDS.forEach(([bx, by, br], i) => {
        const x = bx;
        const y = by + bob;
        const isCrown = CROWN_IDX.includes(i);
        const open = isCrown ? p.loosen : p.loosen * 0.35;
        const r = br * (1 + open * 0.12);
        ctx.fillStyle = rgb18(p.budDark);
        ctx.beginPath();
        ctx.arc(x, y + 0.5, r + 0.5, 0, Math.PI * 2);
        ctx.fill();
        const body = isCrown ? lerpRGB13(p.budMid, p.budCrown, open) : p.budMid;
        ctx.fillStyle = rgb18(body);
        ctx.beginPath();
        ctx.arc(x, y, r, 0, Math.PI * 2);
        ctx.fill();
        const cap2 = isCrown ? lerpRGB13(p.budLight, p.budCrown, open * 0.6) : p.budLight;
        ctx.fillStyle = rgba10(cap2, 0.9);
        ctx.beginPath();
        ctx.arc(x - r * 0.32, y - r * 0.34, r * 0.62, 0, Math.PI * 2);
        ctx.fill();
        if (open > 0.25) {
          ctx.fillStyle = rgba10(p.budDark, 0.5 * open);
          ctx.beginPath();
          ctx.arc(x, y, r * 0.22, 0, Math.PI * 2);
          ctx.fill();
        }
      });
      if (p.gloss > 0.02) {
        const gl = ctx.createRadialGradient(-5, cy - 4, 1, -5, cy - 4, DOME_RX);
        gl.addColorStop(0, rgba10([255, 255, 255], 0.22 + 0.3 * p.gloss));
        gl.addColorStop(1, rgba10([255, 255, 255], 0));
        ctx.fillStyle = gl;
        ctx.fillRect(-DOME_RX - 2, cy - DOME_RY - 4, (DOME_RX + 2) * 2, DOME_RY * 2 + 10);
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba10([210, 230, 250], 0.22 * p.frostAmt);
        ctx.fillRect(-DOME_RX - 2, cy - DOME_RY - 4, (DOME_RX + 2) * 2, DOME_RY);
        ctx.fillStyle = rgba10([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-9, cy - 6],
          [-3, cy - 8],
          [3, cy - 7],
          [8, cy - 5],
          [-11, cy - 2],
          [11, cy - 2],
          [0, cy - 3],
          [-6, cy + 1],
          [6, cy + 1]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba10([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-DOME_RX * 0.7, cy - DOME_RY * 0.5);
        ctx.quadraticCurveTo(-DOME_RX * 0.5, cy - DOME_RY * 1.05, 0, cy - DOME_RY * 1.02);
        ctx.quadraticCurveTo(DOME_RX * 0.5, cy - DOME_RY * 1.05, DOME_RX * 0.7, cy - DOME_RY * 0.5);
        ctx.quadraticCurveTo(DOME_RX * 0.4, cy - DOME_RY * 0.34, 0, cy - DOME_RY * 0.42);
        ctx.quadraticCurveTo(-DOME_RX * 0.4, cy - DOME_RY * 0.34, -DOME_RX * 0.7, cy - DOME_RY * 0.5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba10([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, cy - DOME_RY * 0.55, DOME_RX * 0.55, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba10(p.light, p.lightAmt));
        lg.addColorStop(1, rgba10(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt18(t, amp = 0.85, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw11(season) {
    return (ctx) => paint18(ctx, SP18[season], 0);
  }
  function anim11(season) {
    return (ctx, t) => {
      const bob = bobAt18(t);
      paint18(ctx, SP18[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const cy = DOME_CY + bob;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = cy - 4 + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-5, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.4 % 1;
          const gx = lerp20(-DOME_RX * 0.6, DOME_RX * 0.6, prog);
          ctx.globalAlpha = 0.5;
          ctx.fillStyle = "rgba(255,255,255,1)";
          ctx.beginPath();
          ctx.ellipse(gx, cy - 5, 2.6, 4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(-3, cy - 6, 5, 3.2, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, cy - 2, 7, 4.5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition15(fromIdx) {
    const from = SP18[SEASON_NAMES[fromIdx]];
    const to = SP18[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother18(clamp0119(pp));
      paint18(ctx, lerpP18(from, to, k), 0);
    };
  }
  var springToSummer14 = makeTransition15(0);
  var summerToAutumn14 = makeTransition15(1);
  var autumnToWinter14 = makeTransition15(2);
  var VARIANTS20 = {
    Spring: { draw: draw11("Spring"), anim: anim11("Spring") },
    Summer: { draw: draw11("Summer"), anim: anim11("Summer") },
    Autumn: { draw: draw11("Autumn"), anim: anim11("Autumn") },
    Winter: { draw: draw11("Winter"), anim: anim11("Winter") }
  };
  var TRANSITIONS20 = {
    0: springToSummer14,
    1: summerToAutumn14,
    2: autumnToWinter14
  };

  // src/textures/seasonal/veg/carrot.ts
  function clamp0120(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother19 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp21(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB14(a, b, t) {
    return [lerp21(a[0], b[0], t), lerp21(a[1], b[1], t), lerp21(a[2], b[2], t)];
  }
  function rgb19(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba11(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0120(a)})`;
  }
  function lerpP19(a, b, t) {
    return {
      rootLight: lerpRGB14(a.rootLight, b.rootLight, t),
      rootMid: lerpRGB14(a.rootMid, b.rootMid, t),
      rootDark: lerpRGB14(a.rootDark, b.rootDark, t),
      topLight: lerpRGB14(a.topLight, b.topLight, t),
      topMid: lerpRGB14(a.topMid, b.topMid, t),
      topDark: lerpRGB14(a.topDark, b.topDark, t),
      padGrass: lerpRGB14(a.padGrass, b.padGrass, t),
      padDark: lerpRGB14(a.padDark, b.padDark, t),
      soil: lerpRGB14(a.soil, b.soil, t),
      outline: lerpRGB14(a.outline, b.outline, t),
      light: lerpRGB14(a.light, b.light, t),
      lightAmt: lerp21(a.lightAmt, b.lightAmt, t),
      ripeness: lerp21(a.ripeness, b.ripeness, t),
      gloss: lerp21(a.gloss, b.gloss, t),
      freckleAmt: lerp21(a.freckleAmt, b.freckleAmt, t),
      frostAmt: lerp21(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp21(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp21(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp21(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp21(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP11(p) {
    return {
      ...p,
      lightAmt: clamp0120(p.lightAmt),
      ripeness: clamp0120(p.ripeness),
      gloss: clamp0120(p.gloss),
      freckleAmt: clamp0120(p.freckleAmt),
      frostAmt: clamp0120(p.frostAmt),
      snowCapAmt: clamp0120(p.snowCapAmt),
      padSnowAmt: clamp0120(p.padSnowAmt),
      blossomAmt: clamp0120(p.blossomAmt),
      fallenLeafAmt: clamp0120(p.fallenLeafAmt)
    };
  }
  var SP19 = {
    // Spring — a touch paler/matte orange, ferny fresh top; dewy lime pad + a
    // pale blossom. Cool-bright light.
    Spring: {
      rootLight: [252, 176, 96],
      rootMid: [238, 138, 50],
      rootDark: [186, 96, 32],
      topLight: [168, 224, 110],
      topMid: [108, 186, 70],
      topDark: [62, 132, 50],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [104, 52, 18],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.35,
      gloss: 0.2,
      freckleAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full ripe glossy bright-orange carrot, lush green top (PEAK);
    // warm light, strong gloss, mid-green pad.
    Summer: {
      rootLight: [255, 168, 70],
      rootMid: [248, 124, 28],
      rootDark: [184, 80, 18],
      topLight: [132, 214, 84],
      topMid: [78, 174, 56],
      topDark: [42, 120, 42],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [108, 48, 12],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 1,
      gloss: 0.95,
      freckleAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fat carrot, feathery top yellowing, a couple of faint freckles on
    // the skin; olive-tan pad, fallen leaves, low amber light.
    Autumn: {
      rootLight: [240, 150, 64],
      rootMid: [224, 110, 34],
      rootDark: [160, 70, 22],
      topLight: [206, 204, 96],
      topMid: [158, 162, 62],
      topDark: [108, 116, 44],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [98, 44, 16],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 0.85,
      gloss: 0.4,
      freckleAmt: 0.7,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — frost-dusted carrot (pale frost over the orange, still CLEARLY
    // orange) + snow on the pad; cool blue-grey light.
    Winter: {
      rootLight: [240, 162, 96],
      rootMid: [218, 120, 56],
      rootDark: [156, 84, 46],
      topLight: [150, 188, 158],
      topMid: [98, 146, 110],
      topDark: [62, 104, 80],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [78, 52, 44],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.7,
      gloss: 0.22,
      freckleAmt: 0,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var TIP = [-15, 14];
  var SHOULDER = [11, -7];
  var ROOT_HALF = 6.6;
  var AXX = SHOULDER[0] - TIP[0];
  var AXY = SHOULDER[1] - TIP[1];
  var AXLEN = Math.hypot(AXX, AXY);
  var UX = AXX / AXLEN;
  var UY = AXY / AXLEN;
  var PX = -UY;
  var PY = UX;
  function onAxis(f, w) {
    const cx = lerp21(TIP[0], SHOULDER[0], f);
    const cy = lerp21(TIP[1], SHOULDER[1], f);
    return [cx + PX * w, cy + PY * w];
  }
  function carrotBodyPath(ctx, bob) {
    const widthAt = (f) => {
      const w = Math.sin(Math.min(1, f * 1.05) * Math.PI * 0.5);
      return ROOT_HALF * (0.12 + 0.88 * w);
    };
    const steps = 14;
    ctx.beginPath();
    for (let i = 0; i <= steps; i++) {
      const f = i / steps;
      const [x, y] = onAxis(f, widthAt(f));
      if (i === 0) ctx.moveTo(x, y + bob);
      else ctx.lineTo(x, y + bob);
    }
    const [sx, sy] = onAxis(1.04, 0);
    ctx.lineTo(sx, sy + bob);
    for (let i = steps; i >= 0; i--) {
      const f = i / steps;
      const [x, y] = onAxis(f, -widthAt(f));
      ctx.lineTo(x, y + bob);
    }
    ctx.closePath();
  }
  var FRONDS2 = [
    // [angle in radians (from +x, up is negative), length]
    [-1.95, 15],
    [-1.55, 17],
    [-1.15, 16],
    [-0.78, 13.5],
    [-0.42, 11.5]
  ];
  function carrotTop(ctx, p, bob, sway) {
    const [bx, by0] = onAxis(0.98, 0);
    const by = by0 + bob;
    ctx.save();
    ctx.lineCap = "round";
    ctx.lineJoin = "round";
    FRONDS2.forEach(([ang, len], i) => {
      const s = sway * (0.4 + i * 0.18);
      const tx = bx + Math.cos(ang) * len + s;
      const ty = by + Math.sin(ang) * len;
      const mx = bx + Math.cos(ang) * len * 0.5 + s * 0.5;
      const my = by + Math.sin(ang) * len * 0.55 - 2;
      ctx.strokeStyle = rgb19(p.topDark);
      ctx.lineWidth = 2.6;
      ctx.beginPath();
      ctx.moveTo(bx, by);
      ctx.quadraticCurveTo(mx, my, tx, ty);
      ctx.stroke();
      ctx.strokeStyle = rgb19(p.topMid);
      ctx.lineWidth = 1.5;
      ctx.beginPath();
      ctx.moveTo(bx, by);
      ctx.quadraticCurveTo(mx, my, tx, ty);
      ctx.stroke();
      ctx.strokeStyle = rgb19(p.topLight);
      ctx.lineWidth = 0.8;
      for (let k = 1; k <= 4; k++) {
        const f = k / 5;
        const px = lerp21(bx, tx, f);
        const py = lerp21(by, ty, f) + Math.sin(f * Math.PI) * -1.4;
        const nrm = ang - Math.PI / 2;
        ctx.beginPath();
        ctx.moveTo(px, py);
        ctx.lineTo(px + Math.cos(nrm) * 2.2, py + Math.sin(nrm) * 2.2);
        ctx.moveTo(px, py);
        ctx.lineTo(px - Math.cos(nrm) * 2.2, py - Math.sin(nrm) * 2.2);
        ctx.stroke();
      }
      ctx.fillStyle = rgb19(p.topLight);
      ctx.beginPath();
      ctx.arc(tx, ty, 1.1, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.restore();
  }
  function paint19(ctx, raw, bob) {
    const p = clampP11(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba11(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb19(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb19(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb19(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba11([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba11([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba11([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba11([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-12, 18.8], [12, 17.8]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba11(idx === 0 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba11([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [13, 18.4, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba11(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba11([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.save();
      ctx.fillStyle = rgba11(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(-1, 13.5 + bob * 0.4, 15, 3.4, -0.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.fillStyle = rgba11(p.soil, 0.85);
      ctx.beginPath();
      ctx.ellipse(TIP[0] + 2, TIP[1] + 1 + bob, 4, 2, -0.5, 0, Math.PI * 2);
      ctx.fill();
      carrotTop(ctx, p, bob, 0);
      ctx.save();
      ctx.lineJoin = "round";
      carrotBodyPath(ctx, bob);
      ctx.lineWidth = 2.4;
      ctx.strokeStyle = rgb19(p.outline);
      ctx.stroke();
      ctx.fillStyle = rgb19(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      carrotBodyPath(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb19(p.rootMid);
      ctx.fillRect(-24, -24, 48, 48);
      const [lx0, ly0] = onAxis(0.5, ROOT_HALF + 4);
      const [lx1, ly1] = onAxis(0.5, -ROOT_HALF - 4);
      const litGrad = ctx.createLinearGradient(lx0, ly0 + bob, lx1, ly1 + bob);
      litGrad.addColorStop(0, rgb19(p.rootLight));
      litGrad.addColorStop(0.5, rgb19(p.rootMid));
      litGrad.addColorStop(1, rgb19(p.rootDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.fillRect(-24, -24, 48, 48);
      ctx.globalAlpha = 1;
      const [tgx, tgy] = TIP;
      const [sgx, sgy] = SHOULDER;
      const tipGrad = ctx.createLinearGradient(tgx, tgy + bob, sgx, sgy + bob);
      tipGrad.addColorStop(0, rgba11(p.rootDark, 0.55));
      tipGrad.addColorStop(0.35, rgba11(p.rootDark, 0));
      ctx.fillStyle = tipGrad;
      ctx.fillRect(-24, -24, 48, 48);
      ctx.strokeStyle = rgba11(p.rootDark, 0.5);
      ctx.lineWidth = 1;
      for (let i = 1; i <= 6; i++) {
        const f = i / 7;
        const [a0x, a0y] = onAxis(f, ROOT_HALF + 2);
        const [a1x, a1y] = onAxis(f, -(ROOT_HALF + 2));
        ctx.beginPath();
        ctx.moveTo(a0x, a0y + bob);
        ctx.lineTo(a1x, a1y + bob);
        ctx.stroke();
      }
      if (p.freckleAmt > 0.02) {
        ctx.fillStyle = rgba11(p.rootDark, 0.5 * p.freckleAmt);
        const fr = [[0.45, 1.5], [0.6, -2], [0.72, 0.5], [0.55, 2.4]];
        fr.forEach(([f, w]) => {
          const [fx, fy] = onAxis(f, w);
          ctx.beginPath();
          ctx.arc(fx, fy + bob, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.gloss > 0.02) {
        ctx.strokeStyle = rgba11([255, 255, 255], 0.18 + 0.55 * p.gloss);
        ctx.lineWidth = 1.8;
        ctx.beginPath();
        const [g0x, g0y] = onAxis(0.2, ROOT_HALF * 0.45);
        const [g1x, g1y] = onAxis(0.85, ROOT_HALF * 0.55);
        const [gmx, gmy] = onAxis(0.55, ROOT_HALF * 0.62);
        ctx.moveTo(g0x, g0y + bob);
        ctx.quadraticCurveTo(gmx, gmy + bob, g1x, g1y + bob);
        ctx.stroke();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba11([214, 232, 250], 0.26 * p.frostAmt);
        ctx.fillRect(-24, -24, 48, 48);
        ctx.fillStyle = rgba11([236, 246, 255], 0.7 * p.frostAmt);
        for (let i = 1; i <= 7; i++) {
          const f = i / 8;
          const [fx, fy] = onAxis(f, i % 2 === 0 ? 1.6 : -1.4);
          ctx.beginPath();
          ctx.arc(fx, fy + bob, 0.7, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba11([246, 251, 255], 0.92 * a);
        ctx.beginPath();
        for (let i = 0; i <= 12; i++) {
          const f = i / 12;
          const w = ROOT_HALF * (0.12 + 0.88 * Math.sin(Math.min(1, f * 1.05) * Math.PI * 0.5));
          const [x, y] = onAxis(f, w);
          if (i === 0) ctx.moveTo(x, y + bob);
          else ctx.lineTo(x, y + bob);
        }
        for (let i = 12; i >= 0; i--) {
          const f = i / 12;
          const w = ROOT_HALF * (0.12 + 0.88 * Math.sin(Math.min(1, f * 1.05) * Math.PI * 0.5));
          const [x, y] = onAxis(f, w * 0.45);
          ctx.lineTo(x, y + bob);
        }
        ctx.closePath();
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba11(p.light, p.lightAmt));
        lg.addColorStop(1, rgba11(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt19(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw12(season) {
    return (ctx) => paint19(ctx, SP19[season], 0);
  }
  function anim12(season) {
    return (ctx, t) => {
      const bob = bobAt19(t);
      paint19(ctx, SP19[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const p = clampP11(SP19[season]);
        const swayAmp = season === "Spring" ? 1.8 : season === "Autumn" ? 1.6 : season === "Summer" ? 1.1 : 0.9;
        const sway = Math.sin(t * 1.7) * swayAmp + Math.sin(t * 1.7 + 1.1) * swayAmp * 0.4;
        carrotTop(ctx, p, bob, sway);
        if (season === "Spring") {
          const g = 0.2 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          const f = 0.35 + 0.25 * (0.5 + 0.5 * Math.sin(t * 1));
          const [gx, gy] = onAxis(f, ROOT_HALF * 0.4);
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(gx, gy + bob, 1.1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const [gx, gy] = onAxis(lerp21(0.2, 0.9, prog), ROOT_HALF * 0.5);
          ctx.fillStyle = "rgba(255,255,255,0.8)";
          ctx.beginPath();
          ctx.ellipse(gx, gy + bob, 1.6, 1, Math.atan2(UY, UX), 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.arc(gx, gy + bob, 1, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          ctx.strokeStyle = rgba11(p.topLight, 0.7);
          ctx.lineWidth = 0.8;
          const [bx, by] = onAxis(0.98, 0);
          FRONDS2.forEach(([ang, len], i) => {
            const flut = Math.sin(t * 3.4 + i * 1.3) * 1.4;
            const tx = bx + Math.cos(ang) * len + sway * (0.4 + i * 0.18) + flut;
            const ty = by + Math.sin(ang) * len;
            ctx.beginPath();
            ctx.moveTo(tx, ty + bob);
            ctx.lineTo(tx + 1.6, ty + bob - 1.4);
            ctx.stroke();
          });
        } else {
          const seeds = [
            [-10, 0, 1],
            [4, 0.4, 0.9],
            [10, 0.7, 0.8],
            [-3, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          const [cx, cy] = onAxis(0.5, ROOT_HALF * 0.3);
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(cx, cy + bob, 7, 3.2, Math.atan2(UY, UX), 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition16(fromIdx) {
    const from = SP19[SEASON_NAMES[fromIdx]];
    const to = SP19[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother19(clamp0120(pp));
      paint19(ctx, lerpP19(from, to, k), 0);
    };
  }
  var springToSummer15 = makeTransition16(0);
  var summerToAutumn15 = makeTransition16(1);
  var autumnToWinter15 = makeTransition16(2);
  var VARIANTS21 = {
    Spring: { draw: draw12("Spring"), anim: anim12("Spring") },
    Summer: { draw: draw12("Summer"), anim: anim12("Summer") },
    Autumn: { draw: draw12("Autumn"), anim: anim12("Autumn") },
    Winter: { draw: draw12("Winter"), anim: anim12("Winter") }
  };
  var TRANSITIONS21 = {
    0: springToSummer15,
    1: summerToAutumn15,
    2: autumnToWinter15
  };

  // src/textures/seasonal/veg/cucumber.ts
  function clamp0121(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother20 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp22(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB15(a, b, t) {
    return [lerp22(a[0], b[0], t), lerp22(a[1], b[1], t), lerp22(a[2], b[2], t)];
  }
  function rgb20(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba12(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0121(a)})`;
  }
  function lerpP20(a, b, t) {
    return {
      skinLight: lerpRGB15(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB15(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB15(a.skinDark, b.skinDark, t),
      speckle: lerpRGB15(a.speckle, b.speckle, t),
      flower: lerpRGB15(a.flower, b.flower, t),
      leaf: lerpRGB15(a.leaf, b.leaf, t),
      padGrass: lerpRGB15(a.padGrass, b.padGrass, t),
      padDark: lerpRGB15(a.padDark, b.padDark, t),
      soil: lerpRGB15(a.soil, b.soil, t),
      outline: lerpRGB15(a.outline, b.outline, t),
      light: lerpRGB15(a.light, b.light, t),
      lightAmt: lerp22(a.lightAmt, b.lightAmt, t),
      ripeness: lerp22(a.ripeness, b.ripeness, t),
      gloss: lerp22(a.gloss, b.gloss, t),
      speckleAmt: lerp22(a.speckleAmt, b.speckleAmt, t),
      frostAmt: lerp22(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp22(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp22(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp22(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp22(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP12(p) {
    return {
      ...p,
      lightAmt: clamp0121(p.lightAmt),
      ripeness: clamp0121(p.ripeness),
      gloss: clamp0121(p.gloss),
      speckleAmt: clamp0121(p.speckleAmt),
      frostAmt: clamp0121(p.frostAmt),
      snowCapAmt: clamp0121(p.snowCapAmt),
      padSnowAmt: clamp0121(p.padSnowAmt),
      blossomAmt: clamp0121(p.blossomAmt),
      fallenLeafAmt: clamp0121(p.fallenLeafAmt)
    };
  }
  var SP20 = {
    // Spring — small young PALE-green cucumber, dewy lime pad + tiny blossom.
    Spring: {
      skinLight: [186, 222, 120],
      skinMid: [126, 186, 78],
      skinDark: [76, 138, 58],
      speckle: [214, 240, 168],
      flower: [255, 224, 96],
      leaf: [120, 188, 84],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [40, 70, 34],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.2,
      gloss: 0.3,
      speckleAmt: 0.45,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full firm MID-green cucumber, crisp pale speckles (PEAK). Warm
    // light, strong sheen, saturated mid-green pad.
    Summer: {
      skinLight: [150, 210, 96],
      skinMid: [78, 162, 60],
      skinDark: [40, 108, 46],
      speckle: [206, 238, 156],
      flower: [255, 214, 70],
      leaf: [86, 164, 66],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [26, 70, 34],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.75,
      gloss: 0.9,
      speckleAmt: 1,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — dulling DEEPER green, a touch waxy / overripe; olive-tan pad,
    // fallen leaves. Low amber light. Still clearly green.
    Autumn: {
      skinLight: [120, 158, 74],
      skinMid: [70, 116, 52],
      skinDark: [42, 78, 38],
      speckle: [168, 188, 128],
      flower: [220, 188, 84],
      leaf: [132, 138, 64],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [34, 56, 30],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.5,
      speckleAmt: 0.6,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — frost-dusted green cucumber + a small snow cap, still clearly
    // GREEN underneath; snowy pad, cool blue-grey light.
    Winter: {
      skinLight: [120, 168, 110],
      skinMid: [70, 126, 78],
      skinDark: [44, 88, 58],
      speckle: [206, 230, 224],
      flower: [228, 206, 120],
      leaf: [104, 142, 96],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [40, 56, 56],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.85,
      gloss: 0.3,
      speckleAmt: 0.35,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var CUKE_HALF = 6.4;
  var CUKE_AX = -13.5;
  var CUKE_AY = 9.5;
  var CUKE_BX = 14.5;
  var CUKE_BY = -7.5;
  var CUKE_CX = 1.5;
  var CUKE_CY = 5.5;
  function spineAt(s) {
    const u = 1 - s;
    const x = u * u * CUKE_AX + 2 * u * s * CUKE_CX + s * s * CUKE_BX;
    const y = u * u * CUKE_AY + 2 * u * s * CUKE_CY + s * s * CUKE_BY;
    return [x, y];
  }
  function cucumberBodyPath(ctx, bob, half) {
    const N = 14;
    const top = [];
    const bottomPts = [];
    for (let i = 0; i <= N; i++) {
      const s = i / N;
      const [px, py] = spineAt(s);
      const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
      const [mx, my] = spineAt(Math.max(0, s - 1e-3));
      let tx = nx - mx;
      let ty = ny - my;
      const len = Math.hypot(tx, ty) || 1;
      tx /= len;
      ty /= len;
      const onx = -ty;
      const ony = tx;
      const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
      const h = half * taper;
      top.push([px + onx * h, py + ony * h + bob]);
      bottomPts.push([px - onx * h, py - ony * h + bob]);
    }
    ctx.beginPath();
    ctx.moveTo(top[0][0], top[0][1]);
    for (let i = 1; i < top.length; i++) ctx.lineTo(top[i][0], top[i][1]);
    for (let i = bottomPts.length - 1; i >= 0; i--) ctx.lineTo(bottomPts[i][0], bottomPts[i][1]);
    ctx.closePath();
  }
  function paint20(ctx, raw, bob) {
    const p = clampP12(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba12(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb20(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb20(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb20(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba12([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba12([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba12([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba12([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba12(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba12([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba12(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba12([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb20(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, 15 + bob, 13, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba12(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, 15.6 + bob, 14, 2.4, -0.18, 0, Math.PI * 2);
      ctx.fill();
      cucumberBodyPath(ctx, bob, CUKE_HALF + 0.9);
      ctx.fillStyle = rgb20(p.outline);
      ctx.fill();
      ctx.save();
      cucumberBodyPath(ctx, bob, CUKE_HALF);
      ctx.clip();
      ctx.fillStyle = rgb20(p.skinMid);
      ctx.fillRect(-24, -24, 48, 48);
      const [lax, lay] = spineAt(0.1);
      const [lbx, lby] = spineAt(0.9);
      const litGrad = ctx.createLinearGradient(lax, lay - CUKE_HALF, lbx, lby + CUKE_HALF);
      litGrad.addColorStop(0, rgb20(p.skinLight));
      litGrad.addColorStop(0.5, rgb20(p.skinMid));
      litGrad.addColorStop(1, rgb20(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.fillRect(-24, -24, 48, 48);
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba12(p.skinDark, 0.7);
      ctx.lineWidth = 1.2;
      [-0.55, 0, 0.5].forEach((off) => {
        ctx.beginPath();
        for (let i = 0; i <= 14; i++) {
          const s = i / 14;
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
          const h = CUKE_HALF * taper * off;
          const x = px + onx * h, y = py + ony * h + bob;
          if (i === 0) ctx.moveTo(x, y);
          else ctx.lineTo(x, y);
        }
        ctx.stroke();
      });
      if (p.speckleAmt > 0.02) {
        ctx.fillStyle = rgba12(p.speckle, 0.85 * p.speckleAmt);
        const bumps = [
          [0.18, -0.45, 0.9],
          [0.32, 0.1, 0.7],
          [0.48, -0.25, 0.85],
          [0.62, 0.2, 0.7],
          [0.74, -0.4, 0.8],
          [0.88, -0.05, 0.6],
          [0.26, -0.15, 0.6],
          [0.56, -0.5, 0.7],
          [0.4, 0.35, 0.6]
        ];
        bumps.forEach(([s, off, r]) => {
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
          const h = CUKE_HALF * taper * off;
          ctx.beginPath();
          ctx.arc(px + onx * h, py + ony * h + bob, r, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.gloss > 0.02) {
        ctx.strokeStyle = rgba12([255, 255, 255], 0.18 + 0.5 * p.gloss);
        ctx.lineWidth = 1.6;
        ctx.beginPath();
        for (let i = 2; i <= 12; i++) {
          const s = i / 14;
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
          const h = CUKE_HALF * taper * -0.55;
          const x = px + onx * h, y = py + ony * h + bob;
          if (i === 2) ctx.moveTo(x, y);
          else ctx.lineTo(x, y);
        }
        ctx.stroke();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba12([210, 230, 250], 0.22 * p.frostAmt);
        ctx.fillRect(-24, -24, 48, 28);
        ctx.fillStyle = rgba12([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [0.2, -0.4],
          [0.36, -0.1],
          [0.5, -0.35],
          [0.66, 0],
          [0.8, -0.3],
          [0.9, -0.5]
        ];
        speck.forEach(([s, off]) => {
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const h = CUKE_HALF * off;
          ctx.beginPath();
          ctx.arc(px + onx * h, py + ony * h + bob, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba12([246, 251, 255], 0.92 * a);
        ctx.beginPath();
        for (let i = 2; i <= 12; i++) {
          const s = i / 14;
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
          const h = CUKE_HALF * taper * -0.82;
          const x = px + onx * h, y = py + ony * h + bob;
          if (i === 2) ctx.moveTo(x, y);
          else ctx.lineTo(x, y);
        }
        for (let i = 12; i >= 2; i--) {
          const s = i / 14;
          const [px, py] = spineAt(s);
          const [nx, ny] = spineAt(Math.min(1, s + 1e-3));
          const [mx, my] = spineAt(Math.max(0, s - 1e-3));
          let tx = nx - mx, ty = ny - my;
          const len = Math.hypot(tx, ty) || 1;
          const onx = -ty / len, ony = tx / len;
          const taper = 0.55 + 0.45 * Math.sin(Math.PI * s);
          const h = CUKE_HALF * taper * -0.38;
          ctx.lineTo(px + onx * h, py + ony * h + bob);
        }
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba12([205, 222, 242], 0.4 * a);
        const [mx2, my2] = spineAt(0.5);
        ctx.beginPath();
        ctx.ellipse(mx2, my2 - CUKE_HALF * 0.6 + bob, 7, 1.4, -0.6, 0, Math.PI * 2);
        ctx.fill();
      }
      const [sx0, sy0] = spineAt(0);
      const stemTipX = sx0 - 3.2;
      const stemTipY = sy0 - 2.6 + bob;
      ctx.strokeStyle = rgb20(p.outline);
      ctx.lineWidth = 2.6;
      ctx.beginPath();
      ctx.moveTo(sx0, sy0 + bob);
      ctx.quadraticCurveTo(sx0 - 2.4, sy0 - 1.6 + bob, stemTipX, stemTipY);
      ctx.stroke();
      ctx.strokeStyle = rgb20(p.leaf);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.moveTo(sx0, sy0 + bob);
      ctx.quadraticCurveTo(sx0 - 2.4, sy0 - 1.6 + bob, stemTipX, stemTipY);
      ctx.stroke();
      ctx.save();
      ctx.translate(stemTipX - 1.5, stemTipY + 2.6);
      ctx.rotate(-0.7);
      ctx.fillStyle = rgb20(p.leaf);
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.4, 1.9, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba12(p.outline, 0.7);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(-3.2, 0);
      ctx.lineTo(3.2, 0);
      ctx.stroke();
      ctx.restore();
      ctx.save();
      ctx.translate(stemTipX, stemTipY);
      ctx.fillStyle = rgb20(p.flower);
      for (let k = 0; k < 5; k++) {
        const ang = k / 5 * Math.PI * 2 - 0.3;
        ctx.beginPath();
        ctx.ellipse(Math.cos(ang) * 2, Math.sin(ang) * 2, 1.5, 1, ang, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.strokeStyle = rgba12(p.outline, 0.45);
      ctx.lineWidth = 0.6;
      for (let k = 0; k < 5; k++) {
        const ang = k / 5 * Math.PI * 2 - 0.3;
        ctx.beginPath();
        ctx.ellipse(Math.cos(ang) * 2, Math.sin(ang) * 2, 1.5, 1, ang, 0, Math.PI * 2);
        ctx.stroke();
      }
      ctx.fillStyle = rgba12([246, 176, 56], 0.95);
      ctx.beginPath();
      ctx.arc(0, 0, 1.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba12(p.light, p.lightAmt));
        lg.addColorStop(1, rgba12(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt20(t, amp = 0.8, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw13(season) {
    return (ctx) => paint20(ctx, SP20[season], 0);
  }
  function flowerAnchor(bob) {
    const [sx0, sy0] = spineAt(0);
    return [sx0 - 3.2, sy0 - 2.6 + bob];
  }
  function anim13(season) {
    return (ctx, t) => {
      const bob = bobAt20(t);
      paint20(ctx, SP20[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const [fx, fy] = flowerAnchor(bob);
        const nod = Math.sin(t * 1.6) * 0.18;
        ctx.save();
        ctx.translate(fx, fy);
        ctx.rotate(nod);
        ctx.fillStyle = rgba12(SP20[season].flower, 0.5);
        ctx.beginPath();
        ctx.ellipse(0, -2.2, 1.3, 0.9, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          const [gx, gy] = spineAt(0.45 + 0.1 * Math.sin(t * 1.1));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(gx, gy - 3 + bob, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const [gx, gy] = spineAt(0.1 + prog * 0.8);
          ctx.fillStyle = "rgba(255,255,255,0.85)";
          ctx.beginPath();
          ctx.ellipse(gx, gy - CUKE_HALF * 0.5 + bob, 2, 1.1, -0.7, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.35)";
          ctx.beginPath();
          ctx.ellipse(gx, gy - CUKE_HALF * 0.2 + bob, 1.2, 0.7, -0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          const [gx, gy] = spineAt(0.5);
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(gx, gy - 2 + bob, 6, 2.6, -0.7, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [12, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fxs, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fys = -22 + prog * 40;
            const dx = fxs + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fys, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          const [gx, gy] = spineAt(0.5);
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(gx, gy - 1 + bob, 7, 3.2, -0.7, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition17(fromIdx) {
    const from = SP20[SEASON_NAMES[fromIdx]];
    const to = SP20[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother20(clamp0121(pp));
      paint20(ctx, lerpP20(from, to, k), 0);
    };
  }
  var springToSummer16 = makeTransition17(0);
  var summerToAutumn16 = makeTransition17(1);
  var autumnToWinter16 = makeTransition17(2);
  var VARIANTS22 = {
    Spring: { draw: draw13("Spring"), anim: anim13("Spring") },
    Summer: { draw: draw13("Summer"), anim: anim13("Summer") },
    Autumn: { draw: draw13("Autumn"), anim: anim13("Autumn") },
    Winter: { draw: draw13("Winter"), anim: anim13("Winter") }
  };
  var TRANSITIONS22 = {
    0: springToSummer16,
    1: summerToAutumn16,
    2: autumnToWinter16
  };

  // src/textures/seasonal/veg/eggplant.ts
  function clamp0122(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother21 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp23(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB16(a, b, t) {
    return [lerp23(a[0], b[0], t), lerp23(a[1], b[1], t), lerp23(a[2], b[2], t)];
  }
  function rgb21(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba13(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0122(a)})`;
  }
  function lerpP21(a, b, t) {
    return {
      skinLight: lerpRGB16(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB16(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB16(a.skinDark, b.skinDark, t),
      cap: lerpRGB16(a.cap, b.cap, t),
      capDark: lerpRGB16(a.capDark, b.capDark, t),
      padGrass: lerpRGB16(a.padGrass, b.padGrass, t),
      padDark: lerpRGB16(a.padDark, b.padDark, t),
      soil: lerpRGB16(a.soil, b.soil, t),
      outline: lerpRGB16(a.outline, b.outline, t),
      light: lerpRGB16(a.light, b.light, t),
      lightAmt: lerp23(a.lightAmt, b.lightAmt, t),
      ripeness: lerp23(a.ripeness, b.ripeness, t),
      gloss: lerp23(a.gloss, b.gloss, t),
      capDry: lerp23(a.capDry, b.capDry, t),
      frostAmt: lerp23(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp23(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp23(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp23(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp23(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP13(p) {
    return {
      ...p,
      lightAmt: clamp0122(p.lightAmt),
      ripeness: clamp0122(p.ripeness),
      gloss: clamp0122(p.gloss),
      capDry: clamp0122(p.capDry),
      frostAmt: clamp0122(p.frostAmt),
      snowCapAmt: clamp0122(p.snowCapAmt),
      padSnowAmt: clamp0122(p.padSnowAmt),
      blossomAmt: clamp0122(p.blossomAmt),
      fallenLeafAmt: clamp0122(p.fallenLeafAmt)
    };
  }
  var SP21 = {
    // Spring — small pale young fruit, matte, fresh green cap; lime dewy pad + blossom.
    Spring: {
      skinLight: [168, 126, 196],
      skinMid: [128, 84, 162],
      skinDark: [86, 52, 116],
      cap: [128, 188, 78],
      capDark: [78, 134, 52],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [44, 28, 56],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.2,
      gloss: 0.22,
      capDry: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full glossy deep PURPLE eggplant (PEAK); bright specular streak,
    // mid-green pad, warm light.
    Summer: {
      skinLight: [150, 86, 196],
      skinMid: [96, 44, 142],
      skinDark: [58, 24, 92],
      cap: [96, 174, 60],
      capDark: [56, 122, 44],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [34, 16, 54],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.8,
      gloss: 0.96,
      capDry: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — very deep ripe purple, slightly less glossy, calyx drying a touch;
    // olive-tan pad + fallen leaves, low amber light.
    Autumn: {
      skinLight: [120, 66, 156],
      skinMid: [78, 36, 116],
      skinDark: [46, 20, 76],
      cap: [126, 142, 72],
      capDark: [92, 100, 50],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [32, 16, 48],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.4,
      capDry: 0.65,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — frost-dusted purple (cool, still clearly purple) + snow cap on the
    // shoulder; snowy pad, cool blue-grey light.
    Winter: {
      skinLight: [144, 110, 184],
      skinMid: [104, 70, 148],
      skinDark: [66, 44, 104],
      cap: [120, 150, 120],
      capDark: [82, 112, 86],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [44, 34, 60],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.9,
      gloss: 0.28,
      capDry: 0.15,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var EGG_TOP = -11;
  var EGG_BOT = 17;
  var EGG_HALF = 11.5;
  function eggplantBodyPath(ctx, bob) {
    const t = EGG_TOP + bob;
    const b = EGG_BOT + bob;
    const h = EGG_HALF;
    const midY = lerp23(t, b, 0.62);
    ctx.beginPath();
    ctx.moveTo(-4.4, t);
    ctx.quadraticCurveTo(-4, t - 2.6, 0, t - 2.8);
    ctx.quadraticCurveTo(4, t - 2.6, 4.4, t);
    ctx.quadraticCurveTo(7.4, lerp23(t, b, 0.22), h * 0.96, midY - 2);
    ctx.quadraticCurveTo(h + 0.6, midY + 1.5, h * 0.86, lerp23(midY, b, 0.62));
    ctx.quadraticCurveTo(h * 0.6, b + 1.6, 0, b + 2.2);
    ctx.quadraticCurveTo(-h * 0.6, b + 1.6, -h * 0.86, lerp23(midY, b, 0.62));
    ctx.quadraticCurveTo(-h - 0.6, midY + 1.5, -h * 0.96, midY - 2);
    ctx.quadraticCurveTo(-7.4, lerp23(t, b, 0.22), -4.4, t);
    ctx.closePath();
  }
  function paint21(ctx, raw, bob) {
    const p = clampP13(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba13(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb21(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb21(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb21(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba13([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba13([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba13([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba13([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba13(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba13([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba13(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba13([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb21(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, EGG_BOT + bob + 1.8, 8.5, 2.5, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba13(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, EGG_BOT + bob + 2.3, 10.5, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      eggplantBodyPath(ctx, bob);
      ctx.fillStyle = rgb21(p.outline);
      ctx.fill();
      ctx.save();
      eggplantBodyPath(ctx, bob);
      ctx.clip();
      const top = EGG_TOP + bob;
      const bot = EGG_BOT + bob;
      const midY = lerp23(top, bot, 0.62);
      ctx.fillStyle = rgb21(p.skinMid);
      ctx.fillRect(-EGG_HALF - 3, top - 6, (EGG_HALF + 3) * 2, bot - top + 12);
      const litGrad = ctx.createLinearGradient(-EGG_HALF, top - 4, EGG_HALF, bot);
      litGrad.addColorStop(0, rgb21(p.skinLight));
      litGrad.addColorStop(0.5, rgb21(p.skinMid));
      litGrad.addColorStop(1, rgb21(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-1.5, lerp23(top, bot, 0.5), EGG_HALF + 2, (bot - top) * 0.56, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.fillStyle = rgba13(p.skinDark, 0.55);
      ctx.beginPath();
      ctx.ellipse(EGG_HALF * 0.42, midY, EGG_HALF * 0.62, (bot - top) * 0.4, -0.18, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba13(p.skinDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(0, bot - 1.5, EGG_HALF * 0.8, 4.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba13(p.skinLight, 0.4 + 0.3 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(-4.5, lerp23(top, bot, 0.34), EGG_HALF * 0.5, (bot - top) * 0.34, -0.18, 0, Math.PI * 2);
      ctx.fill();
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba13([255, 255, 255], 0.14 + 0.62 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-4.6, lerp23(top, bot, 0.42), 1.5, (bot - top) * 0.36, -0.08, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba13([255, 255, 255], 0.08 + 0.34 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-0.5, lerp23(top, bot, 0.36), 0.9, (bot - top) * 0.26, -0.04, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba13([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp23(top, bot, 0.32), EGG_HALF, (bot - top) * 0.34, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba13([235, 246, 255], 0.68 * p.frostAmt);
        const speck = [
          [-7, top + 4],
          [-2, top + 2.5],
          [3, top + 4],
          [7, lerp23(top, bot, 0.3)],
          [-5, lerp23(top, bot, 0.5)],
          [5, lerp23(top, bot, 0.52)],
          [0, lerp23(top, bot, 0.4)],
          [-3, lerp23(top, bot, 0.66)]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.65, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba13([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-4.6, top + 0.5);
        ctx.quadraticCurveTo(-3.4, top - 4, 0, top - 3.4);
        ctx.quadraticCurveTo(3.4, top - 4, 4.6, top + 0.5);
        ctx.quadraticCurveTo(2.6, top + 2.6, 0, top + 1.6);
        ctx.quadraticCurveTo(-2.6, top + 2.6, -4.6, top + 0.5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba13([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 1, 4, 1.3, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const capBaseY = top - 1.5;
      const capBack = lerpRGB16(p.cap, p.capDark, 0.5);
      const sepals = [
        // [angle-ish x reach, downward droop]
        [-6.4, 3.4],
        [-3.4, 4.6],
        [0, 5],
        [3.4, 4.6],
        [6.4, 3.4]
      ];
      ctx.fillStyle = rgb21(capBack);
      sepals.forEach(([sx, dy]) => {
        ctx.beginPath();
        ctx.moveTo(sx * 0.35, capBaseY - 1.4);
        ctx.quadraticCurveTo(sx * 0.9, capBaseY + 0.4, sx, capBaseY + dy);
        ctx.quadraticCurveTo(sx * 0.55, capBaseY + dy * 0.5, sx * 0.18, capBaseY + 0.6);
        ctx.closePath();
        ctx.fill();
      });
      ctx.fillStyle = rgb21(p.cap);
      sepals.forEach(([sx, dy], i) => {
        if (i % 2 === 1) return;
        ctx.beginPath();
        ctx.moveTo(sx * 0.32, capBaseY - 1.8);
        ctx.quadraticCurveTo(sx * 0.85, capBaseY - 0.2, sx * 0.92, capBaseY + dy * 0.92);
        ctx.quadraticCurveTo(sx * 0.5, capBaseY + dy * 0.4, sx * 0.14, capBaseY + 0.2);
        ctx.closePath();
        ctx.fill();
      });
      ctx.strokeStyle = rgb21(p.outline);
      ctx.lineWidth = 0.9;
      sepals.forEach(([sx, dy]) => {
        ctx.beginPath();
        ctx.moveTo(sx * 0.35, capBaseY - 1.2);
        ctx.quadraticCurveTo(sx * 0.9, capBaseY + 0.4, sx, capBaseY + dy);
        ctx.stroke();
      });
      ctx.fillStyle = rgb21(p.cap);
      ctx.beginPath();
      ctx.ellipse(0, capBaseY, 3.4, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba13(lerpRGB16(p.cap, [255, 255, 240], 0.5), 0.5);
      ctx.beginPath();
      ctx.ellipse(-1.4, capBaseY - 0.8, 1.6, 1, -0.3, 0, Math.PI * 2);
      ctx.fill();
      if (p.capDry > 0.02) {
        ctx.fillStyle = rgba13([176, 150, 86], 0.5 * p.capDry);
        sepals.forEach(([sx, dy]) => {
          ctx.beginPath();
          ctx.arc(sx, capBaseY + dy * 0.92, 1.4, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.strokeStyle = rgb21(p.outline);
      ctx.lineWidth = 3.6;
      ctx.beginPath();
      ctx.moveTo(-0.2, capBaseY - 1);
      ctx.quadraticCurveTo(-0.8, capBaseY - 5.5, 0.8, capBaseY - 8);
      ctx.stroke();
      ctx.strokeStyle = rgb21(p.cap);
      ctx.lineWidth = 2.2;
      ctx.beginPath();
      ctx.moveTo(-0.2, capBaseY - 1);
      ctx.quadraticCurveTo(-0.8, capBaseY - 5.5, 0.8, capBaseY - 8);
      ctx.stroke();
      ctx.strokeStyle = rgba13(lerpRGB16(p.cap, [255, 255, 240], 0.4), 0.45);
      ctx.lineWidth = 0.8;
      ctx.beginPath();
      ctx.moveTo(-1, capBaseY - 2);
      ctx.quadraticCurveTo(-1.4, capBaseY - 5.5, -0.4, capBaseY - 7.5);
      ctx.stroke();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba13(p.light, p.lightAmt));
        lg.addColorStop(1, rgba13(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt21(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw14(season) {
    return (ctx) => paint21(ctx, SP21[season], 0);
  }
  function anim14(season) {
    return (ctx, t) => {
      const bob = bobAt21(t);
      paint21(ctx, SP21[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = 0 + bob + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-4.5, gy, 1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const top = EGG_TOP + bob;
          const bot = EGG_BOT + bob;
          const gy = lerp23(top + 2, bot - 3, prog);
          ctx.fillStyle = "rgba(255,255,255,0.85)";
          ctx.beginPath();
          ctx.ellipse(-4.6, gy, 1.4, 2.8, -0.08, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.ellipse(-0.5, gy * 0.96, 0.9, 2, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,236,200,${s})`;
          ctx.beginPath();
          ctx.ellipse(-3.5, -2 + bob, 4.2, 3, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 2 + bob, 5.5, 4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition18(fromIdx) {
    const from = SP21[SEASON_NAMES[fromIdx]];
    const to = SP21[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother21(clamp0122(pp));
      paint21(ctx, lerpP21(from, to, k), 0);
    };
  }
  var springToSummer17 = makeTransition18(0);
  var summerToAutumn17 = makeTransition18(1);
  var autumnToWinter17 = makeTransition18(2);
  var VARIANTS23 = {
    Spring: { draw: draw14("Spring"), anim: anim14("Spring") },
    Summer: { draw: draw14("Summer"), anim: anim14("Summer") },
    Autumn: { draw: draw14("Autumn"), anim: anim14("Autumn") },
    Winter: { draw: draw14("Winter"), anim: anim14("Winter") }
  };
  var TRANSITIONS23 = {
    0: springToSummer17,
    1: summerToAutumn17,
    2: autumnToWinter17
  };

  // src/textures/seasonal/veg/squash.ts
  function clamp0123(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother22 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp24(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB17(a, b, t) {
    return [lerp24(a[0], b[0], t), lerp24(a[1], b[1], t), lerp24(a[2], b[2], t)];
  }
  function rgb22(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba14(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0123(a)})`;
  }
  function lerpP22(a, b, t) {
    return {
      skinLight: lerpRGB17(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB17(a.skinMid, b.skinMid, t),
      skinDark: lerpRGB17(a.skinDark, b.skinDark, t),
      stem: lerpRGB17(a.stem, b.stem, t),
      padGrass: lerpRGB17(a.padGrass, b.padGrass, t),
      padDark: lerpRGB17(a.padDark, b.padDark, t),
      soil: lerpRGB17(a.soil, b.soil, t),
      outline: lerpRGB17(a.outline, b.outline, t),
      light: lerpRGB17(a.light, b.light, t),
      lightAmt: lerp24(a.lightAmt, b.lightAmt, t),
      ripeness: lerp24(a.ripeness, b.ripeness, t),
      gloss: lerp24(a.gloss, b.gloss, t),
      frostAmt: lerp24(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp24(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp24(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp24(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp24(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP14(p) {
    return {
      ...p,
      lightAmt: clamp0123(p.lightAmt),
      ripeness: clamp0123(p.ripeness),
      gloss: clamp0123(p.gloss),
      frostAmt: clamp0123(p.frostAmt),
      snowCapAmt: clamp0123(p.snowCapAmt),
      padSnowAmt: clamp0123(p.padSnowAmt),
      blossomAmt: clamp0123(p.blossomAmt),
      fallenLeafAmt: clamp0123(p.fallenLeafAmt)
    };
  }
  var SP22 = {
    // Spring — fresh pastel; young green-tinged golden squash, matte; dewy lime pad + blossom.
    Spring: {
      skinLight: [232, 214, 132],
      skinMid: [206, 176, 84],
      skinDark: [150, 124, 56],
      stem: [126, 150, 74],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [86, 66, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.25,
      gloss: 0.14,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK full golden squash, smooth; saturated mid-green pad, warm light.
    Summer: {
      skinLight: [255, 214, 96],
      skinMid: [238, 174, 50],
      skinDark: [176, 116, 26],
      stem: [120, 132, 60],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [104, 68, 22],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.7,
      gloss: 0.6,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — deep amber ripe squash, richest; olive-tan browning pad, fallen leaves.
    Autumn: {
      skinLight: [240, 174, 70],
      skinMid: [212, 132, 38],
      skinDark: [150, 84, 26],
      stem: [128, 110, 52],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [96, 54, 20],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; snow-capped + frosted golden squash, snowy pad.
    Winter: {
      skinLight: [222, 184, 104],
      skinMid: [196, 150, 70],
      skinDark: [136, 100, 52],
      stem: [120, 116, 86],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [70, 54, 44],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.85,
      gloss: 0.2,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var BULB_CY = 11;
  var BULB_RX = 12.5;
  var BULB_RY = 8.5;
  var WAIST_Y = 0;
  var WAIST_HALF = 6;
  var NECK_TOP_Y = -16;
  var NECK_HALF = 5.4;
  var RIB_FRACS = [-0.62, -0.2, 0.2, 0.62];
  function gourdBodyPath(ctx, bob) {
    const bcy = BULB_CY + bob;
    const wy = WAIST_Y + bob;
    const nty = NECK_TOP_Y + bob;
    ctx.beginPath();
    ctx.moveTo(-NECK_HALF, nty + 1);
    ctx.quadraticCurveTo(-NECK_HALF, nty - 2.5, 0, nty - 3);
    ctx.quadraticCurveTo(NECK_HALF, nty - 2.5, NECK_HALF, nty + 1);
    ctx.quadraticCurveTo(NECK_HALF + 1.2, lerp24(nty, wy, 0.5), WAIST_HALF, wy);
    ctx.quadraticCurveTo(BULB_RX, bcy - BULB_RY * 0.7, BULB_RX, bcy);
    ctx.quadraticCurveTo(BULB_RX, bcy + BULB_RY, 0, bcy + BULB_RY);
    ctx.quadraticCurveTo(-BULB_RX, bcy + BULB_RY, -BULB_RX, bcy);
    ctx.quadraticCurveTo(-BULB_RX, bcy - BULB_RY * 0.7, -WAIST_HALF, wy);
    ctx.quadraticCurveTo(-NECK_HALF - 1.2, lerp24(nty, wy, 0.5), -NECK_HALF, nty + 1);
    ctx.closePath();
  }
  function halfWidthAt(y) {
    if (y <= WAIST_Y) {
      const k2 = clamp0123((y - NECK_TOP_Y) / (WAIST_Y - NECK_TOP_Y));
      return lerp24(NECK_HALF, WAIST_HALF, k2);
    }
    const bottom = BULB_CY + BULB_RY;
    if (y <= BULB_CY) {
      const k2 = clamp0123((y - WAIST_Y) / (BULB_CY - WAIST_Y));
      return lerp24(WAIST_HALF, BULB_RX, k2);
    }
    const k = clamp0123((y - BULB_CY) / (bottom - BULB_CY));
    return lerp24(BULB_RX, 0.5, k);
  }
  function paint22(ctx, raw, bob) {
    const p = clampP14(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba14(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb22(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb22(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb22(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba14([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba14([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba14([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba14([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba14(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba14([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba14(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba14([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb22(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, BULB_CY + BULB_RY + bob + 0.5, 9, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba14(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, BULB_CY + BULB_RY + bob + 1, 12, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.translate(0, 0);
      gourdBodyPath(ctx, bob);
      ctx.lineWidth = 2.2;
      ctx.strokeStyle = rgb22(p.outline);
      ctx.stroke();
      ctx.fillStyle = rgb22(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      gourdBodyPath(ctx, bob);
      ctx.clip();
      const bcy = BULB_CY + bob;
      const nty = NECK_TOP_Y + bob;
      ctx.fillStyle = rgb22(p.skinMid);
      ctx.fillRect(-BULB_RX - 3, nty - 6, (BULB_RX + 3) * 2, bcy + BULB_RY - nty + 12);
      const litGrad = ctx.createLinearGradient(-BULB_RX, nty, BULB_RX, bcy + BULB_RY);
      litGrad.addColorStop(0, rgb22(p.skinLight));
      litGrad.addColorStop(0.5, rgb22(p.skinMid));
      litGrad.addColorStop(1, rgb22(p.skinDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-2.5, lerp24(nty, bcy, 0.55), BULB_RX + 2, (bcy + BULB_RY - nty) * 0.6, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      const yTop = nty - 1;
      const yBot = bcy + BULB_RY - 1;
      RIB_FRACS.forEach((frac) => {
        ctx.strokeStyle = rgba14(p.skinDark, 0.62);
        ctx.lineWidth = 1.7;
        ctx.beginPath();
        let first = true;
        for (let y = yTop; y <= yBot; y += 1.6) {
          const hw = halfWidthAt(y - bob);
          const x = frac * hw;
          if (first) {
            ctx.moveTo(x, y);
            first = false;
          } else ctx.lineTo(x, y);
        }
        ctx.stroke();
        ctx.strokeStyle = rgba14(p.skinLight, 0.4);
        ctx.lineWidth = 1;
        ctx.beginPath();
        first = true;
        for (let y = yTop; y <= yBot; y += 1.6) {
          const hw = halfWidthAt(y - bob);
          const x = frac * hw - 1.3;
          if (first) {
            ctx.moveTo(x, y);
            first = false;
          } else ctx.lineTo(x, y);
        }
        ctx.stroke();
      });
      ctx.fillStyle = rgba14(p.skinDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(5, bcy + 2.5, BULB_RX * 0.7, BULB_RY * 0.9, -0.2, 0, Math.PI * 2);
      ctx.fill();
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba14([255, 255, 255], 0.12 + 0.5 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-4.5, lerp24(nty, bcy, 0.4), 2.2, (bcy - nty) * 0.5, -0.1, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba14([255, 255, 255], 0.08 + 0.32 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-6, bcy + 1, 1.8, 3.4, -0.1, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba14([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp24(nty, bcy, 0.35), BULB_RX, (bcy - nty) * 0.45, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba14([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-7, nty + 4],
          [-2, nty + 2],
          [3, nty + 5],
          [7, nty + 8],
          [-6, lerp24(nty, bcy, 0.5)],
          [5, lerp24(nty, bcy, 0.55)],
          [0, lerp24(nty, bcy, 0.35)],
          [-9, bcy],
          [8, bcy - 1]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba14([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-NECK_HALF - 0.5, nty + 1.5);
        ctx.quadraticCurveTo(-3, nty - 4, 0, nty - 3.5);
        ctx.quadraticCurveTo(3, nty - 4, NECK_HALF + 0.5, nty + 1.5);
        ctx.quadraticCurveTo(2.5, nty + 4, 0, nty + 2.8);
        ctx.quadraticCurveTo(-2.5, nty + 4, -NECK_HALF - 0.5, nty + 1.5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba14([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, nty + 2, NECK_HALF * 0.7, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba14([240, 248, 255], 0.7 * a);
        ctx.beginPath();
        ctx.ellipse(-6, bcy - BULB_RY + 1, 3.6, 1.6, -0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      const stemBaseY = nty - 2;
      ctx.strokeStyle = rgb22(p.outline);
      ctx.lineWidth = 4.6;
      ctx.beginPath();
      ctx.moveTo(0.2, stemBaseY + 1.5);
      ctx.quadraticCurveTo(-0.4, stemBaseY - 3, 1.2, stemBaseY - 6.5);
      ctx.stroke();
      ctx.strokeStyle = rgb22(p.stem);
      ctx.lineWidth = 3;
      ctx.beginPath();
      ctx.moveTo(0.2, stemBaseY + 1.5);
      ctx.quadraticCurveTo(-0.4, stemBaseY - 3, 1.2, stemBaseY - 6.5);
      ctx.stroke();
      ctx.strokeStyle = rgba14(p.skinLight, 0.45);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(-0.7, stemBaseY - 0.5);
      ctx.quadraticCurveTo(-1.1, stemBaseY - 3.5, 0.4, stemBaseY - 6);
      ctx.stroke();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba14(p.light, p.lightAmt));
        lg.addColorStop(1, rgba14(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt22(t, amp = 0.8, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw15(season) {
    return (ctx) => paint22(ctx, SP22[season], 0);
  }
  function anim15(season) {
    return (ctx, t) => {
      const bob = bobAt22(t);
      paint22(ctx, SP22[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.24 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -2 + bob + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-5, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const gy = lerp24(NECK_TOP_Y + 1 + bob, BULB_CY + BULB_RY - 2 + bob, prog);
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(-4.5, gy, 1.6, 3, -0.1, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.35)";
          ctx.beginPath();
          ctx.ellipse(0.5, gy * 0.96, 1, 2, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,228,180,${s})`;
          ctx.beginPath();
          ctx.ellipse(-4, 2 + bob, 5, 3.6, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 4 + bob, 7, 5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition19(fromIdx) {
    const from = SP22[SEASON_NAMES[fromIdx]];
    const to = SP22[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother22(clamp0123(pp));
      paint22(ctx, lerpP22(from, to, k), 0);
    };
  }
  var springToSummer18 = makeTransition19(0);
  var summerToAutumn18 = makeTransition19(1);
  var autumnToWinter18 = makeTransition19(2);
  var VARIANTS24 = {
    Spring: { draw: draw15("Spring"), anim: anim15("Spring") },
    Summer: { draw: draw15("Summer"), anim: anim15("Summer") },
    Autumn: { draw: draw15("Autumn"), anim: anim15("Autumn") },
    Winter: { draw: draw15("Winter"), anim: anim15("Winter") }
  };
  var TRANSITIONS24 = {
    0: springToSummer18,
    1: summerToAutumn18,
    2: autumnToWinter18
  };

  // src/textures/seasonal/veg/turnip.ts
  function clamp0124(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother23 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp25(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB18(a, b, t) {
    return [lerp25(a[0], b[0], t), lerp25(a[1], b[1], t), lerp25(a[2], b[2], t)];
  }
  function rgb23(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba15(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0124(a)})`;
  }
  function lerpP23(a, b, t) {
    return {
      bulbLight: lerpRGB18(a.bulbLight, b.bulbLight, t),
      bulbMid: lerpRGB18(a.bulbMid, b.bulbMid, t),
      bulbDark: lerpRGB18(a.bulbDark, b.bulbDark, t),
      shoulderLight: lerpRGB18(a.shoulderLight, b.shoulderLight, t),
      shoulderMid: lerpRGB18(a.shoulderMid, b.shoulderMid, t),
      shoulderDark: lerpRGB18(a.shoulderDark, b.shoulderDark, t),
      leafLight: lerpRGB18(a.leafLight, b.leafLight, t),
      leafMid: lerpRGB18(a.leafMid, b.leafMid, t),
      leafDark: lerpRGB18(a.leafDark, b.leafDark, t),
      root: lerpRGB18(a.root, b.root, t),
      padGrass: lerpRGB18(a.padGrass, b.padGrass, t),
      padDark: lerpRGB18(a.padDark, b.padDark, t),
      soil: lerpRGB18(a.soil, b.soil, t),
      outline: lerpRGB18(a.outline, b.outline, t),
      light: lerpRGB18(a.light, b.light, t),
      lightAmt: lerp25(a.lightAmt, b.lightAmt, t),
      ripeness: lerp25(a.ripeness, b.ripeness, t),
      gloss: lerp25(a.gloss, b.gloss, t),
      frostAmt: lerp25(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp25(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp25(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp25(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp25(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP15(p) {
    return {
      ...p,
      lightAmt: clamp0124(p.lightAmt),
      ripeness: clamp0124(p.ripeness),
      gloss: clamp0124(p.gloss),
      frostAmt: clamp0124(p.frostAmt),
      snowCapAmt: clamp0124(p.snowCapAmt),
      padSnowAmt: clamp0124(p.padSnowAmt),
      blossomAmt: clamp0124(p.blossomAmt),
      fallenLeafAmt: clamp0124(p.fallenLeafAmt)
    };
  }
  var SP23 = {
    // Spring — small pale young turnip; bright young lime leaves; dewy lime pad +
    // blossom. Cool-bright light. (Small bulb via low ripeness.)
    Spring: {
      bulbLight: [248, 250, 248],
      bulbMid: [226, 230, 230],
      bulbDark: [186, 196, 200],
      shoulderLight: [216, 120, 196],
      shoulderMid: [186, 78, 166],
      shoulderDark: [138, 52, 122],
      leafLight: [168, 224, 110],
      leafMid: [110, 188, 74],
      leafDark: [66, 138, 52],
      root: [228, 226, 214],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [58, 48, 60],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.2,
      gloss: 0.22,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK: plump ripe turnip, crisp white bulb, VIVID magenta shoulder,
    // lush deep-green leaves; warm light, strong gloss; mid-green pad.
    Summer: {
      bulbLight: [255, 255, 255],
      bulbMid: [238, 240, 238],
      bulbDark: [196, 204, 206],
      shoulderLight: [234, 96, 206],
      shoulderMid: [204, 52, 178],
      shoulderDark: [150, 30, 130],
      leafLight: [128, 204, 84],
      leafMid: [78, 166, 58],
      leafDark: [44, 116, 42],
      root: [236, 234, 222],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [54, 40, 56],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 0.78,
      gloss: 0.95,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fat mature turnip (largest bulb); leaves yellowing a touch;
    // olive-tan browning pad + a couple of fallen leaves; low amber light.
    Autumn: {
      bulbLight: [246, 244, 232],
      bulbMid: [222, 218, 202],
      bulbDark: [180, 174, 156],
      shoulderLight: [210, 96, 168],
      shoulderMid: [176, 60, 142],
      shoulderDark: [128, 44, 108],
      leafLight: [196, 196, 88],
      leafMid: [150, 154, 64],
      leafDark: [104, 104, 48],
      root: [214, 204, 178],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [62, 46, 44],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 1,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; frost-dusted turnip + a small snow cap; the
    // white-and-magenta identity stays CLEARLY visible; snowy pad. No white-out.
    Winter: {
      bulbLight: [236, 244, 252],
      bulbMid: [206, 218, 230],
      bulbDark: [160, 178, 196],
      shoulderLight: [196, 118, 188],
      shoulderMid: [160, 80, 154],
      shoulderDark: [112, 56, 110],
      leafLight: [150, 186, 156],
      leafMid: [104, 148, 116],
      leafDark: [66, 106, 84],
      root: [206, 212, 218],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [52, 48, 64],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.82,
      gloss: 0.25,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var BULB_CY2 = 7;
  var BULB_RX2 = 13;
  var BULB_RY2 = 12;
  var SHOULDER_FRAC = 0.34;
  function bulbRadii(ripeness) {
    const k = 0.74 + 0.26 * clamp0124(ripeness);
    return { rx: BULB_RX2 * k, ry: BULB_RY2 * k };
  }
  function bulbPath(ctx, cy, rx, ry) {
    const top = cy - ry;
    const bot = cy + ry;
    ctx.beginPath();
    ctx.moveTo(0, top);
    ctx.bezierCurveTo(-rx * 1.02, top + ry * 0.15, -rx * 1.1, cy + ry * 0.1, -rx * 0.7, bot - ry * 0.12);
    ctx.quadraticCurveTo(-rx * 0.42, bot + ry * 0.12, 0, bot);
    ctx.quadraticCurveTo(rx * 0.42, bot + ry * 0.12, rx * 0.7, bot - ry * 0.12);
    ctx.bezierCurveTo(rx * 1.1, cy + ry * 0.1, rx * 1.02, top + ry * 0.15, 0, top);
    ctx.closePath();
  }
  function paint23(ctx, raw, bob) {
    const p = clampP15(raw);
    const { rx, ry } = bulbRadii(p.ripeness);
    const cy = BULB_CY2 + bob;
    const top = cy - ry;
    const bot = cy + ry;
    const splitY = top + ry * 2 * SHOULDER_FRAC;
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba15(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb23(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb23(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb23(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba15([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba15([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba15([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba15([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba15(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba15([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba15(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba15([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb23(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, bot + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba15(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, bot + 2, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb23(p.outline);
      ctx.lineWidth = 2.4;
      ctx.beginPath();
      ctx.moveTo(0, bot - 1);
      ctx.quadraticCurveTo(0.6, bot + 3.5, -0.4, bot + 7);
      ctx.stroke();
      ctx.strokeStyle = rgb23(p.root);
      ctx.lineWidth = 1.2;
      ctx.beginPath();
      ctx.moveTo(0, bot - 1);
      ctx.quadraticCurveTo(0.6, bot + 3.5, -0.4, bot + 7);
      ctx.stroke();
      ctx.save();
      bulbPath(ctx, cy, rx + 1.1, ry + 1.1);
      ctx.fillStyle = rgb23(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      bulbPath(ctx, cy, rx, ry);
      ctx.clip();
      ctx.fillStyle = rgb23(p.bulbMid);
      ctx.fillRect(-rx - 3, top - 3, (rx + 3) * 2, ry * 2 + 6);
      const bulbGrad = ctx.createLinearGradient(-rx, top, rx, bot);
      bulbGrad.addColorStop(0, rgb23(p.bulbLight));
      bulbGrad.addColorStop(0.5, rgb23(p.bulbMid));
      bulbGrad.addColorStop(1, rgb23(p.bulbDark));
      ctx.fillStyle = bulbGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-1.5, cy + ry * 0.18, rx + 2, ry * 0.96, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      const shGrad = ctx.createLinearGradient(0, top, 0, splitY + 2);
      shGrad.addColorStop(0, rgb23(p.shoulderLight));
      shGrad.addColorStop(0.55, rgb23(p.shoulderMid));
      shGrad.addColorStop(1, rgb23(p.shoulderDark));
      ctx.fillStyle = shGrad;
      ctx.beginPath();
      ctx.moveTo(-rx - 2, splitY);
      ctx.quadraticCurveTo(0, splitY + ry * 0.34, rx + 2, splitY);
      ctx.lineTo(rx + 2, top - 4);
      ctx.lineTo(-rx - 2, top - 4);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgba15(p.shoulderDark, 0.4);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.moveTo(-rx, splitY - 0.5);
      ctx.quadraticCurveTo(0, splitY + ry * 0.3, rx, splitY - 0.5);
      ctx.stroke();
      ctx.fillStyle = rgba15(p.shoulderLight, 0.5);
      ctx.beginPath();
      ctx.ellipse(-rx * 0.4, top + ry * 0.34, rx * 0.42, ry * 0.3, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba15(p.bulbDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(rx * 0.62, cy + ry * 0.28, rx * 0.42, ry * 0.7, 0.15, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba15(p.bulbDark, 0.3);
      ctx.lineWidth = 0.8;
      [-rx * 0.4, 0, rx * 0.4].forEach((sx) => {
        ctx.beginPath();
        ctx.moveTo(sx, splitY + 1);
        ctx.quadraticCurveTo(sx * 0.4, lerp25(splitY, bot, 0.6), sx * 0.1, bot - 2);
        ctx.stroke();
      });
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba15([255, 255, 255], 0.18 + 0.5 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-rx * 0.42, cy - ry * 0.1, rx * 0.22, ry * 0.4, -0.2, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba15([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, cy - ry * 0.25, rx * 0.92, ry * 0.55, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba15([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-rx * 0.5, top + 4],
          [-rx * 0.1, top + 2.5],
          [rx * 0.4, top + 5],
          [-rx * 0.3, cy],
          [rx * 0.3, cy + 2],
          [0, cy - ry * 0.4]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba15([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-rx * 0.62, top + 2);
        ctx.quadraticCurveTo(-rx * 0.3, top - 4.5, 0, top - 3.5);
        ctx.quadraticCurveTo(rx * 0.3, top - 4.5, rx * 0.62, top + 2);
        ctx.quadraticCurveTo(rx * 0.34, top + 4.5, 0, top + 3);
        ctx.quadraticCurveTo(-rx * 0.34, top + 4.5, -rx * 0.62, top + 2);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba15([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 2.2, rx * 0.5, 1.5, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      drawLeaves(ctx, p, top, 0);
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba15(p.light, p.lightAmt));
        lg.addColorStop(1, rgba15(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function drawLeaves(ctx, p, topY, sway) {
    const leaves = [
      [-0.62, 12, 3],
      [-0.26, 15, 3.4],
      [0.04, 16, 3.6],
      [0.34, 14, 3.2],
      [0.66, 11, 2.8]
    ];
    ctx.save();
    ctx.translate(0, topY - 1);
    leaves.forEach(([baseAng, len, w], i) => {
      const lean = sway * (0.6 + 0.4 * Math.abs(baseAng) / 0.66);
      const ang = baseAng + lean;
      const tipX = Math.sin(ang) * len;
      const tipY = -Math.cos(ang) * len;
      const midX = Math.sin(ang) * len * 0.5 - Math.cos(ang) * 0.6;
      const midY = -Math.cos(ang) * len * 0.5;
      ctx.strokeStyle = rgb23(p.outline);
      ctx.lineWidth = w + 1.6;
      ctx.beginPath();
      ctx.moveTo(0, 1);
      ctx.quadraticCurveTo(midX, midY, tipX, tipY);
      ctx.stroke();
      ctx.strokeStyle = i % 2 === 0 ? rgb23(p.leafMid) : rgb23(p.leafLight);
      ctx.lineWidth = w;
      ctx.beginPath();
      ctx.moveTo(0, 1);
      ctx.quadraticCurveTo(midX, midY, tipX, tipY);
      ctx.stroke();
      ctx.strokeStyle = rgba15(p.leafDark, 0.5);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(0, 0.5);
      ctx.quadraticCurveTo(midX * 0.92, midY * 0.92, tipX * 0.9, tipY * 0.9);
      ctx.stroke();
    });
    ctx.fillStyle = rgb23(p.leafDark);
    ctx.beginPath();
    ctx.ellipse(0, 1.5, 3.2, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function bobAt23(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw16(season) {
    return (ctx) => paint23(ctx, SP23[season], 0);
  }
  function anim16(season) {
    return (ctx, t) => {
      const bob = bobAt23(t);
      paint23(ctx, SP23[season], bob);
      const p = clampP15(SP23[season]);
      const { ry } = bulbRadii(p.ripeness);
      const leafTopY = BULB_CY2 + bob - ry;
      const sway = Math.sin(t * 1.3) * 0.1;
      ctx.save();
      try {
        drawLeaves(ctx, p, leafTopY, sway);
      } finally {
        ctx.restore();
      }
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = BULB_CY2 + bob + Math.sin(t * 1.1) * 1.2;
          ctx.beginPath();
          ctx.arc(-5, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const { ry: ry2 } = bulbRadii(p.ripeness);
          const gy = lerp25(BULB_CY2 + bob - ry2 * 0.4, BULB_CY2 + bob + ry2 * 0.6, prog);
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(-4.5, gy, 1.4, 2.4, -0.1, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fx = 9 - prog * 5 + Math.sin(prog * Math.PI * 3) * 2.2;
          const fy = -6 + prog * 26;
          ctx.save();
          ctx.translate(fx, fy);
          ctx.rotate(prog * Math.PI * 2);
          ctx.globalAlpha = 0.4 + 0.5 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "rgb(196,150,64)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, BULB_CY2 + bob - 2, 6, 4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition20(fromIdx) {
    const from = SP23[SEASON_NAMES[fromIdx]];
    const to = SP23[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother23(clamp0124(pp));
      paint23(ctx, lerpP23(from, to, k), 0);
    };
  }
  var springToSummer19 = makeTransition20(0);
  var summerToAutumn19 = makeTransition20(1);
  var autumnToWinter19 = makeTransition20(2);
  var VARIANTS25 = {
    Spring: { draw: draw16("Spring"), anim: anim16("Spring") },
    Summer: { draw: draw16("Summer"), anim: anim16("Summer") },
    Autumn: { draw: draw16("Autumn"), anim: anim16("Autumn") },
    Winter: { draw: draw16("Winter"), anim: anim16("Winter") }
  };
  var TRANSITIONS25 = {
    0: springToSummer19,
    1: summerToAutumn19,
    2: autumnToWinter19
  };

  // src/textures/seasonal/grass/grass.ts
  function clamp0125(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother24 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp26(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB19(a, b, t) {
    return [lerp26(a[0], b[0], t), lerp26(a[1], b[1], t), lerp26(a[2], b[2], t)];
  }
  function rgb24(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba16(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0125(a)})`;
  }
  function lerpP24(a, b, t) {
    return {
      bladeLight: lerpRGB19(a.bladeLight, b.bladeLight, t),
      bladeMid: lerpRGB19(a.bladeMid, b.bladeMid, t),
      bladeDark: lerpRGB19(a.bladeDark, b.bladeDark, t),
      tipTint: lerpRGB19(a.tipTint, b.tipTint, t),
      padGrass: lerpRGB19(a.padGrass, b.padGrass, t),
      padDark: lerpRGB19(a.padDark, b.padDark, t),
      soil: lerpRGB19(a.soil, b.soil, t),
      outline: lerpRGB19(a.outline, b.outline, t),
      light: lerpRGB19(a.light, b.light, t),
      lightAmt: lerp26(a.lightAmt, b.lightAmt, t),
      lushness: lerp26(a.lushness, b.lushness, t),
      tipAmt: lerp26(a.tipAmt, b.tipAmt, t),
      bentAmt: lerp26(a.bentAmt, b.bentAmt, t),
      frostAmt: lerp26(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp26(a.snowCapAmt, b.snowCapAmt, t),
      snowBlanketAmt: lerp26(a.snowBlanketAmt, b.snowBlanketAmt, t),
      padSnowAmt: lerp26(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp26(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp26(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP16(p) {
    return {
      ...p,
      lightAmt: clamp0125(p.lightAmt),
      lushness: clamp0125(p.lushness),
      tipAmt: clamp0125(p.tipAmt),
      bentAmt: clamp0125(p.bentAmt),
      frostAmt: clamp0125(p.frostAmt),
      snowCapAmt: clamp0125(p.snowCapAmt),
      snowBlanketAmt: clamp0125(p.snowBlanketAmt),
      padSnowAmt: clamp0125(p.padSnowAmt),
      blossomAmt: clamp0125(p.blossomAmt),
      fallenLeafAmt: clamp0125(p.fallenLeafAmt)
    };
  }
  var SP24 = {
    // Spring — fresh lime-green new blades, dewy. Lime dewy pad + blossom.
    Spring: {
      bladeLight: [186, 232, 110],
      bladeMid: [126, 200, 76],
      bladeDark: [70, 138, 50],
      tipTint: [210, 244, 140],
      padGrass: [134, 210, 90],
      padDark: [74, 142, 60],
      soil: [120, 84, 48],
      outline: [40, 70, 32],
      light: [232, 246, 224],
      lightAmt: 0.16,
      lushness: 0.55,
      tipAmt: 0.3,
      bentAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      snowBlanketAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — lush deep-green blades (PEAK). Mid-green pad, warm light.
    Summer: {
      bladeLight: [150, 214, 78],
      bladeMid: [70, 162, 56],
      bladeDark: [34, 104, 42],
      tipTint: [168, 224, 96],
      padGrass: [86, 172, 72],
      padDark: [42, 112, 50],
      soil: [126, 86, 48],
      outline: [24, 70, 30],
      light: [255, 242, 206],
      lightAmt: 0.18,
      lushness: 1,
      tipAmt: 0.2,
      bentAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      snowBlanketAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — golden-brown blade tips, a few blades bent. Olive-tan pad, leaves.
    Autumn: {
      bladeLight: [180, 192, 90],
      bladeMid: [120, 146, 60],
      bladeDark: [80, 96, 44],
      tipTint: [216, 170, 64],
      // golden-brown tips
      padGrass: [156, 152, 88],
      padDark: [108, 98, 54],
      soil: [120, 80, 44],
      outline: [62, 56, 26],
      light: [248, 212, 150],
      lightAmt: 0.2,
      lushness: 0.5,
      tipAmt: 0.85,
      bentAmt: 0.8,
      frostAmt: 0,
      snowCapAmt: 0,
      snowBlanketAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — blades poke up through a snow blanket; frost on tips, snow caps at
    // the base; blades still green-grey visible; cool light.
    Winter: {
      bladeLight: [148, 178, 142],
      bladeMid: [94, 134, 102],
      bladeDark: [60, 96, 78],
      tipTint: [206, 224, 224],
      padGrass: [180, 198, 216],
      padDark: [122, 148, 174],
      soil: [128, 110, 96],
      outline: [50, 66, 70],
      light: [206, 226, 252],
      lightAmt: 0.3,
      lushness: 0.4,
      tipAmt: 0.4,
      bentAmt: 0.15,
      frostAmt: 0.75,
      snowCapAmt: 0.85,
      snowBlanketAmt: 0.8,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var TUFT_BASE_Y = 17;
  var BLADES = [
    // back row (darker, drawn first for depth)
    { baseX: -2, tipX: -10.5, tipY: -12, ctrl: -5, width: 2.4, back: true },
    { baseX: 2, tipX: 10.5, tipY: -12, ctrl: 5, width: 2.4, back: true },
    { baseX: -1, tipX: -5.5, tipY: -20, ctrl: -2.5, width: 2.6, back: true },
    { baseX: 1, tipX: 5.5, tipY: -20, ctrl: 2.5, width: 2.6, back: true },
    { baseX: 0, tipX: 0.5, tipY: -23, ctrl: 0, width: 2.8, back: true },
    // front row (brighter, drawn over)
    { baseX: -3, tipX: -13, tipY: -7, ctrl: -8, width: 2.2, back: false },
    { baseX: 3, tipX: 13, tipY: -7, ctrl: 8, width: 2.2, back: false },
    { baseX: -2.2, tipX: -8.5, tipY: -15, ctrl: -5.5, width: 2.4, back: false },
    { baseX: 2.2, tipX: 8.5, tipY: -15, ctrl: 5.5, width: 2.4, back: false },
    { baseX: -1.2, tipX: -3, tipY: -21, ctrl: -2, width: 2.5, back: false },
    { baseX: 1.2, tipX: 3, tipY: -21, ctrl: 2, width: 2.5, back: false },
    { baseX: -0.4, tipX: -1.5, tipY: -24, ctrl: -1, width: 2.6, back: false }
  ];
  function bladeSway(b, bend, phase) {
    const reach = Math.abs(b.tipX) / 13 * 0.6 + -b.tipY / 24 * 0.4;
    return bend * reach * Math.sin(phase + Math.abs(b.baseX) * 0.5);
  }
  function paint24(ctx, raw, bob, bend = 0, phase = 0) {
    const p = clampP16(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba16(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb24(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb24(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb24(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.2);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba16([255, 255, 255], 0.16);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba16([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba16([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba16([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba16(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba16([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba16(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba16([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb24(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, TUFT_BASE_Y + 2, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba16(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(2.5, TUFT_BASE_Y + 2.4, 10, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const drawBlade = (b, idx) => {
        const sway = bladeSway(b, bend, phase);
        const bendOver = p.bentAmt * (b.back ? 0 : idx % 3 === 0 ? 1 : 0);
        const baseX = b.baseX;
        const baseY = TUFT_BASE_Y + bob;
        const tipX = b.tipX + sway + bendOver * (b.tipX > 0 ? 7 : -1.5);
        const tipY = b.tipY + bob + bendOver * 9;
        const cx = b.baseX + b.ctrl + sway * 0.6 + bendOver * 3;
        const cy = lerp26(baseY, tipY, 0.5) - 2;
        ctx.strokeStyle = rgb24(p.outline);
        ctx.lineWidth = b.width + 1.6;
        ctx.beginPath();
        ctx.moveTo(baseX, baseY);
        ctx.quadraticCurveTo(cx, cy, tipX, tipY);
        ctx.stroke();
        ctx.strokeStyle = rgb24(b.back ? p.bladeDark : p.bladeMid);
        ctx.lineWidth = b.width;
        ctx.beginPath();
        ctx.moveTo(baseX, baseY);
        ctx.quadraticCurveTo(cx, cy, tipX, tipY);
        ctx.stroke();
        if (!b.back) {
          ctx.strokeStyle = rgba16(p.bladeLight, 0.8);
          ctx.lineWidth = Math.max(0.7, b.width - 1.4);
          ctx.beginPath();
          ctx.moveTo(baseX - 0.3, baseY - 1);
          ctx.quadraticCurveTo(cx - 0.6, cy, tipX - 0.4, tipY);
          ctx.stroke();
        }
        if (p.tipAmt > 0.02) {
          ctx.strokeStyle = rgba16(p.tipTint, 0.9 * p.tipAmt);
          ctx.lineWidth = b.width;
          const ttX = lerp26(cx, tipX, 0.55);
          const ttY = lerp26(cy, tipY, 0.55);
          ctx.beginPath();
          ctx.moveTo(ttX, ttY);
          ctx.quadraticCurveTo(lerp26(ttX, tipX, 0.5), lerp26(ttY, tipY, 0.5), tipX, tipY);
          ctx.stroke();
        }
        if (p.frostAmt > 0.02) {
          ctx.fillStyle = rgba16([236, 248, 255], 0.85 * p.frostAmt);
          ctx.beginPath();
          ctx.arc(tipX, tipY, 0.8 + 0.4 * p.frostAmt, 0, Math.PI * 2);
          ctx.fill();
        }
      };
      BLADES.forEach((b, i) => {
        if (b.back) drawBlade(b, i);
      });
      BLADES.forEach((b, i) => {
        if (!b.back) drawBlade(b, i);
      });
      if (p.snowBlanketAmt > 0.02) {
        const a = p.snowBlanketAmt;
        const by = TUFT_BASE_Y + bob;
        ctx.fillStyle = rgba16([244, 250, 255], 0.9 * a);
        ctx.beginPath();
        ctx.moveTo(-9, by + 1);
        ctx.quadraticCurveTo(-7, by - 5, -3, by - 4.5);
        ctx.quadraticCurveTo(0, by - 6.5, 3, by - 4.5);
        ctx.quadraticCurveTo(7, by - 5, 9, by + 1);
        ctx.quadraticCurveTo(0, by + 3, -9, by + 1);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba16([206, 224, 244], 0.55 * a);
        ctx.beginPath();
        ctx.ellipse(0, by + 0.5, 8, 2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba16([248, 252, 255], 0.85 * a);
        [[-4.5, -3], [4, -5], [-1, -8], [2.5, -1]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.ellipse(sx, by + sy, 1.8, 1.2, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.snowCapAmt > 0.02) {
        ctx.fillStyle = rgba16([252, 254, 255], 0.95 * p.snowCapAmt);
        const by = TUFT_BASE_Y + bob;
        ctx.beginPath();
        ctx.ellipse(0, by + 0.5, 9 * (0.6 + 0.4 * p.snowCapAmt), 2.6, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba16(p.light, p.lightAmt));
        lg.addColorStop(1, rgba16(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt24(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw17(season) {
    return (ctx) => paint24(ctx, SP24[season], 0);
  }
  function anim17(season) {
    return (ctx, t) => {
      const bob = bobAt24(t);
      const bendBase = season === "Summer" ? 1.7 : season === "Winter" ? 1.2 : season === "Autumn" ? 1.5 : 1.9;
      const bend = bendBase * (1 - Math.cos(t * 1.5)) * 0.5 + bendBase * 0.35 * Math.sin(t * 0.9);
      const phase = t * 1.5;
      paint24(ctx, SP24[season], bob, bend, phase);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -4 + bob + Math.sin(t * 1.1) * 1.4;
          ctx.beginPath();
          ctx.arc(-3, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const gy = lerp26(15 + bob, -18 + bob, prog);
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.ellipse(-4 + prog * 3, gy, 1.2, 2.4, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = `rgba(255,230,170,${s})`;
          ctx.beginPath();
          ctx.ellipse(2, -12 + bob, 6, 4.5, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phaseF, r]) => {
            const prog = ((t / 3 + phaseF) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phaseF * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, -2 + bob, 7, 5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition21(fromIdx) {
    const from = SP24[SEASON_NAMES[fromIdx]];
    const to = SP24[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother24(clamp0125(pp));
      paint24(ctx, lerpP24(from, to, k), 0);
    };
  }
  var springToSummer20 = makeTransition21(0);
  var summerToAutumn20 = makeTransition21(1);
  var autumnToWinter20 = makeTransition21(2);
  var VARIANTS26 = {
    Spring: { draw: draw17("Spring"), anim: anim17("Spring") },
    Summer: { draw: draw17("Summer"), anim: anim17("Summer") },
    Autumn: { draw: draw17("Autumn"), anim: anim17("Autumn") },
    Winter: { draw: draw17("Winter"), anim: anim17("Winter") }
  };
  var TRANSITIONS26 = {
    0: springToSummer20,
    1: summerToAutumn20,
    2: autumnToWinter20
  };

  // src/textures/seasonal/grass/meadow.ts
  function clamp0126(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother25 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp27(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB20(a, b, t) {
    return [lerp27(a[0], b[0], t), lerp27(a[1], b[1], t), lerp27(a[2], b[2], t)];
  }
  function rgb25(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba17(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0126(a)})`;
  }
  function lerpP25(a, b, t) {
    return {
      bladeLight: lerpRGB20(a.bladeLight, b.bladeLight, t),
      bladeMid: lerpRGB20(a.bladeMid, b.bladeMid, t),
      bladeDark: lerpRGB20(a.bladeDark, b.bladeDark, t),
      stalk: lerpRGB20(a.stalk, b.stalk, t),
      seedHead: lerpRGB20(a.seedHead, b.seedHead, t),
      seedHeadDark: lerpRGB20(a.seedHeadDark, b.seedHeadDark, t),
      padGrass: lerpRGB20(a.padGrass, b.padGrass, t),
      padDark: lerpRGB20(a.padDark, b.padDark, t),
      soil: lerpRGB20(a.soil, b.soil, t),
      outline: lerpRGB20(a.outline, b.outline, t),
      light: lerpRGB20(a.light, b.light, t),
      lightAmt: lerp27(a.lightAmt, b.lightAmt, t),
      droop: lerp27(a.droop, b.droop, t),
      toSeed: lerp27(a.toSeed, b.toSeed, t),
      gloss: lerp27(a.gloss, b.gloss, t),
      frostAmt: lerp27(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp27(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp27(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp27(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp27(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP17(p) {
    return {
      ...p,
      lightAmt: clamp0126(p.lightAmt),
      droop: clamp0126(p.droop),
      toSeed: clamp0126(p.toSeed),
      gloss: clamp0126(p.gloss),
      frostAmt: clamp0126(p.frostAmt),
      snowCapAmt: clamp0126(p.snowCapAmt),
      padSnowAmt: clamp0126(p.padSnowAmt),
      blossomAmt: clamp0126(p.blossomAmt),
      fallenLeafAmt: clamp0126(p.fallenLeafAmt)
    };
  }
  var SP25 = {
    // Spring — fresh green tuft, young pale-green upright stalks; dewy lime pad +
    // blossom. Cool-bright light, stalks crisp and upright.
    Spring: {
      bladeLight: [156, 214, 104],
      bladeMid: [104, 176, 70],
      bladeDark: [58, 118, 48],
      stalk: [168, 206, 118],
      seedHead: [196, 224, 140],
      seedHeadDark: [142, 182, 96],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [40, 64, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      droop: 0.05,
      toSeed: 0,
      gloss: 0.2,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full soft mid-green tuft, plump green seed heads (PEAK). Saturated
    // mid-green pad, warm light, soft shimmer on the blades.
    Summer: {
      bladeLight: [148, 206, 88],
      bladeMid: [86, 168, 60],
      bladeDark: [46, 112, 44],
      stalk: [120, 178, 78],
      seedHead: [150, 196, 96],
      seedHeadDark: [98, 150, 64],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [34, 70, 28],
      light: [255, 240, 206],
      lightAmt: 0.18,
      droop: 0.1,
      toSeed: 0,
      gloss: 0.85,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — amber/straw tuft, seed heads drooping and going to seed; olive-tan
    // pad, a couple fallen leaves. Low amber light.
    Autumn: {
      bladeLight: [214, 178, 96],
      bladeMid: [180, 140, 64],
      bladeDark: [124, 92, 42],
      stalk: [196, 162, 90],
      seedHead: [216, 188, 118],
      seedHeadDark: [160, 128, 72],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [78, 52, 24],
      light: [248, 210, 150],
      lightAmt: 0.2,
      droop: 0.95,
      toSeed: 0.85,
      gloss: 0.3,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — bleached straw stalks, snow caps on the seed heads, snow blanket on
    // the pad, cool light. Stalks still standing, frost dusting.
    Winter: {
      bladeLight: [196, 188, 158],
      bladeMid: [158, 150, 122],
      bladeDark: [110, 104, 88],
      stalk: [186, 178, 150],
      seedHead: [204, 196, 168],
      seedHeadDark: [150, 144, 122],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [60, 56, 56],
      light: [206, 226, 252],
      lightAmt: 0.3,
      droop: 0.35,
      toSeed: 0.25,
      gloss: 0.18,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var TUFT_BASE_Y2 = 18;
  var TUFT_TOP = -6;
  var STALK_TOP2 = -22;
  var BLADES2 = [
    [-12, -16, -2, 0],
    [-8, -12, -7, 0],
    [-4.5, -7, -10, 1],
    [-2, -3.5, TUFT_TOP, 1],
    [0.5, 1, TUFT_TOP - 1, 1],
    [3, 5, -9.5, 1],
    [6.5, 10, -6.5, 0],
    [10, 14, -2.5, 0],
    [13, 17, -1, 0]
  ];
  var STALKS = [
    [-4.2, -5.6, 0],
    [-1.3, -1, 1],
    [1.6, 2.4, 1],
    [4.4, 6.2, 0]
  ];
  function drawStalk(ctx, p, baseX, headX, layer, bob, sway) {
    const baseY = TUFT_BASE_Y2 - 4 + bob;
    const droopX = headX + p.droop * 5 * Math.sign(headX || 1);
    const headY = STALK_TOP2 + bob + p.droop * 6 + Math.abs(baseX) * 0.4;
    const tipX = droopX + sway;
    const midX = lerp27(baseX, tipX, 0.5) + sway * 0.5 + p.droop * 2 * Math.sign(headX || 1);
    const midY = lerp27(baseY, headY, 0.5);
    const stalkCol = layer === 0 ? rgba17(p.stalk, 0.9) : rgb25(p.stalk);
    ctx.strokeStyle = rgba17(p.outline, 0.85);
    ctx.lineWidth = 2.2;
    ctx.beginPath();
    ctx.moveTo(baseX, baseY);
    ctx.quadraticCurveTo(midX, midY, tipX, headY);
    ctx.stroke();
    ctx.strokeStyle = stalkCol;
    ctx.lineWidth = 1.2;
    ctx.beginPath();
    ctx.moveTo(baseX, baseY);
    ctx.quadraticCurveTo(midX, midY, tipX, headY);
    ctx.stroke();
    const tilt = p.droop * 0.5 * Math.sign(headX || 1);
    ctx.save();
    ctx.translate(tipX, headY - 1.6);
    ctx.rotate(tilt);
    ctx.fillStyle = rgb25(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 2.6, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb25(p.seedHead);
    ctx.beginPath();
    ctx.ellipse(0, 0, 2, 3.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgba17(p.seedHeadDark, 0.7);
    ctx.beginPath();
    ctx.ellipse(0.7, 0.4, 1.1, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgba17(p.bladeLight, 0.5);
    ctx.beginPath();
    ctx.ellipse(-0.7, -1, 0.7, 1.8, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint25(ctx, raw, bob) {
    const p = clampP17(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba17(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb25(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb25(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb25(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba17([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba17([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba17([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba17([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba17(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba17([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba17(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba17([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb25(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, TUFT_BASE_Y2 + bob + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba17(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, TUFT_BASE_Y2 + bob + 2, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      const baseY = TUFT_BASE_Y2 + bob;
      const drawBladeLayer = (wantLayer) => {
        BLADES2.forEach(([bx, tipX, tipY, layer]) => {
          if (layer !== wantLayer) return;
          const ty = tipY + bob;
          const midX = lerp27(bx, tipX, 0.5);
          const midY = lerp27(baseY, ty, 0.45);
          ctx.strokeStyle = layer === 0 ? rgb25(p.bladeDark) : rgba17(p.outline, 0.8);
          ctx.lineWidth = layer === 0 ? 3 : 3.4;
          ctx.beginPath();
          ctx.moveTo(bx, baseY);
          ctx.quadraticCurveTo(midX, midY, tipX, ty);
          ctx.stroke();
          ctx.strokeStyle = layer === 0 ? rgba17(p.bladeMid, 0.95) : rgb25(p.bladeMid);
          ctx.lineWidth = layer === 0 ? 1.6 : 1.9;
          ctx.beginPath();
          ctx.moveTo(bx, baseY);
          ctx.quadraticCurveTo(midX, midY, tipX, ty);
          ctx.stroke();
          if (layer === 1) {
            ctx.strokeStyle = rgba17(p.bladeLight, 0.7);
            ctx.lineWidth = 0.9;
            ctx.beginPath();
            ctx.moveTo(bx - 0.6, baseY - 1);
            ctx.quadraticCurveTo(midX - 0.8, midY, tipX - 0.5, ty);
            ctx.stroke();
          }
        });
      };
      drawBladeLayer(0);
      STALKS.forEach(([baseX, headX, layer]) => {
        drawStalk(ctx, p, baseX, headX, layer, bob, 0);
      });
      drawBladeLayer(1);
      if (p.toSeed > 0.02) {
        ctx.fillStyle = rgba17([238, 226, 188], 0.5 * p.toSeed);
        const fluff = [
          [-6, STALK_TOP2 + 4],
          [-1, STALK_TOP2 + 6],
          [3, STALK_TOP2 + 3],
          [7, STALK_TOP2 + 7]
        ];
        fluff.forEach(([fx, fy]) => {
          ctx.beginPath();
          ctx.arc(fx, fy + bob + p.droop * 5, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba17([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-12, -2],
          [-7, -7],
          [-3, TUFT_TOP],
          [1, TUFT_TOP - 1],
          [5, -9],
          [10, -6],
          [14, -2],
          [-5, STALK_TOP2 + 2],
          [2, STALK_TOP2 + 2]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy + bob, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba17([246, 251, 255], 0.95 * a);
        STALKS.forEach(([baseX, headX]) => {
          const droopX = headX + p.droop * 5 * Math.sign(headX || 1);
          const headY = STALK_TOP2 + bob + p.droop * 6 + Math.abs(baseX) * 0.4;
          ctx.beginPath();
          ctx.ellipse(droopX, headY - 3.6, 2.4, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.fillStyle = rgba17([238, 246, 255], 0.6 * a);
        ctx.beginPath();
        ctx.ellipse(0, TUFT_TOP + bob + 2, 6, 2.2, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.gloss > 0.02) {
        ctx.strokeStyle = rgba17([255, 255, 255], 0.1 + 0.22 * p.gloss);
        ctx.lineWidth = 0.8;
        [-7, -2, 4].forEach((bx) => {
          ctx.beginPath();
          ctx.moveTo(bx, baseY - 3);
          ctx.lineTo(bx - 1.5, lerp27(baseY, TUFT_TOP + bob, 0.6));
          ctx.stroke();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba17(p.light, p.lightAmt));
        lg.addColorStop(1, rgba17(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt25(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw18(season) {
    return (ctx) => paint25(ctx, SP25[season], 0);
  }
  function anim18(season) {
    const p = SP25[season];
    return (ctx, t) => {
      const bob = bobAt25(t);
      paint25(ctx, p, bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const cp = clampP17(p);
        STALKS.forEach(([baseX, headX, layer], i) => {
          const sway = Math.sin(t * 1.3 + i * 1.1) * 1.4 + Math.sin(t * 0.7) * 0.5;
          drawStalk(ctx, cp, baseX, headX, layer, bob, sway);
        });
        if (season === "Spring") {
          const g = 0.2 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -4 + bob + Math.sin(t * 1.1) * 2;
          ctx.beginPath();
          ctx.arc(-4, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.35 % 1;
          const gy = lerp27(TUFT_BASE_Y2 + bob - 2, TUFT_TOP + bob, prog);
          ctx.fillStyle = "rgba(255,255,255,0.35)";
          ctx.beginPath();
          ctx.ellipse(-3, gy, 2.4, 1.2, -0.3, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const prog = (t / 4 % 1 + 1) % 1;
          const sx = 4 + Math.sin(prog * Math.PI * 2) * 4;
          const sy = STALK_TOP2 + 6 + prog * 24;
          ctx.globalAlpha = 0.55 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "rgba(216,188,118,1)";
          ctx.beginPath();
          ctx.ellipse(sx, sy, 1.1, 1.8, prog * 3, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 2 + bob, 7, 5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition22(fromIdx) {
    const from = SP25[SEASON_NAMES[fromIdx]];
    const to = SP25[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother25(clamp0126(pp));
      paint25(ctx, lerpP25(from, to, k), 0);
    };
  }
  var springToSummer21 = makeTransition22(0);
  var summerToAutumn21 = makeTransition22(1);
  var autumnToWinter21 = makeTransition22(2);
  var VARIANTS27 = {
    Spring: { draw: draw18("Spring"), anim: anim18("Spring") },
    Summer: { draw: draw18("Summer"), anim: anim18("Summer") },
    Autumn: { draw: draw18("Autumn"), anim: anim18("Autumn") },
    Winter: { draw: draw18("Winter"), anim: anim18("Winter") }
  };
  var TRANSITIONS27 = {
    0: springToSummer21,
    1: summerToAutumn21,
    2: autumnToWinter21
  };

  // src/textures/seasonal/grass/spiky.ts
  function clamp0127(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother26 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp28(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB21(a, b, t) {
    return [lerp28(a[0], b[0], t), lerp28(a[1], b[1], t), lerp28(a[2], b[2], t)];
  }
  function rgb26(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba18(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0127(a)})`;
  }
  function lerpP26(a, b, t) {
    return {
      bladeLight: lerpRGB21(a.bladeLight, b.bladeLight, t),
      bladeMid: lerpRGB21(a.bladeMid, b.bladeMid, t),
      bladeDark: lerpRGB21(a.bladeDark, b.bladeDark, t),
      tip: lerpRGB21(a.tip, b.tip, t),
      padGrass: lerpRGB21(a.padGrass, b.padGrass, t),
      padDark: lerpRGB21(a.padDark, b.padDark, t),
      soil: lerpRGB21(a.soil, b.soil, t),
      outline: lerpRGB21(a.outline, b.outline, t),
      light: lerpRGB21(a.light, b.light, t),
      lightAmt: lerp28(a.lightAmt, b.lightAmt, t),
      ripeness: lerp28(a.ripeness, b.ripeness, t),
      gloss: lerp28(a.gloss, b.gloss, t),
      tipRust: lerp28(a.tipRust, b.tipRust, t),
      frostAmt: lerp28(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp28(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp28(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp28(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp28(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP18(p) {
    return {
      ...p,
      lightAmt: clamp0127(p.lightAmt),
      ripeness: clamp0127(p.ripeness),
      gloss: clamp0127(p.gloss),
      tipRust: clamp0127(p.tipRust),
      frostAmt: clamp0127(p.frostAmt),
      snowCapAmt: clamp0127(p.snowCapAmt),
      padSnowAmt: clamp0127(p.padSnowAmt),
      blossomAmt: clamp0127(p.blossomAmt),
      fallenLeafAmt: clamp0127(p.fallenLeafAmt)
    };
  }
  var SP26 = {
    // Spring — vivid new green-olive spikes, crisp; dewy lime pad + blossom.
    Spring: {
      bladeLight: [186, 222, 96],
      bladeMid: [128, 178, 60],
      bladeDark: [78, 124, 42],
      tip: [206, 232, 130],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [44, 70, 30],
      light: [232, 244, 226],
      lightAmt: 0.16,
      ripeness: 0.4,
      gloss: 0.16,
      tipRust: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — hard saturated OLIVE spikes (PEAK); mid-green pad, warm light.
    Summer: {
      bladeLight: [168, 196, 70],
      bladeMid: [118, 150, 44],
      bladeDark: [74, 100, 30],
      tip: [188, 210, 92],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [50, 66, 24],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 1,
      gloss: 0.5,
      tipRust: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — rust-tipped, brittle, drier olive spikes; olive-tan pad + leaves.
    Autumn: {
      bladeLight: [156, 162, 74],
      bladeMid: [120, 120, 52],
      bladeDark: [82, 78, 36],
      tip: [180, 96, 40],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [62, 52, 26],
      light: [248, 210, 150],
      lightAmt: 0.2,
      ripeness: 0.7,
      gloss: 0.22,
      tipRust: 0.9,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — frost-stiffened spikes, frost on tips + snow caught at the base,
    // olive still visible; snowy pad, cool light. No white-out.
    Winter: {
      bladeLight: [134, 152, 96],
      bladeMid: [98, 116, 70],
      bladeDark: [66, 82, 56],
      tip: [222, 236, 248],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [54, 56, 56],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.5,
      gloss: 0.2,
      tipRust: 0,
      frostAmt: 0.8,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var CLUMP_BASE_Y = 16;
  var SPIKES = [
    [-1.18, 16, 2],
    // far left, low flare
    [-0.86, 24, 2.2],
    // left mid
    [-0.52, 32, 2.3],
    // left upper
    [-0.2, 38, 2.4],
    // centre-left (tallest)
    [0.06, 39, 2.4],
    // centre (tallest)
    [0.3, 37, 2.4],
    // centre-right
    [0.6, 31, 2.3],
    // right upper
    [0.92, 23, 2.2],
    // right mid
    [1.22, 15, 2]
    // far right, low flare
  ];
  function spikePath(ctx, ang, len, half, bob, lean) {
    const baseY = CLUMP_BASE_Y + bob;
    const bx = Math.sin(ang) * 3.2;
    const by = baseY;
    const tipX = Math.sin(ang) * len + lean;
    const tipY = by - Math.cos(ang) * len;
    const px = Math.cos(ang) * half;
    const py = Math.sin(ang) * half;
    ctx.beginPath();
    ctx.moveTo(bx - px, by - py);
    ctx.lineTo(bx + px, by + py);
    ctx.lineTo(tipX, tipY);
    ctx.closePath();
    return { tipX, tipY };
  }
  function paint26(ctx, raw, bob, quiver) {
    const p = clampP18(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba18(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb26(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb26(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb26(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba18([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba18([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba18([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba18([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba18(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba18([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba18(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba18([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb26(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, CLUMP_BASE_Y + bob + 1.5, 8, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba18(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, CLUMP_BASE_Y + bob + 2, 12, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      const order = SPIKES.map((_, i) => i).sort(
        (a, b) => Math.abs(SPIKES[b][0]) - Math.abs(SPIKES[a][0])
      );
      order.forEach((i) => {
        const [ang, len, half] = SPIKES[i];
        const lean = quiver ? quiver[i] : 0;
        const depth = 1 - Math.min(1, Math.abs(ang) / 1.25);
        spikePath(ctx, ang, len + 1.2, half + 0.9, bob, lean);
        ctx.fillStyle = rgb26(p.outline);
        ctx.fill();
        const body = lerpRGB21(p.bladeDark, p.bladeMid, depth);
        spikePath(ctx, ang, len, half, bob, lean);
        ctx.fillStyle = rgb26(body);
        ctx.fill();
        ctx.save();
        const { tipX, tipY } = spikePath(ctx, ang, len, half, bob, lean);
        ctx.clip();
        const lit = lerpRGB21(p.bladeMid, p.bladeLight, 0.7 * depth + 0.3);
        const grad = ctx.createLinearGradient(
          -half - 4,
          CLUMP_BASE_Y + bob,
          tipX + half + 4,
          tipY
        );
        grad.addColorStop(0, rgb26(lit));
        grad.addColorStop(0.5, rgb26(body));
        grad.addColorStop(1, rgb26(p.bladeDark));
        ctx.fillStyle = grad;
        ctx.globalAlpha = 0.85;
        ctx.fillRect(-24, -24, 48, 48);
        ctx.globalAlpha = 1;
        ctx.restore();
        ctx.strokeStyle = rgba18(p.bladeLight, 0.45 + 0.3 * depth);
        ctx.lineWidth = 0.9;
        const bx = Math.sin(ang) * 3.2;
        ctx.beginPath();
        ctx.moveTo(bx, CLUMP_BASE_Y + bob - 1);
        ctx.lineTo(tipX, tipY);
        ctx.stroke();
        if (p.gloss > 0.02) {
          ctx.strokeStyle = rgba18([255, 255, 255], 0.12 + 0.4 * p.gloss * depth);
          ctx.lineWidth = 0.7;
          const px = Math.cos(ang) * half;
          const py = Math.sin(ang) * half;
          ctx.beginPath();
          ctx.moveTo(bx - px * 0.8, CLUMP_BASE_Y + bob - py * 0.8);
          ctx.lineTo(tipX, tipY);
          ctx.stroke();
        }
        if (p.tipRust > 0.02) {
          ctx.fillStyle = rgba18(p.tip, 0.85 * p.tipRust);
          ctx.beginPath();
          ctx.ellipse(tipX, tipY + 1.4, 1.5, 2.4, ang, 0, Math.PI * 2);
          ctx.fill();
        }
        if (p.frostAmt > 0.02) {
          ctx.fillStyle = rgba18(p.tip, 0.7 * p.frostAmt);
          ctx.beginPath();
          ctx.ellipse(tipX, tipY + 1.2, 1.6, 2.6, ang, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgba18([255, 255, 255], 0.7 * p.frostAmt);
          ctx.beginPath();
          ctx.arc(tipX, tipY, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
      });
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba18([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.ellipse(0, CLUMP_BASE_Y + bob - 1.5, 9.5, 3.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba18([214, 230, 246], 0.55 * a);
        ctx.beginPath();
        ctx.ellipse(1.5, CLUMP_BASE_Y + bob, 8, 2.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba18([255, 255, 255], 0.85 * a);
        [[-7, CLUMP_BASE_Y + bob - 3], [6, CLUMP_BASE_Y + bob - 3.5], [-2, CLUMP_BASE_Y + bob - 4]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.ellipse(sx, sy, 1.6, 1.1, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba18(p.light, p.lightAmt));
        lg.addColorStop(1, rgba18(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt26(t, amp = 0.45, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function quiverAt(t) {
    return SPIKES.map((_, i) => {
      const w = 1.4;
      const phase = i * 0.7;
      return 0.35 * (1 - Math.cos(w * t + phase)) * 0.5 * Math.sign(SPIKES[i][0] || 0.1);
    });
  }
  function draw19(season) {
    return (ctx) => paint26(ctx, SP26[season], 0);
  }
  function anim19(season) {
    return (ctx, t) => {
      const bob = bobAt26(t);
      const quiver = quiverAt(t);
      paint26(ctx, SP26[season], bob, quiver);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -2 + bob + Math.sin(t * 1.1) * 1;
          ctx.beginPath();
          ctx.arc(-3, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const sy = lerp28(CLUMP_BASE_Y + bob, -22, prog);
          ctx.fillStyle = "rgba(255,255,255,0.55)";
          ctx.beginPath();
          ctx.ellipse(Math.sin(0.06) * sy * -0.04, sy, 1, 2, 0.04, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const sh = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 3.4));
          ctx.strokeStyle = `rgba(180,96,40,${sh})`;
          ctx.lineWidth = 0.8;
          SPIKES.forEach(([ang, len], i) => {
            const flick = Math.sin(t * 5 + i * 1.3) * 0.7;
            const tipX = Math.sin(ang) * len + quiver[i] + flick;
            const tipY = CLUMP_BASE_Y + bob - Math.cos(ang) * len;
            ctx.beginPath();
            ctx.moveTo(tipX - flick, tipY + 1.5);
            ctx.lineTo(tipX, tipY - 1.2);
            ctx.stroke();
          });
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-2, -2 + bob, 7, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition23(fromIdx) {
    const from = SP26[SEASON_NAMES[fromIdx]];
    const to = SP26[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother26(clamp0127(pp));
      paint26(ctx, lerpP26(from, to, k), 0);
    };
  }
  var springToSummer22 = makeTransition23(0);
  var summerToAutumn22 = makeTransition23(1);
  var autumnToWinter22 = makeTransition23(2);
  var VARIANTS28 = {
    Spring: { draw: draw19("Spring"), anim: anim19("Spring") },
    Summer: { draw: draw19("Summer"), anim: anim19("Summer") },
    Autumn: { draw: draw19("Autumn"), anim: anim19("Autumn") },
    Winter: { draw: draw19("Winter"), anim: anim19("Winter") }
  };
  var TRANSITIONS28 = {
    0: springToSummer22,
    1: summerToAutumn22,
    2: autumnToWinter22
  };

  // src/textures/seasonal/grass/clover.ts
  function clamp0128(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother27 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp29(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB22(a, b, t) {
    return [lerp29(a[0], b[0], t), lerp29(a[1], b[1], t), lerp29(a[2], b[2], t)];
  }
  function rgb27(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba19(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0128(a)})`;
  }
  function lerpP27(a, b, t) {
    return {
      leafLight: lerpRGB22(a.leafLight, b.leafLight, t),
      leafMid: lerpRGB22(a.leafMid, b.leafMid, t),
      leafDark: lerpRGB22(a.leafDark, b.leafDark, t),
      chevron: lerpRGB22(a.chevron, b.chevron, t),
      stem: lerpRGB22(a.stem, b.stem, t),
      bloomPetal: lerpRGB22(a.bloomPetal, b.bloomPetal, t),
      bloomCore: lerpRGB22(a.bloomCore, b.bloomCore, t),
      padGrass: lerpRGB22(a.padGrass, b.padGrass, t),
      padDark: lerpRGB22(a.padDark, b.padDark, t),
      soil: lerpRGB22(a.soil, b.soil, t),
      outline: lerpRGB22(a.outline, b.outline, t),
      light: lerpRGB22(a.light, b.light, t),
      lightAmt: lerp29(a.lightAmt, b.lightAmt, t),
      vitality: lerp29(a.vitality, b.vitality, t),
      edgeBrown: lerp29(a.edgeBrown, b.edgeBrown, t),
      flatten: lerp29(a.flatten, b.flatten, t),
      frostAmt: lerp29(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp29(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp29(a.padSnowAmt, b.padSnowAmt, t),
      bloomAmt: lerp29(a.bloomAmt, b.bloomAmt, t),
      blossomAmt: lerp29(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp29(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP19(p) {
    return {
      ...p,
      lightAmt: clamp0128(p.lightAmt),
      vitality: clamp0128(p.vitality),
      edgeBrown: clamp0128(p.edgeBrown),
      flatten: clamp0128(p.flatten),
      frostAmt: clamp0128(p.frostAmt),
      snowCapAmt: clamp0128(p.snowCapAmt),
      padSnowAmt: clamp0128(p.padSnowAmt),
      bloomAmt: clamp0128(p.bloomAmt),
      blossomAmt: clamp0128(p.blossomAmt),
      fallenLeafAmt: clamp0128(p.fallenLeafAmt)
    };
  }
  var SP27 = {
    // Spring — vivid fresh green trefoil leaves + fresh white-pink puffs; dewy
    // lime pad, cool-bright light, pad blossoms resting on the turf.
    Spring: {
      leafLight: [150, 214, 96],
      leafMid: [96, 178, 64],
      leafDark: [54, 120, 46],
      chevron: [206, 240, 168],
      stem: [108, 168, 60],
      bloomPetal: [255, 246, 250],
      bloomCore: [255, 224, 236],
      padGrass: [132, 210, 88],
      padDark: [74, 142, 60],
      soil: [120, 84, 48],
      outline: [40, 66, 32],
      light: [232, 246, 226],
      lightAmt: 0.16,
      vitality: 0.85,
      edgeBrown: 0,
      flatten: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      bloomAmt: 0.7,
      blossomAmt: 0.8,
      fallenLeafAmt: 0
    },
    // Summer — lush full green mound in FULL bloom (PEAK, most blossoms); richest
    // saturated palette, mid-green pad, warm strong light.
    Summer: {
      leafLight: [126, 206, 74],
      leafMid: [70, 162, 52],
      leafDark: [34, 100, 36],
      chevron: [188, 232, 150],
      stem: [82, 152, 50],
      bloomPetal: [255, 250, 252],
      bloomCore: [255, 206, 224],
      padGrass: [86, 172, 70],
      padDark: [44, 112, 48],
      soil: [126, 86, 48],
      outline: [26, 70, 28],
      light: [255, 242, 208],
      lightAmt: 0.18,
      vitality: 1,
      edgeBrown: 0,
      flatten: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      bloomAmt: 1,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — leaves browning at the edges, blossoms gone brown / to seed;
    // olive-tan pad with a couple of fallen leaves, low amber light.
    Autumn: {
      leafLight: [168, 178, 86],
      leafMid: [120, 134, 56],
      leafDark: [80, 86, 40],
      chevron: [206, 196, 132],
      stem: [134, 120, 56],
      bloomPetal: [206, 178, 132],
      bloomCore: [158, 122, 70],
      padGrass: [156, 152, 88],
      padDark: [106, 96, 52],
      soil: [120, 80, 44],
      outline: [62, 52, 26],
      light: [248, 210, 150],
      lightAmt: 0.2,
      vitality: 0.45,
      edgeBrown: 0.85,
      flatten: 0.15,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      bloomAmt: 0.45,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — mound flattened and frosted, a snow dusting over the leaves,
    // green-grey still visible underneath; snowy pad, cool blue-grey light.
    Winter: {
      leafLight: [120, 156, 122],
      leafMid: [84, 118, 92],
      leafDark: [54, 82, 66],
      chevron: [186, 206, 196],
      stem: [108, 124, 102],
      bloomPetal: [222, 230, 238],
      bloomCore: [196, 206, 218],
      padGrass: [178, 198, 216],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [52, 60, 60],
      light: [206, 226, 252],
      lightAmt: 0.3,
      vitality: 0.3,
      edgeBrown: 0.2,
      flatten: 0.7,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      bloomAmt: 0.3,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var MOUND_BASE_Y = 15.5;
  var MOUND_HALF = 16.5;
  var MOUND_TOP_Y = -7;
  function moundTopY(flatten) {
    return lerp29(MOUND_TOP_Y, MOUND_BASE_Y - 6, clamp0128(flatten));
  }
  function moundPath(ctx, bob, flatten) {
    const top = moundTopY(flatten) + bob;
    const base = MOUND_BASE_Y + bob;
    const h = MOUND_HALF;
    ctx.beginPath();
    ctx.moveTo(-h, base);
    ctx.quadraticCurveTo(-h, top + 3, -h * 0.5, top + 0.5);
    ctx.quadraticCurveTo(0, top - 2.5, h * 0.5, top + 0.5);
    ctx.quadraticCurveTo(h, top + 3, h, base);
    ctx.quadraticCurveTo(h * 0.5, base + 3, 0, base + 2.6);
    ctx.quadraticCurveTo(-h * 0.5, base + 3, -h, base);
    ctx.closePath();
  }
  var LEAVES = [
    [-11, 8, 0.95, -0.35],
    [-6, 0, 1.05, -0.15],
    [0, -3.5, 1.1, 0],
    [6, 0, 1.05, 0.15],
    [11, 8, 0.95, 0.35],
    [-9, 13, 0.8, -0.5],
    [9, 13, 0.8, 0.5],
    [0, 9, 0.9, 0]
  ];
  var BLOOMS = [
    [-7, 2, -8.5, -11],
    [4, -1, 6, -13.5],
    [10, 6, 12.5, -4]
  ];
  var PAD_BLOSSOMS = [[-13, 19], [12, 18.4], [-3, 21]];
  function trefoil(ctx, p, s) {
    const angles = [-Math.PI / 2 - 0.7, -Math.PI / 2 + 0.7, Math.PI / 2 + 0.05];
    angles.forEach((ang, idx) => {
      const dist = 2.6 * s;
      const lx = Math.cos(ang) * dist;
      const ly = Math.sin(ang) * dist;
      ctx.save();
      ctx.translate(lx, ly);
      ctx.rotate(ang + Math.PI / 2);
      const w = 2.6 * s;
      const hgt = 3.2 * s;
      ctx.beginPath();
      ctx.moveTo(0, hgt * 0.55);
      ctx.quadraticCurveTo(-w, hgt * 0.1, -w * 0.85, -hgt * 0.7);
      ctx.quadraticCurveTo(-w * 0.4, -hgt, 0, -hgt * 0.55);
      ctx.quadraticCurveTo(w * 0.4, -hgt, w * 0.85, -hgt * 0.7);
      ctx.quadraticCurveTo(w, hgt * 0.1, 0, hgt * 0.55);
      ctx.closePath();
      ctx.fillStyle = rgb27(p.leafMid);
      ctx.fill();
      if (p.edgeBrown > 0.02) {
        ctx.save();
        ctx.clip();
        ctx.strokeStyle = rgba19([150, 110, 50], 0.6 * p.edgeBrown);
        ctx.lineWidth = 1.4 * s;
        ctx.beginPath();
        ctx.moveTo(0, hgt * 0.55);
        ctx.quadraticCurveTo(-w, hgt * 0.1, -w * 0.85, -hgt * 0.7);
        ctx.quadraticCurveTo(-w * 0.4, -hgt, 0, -hgt * 0.55);
        ctx.quadraticCurveTo(w * 0.4, -hgt, w * 0.85, -hgt * 0.7);
        ctx.quadraticCurveTo(w, hgt * 0.1, 0, hgt * 0.55);
        ctx.stroke();
        ctx.restore();
      }
      ctx.fillStyle = rgba19(p.leafLight, 0.85);
      ctx.beginPath();
      ctx.ellipse(-w * 0.25, -hgt * 0.3, w * 0.5, hgt * 0.42, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba19(p.chevron, 0.85 * (idx === 2 ? 0.85 : 1));
      ctx.lineWidth = 0.9 * s;
      ctx.beginPath();
      ctx.moveTo(-w * 0.5, hgt * 0.05);
      ctx.lineTo(0, -hgt * 0.18);
      ctx.lineTo(w * 0.5, hgt * 0.05);
      ctx.stroke();
      ctx.strokeStyle = rgba19(p.outline, 0.7);
      ctx.lineWidth = 0.7 * s;
      ctx.beginPath();
      ctx.moveTo(0, hgt * 0.55);
      ctx.quadraticCurveTo(-w, hgt * 0.1, -w * 0.85, -hgt * 0.7);
      ctx.quadraticCurveTo(-w * 0.4, -hgt, 0, -hgt * 0.55);
      ctx.quadraticCurveTo(w * 0.4, -hgt, w * 0.85, -hgt * 0.7);
      ctx.quadraticCurveTo(w, hgt * 0.1, 0, hgt * 0.55);
      ctx.stroke();
      ctx.restore();
    });
  }
  function puffBloom(ctx, p, baseX, baseY, topX, topY, open, sway) {
    const o = clamp0128(open);
    if (o < 0.03) return;
    const tx = topX + sway;
    ctx.strokeStyle = rgb27(p.stem);
    ctx.lineWidth = 1.6;
    ctx.beginPath();
    ctx.moveTo(baseX, baseY);
    ctx.quadraticCurveTo((baseX + tx) / 2 + sway * 0.4, (baseY + topY) / 2, tx, topY + 1.5);
    ctx.stroke();
    const r = 3.4 * (0.55 + 0.45 * o);
    ctx.fillStyle = rgba19(p.outline, 0.45);
    ctx.beginPath();
    ctx.arc(tx, topY, r + 0.8, 0, Math.PI * 2);
    ctx.fill();
    const n = 9;
    for (let k = 0; k < n; k++) {
      const ang = k / n * Math.PI * 2 + 0.2;
      const px = tx + Math.cos(ang) * r * 0.78;
      const py = topY + Math.sin(ang) * r * 0.78;
      ctx.fillStyle = rgb27(k % 2 === 0 ? p.bloomPetal : p.bloomCore);
      ctx.beginPath();
      ctx.ellipse(px, py, r * 0.42, r * 0.3, ang, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb27(p.bloomCore);
    ctx.beginPath();
    ctx.arc(tx, topY, r * 0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgba19([255, 255, 255], 0.5 * o);
    ctx.beginPath();
    ctx.arc(tx - r * 0.3, topY - r * 0.3, r * 0.28, 0, Math.PI * 2);
    ctx.fill();
  }
  function paint27(ctx, raw, bob) {
    const p = clampP19(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba19(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb27(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb27(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb27(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba19([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba19([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba19([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba19([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        PAD_BLOSSOMS.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba19(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba19([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const fallen = [
          [-12, 19.6, -0.5, [168, 134, 64]],
          [12, 18.6, 0.7, [148, 96, 44]]
        ];
        fallen.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.scale(0.7, 0.7);
          ctx.globalAlpha = a;
          trefoil(ctx, { ...p, leafMid: col, leafLight: col, edgeBrown: 1 }, 0.85);
          ctx.restore();
        });
        ctx.globalAlpha = 1;
      }
      ctx.fillStyle = rgb27(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, MOUND_BASE_Y + bob + 2.5, 12, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba19(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(2.5, MOUND_BASE_Y + bob + 3, 14, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      moundPath(ctx, bob, p.flatten);
      ctx.fillStyle = rgb27(p.outline);
      ctx.fill();
      ctx.save();
      moundPath(ctx, bob, p.flatten);
      ctx.clip();
      const top = moundTopY(p.flatten) + bob;
      const base = MOUND_BASE_Y + bob;
      ctx.fillStyle = rgb27(p.leafDark);
      ctx.fillRect(-MOUND_HALF - 2, top - 6, (MOUND_HALF + 2) * 2, base - top + 12);
      const dome = ctx.createRadialGradient(-6, top + 2, 2, -2, lerp29(top, base, 0.4), 24);
      dome.addColorStop(0, rgb27(p.leafLight));
      dome.addColorStop(0.5, rgb27(p.leafMid));
      dome.addColorStop(1, rgb27(p.leafDark));
      ctx.fillStyle = dome;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(0, lerp29(top, base, 0.45), MOUND_HALF + 1, (base - top) * 0.7 + 4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      LEAVES.forEach(([cx, cy, sc, rot]) => {
        ctx.save();
        const ly = lerp29(cy, cy * 0.4 + 4, clamp0128(p.flatten));
        ctx.translate(cx, top + ly + 0.5);
        ctx.rotate(rot);
        trefoil(ctx, p, sc * lerp29(1, 0.86, clamp0128(p.flatten)));
        ctx.restore();
      });
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba19([210, 230, 250], 0.22 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(0, lerp29(top, base, 0.35), MOUND_HALF, (base - top) * 0.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba19([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-10, top + 4],
          [-4, top + 2],
          [3, top + 3],
          [9, top + 5],
          [-7, lerp29(top, base, 0.4)],
          [6, lerp29(top, base, 0.45)],
          [0, lerp29(top, base, 0.3)]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba19([246, 251, 255], 0.9 * a);
        ctx.beginPath();
        ctx.moveTo(-MOUND_HALF * 0.7, top + 4);
        ctx.quadraticCurveTo(-6, top - 2, 0, top - 1);
        ctx.quadraticCurveTo(6, top - 2, MOUND_HALF * 0.7, top + 4);
        ctx.quadraticCurveTo(MOUND_HALF * 0.4, top + 6.5, 0, top + 5);
        ctx.quadraticCurveTo(-MOUND_HALF * 0.4, top + 6.5, -MOUND_HALF * 0.7, top + 4);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba19([235, 244, 255], 0.7 * a);
        [[-8, top + 6], [5, top + 7], [-2, top + 9], [10, top + 8]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.ellipse(sx, sy, 2.4, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      BLOOMS.forEach(([bx, by, tx, ty], i) => {
        const open = p.bloomAmt * (i === 2 ? 0.85 : 1);
        puffBloom(ctx, p, bx, MOUND_BASE_Y + by * 0 + by + bob, tx, ty + bob, open, 0);
      });
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba19(p.light, p.lightAmt));
        lg.addColorStop(1, rgba19(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt27(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw20(season) {
    return (ctx) => paint27(ctx, SP27[season], 0);
  }
  function anim20(season) {
    return (ctx, t) => {
      const bob = bobAt27(t);
      paint27(ctx, SP27[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const p = clampP19(SP27[season]);
        const top = moundTopY(p.flatten) + bob;
        if (season === "Spring") {
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          const gy = top + 6 + bob + Math.sin(t * 1.1) * 1.4;
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(-5, gy, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.1 + 0.14 * (0.5 + 0.5 * Math.sin(t * 1.3));
          ctx.fillStyle = `rgba(255,255,236,${s})`;
          ctx.beginPath();
          ctx.ellipse(-3, top + 7, 7, 4.5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          const sway = Math.sin(t * 1.6) * 1.6;
          puffBloom(ctx, p, 4, -1 + bob, 6, -13.5 + bob, p.bloomAmt, sway);
        } else if (season === "Autumn") {
          const prog = t / 3 % 1;
          const fx = 8 - prog * 14 + Math.sin(prog * Math.PI * 2) * 3;
          const fy = top - 4 + prog * 22;
          ctx.save();
          ctx.translate(fx, fy);
          ctx.rotate(prog * Math.PI * 2);
          ctx.globalAlpha = 0.5 + 0.45 * Math.sin(prog * Math.PI);
          ctx.scale(0.6, 0.6);
          trefoil(ctx, { ...p, leafMid: [158, 116, 52], leafLight: [188, 150, 80], edgeBrown: 1 }, 0.8);
          ctx.restore();
          ctx.globalAlpha = 1;
        } else {
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 38;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, top + 6 + bob, 8, 4.5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition24(fromIdx) {
    const from = SP27[SEASON_NAMES[fromIdx]];
    const to = SP27[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother27(clamp0128(pp));
      paint27(ctx, lerpP27(from, to, k), 0);
    };
  }
  var springToSummer23 = makeTransition24(0);
  var summerToAutumn23 = makeTransition24(1);
  var autumnToWinter23 = makeTransition24(2);
  var VARIANTS29 = {
    Spring: { draw: draw20("Spring"), anim: anim20("Spring") },
    Summer: { draw: draw20("Summer"), anim: anim20("Summer") },
    Autumn: { draw: draw20("Autumn"), anim: anim20("Autumn") },
    Winter: { draw: draw20("Winter"), anim: anim20("Winter") }
  };
  var TRANSITIONS29 = {
    0: springToSummer23,
    1: summerToAutumn23,
    2: autumnToWinter23
  };

  // src/textures/seasonal/flower/pansy.ts
  var SP28 = {
    // Spring — fresh violet bloom; dewy lime pad; cool-bright light.
    Spring: {
      petalLow: [196, 142, 240],
      petalSide: [142, 72, 200],
      petalUp: [96, 46, 146],
      petalEdge: [61, 26, 99],
      blotch: [44, 18, 70],
      throat: [244, 210, 58],
      throatDark: [200, 154, 22],
      stem: [79, 154, 46],
      stemDark: [44, 80, 24],
      leaf: [90, 163, 54],
      leafDark: [47, 94, 28],
      pad: [126, 206, 96],
      padDark: [70, 142, 60],
      soil: [96, 66, 36],
      outline: [40, 40, 44],
      light: [220, 238, 255],
      // cool-bright
      lightAmt: 0.16,
      shadowAmt: 0.2,
      bloomOpen: 0.92,
      droop: 0,
      whiskerAmt: 0.82,
      glossAmt: 0.5,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.5,
      fallenLeafAmt: 0
    },
    // Summer — saturated violet bloom (PEAK), brightest throat; warm light.
    Summer: {
      petalLow: [204, 150, 248],
      petalSide: [150, 70, 212],
      petalUp: [100, 44, 156],
      petalEdge: [58, 24, 96],
      blotch: [42, 16, 66],
      throat: [250, 216, 58],
      throatDark: [206, 158, 22],
      stem: [86, 166, 50],
      stemDark: [44, 84, 26],
      leaf: [96, 172, 58],
      leafDark: [48, 98, 30],
      pad: [78, 168, 70],
      padDark: [44, 112, 46],
      soil: [88, 58, 30],
      outline: [38, 38, 42],
      light: [255, 246, 214],
      // warm
      lightAmt: 0.14,
      shadowAmt: 0.34,
      bloomOpen: 1,
      droop: 0,
      whiskerAmt: 1,
      glossAmt: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fading mauve bloom, browning edges, gentle droop; olive-tan pad.
    Autumn: {
      petalLow: [196, 160, 188],
      petalSide: [160, 114, 150],
      petalUp: [120, 80, 102],
      petalEdge: [122, 82, 48],
      // browning
      blotch: [86, 56, 50],
      throat: [222, 178, 64],
      throatDark: [168, 122, 36],
      stem: [120, 116, 60],
      stemDark: [78, 66, 28],
      leaf: [176, 158, 62],
      // yellowing
      leafDark: [107, 82, 16],
      pad: [140, 150, 84],
      padDark: [96, 102, 54],
      soil: [92, 60, 30],
      outline: [54, 46, 36],
      light: [255, 224, 168],
      // low amber
      lightAmt: 0.2,
      shadowAmt: 0.26,
      bloomOpen: 0.9,
      droop: 0.22,
      whiskerAmt: 0.4,
      glossAmt: 0,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7
    },
    // Winter — withered-but-present: greyed cool violet, frost-dusted, a snow
    // cap, drooping a touch; snow-blued pad. Still a readable pansy face.
    Winter: {
      petalLow: [196, 182, 206],
      petalSide: [150, 122, 168],
      petalUp: [110, 90, 128],
      petalEdge: [86, 78, 96],
      // cool grey
      blotch: [74, 66, 84],
      throat: [212, 192, 124],
      // muted, still reads as the throat
      throatDark: [160, 142, 86],
      stem: [110, 108, 96],
      stemDark: [70, 70, 64],
      leaf: [122, 134, 112],
      // greyed sage
      leafDark: [72, 84, 72],
      pad: [206, 220, 234],
      // snow-blued grass
      padDark: [150, 170, 192],
      soil: [110, 104, 96],
      outline: [62, 64, 74],
      light: [206, 224, 248],
      // cool blue-grey
      lightAmt: 0.22,
      shadowAmt: 0.16,
      bloomOpen: 0.82,
      droop: 0.3,
      whiskerAmt: 0.2,
      glossAmt: 0,
      frostAmt: 0.82,
      snowCapAmt: 0.5,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var clamp0129 = (x) => x < 0 || Number.isNaN(x) ? 0 : x > 1 ? 1 : x;
  var smoother28 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function rgb28(c, a = 1) {
    const r = Math.round(clamp0129(c[0] / 255) * 255);
    const g = Math.round(clamp0129(c[1] / 255) * 255);
    const b = Math.round(clamp0129(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0129(a)})`;
  }
  function mixRGB(a, b, k) {
    const t = clamp0129(k);
    return [a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t, a[2] + (b[2] - a[2]) * t];
  }
  function lerp30(a, b, k) {
    return a + (b - a) * clamp0129(k);
  }
  function lerpP28(a, b, k) {
    return {
      petalLow: mixRGB(a.petalLow, b.petalLow, k),
      petalSide: mixRGB(a.petalSide, b.petalSide, k),
      petalUp: mixRGB(a.petalUp, b.petalUp, k),
      petalEdge: mixRGB(a.petalEdge, b.petalEdge, k),
      blotch: mixRGB(a.blotch, b.blotch, k),
      throat: mixRGB(a.throat, b.throat, k),
      throatDark: mixRGB(a.throatDark, b.throatDark, k),
      stem: mixRGB(a.stem, b.stem, k),
      stemDark: mixRGB(a.stemDark, b.stemDark, k),
      leaf: mixRGB(a.leaf, b.leaf, k),
      leafDark: mixRGB(a.leafDark, b.leafDark, k),
      pad: mixRGB(a.pad, b.pad, k),
      padDark: mixRGB(a.padDark, b.padDark, k),
      soil: mixRGB(a.soil, b.soil, k),
      outline: mixRGB(a.outline, b.outline, k),
      light: mixRGB(a.light, b.light, k),
      lightAmt: lerp30(a.lightAmt, b.lightAmt, k),
      shadowAmt: lerp30(a.shadowAmt, b.shadowAmt, k),
      bloomOpen: lerp30(a.bloomOpen, b.bloomOpen, k),
      droop: lerp30(a.droop, b.droop, k),
      whiskerAmt: lerp30(a.whiskerAmt, b.whiskerAmt, k),
      glossAmt: lerp30(a.glossAmt, b.glossAmt, k),
      frostAmt: lerp30(a.frostAmt, b.frostAmt, k),
      snowCapAmt: lerp30(a.snowCapAmt, b.snowCapAmt, k),
      padSnowAmt: lerp30(a.padSnowAmt, b.padSnowAmt, k),
      blossomAmt: lerp30(a.blossomAmt, b.blossomAmt, k),
      fallenLeafAmt: lerp30(a.fallenLeafAmt, b.fallenLeafAmt, k)
    };
  }
  var BLOOM_CX = 0;
  var BLOOM_CY = -4;
  var STEM_BASE = [0, 17];
  function petal(ctx, angle, len, width, open, fill, edge) {
    if (open <= 1e-3) return;
    const L = len * (0.5 + 0.5 * open);
    const W = width * (0.45 + 0.55 * open);
    ctx.save();
    ctx.rotate(angle);
    ctx.fillStyle = fill;
    ctx.strokeStyle = edge;
    ctx.lineWidth = 1.1;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-W, -L * 0.55, -W * 0.55, -L);
    ctx.quadraticCurveTo(0, -L * 1.12, W * 0.55, -L);
    ctx.quadraticCurveTo(W, -L * 0.55, 0, 0);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
    ctx.restore();
  }
  function drawBloom(ctx, p, cx, cy) {
    const open = clamp0129(p.bloomOpen);
    ctx.save();
    ctx.translate(cx, cy);
    ctx.rotate(p.droop);
    petal(ctx, Math.PI * 0.18, 9, 6, open, rgb28(p.petalUp), rgb28(p.petalEdge));
    petal(ctx, -Math.PI * 0.18, 9, 6, open, rgb28(p.petalUp), rgb28(p.petalEdge));
    petal(ctx, Math.PI * 0.62, 10, 6.6, open, rgb28(p.petalSide), rgb28(p.petalEdge));
    petal(ctx, -Math.PI * 0.62, 10, 6.6, open, rgb28(p.petalSide), rgb28(p.petalEdge));
    petal(ctx, Math.PI, 11, 8, open, rgb28(p.petalLow), rgb28(p.petalEdge));
    ctx.fillStyle = rgb28(p.blotch, 0.9);
    ctx.beginPath();
    ctx.ellipse(0, 2.4, 3.4 * open, 3 * open, 0, 0, Math.PI * 2);
    ctx.fill();
    const wA = clamp0129(p.whiskerAmt) * Math.max(0, (open - 0.3) / 0.7);
    if (wA > 0.02) {
      ctx.strokeStyle = rgb28(p.blotch, wA);
      ctx.lineWidth = 0.9;
      ctx.lineCap = "round";
      for (const a of [-0.95, -0.5, 0, 0.5, 0.95]) {
        const ang = Math.PI - a;
        ctx.beginPath();
        ctx.moveTo(0, 1.4);
        ctx.lineTo(Math.cos(ang + Math.PI / 2) * 7, 1.4 + Math.sin(ang + Math.PI / 2) * 7);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    ctx.fillStyle = rgb28(p.throat);
    ctx.beginPath();
    ctx.arc(0, 0.6, 2.7, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb28(p.throatDark);
    ctx.beginPath();
    ctx.arc(0, 1.4, 1.4, 0, Math.PI * 2);
    ctx.fill();
    if (p.frostAmt > 0.01) {
      ctx.fillStyle = rgb28([236, 244, 255], 0.6 * p.frostAmt);
      for (const [fx, fy] of [[-4.5, -6], [-1, -8], [3.5, -6.5], [-6.5, -3], [5.5, -3.5]]) {
        ctx.beginPath();
        ctx.arc(fx, fy, 0.85, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.glossAmt > 0.01) {
      ctx.fillStyle = rgb28([255, 255, 255], 0.22 + p.glossAmt * 0.4);
      ctx.beginPath();
      ctx.arc(-2.6, -4.4, 1.2 + p.glossAmt * 0.8, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.snowCapAmt > 0.01) {
      ctx.fillStyle = rgb28([248, 252, 255], 0.9 * p.snowCapAmt);
      ctx.beginPath();
      ctx.moveTo(-4.6, -7);
      ctx.quadraticCurveTo(-0.5, -10.4, 4.4, -7.2);
      ctx.quadraticCurveTo(0, -8.4, -4.6, -7);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb28([214, 228, 244], 0.45 * p.snowCapAmt);
      ctx.beginPath();
      ctx.ellipse(-0.2, -7.4, 3.9, 1, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  }
  function drawStemAndLeaves(ctx, p) {
    ctx.lineCap = "round";
    ctx.strokeStyle = rgb28(p.stemDark);
    ctx.lineWidth = 4;
    ctx.beginPath();
    ctx.moveTo(STEM_BASE[0], STEM_BASE[1]);
    ctx.quadraticCurveTo(1.5, 6, BLOOM_CX, BLOOM_CY + 4);
    ctx.stroke();
    ctx.strokeStyle = rgb28(p.stem);
    ctx.lineWidth = 1.8;
    ctx.beginPath();
    ctx.moveTo(STEM_BASE[0], STEM_BASE[1]);
    ctx.quadraticCurveTo(1.5, 6, BLOOM_CX, BLOOM_CY + 4);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb28(p.leaf);
    ctx.strokeStyle = rgb28(p.leafDark);
    ctx.lineWidth = 1.3;
    ctx.beginPath();
    ctx.moveTo(0, 13);
    ctx.quadraticCurveTo(-11, 12, -15, 5);
    ctx.quadraticCurveTo(-7, 11, 0, 15);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
    ctx.beginPath();
    ctx.moveTo(0, 12);
    ctx.quadraticCurveTo(11, 11, 15, 4);
    ctx.quadraticCurveTo(7, 10, 0, 14);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
  }
  function paint28(ctx, p, bob) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.fillStyle = `rgba(0,0,0,${0.16 + p.shadowAmt * 0.26})`;
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 14, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      drawPad2(ctx, p);
      ctx.save();
      ctx.translate(0, bob);
      drawStemAndLeaves(ctx, p);
      drawBloom(ctx, p, BLOOM_CX, BLOOM_CY);
      ctx.restore();
      drawPadDressing2(ctx, p);
      if (p.lightAmt > 1e-3) {
        ctx.save();
        ctx.globalCompositeOperation = "soft-light";
        const lg = ctx.createRadialGradient(-10, -10, 2, 0, 0, 40);
        lg.addColorStop(0, rgb28(p.light, p.lightAmt));
        lg.addColorStop(1, rgb28(p.light, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.arc(0, 0, 40, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function drawPad2(ctx, p) {
    const cy = 19;
    ctx.fillStyle = rgb28(p.padDark);
    ctx.beginPath();
    ctx.ellipse(0, cy + 1.6, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb28(p.pad);
    ctx.beginPath();
    ctx.ellipse(0, cy, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb28(p.outline, 0.5);
    ctx.lineWidth = 1;
    ctx.stroke();
    ctx.fillStyle = rgb28(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, cy - 0.6, 5.5, 2.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb28(p.padDark);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    for (let i = 0; i < 13; i++) {
      const a = Math.PI * (0.08 + i / 12 * 0.84);
      const ex = Math.cos(a) * -18;
      const ey = cy + Math.sin(a) * 4.6;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + (i % 2 ? 1.2 : -1.2), ey - 2.4 - i % 3);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.strokeStyle = rgb28(mixRGB(p.pad, [255, 255, 255], 0.35), 0.7);
    ctx.lineWidth = 1.4;
    ctx.beginPath();
    ctx.ellipse(-2, cy - 0.6, 13, 3, 0, Math.PI * 1.05, Math.PI * 1.9);
    ctx.stroke();
  }
  function drawPadDressing2(ctx, p) {
    const cy = 19;
    if (p.blossomAmt > 1e-3) {
      const spots = [[-12, 18.5], [11, 19.5], [6, 21], [-7, 21]];
      spots.forEach(([sx, sy], i) => {
        if (i / spots.length < p.blossomAmt + 0.05) {
          ctx.fillStyle = `rgba(248,210,232,${0.9 * clamp0129(p.blossomAmt * 1.6)})`;
          ctx.beginPath();
          ctx.arc(sx, sy, 1.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(255,238,120,${0.9 * clamp0129(p.blossomAmt * 1.6)})`;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.5, 0, Math.PI * 2);
          ctx.fill();
        }
      });
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [184, 108, 44]],
        [10, 20.5, 0.7, [156, 76, 36]],
        [2, 22, 0.2, [196, 140, 56]]
      ];
      leaves.forEach(([lx, ly, rot, col], i) => {
        if (i / leaves.length < p.fallenLeafAmt + 0.05) {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgb28(col, clamp0129(p.fallenLeafAmt * 1.4));
          ctx.strokeStyle = rgb28(p.outline, 0.5 * clamp0129(p.fallenLeafAmt * 1.4));
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.8, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.stroke();
          ctx.restore();
        }
      });
    }
    if (p.padSnowAmt > 1e-3) {
      ctx.save();
      ctx.globalAlpha = clamp0129(p.padSnowAmt);
      const sg = ctx.createLinearGradient(0, cy - 4, 0, cy + 4);
      sg.addColorStop(0, "rgba(250,253,255,1)");
      sg.addColorStop(1, "rgba(214,228,244,1)");
      ctx.fillStyle = sg;
      ctx.beginPath();
      ctx.ellipse(-1, cy - 0.8, 16.5, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.9)";
      for (const [sx, sy] of [[-9, 18], [5, 19.5], [12, 18.5], [-2, 20.5]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.restore();
    }
  }
  function bobAt28(t, amp = 0.8, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function makeDraw6(season) {
    return (ctx) => paint28(ctx, SP28[season], 0);
  }
  function makeAnim2(season) {
    return (ctx, t) => {
      const bob = bobAt28(t);
      const sway = Math.sin(t * 1.2) * 0.7 * (1 - Math.cos(t * 1.2)) * 0.5;
      ctx.save();
      try {
        ctx.translate(sway, 0);
        paint28(ctx, SP28[season], bob);
        ctx.translate(0, bob);
        if (season === "Spring" || season === "Summer") {
          const glint = 0.2 + 0.32 * (0.5 + 0.5 * Math.sin(t * 2.3));
          ctx.fillStyle = `rgba(255,255,255,${glint})`;
          ctx.beginPath();
          ctx.arc(BLOOM_CX - 2.6, BLOOM_CY - 4.4, 1 + glint, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const k = t / 3.6 % 1;
          const px = BLOOM_CX + 6 + k * 9;
          const py = BLOOM_CY + 2 + k * 18;
          ctx.save();
          ctx.globalAlpha = Math.max(0, 0.95 * (1 - k));
          ctx.translate(px, py);
          ctx.rotate(k * 2.4);
          ctx.fillStyle = rgb28(SP28.Autumn.petalSide);
          ctx.strokeStyle = rgb28(SP28.Autumn.petalEdge);
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.moveTo(0, 0);
          ctx.quadraticCurveTo(-4, -3, -2, -7);
          ctx.quadraticCurveTo(0, -9, 2, -7);
          ctx.quadraticCurveTo(4, -3, 0, 0);
          ctx.closePath();
          ctx.fill();
          ctx.stroke();
          ctx.restore();
        } else {
          const span = 30;
          ctx.fillStyle = "rgba(255,255,255,0.9)";
          for (const [fx, ph] of [[-7, 0], [8, 0.55]]) {
            const prog = ((t / 3.2 + ph) % 1 + 1) % 1;
            const y = -20 + prog * span;
            const x = fx + Math.sin(prog * 6.28 + ph * 6) * 3;
            ctx.beginPath();
            ctx.arc(x, y, 1, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = `rgba(210,228,255,${0.1 + 0.08 * (0.5 + 0.5 * Math.sin(t * 0.8))})`;
          ctx.beginPath();
          ctx.ellipse(BLOOM_CX, BLOOM_CY, 11, 11, 0, 0, Math.PI * 2);
          ctx.fill();
        }
      } catch {
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition25(from, to) {
    return (ctx, pp) => {
      const k = smoother28(clamp0129(pp));
      paint28(ctx, lerpP28(SP28[from], SP28[to], k), 0);
    };
  }
  var VARIANTS30 = {
    Spring: { draw: makeDraw6("Spring"), anim: makeAnim2("Spring") },
    Summer: { draw: makeDraw6("Summer"), anim: makeAnim2("Summer") },
    Autumn: { draw: makeDraw6("Autumn"), anim: makeAnim2("Autumn") },
    Winter: { draw: makeDraw6("Winter"), anim: makeAnim2("Winter") }
  };
  var TRANSITIONS30 = {
    0: makeTransition25("Spring", "Summer"),
    1: makeTransition25("Summer", "Autumn"),
    2: makeTransition25("Autumn", "Winter")
  };

  // src/textures/seasonal/flower/heather.ts
  var SP29 = {
    Spring: {
      bell: [150, 196, 96],
      // green buds blushing
      bellHi: [206, 232, 150],
      foliage: [108, 176, 72],
      foliageDark: [52, 102, 38],
      stem: [120, 110, 70],
      pad: [126, 206, 96],
      padDark: [70, 142, 60],
      soil: [96, 66, 36],
      outline: [40, 54, 28],
      light: [220, 238, 255],
      // cool-bright
      lightAmt: 0.16,
      shadowAmt: 0.2,
      bloomAmount: 0.18,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.5,
      fallenLeafAmt: 0
    },
    Summer: {
      bell: [176, 58, 156],
      // vivid violet-magenta
      bellHi: [232, 142, 214],
      foliage: [70, 138, 50],
      foliageDark: [34, 78, 28],
      stem: [108, 92, 56],
      pad: [78, 168, 70],
      // saturated mid-green
      padDark: [44, 112, 46],
      soil: [88, 58, 30],
      outline: [38, 48, 26],
      light: [255, 246, 214],
      // warm
      lightAmt: 0.14,
      shadowAmt: 0.34,
      bloomAmount: 1,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    Autumn: {
      bell: [168, 92, 52],
      // rusty brown, papery
      bellHi: [206, 142, 92],
      foliage: [120, 124, 58],
      // olive
      foliageDark: [70, 70, 34],
      stem: [98, 78, 48],
      pad: [140, 150, 84],
      // olive-tan
      padDark: [96, 102, 54],
      soil: [92, 60, 30],
      outline: [48, 40, 24],
      light: [255, 224, 168],
      // low amber
      lightAmt: 0.2,
      shadowAmt: 0.26,
      bloomAmount: 0.82,
      // open but faded
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7
    },
    Winter: {
      bell: [150, 118, 120],
      // dried, dusty mauve-grey
      bellHi: [214, 200, 206],
      foliage: [96, 116, 96],
      // greyed sage
      foliageDark: [56, 72, 62],
      stem: [104, 96, 84],
      pad: [206, 220, 234],
      // snow-blued grass
      padDark: [150, 170, 192],
      soil: [110, 104, 96],
      outline: [70, 78, 88],
      light: [206, 224, 248],
      // cool blue-grey
      lightAmt: 0.22,
      shadowAmt: 0.16,
      bloomAmount: 0.5,
      frostAmt: 0.85,
      snowCapAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var clamp0130 = (x) => x < 0 ? 0 : x > 1 ? 1 : x;
  var smoother29 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function rgb29(c, a = 1) {
    const r = Math.round(clamp0130(c[0] / 255) * 255);
    const g = Math.round(clamp0130(c[1] / 255) * 255);
    const b = Math.round(clamp0130(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${a})`;
  }
  function mixRGB2(a, b, k) {
    const t = clamp0130(k);
    return [a[0] + (b[0] - a[0]) * t, a[1] + (b[1] - a[1]) * t, a[2] + (b[2] - a[2]) * t];
  }
  function lerp31(a, b, k) {
    return a + (b - a) * clamp0130(k);
  }
  function lerpP29(a, b, k) {
    return {
      bell: mixRGB2(a.bell, b.bell, k),
      bellHi: mixRGB2(a.bellHi, b.bellHi, k),
      foliage: mixRGB2(a.foliage, b.foliage, k),
      foliageDark: mixRGB2(a.foliageDark, b.foliageDark, k),
      stem: mixRGB2(a.stem, b.stem, k),
      pad: mixRGB2(a.pad, b.pad, k),
      padDark: mixRGB2(a.padDark, b.padDark, k),
      soil: mixRGB2(a.soil, b.soil, k),
      outline: mixRGB2(a.outline, b.outline, k),
      light: mixRGB2(a.light, b.light, k),
      lightAmt: lerp31(a.lightAmt, b.lightAmt, k),
      shadowAmt: lerp31(a.shadowAmt, b.shadowAmt, k),
      bloomAmount: lerp31(a.bloomAmount, b.bloomAmount, k),
      frostAmt: lerp31(a.frostAmt, b.frostAmt, k),
      snowCapAmt: lerp31(a.snowCapAmt, b.snowCapAmt, k),
      padSnowAmt: lerp31(a.padSnowAmt, b.padSnowAmt, k),
      blossomAmt: lerp31(a.blossomAmt, b.blossomAmt, k),
      fallenLeafAmt: lerp31(a.fallenLeafAmt, b.fallenLeafAmt, k)
    };
  }
  var STEM_BASE2 = [0, 17];
  var STEM_TOP = [-1, -18];
  var STEM_CTRL = [3, -2];
  function stemPointAt(u) {
    const inv = 1 - u;
    const x = inv * inv * STEM_BASE2[0] + 2 * inv * u * STEM_CTRL[0] + u * u * STEM_TOP[0];
    const y = inv * inv * STEM_BASE2[1] + 2 * inv * u * STEM_CTRL[1] + u * u * STEM_TOP[1];
    return [x, y];
  }
  function buildBells() {
    const list = [];
    for (let i = 0; i < 11; i++) {
      const u = 0.28 + i / 10 * 0.72;
      const [px, py] = stemPointAt(u);
      const side = i % 2 === 0 ? -1 : 1;
      const s = 0.7 + 0.35 * u;
      list.push({ x: px + side * (1.6 + 1.4 * (1 - u)), y: py, side, s });
    }
    for (let i = 0; i < 5; i++) {
      const u = 0.1 + i / 4 * 0.62;
      list.push({ x: -6.5 - u * 5.5, y: 6 - u * 16, side: -1, s: 0.66 + 0.2 * u });
    }
    for (let i = 0; i < 5; i++) {
      const u = 0.1 + i / 4 * 0.62;
      list.push({ x: 6.5 + u * 4.8, y: 8 - u * 15, side: 1, s: 0.66 + 0.2 * u });
    }
    return list;
  }
  var BELLS = buildBells();
  var LEAVES2 = (() => {
    const arr = [];
    for (let i = 0; i < 9; i++) {
      const u = 0.16 + i / 8 * 0.8;
      const [px, py] = stemPointAt(u);
      const side = i % 2 === 0 ? 1 : -1;
      arr.push([px, py, side]);
    }
    return arr;
  })();
  function paint29(ctx, p, bob) {
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = `rgba(0,0,0,${0.18 + p.shadowAmt * 0.28})`;
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 15, 4.2, 0, 0, Math.PI * 2);
      ctx.fill();
      drawPad3(ctx, p);
      drawSprig(ctx, p, bob);
      drawPadDressing3(ctx, p);
      if (p.lightAmt > 1e-3) {
        ctx.save();
        ctx.globalCompositeOperation = "soft-light";
        const lg = ctx.createRadialGradient(-10, -10, 2, 0, 0, 40);
        lg.addColorStop(0, rgb29(p.light, p.lightAmt));
        lg.addColorStop(1, rgb29(p.light, 0));
        ctx.fillStyle = lg;
        ctx.beginPath();
        ctx.arc(0, 0, 40, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      }
    } catch {
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function drawPad3(ctx, p) {
    const cy = 19;
    ctx.fillStyle = rgb29(p.padDark);
    ctx.beginPath();
    ctx.ellipse(0, cy + 1.6, 18, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb29(p.pad);
    ctx.beginPath();
    ctx.ellipse(0, cy, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb29(p.outline, 0.5);
    ctx.lineWidth = 1;
    ctx.stroke();
    ctx.strokeStyle = rgb29(p.padDark);
    ctx.lineWidth = 1.6;
    for (let i = 0; i < 13; i++) {
      const a = Math.PI * (0.08 + i / 12 * 0.84);
      const ex = Math.cos(a) * -18;
      const ey = cy + Math.sin(a) * 4.6;
      ctx.beginPath();
      ctx.moveTo(ex, ey);
      ctx.lineTo(ex + (i % 2 ? 1.2 : -1.2), ey - 2.4 - i % 3);
      ctx.stroke();
    }
    ctx.strokeStyle = rgb29(mixRGB2(p.pad, [255, 255, 255], 0.35), 0.7);
    ctx.lineWidth = 1.4;
    ctx.beginPath();
    ctx.ellipse(-2, cy - 0.6, 13, 3, 0, Math.PI * 1.05, Math.PI * 1.9);
    ctx.stroke();
  }
  function drawSprig(ctx, p, bob) {
    ctx.save();
    ctx.translate(0, bob);
    const drawStemPath = () => {
      ctx.beginPath();
      ctx.moveTo(STEM_BASE2[0], STEM_BASE2[1]);
      ctx.quadraticCurveTo(STEM_CTRL[0], STEM_CTRL[1], STEM_TOP[0], STEM_TOP[1]);
      ctx.moveTo(0, 9);
      ctx.quadraticCurveTo(-6, 4, -12, -9);
      ctx.moveTo(1, 11);
      ctx.quadraticCurveTo(7, 6, 11, -7);
    };
    ctx.strokeStyle = rgb29(p.outline);
    ctx.lineWidth = 3.2;
    drawStemPath();
    ctx.stroke();
    ctx.strokeStyle = rgb29(p.stem);
    ctx.lineWidth = 1.7;
    drawStemPath();
    ctx.stroke();
    LEAVES2.forEach(([lx, ly, side]) => {
      const tx = lx + side * 4.4;
      const ty = ly - 2.6;
      ctx.strokeStyle = rgb29(p.foliageDark);
      ctx.lineWidth = 2;
      ctx.beginPath();
      ctx.moveTo(lx, ly);
      ctx.lineTo(tx, ty);
      ctx.stroke();
      ctx.strokeStyle = rgb29(p.foliage);
      ctx.lineWidth = 1;
      ctx.beginPath();
      ctx.moveTo(lx, ly);
      ctx.lineTo(tx, ty);
      ctx.stroke();
    });
    const open = clamp0130(p.bloomAmount);
    BELLS.forEach((b) => {
      const w = (1.5 + open * 1) * b.s;
      const h = (2.4 + open * 0.6) * b.s;
      ctx.save();
      ctx.translate(b.x, b.y);
      ctx.rotate(b.side * 0.5 * (0.6 + open * 0.4));
      ctx.fillStyle = rgb29(p.bell);
      ctx.strokeStyle = rgb29(p.outline, 0.8);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(-w, -h * 0.3);
      ctx.quadraticCurveTo(-w * 1.05, -h, 0, -h);
      ctx.quadraticCurveTo(w * 1.05, -h, w, -h * 0.3);
      ctx.quadraticCurveTo(w * 0.7, h * 0.9, 0, h);
      ctx.quadraticCurveTo(-w * 0.7, h * 0.9, -w, -h * 0.3);
      ctx.closePath();
      ctx.fill();
      ctx.stroke();
      if (open > 0.5) {
        ctx.fillStyle = rgb29(mixRGB2(p.bell, p.outline, 0.45));
        ctx.beginPath();
        ctx.ellipse(0, h * 0.55, w * 0.5, h * 0.28, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.fillStyle = rgb29(p.bellHi, 0.85);
      ctx.beginPath();
      ctx.ellipse(-w * 0.35, -h * 0.45, w * 0.32, h * 0.3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    });
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = `rgba(232,242,255,${0.55 * p.frostAmt})`;
      BELLS.forEach((b, i) => {
        if (i % 2 === 0) {
          ctx.beginPath();
          ctx.arc(b.x - 0.6, b.y - 0.8, 0.9 * b.s, 0, Math.PI * 2);
          ctx.fill();
        }
      });
      if (p.snowCapAmt > 1e-3) {
        ctx.fillStyle = `rgba(248,252,255,${0.85 * p.snowCapAmt})`;
        const caps = [
          [STEM_TOP[0], STEM_TOP[1] + 1, 2.6],
          [-11, -8, 2],
          [10, -6, 2]
        ];
        caps.forEach(([cx, cyy, r]) => {
          ctx.beginPath();
          ctx.ellipse(cx, cyy, r, r * 0.6, 0, 0, Math.PI * 2);
          ctx.fill();
        });
      }
    }
    ctx.restore();
  }
  function drawPadDressing3(ctx, p) {
    const cy = 19;
    if (p.blossomAmt > 1e-3) {
      const spots = [
        [-12, 18.5],
        [11, 19.5],
        [6, 21],
        [-7, 21]
      ];
      spots.forEach(([sx, sy], i) => {
        if (i / spots.length < p.blossomAmt + 0.05) {
          ctx.fillStyle = `rgba(248,210,232,${0.9 * clamp0130(p.blossomAmt * 1.6)})`;
          ctx.beginPath();
          ctx.arc(sx, sy, 1.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(255,238,120,${0.9 * clamp0130(p.blossomAmt * 1.6)})`;
          ctx.beginPath();
          ctx.arc(sx, sy, 0.5, 0, Math.PI * 2);
          ctx.fill();
        }
      });
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [184, 108, 44]],
        [10, 20.5, 0.7, [156, 76, 36]],
        [2, 22, 0.2, [196, 140, 56]]
      ];
      leaves.forEach(([lx, ly, rot, col], i) => {
        if (i / leaves.length < p.fallenLeafAmt + 0.05) {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgb29(col, clamp0130(p.fallenLeafAmt * 1.4));
          ctx.strokeStyle = rgb29(p.outline, 0.5 * clamp0130(p.fallenLeafAmt * 1.4));
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.8, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.stroke();
          ctx.restore();
        }
      });
    }
    if (p.padSnowAmt > 1e-3) {
      ctx.save();
      ctx.globalAlpha = clamp0130(p.padSnowAmt);
      const sg = ctx.createLinearGradient(0, cy - 4, 0, cy + 4);
      sg.addColorStop(0, "rgba(250,253,255,1)");
      sg.addColorStop(1, "rgba(214,228,244,1)");
      ctx.fillStyle = sg;
      ctx.beginPath();
      ctx.ellipse(-1, cy - 0.8, 16.5, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.9)";
      const sparks = [
        [-9, 18],
        [5, 19.5],
        [12, 18.5],
        [-2, 20.5]
      ];
      sparks.forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.6, 0, Math.PI * 2);
        ctx.fill();
      });
      ctx.restore();
    }
  }
  function bobAt29(t, amp = 0.8, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function makeDraw7(season) {
    return (ctx) => paint29(ctx, SP29[season], 0);
  }
  function makeAnim3(season) {
    return (ctx, t) => {
      const bob = bobAt29(t);
      const sway = Math.sin(t * 1.1) * 0.6 * (1 - Math.cos(t * 1.1)) * 0.5;
      ctx.save();
      try {
        ctx.translate(sway, 0);
        paint29(ctx, SP29[season], bob);
        ctx.translate(0, bob);
        if (season === "Summer") {
          const k = t * 0.4 % 1;
          const gy = -14 + k * 26;
          ctx.fillStyle = `rgba(255,228,255,${0.18 + 0.12 * Math.sin(t * 3)})`;
          ctx.beginPath();
          ctx.ellipse(2, gy, 5, 2.4, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const k = t / 3.4 % 1;
          const fx = 13 + Math.sin(k * 6.28) * 2;
          const fy = -6 + k * 18;
          ctx.save();
          ctx.globalAlpha = 0.9 * (1 - k);
          ctx.fillStyle = rgb29(SP29.Autumn.bell);
          ctx.beginPath();
          ctx.ellipse(fx, fy, 1.3, 1.9, k * 4, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Winter") {
          const span = 30;
          const flakes = [
            [-7, 0],
            [8, 0.55]
          ];
          ctx.fillStyle = "rgba(255,255,255,0.9)";
          flakes.forEach(([fx, ph]) => {
            const prog = ((t / 3.2 + ph) % 1 + 1) % 1;
            const y = -20 + prog * span;
            const x = fx + Math.sin(prog * 6.28 + ph * 6) * 3;
            ctx.beginPath();
            ctx.arc(x, y, 1, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.fillStyle = `rgba(210,228,255,${0.1 + 0.08 * (0.5 + 0.5 * Math.sin(t * 0.8))})`;
          ctx.beginPath();
          ctx.ellipse(-2, 0, 10, 16, 0, 0, Math.PI * 2);
          ctx.fill();
        } else {
          const glint = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${glint})`;
          ctx.beginPath();
          ctx.arc(-3, 6, 1 + glint, 0, Math.PI * 2);
          ctx.fill();
        }
      } catch {
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition26(from, to) {
    return (ctx, pp) => {
      const k = smoother29(clamp0130(pp));
      paint29(ctx, lerpP29(SP29[from], SP29[to], k), 0);
    };
  }
  var VARIANTS31 = {
    Spring: { draw: makeDraw7("Spring"), anim: makeAnim3("Spring") },
    Summer: { draw: makeDraw7("Summer"), anim: makeAnim3("Summer") },
    Autumn: { draw: makeDraw7("Autumn"), anim: makeAnim3("Autumn") },
    Winter: { draw: makeDraw7("Winter"), anim: makeAnim3("Winter") }
  };
  var TRANSITIONS31 = {
    0: makeTransition26("Spring", "Summer"),
    1: makeTransition26("Summer", "Autumn"),
    2: makeTransition26("Autumn", "Winter")
  };

  // src/textures/seasonal/flower/waterLily.ts
  function clamp0131(x) {
    if (!Number.isFinite(x)) return 0;
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp35(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB23(a, b, t) {
    return [lerp35(a[0], b[0], t), lerp35(a[1], b[1], t), lerp35(a[2], b[2], t)];
  }
  function rgb30(c, alpha = 1) {
    const r = Math.round(clamp0131(c[0] / 255) * 255);
    const g = Math.round(clamp0131(c[1] / 255) * 255);
    const b = Math.round(clamp0131(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0131(alpha)})`;
  }
  var smoother30 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt30(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP30 = {
    // Spring — cool-bright; fresh blue water, new small pad, closed pink bud.
    Spring: {
      petal: [236, 168, 196],
      petalTip: [214, 110, 150],
      center: [232, 214, 120],
      pad: [108, 178, 86],
      padDark: [58, 114, 56],
      water: [122, 186, 224],
      waterDeep: [74, 134, 186],
      outline: [40, 58, 70],
      bloomOpen: 0.18,
      iceAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      padYellow: 0,
      gloss: 0.55,
      dressLeaf: 0,
      dressBlossom: 0.7
    },
    // Summer — peak; bright saturated blue, glossy green pad, open lotus.
    Summer: {
      petal: [250, 222, 232],
      petalTip: [240, 138, 178],
      center: [248, 212, 78],
      pad: [86, 172, 62],
      padDark: [44, 104, 44],
      water: [86, 174, 230],
      waterDeep: [44, 124, 196],
      outline: [34, 56, 64],
      bloomOpen: 0.95,
      iceAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      padYellow: 0,
      gloss: 0.9,
      dressLeaf: 0,
      dressBlossom: 0
    },
    // Autumn — low amber light; duller olive water, pad yellowing, bloom closing.
    Autumn: {
      petal: [224, 158, 172],
      petalTip: [196, 96, 110],
      center: [210, 162, 70],
      pad: [134, 156, 64],
      padDark: [92, 100, 44],
      water: [120, 150, 150],
      waterDeep: [78, 110, 116],
      outline: [48, 50, 46],
      bloomOpen: 0.34,
      iceAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      padYellow: 0.85,
      gloss: 0.4,
      dressLeaf: 0.85,
      dressBlossom: 0
    },
    // Winter — cool blue-grey; water frozen to pale ice, pad & bloom frosted.
    Winter: {
      petal: [222, 224, 234],
      petalTip: [196, 178, 200],
      center: [214, 210, 196],
      pad: [148, 176, 168],
      padDark: [96, 128, 130],
      water: [206, 224, 236],
      waterDeep: [168, 196, 216],
      outline: [70, 88, 104],
      bloomOpen: 0.3,
      iceAmt: 0.92,
      frostAmt: 0.85,
      padSnowAmt: 0.7,
      padYellow: 0,
      gloss: 0.5,
      dressLeaf: 0,
      dressBlossom: 0
    }
  };
  function lerpP30(a, b, t) {
    const s = clamp0131(t);
    return {
      petal: lerpRGB23(a.petal, b.petal, s),
      petalTip: lerpRGB23(a.petalTip, b.petalTip, s),
      center: lerpRGB23(a.center, b.center, s),
      pad: lerpRGB23(a.pad, b.pad, s),
      padDark: lerpRGB23(a.padDark, b.padDark, s),
      water: lerpRGB23(a.water, b.water, s),
      waterDeep: lerpRGB23(a.waterDeep, b.waterDeep, s),
      outline: lerpRGB23(a.outline, b.outline, s),
      bloomOpen: lerp35(a.bloomOpen, b.bloomOpen, s),
      iceAmt: lerp35(a.iceAmt, b.iceAmt, s),
      frostAmt: lerp35(a.frostAmt, b.frostAmt, s),
      padSnowAmt: lerp35(a.padSnowAmt, b.padSnowAmt, s),
      padYellow: lerp35(a.padYellow, b.padYellow, s),
      gloss: lerp35(a.gloss, b.gloss, s),
      dressLeaf: lerp35(a.dressLeaf, b.dressLeaf, s),
      dressBlossom: lerp35(a.dressBlossom, b.dressBlossom, s)
    };
  }
  var NO_MICRO = {
    ripple: 0,
    petalGlint: 0,
    iceSheen: 0,
    flakeY: -1,
    flakeX: 0,
    leafDrift: 0,
    dewPulse: 0
  };
  function paint30(ctx, raw, bob, micro = NO_MICRO) {
    const p = {
      ...raw,
      bloomOpen: clamp0131(raw.bloomOpen),
      iceAmt: clamp0131(raw.iceAmt),
      frostAmt: clamp0131(raw.frostAmt),
      padSnowAmt: clamp0131(raw.padSnowAmt),
      padYellow: clamp0131(raw.padYellow),
      gloss: clamp0131(raw.gloss),
      dressLeaf: clamp0131(raw.dressLeaf),
      dressBlossom: clamp0131(raw.dressBlossom)
    };
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgb30([0, 0, 0], 0.2);
      ctx.beginPath();
      ctx.ellipse(2.5, 22.5, 16, 4.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const poolCx = 0;
      const poolCy = 19;
      const poolRx = 18;
      const poolRy = 5.6;
      ctx.fillStyle = rgb30(p.outline, 0.55);
      ctx.beginPath();
      ctx.ellipse(poolCx, poolCy + 0.6, poolRx + 1.1, poolRy + 1.1, 0, 0, Math.PI * 2);
      ctx.fill();
      const wg = ctx.createLinearGradient(0, poolCy - poolRy, 0, poolCy + poolRy);
      wg.addColorStop(0, rgb30(p.water));
      wg.addColorStop(1, rgb30(p.waterDeep));
      ctx.fillStyle = wg;
      ctx.beginPath();
      ctx.ellipse(poolCx, poolCy, poolRx, poolRy, 0, 0, Math.PI * 2);
      ctx.fill();
      if (micro.ripple > 0) {
        ctx.save();
        ctx.beginPath();
        ctx.ellipse(poolCx, poolCy, poolRx, poolRy, 0, 0, Math.PI * 2);
        ctx.clip();
        const a = 0.16 * micro.ripple;
        ctx.strokeStyle = rgb30([255, 255, 255], a);
        ctx.lineWidth = 0.9;
        for (let i = 0; i < 3; i++) {
          const ry = 1.4 + i * 1.6 + micro.ripple * 0.8;
          ctx.beginPath();
          ctx.ellipse(-5 + i * 4, poolCy - 1 + i * 1.2, 7 - i, ry * 0.5, 0, 0, Math.PI);
          ctx.stroke();
        }
        ctx.restore();
      }
      if (p.iceAmt > 0.02) {
        const ig = ctx.createLinearGradient(0, poolCy - poolRy, 0, poolCy + poolRy);
        ig.addColorStop(0, rgb30([238, 248, 255], 0.9 * p.iceAmt));
        ig.addColorStop(1, rgb30([196, 220, 236], 0.85 * p.iceAmt));
        ctx.fillStyle = ig;
        ctx.beginPath();
        ctx.ellipse(poolCx, poolCy, poolRx, poolRy, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb30([150, 184, 208], 0.5 * p.iceAmt);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-12, poolCy + 1);
        ctx.lineTo(-3, poolCy - 1.5);
        ctx.lineTo(6, poolCy + 1.6);
        ctx.lineTo(14, poolCy - 0.8);
        ctx.stroke();
        const sheen2 = 0.18 + micro.iceSheen * 0.28;
        ctx.fillStyle = rgb30([255, 255, 255], sheen2 * p.iceAmt);
        ctx.beginPath();
        ctx.ellipse(-4, poolCy - 1.6, 11, 1.8, -0.08, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb30([255, 255, 255], 0.8 * p.frostAmt);
        const sparkles = [
          [-13, 20],
          [-6, 17.5],
          [3, 21],
          [11, 18.5],
          [15, 20.5],
          [-1, 18]
        ];
        for (const [sx, sy] of sparkles) {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        }
      } else {
        ctx.fillStyle = rgb30([255, 255, 255], 0.16 + 0.22 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(-5, poolCy - 2, 9, 1.5, -0.06, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.dressLeaf > 0.02) {
        ctx.save();
        ctx.globalAlpha = p.dressLeaf;
        const lx = 11 + micro.leafDrift;
        const ly = 21;
        ctx.translate(lx, ly);
        ctx.rotate(0.5 + micro.leafDrift * 0.03);
        ctx.fillStyle = rgb30([186, 116, 48]);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.6, 2.1, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb30([120, 70, 28]);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-3.4, 0);
        ctx.lineTo(3.4, 0);
        ctx.stroke();
        ctx.restore();
        ctx.globalAlpha = 1;
      }
      ctx.translate(0, -bob);
      const padCx = 0;
      const padCy = 16.5;
      const padRx = 14.5;
      const padRy = 6.2;
      const padCol = lerpRGB23(p.pad, [196, 182, 86], p.padYellow * 0.55);
      const padDarkCol = lerpRGB23(p.padDark, [148, 132, 58], p.padYellow * 0.55);
      ctx.fillStyle = rgb30(p.outline, 0.7);
      ctx.beginPath();
      ctx.ellipse(padCx, padCy + 0.8, padRx + 1, padRy + 1, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb30(padDarkCol);
      ctx.beginPath();
      ctx.ellipse(padCx, padCy, padRx, padRy, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb30(padCol);
      ctx.beginPath();
      ctx.ellipse(padCx - 0.6, padCy - 0.9, padRx - 1.2, padRy - 1.4, 0, 0, Math.PI * 2);
      ctx.fill();
      if (p.padYellow > 0.04) {
        ctx.strokeStyle = rgb30([206, 184, 78], 0.6 * p.padYellow);
        ctx.lineWidth = 2.2;
        ctx.beginPath();
        ctx.ellipse(padCx, padCy, padRx - 1.4, padRy - 1.2, 0, 0, Math.PI * 2);
        ctx.stroke();
      }
      ctx.fillStyle = rgb30(p.waterDeep);
      ctx.save();
      ctx.beginPath();
      ctx.moveTo(padCx + 1, padCy);
      ctx.lineTo(padCx + padRx - 1, padCy + 1.4);
      ctx.lineTo(padCx + padRx - 4, padCy + 3.2);
      ctx.closePath();
      ctx.fill();
      ctx.restore();
      ctx.strokeStyle = rgb30(padDarkCol, 0.7);
      ctx.lineWidth = 0.7;
      for (let i = 0; i < 7; i++) {
        const ang = i / 7 * Math.PI * 2 + 0.25;
        if (ang > 0 && ang < 0.5) continue;
        ctx.beginPath();
        ctx.moveTo(padCx, padCy - 0.6);
        ctx.lineTo(
          padCx + Math.cos(ang) * (padRx - 2.5),
          padCy - 0.6 + Math.sin(ang) * (padRy - 1.8)
        );
        ctx.stroke();
      }
      ctx.fillStyle = rgb30([255, 255, 255], 0.12 + 0.18 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(padCx - 4, padCy - 2.4, 5, 1.8, -0.2, 0, Math.PI * 2);
      ctx.fill();
      if (p.padSnowAmt > 0.03) {
        ctx.fillStyle = rgb30([244, 250, 255], 0.85 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(padCx - 1, padCy - 1.6, padRx - 3.5, padRy - 3, -0.05, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.dressBlossom > 0.04) {
        ctx.save();
        ctx.globalAlpha = p.dressBlossom;
        ctx.fillStyle = rgb30([244, 196, 214]);
        ctx.translate(padCx + 8.5, padCy + 0.5);
        for (let i = 0; i < 5; i++) {
          ctx.save();
          ctx.rotate(i / 5 * Math.PI * 2);
          ctx.beginPath();
          ctx.ellipse(0, -1.6, 0.9, 1.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
        ctx.fillStyle = rgb30([244, 220, 120]);
        ctx.beginPath();
        ctx.arc(0, 0, 0.9, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        ctx.globalAlpha = 1;
      }
      const open = p.bloomOpen;
      const bloomCx = 0;
      const bloomCy = 6.5 - open * 4.5;
      const petalCol = p.petal;
      const tipCol = p.petalTip;
      const outlineCol = p.outline;
      const drawPetal = (angle, spread, length, width, fill, tip) => {
        ctx.save();
        ctx.translate(bloomCx, bloomCy);
        ctx.rotate(angle);
        ctx.fillStyle = rgb30(outlineCol, 0.85);
        ctx.beginPath();
        ctx.moveTo(0, spread + 0.6);
        ctx.quadraticCurveTo(-width - 0.6, -length * 0.55, 0, -length - 0.6);
        ctx.quadraticCurveTo(width + 0.6, -length * 0.55, 0, spread + 0.6);
        ctx.fill();
        const pg = ctx.createLinearGradient(0, spread, 0, -length);
        pg.addColorStop(0, rgb30(fill));
        pg.addColorStop(1, rgb30(tip));
        ctx.fillStyle = pg;
        ctx.beginPath();
        ctx.moveTo(0, spread);
        ctx.quadraticCurveTo(-width, -length * 0.55, 0, -length);
        ctx.quadraticCurveTo(width, -length * 0.55, 0, spread);
        ctx.fill();
        ctx.restore();
      };
      const backCount = 6;
      for (let i = 0; i < backCount; i++) {
        const base = (i - (backCount - 1) / 2) * 0.42;
        const angle = base * (0.35 + open * 1.05);
        const length = lerp35(8.5, 12.5, open);
        const width = lerp35(2.4, 4.2, open);
        drawPetal(angle, 2.2, length, width, petalCol, tipCol);
      }
      const frontCount = 5;
      const frontFill = lerpRGB23(petalCol, [255, 255, 255], 0.18);
      for (let i = 0; i < frontCount; i++) {
        const base = (i - (frontCount - 1) / 2) * 0.4;
        const angle = base * (0.28 + open * 0.7);
        const length = lerp35(7.5, 10, open);
        const width = lerp35(2, 3.2, open);
        drawPetal(angle, 1.4, length, width, frontFill, tipCol);
      }
      if (open > 0.12) {
        const ca = clamp0131((open - 0.12) / 0.88);
        ctx.fillStyle = rgb30(outlineCol, 0.8 * ca);
        ctx.beginPath();
        ctx.ellipse(bloomCx, bloomCy - 1.4, 3.2, 2.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb30(p.center, ca);
        ctx.beginPath();
        ctx.ellipse(bloomCx, bloomCy - 1.7, 2.6, 2.1, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb30(lerpRGB23(p.center, [120, 80, 20], 0.4), ca);
        for (let i = 0; i < 6; i++) {
          const a = i / 6 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bloomCx + Math.cos(a) * 1.8, bloomCy - 1.7 + Math.sin(a) * 1.2, 0.5, 0, Math.PI * 2);
          ctx.fill();
        }
      } else {
        ctx.fillStyle = rgb30(outlineCol, 0.8);
        ctx.beginPath();
        ctx.moveTo(bloomCx, bloomCy + 2.4);
        ctx.quadraticCurveTo(bloomCx - 3.6, bloomCy - 3, bloomCx, bloomCy - 9.5);
        ctx.quadraticCurveTo(bloomCx + 3.6, bloomCy - 3, bloomCx, bloomCy + 2.4);
        ctx.fill();
        const bg = ctx.createLinearGradient(0, bloomCy + 2, 0, bloomCy - 9);
        bg.addColorStop(0, rgb30(petalCol));
        bg.addColorStop(1, rgb30(tipCol));
        ctx.fillStyle = bg;
        ctx.beginPath();
        ctx.moveTo(bloomCx, bloomCy + 1.8);
        ctx.quadraticCurveTo(bloomCx - 2.8, bloomCy - 2.8, bloomCx, bloomCy - 8.6);
        ctx.quadraticCurveTo(bloomCx + 2.8, bloomCy - 2.8, bloomCx, bloomCy + 1.8);
        ctx.fill();
      }
      if (p.frostAmt > 0.04) {
        ctx.fillStyle = rgb30([236, 246, 255], 0.3 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(bloomCx, bloomCy - 1, 7.5, 8.5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb30([255, 255, 255], 0.85 * p.frostAmt);
        const fs = [[-3, bloomCy - 4], [2.5, bloomCy - 6], [0, bloomCy + 1]];
        for (const [fx, fy] of fs) {
          ctx.beginPath();
          ctx.arc(fx, fy, 0.6, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      if (micro.petalGlint > 0) {
        ctx.fillStyle = rgb30([255, 255, 255], 0.45 * micro.petalGlint);
        ctx.beginPath();
        ctx.ellipse(bloomCx - 2.2, bloomCy - 3.5, 1.4, 2.2, -0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      if (micro.dewPulse > 0) {
        ctx.fillStyle = rgb30([255, 255, 255], 0.5 * micro.dewPulse);
        ctx.beginPath();
        ctx.arc(bloomCx + 2, bloomCy + 1, 1 + 0.4 * micro.dewPulse, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.restore();
      return;
    } catch {
      try {
        ctx.restore();
      } catch {
      }
    }
    ctx.globalAlpha = 1;
  }
  function makeDraw8(season) {
    return (ctx) => paint30(ctx, SP30[season], 0);
  }
  function animSpring5(ctx, t) {
    const bob = bobAt30(t, 0.7, 1.4);
    const micro = {
      ...NO_MICRO,
      ripple: 0.5 + 0.5 * Math.sin(t * 1.8),
      dewPulse: 0.5 + 0.5 * Math.sin(t * 2.2)
    };
    paint30(ctx, SP30.Spring, bob, micro);
  }
  function animSummer5(ctx, t) {
    const bob = bobAt30(t, 0.9, 1.5);
    const micro = {
      ...NO_MICRO,
      ripple: 0.5 + 0.5 * Math.sin(t * 2),
      petalGlint: 0.5 + 0.5 * Math.sin(t * 1.6)
    };
    paint30(ctx, SP30.Summer, bob, micro);
  }
  function animAutumn5(ctx, t) {
    const bob = bobAt30(t, 0.6, 1.1);
    const micro = {
      ...NO_MICRO,
      ripple: 0.4 + 0.4 * Math.sin(t * 1.4),
      leafDrift: Math.sin(t * 0.9) * 4
    };
    paint30(ctx, SP30.Autumn, bob, micro);
  }
  function animWinter5(ctx, t) {
    const bob = bobAt30(t, 0.5, 1);
    const prog = t / 3.4 % 1;
    const micro = {
      ...NO_MICRO,
      iceSheen: 0.5 + 0.5 * Math.sin(t * 0.9),
      flakeY: prog,
      flakeX: Math.sin(prog * Math.PI * 2) * 5
    };
    paint30(ctx, SP30.Winter, bob, micro);
    if (micro.flakeY >= 0) {
      const fy = -20 + prog * 40;
      const fx = micro.flakeX;
      ctx.save();
      ctx.globalAlpha = 0.9 * (1 - Math.abs(prog - 0.5) * 0.4);
      ctx.fillStyle = "#ffffff";
      ctx.beginPath();
      ctx.arc(fx, fy, 1.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.globalAlpha = 1;
    }
  }
  function makeTransition27(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      paint30(ctx, lerpP30(SP30[from], SP30[to], smoother30(clamp0131(pp))), 0);
    };
  }
  var springToSummer24 = makeTransition27(0);
  var summerToAutumn24 = makeTransition27(1);
  var autumnToWinter24 = makeTransition27(2);
  var VARIANTS32 = {
    Spring: { draw: makeDraw8("Spring"), anim: animSpring5 },
    Summer: { draw: makeDraw8("Summer"), anim: animSummer5 },
    Autumn: { draw: makeDraw8("Autumn"), anim: animAutumn5 },
    Winter: { draw: makeDraw8("Winter"), anim: animWinter5 }
  };
  var TRANSITIONS32 = {
    0: springToSummer24,
    1: summerToAutumn24,
    2: autumnToWinter24
  };

  // src/textures/seasonal/bird/chicken.ts
  function clamp0132(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother31 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp36(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB24(a, b, t) {
    return [lerp36(a[0], b[0], t), lerp36(a[1], b[1], t), lerp36(a[2], b[2], t)];
  }
  function rgb31(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba20(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0132(a)})`;
  }
  function lerpP31(a, b, t) {
    return {
      featherLight: lerpRGB24(a.featherLight, b.featherLight, t),
      featherMid: lerpRGB24(a.featherMid, b.featherMid, t),
      featherDark: lerpRGB24(a.featherDark, b.featherDark, t),
      comb: lerpRGB24(a.comb, b.comb, t),
      combDark: lerpRGB24(a.combDark, b.combDark, t),
      beak: lerpRGB24(a.beak, b.beak, t),
      beakDark: lerpRGB24(a.beakDark, b.beakDark, t),
      padGrass: lerpRGB24(a.padGrass, b.padGrass, t),
      padDark: lerpRGB24(a.padDark, b.padDark, t),
      soil: lerpRGB24(a.soil, b.soil, t),
      outline: lerpRGB24(a.outline, b.outline, t),
      light: lerpRGB24(a.light, b.light, t),
      lightAmt: lerp36(a.lightAmt, b.lightAmt, t),
      fluff: lerp36(a.fluff, b.fluff, t),
      sheen: lerp36(a.sheen, b.sheen, t),
      frostAmt: lerp36(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp36(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp36(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp36(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp36(a.fallenLeafAmt, b.fallenLeafAmt, t),
      chickAmt: lerp36(a.chickAmt, b.chickAmt, t)
    };
  }
  function clampP20(p) {
    return {
      ...p,
      lightAmt: clamp0132(p.lightAmt),
      fluff: clamp0132(p.fluff),
      sheen: clamp0132(p.sheen),
      frostAmt: clamp0132(p.frostAmt),
      snowCapAmt: clamp0132(p.snowCapAmt),
      padSnowAmt: clamp0132(p.padSnowAmt),
      blossomAmt: clamp0132(p.blossomAmt),
      fallenLeafAmt: clamp0132(p.fallenLeafAmt),
      chickAmt: clamp0132(p.chickAmt)
    };
  }
  var SP31 = {
    // Spring — fresh pastel; lime dewy pad + blossom + a yellow chick beside her.
    Spring: {
      featherLight: [252, 250, 244],
      featherMid: [236, 228, 208],
      featherDark: [196, 184, 158],
      comb: [220, 60, 56],
      combDark: [158, 36, 38],
      beak: [240, 158, 52],
      beakDark: [196, 116, 30],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      soil: [120, 84, 48],
      outline: [70, 58, 44],
      light: [232, 244, 226],
      lightAmt: 0.16,
      fluff: 0.4,
      sheen: 0.2,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0,
      chickAmt: 1
    },
    // Summer — PEAK; normal full plumage; saturated mid-green pad; warm light.
    Summer: {
      featherLight: [255, 253, 248],
      featherMid: [242, 234, 214],
      featherDark: [200, 186, 156],
      comb: [228, 56, 50],
      combDark: [164, 34, 34],
      beak: [246, 162, 48],
      beakDark: [202, 118, 26],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      soil: [126, 86, 48],
      outline: [66, 54, 40],
      light: [255, 240, 206],
      lightAmt: 0.18,
      fluff: 0.5,
      sheen: 0.7,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      chickAmt: 0
    },
    // Autumn — slightly fuller feathers; olive-tan browning pad; fallen leaves.
    Autumn: {
      featherLight: [250, 246, 236],
      featherMid: [234, 222, 196],
      featherDark: [190, 174, 144],
      comb: [206, 52, 46],
      combDark: [150, 32, 34],
      beak: [232, 150, 46],
      beakDark: [188, 108, 26],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      soil: [120, 80, 44],
      outline: [66, 52, 38],
      light: [248, 210, 150],
      lightAmt: 0.2,
      fluff: 0.66,
      sheen: 0.32,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85,
      chickAmt: 0
    },
    // Winter — cool blue-grey light; feathers fluffed thick; snow on the back +
    // frost; snowy pad. Hen stays clearly white-and-cream underneath.
    Winter: {
      featherLight: [248, 250, 254],
      featherMid: [224, 224, 222],
      featherDark: [176, 182, 188],
      comb: [196, 56, 56],
      combDark: [140, 36, 42],
      beak: [224, 150, 60],
      beakDark: [180, 112, 40],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      soil: [128, 110, 96],
      outline: [60, 56, 64],
      light: [206, 226, 252],
      lightAmt: 0.3,
      fluff: 1,
      sheen: 0.22,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      chickAmt: 0
    }
  };
  var BODY_CX2 = -1;
  var BODY_CY2 = 4;
  var BODY_RX2 = 12.5;
  var BODY_RY2 = 10.5;
  var HEAD_CX = -8.5;
  var HEAD_CY = -6.5;
  var HEAD_R = 5.6;
  var FOOT_Y = 16.5;
  function henBodyPath(ctx, bob, fluff) {
    const cx = BODY_CX2;
    const cy = BODY_CY2 + bob;
    const rx = BODY_RX2 + fluff * 1.6;
    const ry = BODY_RY2 + fluff * 1.2;
    ctx.beginPath();
    ctx.moveTo(cx - rx, cy + 1.5);
    ctx.quadraticCurveTo(cx - rx + 1, cy - ry + 1, cx - 2, cy - ry);
    ctx.quadraticCurveTo(cx + rx * 0.7, cy - ry - 0.5, cx + rx, cy - ry * 0.4);
    ctx.quadraticCurveTo(cx + rx + 1.5, cy + ry * 0.5, cx + rx * 0.5, cy + ry);
    ctx.quadraticCurveTo(cx - rx * 0.2, cy + ry + 1.5, cx - rx * 0.7, cy + ry * 0.7);
    ctx.quadraticCurveTo(cx - rx - 1, cy + ry * 0.2, cx - rx, cy + 1.5);
    ctx.closePath();
  }
  function paint31(ctx, raw, bob) {
    const p = clampP20(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba20(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb31(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb31(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb31(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba20([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba20([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba20([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba20([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba20(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba20([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba20(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba20([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba20(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(1.5, FOOT_Y + 1.5 + bob * 0.4, 11, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      if (p.chickAmt > 0.01) drawChick(ctx, p, bob);
      const bcy = BODY_CY2 + bob;
      drawLegs(ctx, p, bcy);
      drawTail(ctx, p, bob);
      ctx.lineJoin = "round";
      henBodyPath(ctx, bob, p.fluff);
      ctx.lineWidth = 2.2;
      ctx.strokeStyle = rgb31(p.outline);
      ctx.stroke();
      ctx.fillStyle = rgb31(p.featherMid);
      ctx.fill();
      ctx.save();
      henBodyPath(ctx, bob, p.fluff);
      ctx.clip();
      ctx.fillStyle = rgb31(p.featherDark);
      ctx.beginPath();
      ctx.ellipse(BODY_CX2 + 2, bcy + BODY_RY2 * 0.5, BODY_RX2, BODY_RY2 * 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb31(p.featherMid);
      ctx.beginPath();
      ctx.ellipse(BODY_CX2, bcy + 0.5, BODY_RX2 * 0.92, BODY_RY2 * 0.82, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb31(p.featherLight);
      ctx.beginPath();
      ctx.ellipse(BODY_CX2 - 3.5, bcy - 4, BODY_RX2 * 0.62, BODY_RY2 * 0.52, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba20(p.featherDark, 0.8);
      ctx.lineWidth = 1.8;
      ctx.beginPath();
      ctx.moveTo(BODY_CX2 - 6, bcy - 3.5);
      ctx.quadraticCurveTo(BODY_CX2 + 4, bcy - 1.5, BODY_CX2 + 8.5, bcy + 4.5);
      ctx.stroke();
      ctx.strokeStyle = rgba20(p.featherLight, 0.5);
      ctx.lineWidth = 1;
      [[-4, 1], [0, 3], [4, 5]].forEach(([fx, fy]) => {
        ctx.beginPath();
        ctx.moveTo(BODY_CX2 + fx, bcy + fy);
        ctx.quadraticCurveTo(BODY_CX2 + fx + 2, bcy + fy + 1.5, BODY_CX2 + fx + 4.5, bcy + fy + 1);
        ctx.stroke();
      });
      if (p.sheen > 0.02) {
        ctx.fillStyle = rgba20([255, 255, 255], 0.1 + 0.32 * p.sheen);
        ctx.beginPath();
        ctx.ellipse(BODY_CX2 - 4, bcy - 5, BODY_RX2 * 0.5, BODY_RY2 * 0.32, -0.35, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba20([220, 236, 252], 0.22 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(BODY_CX2, bcy - 5.5, BODY_RX2 * 0.8, BODY_RY2 * 0.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba20([240, 248, 255], 0.7 * p.frostAmt);
        const speck = [
          [-6, -7],
          [-1, -8],
          [3, -7],
          [7, -5],
          [-4, -4],
          [5, -3],
          [0, -5]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(BODY_CX2 + sx, bcy + sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        const ty = bcy - BODY_RY2 - p.fluff * 1.2;
        ctx.fillStyle = rgba20([248, 252, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(BODY_CX2 - 6, ty + 4);
        ctx.quadraticCurveTo(BODY_CX2 - 3, ty - 1, BODY_CX2 + 1, ty + 0.5);
        ctx.quadraticCurveTo(BODY_CX2 + 5, ty - 1.5, BODY_CX2 + 8, ty + 3);
        ctx.quadraticCurveTo(BODY_CX2 + 5, ty + 4.5, BODY_CX2 + 2, ty + 3.5);
        ctx.quadraticCurveTo(BODY_CX2 - 2, ty + 5, BODY_CX2 - 6, ty + 4);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba20([208, 224, 244], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(BODY_CX2 + 1, ty + 4, 6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      drawHead(ctx, p, bob);
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba20(p.light, p.lightAmt));
        lg.addColorStop(1, rgba20(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function drawLegs(ctx, p, bcy) {
    const legs = [
      [-3.5, FOOT_Y],
      // front leg (toward viewer)
      [3.5, FOOT_Y - 0.5]
      // back leg
    ];
    legs.forEach(([lx, fy]) => {
      ctx.strokeStyle = rgb31(p.beakDark);
      ctx.lineWidth = 2.6;
      ctx.beginPath();
      ctx.moveTo(lx, bcy + BODY_RY2 * 0.6);
      ctx.lineTo(lx - 1, fy);
      ctx.stroke();
      ctx.strokeStyle = rgb31(p.beak);
      ctx.lineWidth = 1.6;
      ctx.beginPath();
      ctx.moveTo(lx, bcy + BODY_RY2 * 0.6);
      ctx.lineTo(lx - 1, fy);
      ctx.stroke();
      ctx.strokeStyle = rgb31(p.beak);
      ctx.lineWidth = 1.4;
      [-2.4, 0, 2].forEach((tx) => {
        ctx.beginPath();
        ctx.moveTo(lx - 1, fy);
        ctx.lineTo(lx - 1 + tx, fy + 1.8);
        ctx.stroke();
      });
    });
  }
  function drawTail(ctx, p, bob) {
    const tx = BODY_CX2 + BODY_RX2 - 1;
    const ty = BODY_CY2 + bob - BODY_RY2 * 0.4;
    ctx.save();
    ctx.fillStyle = rgb31(p.outline);
    ctx.beginPath();
    ctx.moveTo(tx - 2, ty + 2);
    ctx.quadraticCurveTo(tx + 6, ty - 7, tx + 10, ty - 8);
    ctx.quadraticCurveTo(tx + 9, ty - 2, tx + 8, ty + 2);
    ctx.quadraticCurveTo(tx + 5, ty + 5, tx - 2, ty + 2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb31(p.featherMid);
    ctx.beginPath();
    ctx.moveTo(tx - 1, ty + 1.5);
    ctx.quadraticCurveTo(tx + 5.5, ty - 6, tx + 9, ty - 7);
    ctx.quadraticCurveTo(tx + 8, ty - 2, tx + 7, ty + 1.5);
    ctx.quadraticCurveTo(tx + 4, ty + 4, tx - 1, ty + 1.5);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgba20(p.featherLight, 0.6);
    ctx.lineWidth = 1.1;
    ctx.beginPath();
    ctx.moveTo(tx, ty + 0.5);
    ctx.quadraticCurveTo(tx + 5.5, ty - 6, tx + 9, ty - 7);
    ctx.stroke();
    ctx.strokeStyle = rgba20(p.featherDark, 0.6);
    ctx.lineWidth = 0.9;
    [[2, -1], [4, -2.5]].forEach(([dx, dy]) => {
      ctx.beginPath();
      ctx.moveTo(tx + dx, ty + dy + 2);
      ctx.lineTo(tx + dx + 3, ty + dy - 3);
      ctx.stroke();
    });
    ctx.restore();
  }
  function drawHead(ctx, p, bob) {
    const hx = HEAD_CX;
    const hy = HEAD_CY + bob;
    ctx.save();
    ctx.strokeStyle = rgb31(p.outline);
    ctx.lineWidth = 7.5;
    ctx.beginPath();
    ctx.moveTo(hx + 1.5, hy + 3);
    ctx.lineTo(BODY_CX2 - 5, BODY_CY2 + bob - 3);
    ctx.stroke();
    ctx.strokeStyle = rgb31(p.featherMid);
    ctx.lineWidth = 5.6;
    ctx.beginPath();
    ctx.moveTo(hx + 1.5, hy + 3);
    ctx.lineTo(BODY_CX2 - 5, BODY_CY2 + bob - 3);
    ctx.stroke();
    ctx.fillStyle = rgb31(p.combDark);
    [-2.6, 0.2, 2.8].forEach((cx, i) => {
      ctx.beginPath();
      ctx.arc(hx + cx, hy - HEAD_R + 0.5, 2 - i * 0.1, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.fillStyle = rgb31(p.comb);
    [-2.6, 0.2, 2.8].forEach((cx, i) => {
      ctx.beginPath();
      ctx.arc(hx + cx - 0.4, hy - HEAD_R - 0.1, 1.7 - i * 0.1, 0, Math.PI * 2);
      ctx.fill();
    });
    ctx.lineWidth = 2;
    ctx.strokeStyle = rgb31(p.outline);
    ctx.beginPath();
    ctx.arc(hx, hy, HEAD_R, 0, Math.PI * 2);
    ctx.stroke();
    ctx.fillStyle = rgb31(p.featherMid);
    ctx.fill();
    ctx.save();
    ctx.beginPath();
    ctx.arc(hx, hy, HEAD_R, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb31(p.featherDark);
    ctx.beginPath();
    ctx.ellipse(hx + 1.5, hy + 2.5, HEAD_R, HEAD_R * 0.7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb31(p.featherLight);
    ctx.beginPath();
    ctx.ellipse(hx - 1.8, hy - 2, HEAD_R * 0.62, HEAD_R * 0.52, -0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb31(p.combDark);
    ctx.beginPath();
    ctx.ellipse(hx - 3.2, hy + HEAD_R - 0.5, 1.6, 2.4, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb31(p.comb);
    ctx.beginPath();
    ctx.ellipse(hx - 3.5, hy + HEAD_R - 1, 1.2, 1.9, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb31(p.beakDark);
    ctx.beginPath();
    ctx.moveTo(hx - HEAD_R + 0.5, hy - 0.5);
    ctx.lineTo(hx - HEAD_R - 4.5, hy + 1.6);
    ctx.lineTo(hx - HEAD_R + 1, hy + 3);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb31(p.beak);
    ctx.beginPath();
    ctx.moveTo(hx - HEAD_R + 0.8, hy - 0.2);
    ctx.lineTo(hx - HEAD_R - 3.8, hy + 1.4);
    ctx.lineTo(hx - HEAD_R + 1, hy + 2.2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb31(p.outline);
    ctx.beginPath();
    ctx.arc(hx - 1.8, hy - 0.6, 1.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = "rgba(255,255,255,0.9)";
    ctx.beginPath();
    ctx.arc(hx - 2.3, hy - 1.1, 0.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function drawChick(ctx, p, bob) {
    const a = clamp0132(p.chickAmt);
    if (a <= 0.01) return;
    const cx = 13.5;
    const cy = 12 + bob * 0.5;
    ctx.save();
    ctx.globalAlpha = a;
    ctx.fillStyle = rgba20(p.outline, 0.22 * a);
    ctx.beginPath();
    ctx.ellipse(cx, cy + 4.5, 4.2, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb31(p.beak);
    ctx.lineWidth = 1;
    [-1.2, 1.2].forEach((lx) => {
      ctx.beginPath();
      ctx.moveTo(cx + lx, cy + 2.5);
      ctx.lineTo(cx + lx, cy + 4.5);
      ctx.stroke();
    });
    ctx.lineWidth = 1.6;
    ctx.strokeStyle = rgb31(p.outline);
    ctx.fillStyle = "rgb(248,214,86)";
    ctx.beginPath();
    ctx.ellipse(cx, cy + 0.5, 3.4, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.stroke();
    ctx.fillStyle = "rgb(252,224,104)";
    ctx.beginPath();
    ctx.arc(cx - 2, cy - 2.4, 2.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb31(p.outline);
    ctx.lineWidth = 1.2;
    ctx.stroke();
    ctx.fillStyle = "rgba(255,245,190,0.8)";
    ctx.beginPath();
    ctx.ellipse(cx - 2.8, cy - 3.2, 1.2, 0.9, -0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb31(p.beakDark);
    ctx.beginPath();
    ctx.moveTo(cx - 4, cy - 2.4);
    ctx.lineTo(cx - 5.6, cy - 1.8);
    ctx.lineTo(cx - 4, cy - 1.4);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb31(p.outline);
    ctx.beginPath();
    ctx.arc(cx - 2.4, cy - 2.6, 0.7, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function bobAt31(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function peckAt(t, w = 1.4) {
    const ph = w * t / (Math.PI * 2) % 1;
    const x = clamp0132((ph - 0.32) / 0.36);
    const hump = x <= 0 || x >= 1 ? 0 : Math.sin(x * Math.PI);
    return hump * hump * 3.2;
  }
  function draw21(season) {
    return (ctx) => paint31(ctx, SP31[season], 0);
  }
  function anim21(season) {
    return (ctx, t) => {
      const bob = bobAt31(t);
      const peck = peckAt(t);
      if (peck < 0.05) {
        paint31(ctx, SP31[season], bob);
      } else {
        paint31(ctx, SP31[season], bob);
        ctx.save();
        try {
          ctx.translate(-1, peck);
          ctx.globalAlpha = 1;
          drawHead(ctx, clampP20(SP31[season]), bob);
        } finally {
          ctx.restore();
        }
      }
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const cb = Math.sin(t * 2) * 0.8;
          const sp = clampP20(SP31.Spring);
          ctx.save();
          ctx.translate(0, cb);
          drawChick(ctx, sp, bob);
          ctx.restore();
        } else if (season === "Summer") {
          const g = 0.18 + 0.26 * (0.5 + 0.5 * Math.sin(t * 1.6));
          const sx = BODY_CX2 - 5 + Math.sin(t * 0.9) * 3;
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.ellipse(sx, BODY_CY2 + bob - 5, 3.6, 2, -0.35, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const rock = Math.sin(t * 1.3) * 0.4;
          ctx.save();
          ctx.translate(-12, 19.6);
          ctx.rotate(-0.5 + rock);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        } else {
          const hx = HEAD_CX;
          const hy = HEAD_CY + bob + peck;
          const puff = 0.5 + 0.5 * Math.sin(t * 1.8);
          const px = hx - HEAD_R - 4.5;
          ctx.fillStyle = `rgba(232,242,255,${0.12 + 0.34 * puff})`;
          ctx.beginPath();
          ctx.ellipse(px - puff * 2, hy + 1.4, 2 + puff * 2.4, 1.6 + puff * 1.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = `rgba(244,250,255,${0.1 + 0.2 * puff})`;
          ctx.beginPath();
          ctx.arc(px - puff * 3.5, hy + 0.6, 1 + puff * 1.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3 % 1 + 1) % 1;
          const fy = -22 + prog * 38;
          const fx = 6 + Math.sin(prog * Math.PI * 2) * 3;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition28(fromIdx) {
    const from = SP31[SEASON_NAMES[fromIdx]];
    const to = SP31[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother31(clamp0132(pp));
      paint31(ctx, lerpP31(from, to, k), 0);
    };
  }
  var springToSummer25 = makeTransition28(0);
  var summerToAutumn25 = makeTransition28(1);
  var autumnToWinter25 = makeTransition28(2);
  var VARIANTS33 = {
    Spring: { draw: draw21("Spring"), anim: anim21("Spring") },
    Summer: { draw: draw21("Summer"), anim: anim21("Summer") },
    Autumn: { draw: draw21("Autumn"), anim: anim21("Autumn") },
    Winter: { draw: draw21("Winter"), anim: anim21("Winter") }
  };
  var TRANSITIONS33 = {
    0: springToSummer25,
    1: summerToAutumn25,
    2: autumnToWinter25
  };

  // src/textures/seasonal/bird/rooster.ts
  function clamp0133(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp37(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB25(a, b, t) {
    return [lerp37(a[0], b[0], t), lerp37(a[1], b[1], t), lerp37(a[2], b[2], t)];
  }
  function rgb32(c, a = 1) {
    const r = Math.round(clamp0133(c[0] / 255) * 255);
    const g = Math.round(clamp0133(c[1] / 255) * 255);
    const b = Math.round(clamp0133(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0133(a)})`;
  }
  function smoother32(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt32(t, amp = 0.9, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var COMB = [214, 44, 42];
  var COMB_SHADE = [150, 26, 30];
  var BODY_LIGHT = [186, 96, 50];
  var BODY_MID = [148, 66, 34];
  var BODY_DARK = [96, 42, 24];
  var HACKLE = [226, 168, 56];
  var HACKLE_DARK = [168, 112, 32];
  var TAIL = [30, 48, 38];
  var TAIL_SHEEN = [70, 132, 92];
  var BEAK = [228, 184, 70];
  var SP32 = {
    // Spring — bright plumage, dewy lime pad + blossom, cool-bright light.
    Spring: {
      comb: COMB,
      combShade: COMB_SHADE,
      bodyLight: BODY_LIGHT,
      bodyMid: BODY_MID,
      bodyDark: BODY_DARK,
      hackle: HACKLE,
      hackleDark: HACKLE_DARK,
      tail: TAIL,
      tailSheen: TAIL_SHEEN,
      beak: BEAK,
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [48, 28, 22],
      lightWash: [220, 240, 255],
      // cool-bright
      lightWashAmt: 0.15,
      fluff: 0.32,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0,
      gloss: 0.4
    },
    // Summer — richest glossy plumage (PEAK); saturated mid-green pad; warm light.
    Summer: {
      comb: COMB,
      combShade: COMB_SHADE,
      bodyLight: BODY_LIGHT,
      bodyMid: BODY_MID,
      bodyDark: BODY_DARK,
      hackle: HACKLE,
      hackleDark: HACKLE_DARK,
      tail: TAIL,
      tailSheen: TAIL_SHEEN,
      beak: BEAK,
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [44, 24, 18],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.16,
      fluff: 0.42,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0,
      gloss: 1
      // peak glossy sheen on the tail
    },
    // Autumn — warm plumage, olive-tan browning pad, a couple of fallen leaves.
    Autumn: {
      comb: COMB,
      combShade: COMB_SHADE,
      bodyLight: BODY_LIGHT,
      bodyMid: BODY_MID,
      bodyDark: BODY_DARK,
      hackle: HACKLE,
      hackleDark: HACKLE_DARK,
      tail: TAIL,
      tailSheen: TAIL_SHEEN,
      beak: BEAK,
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [50, 28, 20],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      fluff: 0.55,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0,
      gloss: 0.55
    },
    // Winter — frosted comb + fluffed feathers + a little snow on the back +
    // breath-fog; snowy pad, cool light. Bird stays CLEARLY its own colours.
    Winter: {
      comb: COMB,
      combShade: COMB_SHADE,
      bodyLight: BODY_LIGHT,
      bodyMid: BODY_MID,
      bodyDark: BODY_DARK,
      hackle: HACKLE,
      hackleDark: HACKLE_DARK,
      tail: TAIL,
      tailSheen: TAIL_SHEEN,
      beak: BEAK,
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [54, 40, 44],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      fluff: 1,
      // puffed up against the cold
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7,
      gloss: 0.35
    }
  };
  function lerpP32(a, b, t) {
    return {
      comb: lerpRGB25(a.comb, b.comb, t),
      combShade: lerpRGB25(a.combShade, b.combShade, t),
      bodyLight: lerpRGB25(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB25(a.bodyMid, b.bodyMid, t),
      bodyDark: lerpRGB25(a.bodyDark, b.bodyDark, t),
      hackle: lerpRGB25(a.hackle, b.hackle, t),
      hackleDark: lerpRGB25(a.hackleDark, b.hackleDark, t),
      tail: lerpRGB25(a.tail, b.tail, t),
      tailSheen: lerpRGB25(a.tailSheen, b.tailSheen, t),
      beak: lerpRGB25(a.beak, b.beak, t),
      padGrass: lerpRGB25(a.padGrass, b.padGrass, t),
      soil: lerpRGB25(a.soil, b.soil, t),
      outline: lerpRGB25(a.outline, b.outline, t),
      lightWash: lerpRGB25(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp37(a.lightWashAmt, b.lightWashAmt, t),
      fluff: lerp37(a.fluff, b.fluff, t),
      frostAmt: lerp37(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp37(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp37(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp37(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp37(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp37(a.breathFogAmt, b.breathFogAmt, t),
      gloss: lerp37(a.gloss, b.gloss, t)
    };
  }
  function clampP21(p) {
    return {
      ...p,
      lightWashAmt: clamp0133(p.lightWashAmt),
      fluff: clamp0133(p.fluff),
      frostAmt: clamp0133(p.frostAmt),
      backSnowAmt: clamp0133(p.backSnowAmt),
      padSnowAmt: clamp0133(p.padSnowAmt),
      blossomAmt: clamp0133(p.blossomAmt),
      fallenLeafAmt: clamp0133(p.fallenLeafAmt),
      breathFogAmt: clamp0133(p.breathFogAmt),
      gloss: clamp0133(p.gloss)
    };
  }
  function paintPad2(ctx, p) {
    ctx.fillStyle = rgb32([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb32(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb32([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb32([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb32([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb32([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb32([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb32(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb32([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg(ctx, p, x, topY, baseY, alpha) {
    ctx.save();
    ctx.globalAlpha = alpha;
    ctx.strokeStyle = rgb32(p.outline);
    ctx.lineWidth = 3.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb32(p.beak);
    ctx.lineWidth = 1.8;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb32(p.beak);
    ctx.lineWidth = 1.4;
    for (const dx of [-2.4, 0, 2.4]) {
      ctx.beginPath();
      ctx.moveTo(x, baseY);
      ctx.lineTo(x + dx, baseY + 1.4);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.restore();
  }
  function bodyPath(ctx, cx, cy, fluff) {
    const s = 1 + fluff * 0.08;
    const rx = 11 * s;
    const ry = 12.5 * s;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.12, 0, Math.PI * 2);
  }
  function paintTail(ctx, p, cx, cy) {
    const baseX = cx + 8;
    const baseY = cy + 2;
    const sickles = [
      // [tip dx, tip dy, control bulge]
      [14, -20, 10],
      [17, -14, 13],
      [16, -7, 12]
    ];
    ctx.strokeStyle = rgb32(p.outline);
    ctx.lineWidth = 6.5;
    ctx.lineCap = "round";
    for (const [tx, ty, cb] of sickles) {
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + cb, baseY - 16, baseX + tx, baseY + ty);
      ctx.stroke();
    }
    ctx.strokeStyle = rgb32(p.tail);
    ctx.lineWidth = 4.4;
    for (const [tx, ty, cb] of sickles) {
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + cb, baseY - 16, baseX + tx, baseY + ty);
      ctx.stroke();
    }
    if (p.gloss > 0.02) {
      ctx.strokeStyle = rgb32(p.tailSheen, 0.5 + 0.45 * p.gloss);
      ctx.lineWidth = 1.5;
      for (const [tx, ty, cb] of sickles) {
        ctx.beginPath();
        ctx.moveTo(baseX + 1, baseY - 1);
        ctx.quadraticCurveTo(baseX + cb + 1, baseY - 17, baseX + tx + 0.6, baseY + ty - 0.6);
        ctx.stroke();
      }
    }
    ctx.lineCap = "butt";
  }
  function paintRooster(ctx, p, bob, headBob, tailFlick) {
    const cx = -1;
    const cy = 2 - bob;
    ctx.save();
    ctx.translate(0, tailFlick * 0);
    paintTail(ctx, p, cx, cy);
    ctx.restore();
    paintLeg(ctx, p, cx - 2.5, cy + 9, 19.2, 1);
    paintLeg(ctx, p, cx + 2.8, cy + 9, 19, 0.9);
    bodyPath(ctx, cx, cy, p.fluff);
    ctx.fillStyle = rgb32(p.outline);
    ctx.save();
    ctx.translate(0, 0);
    bodyPath(ctx, cx, cy, p.fluff + 0.5);
    ctx.fillStyle = rgb32(p.outline);
    ctx.fill();
    ctx.restore();
    bodyPath(ctx, cx, cy, p.fluff);
    ctx.fillStyle = rgb32(p.bodyMid);
    ctx.fill();
    ctx.save();
    bodyPath(ctx, cx, cy, p.fluff);
    ctx.clip();
    ctx.fillStyle = rgb32(p.bodyDark);
    ctx.beginPath();
    ctx.ellipse(cx + 4, cy + 7, 10, 9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.bodyLight);
    ctx.beginPath();
    ctx.ellipse(cx - 4, cy - 3, 7, 8.5, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb32(p.bodyDark, 0.4);
    ctx.lineWidth = 1;
    for (const dx of [-5, -1.5, 2]) {
      ctx.beginPath();
      ctx.moveTo(cx + dx, cy - 6);
      ctx.quadraticCurveTo(cx + dx - 1.5, cy, cx + dx - 2.5, cy + 7);
      ctx.stroke();
    }
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb32([248, 252, 255], 0.9 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(cx + 3, cy - 8.5, 8 * (0.9 + p.fluff * 0.25), 3, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb32([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-2, -9.4], [3, -9.6], [7, -8.4]]) {
        ctx.beginPath();
        ctx.arc(cx + dx, cy + dy, 1.4 + p.fluff * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const nx = cx - 5;
    const ny = cy - 8 + headBob;
    const hackles = [
      [-5, 6, -0.5],
      [-3, 9, -0.25],
      [0, 10, 0],
      [3, 8, 0.25]
    ];
    for (const [dx, len, rot] of hackles) {
      ctx.save();
      ctx.translate(nx + dx, ny + 3);
      ctx.rotate(rot);
      ctx.fillStyle = rgb32(p.hackleDark);
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.quadraticCurveTo(-2.6, len * 0.6, -1.2, len);
      ctx.quadraticCurveTo(0, len * 0.7, 2.6, len * 0.6);
      ctx.closePath();
      ctx.fill();
      ctx.restore();
    }
    for (const [dx, len, rot] of hackles) {
      ctx.save();
      ctx.translate(nx + dx - 0.6, ny + 2.2);
      ctx.rotate(rot);
      ctx.fillStyle = rgb32(p.hackle);
      ctx.beginPath();
      ctx.moveTo(0, 0);
      ctx.quadraticCurveTo(-2, len * 0.6, -0.8, len * 0.92);
      ctx.quadraticCurveTo(0, len * 0.6, 2, len * 0.55);
      ctx.closePath();
      ctx.fill();
      ctx.restore();
    }
    const hx = cx - 7;
    const hy = cy - 12 + headBob;
    ctx.fillStyle = rgb32(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 5.2, 5.4, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.bodyMid);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 4.4, 4.6, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.bodyLight);
    ctx.beginPath();
    ctx.ellipse(hx - 1.4, hy - 1.4, 2.4, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
    const combY = hy - 4.6;
    ctx.fillStyle = rgb32(p.combShade);
    ctx.beginPath();
    ctx.moveTo(hx - 4, combY + 2);
    for (let i = 0; i <= 4; i++) {
      const px = hx - 4 + i * 2.2;
      const peak = combY - 3.4 - (i === 1 || i === 2 ? 1.2 : 0);
      ctx.quadraticCurveTo(px - 0.5, peak, px + 1.1, combY + 1.4);
    }
    ctx.lineTo(hx + 4.6, combY + 2.2);
    ctx.quadraticCurveTo(hx, combY + 3, hx - 4, combY + 2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb32(p.comb);
    ctx.beginPath();
    ctx.moveTo(hx - 4.4, combY + 1.6);
    for (let i = 0; i <= 4; i++) {
      const px = hx - 4.4 + i * 2.2;
      const peak = combY - 4 - (i === 1 || i === 2 ? 1.2 : 0);
      ctx.quadraticCurveTo(px - 0.6, peak, px + 0.8, combY + 0.8);
    }
    ctx.lineTo(hx + 4, combY + 1.6);
    ctx.quadraticCurveTo(hx, combY + 2.4, hx - 4.4, combY + 1.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb32(p.comb);
    for (const dx of [-1.4, 1]) {
      ctx.beginPath();
      ctx.ellipse(hx - 4.4 + dx, hy + 4.2, 1.5, 2.6, 0.1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb32(p.combShade, 0.5);
    ctx.beginPath();
    ctx.ellipse(hx - 4.6, hy + 5, 1.3, 1.8, 0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32(p.beak);
    ctx.beginPath();
    ctx.moveTo(hx - 3.8, hy);
    ctx.lineTo(hx - 8.4, hy + 1.8);
    ctx.lineTo(hx - 3.8, hy + 3.2);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb32(p.outline, 0.6);
    ctx.lineWidth = 0.6;
    ctx.beginPath();
    ctx.moveTo(hx - 8.4, hy + 1.8);
    ctx.lineTo(hx - 4, hy + 1.9);
    ctx.stroke();
    ctx.fillStyle = rgb32([250, 248, 240]);
    ctx.beginPath();
    ctx.arc(hx - 1.2, hy - 0.4, 1.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32([22, 18, 18]);
    ctx.beginPath();
    ctx.arc(hx - 1.4, hy - 0.4, 0.85, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb32([255, 255, 255], 0.9);
    ctx.beginPath();
    ctx.arc(hx - 1.7, hy - 0.8, 0.3, 0, Math.PI * 2);
    ctx.fill();
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb32([235, 246, 255], 0.55 * p.frostAmt);
      for (const [fx, fy] of [
        [hx - 3, combY - 2.5],
        [hx, combY - 3.2],
        [hx + 2.6, combY - 2.2],
        [hx - 1.4, combY - 1],
        [hx + 1.4, combY - 1.2]
      ]) {
        ctx.beginPath();
        ctx.arc(fx, fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.fillStyle = rgb32([255, 255, 255], 0.4 * p.frostAmt);
      for (const [fx, fy] of [[cx - 4, cy - 4], [cx + 2, cy - 6], [cx - 1, cy - 1]]) {
        ctx.beginPath();
        ctx.arc(fx, fy, 0.6, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb32([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 11, hy + 2.4, 3, 2, 0.15, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb32(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 2, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint32(ctx, pIn, bob, headBob = 0, tailFlick = 0) {
    const p = clampP21(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad2(ctx, p);
      paintRooster(ctx, p, bob, headBob, tailFlick);
      paintLightWash(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw22(season) {
    return (ctx) => paint32(ctx, SP32[season], 0);
  }
  function anim22(season) {
    return (ctx, t) => {
      const p = clampP21(SP32[season]);
      const bob = bobAt32(t);
      const loop = t % 5 / 5;
      const gate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 4;
      const headBob = gate * -1.4;
      const tailFlick = Math.sin(t * 8) * gate * 1.2;
      paint32(ctx, SP32[season], bob, headBob, tailFlick);
      ctx.save();
      try {
        const cx = -1;
        const cy = 2 - bob;
        const hx = cx - 7;
        const hy = cy - 12 + headBob;
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb32([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.2);
          const baseX = cx + 8;
          const baseY = cy + 2;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.strokeStyle = rgb32([180, 255, 210], 0.4 + 0.3 * s);
          ctx.lineWidth = 1.4;
          const ty = lerp37(-20, -7, s);
          const cb = lerp37(10, 12, s);
          ctx.beginPath();
          ctx.moveTo(baseX, baseY);
          ctx.quadraticCurveTo(baseX + cb, baseY - 16, baseX + 15, baseY + ty);
          ctx.stroke();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb32([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 5 + breathe * 3;
          ctx.fillStyle = rgb32([235, 244, 255], (0.18 + 0.3 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach - 2, hy + 2.4, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.15, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb32([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition29(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother32(clamp0133(pp));
      paint32(ctx, lerpP32(SP32[from], SP32[to], k), 0);
    };
  }
  var springToSummer26 = makeTransition29(0);
  var summerToAutumn26 = makeTransition29(1);
  var autumnToWinter26 = makeTransition29(2);
  var VARIANTS34 = {
    Spring: { draw: draw22("Spring"), anim: anim22("Spring") },
    Summer: { draw: draw22("Summer"), anim: anim22("Summer") },
    Autumn: { draw: draw22("Autumn"), anim: anim22("Autumn") },
    Winter: { draw: draw22("Winter"), anim: anim22("Winter") }
  };
  var TRANSITIONS34 = {
    0: springToSummer26,
    1: summerToAutumn26,
    2: autumnToWinter26
  };

  // src/textures/seasonal/bird/hen.ts
  function clamp0134(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother33 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp38(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB26(a, b, t) {
    return [lerp38(a[0], b[0], t), lerp38(a[1], b[1], t), lerp38(a[2], b[2], t)];
  }
  function rgb33(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba21(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0134(a)})`;
  }
  function lerpP33(a, b, t) {
    return {
      bodyLight: lerpRGB26(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB26(a.bodyMid, b.bodyMid, t),
      bodyDark: lerpRGB26(a.bodyDark, b.bodyDark, t),
      wing: lerpRGB26(a.wing, b.wing, t),
      wingDark: lerpRGB26(a.wingDark, b.wingDark, t),
      comb: lerpRGB26(a.comb, b.comb, t),
      beak: lerpRGB26(a.beak, b.beak, t),
      egg: lerpRGB26(a.egg, b.egg, t),
      straw: lerpRGB26(a.straw, b.straw, t),
      strawDark: lerpRGB26(a.strawDark, b.strawDark, t),
      padGrass: lerpRGB26(a.padGrass, b.padGrass, t),
      padDark: lerpRGB26(a.padDark, b.padDark, t),
      outline: lerpRGB26(a.outline, b.outline, t),
      light: lerpRGB26(a.light, b.light, t),
      lightAmt: lerp38(a.lightAmt, b.lightAmt, t),
      fluff: lerp38(a.fluff, b.fluff, t),
      sheen: lerp38(a.sheen, b.sheen, t),
      frostAmt: lerp38(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp38(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp38(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp38(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp38(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP22(p) {
    return {
      ...p,
      lightAmt: clamp0134(p.lightAmt),
      fluff: clamp0134(p.fluff),
      sheen: clamp0134(p.sheen),
      frostAmt: clamp0134(p.frostAmt),
      snowCapAmt: clamp0134(p.snowCapAmt),
      padSnowAmt: clamp0134(p.padSnowAmt),
      blossomAmt: clamp0134(p.blossomAmt),
      fallenLeafAmt: clamp0134(p.fallenLeafAmt)
    };
  }
  var SP33 = {
    // Spring — soft fresh plumage, fresh nest; dewy lime pad + blossom; cool-bright.
    Spring: {
      bodyLight: [250, 248, 242],
      bodyMid: [232, 226, 214],
      bodyDark: [196, 186, 170],
      wing: [176, 122, 72],
      wingDark: [128, 84, 48],
      comb: [214, 60, 56],
      beak: [240, 168, 56],
      egg: [248, 240, 222],
      straw: [216, 188, 116],
      strawDark: [160, 130, 70],
      padGrass: [130, 208, 88],
      padDark: [74, 140, 60],
      outline: [70, 54, 38],
      light: [232, 244, 226],
      lightAmt: 0.16,
      fluff: 0.42,
      sheen: 0.12,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — full plumage PEAK, golden straw nest; mid-green pad; warm light, sheen.
    Summer: {
      bodyLight: [255, 253, 248],
      bodyMid: [240, 232, 218],
      bodyDark: [202, 188, 166],
      wing: [192, 130, 70],
      wingDark: [138, 86, 44],
      comb: [226, 48, 44],
      beak: [248, 172, 48],
      egg: [250, 242, 220],
      straw: [236, 202, 110],
      strawDark: [176, 138, 64],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      outline: [74, 52, 32],
      light: [255, 240, 206],
      lightAmt: 0.18,
      fluff: 0.5,
      sheen: 0.85,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fuller feathers, straw drier/amber; olive-tan pad + fallen leaves.
    Autumn: {
      bodyLight: [248, 242, 230],
      bodyMid: [230, 218, 198],
      bodyDark: [192, 172, 146],
      wing: [180, 112, 56],
      wingDark: [126, 76, 36],
      comb: [206, 56, 46],
      beak: [236, 158, 50],
      egg: [246, 236, 214],
      straw: [206, 170, 92],
      strawDark: [150, 116, 56],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      outline: [66, 48, 30],
      light: [248, 210, 150],
      lightAmt: 0.2,
      fluff: 0.6,
      sheen: 0.25,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — thickly fluffed hen, snow-rimmed nest + snow on the back; snowy pad,
    // cool light. Hen + straw stay clearly visible in their own colour underneath.
    Winter: {
      bodyLight: [248, 250, 254],
      bodyMid: [224, 226, 234],
      bodyDark: [184, 190, 204],
      wing: [166, 116, 72],
      wingDark: [120, 80, 50],
      comb: [196, 72, 70],
      beak: [228, 158, 66],
      egg: [240, 238, 234],
      straw: [196, 172, 116],
      strawDark: [142, 120, 78],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      outline: [58, 50, 56],
      light: [206, 226, 252],
      lightAmt: 0.3,
      fluff: 1,
      sheen: 0.18,
      frostAmt: 0.7,
      snowCapAmt: 0.9,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var NEST_CY = 13;
  var NEST_RX = 15;
  var NEST_RY = 5.4;
  var BODY_CY3 = 2;
  function henBodyPath2(ctx, bob, fluff) {
    const cy = BODY_CY3 + bob;
    const rx = 13.5 + fluff * 1.6;
    const ry = 11.5 + fluff * 0.8;
    ctx.beginPath();
    ctx.moveTo(-rx * 0.55, cy - ry * 0.9);
    ctx.quadraticCurveTo(rx * 0.2, cy - ry * 1.18, rx * 0.86, cy - ry * 0.78);
    ctx.quadraticCurveTo(rx * 1.18, cy - ry * 0.2, rx * 0.96, cy + ry * 0.5);
    ctx.quadraticCurveTo(rx * 0.8, cy + ry * 1, rx * 0.2, cy + ry * 1.02);
    ctx.quadraticCurveTo(-rx * 0.4, cy + ry * 1.06, -rx * 0.86, cy + ry * 0.6);
    ctx.quadraticCurveTo(-rx * 1.16, cy + ry * 0.1, -rx * 0.92, cy - ry * 0.5);
    ctx.quadraticCurveTo(-rx * 0.82, cy - ry * 0.95, -rx * 0.55, cy - ry * 0.9);
    ctx.closePath();
  }
  function tailPath(ctx, bob, fluff) {
    const cy = BODY_CY3 + bob;
    const tx = 12 + fluff * 0.6;
    const ty = cy - 8.5;
    ctx.beginPath();
    ctx.moveTo(tx - 2, ty + 5);
    ctx.quadraticCurveTo(tx + 5, ty - 3, tx + 7.5, ty - 8.5);
    ctx.quadraticCurveTo(tx + 9, ty - 4, tx + 6, ty + 1);
    ctx.quadraticCurveTo(tx + 4.5, ty + 4.5, tx - 2, ty + 5);
    ctx.closePath();
  }
  function paint33(ctx, raw, bob) {
    const p = clampP22(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba21(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb33(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba21([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba21([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba21([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba21([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba21(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba21([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-13, 19.6, -0.5, [196, 120, 40]],
          [13, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba21(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba21([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba21(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(3, NEST_CY + 6, 16, 3.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.strawDark);
      ctx.beginPath();
      ctx.ellipse(0, NEST_CY + 1.4, NEST_RX, NEST_RY + 1.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.straw);
      ctx.beginPath();
      ctx.ellipse(0, NEST_CY, NEST_RX, NEST_RY, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.lineWidth = 1.1;
      for (let i = -8; i <= 8; i++) {
        const ang = i / 9 * Math.PI;
        const sx = Math.cos(ang) * NEST_RX * 0.96;
        const sy = NEST_CY + Math.sin(ang) * NEST_RY * 0.9;
        ctx.strokeStyle = rgb33(i % 2 === 0 ? p.strawDark : p.straw);
        ctx.beginPath();
        ctx.moveTo(sx, sy);
        ctx.lineTo(sx + Math.cos(ang) * 2.6, sy + Math.sin(ang) * 1.6 - 1.2);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba21([255, 244, 200], 0.5);
      ctx.lineWidth = 0.9;
      [[-9, NEST_CY + 1], [6, NEST_CY + 0.6], [11, NEST_CY + 1.4]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.moveTo(sx, sy);
        ctx.lineTo(sx + 2.4, sy - 1.2);
        ctx.stroke();
      });
      const eggX = -7.5;
      const eggY = NEST_CY + 1.2;
      ctx.fillStyle = rgb33(p.outline);
      ctx.beginPath();
      ctx.ellipse(eggX, eggY, 4.4, 5.4, -0.15, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.egg);
      ctx.beginPath();
      ctx.ellipse(eggX, eggY - 0.2, 3.6, 4.6, -0.15, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba21(p.strawDark, 0.3);
      ctx.beginPath();
      ctx.ellipse(eggX + 1.4, eggY + 0.8, 2, 3, -0.15, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.7)";
      ctx.beginPath();
      ctx.ellipse(eggX - 1.3, eggY - 2, 1, 1.6, -0.3, 0, Math.PI * 2);
      ctx.fill();
      tailPath(ctx, bob, p.fluff);
      ctx.fillStyle = rgb33(p.outline);
      ctx.fill();
      ctx.save();
      tailPath(ctx, bob, p.fluff);
      ctx.clip();
      ctx.fillStyle = rgb33(p.bodyMid);
      ctx.fillRect(8, BODY_CY3 + bob - 22, 16, 20);
      ctx.fillStyle = rgba21(p.wing, 0.7);
      ctx.beginPath();
      ctx.ellipse(13, BODY_CY3 + bob - 8, 6, 7, -0.4, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      henBodyPath2(ctx, bob, p.fluff);
      ctx.fillStyle = rgb33(p.outline);
      ctx.fill();
      ctx.save();
      henBodyPath2(ctx, bob, p.fluff);
      ctx.clip();
      const cy = BODY_CY3 + bob;
      ctx.fillStyle = rgb33(p.bodyMid);
      ctx.fillRect(-20, cy - 16, 40, 32);
      const litGrad = ctx.createLinearGradient(-14, cy - 12, 12, cy + 12);
      litGrad.addColorStop(0, rgb33(p.bodyLight));
      litGrad.addColorStop(0.5, rgb33(p.bodyMid));
      litGrad.addColorStop(1, rgb33(p.bodyDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-3, cy - 1, 14, 12, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.fillStyle = rgb33(p.wing);
      ctx.beginPath();
      ctx.moveTo(8.5, cy - 5);
      ctx.quadraticCurveTo(13, cy - 1, 11, cy + 6);
      ctx.quadraticCurveTo(4, cy + 9, -2, cy + 6);
      ctx.quadraticCurveTo(2, cy - 1, 8.5, cy - 5);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgba21(p.wingDark, 0.6);
      ctx.beginPath();
      ctx.ellipse(7, cy + 4, 5, 3.4, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba21(p.wingDark, 0.8);
      ctx.lineWidth = 1;
      [0, 1, 2].forEach((k) => {
        ctx.beginPath();
        ctx.moveTo(9 - k * 2.4, cy - 3 + k * 1.2);
        ctx.quadraticCurveTo(4 - k * 2, cy + 3 + k, -1 - k * 1.4, cy + 5);
        ctx.stroke();
      });
      ctx.fillStyle = rgba21(p.bodyDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(-9, cy + 7, 7, 5, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba21(p.bodyDark, 0.45);
      ctx.lineWidth = 0.9;
      const fluffN = 5 + Math.round(p.fluff * 4);
      for (let i = 0; i < fluffN; i++) {
        const fx = -11 + i / Math.max(1, fluffN - 1) * 18;
        const fy = cy + 3 + Math.sin(i * 1.7) * 1.5;
        ctx.beginPath();
        ctx.moveTo(fx, fy);
        ctx.quadraticCurveTo(fx + 1.4, fy + 2.2, fx + 2.8, fy + 0.6);
        ctx.stroke();
      }
      if (p.sheen > 0.02) {
        ctx.fillStyle = rgba21([255, 255, 255], 0.1 + 0.16 * p.sheen);
        ctx.beginPath();
        ctx.ellipse(-5, cy - 4, 6, 4, -0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba21([210, 230, 250], 0.22 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-2, cy - 6, 12, 5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba21([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-8, cy - 8],
          [-3, cy - 9],
          [3, cy - 8],
          [8, cy - 6],
          [-6, cy - 4],
          [5, cy - 3],
          [0, cy - 6]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba21([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-9, cy - 10);
        ctx.quadraticCurveTo(0, cy - 15, 9, cy - 11);
        ctx.quadraticCurveTo(4, cy - 9, 2, cy - 10.5);
        ctx.quadraticCurveTo(-2, cy - 8.5, -4, cy - 10.5);
        ctx.quadraticCurveTo(-7, cy - 9, -9, cy - 10);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba21([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(-1, cy - 10.5, 8, 1.6, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      const headX = -11;
      const headY = cy - 11;
      ctx.fillStyle = rgb33(p.outline);
      ctx.beginPath();
      ctx.moveTo(-5, cy - 7);
      ctx.quadraticCurveTo(-12, cy - 8, headX - 1, headY + 4);
      ctx.lineTo(headX + 4, headY + 5);
      ctx.quadraticCurveTo(-4, cy - 3, -1, cy - 6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb33(p.outline);
      ctx.beginPath();
      ctx.ellipse(headX, headY, 6.2, 6, -0.1, 0, Math.PI * 2);
      ctx.fill();
      const headGrad = ctx.createLinearGradient(headX - 5, headY - 5, headX + 4, headY + 5);
      headGrad.addColorStop(0, rgb33(p.bodyLight));
      headGrad.addColorStop(1, rgb33(p.bodyMid));
      ctx.fillStyle = headGrad;
      ctx.beginPath();
      ctx.ellipse(headX, headY, 5, 4.8, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.bodyMid);
      ctx.beginPath();
      ctx.moveTo(-5.5, cy - 7);
      ctx.quadraticCurveTo(-11, cy - 8, headX, headY + 3.5);
      ctx.quadraticCurveTo(-5, cy - 3.5, -2, cy - 6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb33(p.comb);
      ctx.beginPath();
      ctx.moveTo(headX - 2.5, headY - 4.6);
      ctx.quadraticCurveTo(headX - 2, headY - 8, headX, headY - 5.6);
      ctx.quadraticCurveTo(headX + 1.4, headY - 8.2, headX + 2, headY - 5.4);
      ctx.quadraticCurveTo(headX + 3.4, headY - 7.4, headX + 3.6, headY - 4.4);
      ctx.quadraticCurveTo(headX + 1, headY - 3.4, headX - 2.5, headY - 4.6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb33(p.comb);
      ctx.beginPath();
      ctx.ellipse(headX - 4.2, headY + 3.6, 1.4, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb33(p.beak);
      ctx.beginPath();
      ctx.moveTo(headX - 4.4, headY - 0.4);
      ctx.lineTo(headX - 9.4, headY + 1.6);
      ctx.lineTo(headX - 4.2, headY + 2.4);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgba21(p.outline, 0.6);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(headX - 8.6, headY + 1.5);
      ctx.lineTo(headX - 4.3, headY + 1.4);
      ctx.stroke();
      ctx.fillStyle = rgb33(p.outline);
      ctx.beginPath();
      ctx.arc(headX - 1.6, headY - 0.4, 1.25, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = "rgba(255,255,255,0.85)";
      ctx.beginPath();
      ctx.arc(headX - 2, headY - 0.9, 0.45, 0, Math.PI * 2);
      ctx.fill();
      if (p.snowCapAmt > 0.02) {
        ctx.fillStyle = rgba21([246, 251, 255], 0.9 * p.snowCapAmt);
        ctx.beginPath();
        ctx.ellipse(headX, headY - 5.2, 3.2, 1.3, -0.1, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.strokeStyle = rgba21([246, 251, 255], 0.9 * a);
        ctx.lineWidth = 2.6;
        ctx.beginPath();
        ctx.ellipse(0, NEST_CY - 0.4, NEST_RX - 0.6, NEST_RY, 0, Math.PI * 0.08, Math.PI * 0.92);
        ctx.stroke();
        ctx.fillStyle = rgba21([255, 255, 255], 0.8 * a);
        [[-12, NEST_CY + 1], [12, NEST_CY + 1], [0, NEST_CY + 3.4]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 1, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba21(p.light, p.lightAmt));
        lg.addColorStop(1, rgba21(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt33(t, amp = 0.8, w = 1.1) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw23(season) {
    return (ctx) => paint33(ctx, SP33[season], 0);
  }
  function anim23(season) {
    return (ctx, t) => {
      const bob = bobAt33(t);
      paint33(ctx, SP33[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const headX = -11;
        const headY = BODY_CY3 + bob - 11;
        const blinkPhase = t % 4.2 / 4.2;
        if (blinkPhase > 0.92) {
          const k = (blinkPhase - 0.92) / 0.08;
          const lid = Math.sin(k * Math.PI);
          ctx.fillStyle = rgb33(SP33[season].bodyMid);
          ctx.beginPath();
          ctx.ellipse(headX - 1.6, headY - 0.4, 1.5, 1.5 * lid, 0, 0, Math.PI * 2);
          ctx.fill();
        }
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = NEST_CY + bob - 1 + Math.sin(t * 1.1) * 1;
          ctx.beginPath();
          ctx.arc(4, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.35 % 1;
          const sx = lerp38(-12, 8, prog);
          ctx.fillStyle = "rgba(255,255,255,0.3)";
          ctx.beginPath();
          ctx.ellipse(sx, BODY_CY3 + bob - 6, 3, 2, -0.3, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const sway = Math.sin(t * 1.4) * 1.8;
          ctx.strokeStyle = rgb33(SP33.Autumn.strawDark);
          ctx.lineWidth = 1.1;
          ctx.beginPath();
          ctx.moveTo(11, NEST_CY - 0.5);
          ctx.quadraticCurveTo(13 + sway * 0.5, NEST_CY - 4, 14 + sway, NEST_CY - 7);
          ctx.stroke();
          ctx.strokeStyle = rgba21([236, 202, 110], 0.7);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(11, NEST_CY - 0.5);
          ctx.quadraticCurveTo(13 + sway * 0.5, NEST_CY - 4, 14 + sway, NEST_CY - 7);
          ctx.stroke();
        } else {
          const beakTipX = headX - 9.4;
          const beakTipY = headY + 1.6;
          const bp = t % 2.6 / 2.6;
          const puff = Math.sin(bp * Math.PI);
          if (puff > 0.01) {
            ctx.globalAlpha = 0.32 * puff;
            ctx.fillStyle = "rgba(232,242,255,1)";
            const px = beakTipX - 2 - bp * 4;
            const py = beakTipY + 1 + bp * 2;
            ctx.beginPath();
            ctx.arc(px, py, 1.6 + bp * 2.2, 0, Math.PI * 2);
            ctx.fill();
            ctx.beginPath();
            ctx.arc(px - 1.6, py + 0.8, 1 + bp * 1.4, 0, Math.PI * 2);
            ctx.fill();
            ctx.globalAlpha = 1;
          }
          const prog = t / 3.2 % 1;
          const fy = -22 + prog * 38;
          const fx = 6 + Math.sin(prog * Math.PI * 2) * 3;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition30(fromIdx) {
    const from = SP33[SEASON_NAMES[fromIdx]];
    const to = SP33[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother33(clamp0134(pp));
      paint33(ctx, lerpP33(from, to, k), 0);
    };
  }
  var springToSummer27 = makeTransition30(0);
  var summerToAutumn27 = makeTransition30(1);
  var autumnToWinter27 = makeTransition30(2);
  var VARIANTS35 = {
    Spring: { draw: draw23("Spring"), anim: anim23("Spring") },
    Summer: { draw: draw23("Summer"), anim: anim23("Summer") },
    Autumn: { draw: draw23("Autumn"), anim: anim23("Autumn") },
    Winter: { draw: draw23("Winter"), anim: anim23("Winter") }
  };
  var TRANSITIONS35 = {
    0: springToSummer27,
    1: summerToAutumn27,
    2: autumnToWinter27
  };

  // src/textures/seasonal/bird/turkey.ts
  function clamp0135(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp39(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB27(a, b, t) {
    return [lerp39(a[0], b[0], t), lerp39(a[1], b[1], t), lerp39(a[2], b[2], t)];
  }
  function rgb34(c, a = 1) {
    const r = Math.round(clamp0135(c[0] / 255) * 255);
    const g = Math.round(clamp0135(c[1] / 255) * 255);
    const b = Math.round(clamp0135(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0135(a)})`;
  }
  function smoother34(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt34(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP34 = {
    // Spring — bronze plumage, dewy lime pad + blossom, cool-bright light.
    Spring: {
      bodyLight: [150, 104, 54],
      bodyMid: [108, 70, 36],
      bodyDark: [66, 42, 24],
      sheen: [120, 140, 96],
      tailLight: [168, 122, 66],
      tailMid: [120, 80, 42],
      tailDark: [74, 48, 26],
      tailTip: [222, 196, 150],
      wattle: [204, 52, 46],
      beak: [206, 170, 96],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [44, 28, 18],
      lightWash: [216, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      irid: 0.32,
      fluff: 0.12,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — richest iridescent bronze (PEAK), saturated mid-green pad, warm light.
    Summer: {
      bodyLight: [178, 120, 58],
      bodyMid: [128, 78, 36],
      bodyDark: [76, 44, 22],
      sheen: [96, 168, 132],
      // strong green-gold iridescence
      tailLight: [192, 138, 70],
      tailMid: [134, 86, 42],
      tailDark: [82, 50, 26],
      tailTip: [236, 210, 158],
      wattle: [214, 44, 40],
      beak: [214, 176, 98],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [46, 28, 16],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.16,
      irid: 1,
      // PEAK sheen
      fluff: 0.2,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — warm bronze, olive-tan browning pad, fallen leaves, low amber light.
    Autumn: {
      bodyLight: [166, 110, 52],
      bodyMid: [120, 72, 34],
      bodyDark: [72, 42, 22],
      sheen: [150, 120, 70],
      // warm gold sheen
      tailLight: [186, 128, 62],
      tailMid: [128, 80, 40],
      tailDark: [78, 48, 24],
      tailTip: [230, 198, 138],
      wattle: [200, 48, 42],
      beak: [206, 168, 92],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [48, 30, 18],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      irid: 0.5,
      fluff: 0.34,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — fluffed feathers, a little snow on back/tail, breath fog; snowy
    // pad, cool blue-grey light. The bronze stays clearly visible underneath.
    Winter: {
      bodyLight: [142, 100, 56],
      bodyMid: [104, 70, 40],
      bodyDark: [66, 44, 30],
      sheen: [120, 140, 150],
      tailLight: [160, 120, 70],
      tailMid: [116, 82, 48],
      tailDark: [74, 50, 32],
      tailTip: [226, 214, 196],
      wattle: [196, 56, 54],
      beak: [200, 172, 116],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [52, 38, 34],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      irid: 0.28,
      fluff: 1,
      // extra-puffed feathers
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP34(a, b, t) {
    return {
      bodyLight: lerpRGB27(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB27(a.bodyMid, b.bodyMid, t),
      bodyDark: lerpRGB27(a.bodyDark, b.bodyDark, t),
      sheen: lerpRGB27(a.sheen, b.sheen, t),
      tailLight: lerpRGB27(a.tailLight, b.tailLight, t),
      tailMid: lerpRGB27(a.tailMid, b.tailMid, t),
      tailDark: lerpRGB27(a.tailDark, b.tailDark, t),
      tailTip: lerpRGB27(a.tailTip, b.tailTip, t),
      wattle: lerpRGB27(a.wattle, b.wattle, t),
      beak: lerpRGB27(a.beak, b.beak, t),
      padGrass: lerpRGB27(a.padGrass, b.padGrass, t),
      soil: lerpRGB27(a.soil, b.soil, t),
      outline: lerpRGB27(a.outline, b.outline, t),
      lightWash: lerpRGB27(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp39(a.lightWashAmt, b.lightWashAmt, t),
      irid: lerp39(a.irid, b.irid, t),
      fluff: lerp39(a.fluff, b.fluff, t),
      frostAmt: lerp39(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp39(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp39(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp39(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp39(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp39(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP23(p) {
    return {
      ...p,
      lightWashAmt: clamp0135(p.lightWashAmt),
      irid: clamp0135(p.irid),
      fluff: clamp0135(p.fluff),
      frostAmt: clamp0135(p.frostAmt),
      backSnowAmt: clamp0135(p.backSnowAmt),
      padSnowAmt: clamp0135(p.padSnowAmt),
      blossomAmt: clamp0135(p.blossomAmt),
      fallenLeafAmt: clamp0135(p.fallenLeafAmt),
      breathFogAmt: clamp0135(p.breathFogAmt)
    };
  }
  function paintPad3(ctx, p) {
    ctx.fillStyle = rgb34([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb34(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb34([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb34([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb34([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb34([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb34([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb34(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb34([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg2(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb34([196, 132, 96]);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb34([176, 116, 84]);
    ctx.lineWidth = 1.6;
    for (const dx of [-2, 0, 2]) {
      ctx.beginPath();
      ctx.moveTo(x, baseY);
      ctx.lineTo(x + dx, baseY + 1.6);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintTailFan(ctx, p, cx, cy, fanSpread) {
    const feathers = 11;
    const half = (feathers - 1) / 2;
    const arc = Math.PI * 0.96 * (1 + p.fluff * 0.06) + fanSpread;
    const len = 18.5 + p.fluff * 1.2;
    ctx.fillStyle = rgb34(p.outline);
    ctx.beginPath();
    ctx.ellipse(cx, cy, len * 0.92, len * 0.84, 0, Math.PI, Math.PI * 2);
    ctx.fill();
    for (let i = 0; i < feathers; i++) {
      const f = (i - half) / half;
      const ang = -Math.PI / 2 + f * (arc / 2);
      const tipX = cx + Math.cos(ang) * len;
      const tipY = cy + Math.sin(ang) * len * 0.9;
      ctx.save();
      ctx.translate(cx, cy);
      ctx.rotate(ang + Math.PI / 2);
      const fl = len;
      ctx.fillStyle = rgb34(p.tailDark);
      ctx.beginPath();
      ctx.ellipse(0, -fl * 0.5, 2.6, fl * 0.5, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb34(p.tailMid);
      ctx.beginPath();
      ctx.ellipse(0, -fl * 0.56, 2, fl * 0.42, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb34(p.tailLight, 0.92);
      ctx.beginPath();
      ctx.ellipse(-0.4, -fl * 0.62, 1.4, fl * 0.34, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb34(p.tailTip);
      ctx.beginPath();
      ctx.ellipse(0, -fl * 0.9, 1.8, 2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      void tipX;
      void tipY;
    }
    if (p.irid > 1e-3) {
      ctx.save();
      ctx.globalCompositeOperation = "soft-light";
      ctx.fillStyle = rgb34(p.sheen, 0.18 + 0.3 * p.irid);
      ctx.beginPath();
      ctx.ellipse(cx, cy - len * 0.42, len * 0.7, len * 0.36, 0, Math.PI, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb34([248, 252, 255], 0.85 * p.backSnowAmt);
      for (let i = 0; i < feathers; i += 2) {
        const f = (i - half) / half;
        const ang = -Math.PI / 2 + f * (arc / 2);
        const tipX = cx + Math.cos(ang) * len;
        const tipY = cy + Math.sin(ang) * len * 0.9;
        ctx.beginPath();
        ctx.ellipse(tipX, tipY, 2, 1.4, ang, 0, Math.PI * 2);
        ctx.fill();
      }
    }
  }
  function paintTurkey(ctx, p, bob, fanSpread) {
    const bx = 1;
    const by = 6 - bob;
    paintTailFan(ctx, p, bx + 4, by - 3, fanSpread);
    paintLeg2(ctx, p, bx - 2.5, by + 5, 19.4);
    paintLeg2(ctx, p, bx + 3.5, by + 5, 19.2);
    const vol = 1 + p.fluff * 0.12;
    const rx = 12.5 * vol;
    const ry = 10.5 * vol;
    ctx.fillStyle = rgb34(p.outline);
    ctx.beginPath();
    ctx.ellipse(bx, by, rx + 1.2, ry + 1.2, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.bodyDark);
    ctx.beginPath();
    ctx.ellipse(bx, by, rx, ry, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.bodyMid);
    ctx.beginPath();
    ctx.ellipse(bx - 1, by - 1, rx * 0.9, ry * 0.88, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.bodyLight);
    ctx.beginPath();
    ctx.ellipse(bx - 3, by - 3, rx * 0.6, ry * 0.58, -0.12, 0, Math.PI * 2);
    ctx.fill();
    if (p.fluff > 1e-3) {
      ctx.fillStyle = rgb34(p.bodyMid, 0.9);
      const lumps = 7;
      for (let i = 0; i < lumps; i++) {
        const a = Math.PI * 0.2 + i / (lumps - 1) * Math.PI * 0.9;
        const lx = bx + Math.cos(a) * rx * 0.98;
        const ly = by + Math.sin(a) * ry * 0.98;
        ctx.beginPath();
        ctx.arc(lx, ly, 1.6 + p.fluff * 1.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.irid > 1e-3) {
      ctx.save();
      ctx.globalCompositeOperation = "soft-light";
      ctx.fillStyle = rgb34(p.sheen, 0.2 + 0.34 * p.irid);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 1, rx * 0.66, ry * 0.6, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.strokeStyle = rgb34(p.bodyDark, 0.6);
    ctx.lineWidth = 1.4;
    ctx.beginPath();
    ctx.moveTo(bx + 6, by - 3);
    ctx.quadraticCurveTo(bx + 9, by + 2, bx + 5, by + 7);
    ctx.stroke();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb34([248, 252, 255], 0.9 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - ry * 0.74, rx * 0.6, 2.8, -0.12, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb34([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-7, -2],
        [-2, 3],
        [3, -1],
        [7, 3],
        [0, -4],
        [-4, -5],
        [5, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const nx = bx - 9;
    const ny = by - 5;
    const hx = nx - 4.5;
    const hy = ny - 7.5;
    ctx.strokeStyle = rgb34(p.outline);
    ctx.lineWidth = 5.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(nx, ny);
    ctx.quadraticCurveTo(nx - 4, ny - 4, hx, hy + 1.5);
    ctx.stroke();
    ctx.strokeStyle = rgb34(p.bodyMid);
    ctx.lineWidth = 3.6;
    ctx.beginPath();
    ctx.moveTo(nx, ny);
    ctx.quadraticCurveTo(nx - 4, ny - 4, hx, hy + 1.5);
    ctx.stroke();
    ctx.strokeStyle = rgb34(p.bodyLight, 0.7);
    ctx.lineWidth = 1.4;
    ctx.beginPath();
    ctx.moveTo(nx - 1.4, ny - 1);
    ctx.quadraticCurveTo(nx - 5, ny - 4.5, hx - 1, hy + 1.5);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb34(p.wattle);
    ctx.beginPath();
    ctx.ellipse(nx - 2.2, ny - 1.5, 2.2, 3.4, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.beginPath();
    ctx.ellipse(nx - 3.4, ny + 1.5, 1.5, 2.2, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34([150, 30, 28], 0.5);
    ctx.beginPath();
    ctx.ellipse(nx - 1.6, ny + 0.4, 1.1, 2.4, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 3.6, 3.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34([198, 142, 108]);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 3, 2.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34([255, 255, 255], 0.18);
    ctx.beginPath();
    ctx.ellipse(hx - 1, hy - 1, 1.3, 1.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34(p.wattle);
    ctx.beginPath();
    ctx.moveTo(hx - 2.6, hy - 1.2);
    ctx.quadraticCurveTo(hx - 5.2, hy + 0.5, hx - 4.4, hy + 3.2);
    ctx.quadraticCurveTo(hx - 3.4, hy + 1.2, hx - 2.2, hy + 0.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb34(p.beak);
    ctx.beginPath();
    ctx.moveTo(hx - 2.6, hy + 0.2);
    ctx.lineTo(hx - 5.6, hy + 1.4);
    ctx.lineTo(hx - 2.4, hy + 2.2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb34([245, 245, 240]);
    ctx.beginPath();
    ctx.arc(hx - 0.6, hy - 0.4, 1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb34([18, 16, 18]);
    ctx.beginPath();
    ctx.arc(hx - 0.7, hy - 0.3, 0.55, 0, Math.PI * 2);
    ctx.fill();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb34([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 6.5, hy + 1.8, 3, 2, 0.1, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash2(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb34(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint34(ctx, pIn, bob, fanSpread = 0) {
    const p = clampP23(pIn);
    ctx.save();
    try {
      paintPad3(ctx, p);
      paintTurkey(ctx, p, bob, fanSpread);
      paintLightWash2(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw24(season) {
    return (ctx) => paint34(ctx, SP34[season], 0, 0);
  }
  function anim24(season) {
    return (ctx, t) => {
      const p = clampP23(SP34[season]);
      const bob = bobAt34(t);
      const loop = t % 6 / 6;
      const shimmer = Math.sin(t * 1.5) * 0.5 + 0.5;
      const fanSpread = (1 - Math.cos(t * 1.5)) * 0.5 * 0.05;
      void shimmer;
      paint34(ctx, SP34[season], bob, fanSpread);
      ctx.save();
      try {
        const bx = 1;
        const by = 6 - bob;
        const nx = bx - 9;
        const ny = by - 5;
        const hx = nx - 4.5;
        const hy = ny - 7.5;
        const headGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 3;
        const headDip = headGate * 1.6;
        if (headDip > 0.01) {
          const dhy = hy + headDip;
          ctx.fillStyle = rgb34(p.outline);
          ctx.beginPath();
          ctx.ellipse(hx, dhy, 3.6, 3.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb34([198, 142, 108]);
          ctx.beginPath();
          ctx.ellipse(hx, dhy, 3, 2.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb34(p.beak);
          ctx.beginPath();
          ctx.moveTo(hx - 2.6, dhy + 0.2);
          ctx.lineTo(hx - 5.6, dhy + 1.4);
          ctx.lineTo(hx - 2.4, dhy + 2.2);
          ctx.closePath();
          ctx.fill();
          ctx.fillStyle = rgb34([18, 16, 18]);
          ctx.beginPath();
          ctx.arc(hx - 0.7, dhy - 0.3, 0.55, 0, Math.PI * 2);
          ctx.fill();
        }
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb34([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1);
          const sx = bx + 4 - 12 + s * 24;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb34(p.sheen, 0.4);
          ctx.beginPath();
          ctx.ellipse(sx, by - 11, 3.2, 8, 0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb34([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb34([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - 3 - reach, hy + 1.8, 2.4 + breathe * 1.8, 1.6 + breathe * 1.1, 0.1, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb34([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb34([206, 224, 255], 0.1 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 12, 10, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition31(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother34(clamp0135(pp));
      paint34(ctx, lerpP34(SP34[from], SP34[to], k), 0, 0);
    };
  }
  var springToSummer28 = makeTransition31(0);
  var summerToAutumn28 = makeTransition31(1);
  var autumnToWinter28 = makeTransition31(2);
  var VARIANTS36 = {
    Spring: { draw: draw24("Spring"), anim: anim24("Spring") },
    Summer: { draw: draw24("Summer"), anim: anim24("Summer") },
    Autumn: { draw: draw24("Autumn"), anim: anim24("Autumn") },
    Winter: { draw: draw24("Winter"), anim: anim24("Winter") }
  };
  var TRANSITIONS36 = {
    0: springToSummer28,
    1: summerToAutumn28,
    2: autumnToWinter28
  };

  // src/textures/seasonal/bird/goose.ts
  function clamp0136(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother35 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp40(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB28(a, b, t) {
    return [lerp40(a[0], b[0], t), lerp40(a[1], b[1], t), lerp40(a[2], b[2], t)];
  }
  function rgb35(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba22(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0136(a)})`;
  }
  function lerpP35(a, b, t) {
    return {
      bodyLight: lerpRGB28(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB28(a.bodyMid, b.bodyMid, t),
      bodyShade: lerpRGB28(a.bodyShade, b.bodyShade, t),
      bill: lerpRGB28(a.bill, b.bill, t),
      billDark: lerpRGB28(a.billDark, b.billDark, t),
      feet: lerpRGB28(a.feet, b.feet, t),
      eye: lerpRGB28(a.eye, b.eye, t),
      padGrass: lerpRGB28(a.padGrass, b.padGrass, t),
      padDark: lerpRGB28(a.padDark, b.padDark, t),
      outline: lerpRGB28(a.outline, b.outline, t),
      light: lerpRGB28(a.light, b.light, t),
      lightAmt: lerp40(a.lightAmt, b.lightAmt, t),
      plumage: lerp40(a.plumage, b.plumage, t),
      fluff: lerp40(a.fluff, b.fluff, t),
      snowCapAmt: lerp40(a.snowCapAmt, b.snowCapAmt, t),
      frostAmt: lerp40(a.frostAmt, b.frostAmt, t),
      padSnowAmt: lerp40(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp40(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp40(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP24(p) {
    return {
      ...p,
      lightAmt: clamp0136(p.lightAmt),
      plumage: clamp0136(p.plumage),
      fluff: clamp0136(p.fluff),
      snowCapAmt: clamp0136(p.snowCapAmt),
      frostAmt: clamp0136(p.frostAmt),
      padSnowAmt: clamp0136(p.padSnowAmt),
      blossomAmt: clamp0136(p.blossomAmt),
      fallenLeafAmt: clamp0136(p.fallenLeafAmt)
    };
  }
  var SP35 = {
    // Spring — clean white plumage; bright lime dewy pad + a blossom. Cool-bright.
    Spring: {
      bodyLight: [255, 255, 255],
      bodyMid: [238, 240, 242],
      bodyShade: [196, 202, 212],
      bill: [246, 150, 46],
      billDark: [196, 104, 28],
      feet: [240, 142, 44],
      eye: [40, 34, 30],
      padGrass: [128, 206, 86],
      padDark: [72, 138, 58],
      outline: [70, 70, 84],
      light: [232, 244, 226],
      lightAmt: 0.16,
      plumage: 0.85,
      fluff: 0,
      snowCapAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — brightest white PEAK; saturated mid-green pad; warm strong light.
    Summer: {
      bodyLight: [255, 255, 255],
      bodyMid: [246, 248, 250],
      bodyShade: [206, 210, 218],
      bill: [252, 158, 42],
      billDark: [206, 110, 26],
      feet: [248, 150, 40],
      eye: [38, 32, 28],
      padGrass: [86, 168, 70],
      padDark: [44, 110, 48],
      outline: [74, 70, 78],
      light: [255, 240, 206],
      lightAmt: 0.18,
      plumage: 1,
      fluff: 0,
      snowCapAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — white plumage in low amber light; olive-tan pad; fallen leaves.
    Autumn: {
      bodyLight: [255, 252, 244],
      bodyMid: [240, 234, 222],
      bodyShade: [198, 188, 178],
      bill: [240, 138, 40],
      billDark: [190, 96, 26],
      feet: [234, 132, 40],
      eye: [42, 32, 26],
      padGrass: [150, 152, 86],
      padDark: [104, 96, 52],
      outline: [78, 64, 60],
      light: [248, 210, 150],
      lightAmt: 0.2,
      plumage: 0.9,
      fluff: 0,
      snowCapAmt: 0,
      frostAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — fluffed white feathers read against COOL blue shadows (not a
    // white-out); snow on the back, frost dusting, breath-fog at the bill, snowy
    // pad. Body stays clearly white; bill stays orange.
    Winter: {
      bodyLight: [250, 252, 255],
      bodyMid: [224, 232, 244],
      bodyShade: [160, 178, 204],
      // cool blue shadow so white reads in snow
      bill: [238, 138, 48],
      billDark: [184, 96, 34],
      feet: [228, 126, 44],
      eye: [40, 38, 44],
      padGrass: [176, 196, 214],
      padDark: [120, 146, 172],
      outline: [62, 64, 86],
      light: [206, 226, 252],
      lightAmt: 0.3,
      plumage: 0.8,
      fluff: 0.85,
      snowCapAmt: 0.8,
      frostAmt: 0.6,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var BODY_CX3 = 1.5;
  var BODY_CY4 = 4.5;
  var BODY_RX3 = 13.5;
  var BODY_RY3 = 11;
  var TAIL_X = 15;
  var TAIL_Y = -1.5;
  var NECK_BASE_X = -6;
  var NECK_BASE_Y = -3;
  var HEAD_X = -13.5;
  var HEAD_Y = -14;
  var HEAD_R2 = 4.4;
  var BILL_TIP_X = -20.5;
  var BILL_TIP_Y = -11.5;
  function bodyPath2(ctx, bob) {
    ctx.beginPath();
    ctx.ellipse(BODY_CX3, BODY_CY4 + bob, BODY_RX3, BODY_RY3, -0.12, 0, Math.PI * 2);
  }
  function neckStroke(ctx, bob, width, color) {
    ctx.strokeStyle = color;
    ctx.lineWidth = width;
    ctx.beginPath();
    ctx.moveTo(NECK_BASE_X, NECK_BASE_Y + bob);
    ctx.quadraticCurveTo(-13, -6 + bob, HEAD_X, HEAD_Y + bob);
    ctx.stroke();
  }
  function paint35(ctx, raw, bob) {
    const p = clampP24(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba22(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb35(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb35(p.padGrass);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb35(p.padDark);
      ctx.lineWidth = 1.1;
      for (let i = -7; i <= 7; i++) {
        const tx = i * 2.4;
        const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
        ctx.beginPath();
        ctx.moveTo(tx, ty + 0.4);
        ctx.lineTo(tx - 0.8, ty - 2.4);
        ctx.stroke();
      }
      ctx.strokeStyle = rgba22([255, 255, 255], 0.18);
      ctx.lineWidth = 1;
      for (let i = -5; i <= 5; i += 2) {
        const tx = i * 2.6 - 2;
        ctx.beginPath();
        ctx.moveTo(tx, 18.4);
        ctx.lineTo(tx - 0.6, 16.6);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba22([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba22([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba22([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba22(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba22([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba22(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba22([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba22(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(3, 15.5 + bob * 0.5, 13, 3.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const footY = 14 + bob * 0.4;
      ctx.strokeStyle = rgb35(p.outline);
      ctx.lineWidth = 2.6;
      [-3.5, 4].forEach((fx) => {
        ctx.beginPath();
        ctx.moveTo(fx, 9 + bob);
        ctx.lineTo(fx + 0.6, footY);
        ctx.stroke();
      });
      ctx.fillStyle = rgb35(p.feet);
      [-3.5, 4].forEach((fx) => {
        ctx.beginPath();
        ctx.moveTo(fx + 0.6, footY - 1.4);
        ctx.lineTo(fx - 2.4, footY + 1.6);
        ctx.lineTo(fx + 3.4, footY + 1.6);
        ctx.closePath();
        ctx.fill();
      });
      ctx.strokeStyle = rgb35(p.feet);
      ctx.lineWidth = 2;
      [-3.5, 4].forEach((fx) => {
        ctx.beginPath();
        ctx.moveTo(fx, 9 + bob);
        ctx.lineTo(fx + 0.6, footY - 0.5);
        ctx.stroke();
      });
      ctx.fillStyle = rgb35(p.bodyShade);
      ctx.beginPath();
      ctx.moveTo(BODY_CX3 + 9, BODY_CY4 - 6 + bob);
      ctx.lineTo(TAIL_X + 3, TAIL_Y + bob);
      ctx.lineTo(BODY_CX3 + 10, BODY_CY4 + 2 + bob);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb35(p.outline);
      ctx.lineWidth = 1;
      ctx.stroke();
      neckStroke(ctx, bob, 8.6, rgb35(p.outline));
      neckStroke(ctx, bob, 6.2, rgb35(p.bodyMid));
      ctx.save();
      ctx.beginPath();
      ctx.moveTo(NECK_BASE_X, NECK_BASE_Y + bob);
      ctx.quadraticCurveTo(-13, -6 + bob, HEAD_X, HEAD_Y + bob);
      ctx.lineWidth = 6.2;
      ctx.strokeStyle = rgb35(p.bodyMid);
      ctx.stroke();
      ctx.restore();
      neckStroke(ctx, bob, 2.2, rgba22(p.bodyLight, 0.85));
      bodyPath2(ctx, bob);
      ctx.fillStyle = rgb35(p.outline);
      ctx.save();
      ctx.lineWidth = 2.4;
      ctx.strokeStyle = rgb35(p.outline);
      ctx.stroke();
      ctx.restore();
      bodyPath2(ctx, bob);
      ctx.fillStyle = rgb35(p.bodyMid);
      ctx.fill();
      ctx.save();
      bodyPath2(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgba22(p.bodyShade, 0.9);
      ctx.beginPath();
      ctx.ellipse(BODY_CX3 + 4, BODY_CY4 + 5 + bob, BODY_RX3, BODY_RY3 * 0.7, -0.1, 0, Math.PI * 2);
      ctx.fill();
      const bg = ctx.createLinearGradient(
        BODY_CX3 - BODY_RX3,
        BODY_CY4 - BODY_RY3 + bob,
        BODY_CX3 + BODY_RX3,
        BODY_CY4 + BODY_RY3 + bob
      );
      bg.addColorStop(0, rgb35(p.bodyLight));
      bg.addColorStop(0.5, rgb35(p.bodyMid));
      bg.addColorStop(1, rgb35(p.bodyShade));
      ctx.fillStyle = bg;
      ctx.globalAlpha = 0.85;
      ctx.beginPath();
      ctx.ellipse(BODY_CX3 - 3, BODY_CY4 - 2 + bob, BODY_RX3 * 0.9, BODY_RY3 * 0.85, -0.12, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba22(p.bodyShade, 0.7);
      ctx.lineWidth = 1.6;
      ctx.beginPath();
      ctx.moveTo(BODY_CX3 - 6, BODY_CY4 - 3 + bob);
      ctx.quadraticCurveTo(BODY_CX3 + 6, BODY_CY4 - 1 + bob, BODY_CX3 + 11, BODY_CY4 + 4 + bob);
      ctx.stroke();
      ctx.strokeStyle = rgba22(p.bodyShade, 0.5);
      ctx.lineWidth = 1.1;
      [3, 6].forEach((dy) => {
        ctx.beginPath();
        ctx.moveTo(BODY_CX3 - 3, BODY_CY4 + dy + bob);
        ctx.quadraticCurveTo(BODY_CX3 + 7, BODY_CY4 + dy + bob, BODY_CX3 + 11, BODY_CY4 + dy + 1 + bob);
        ctx.stroke();
      });
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba22([214, 232, 250], 0.22 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(BODY_CX3 - 2, BODY_CY4 - 5 + bob, BODY_RX3 * 0.85, BODY_RY3 * 0.45, -0.12, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba22([240, 248, 255], 0.7 * p.frostAmt);
        const speck = [
          [-6, -3],
          [-1, -5],
          [4, -4],
          [8, -1],
          [-3, -1],
          [2, -2]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(BODY_CX3 + sx, BODY_CY4 + sy + bob, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.fluff > 0.02) {
        ctx.save();
        ctx.strokeStyle = rgba22([250, 253, 255], 0.5 * p.fluff);
        ctx.lineWidth = 1.2;
        const n = 22;
        for (let i = 0; i < n; i++) {
          const ang = i / n * Math.PI * 2;
          const ax = BODY_CX3 + Math.cos(ang) * BODY_RX3;
          const ay = BODY_CY4 + bob + Math.sin(ang) * BODY_RY3;
          const ox = ax + Math.cos(ang) * (1.6 + 1.2 * Math.sin(i * 1.7));
          const oy = ay + Math.sin(ang) * (1.6 + 1.2 * Math.sin(i * 1.3));
          ctx.beginPath();
          ctx.moveTo(ax, ay);
          ctx.lineTo(ox, oy);
          ctx.stroke();
        }
        ctx.restore();
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba22([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(BODY_CX3 - 9, BODY_CY4 - 8 + bob);
        ctx.quadraticCurveTo(BODY_CX3 - 3, BODY_CY4 - 13 + bob, BODY_CX3 + 4, BODY_CY4 - 11 + bob);
        ctx.quadraticCurveTo(BODY_CX3 + 11, BODY_CY4 - 9 + bob, BODY_CX3 + 12, BODY_CY4 - 5 + bob);
        ctx.quadraticCurveTo(BODY_CX3 + 6, BODY_CY4 - 7 + bob, BODY_CX3 + 1, BODY_CY4 - 6 + bob);
        ctx.quadraticCurveTo(BODY_CX3 - 4, BODY_CY4 - 5 + bob, BODY_CX3 - 9, BODY_CY4 - 8 + bob);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba22([210, 226, 244], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(BODY_CX3 - 1, BODY_CY4 - 8 + bob, 8, 1.8, -0.12, 0, Math.PI * 2);
        ctx.fill();
      }
      const hx = HEAD_X;
      const hy = HEAD_Y + bob;
      ctx.fillStyle = rgb35(p.outline);
      ctx.beginPath();
      ctx.arc(hx, hy, HEAD_R2 + 0.9, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb35(p.bodyMid);
      ctx.beginPath();
      ctx.arc(hx, hy, HEAD_R2, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba22(p.bodyLight, 0.9);
      ctx.beginPath();
      ctx.arc(hx - 1.2, hy - 1.4, HEAD_R2 * 0.6, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba22(p.bodyShade, 0.6);
      ctx.beginPath();
      ctx.ellipse(hx + 1, hy + 1.6, HEAD_R2 * 0.7, HEAD_R2 * 0.5, 0, 0, Math.PI * 2);
      ctx.fill();
      const btx = BILL_TIP_X;
      const bty = BILL_TIP_Y + bob;
      ctx.fillStyle = rgb35(p.bill);
      ctx.beginPath();
      ctx.moveTo(hx - 2.4, hy - 1.2);
      ctx.quadraticCurveTo(btx + 2, bty - 1.5, btx, bty);
      ctx.lineTo(btx + 1.5, bty + 2.6);
      ctx.quadraticCurveTo(hx - 3.5, hy + 2.4, hx - 2.4, hy - 1.2);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb35(p.billDark);
      ctx.beginPath();
      ctx.moveTo(hx - 2.4, hy + 0.8);
      ctx.quadraticCurveTo(btx + 2, bty + 1.6, btx + 1.5, bty + 2.6);
      ctx.quadraticCurveTo(hx - 3.5, hy + 2.4, hx - 2.4, hy + 0.8);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb35(p.billDark);
      ctx.beginPath();
      ctx.arc(hx - 5.5, hy - 0.2, 0.7, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba22(p.outline, 0.7);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(hx - 2.4, hy - 1.2);
      ctx.quadraticCurveTo(btx + 2, bty - 1.5, btx, bty);
      ctx.lineTo(btx + 1.5, bty + 2.6);
      ctx.stroke();
      ctx.fillStyle = rgb35(p.eye);
      ctx.beginPath();
      ctx.arc(hx - 1.4, hy - 0.6, 1.05, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba22([255, 255, 255], 0.85);
      ctx.beginPath();
      ctx.arc(hx - 1.8, hy - 1, 0.4, 0, Math.PI * 2);
      ctx.fill();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba22(p.light, p.lightAmt));
        lg.addColorStop(1, rgba22(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt35(t, amp = 0.9, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw25(season) {
    return (ctx) => paint35(ctx, SP35[season], 0);
  }
  function anim25(season) {
    return (ctx, t) => {
      const breathe = bobAt35(t);
      const nod = 0.6 * (1 - Math.cos(t * 0.7)) * 0.5;
      paint35(ctx, SP35[season], breathe + nod);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.2 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gx = -8 + Math.sin(t * 0.8) * 5;
          ctx.beginPath();
          ctx.arc(gx, 17.5, 0.9 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.1 + 0.16 * (0.5 + 0.5 * Math.sin(t * 1.1));
          ctx.fillStyle = `rgba(255,255,240,${s})`;
          ctx.beginPath();
          ctx.ellipse(BODY_CX3 - 3, BODY_CY4 - 4 + breathe, 7, 4.5, -0.12, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const prog = (t / 4 % 1 + 1) % 1;
          const lx = -12 + prog * 4;
          const ly = 19.6 - Math.sin(prog * Math.PI) * 2;
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(-0.5 + Math.sin(t * 1.6) * 0.4);
          ctx.fillStyle = "rgba(196,120,40,0.95)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        } else {
          const cyc = (t / 2.6 % 1 + 1) % 1;
          const puff = Math.sin(cyc * Math.PI);
          if (puff > 0.01) {
            const px = BILL_TIP_X - 2 - cyc * 4;
            const py = BILL_TIP_Y - 1 + breathe - cyc * 2;
            ctx.fillStyle = `rgba(236,244,255,${0.5 * puff})`;
            ctx.beginPath();
            ctx.arc(px, py, 1.6 + cyc * 2.4, 0, Math.PI * 2);
            ctx.fill();
            ctx.fillStyle = `rgba(255,255,255,${0.3 * puff})`;
            ctx.beginPath();
            ctx.arc(px + 0.8, py + 0.4, 1 + cyc * 1.4, 0, Math.PI * 2);
            ctx.fill();
          }
          const fp = (t / 3.4 % 1 + 1) % 1;
          const fy = -22 + fp * 40;
          const fx = 7 + Math.sin(fp * Math.PI * 2) * 3.5;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(fp * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition32(fromIdx) {
    const from = SP35[SEASON_NAMES[fromIdx]];
    const to = SP35[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother35(clamp0136(pp));
      paint35(ctx, lerpP35(from, to, k), 0);
    };
  }
  var springToSummer29 = makeTransition32(0);
  var summerToAutumn29 = makeTransition32(1);
  var autumnToWinter29 = makeTransition32(2);
  var VARIANTS37 = {
    Spring: { draw: draw25("Spring"), anim: anim25("Spring") },
    Summer: { draw: draw25("Summer"), anim: anim25("Summer") },
    Autumn: { draw: draw25("Autumn"), anim: anim25("Autumn") },
    Winter: { draw: draw25("Winter"), anim: anim25("Winter") }
  };
  var TRANSITIONS37 = {
    0: springToSummer29,
    1: summerToAutumn29,
    2: autumnToWinter29
  };

  // src/textures/seasonal/bird/pheasant.ts
  function clamp0137(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp41(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB29(a, b, t) {
    return [lerp41(a[0], b[0], t), lerp41(a[1], b[1], t), lerp41(a[2], b[2], t)];
  }
  function rgb36(c, a = 1) {
    const r = Math.round(clamp0137(c[0] / 255) * 255);
    const g = Math.round(clamp0137(c[1] / 255) * 255);
    const b = Math.round(clamp0137(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0137(a)})`;
  }
  function smoother36(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt36(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP36 = {
    // Spring — bright fresh plumage, dewy lime pad + blossom, cool-bright light.
    Spring: {
      bodyLight: [206, 130, 64],
      bodyMid: [168, 96, 44],
      bodyDark: [104, 56, 26],
      headGreen: [30, 86, 60],
      headHi: [74, 150, 110],
      wattle: [206, 48, 44],
      ring: [244, 246, 240],
      beakLeg: [196, 168, 96],
      tail: [180, 132, 74],
      tailBar: [86, 56, 30],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [54, 40, 28],
      lightWash: [216, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      plumage: 0.55,
      fluff: 0.1,
      sheen: 0.3,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — PEAK: richest iridescent copper-and-green, mid-green pad, warm light.
    Summer: {
      bodyLight: [224, 138, 58],
      bodyMid: [186, 98, 38],
      bodyDark: [110, 52, 22],
      headGreen: [22, 98, 66],
      headHi: [96, 188, 138],
      wattle: [224, 42, 40],
      ring: [250, 252, 248],
      beakLeg: [206, 180, 104],
      tail: [198, 142, 76],
      tailBar: [78, 48, 26],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [50, 36, 24],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      plumage: 1,
      // peak richness
      fluff: 0.18,
      sheen: 0.85,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — warm coppery plumage, olive-tan pad, fallen leaves, low amber light.
    Autumn: {
      bodyLight: [212, 124, 50],
      bodyMid: [176, 88, 34],
      bodyDark: [104, 50, 22],
      headGreen: [30, 82, 56],
      headHi: [78, 152, 108],
      wattle: [210, 46, 38],
      ring: [240, 236, 222],
      beakLeg: [200, 168, 92],
      tail: [192, 130, 66],
      tailBar: [82, 50, 26],
      padGrass: [156, 142, 78],
      // olive-tan browning grass
      soil: [104, 84, 44],
      outline: [56, 40, 26],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      plumage: 0.75,
      fluff: 0.32,
      sheen: 0.4,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — fluffed feathers, snow on back/tail, breath fog, snowy pad, cool light.
    Winter: {
      bodyLight: [192, 122, 62],
      bodyMid: [156, 90, 44],
      bodyDark: [98, 52, 30],
      headGreen: [36, 80, 64],
      headHi: [80, 144, 116],
      wattle: [198, 60, 56],
      ring: [248, 250, 252],
      beakLeg: [192, 176, 124],
      tail: [176, 128, 78],
      tailBar: [80, 54, 36],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [62, 50, 50],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      plumage: 0.6,
      fluff: 1,
      // puffed up against the cold
      sheen: 0.25,
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP36(a, b, t) {
    return {
      bodyLight: lerpRGB29(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB29(a.bodyMid, b.bodyMid, t),
      bodyDark: lerpRGB29(a.bodyDark, b.bodyDark, t),
      headGreen: lerpRGB29(a.headGreen, b.headGreen, t),
      headHi: lerpRGB29(a.headHi, b.headHi, t),
      wattle: lerpRGB29(a.wattle, b.wattle, t),
      ring: lerpRGB29(a.ring, b.ring, t),
      beakLeg: lerpRGB29(a.beakLeg, b.beakLeg, t),
      tail: lerpRGB29(a.tail, b.tail, t),
      tailBar: lerpRGB29(a.tailBar, b.tailBar, t),
      padGrass: lerpRGB29(a.padGrass, b.padGrass, t),
      soil: lerpRGB29(a.soil, b.soil, t),
      outline: lerpRGB29(a.outline, b.outline, t),
      lightWash: lerpRGB29(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp41(a.lightWashAmt, b.lightWashAmt, t),
      plumage: lerp41(a.plumage, b.plumage, t),
      fluff: lerp41(a.fluff, b.fluff, t),
      sheen: lerp41(a.sheen, b.sheen, t),
      frostAmt: lerp41(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp41(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp41(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp41(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp41(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp41(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP25(p) {
    return {
      ...p,
      lightWashAmt: clamp0137(p.lightWashAmt),
      plumage: clamp0137(p.plumage),
      fluff: clamp0137(p.fluff),
      sheen: clamp0137(p.sheen),
      frostAmt: clamp0137(p.frostAmt),
      backSnowAmt: clamp0137(p.backSnowAmt),
      padSnowAmt: clamp0137(p.padSnowAmt),
      blossomAmt: clamp0137(p.blossomAmt),
      fallenLeafAmt: clamp0137(p.fallenLeafAmt),
      breathFogAmt: clamp0137(p.breathFogAmt)
    };
  }
  function paintPad4(ctx, p) {
    ctx.fillStyle = rgb36([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb36(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb36([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb36([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb36([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb36([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb36([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb36(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb36([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg3(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb36(p.beakLeg);
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.lineWidth = 1.1;
    for (const tx of [-1.8, 0, 1.8]) {
      ctx.beginPath();
      ctx.moveTo(x, baseY);
      ctx.lineTo(x + tx, baseY + 1.6);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintTail2(ctx, p, bx, by) {
    const rootX = bx + 9;
    const rootY = by - 2;
    const feathers = [
      // [tip x, tip y, half-width] — central longest, outer two shorter
      [22, -18, 2.2],
      [23, -13, 1.9],
      [20, -8, 1.7]
    ];
    for (const [tx, ty, hw] of feathers) {
      ctx.fillStyle = rgb36(p.outline);
      ctx.beginPath();
      ctx.moveTo(rootX, rootY + hw + 1);
      ctx.quadraticCurveTo(bx + (tx + rootX) * 0.4, by + (ty - 2), tx, ty);
      ctx.quadraticCurveTo(bx + (tx + rootX) * 0.42, by + (ty + 2), rootX, rootY - hw - 1);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb36(p.tail);
      ctx.beginPath();
      ctx.moveTo(rootX, rootY + hw);
      ctx.quadraticCurveTo(bx + (tx + rootX) * 0.4, by + (ty - 1.4), tx, ty);
      ctx.quadraticCurveTo(bx + (tx + rootX) * 0.42, by + (ty + 1.4), rootX, rootY - hw);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb36(p.tailBar, 0.85);
      ctx.lineWidth = 1.1;
      for (let s = 0.2; s < 0.95; s += 0.16) {
        const cxp = lerp41(rootX, tx, s);
        const cyp = lerp41(rootY, ty, s);
        const nx = ty - rootY;
        const ny = -(tx - rootX);
        const nlen = Math.hypot(nx, ny) || 1;
        const ux = nx / nlen * (hw - 0.4);
        const uy = ny / nlen * (hw - 0.4);
        ctx.beginPath();
        ctx.moveTo(cxp - ux, cyp - uy);
        ctx.lineTo(cxp + ux, cyp + uy);
        ctx.stroke();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.strokeStyle = rgb36([248, 252, 255], 0.85 * p.backSnowAmt);
      ctx.lineWidth = 1.6;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(rootX + 2, rootY - 2);
      ctx.quadraticCurveTo(bx + 14, by - 14, 22, -17);
      ctx.stroke();
      ctx.lineCap = "butt";
    }
  }
  function paintPheasant(ctx, p, bob) {
    const bx = -1;
    const by = 5 - bob;
    const vol = 0.92 + p.fluff * 0.16;
    const rx = 11 * vol;
    const ry = 8 * vol;
    paintTail2(ctx, p, bx, by);
    paintLeg3(ctx, p, bx + 1.5, by + 5.5, 18.8);
    paintLeg3(ctx, p, bx - 3.5, by + 5.5, 19.2);
    ctx.save();
    ctx.translate(bx, by);
    ctx.rotate(-0.12);
    ctx.fillStyle = rgb36(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, rx + 1.2, ry + 1.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.bodyMid);
    ctx.beginPath();
    ctx.ellipse(0, 0, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.bodyLight, 0.92);
    ctx.beginPath();
    ctx.ellipse(-2.4, -2.4, rx * 0.72, ry * 0.66, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.bodyDark, 0.55);
    ctx.beginPath();
    ctx.ellipse(3, 3, rx * 0.66, ry * 0.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.bodyDark, 0.7 + 0.2 * p.plumage);
    for (const [fx, fy] of [
      [-4, -1],
      [0, -3],
      [3, -1],
      [-2, 2],
      [4, 1.5],
      [1, 3],
      [-5, 2.5],
      [6, -1.5]
    ]) {
      ctx.beginPath();
      ctx.ellipse(fx, fy, 1.4, 0.9, 0.4, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb36([248, 252, 255], 0.9 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 0.5, by - ry + 1, rx * 0.78, 3, -0.12, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb36([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-4, -ry], [1, -ry - 0.6], [5, -ry + 0.6]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb36([255, 255, 255], 0.75 * p.frostAmt);
      for (const [fx, fy] of [
        [-6, -2],
        [-1, 3],
        [4, -1],
        [7, 2],
        [1, -4],
        [-4, 4]
      ]) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const nx = bx - 8.5;
    const ny = by - 1;
    const hx = bx - 12.5;
    const hy = by - 5;
    ctx.strokeStyle = rgb36(p.outline);
    ctx.lineWidth = 6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(nx + 2, ny + 1);
    ctx.lineTo(hx + 1, hy + 3);
    ctx.stroke();
    ctx.strokeStyle = rgb36(p.bodyMid);
    ctx.lineWidth = 4.4;
    ctx.beginPath();
    ctx.moveTo(nx + 2, ny + 1);
    ctx.lineTo(hx + 1, hy + 3);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.save();
    ctx.translate((nx + hx) * 0.5 + 1, (ny + hy) * 0.5 + 2);
    ctx.rotate(-0.5);
    ctx.fillStyle = rgb36(p.ring);
    ctx.beginPath();
    ctx.ellipse(0, 0, 3.4, 1.7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.outline, 0.3);
    ctx.beginPath();
    ctx.ellipse(0, 1, 3, 0.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb36(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 4.4, 4.1, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.headGreen);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 3.8, 3.5, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.headHi, 0.4 + 0.4 * p.sheen);
    ctx.beginPath();
    ctx.ellipse(hx - 1.4, hy - 1.4, 1.8, 1.5, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.wattle);
    ctx.beginPath();
    ctx.ellipse(hx - 1.6, hy + 0.6, 2.2, 1.7, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36([250, 246, 220]);
    ctx.beginPath();
    ctx.arc(hx - 1.4, hy - 0.2, 1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36([20, 16, 14]);
    ctx.beginPath();
    ctx.arc(hx - 1.6, hy - 0.1, 0.6, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb36(p.beakLeg);
    ctx.beginPath();
    ctx.moveTo(hx - 3.4, hy + 0.2);
    ctx.lineTo(hx - 7, hy + 1.4);
    ctx.lineTo(hx - 3.4, hy + 1.8);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb36(p.outline, 0.6);
    ctx.lineWidth = 0.7;
    ctx.beginPath();
    ctx.moveTo(hx - 3.4, hy + 1);
    ctx.lineTo(hx - 6.6, hy + 1.5);
    ctx.stroke();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb36([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 9, hy + 1.6, 3, 2, 0.1, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash3(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb36(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 2, 28, 28, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint36(ctx, pIn, bob) {
    const p = clampP25(pIn);
    ctx.save();
    try {
      paintPad4(ctx, p);
      paintPheasant(ctx, p, bob);
      paintLightWash3(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw26(season) {
    return (ctx) => paint36(ctx, SP36[season], 0);
  }
  function anim26(season) {
    return (ctx, t) => {
      const p = clampP25(SP36[season]);
      const bob = bobAt36(t);
      const headBob = (1 - Math.cos(t * 1.5)) * 0.5 * 0.8;
      const tailSway = Math.sin(t * 0.7) * 1.2;
      paint36(ctx, SP36[season], bob);
      ctx.save();
      try {
        const bx = -1;
        const by = 5 - bob;
        const hx = bx - 12.5;
        const hy = by - 5;
        if (Math.abs(tailSway) > 0.05) {
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb36([255, 240, 210], 0.18);
          ctx.beginPath();
          ctx.ellipse(bx + 14 + tailSway, by - 12, 2.4, 5, 0.6, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
        if (headBob > 0.02) {
          ctx.fillStyle = rgb36([0, 0, 0], 0.08);
          ctx.beginPath();
          ctx.ellipse(hx, hy + 4 + headBob, 3.4, 1.2, 0, 0, Math.PI * 2);
          ctx.fill();
        }
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb36([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.2);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb36([130, 220, 170], 0.3 + 0.3 * s);
          ctx.beginPath();
          ctx.ellipse(hx - 0.6, hy - 0.6 + headBob, 2.6, 2.4, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb36([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb36([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach - 5, hy + 1.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.1, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb36([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb36([206, 224, 255], 0.1 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 11, 8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition33(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother36(clamp0137(pp));
      paint36(ctx, lerpP36(SP36[from], SP36[to], k), 0);
    };
  }
  var springToSummer30 = makeTransition33(0);
  var summerToAutumn30 = makeTransition33(1);
  var autumnToWinter30 = makeTransition33(2);
  var VARIANTS38 = {
    Spring: { draw: draw26("Spring"), anim: anim26("Spring") },
    Summer: { draw: draw26("Summer"), anim: anim26("Summer") },
    Autumn: { draw: draw26("Autumn"), anim: anim26("Autumn") },
    Winter: { draw: draw26("Winter"), anim: anim26("Winter") }
  };
  var TRANSITIONS38 = {
    0: springToSummer30,
    1: summerToAutumn30,
    2: autumnToWinter30
  };

  // src/textures/seasonal/bird/parrot.ts
  function clamp0138(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp42(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB30(a, b, t) {
    return [lerp42(a[0], b[0], t), lerp42(a[1], b[1], t), lerp42(a[2], b[2], t)];
  }
  function rgb37(c, a = 1) {
    const r = Math.round(clamp0138(c[0] / 255) * 255);
    const g = Math.round(clamp0138(c[1] / 255) * 255);
    const b = Math.round(clamp0138(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0138(a)})`;
  }
  function smoother37(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt37(t, amp = 0.95, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP37 = {
    // Spring — bright plumage, dewy lime pad + blossom, cool-bright light.
    Spring: {
      headLight: [236, 70, 60],
      headDark: [168, 32, 36],
      wingLight: [78, 188, 88],
      wingDark: [34, 122, 58],
      tailLight: [74, 132, 230],
      tailDark: [36, 76, 168],
      beak: [236, 226, 200],
      perchLight: [156, 116, 72],
      perchDark: [104, 72, 42],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [44, 36, 40],
      lightWash: [214, 238, 255],
      // cool-bright
      lightWashAmt: 0.16,
      saturation: 0.8,
      fluff: 0.12,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — richest, most-saturated tricolour (PEAK); saturated mid-green pad.
    Summer: {
      headLight: [248, 58, 48],
      headDark: [182, 22, 28],
      wingLight: [70, 206, 84],
      wingDark: [26, 134, 56],
      tailLight: [58, 122, 244],
      tailDark: [28, 70, 188],
      beak: [244, 234, 206],
      perchLight: [150, 110, 66],
      perchDark: [98, 66, 38],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [40, 32, 36],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.16,
      saturation: 1,
      // PEAK
      fluff: 0.16,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — vivid plumage held; olive-tan pad, a couple of fallen leaves.
    Autumn: {
      headLight: [228, 64, 50],
      headDark: [166, 30, 32],
      wingLight: [82, 178, 80],
      wingDark: [38, 118, 54],
      tailLight: [70, 124, 222],
      tailDark: [34, 74, 162],
      beak: [234, 220, 188],
      perchLight: [148, 104, 60],
      perchDark: [98, 64, 36],
      padGrass: [156, 142, 78],
      // olive-tan browning grass
      soil: [104, 84, 44],
      outline: [46, 36, 36],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      saturation: 0.85,
      fluff: 0.22,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — fluffed feathers, snow on back/perch, breath-fog, snowy pad, cool light.
    Winter: {
      headLight: [220, 78, 70],
      headDark: [150, 40, 46],
      wingLight: [86, 168, 92],
      wingDark: [40, 108, 60],
      tailLight: [82, 128, 210],
      tailDark: [44, 78, 152],
      beak: [228, 224, 214],
      perchLight: [150, 130, 116],
      perchDark: [104, 86, 74],
      padGrass: [222, 232, 244],
      // snow on the pad (muted grey-green showing)
      soil: [150, 168, 190],
      outline: [54, 48, 58],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.26,
      saturation: 0.72,
      fluff: 1,
      // whimsically puffed against the cold
      frostAmt: 0.65,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP37(a, b, t) {
    return {
      headLight: lerpRGB30(a.headLight, b.headLight, t),
      headDark: lerpRGB30(a.headDark, b.headDark, t),
      wingLight: lerpRGB30(a.wingLight, b.wingLight, t),
      wingDark: lerpRGB30(a.wingDark, b.wingDark, t),
      tailLight: lerpRGB30(a.tailLight, b.tailLight, t),
      tailDark: lerpRGB30(a.tailDark, b.tailDark, t),
      beak: lerpRGB30(a.beak, b.beak, t),
      perchLight: lerpRGB30(a.perchLight, b.perchLight, t),
      perchDark: lerpRGB30(a.perchDark, b.perchDark, t),
      padGrass: lerpRGB30(a.padGrass, b.padGrass, t),
      soil: lerpRGB30(a.soil, b.soil, t),
      outline: lerpRGB30(a.outline, b.outline, t),
      lightWash: lerpRGB30(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp42(a.lightWashAmt, b.lightWashAmt, t),
      saturation: lerp42(a.saturation, b.saturation, t),
      fluff: lerp42(a.fluff, b.fluff, t),
      frostAmt: lerp42(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp42(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp42(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp42(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp42(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp42(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP26(p) {
    return {
      ...p,
      lightWashAmt: clamp0138(p.lightWashAmt),
      saturation: clamp0138(p.saturation),
      fluff: clamp0138(p.fluff),
      frostAmt: clamp0138(p.frostAmt),
      backSnowAmt: clamp0138(p.backSnowAmt),
      padSnowAmt: clamp0138(p.padSnowAmt),
      blossomAmt: clamp0138(p.blossomAmt),
      fallenLeafAmt: clamp0138(p.fallenLeafAmt),
      breathFogAmt: clamp0138(p.breathFogAmt)
    };
  }
  var PERCH_Y = 13;
  var PERCH_HALF = 16;
  var BODY_CX4 = 1;
  var BODY_CY5 = -1;
  function paintPad5(ctx, p) {
    ctx.fillStyle = rgb37([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21.5, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb37(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb37([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb37([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb37([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb37([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb37([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb37(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb37([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintPerch(ctx, p) {
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, PERCH_Y + 1, PERCH_HALF + 1.4, 3.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.perchDark);
    ctx.beginPath();
    ctx.ellipse(0, PERCH_Y + 1, PERCH_HALF, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.perchLight);
    ctx.beginPath();
    ctx.ellipse(0, PERCH_Y, PERCH_HALF, 2.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb37(p.perchDark, 0.7);
    ctx.lineWidth = 0.7;
    for (const gx of [-9, -2, 6, 12]) {
      ctx.beginPath();
      ctx.moveTo(gx, PERCH_Y - 1.4);
      ctx.lineTo(gx + 1, PERCH_Y + 1.2);
      ctx.stroke();
    }
    ctx.fillStyle = rgb37(p.perchDark);
    for (const ex of [-PERCH_HALF, PERCH_HALF]) {
      ctx.beginPath();
      ctx.ellipse(ex, PERCH_Y, 1.5, 2.1, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb37(p.perchLight, 0.5);
      ctx.beginPath();
      ctx.ellipse(ex, PERCH_Y - 0.3, 0.8, 1.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb37(p.perchDark);
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb37([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, PERCH_Y - 1.4, PERCH_HALF * 0.92, 1.5, 0, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintFoot(ctx, p, x, y) {
    ctx.strokeStyle = rgb37([70, 58, 52]);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    for (const dx of [-1.8, 0.2, 2]) {
      ctx.beginPath();
      ctx.moveTo(x, y - 1.6);
      ctx.quadraticCurveTo(x + dx, y - 0.2, x + dx * 1.3, y + 1.4);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintParrot(ctx, p, bob) {
    const cx = BODY_CX4;
    const cy = BODY_CY5 - bob;
    const puff = 1 + p.fluff * 0.18;
    paintFoot(ctx, p, cx - 3.5, PERCH_Y - 1);
    paintFoot(ctx, p, cx + 2.5, PERCH_Y - 1);
    ctx.save();
    ctx.translate(cx + 5, cy + 4);
    ctx.rotate(0.5);
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.4, 12.4 * puff, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.tailDark);
    ctx.beginPath();
    ctx.ellipse(0, 0, 3.6, 11.4 * puff, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.tailLight);
    ctx.beginPath();
    ctx.ellipse(-0.8, -1, 1.8, 9.6 * puff, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.ellipse(cx + 3.4, cy + 1, 7.4 * puff, 9.6 * puff, 0.34, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.wingDark);
    ctx.beginPath();
    ctx.ellipse(cx + 3.4, cy + 1, 6.6 * puff, 8.8 * puff, 0.34, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.wingLight);
    ctx.beginPath();
    ctx.ellipse(cx + 2.2, cy - 0.6, 4.6 * puff, 6.8 * puff, 0.34, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb37(p.wingDark, 0.85);
    ctx.lineWidth = 0.8;
    for (const k of [0, 1, 2]) {
      ctx.beginPath();
      ctx.moveTo(cx + 1.2 + k * 1.8, cy - 4 + k * 1.2);
      ctx.quadraticCurveTo(cx + 4 + k * 1.6, cy + 1 + k, cx + 4.6 + k * 1.4, cy + 6 + k * 0.6);
      ctx.stroke();
    }
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.ellipse(cx - 2.4, cy + 2, 7.4 * puff, 9.4 * puff, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.headDark);
    ctx.beginPath();
    ctx.ellipse(cx - 2.4, cy + 2, 6.6 * puff, 8.6 * puff, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.headLight);
    ctx.beginPath();
    ctx.ellipse(cx - 3.6, cy + 0.4, 4.6 * puff, 6.4 * puff, -0.12, 0, Math.PI * 2);
    ctx.fill();
    const hx = cx - 5;
    const hy = cy - 7;
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.arc(hx, hy, 6.1 * puff, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.headDark);
    ctx.beginPath();
    ctx.arc(hx, hy, 5.4 * puff, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37(p.headLight);
    ctx.beginPath();
    ctx.arc(hx - 1.4, hy - 1.6, 3.8 * puff, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37([244, 238, 228]);
    ctx.beginPath();
    ctx.ellipse(hx - 1.2, hy + 0.2, 3, 2.6, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb37([200, 120, 110], 0.6);
    ctx.lineWidth = 0.5;
    for (const ly of [-0.6, 0.8]) {
      ctx.beginPath();
      ctx.moveTo(hx - 3.4, hy + ly);
      ctx.lineTo(hx + 0.4, hy + ly + 0.3);
      ctx.stroke();
    }
    ctx.fillStyle = rgb37([250, 248, 240]);
    ctx.beginPath();
    ctx.arc(hx - 1.2, hy - 0.4, 1.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37([24, 20, 22]);
    ctx.beginPath();
    ctx.arc(hx - 1, hy - 0.3, 0.85, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb37([255, 255, 255], 0.9);
    ctx.beginPath();
    ctx.arc(hx - 1.4, hy - 0.7, 0.32, 0, Math.PI * 2);
    ctx.fill();
    const bxk = hx - 4.4;
    const byk = hy + 1.6;
    ctx.fillStyle = rgb37(p.outline);
    ctx.beginPath();
    ctx.moveTo(bxk + 3.2, byk - 2.6);
    ctx.quadraticCurveTo(bxk - 2.8, byk - 1.8, bxk - 2.6, byk + 2.2);
    ctx.quadraticCurveTo(bxk - 1.6, byk + 4.6, bxk + 1, byk + 3.2);
    ctx.quadraticCurveTo(bxk + 2.6, byk + 1.4, bxk + 3.6, byk - 0.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb37(p.beak);
    ctx.beginPath();
    ctx.moveTo(bxk + 2.8, byk - 2);
    ctx.quadraticCurveTo(bxk - 2, byk - 1.2, bxk - 1.8, byk + 1.8);
    ctx.quadraticCurveTo(bxk - 1, byk + 3.6, bxk + 0.8, byk + 2.4);
    ctx.quadraticCurveTo(bxk + 2, byk + 1, bxk + 3, byk - 0.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb37(p.outline, 0.5);
    ctx.beginPath();
    ctx.moveTo(bxk - 1.4, byk + 1.2);
    ctx.quadraticCurveTo(bxk - 0.6, byk + 3, bxk + 0.8, byk + 2.2);
    ctx.lineTo(bxk + 0.2, byk + 1.2);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb37([60, 48, 44]);
    ctx.beginPath();
    ctx.arc(bxk + 1.8, byk - 1, 0.5, 0, Math.PI * 2);
    ctx.fill();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb37([248, 252, 255], 0.9 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 0.6, hy - 4.2, 4 * puff, 1.8, -0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.ellipse(cx + 3.4, cy - 5, 5.2 * puff, 1.8, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb37([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [cx - 4, cy - 2],
        [cx + 2, cy - 3],
        [cx + 5, cy + 2],
        [hx - 2, hy + 3],
        [cx + 4, cy + 6],
        [cx - 6, cy + 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(fx, fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb37([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(bxk - 3.6, byk + 1.4, 3, 2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash4(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb37(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 2, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paintSaturationGlaze(ctx, p) {
    if (p.saturation <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb37([255, 246, 232], 0.06 * p.saturation);
    ctx.beginPath();
    ctx.ellipse(BODY_CX4 - 2, BODY_CY5, 12, 14, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint37(ctx, pIn, bob) {
    const p = clampP26(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad5(ctx, p);
      paintPerch(ctx, p);
      paintParrot(ctx, p, bob);
      paintSaturationGlaze(ctx, p);
      paintLightWash4(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw27(season) {
    return (ctx) => paint37(ctx, SP37[season], 0);
  }
  function anim27(season) {
    return (ctx, t) => {
      const p = clampP26(SP37[season]);
      const bob = bobAt37(t);
      paint37(ctx, SP37[season], bob);
      ctx.save();
      try {
        const cx = BODY_CX4;
        const cy = BODY_CY5 - bob;
        const hx = cx - 5;
        const hy = cy - 7;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 8) * flickGate;
        if (flickGate > 0.02) {
          ctx.save();
          ctx.translate(hx, hy);
          ctx.rotate(flick * 0.12);
          ctx.fillStyle = rgb37(p.headLight, 0.55);
          ctx.beginPath();
          ctx.arc(-1.4, -1.6, 3.6 * (1 + p.fluff * 0.18), 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
        ctx.save();
        ctx.translate(cx + 5, cy + 4);
        ctx.rotate(0.5 + flick * 0.16);
        ctx.fillStyle = rgb37(p.tailLight, 0.45 * flickGate);
        ctx.beginPath();
        ctx.ellipse(-0.8, -1, 1.6, 9.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb37([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = cx - 8 + s * 16;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb37([255, 255, 255], 0.34);
          ctx.beginPath();
          ctx.ellipse(sx, cy, 3, 8, 0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb37([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const bxk = hx - 4.4;
          const byk = hy + 1.6;
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 3 + breathe * 3;
          ctx.fillStyle = rgb37([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(bxk - reach, byk + 1.4, 2.4 + breathe * 1.8, 1.6 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb37([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb37([206, 224, 255], 0.1 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(cx - 2, cy, 11, 13, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition34(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother37(clamp0138(pp));
      paint37(ctx, lerpP37(SP37[from], SP37[to], k), 0);
    };
  }
  var springToSummer31 = makeTransition34(0);
  var summerToAutumn31 = makeTransition34(1);
  var autumnToWinter31 = makeTransition34(2);
  var VARIANTS39 = {
    Spring: { draw: draw27("Spring"), anim: anim27("Spring") },
    Summer: { draw: draw27("Summer"), anim: anim27("Summer") },
    Autumn: { draw: draw27("Autumn"), anim: anim27("Autumn") },
    Winter: { draw: draw27("Winter"), anim: anim27("Winter") }
  };
  var TRANSITIONS39 = {
    0: springToSummer31,
    1: summerToAutumn31,
    2: autumnToWinter31
  };

  // src/textures/seasonal/bird/phoenix.ts
  function clamp0139(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp43(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB31(a, b, t) {
    return [lerp43(a[0], b[0], t), lerp43(a[1], b[1], t), lerp43(a[2], b[2], t)];
  }
  function rgb38(c, a = 1) {
    const r = Math.round(clamp0139(c[0] / 255) * 255);
    const g = Math.round(clamp0139(c[1] / 255) * 255);
    const b = Math.round(clamp0139(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0139(a)})`;
  }
  function smoother38(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt38(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP38 = {
    // Spring — fiery plumage; dewy bright-lime pad + blossom; a few ember sparks.
    Spring: {
      plumeLight: [255, 214, 96],
      plumeMid: [248, 150, 40],
      plumeDeep: [206, 70, 24],
      crest: [255, 176, 56],
      beakLeg: [196, 132, 60],
      emberGlow: [255, 168, 70],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [82, 36, 22],
      lightWash: [220, 240, 255],
      // cool-bright
      lightWashAmt: 0.14,
      fireWarmth: 0.78,
      emberSparkAmt: 0.5,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      padSnowAmt: 0,
      meltRingAmt: 0,
      steamAmt: 0
    },
    // Summer — richest blazing orange-gold (PEAK); saturated mid-green pad.
    Summer: {
      plumeLight: [255, 224, 110],
      plumeMid: [255, 150, 30],
      plumeDeep: [214, 60, 18],
      crest: [255, 186, 48],
      beakLeg: [200, 134, 58],
      emberGlow: [255, 160, 60],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [86, 34, 18],
      lightWash: [255, 240, 200],
      // warm
      lightWashAmt: 0.16,
      fireWarmth: 1,
      // PEAK blaze
      emberSparkAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      padSnowAmt: 0,
      meltRingAmt: 0,
      steamAmt: 0
    },
    // Autumn — warm fiery plumage; olive-tan browning pad; a couple fallen leaves.
    Autumn: {
      plumeLight: [252, 204, 92],
      plumeMid: [244, 140, 36],
      plumeDeep: [198, 62, 22],
      crest: [250, 168, 50],
      beakLeg: [190, 126, 56],
      emberGlow: [252, 158, 64],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [80, 36, 20],
      lightWash: [255, 200, 130],
      // low amber
      lightWashAmt: 0.2,
      fireWarmth: 0.82,
      emberSparkAmt: 0.6,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      padSnowAmt: 0,
      meltRingAmt: 0,
      steamAmt: 0
    },
    // Winter — cold blue-grey light; pad snow-covered FURTHER OUT, but the bird's
    // heat melts a wet dark MELT RING around it with rising steam. Fire still warm.
    Winter: {
      plumeLight: [252, 206, 96],
      plumeMid: [246, 142, 36],
      plumeDeep: [202, 64, 22],
      crest: [250, 170, 52],
      beakLeg: [188, 128, 64],
      emberGlow: [255, 156, 66],
      padGrass: [206, 222, 238],
      // muted grey-green under snow
      soil: [140, 158, 180],
      outline: [70, 52, 56],
      lightWash: [200, 220, 250],
      // cool blue-grey
      lightWashAmt: 0.26,
      fireWarmth: 0.8,
      // still warm — NOT dimmed, NOT flashed
      emberSparkAmt: 0.45,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      padSnowAmt: 0.85,
      meltRingAmt: 0.9,
      steamAmt: 0.75
    }
  };
  function lerpP38(a, b, t) {
    return {
      plumeLight: lerpRGB31(a.plumeLight, b.plumeLight, t),
      plumeMid: lerpRGB31(a.plumeMid, b.plumeMid, t),
      plumeDeep: lerpRGB31(a.plumeDeep, b.plumeDeep, t),
      crest: lerpRGB31(a.crest, b.crest, t),
      beakLeg: lerpRGB31(a.beakLeg, b.beakLeg, t),
      emberGlow: lerpRGB31(a.emberGlow, b.emberGlow, t),
      padGrass: lerpRGB31(a.padGrass, b.padGrass, t),
      soil: lerpRGB31(a.soil, b.soil, t),
      outline: lerpRGB31(a.outline, b.outline, t),
      lightWash: lerpRGB31(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp43(a.lightWashAmt, b.lightWashAmt, t),
      fireWarmth: lerp43(a.fireWarmth, b.fireWarmth, t),
      emberSparkAmt: lerp43(a.emberSparkAmt, b.emberSparkAmt, t),
      blossomAmt: lerp43(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp43(a.fallenLeafAmt, b.fallenLeafAmt, t),
      padSnowAmt: lerp43(a.padSnowAmt, b.padSnowAmt, t),
      meltRingAmt: lerp43(a.meltRingAmt, b.meltRingAmt, t),
      steamAmt: lerp43(a.steamAmt, b.steamAmt, t)
    };
  }
  function clampP27(p) {
    return {
      ...p,
      lightWashAmt: clamp0139(p.lightWashAmt),
      fireWarmth: clamp0139(p.fireWarmth),
      emberSparkAmt: clamp0139(p.emberSparkAmt),
      blossomAmt: clamp0139(p.blossomAmt),
      fallenLeafAmt: clamp0139(p.fallenLeafAmt),
      padSnowAmt: clamp0139(p.padSnowAmt),
      meltRingAmt: clamp0139(p.meltRingAmt),
      steamAmt: clamp0139(p.steamAmt)
    };
  }
  function paintPad6(ctx, p) {
    ctx.fillStyle = rgb38([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb38(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb38([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb38([244, 250, 255], 0.88 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17.4, 4.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb38([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-13, 18.6], [13, 18.2], [-9, 20.4], [11, 20.6]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.meltRingAmt > 1e-3) {
      const a = p.meltRingAmt;
      ctx.fillStyle = rgb38([54, 46, 50], 0.62 * a);
      ctx.beginPath();
      ctx.ellipse(0, 19.2, 11.5, 3.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb38([40, 34, 40], 0.55 * a);
      ctx.beginPath();
      ctx.ellipse(0, 19.4, 8.2, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb38([255, 170, 80], 0.22 * a);
      ctx.beginPath();
      ctx.ellipse(-1.5, 18.8, 6, 1.7, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb38([226, 238, 250], 0.5 * a);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.ellipse(0, 19.2, 11.5, 3.6, 0, 0, Math.PI * 2);
      ctx.stroke();
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb38([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb38([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [1, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb38(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb38([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintEmberGlow(ctx, p, cx, cy) {
    ctx.save();
    ctx.globalCompositeOperation = "lighter";
    const r = 17;
    const g = ctx.createRadialGradient(cx, cy, 1, cx, cy, r);
    const a = 0.16 + 0.07 * p.fireWarmth;
    g.addColorStop(0, rgb38(p.emberGlow, a));
    g.addColorStop(0.55, rgb38(p.emberGlow, a * 0.45));
    g.addColorStop(1, rgb38(p.emberGlow, 0));
    ctx.fillStyle = g;
    ctx.beginPath();
    ctx.ellipse(cx, cy, r, r * 0.92, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paintLeg4(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb38(p.beakLeg);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.lineWidth = 1.3;
    for (const dx of [-1.8, 0, 1.8]) {
      ctx.beginPath();
      ctx.moveTo(x, baseY);
      ctx.lineTo(x + dx, baseY + 1.4);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintTail3(ctx, p, bx, by, flick) {
    const rootX = bx + 7;
    const rootY = by + 3;
    const plumes = [
      [-0.1, 22, 5.2, p.plumeDeep],
      [0.18, 26, 5.6, p.plumeMid],
      [0.42, 23, 4.8, p.crest],
      [0.66, 18, 3.8, p.plumeLight]
    ];
    plumes.forEach(([ang, len, wid, col], i) => {
      const f = flick * (0.5 + i * 0.22);
      const dirX = Math.cos(ang);
      const tipX = rootX + dirX * len + f;
      const tipY = rootY - Math.cos(ang * 0.6) * len * 0.9 - len * 0.18;
      const ctrlX = rootX + dirX * len * 0.45 + f * 0.5;
      const ctrlY = rootY - len * 0.55;
      ctx.fillStyle = rgb38(col);
      ctx.beginPath();
      ctx.moveTo(rootX - wid * 0.5, rootY);
      ctx.quadraticCurveTo(ctrlX - wid * 0.5, ctrlY, tipX, tipY);
      ctx.quadraticCurveTo(ctrlX + wid * 0.6, ctrlY + 1.5, rootX + wid * 0.5, rootY + 1);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb38(p.plumeLight, 0.5);
      ctx.lineWidth = 1.1;
      ctx.beginPath();
      ctx.moveTo(rootX, rootY);
      ctx.quadraticCurveTo(ctrlX, ctrlY, tipX - f * 0.3, tipY + 1.5);
      ctx.stroke();
    });
  }
  function paintPhoenix(ctx, p, bob, tailFlick) {
    const bx = -2;
    const by = 4 - bob;
    paintEmberGlow(ctx, p, bx, by - 1);
    paintLeg4(ctx, p, bx + 1.5, by + 7, 18.6);
    paintLeg4(ctx, p, bx - 2.5, by + 7, 19);
    paintTail3(ctx, p, bx, by, tailFlick);
    ctx.fillStyle = rgb38(p.outline);
    ctx.beginPath();
    ctx.ellipse(bx, by, 9.2, 10.4, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.plumeDeep);
    ctx.beginPath();
    ctx.ellipse(bx, by + 0.4, 8.4, 9.6, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.plumeMid);
    ctx.beginPath();
    ctx.ellipse(bx - 0.6, by - 0.6, 7.6, 8.8, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.plumeLight);
    ctx.beginPath();
    ctx.ellipse(bx - 2.4, by - 3.2, 4.8, 5.4, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(bx + 2.2, by - 0.5);
    ctx.rotate(-0.18);
    ctx.fillStyle = rgb38(p.plumeDeep);
    ctx.beginPath();
    ctx.ellipse(0, 0, 5.6, 7.2, 0, 0, Math.PI * 2);
    ctx.fill();
    for (let row = 0; row < 3; row++) {
      const col = row === 2 ? p.crest : p.plumeMid;
      ctx.fillStyle = rgb38(col);
      for (let i = 0; i < 3; i++) {
        const fx = -2 + i * 2.2;
        const fy = -3 + row * 3.2;
        ctx.beginPath();
        ctx.ellipse(fx, fy, 2, 3.4, 0.5, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.restore();
    ctx.fillStyle = rgb38(p.plumeLight, 0.5);
    for (const [fx, fy] of [[-4.2, -1], [-3.6, 2], [-3.2, 5]]) {
      ctx.beginPath();
      ctx.ellipse(bx + fx, by + fy, 1.8, 1.2, -0.3, 0, Math.PI * 2);
      ctx.fill();
    }
    const hx = bx - 6.5;
    const hy = by - 8.5;
    ctx.strokeStyle = rgb38(p.plumeMid);
    ctx.lineWidth = 5.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx - 2, by - 3);
    ctx.quadraticCurveTo(bx - 5, by - 7, hx + 0.5, hy + 2.5);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb38(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 4.6, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.plumeMid);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 4, 3.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38(p.plumeLight);
    ctx.beginPath();
    ctx.ellipse(hx - 1.4, hy - 1.4, 2.2, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    for (const [cx2, cy2, len, ang] of [
      [hx + 1.2, hy - 3.4, 6, -0.2],
      [hx + 2.6, hy - 2.6, 5, 0.15],
      [hx - 0.2, hy - 3.2, 4.5, -0.5]
    ]) {
      ctx.fillStyle = rgb38(p.crest);
      ctx.beginPath();
      ctx.moveTo(cx2 - 1.1, cy2 + 1);
      ctx.quadraticCurveTo(cx2 + Math.sin(ang) * len * 0.5, cy2 - len * 0.6, cx2 + Math.sin(ang) * len, cy2 - len);
      ctx.quadraticCurveTo(cx2 + 1, cy2 - len * 0.4, cx2 + 1.1, cy2 + 1);
      ctx.closePath();
      ctx.fill();
    }
    ctx.fillStyle = rgb38(p.plumeLight, 0.7);
    for (const [tx, ty] of [[hx + 1.2 + Math.sin(-0.2) * 6, hy - 3.4 - 6], [hx + 2.6 + Math.sin(0.15) * 5, hy - 2.6 - 5]]) {
      ctx.beginPath();
      ctx.arc(tx, ty, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb38(p.beakLeg);
    ctx.beginPath();
    ctx.moveTo(hx - 3.4, hy + 0.4);
    ctx.lineTo(hx - 6.4, hy + 1.8);
    ctx.lineTo(hx - 3.2, hy + 2.4);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb38([28, 18, 14]);
    ctx.beginPath();
    ctx.arc(hx - 1.2, hy - 0.2, 1.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb38([255, 236, 190], 0.9);
    ctx.beginPath();
    ctx.arc(hx - 1.6, hy - 0.7, 0.45, 0, Math.PI * 2);
    ctx.fill();
  }
  function paintEmberSparks(ctx, p, bx, by, rise) {
    if (p.emberSparkAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "lighter";
    const seeds = [
      [-7, 0, 1],
      [5, 0.35, 0.9],
      [9, 0.62, 0.8],
      [-2, 0.8, 1],
      [2, 0.18, 0.7]
    ];
    const n = Math.max(1, Math.round(seeds.length * p.emberSparkAmt));
    for (let i = 0; i < n; i++) {
      const [sx, phase, sz] = seeds[i];
      const prog = ((rise + phase) % 1 + 1) % 1;
      const ey = by - 2 - prog * 20;
      const ex = bx + sx + Math.sin(prog * Math.PI * 2 + phase * 6) * 2.4;
      const fade = Math.sin(prog * Math.PI);
      ctx.fillStyle = rgb38([255, 196, 96], 0.85 * fade * p.emberSparkAmt);
      ctx.beginPath();
      ctx.arc(ex, ey, sz * (0.7 + 0.5 * fade), 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  }
  function paintSteam(ctx, p, drift) {
    if (p.steamAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "screen";
    const seeds = [
      [-7, 0],
      [6, 0.4],
      [0, 0.7]
    ];
    for (const [sx, phase] of seeds) {
      const prog = ((drift + phase) % 1 + 1) % 1;
      const wy = 17 - prog * 16;
      const wx = sx + Math.sin(prog * Math.PI * 2 + phase * 5) * 3;
      const fade = Math.sin(prog * Math.PI);
      ctx.fillStyle = rgb38([232, 240, 248], 0.34 * fade * p.steamAmt);
      ctx.beginPath();
      ctx.ellipse(wx, wy, 2.6 + prog * 1.6, 3.4 + prog * 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  }
  function paintLightWash5(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb38(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint38(ctx, pIn, bob, tailFlick = 0, emberRise = 0, steamDrift = 0) {
    const p = clampP27(pIn);
    ctx.save();
    try {
      paintPad6(ctx, p);
      paintSteam(ctx, p, steamDrift);
      paintPhoenix(ctx, p, bob, tailFlick);
      paintEmberSparks(ctx, p, -2, 4 - bob, emberRise);
      paintLightWash5(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw28(season) {
    return (ctx) => paint38(ctx, SP38[season], 0, 0, 0.2, 0.2);
  }
  function anim28(season) {
    return (ctx, t) => {
      const p = clampP27(SP38[season]);
      const bob = bobAt38(t);
      const flickAmp = 1.4 + 1.6 * p.fireWarmth + (season === "Summer" ? 1 : 0);
      const tailFlick = Math.sin(t * 2.3) * flickAmp + Math.sin(t * 3.7) * (0.5 * flickAmp);
      const emberRise = t / 2.6 % 1;
      const steamDrift = t / 3.4 % 1;
      paint38(ctx, SP38[season], bob, tailFlick, emberRise, steamDrift);
      ctx.save();
      try {
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb38([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-9, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(11, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          ctx.save();
          ctx.globalCompositeOperation = "lighter";
          const pr = t / 1.8 % 1;
          const ey = -10 + 4 - pr * 14;
          const fade = Math.sin(pr * Math.PI);
          ctx.fillStyle = rgb38([255, 214, 120], 0.7 * fade);
          ctx.beginPath();
          ctx.arc(-1 + Math.sin(pr * 6) * 2, ey, 1 + fade * 0.6, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb38([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 30;
          const fxx = 13 + Math.sin(prog * Math.PI * 2) * 3;
          ctx.fillStyle = rgb38([255, 255, 255], 0.8 * (0.5 + 0.5 * Math.sin(prog * Math.PI)));
          ctx.beginPath();
          ctx.arc(fxx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition35(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother38(clamp0139(pp));
      paint38(ctx, lerpP38(SP38[from], SP38[to], k), 0, 0, 0.2, 0.2);
    };
  }
  var springToSummer32 = makeTransition35(0);
  var summerToAutumn32 = makeTransition35(1);
  var autumnToWinter32 = makeTransition35(2);
  var VARIANTS40 = {
    Spring: { draw: draw28("Spring"), anim: anim28("Spring") },
    Summer: { draw: draw28("Summer"), anim: anim28("Summer") },
    Autumn: { draw: draw28("Autumn"), anim: anim28("Autumn") },
    Winter: { draw: draw28("Winter"), anim: anim28("Winter") }
  };
  var TRANSITIONS40 = {
    0: springToSummer32,
    1: summerToAutumn32,
    2: autumnToWinter32
  };

  // src/textures/seasonal/bird/dodo.ts
  function clamp0140(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp44(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB32(a, b, t) {
    return [lerp44(a[0], b[0], t), lerp44(a[1], b[1], t), lerp44(a[2], b[2], t)];
  }
  function rgb39(c, a = 1) {
    const r = Math.round(clamp0140(c[0] / 255) * 255);
    const g = Math.round(clamp0140(c[1] / 255) * 255);
    const b = Math.round(clamp0140(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0140(a)})`;
  }
  function smoother39(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt39(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP39 = {
    // Spring — soft (lightly-desaturated) grey plumage, dewy lime pad + blossom.
    Spring: {
      bodyLight: [188, 192, 196],
      bodyMid: [150, 154, 160],
      bodyShadow: [108, 112, 120],
      faceBare: [176, 174, 170],
      beak: [206, 192, 130],
      beakDark: [150, 134, 84],
      legs: [224, 188, 86],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [62, 60, 64],
      lightWash: [216, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      plumeVolume: 0.22,
      sheenAmt: 0,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — full grey-brown plumage (PEAK, richest), mid-green pad, warm light.
    Summer: {
      bodyLight: [176, 170, 162],
      bodyMid: [138, 130, 122],
      bodyShadow: [96, 90, 86],
      faceBare: [168, 162, 154],
      beak: [212, 196, 126],
      beakDark: [152, 132, 76],
      legs: [232, 192, 80],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [56, 52, 50],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      plumeVolume: 0.42,
      sheenAmt: 0.6,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — fuller plumage, olive-tan browning pad, fallen leaves, amber light.
    Autumn: {
      bodyLight: [180, 168, 152],
      bodyMid: [142, 128, 112],
      bodyShadow: [100, 88, 76],
      faceBare: [170, 160, 148],
      beak: [206, 184, 110],
      beakDark: [148, 124, 68],
      legs: [220, 178, 70],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [58, 50, 44],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      plumeVolume: 0.6,
      sheenAmt: 0,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — fluffed feathers, cool blue-grey light, snow on the back + a breath
    // fog at the beak; snowy pad. The grey dodo stays clearly visible underneath.
    Winter: {
      bodyLight: [196, 200, 208],
      bodyMid: [150, 156, 166],
      bodyShadow: [104, 110, 124],
      faceBare: [178, 178, 182],
      beak: [200, 188, 134],
      beakDark: [146, 130, 86],
      legs: [214, 176, 84],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [70, 70, 84],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      plumeVolume: 1,
      // extra-fluffed feathers
      sheenAmt: 0,
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP39(a, b, t) {
    return {
      bodyLight: lerpRGB32(a.bodyLight, b.bodyLight, t),
      bodyMid: lerpRGB32(a.bodyMid, b.bodyMid, t),
      bodyShadow: lerpRGB32(a.bodyShadow, b.bodyShadow, t),
      faceBare: lerpRGB32(a.faceBare, b.faceBare, t),
      beak: lerpRGB32(a.beak, b.beak, t),
      beakDark: lerpRGB32(a.beakDark, b.beakDark, t),
      legs: lerpRGB32(a.legs, b.legs, t),
      padGrass: lerpRGB32(a.padGrass, b.padGrass, t),
      soil: lerpRGB32(a.soil, b.soil, t),
      outline: lerpRGB32(a.outline, b.outline, t),
      lightWash: lerpRGB32(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp44(a.lightWashAmt, b.lightWashAmt, t),
      plumeVolume: lerp44(a.plumeVolume, b.plumeVolume, t),
      sheenAmt: lerp44(a.sheenAmt, b.sheenAmt, t),
      frostAmt: lerp44(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp44(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp44(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp44(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp44(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp44(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP28(p) {
    return {
      ...p,
      lightWashAmt: clamp0140(p.lightWashAmt),
      plumeVolume: clamp0140(p.plumeVolume),
      sheenAmt: clamp0140(p.sheenAmt),
      frostAmt: clamp0140(p.frostAmt),
      backSnowAmt: clamp0140(p.backSnowAmt),
      padSnowAmt: clamp0140(p.padSnowAmt),
      blossomAmt: clamp0140(p.blossomAmt),
      fallenLeafAmt: clamp0140(p.fallenLeafAmt),
      breathFogAmt: clamp0140(p.breathFogAmt)
    };
  }
  function paintPad7(ctx, p) {
    ctx.fillStyle = rgb39([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb39(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb39([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb39([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb39([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb39([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb39([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb39(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb39([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg5(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb39(p.outline);
    ctx.lineWidth = 4.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb39(p.legs);
    ctx.lineWidth = 2.8;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb39(p.legs);
    ctx.lineWidth = 1.8;
    for (const dx of [-2.6, -0.4, 2]) {
      ctx.beginPath();
      ctx.moveTo(x, baseY);
      ctx.lineTo(x + dx, baseY + 1.4);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintBody(ctx, p, cx, cy, fill, scallop) {
    const vol = 0.9 + p.plumeVolume * 0.22;
    const rx = 11.5 * vol;
    const ry = 12.5 * vol;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
    const lumps = 13;
    const lumpR = (1.6 + p.plumeVolume * 1.4) * scallop;
    for (let i = 0; i < lumps; i++) {
      const a = i / lumps * Math.PI * 2 - Math.PI / 2;
      const lx = cx + Math.cos(a) * rx * 0.96;
      const ly = cy + Math.sin(a) * ry * 0.96;
      ctx.beginPath();
      ctx.arc(lx, ly, lumpR, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintDodo(ctx, p, bob) {
    const bx = 1;
    const by = 3 - bob;
    paintLeg5(ctx, p, bx - 3, by + 8, 19.4);
    paintLeg5(ctx, p, bx + 3.5, by + 8, 18.9);
    ctx.save();
    ctx.strokeStyle = rgb39(p.bodyLight);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    for (const [ox, oy, curl] of [[0, -2, 0.8], [1.6, 0.4, 1], [0.6, 2.2, 0.6]]) {
      ctx.beginPath();
      ctx.moveTo(bx + 9.5 + ox, by - 4 + oy);
      ctx.quadraticCurveTo(bx + 13 + ox, by - 5 + oy, bx + 13.6 + ox, by - 2 + oy + curl);
      ctx.quadraticCurveTo(bx + 14 + ox, by + 0.6 + oy, bx + 12 + ox, by + oy);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.restore();
    paintBody(ctx, p, bx, by, rgb39(p.outline), 1.16);
    paintBody(ctx, p, bx, by, rgb39(p.bodyShadow), 1);
    ctx.fillStyle = rgb39(p.bodyMid);
    ctx.beginPath();
    ctx.ellipse(bx, by, 11.2, 12, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(-1.6, -2);
    ctx.fillStyle = rgb39(p.bodyLight);
    ctx.beginPath();
    ctx.ellipse(bx, by, 8.4, 9.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.translate(bx - 8.5, by + 1.5);
    ctx.rotate(0.5);
    ctx.fillStyle = rgb39(p.bodyShadow);
    ctx.beginPath();
    ctx.ellipse(0, 0, 3.6, 2.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39(p.bodyMid);
    ctx.beginPath();
    ctx.ellipse(-0.4, -0.4, 2.8, 1.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb39([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 9.6, 8 * (0.9 + p.plumeVolume * 0.25), 3.2, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb39([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-4, -10.6], [1, -11.2], [5, -10]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.4 + p.plumeVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb39([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-6, -2],
        [-2, 4],
        [3, -4],
        [6, 1],
        [0, -6],
        [-4, 1],
        [4, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 7.5;
    const hy = by - 9.5;
    ctx.strokeStyle = rgb39(p.outline);
    ctx.lineWidth = 7;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 5);
    ctx.lineTo(hx + 1.5, hy + 3);
    ctx.stroke();
    ctx.strokeStyle = rgb39(p.bodyMid);
    ctx.lineWidth = 5;
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 5);
    ctx.lineTo(hx + 1.5, hy + 3);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb39(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 6.2, 6.6, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39(p.faceBare);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 5.4, 5.8, -0.12, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39([255, 255, 255], 0.12);
    ctx.beginPath();
    ctx.ellipse(hx - 1.6, hy - 1.8, 2.4, 2.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39([245, 245, 240]);
    ctx.beginPath();
    ctx.arc(hx - 1.6, hy - 1, 1.5, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39([22, 20, 22]);
    ctx.beginPath();
    ctx.arc(hx - 1.8, hy - 0.8, 0.85, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb39([255, 255, 255], 0.9);
    ctx.beginPath();
    ctx.arc(hx - 2.1, hy - 1.2, 0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(hx - 3.5, hy + 2.6);
    ctx.rotate(0.5);
    ctx.fillStyle = rgb39(p.outline);
    ctx.beginPath();
    ctx.moveTo(2.4, -4);
    ctx.quadraticCurveTo(-6.5, -2.4, -8.6, 3.6);
    ctx.quadraticCurveTo(-9.6, 6.8, -6.2, 7.4);
    ctx.quadraticCurveTo(-7.4, 4.6, -3, 3.2);
    ctx.quadraticCurveTo(0.6, 2.2, 2.4, -1);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb39(p.beak);
    ctx.beginPath();
    ctx.moveTo(2, -3.4);
    ctx.quadraticCurveTo(-6, -1.8, -7.9, 3.6);
    ctx.quadraticCurveTo(-8.7, 6.2, -6, 6.7);
    ctx.quadraticCurveTo(-6.9, 4.4, -2.8, 2.9);
    ctx.quadraticCurveTo(0.4, 1.9, 2, -0.8);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb39(p.beakDark);
    ctx.beginPath();
    ctx.moveTo(-5.6, 6.6);
    ctx.quadraticCurveTo(-8.6, 6, -7.6, 3.4);
    ctx.quadraticCurveTo(-6.4, 4.6, -3, 3.2);
    ctx.quadraticCurveTo(-4.2, 5.2, -5.6, 6.6);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb39([255, 255, 255], 0.32);
    ctx.lineWidth = 1;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(1.4, -2.6);
    ctx.quadraticCurveTo(-5, -1, -7, 3);
    ctx.stroke();
    ctx.strokeStyle = rgb39(p.beakDark);
    ctx.lineWidth = 0.8;
    ctx.beginPath();
    ctx.moveTo(-1.4, -0.4);
    ctx.lineTo(-3.2, 0.8);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb39([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 9, hy + 6, 3, 2.1, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash6(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb39(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint39(ctx, pIn, bob) {
    const p = clampP28(pIn);
    ctx.save();
    try {
      paintPad7(ctx, p);
      paintDodo(ctx, p, bob);
      paintLightWash6(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw29(season) {
    return (ctx) => paint39(ctx, SP39[season], 0);
  }
  function anim29(season) {
    return (ctx, t) => {
      const p = clampP28(SP39[season]);
      const bob = bobAt39(t);
      const loop = t % 5 / 5;
      const waddle = Math.sin(loop * Math.PI * 2) * (1 - bob / 1.2) * 1.4;
      const headDip = Math.max(0, Math.sin(loop * Math.PI * 4)) ** 2 * 1.3;
      ctx.save();
      ctx.translate(waddle * 0.5, 0);
      paint39(ctx, SP39[season], bob);
      ctx.restore();
      ctx.save();
      try {
        const bx = 1 + waddle * 0.5;
        const by = 3 - bob;
        const hx = bx - 7.5;
        const hy = by - 9.5 + headDip;
        if (headDip > 0.05) {
          ctx.fillStyle = rgb39(p.faceBare);
          ctx.beginPath();
          ctx.ellipse(hx, hy, 5.2, 5.6, -0.12, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb39([22, 20, 22]);
          ctx.beginPath();
          ctx.arc(hx - 1.8, hy - 0.8, 0.85, 0, Math.PI * 2);
          ctx.fill();
          ctx.save();
          ctx.translate(hx - 3.5, hy + 2.6);
          ctx.rotate(0.5);
          ctx.fillStyle = rgb39(p.beak);
          ctx.beginPath();
          ctx.moveTo(2, -3.4);
          ctx.quadraticCurveTo(-6, -1.8, -7.9, 3.6);
          ctx.quadraticCurveTo(-8.7, 6.2, -6, 6.7);
          ctx.quadraticCurveTo(-6.9, 4.4, -2.8, 2.9);
          ctx.quadraticCurveTo(0.4, 1.9, 2, -0.8);
          ctx.closePath();
          ctx.fill();
          ctx.restore();
        }
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb39([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb39([255, 255, 255], 0.3 + 0.15 * p.sheenAmt);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 8, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb39([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb39([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - 6 - reach, hy + 6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb39([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb39([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 11.5, 12.5, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition36(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother39(clamp0140(pp));
      paint39(ctx, lerpP39(SP39[from], SP39[to], k), 0);
    };
  }
  var springToSummer33 = makeTransition36(0);
  var summerToAutumn33 = makeTransition36(1);
  var autumnToWinter33 = makeTransition36(2);
  var VARIANTS41 = {
    Spring: { draw: draw29("Spring"), anim: anim29("Spring") },
    Summer: { draw: draw29("Summer"), anim: anim29("Summer") },
    Autumn: { draw: draw29("Autumn"), anim: anim29("Autumn") },
    Winter: { draw: draw29("Winter"), anim: anim29("Winter") }
  };
  var TRANSITIONS41 = {
    0: springToSummer33,
    1: summerToAutumn33,
    2: autumnToWinter33
  };

  // src/textures/seasonal/herd/sheep.ts
  function clamp0141(x) {
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp45(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB33(a, b, t) {
    return [lerp45(a[0], b[0], t), lerp45(a[1], b[1], t), lerp45(a[2], b[2], t)];
  }
  function rgb40(c, a = 1) {
    const r = Math.round(clamp0141(c[0] / 255) * 255);
    const g = Math.round(clamp0141(c[1] / 255) * 255);
    const b = Math.round(clamp0141(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0141(a)})`;
  }
  function smoother40(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt40(t, amp = 1.1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP40 = {
    Spring: {
      fleeceLight: [248, 250, 248],
      fleeceShadow: [206, 212, 214],
      faceDark: [58, 52, 56],
      padGrass: [126, 198, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [60, 54, 50],
      lightWash: [212, 238, 255],
      // cool-bright
      lightWashAmt: 0.16,
      fleeceVolume: 0.18,
      // recently-shorn: slimmer
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Summer: {
      fleeceLight: [250, 250, 246],
      fleeceShadow: [200, 200, 196],
      faceDark: [54, 48, 50],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [54, 48, 44],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      fleeceVolume: 0.5,
      // full normal fleece
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Autumn: {
      fleeceLight: [244, 240, 230],
      fleeceShadow: [198, 190, 178],
      faceDark: [56, 46, 44],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [58, 46, 38],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      fleeceVolume: 0.72,
      // fuller, thicker
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    Winter: {
      fleeceLight: [252, 253, 255],
      fleeceShadow: [206, 216, 228],
      faceDark: [60, 56, 64],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [70, 70, 84],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      fleeceVolume: 1,
      // extra-thick puffed wool
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP40(a, b, t) {
    return {
      fleeceLight: lerpRGB33(a.fleeceLight, b.fleeceLight, t),
      fleeceShadow: lerpRGB33(a.fleeceShadow, b.fleeceShadow, t),
      faceDark: lerpRGB33(a.faceDark, b.faceDark, t),
      padGrass: lerpRGB33(a.padGrass, b.padGrass, t),
      soil: lerpRGB33(a.soil, b.soil, t),
      outline: lerpRGB33(a.outline, b.outline, t),
      lightWash: lerpRGB33(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp45(a.lightWashAmt, b.lightWashAmt, t),
      fleeceVolume: lerp45(a.fleeceVolume, b.fleeceVolume, t),
      frostAmt: lerp45(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp45(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp45(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp45(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp45(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp45(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP29(p) {
    return {
      ...p,
      lightWashAmt: clamp0141(p.lightWashAmt),
      fleeceVolume: clamp0141(p.fleeceVolume),
      frostAmt: clamp0141(p.frostAmt),
      backSnowAmt: clamp0141(p.backSnowAmt),
      padSnowAmt: clamp0141(p.padSnowAmt),
      blossomAmt: clamp0141(p.blossomAmt),
      fallenLeafAmt: clamp0141(p.fallenLeafAmt),
      breathFogAmt: clamp0141(p.breathFogAmt)
    };
  }
  function paintPad8(ctx, p) {
    ctx.fillStyle = rgb40([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb40(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb40(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb40(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb40([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb40([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb40([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb40([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb40([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb40(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb40([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg6(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb40(p.faceDark);
    ctx.lineWidth = 3;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb40([28, 24, 26]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.8, 1.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function paintFleeceBody(ctx, p, cx, cy, fill, scallop) {
    const vol = 0.85 + p.fleeceVolume * 0.4;
    const rx = 13 * vol;
    const ry = 9.2 * vol;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
    const lumps = 11;
    const lumpR = (2.2 + p.fleeceVolume * 1.6) * scallop;
    for (let i = 0; i < lumps; i++) {
      const a = i / lumps * Math.PI * 2 - Math.PI / 2;
      const lx = cx + Math.cos(a) * rx * 0.98;
      const ly = cy + Math.sin(a) * ry * 0.98;
      ctx.beginPath();
      ctx.arc(lx, ly, lumpR, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintSheep(ctx, p, bob) {
    const bx = -1;
    const by = 4 - bob;
    ctx.save();
    ctx.globalAlpha = 0.85;
    paintLeg6(ctx, p, bx + 6.5, by + 6, 18.5);
    paintLeg6(ctx, p, bx - 7.5, by + 6, 18.8);
    ctx.restore();
    paintLeg6(ctx, p, bx + 3.5, by + 6, 19.2);
    paintLeg6(ctx, p, bx - 4.5, by + 6, 19.4);
    paintFleeceBody(ctx, p, bx, by, rgb40(p.outline), 1.18);
    paintFleeceBody(ctx, p, bx, by, rgb40(p.fleeceShadow), 1);
    ctx.save();
    ctx.translate(-1.4, -1.6);
    paintFleeceBody(ctx, p, bx, by, rgb40(p.fleeceLight), 0.82);
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb40([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 7.6, 9 * (0.9 + p.fleeceVolume * 0.3), 3.4, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb40([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -8.4], [1, -9], [5, -8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.6 + p.fleeceVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb40([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-3, 4],
        [4, -2],
        [8, 2],
        [0, -3],
        [-5, -5],
        [6, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 11;
    const hy = by + 2;
    ctx.fillStyle = rgb40(p.faceDark);
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 2.6, hy - 4);
      ctx.rotate(side * 0.9 - 0.3);
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.4, 1.7, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb40(p.fleeceLight);
    ctx.beginPath();
    ctx.ellipse(hx, hy - 4.4, 3.4, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.28);
    ctx.fillStyle = rgb40(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.8, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb40(p.faceDark);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.1, 4.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb40([255, 255, 255], 0.12);
    ctx.beginPath();
    ctx.ellipse(-1.4, -1.6, 2, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb40([245, 245, 240]);
    for (const ex of [-1.6, 1.6]) {
      ctx.beginPath();
      ctx.arc(ex, -1.2, 1.05, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb40([20, 18, 20]);
    for (const ex of [-1.4, 1.8]) {
      ctx.beginPath();
      ctx.arc(ex, -1, 0.6, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb40([18, 16, 18]);
    for (const ex of [-1.1, 1.1]) {
      ctx.beginPath();
      ctx.ellipse(ex, 3.2, 0.5, 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    ctx.fillStyle = rgb40(p.fleeceLight);
    ctx.beginPath();
    ctx.arc(bx + 12.5, by - 1, 2.4 + p.fleeceVolume * 0.6, 0, Math.PI * 2);
    ctx.fill();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb40([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 6, hy + 3.4, 3.2, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash7(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb40(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint40(ctx, pIn, bob) {
    const p = clampP29(pIn);
    ctx.save();
    try {
      paintPad8(ctx, p);
      paintSheep(ctx, p, bob);
      paintLightWash7(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw30(season) {
    return (ctx) => paint40(ctx, SP40[season], 0);
  }
  function anim30(season) {
    return (ctx, t) => {
      const p = clampP29(SP40[season]);
      const bob = bobAt40(t);
      paint40(ctx, SP40[season], bob);
      ctx.save();
      try {
        const bx = -1;
        const by = 4 - bob;
        const hx = bx - 11;
        const hy = by + 2;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.fillStyle = rgb40(p.faceDark);
        ctx.save();
        ctx.translate(hx + 2.6, hy - 4);
        ctx.rotate(0.6 + flick * 0.5);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.7, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        ctx.fillStyle = rgb40(p.fleeceLight);
        ctx.beginPath();
        ctx.arc(bx + 12.5 + flick * 1.4, by - 1, 2.4 + p.fleeceVolume * 0.6, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb40([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb40([255, 255, 255], 0.35);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb40([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb40([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach, hy + 3.4, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb40([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb40([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition37(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother40(clamp0141(pp));
      paint40(ctx, lerpP40(SP40[from], SP40[to], k), 0);
    };
  }
  var springToSummer34 = makeTransition37(0);
  var summerToAutumn34 = makeTransition37(1);
  var autumnToWinter34 = makeTransition37(2);
  var VARIANTS42 = {
    Spring: { draw: draw30("Spring"), anim: anim30("Spring") },
    Summer: { draw: draw30("Summer"), anim: anim30("Summer") },
    Autumn: { draw: draw30("Autumn"), anim: anim30("Autumn") },
    Winter: { draw: draw30("Winter"), anim: anim30("Winter") }
  };
  var TRANSITIONS42 = {
    0: springToSummer34,
    1: summerToAutumn34,
    2: autumnToWinter34
  };

  // src/textures/seasonal/herd/pig.ts
  function clamp0142(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp46(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB34(a, b, t) {
    return [lerp46(a[0], b[0], t), lerp46(a[1], b[1], t), lerp46(a[2], b[2], t)];
  }
  function rgb41(c, a = 1) {
    const r = Math.round(clamp0142(c[0] / 255) * 255);
    const g = Math.round(clamp0142(c[1] / 255) * 255);
    const b = Math.round(clamp0142(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0142(a)})`;
  }
  function smoother41(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt41(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP41 = {
    // Spring — smooth pink, dewy lime pad + blossom, cool-bright light.
    Spring: {
      skinLight: [252, 200, 200],
      skinMid: [238, 162, 168],
      skinShadow: [196, 116, 128],
      snout: [232, 150, 158],
      hoof: [70, 50, 52],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 134, 54],
      outline: [120, 70, 78],
      lightWash: [216, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.1,
      // smooth-coated
      bristleAmt: 0.05,
      glossAmt: 0.25,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — healthy PEAK pink, saturated mid-green pad, warm light, soft sheen.
    Summer: {
      skinLight: [255, 196, 196],
      skinMid: [244, 156, 162],
      skinShadow: [196, 106, 120],
      snout: [240, 146, 154],
      hoof: [64, 44, 46],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [116, 62, 70],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.16,
      // sleek healthy coat
      bristleAmt: 0.08,
      glossAmt: 0.55,
      // peak skin sheen
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — pink with slightly fuller bristles, olive-tan pad, fallen leaves.
    Autumn: {
      skinLight: [246, 192, 188],
      skinMid: [232, 150, 152],
      skinShadow: [190, 104, 112],
      snout: [228, 142, 146],
      hoof: [66, 46, 44],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [112, 62, 64],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.5,
      // slightly fuller bristles
      bristleAmt: 0.4,
      glossAmt: 0.3,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — pink under thicker pale bristles + back snow; snowy pad, cool light,
    // breath-fog at the snout. The pink hide stays clearly visible underneath.
    Winter: {
      skinLight: [244, 196, 198],
      skinMid: [224, 156, 162],
      skinShadow: [180, 116, 132],
      snout: [220, 148, 156],
      hoof: [70, 58, 64],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [104, 72, 84],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick pale bristles
      bristleAmt: 0.95,
      glossAmt: 0.18,
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP41(a, b, t) {
    return {
      skinLight: lerpRGB34(a.skinLight, b.skinLight, t),
      skinMid: lerpRGB34(a.skinMid, b.skinMid, t),
      skinShadow: lerpRGB34(a.skinShadow, b.skinShadow, t),
      snout: lerpRGB34(a.snout, b.snout, t),
      hoof: lerpRGB34(a.hoof, b.hoof, t),
      padGrass: lerpRGB34(a.padGrass, b.padGrass, t),
      soil: lerpRGB34(a.soil, b.soil, t),
      outline: lerpRGB34(a.outline, b.outline, t),
      lightWash: lerpRGB34(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp46(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp46(a.coatVolume, b.coatVolume, t),
      bristleAmt: lerp46(a.bristleAmt, b.bristleAmt, t),
      glossAmt: lerp46(a.glossAmt, b.glossAmt, t),
      frostAmt: lerp46(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp46(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp46(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp46(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp46(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp46(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP30(p) {
    return {
      ...p,
      lightWashAmt: clamp0142(p.lightWashAmt),
      coatVolume: clamp0142(p.coatVolume),
      bristleAmt: clamp0142(p.bristleAmt),
      glossAmt: clamp0142(p.glossAmt),
      frostAmt: clamp0142(p.frostAmt),
      backSnowAmt: clamp0142(p.backSnowAmt),
      padSnowAmt: clamp0142(p.padSnowAmt),
      blossomAmt: clamp0142(p.blossomAmt),
      fallenLeafAmt: clamp0142(p.fallenLeafAmt),
      breathFogAmt: clamp0142(p.breathFogAmt)
    };
  }
  function paintPad9(ctx, p) {
    ctx.fillStyle = rgb41([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb41(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb41([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb41([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb41([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb41([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb41([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb41(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb41([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg7(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb41(p.skinShadow);
    ctx.lineWidth = 4.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb41(p.hoof);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 2, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function paintBody2(ctx, p, cx, cy, fill, scale) {
    const rx = 13.4 * scale;
    const ry = 10.4 * scale;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function paintPig(ctx, p, bob) {
    const bx = 0;
    const by = 5 - bob;
    ctx.save();
    ctx.globalAlpha = 0.85;
    paintLeg7(ctx, p, bx + 7, by + 6, 18.6);
    paintLeg7(ctx, p, bx - 6, by + 6, 18.9);
    ctx.restore();
    paintLeg7(ctx, p, bx + 4, by + 7, 19.2);
    paintLeg7(ctx, p, bx - 8.5, by + 7, 19.4);
    drawCurlyTail(ctx, p, bx + 12.6, by - 2, 0);
    paintBody2(ctx, p, bx, by, rgb41(p.outline), 1.06);
    paintBody2(ctx, p, bx, by, rgb41(p.skinMid), 1);
    ctx.save();
    ctx.beginPath();
    ctx.ellipse(bx, by, 13.4, 10.4, 0, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb41(p.skinShadow, 0.7);
    ctx.beginPath();
    ctx.ellipse(bx + 2, by + 5.5, 13, 7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.skinLight);
    ctx.beginPath();
    ctx.ellipse(bx - 2.4, by - 3.4, 9.6, 6.4, -0.12, 0, Math.PI * 2);
    ctx.fill();
    if (p.glossAmt > 1e-3) {
      ctx.fillStyle = rgb41([255, 255, 255], 0.32 * p.glossAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 4, by - 5.2, 4.4, 2, -0.2, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.bristleAmt > 0.02) {
      const n = Math.round(6 + p.bristleAmt * 10);
      ctx.lineCap = "round";
      for (let i = 0; i < n; i++) {
        const u = i / Math.max(1, n - 1);
        const sx = bx - 9 + u * 19;
        const top = by - 9.4 - Math.sin(u * Math.PI) * 1.4;
        const len = 1.8 + p.coatVolume * 2.2;
        const pale = rgb41([238, 226, 224], 0.55 * p.bristleAmt);
        ctx.strokeStyle = pale;
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(sx, top + 1.6);
        ctx.lineTo(sx - 0.6, top - len);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb41([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 8.4, 9.4, 3.2, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb41([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -9], [1, -9.6], [5, -8.6]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb41([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-7, 0],
        [-2, 4],
        [4, -1],
        [8, 2],
        [0, -2],
        [-4, -4],
        [6, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 11.5;
    const hy = by + 3.2;
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 3.2 - 1, hy - 7.4);
      ctx.rotate(side * 0.5 - 0.25);
      ctx.fillStyle = rgb41(p.outline);
      ctx.beginPath();
      ctx.moveTo(0, -0.4);
      ctx.lineTo(3.4, 1.2);
      ctx.lineTo(1, 5.4);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb41(p.skinMid);
      ctx.beginPath();
      ctx.moveTo(0.2, 0.2);
      ctx.lineTo(2.8, 1.4);
      ctx.lineTo(1.1, 4.6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb41(p.skinShadow, 0.7);
      ctx.beginPath();
      ctx.moveTo(0.8, 1);
      ctx.lineTo(2.2, 1.7);
      ctx.lineTo(1.3, 3.6);
      ctx.closePath();
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.16);
    ctx.fillStyle = rgb41(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 7.4, 6.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.skinMid);
    ctx.beginPath();
    ctx.ellipse(0, 0, 6.6, 5.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.skinLight, 0.9);
    ctx.beginPath();
    ctx.ellipse(-1.8, -1.8, 3.2, 2.6, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41([245, 245, 240]);
    for (const ex of [-1.6, 1.8]) {
      ctx.beginPath();
      ctx.arc(ex, -1.6, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb41([22, 18, 20]);
    for (const ex of [-1.5, 1.9]) {
      ctx.beginPath();
      ctx.arc(ex, -1.4, 0.6, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb41(p.outline);
    ctx.beginPath();
    ctx.ellipse(-0.6, 3.4, 4.6, 3.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.snout);
    ctx.beginPath();
    ctx.ellipse(-0.6, 3.4, 3.9, 2.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41(p.skinLight, 0.55);
    ctx.beginPath();
    ctx.ellipse(-1.4, 2.2, 2.2, 1, -0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb41([90, 48, 56]);
    for (const ex of [-2, 0.8]) {
      ctx.beginPath();
      ctx.ellipse(ex, 3.6, 0.7, 1.1, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb41([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 5.6, hy + 3.8, 3.2, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function drawCurlyTail(ctx, p, ox, oy, wiggle) {
    ctx.save();
    ctx.translate(ox, oy);
    ctx.strokeStyle = rgb41(p.outline);
    ctx.lineWidth = 3;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(3.4, -1.4 + wiggle, 2.6, 1.6 + wiggle);
    ctx.quadraticCurveTo(1.9, 4 + wiggle, 4, 3.4 + wiggle * 0.6);
    ctx.stroke();
    ctx.strokeStyle = rgb41(p.skinMid);
    ctx.lineWidth = 1.8;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(3.4, -1.4 + wiggle, 2.6, 1.6 + wiggle);
    ctx.quadraticCurveTo(1.9, 4 + wiggle, 4, 3.4 + wiggle * 0.6);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.restore();
  }
  function paintLightWash8(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb41(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint41(ctx, pIn, bob) {
    const p = clampP30(pIn);
    ctx.save();
    try {
      paintPad9(ctx, p);
      paintPig(ctx, p, bob);
      paintLightWash8(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw31(season) {
    return (ctx) => paint41(ctx, SP41[season], 0);
  }
  function anim31(season) {
    return (ctx, t) => {
      const p = clampP30(SP41[season]);
      const bob = bobAt41(t);
      paint41(ctx, SP41[season], bob);
      ctx.save();
      try {
        const bx = 0;
        const by = 5 - bob;
        const hx = bx - 11.5;
        const hy = by + 3.2;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        const snoutDip = flickGate * 1.4;
        ctx.save();
        ctx.translate(hx + 3.2 - 1, hy - 7.4);
        ctx.rotate(0.5 - 0.25 + flick * 0.45);
        ctx.fillStyle = rgb41(p.outline);
        ctx.beginPath();
        ctx.moveTo(0, -0.4);
        ctx.lineTo(3.4, 1.2);
        ctx.lineTo(1, 5.4);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb41(p.skinMid);
        ctx.beginPath();
        ctx.moveTo(0.2, 0.2);
        ctx.lineTo(2.8, 1.4);
        ctx.lineTo(1.1, 4.6);
        ctx.closePath();
        ctx.fill();
        ctx.restore();
        drawCurlyTail(ctx, p, bx + 12.6, by - 2, flick * 1.1);
        if (snoutDip > 0.01) {
          ctx.save();
          ctx.translate(hx, hy + snoutDip);
          ctx.rotate(0.16);
          ctx.fillStyle = rgb41(p.snout);
          ctx.beginPath();
          ctx.ellipse(-0.6, 3.4, 3.9, 2.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb41([90, 48, 56]);
          for (const ex of [-2, 0.8]) {
            ctx.beginPath();
            ctx.ellipse(ex, 3.6, 0.7, 1.1, 0, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.restore();
        }
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb41([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 8 + s * 16;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb41([255, 255, 255], 0.35);
          ctx.beginPath();
          ctx.ellipse(sx, by - 4, 3, 5, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb41([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb41([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - 4.4 - reach, hy + 3.8, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb41([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb41([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13.4, 10.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition38(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother41(clamp0142(pp));
      paint41(ctx, lerpP41(SP41[from], SP41[to], k), 0);
    };
  }
  var springToSummer35 = makeTransition38(0);
  var summerToAutumn35 = makeTransition38(1);
  var autumnToWinter35 = makeTransition38(2);
  var VARIANTS43 = {
    Spring: { draw: draw31("Spring"), anim: anim31("Spring") },
    Summer: { draw: draw31("Summer"), anim: anim31("Summer") },
    Autumn: { draw: draw31("Autumn"), anim: anim31("Autumn") },
    Winter: { draw: draw31("Winter"), anim: anim31("Winter") }
  };
  var TRANSITIONS43 = {
    0: springToSummer35,
    1: summerToAutumn35,
    2: autumnToWinter35
  };

  // src/textures/seasonal/herd/hog.ts
  function clamp0143(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp47(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB35(a, b, t) {
    return [lerp47(a[0], b[0], t), lerp47(a[1], b[1], t), lerp47(a[2], b[2], t)];
  }
  function rgb42(c, a = 1) {
    const r = Math.round(clamp0143(c[0] / 255) * 255);
    const g = Math.round(clamp0143(c[1] / 255) * 255);
    const b = Math.round(clamp0143(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0143(a)})`;
  }
  function smoother42(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt42(t, amp = 1.2, w = 1.2) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP42 = {
    // Spring — fresh pastel light; brown bristly coat (sleeker); dewy lime pad.
    Spring: {
      hideLight: [150, 108, 78],
      hideMid: [118, 80, 54],
      hideShadow: [80, 52, 34],
      snout: [156, 110, 100],
      bristle: [70, 46, 30],
      tusk: [236, 230, 210],
      padGrass: [126, 198, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [50, 32, 22],
      lightWash: [212, 238, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.3,
      // sleeker bristly coat
      sheenAmt: 0.1,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — richest saturated brown (PEAK), full coat; warm light; coat sheen.
    Summer: {
      hideLight: [160, 112, 76],
      hideMid: [124, 82, 52],
      hideShadow: [82, 52, 32],
      snout: [164, 112, 100],
      bristle: [68, 44, 28],
      tusk: [238, 232, 212],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [48, 30, 20],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.5,
      // full normal coat
      sheenAmt: 0.7,
      // coarse-coat sheen peaks
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — deeper warm brown, fuller coarse coat; olive-tan pad; fallen leaves.
    Autumn: {
      hideLight: [148, 100, 66],
      hideMid: [112, 74, 46],
      hideShadow: [74, 46, 28],
      snout: [152, 102, 92],
      bristle: [62, 40, 24],
      tusk: [232, 224, 202],
      padGrass: [156, 142, 78],
      // olive-tan browning grass
      soil: [104, 84, 44],
      outline: [46, 28, 18],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.72,
      // fuller, thicker coarse coat
      sheenAmt: 0.3,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — cool blue-grey light; shaggy thick coat; snow on the back;
    // snowy pad + frost; breath-fog at the snout. Stays clearly brown underneath.
    Winter: {
      hideLight: [138, 100, 76],
      hideMid: [106, 74, 52],
      hideShadow: [72, 50, 36],
      snout: [148, 108, 102],
      bristle: [60, 42, 30],
      tusk: [240, 236, 222],
      padGrass: [222, 232, 244],
      // snow on the pad (muted grey-green under)
      soil: [150, 168, 190],
      outline: [56, 40, 36],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // shaggy thick winter coat
      sheenAmt: 0.12,
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP42(a, b, t) {
    return {
      hideLight: lerpRGB35(a.hideLight, b.hideLight, t),
      hideMid: lerpRGB35(a.hideMid, b.hideMid, t),
      hideShadow: lerpRGB35(a.hideShadow, b.hideShadow, t),
      snout: lerpRGB35(a.snout, b.snout, t),
      bristle: lerpRGB35(a.bristle, b.bristle, t),
      tusk: lerpRGB35(a.tusk, b.tusk, t),
      padGrass: lerpRGB35(a.padGrass, b.padGrass, t),
      soil: lerpRGB35(a.soil, b.soil, t),
      outline: lerpRGB35(a.outline, b.outline, t),
      lightWash: lerpRGB35(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp47(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp47(a.coatVolume, b.coatVolume, t),
      sheenAmt: lerp47(a.sheenAmt, b.sheenAmt, t),
      frostAmt: lerp47(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp47(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp47(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp47(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp47(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp47(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP31(p) {
    return {
      ...p,
      lightWashAmt: clamp0143(p.lightWashAmt),
      coatVolume: clamp0143(p.coatVolume),
      sheenAmt: clamp0143(p.sheenAmt),
      frostAmt: clamp0143(p.frostAmt),
      backSnowAmt: clamp0143(p.backSnowAmt),
      padSnowAmt: clamp0143(p.padSnowAmt),
      blossomAmt: clamp0143(p.blossomAmt),
      fallenLeafAmt: clamp0143(p.fallenLeafAmt),
      breathFogAmt: clamp0143(p.breathFogAmt)
    };
  }
  function paintPad10(ctx, p) {
    ctx.fillStyle = rgb42([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb42(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb42([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb42([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb42([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb42([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb42([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb42(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb42([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg8(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb42(p.hideShadow);
    ctx.lineWidth = 4.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb42([30, 22, 18]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 2.1, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath3(ctx, cx, cy, rx, ry) {
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
  }
  function paintHog(ctx, p, bob) {
    const bx = 1;
    const by = 4 - bob;
    const rx = 14.5;
    const ry = 9.6;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg8(ctx, p, bx + 8.5, by + 5.5, 18.6);
    paintLeg8(ctx, p, bx - 6.5, by + 5.5, 18.9);
    ctx.restore();
    paintLeg8(ctx, p, bx + 5, by + 6, 19.3);
    paintLeg8(ctx, p, bx - 3, by + 6, 19.5);
    bodyPath3(ctx, bx, by, rx + 1.2, ry + 1.2);
    ctx.fillStyle = rgb42(p.outline);
    ctx.fill();
    bodyPath3(ctx, bx, by, rx, ry);
    ctx.fillStyle = rgb42(p.hideMid);
    ctx.fill();
    ctx.save();
    bodyPath3(ctx, bx, by, rx, ry);
    ctx.clip();
    ctx.fillStyle = rgb42(p.hideShadow, 0.8);
    ctx.beginPath();
    ctx.ellipse(bx, by + 5, rx, ry * 0.7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.hideLight);
    ctx.beginPath();
    ctx.ellipse(bx - 2.5, by - 3.2, rx * 0.78, ry * 0.6, -0.08, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    const bristleLen = 2.2 + p.coatVolume * 3.4;
    const bristleN = Math.round(9 + p.coatVolume * 5);
    ctx.strokeStyle = rgb42(p.bristle);
    ctx.lineWidth = 1.1;
    ctx.lineCap = "round";
    for (let i = 0; i < bristleN; i++) {
      const f = i / (bristleN - 1);
      const ang = lerp47(-2.5, -0.55, f);
      const sx = bx + Math.cos(ang) * rx * 0.96;
      const sy = by + Math.sin(ang) * ry * 0.96;
      const jitter = (i % 2 === 0 ? 1 : 0.7) * bristleLen;
      ctx.beginPath();
      ctx.moveTo(sx, sy);
      ctx.lineTo(sx + Math.cos(ang) * jitter * 0.4, sy - jitter);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    if (p.sheenAmt > 1e-3) {
      ctx.save();
      bodyPath3(ctx, bx, by, rx, ry);
      ctx.clip();
      ctx.globalCompositeOperation = "soft-light";
      ctx.fillStyle = rgb42([255, 240, 210], 0.4 * p.sheenAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 4, rx * 0.7, ry * 0.42, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb42([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 7.4, rx * 0.7, 3.2, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb42([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-6, -8], [0, -8.6], [6, -7.8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.6, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb42([255, 255, 255], 0.75 * p.frostAmt);
      const pts = [
        [-9, -1],
        [-3, 3],
        [5, -2],
        [9, 2],
        [1, -4],
        [-5, -5],
        [8, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.strokeStyle = rgb42(p.hideShadow);
    ctx.lineWidth = 1.6;
    ctx.beginPath();
    ctx.moveTo(bx + rx - 0.5, by - 2);
    ctx.quadraticCurveTo(bx + rx + 3.5, by - 4, bx + rx + 2, by - 0.5);
    ctx.quadraticCurveTo(bx + rx + 0.5, by + 0.8, bx + rx + 2.2, by + 1.2);
    ctx.stroke();
    const hx = bx - 13.5;
    const hy = by + 2.5;
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 3.2, hy - 5.5);
      ctx.rotate(side * 0.5 - 0.15);
      ctx.fillStyle = rgb42(p.outline);
      ctx.beginPath();
      ctx.moveTo(-2.4, 2.2);
      ctx.quadraticCurveTo(-1.2, -3.2, 1.2, -2.6);
      ctx.quadraticCurveTo(2.4, -0.2, 2.2, 2.4);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb42(p.hideMid);
      ctx.beginPath();
      ctx.moveTo(-1.8, 1.8);
      ctx.quadraticCurveTo(-0.9, -2.4, 0.9, -1.9);
      ctx.quadraticCurveTo(1.8, -0.1, 1.6, 1.9);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb42(p.snout, 0.7);
      ctx.beginPath();
      ctx.ellipse(0, 0.4, 0.8, 1.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb42(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx + 1, hy, 8.2, 7.4, 0.08, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.hideMid);
    ctx.beginPath();
    ctx.ellipse(hx + 1, hy, 7.4, 6.6, 0.08, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.hideLight, 0.85);
    ctx.beginPath();
    ctx.ellipse(hx - 1.4, hy - 2.4, 4, 3.4, -0.1, 0, Math.PI * 2);
    ctx.fill();
    const snx = hx - 6;
    const sny = hy + 2.6;
    ctx.fillStyle = rgb42(p.hideMid);
    ctx.beginPath();
    ctx.moveTo(hx - 1, hy - 1.5);
    ctx.quadraticCurveTo(snx - 1, sny - 4, snx - 2.5, sny);
    ctx.quadraticCurveTo(snx - 1, sny + 4.5, hx - 1, hy + 4.5);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb42(p.outline);
    ctx.beginPath();
    ctx.ellipse(snx - 2, sny, 3.6, 4.4, -0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42(p.snout);
    ctx.beginPath();
    ctx.ellipse(snx - 2, sny, 3, 3.8, -0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42([40, 24, 28]);
    for (const ny of [-1.2, 1.4]) {
      ctx.beginPath();
      ctx.ellipse(snx - 2.6, sny + ny, 0.7, 1, -0.15, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb42([255, 235, 232], 0.3);
    ctx.beginPath();
    ctx.ellipse(snx - 3.4, sny - 1.6, 1.1, 1.4, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb42(p.tusk);
    ctx.lineWidth = 1.5;
    ctx.lineCap = "round";
    for (const side of [0, 1]) {
      const tx = snx + 0.4 + side * 2.6;
      const ty = sny + 3.4;
      ctx.beginPath();
      ctx.moveTo(tx, ty);
      ctx.quadraticCurveTo(tx - 0.6, ty - 2.2, tx - 0.2, ty - 3.4);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb42([245, 242, 236]);
    ctx.beginPath();
    ctx.arc(hx + 0.6, hy - 1.4, 1.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb42([22, 16, 16]);
    ctx.beginPath();
    ctx.arc(hx + 0.9, hy - 1.3, 0.65, 0, Math.PI * 2);
    ctx.fill();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb42([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(snx - 6, sny + 0.6, 3.2, 2.2, 0.1, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash9(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb42(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint42(ctx, pIn, bob) {
    const p = clampP31(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad10(ctx, p);
      paintHog(ctx, p, bob);
      paintLightWash9(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw32(season) {
    return (ctx) => paint42(ctx, SP42[season], 0);
  }
  function anim32(season) {
    return (ctx, t) => {
      const p = clampP31(SP42[season]);
      const bob = bobAt42(t);
      paint42(ctx, SP42[season], bob);
      ctx.save();
      try {
        const bx = 1;
        const by = 4 - bob;
        const hx = bx - 13.5;
        const hy = by + 2.5;
        const snx = hx - 6;
        const sny = hy + 2.6;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        const snoutDip = flickGate * 1.3;
        ctx.save();
        ctx.translate(hx + 3.2, hy - 5.5);
        ctx.rotate(0.35 + flick * 0.5);
        ctx.fillStyle = rgb42(p.hideMid);
        ctx.beginPath();
        ctx.moveTo(-1.8, 1.8);
        ctx.quadraticCurveTo(-0.9, -2.4, 0.9, -1.9);
        ctx.quadraticCurveTo(1.8, -0.1, 1.6, 1.9);
        ctx.closePath();
        ctx.fill();
        ctx.restore();
        if (snoutDip > 0.02) {
          ctx.fillStyle = rgb42(p.snout);
          ctx.beginPath();
          ctx.ellipse(snx - 2, sny + snoutDip, 3, 3.8, -0.15, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb42([40, 24, 28]);
          for (const ny of [-1.2, 1.4]) {
            ctx.beginPath();
            ctx.ellipse(snx - 2.6, sny + snoutDip + ny, 0.7, 1, -0.15, 0, Math.PI * 2);
            ctx.fill();
          }
        }
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb42([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb42([255, 244, 214], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 3, 3.2, 5.4, 0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb42([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.2);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb42([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(snx - reach, sny + 0.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.1, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb42([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb42([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 14.5, 9.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition39(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother42(clamp0143(pp));
      paint42(ctx, lerpP42(SP42[from], SP42[to], k), 0);
    };
  }
  var springToSummer36 = makeTransition39(0);
  var summerToAutumn36 = makeTransition39(1);
  var autumnToWinter36 = makeTransition39(2);
  var VARIANTS44 = {
    Spring: { draw: draw32("Spring"), anim: anim32("Spring") },
    Summer: { draw: draw32("Summer"), anim: anim32("Summer") },
    Autumn: { draw: draw32("Autumn"), anim: anim32("Autumn") },
    Winter: { draw: draw32("Winter"), anim: anim32("Winter") }
  };
  var TRANSITIONS44 = {
    0: springToSummer36,
    1: summerToAutumn36,
    2: autumnToWinter36
  };

  // src/textures/seasonal/herd/boar.ts
  function clamp0144(x) {
    return x < 0 || Number.isNaN(x) ? 0 : x > 1 ? 1 : x;
  }
  function lerp48(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB36(a, b, t) {
    return [lerp48(a[0], b[0], t), lerp48(a[1], b[1], t), lerp48(a[2], b[2], t)];
  }
  function rgb43(c, a = 1) {
    const r = Math.round(clamp0144(c[0] / 255) * 255);
    const g = Math.round(clamp0144(c[1] / 255) * 255);
    const b = Math.round(clamp0144(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0144(a)})`;
  }
  function smoother43(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt43(t, amp = 1, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP43 = {
    // Spring — dark bristly coat (lightly cooler), dewy lime pad + blossom.
    Spring: {
      coatLight: [102, 86, 72],
      coatMid: [74, 60, 50],
      coatShadow: [48, 38, 32],
      bristle: [40, 32, 28],
      snout: [70, 54, 48],
      tusk: [248, 246, 234],
      hoof: [30, 24, 22],
      padGrass: [126, 200, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [30, 24, 20],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.42,
      // sleeker spring coat
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.62,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — full dark coat (PEAK richness), saturated mid-green pad, warm light.
    Summer: {
      coatLight: [110, 92, 76],
      coatMid: [78, 62, 50],
      coatShadow: [46, 36, 28],
      bristle: [38, 30, 24],
      snout: [74, 56, 48],
      tusk: [250, 248, 236],
      hoof: [28, 22, 20],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [28, 22, 18],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.16,
      coatVolume: 0.6,
      // full coat
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — coat warms slightly, fuller bristle ridge; olive-tan pad + leaves.
    Autumn: {
      coatLight: [114, 88, 66],
      coatMid: [82, 60, 44],
      coatShadow: [50, 36, 26],
      bristle: [44, 32, 24],
      snout: [80, 56, 44],
      tusk: [246, 242, 226],
      hoof: [30, 22, 18],
      padGrass: [156, 142, 78],
      // olive-tan browning grass
      soil: [104, 84, 44],
      outline: [32, 24, 18],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.82,
      // fuller bristle ridge
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — cool blue-grey light; frost on bristles + snow on the back; snowy
    // pad; breath-fog at the snout. Tusks stay bright.
    Winter: {
      coatLight: [98, 88, 84],
      coatMid: [70, 60, 58],
      coatShadow: [46, 40, 40],
      bristle: [42, 38, 40],
      snout: [70, 58, 56],
      tusk: [252, 252, 244],
      hoof: [34, 28, 30],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [40, 36, 40],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.26,
      coatVolume: 1,
      // thickest winter coat + bristle ridge
      frostAmt: 0.72,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.72
    }
  };
  function lerpP43(a, b, t) {
    return {
      coatLight: lerpRGB36(a.coatLight, b.coatLight, t),
      coatMid: lerpRGB36(a.coatMid, b.coatMid, t),
      coatShadow: lerpRGB36(a.coatShadow, b.coatShadow, t),
      bristle: lerpRGB36(a.bristle, b.bristle, t),
      snout: lerpRGB36(a.snout, b.snout, t),
      tusk: lerpRGB36(a.tusk, b.tusk, t),
      hoof: lerpRGB36(a.hoof, b.hoof, t),
      padGrass: lerpRGB36(a.padGrass, b.padGrass, t),
      soil: lerpRGB36(a.soil, b.soil, t),
      outline: lerpRGB36(a.outline, b.outline, t),
      lightWash: lerpRGB36(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp48(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp48(a.coatVolume, b.coatVolume, t),
      frostAmt: lerp48(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp48(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp48(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp48(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp48(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp48(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP32(p) {
    return {
      ...p,
      lightWashAmt: clamp0144(p.lightWashAmt),
      coatVolume: clamp0144(p.coatVolume),
      frostAmt: clamp0144(p.frostAmt),
      backSnowAmt: clamp0144(p.backSnowAmt),
      padSnowAmt: clamp0144(p.padSnowAmt),
      blossomAmt: clamp0144(p.blossomAmt),
      fallenLeafAmt: clamp0144(p.fallenLeafAmt),
      breathFogAmt: clamp0144(p.breathFogAmt)
    };
  }
  function paintPad11(ctx, p) {
    ctx.fillStyle = rgb43([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb43(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb43([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb43([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb43([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb43([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb43([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb43(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb43([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg9(ctx, p, x, topY, baseY, width = 3.4) {
    ctx.strokeStyle = rgb43(p.coatShadow);
    ctx.lineWidth = width;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb43(p.hoof);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.9, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath4(ctx, cx, cy, vol) {
    const w = 14 + vol * 1.2;
    const h = 8.5 + vol * 1;
    ctx.beginPath();
    ctx.moveTo(cx + w, cy + 1);
    ctx.quadraticCurveTo(cx + w + 1.5, cy - h * 0.7, cx + w * 0.45, cy - h);
    ctx.quadraticCurveTo(cx - w * 0.15, cy - h * 1.25, cx - w * 0.7, cy - h * 0.85);
    ctx.quadraticCurveTo(cx - w * 1.05, cy - h * 0.5, cx - w * 0.98, cy + h * 0.35);
    ctx.quadraticCurveTo(cx - w * 0.5, cy + h * 0.95, cx + w * 0.2, cy + h * 0.9);
    ctx.quadraticCurveTo(cx + w * 0.8, cy + h * 0.8, cx + w, cy + 1);
    ctx.closePath();
  }
  function paintBristleRidge(ctx, p, cx, cy, vol, twitch) {
    const h = 8.5 + vol * 1;
    const n = 11;
    ctx.strokeStyle = rgb43(p.bristle);
    ctx.lineWidth = 1.5;
    ctx.lineCap = "round";
    for (let i = 0; i < n; i++) {
      const f = i / (n - 1);
      const bx = cx + lerp48(13, -10, f);
      const back = -h - 1.4 - Math.sin(f * Math.PI) * 2.6;
      const by = cy + back;
      const len = 3 + vol * 2.2 + Math.sin(f * Math.PI) * 1.4;
      const sway = twitch * Math.sin(i * 1.7) * 0.9;
      ctx.beginPath();
      ctx.moveTo(bx, by);
      ctx.lineTo(bx - 1.2 + sway, by - len);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
  }
  function paintBoar(ctx, p, bob, twitch, toss) {
    const cx = 1;
    const cy = 3 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg9(ctx, p, cx + 8, cy + 6, 18.6, 3.6);
    paintLeg9(ctx, p, cx - 3, cy + 6, 19, 3.6);
    ctx.restore();
    paintLeg9(ctx, p, cx + 10.5, cy + 6, 18.9, 3.8);
    paintLeg9(ctx, p, cx - 6, cy + 6, 19.3, 3.8);
    ctx.fillStyle = rgb43(p.outline);
    bodyPath4(ctx, cx, cy, vol + 0.35);
    ctx.fill();
    ctx.fillStyle = rgb43(p.coatMid);
    bodyPath4(ctx, cx, cy, vol);
    ctx.fill();
    ctx.save();
    bodyPath4(ctx, cx, cy, vol);
    ctx.clip();
    ctx.fillStyle = rgb43(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(cx + 2, cy + 6, 14, 6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(cx - 3, cy - 5, 10, 4.4, -0.25, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43([255, 255, 255], 0.08);
    ctx.beginPath();
    ctx.ellipse(cx - 4, cy - 6, 4.5, 2, -0.3, 0, Math.PI * 2);
    ctx.fill();
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb43([255, 255, 255], 0.75 * p.frostAmt);
      const pts = [
        [-8, -4],
        [-2, -6],
        [4, -3],
        [9, -1],
        [0, -2],
        [-5, 0],
        [7, 2]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(cx + fx, cy + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.restore();
    paintBristleRidge(ctx, p, cx, cy, vol, twitch);
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb43([235, 246, 255], 0.7 * p.frostAmt);
      const h = 8.5 + vol * 1;
      for (let i = 0; i < 6; i++) {
        const f = i / 5;
        const bx = cx + lerp48(11, -8, f);
        const len = 3 + vol * 2.2 + Math.sin(f * Math.PI) * 1.4;
        const by = cy - h - 1.4 - Math.sin(f * Math.PI) * 2.6 - len;
        ctx.beginPath();
        ctx.arc(bx - 1.2, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      const h = 8.5 + vol * 1;
      ctx.fillStyle = rgb43([248, 252, 255], 0.9 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(cx - 3, cy - h - 0.5, 8.5, 3, -0.18, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb43([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-6, -1.2], [-1, -1.6], [4, -0.8]]) {
        ctx.beginPath();
        ctx.arc(cx + dx, cy - h - 0.5 + dy, 1.5, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.strokeStyle = rgb43(p.coatShadow);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(cx + 13.5, cy - 1);
    ctx.quadraticCurveTo(cx + 16.5, cy - 2, cx + 16, cy - 4);
    ctx.quadraticCurveTo(cx + 15.4, cy - 5.4, cx + 16.6, cy - 5.6);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.save();
    ctx.translate(cx - 9, cy + 1);
    ctx.rotate(-toss * 0.14);
    const hx = 0;
    const hy = 0;
    ctx.fillStyle = rgb43(p.coatShadow);
    ctx.save();
    ctx.translate(hx + 1.5, hy - 5.5);
    ctx.rotate(-0.5);
    ctx.beginPath();
    ctx.ellipse(0, 0, 2.6, 3.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb43(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx - 1, hy - 0.5, 7.2, 6.4, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.coatMid);
    ctx.beginPath();
    ctx.ellipse(hx - 1, hy - 0.5, 6.4, 5.7, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(hx + 1.5, hy + 2, 4, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(hx - 4.5, hy + 2.5);
    ctx.rotate(0.32);
    ctx.fillStyle = rgb43(p.outline);
    ctx.beginPath();
    ctx.ellipse(-3.4, 0, 6.2, 3.5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.snout);
    ctx.beginPath();
    ctx.ellipse(-3.4, 0, 5.4, 2.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(-8.2, 0, 2, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43([16, 12, 14]);
    for (const ny of [-0.9, 0.9]) {
      ctx.beginPath();
      ctx.ellipse(-8.4, ny, 0.5, 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    for (const side of [1, 0.6]) {
      ctx.strokeStyle = rgb43(p.outline);
      ctx.lineWidth = 2.6 * side;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(hx - 6.5, hy + 4 + (1 - side) * 1.5);
      ctx.quadraticCurveTo(hx - 9.5, hy + 2.5, hx - 9.8 - side, hy - 1.5 - side);
      ctx.stroke();
      ctx.strokeStyle = rgb43(p.tusk);
      ctx.lineWidth = 1.7 * side;
      ctx.beginPath();
      ctx.moveTo(hx - 6.5, hy + 4 + (1 - side) * 1.5);
      ctx.quadraticCurveTo(hx - 9.5, hy + 2.5, hx - 9.8 - side, hy - 1.5 - side);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb43([245, 244, 238]);
    ctx.beginPath();
    ctx.arc(hx + 0.4, hy - 1.4, 1.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43([18, 14, 16]);
    ctx.beginPath();
    ctx.arc(hx + 0.6, hy - 1.3, 0.7, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb43([255, 255, 255], 0.8);
    ctx.beginPath();
    ctx.arc(hx + 0.2, hy - 1.7, 0.28, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb43([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(cx - 20, cy + 3.5, 3.2, 2.2, 0.1, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash10(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb43(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint43(ctx, pIn, bob, twitch = 0, toss = 0) {
    const p = clampP32(pIn);
    ctx.save();
    try {
      paintPad11(ctx, p);
      paintBoar(ctx, p, bob, twitch, toss);
      paintLightWash10(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw33(season) {
    return (ctx) => paint43(ctx, SP43[season], 0);
  }
  function anim33(season) {
    return (ctx, t) => {
      const p = clampP32(SP43[season]);
      const bob = bobAt43(t);
      const loop = t % 5.5 / 5.5;
      const gate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 5;
      const twitch = Math.sin(t * 11) * gate;
      const toss = gate;
      paint43(ctx, SP43[season], bob, twitch, toss);
      ctx.save();
      try {
        const cx = 1;
        const cy = 3 - bob;
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb43([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = cx - 11 + s * 22;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb43([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, cy - 4, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb43([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 2 + breathe * 3;
          ctx.fillStyle = rgb43([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(cx - 20 - reach, cy + 3.5, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.1, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb43([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb43([206, 224, 255], 0.1 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(cx, cy - 1, 14, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition40(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother43(clamp0144(pp));
      paint43(ctx, lerpP43(SP43[from], SP43[to], k), 0);
    };
  }
  var springToSummer37 = makeTransition40(0);
  var summerToAutumn37 = makeTransition40(1);
  var autumnToWinter37 = makeTransition40(2);
  var VARIANTS45 = {
    Spring: { draw: draw33("Spring"), anim: anim33("Spring") },
    Summer: { draw: draw33("Summer"), anim: anim33("Summer") },
    Autumn: { draw: draw33("Autumn"), anim: anim33("Autumn") },
    Winter: { draw: draw33("Winter"), anim: anim33("Winter") }
  };
  var TRANSITIONS45 = {
    0: springToSummer37,
    1: summerToAutumn37,
    2: autumnToWinter37
  };

  // src/textures/seasonal/herd/warthog.ts
  function clamp0145(x) {
    return x < 0 || Number.isNaN(x) ? 0 : x > 1 ? 1 : x;
  }
  function lerp49(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB37(a, b, t) {
    return [lerp49(a[0], b[0], t), lerp49(a[1], b[1], t), lerp49(a[2], b[2], t)];
  }
  function rgb44(c, a = 1) {
    const r = Math.round(clamp0145(c[0] / 255) * 255);
    const g = Math.round(clamp0145(c[1] / 255) * 255);
    const b = Math.round(clamp0145(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0145(a)})`;
  }
  function smoother44(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt44(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP44 = {
    // Spring — grey coat, dewy lime pad + blossom; cool-bright light.
    Spring: {
      hideLight: [158, 154, 150],
      hideMid: [120, 116, 114],
      hideShadow: [82, 80, 80],
      maneDark: [62, 56, 52],
      tusk: [236, 230, 214],
      padGrass: [126, 198, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [44, 40, 38],
      lightWash: [212, 238, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.35,
      // modest mane
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — full grey coat (PEAK), saturated mid-green pad; warm light.
    Summer: {
      hideLight: [162, 158, 152],
      hideMid: [122, 118, 114],
      hideShadow: [80, 78, 76],
      maneDark: [60, 54, 48],
      tusk: [240, 234, 216],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [42, 38, 34],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.55,
      // full normal mane
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — fuller mane tuft, olive-tan browning pad + fallen leaves; amber.
    Autumn: {
      hideLight: [156, 150, 142],
      hideMid: [118, 112, 106],
      hideShadow: [78, 74, 70],
      maneDark: [58, 50, 42],
      tusk: [236, 226, 202],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [44, 38, 32],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.78,
      // fuller mane tuft
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — grey coat dusted with snow on back/mane, breath-fog, snowy pad.
    Winter: {
      hideLight: [160, 162, 168],
      hideMid: [116, 118, 124],
      hideShadow: [78, 80, 88],
      maneDark: [64, 60, 62],
      tusk: [240, 238, 230],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [52, 50, 56],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thickest mane
      frostAmt: 0.65,
      backSnowAmt: 0.75,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.75
    }
  };
  function lerpP44(a, b, t) {
    return {
      hideLight: lerpRGB37(a.hideLight, b.hideLight, t),
      hideMid: lerpRGB37(a.hideMid, b.hideMid, t),
      hideShadow: lerpRGB37(a.hideShadow, b.hideShadow, t),
      maneDark: lerpRGB37(a.maneDark, b.maneDark, t),
      tusk: lerpRGB37(a.tusk, b.tusk, t),
      padGrass: lerpRGB37(a.padGrass, b.padGrass, t),
      soil: lerpRGB37(a.soil, b.soil, t),
      outline: lerpRGB37(a.outline, b.outline, t),
      lightWash: lerpRGB37(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp49(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp49(a.coatVolume, b.coatVolume, t),
      frostAmt: lerp49(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp49(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp49(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp49(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp49(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp49(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP33(p) {
    return {
      ...p,
      lightWashAmt: clamp0145(p.lightWashAmt),
      coatVolume: clamp0145(p.coatVolume),
      frostAmt: clamp0145(p.frostAmt),
      backSnowAmt: clamp0145(p.backSnowAmt),
      padSnowAmt: clamp0145(p.padSnowAmt),
      blossomAmt: clamp0145(p.blossomAmt),
      fallenLeafAmt: clamp0145(p.fallenLeafAmt),
      breathFogAmt: clamp0145(p.breathFogAmt)
    };
  }
  function paintPad12(ctx, p) {
    ctx.fillStyle = rgb44([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb44(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb44([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb44([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb44([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb44([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb44([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb44(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb44([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg10(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb44(p.hideShadow);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb44([26, 22, 22]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.5, 1.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function paintMane(ctx, p, pts, fill, scale) {
    ctx.fillStyle = fill;
    const spikeH = (3.2 + p.coatVolume * 3.4) * scale;
    for (let i = 0; i < pts.length; i++) {
      const [sx, sy] = pts[i];
      const h = spikeH * (i % 2 === 0 ? 1 : 0.7);
      ctx.beginPath();
      ctx.moveTo(sx - 1.7, sy);
      ctx.lineTo(sx + (i - pts.length / 2) * 0.2, sy - h);
      ctx.lineTo(sx + 1.7, sy);
      ctx.closePath();
      ctx.fill();
    }
  }
  function paintHideBody(ctx, cx, cy, fill, scale) {
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, 13 * scale, 7.6 * scale, 0, 0, Math.PI * 2);
    ctx.fill();
  }
  function paintWarthog(ctx, p, bob) {
    const bx = 0;
    const by = 5 - bob;
    ctx.save();
    ctx.globalAlpha = 0.85;
    paintLeg10(ctx, p, bx + 7, by + 5, 18.6);
    paintLeg10(ctx, p, bx - 6, by + 5, 18.9);
    ctx.restore();
    paintLeg10(ctx, p, bx + 4, by + 5, 19.2);
    paintLeg10(ctx, p, bx - 3, by + 5, 19.4);
    ctx.strokeStyle = rgb44(p.hideShadow);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx + 12, by);
    ctx.quadraticCurveTo(bx + 16, by - 3, bx + 15, by - 8);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb44(p.maneDark);
    ctx.beginPath();
    ctx.arc(bx + 15, by - 8.5, 1.8 + p.coatVolume * 0.7, 0, Math.PI * 2);
    ctx.fill();
    paintHideBody(ctx, bx, by, rgb44(p.outline), 1.12);
    paintHideBody(ctx, bx, by, rgb44(p.hideShadow), 1);
    paintHideBody(ctx, bx, by, rgb44(p.hideMid), 0.94);
    ctx.save();
    ctx.translate(-1.2, -1.8);
    paintHideBody(ctx, bx, by, rgb44(p.hideLight), 0.7);
    ctx.restore();
    const manePts = [
      [bx - 9.5, by - 4.5],
      [bx - 6.5, by - 6.6],
      [bx - 3, by - 7.4],
      [bx + 0.5, by - 7.4],
      [bx + 4, by - 6.8],
      [bx + 7.5, by - 5.6],
      [bx + 10.5, by - 4]
    ];
    paintMane(ctx, p, manePts, rgb44(p.maneDark), 1);
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb44([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx + 1, by - 6.4, 9 * (0.9 + p.coatVolume * 0.25), 3, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb44([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -7.4], [1, -8], [6, -7]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + p.coatVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb44([255, 255, 255], 0.78 * p.frostAmt);
      const pts = [
        [-7, 1],
        [-2, 4],
        [4, -1],
        [8, 2],
        [0, -2],
        [-4, -3],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 12;
    const hy = by + 2;
    ctx.fillStyle = rgb44(p.hideShadow);
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + 1.5 + side * 2.2, hy - 5);
      ctx.rotate(side * 0.7 - 0.2);
      ctx.beginPath();
      ctx.ellipse(0, 0, 2.4, 1.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.22);
    ctx.fillStyle = rgb44(p.outline);
    ctx.beginPath();
    ctx.ellipse(-1.2, 0.6, 6.6, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44(p.hideMid);
    ctx.beginPath();
    ctx.ellipse(-1.2, 0.6, 5.9, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44(p.hideLight, 0.85);
    ctx.beginPath();
    ctx.ellipse(-2.6, -1.4, 3, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44(p.hideShadow);
    ctx.beginPath();
    ctx.ellipse(-5.4, 2.6, 2.8, 2.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44([22, 18, 18]);
    for (const ex of [-0.9, 0.9]) {
      ctx.beginPath();
      ctx.ellipse(-5.4 + ex, 2.8, 0.55, 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb44(p.hideLight);
    for (const [wx, wy] of [[-3.6, 1.2], [-1, -0.2]]) {
      ctx.beginPath();
      ctx.ellipse(wx, wy, 1.4, 1, -0.3, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb44(p.outline, 0.6);
      ctx.lineWidth = 0.5;
      ctx.stroke();
    }
    ctx.fillStyle = rgb44([245, 244, 238]);
    ctx.beginPath();
    ctx.arc(0.4, -1.6, 1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb44([20, 18, 18]);
    ctx.beginPath();
    ctx.arc(0.6, -1.5, 0.58, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.22);
    ctx.lineCap = "round";
    const drawTusk = (baseX, baseY, dir, len, width) => {
      ctx.strokeStyle = rgb44(p.outline);
      ctx.lineWidth = width + 1;
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + dir * 1.6, baseY - len * 0.5, baseX + dir * 1, baseY - len);
      ctx.stroke();
      ctx.strokeStyle = rgb44(p.tusk);
      ctx.lineWidth = width;
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + dir * 1.6, baseY - len * 0.5, baseX + dir * 1, baseY - len);
      ctx.stroke();
    };
    drawTusk(-6.4, 3.2, -1, 5.2, 1.9);
    drawTusk(-5.6, 3.6, -1, 3, 1.5);
    ctx.globalAlpha = 0.85;
    drawTusk(-4.4, 3.4, -1, 4.2, 1.6);
    drawTusk(-3.8, 3.7, -1, 2.4, 1.3);
    ctx.globalAlpha = 1;
    ctx.lineCap = "butt";
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb44([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 7, hy + 4, 3.2, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash11(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb44(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint44(ctx, pIn, bob) {
    const p = clampP33(pIn);
    ctx.save();
    try {
      paintPad12(ctx, p);
      paintWarthog(ctx, p, bob);
      paintLightWash11(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw34(season) {
    return (ctx) => paint44(ctx, SP44[season], 0);
  }
  function anim34(season) {
    return (ctx, t) => {
      const p = clampP33(SP44[season]);
      const bob = bobAt44(t);
      paint44(ctx, SP44[season], bob);
      ctx.save();
      try {
        const bx = 0;
        const by = 5 - bob;
        const hx = bx - 12;
        const hy = by + 2;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.fillStyle = rgb44(p.maneDark);
        const spikeH = 3.2 + p.coatVolume * 3.4;
        for (const sx of [bx - 3, bx + 0.5]) {
          ctx.save();
          ctx.translate(sx, by - 7.4);
          ctx.rotate(flick * 0.35);
          ctx.beginPath();
          ctx.moveTo(-1.7, 0);
          ctx.lineTo(0, -spikeH);
          ctx.lineTo(1.7, 0);
          ctx.closePath();
          ctx.fill();
          ctx.restore();
        }
        ctx.fillStyle = rgb44(p.hideShadow);
        ctx.save();
        ctx.translate(hx + 3.7, hy - 5);
        ctx.rotate(0.5 + flick * 0.5);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.4, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb44([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 10 + s * 20;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb44([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 1, 3, 5, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb44([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb44([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - 3 - reach, hy + 4, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb44([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb44([206, 224, 255], 0.1 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13, 7.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition41(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother44(clamp0145(pp));
      paint44(ctx, lerpP44(SP44[from], SP44[to], k), 0);
    };
  }
  var springToSummer38 = makeTransition41(0);
  var summerToAutumn38 = makeTransition41(1);
  var autumnToWinter38 = makeTransition41(2);
  var VARIANTS46 = {
    Spring: { draw: draw34("Spring"), anim: anim34("Spring") },
    Summer: { draw: draw34("Summer"), anim: anim34("Summer") },
    Autumn: { draw: draw34("Autumn"), anim: anim34("Autumn") },
    Winter: { draw: draw34("Winter"), anim: anim34("Winter") }
  };
  var TRANSITIONS46 = {
    0: springToSummer38,
    1: summerToAutumn38,
    2: autumnToWinter38
  };

  // src/textures/seasonal/herd/alpaca.ts
  function clamp0146(x) {
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp50(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB38(a, b, t) {
    return [lerp50(a[0], b[0], t), lerp50(a[1], b[1], t), lerp50(a[2], b[2], t)];
  }
  function rgb45(c, a = 1) {
    const r = Math.round(clamp0146(c[0] / 255) * 255);
    const g = Math.round(clamp0146(c[1] / 255) * 255);
    const b = Math.round(clamp0146(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0146(a)})`;
  }
  function smoother45(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt45(t, amp = 1.1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP45 = {
    Spring: {
      fleeceLight: [248, 238, 214],
      // light recently-shorn cream
      fleeceShadow: [216, 198, 162],
      faceDark: [120, 96, 70],
      faceLight: [236, 218, 186],
      padGrass: [126, 198, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [92, 72, 50],
      lightWash: [212, 238, 255],
      // cool-bright
      lightWashAmt: 0.16,
      fleeceVolume: 0.16,
      // recently-shorn: slimmer fleece
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Summer: {
      fleeceLight: [244, 230, 200],
      // full warm cream/tan (PEAK)
      fleeceShadow: [210, 188, 150],
      faceDark: [116, 90, 64],
      faceLight: [230, 210, 176],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [88, 66, 46],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      fleeceVolume: 0.55,
      // full fleece (peak coverage)
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Autumn: {
      fleeceLight: [238, 222, 188],
      // slightly warmer tan
      fleeceShadow: [204, 178, 138],
      faceDark: [112, 84, 58],
      faceLight: [224, 202, 166],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [86, 62, 42],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      fleeceVolume: 0.74,
      // thicker fleece
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    Winter: {
      fleeceLight: [250, 244, 230],
      // cool-lit cream, still clearly cream
      fleeceShadow: [214, 204, 192],
      faceDark: [104, 84, 70],
      faceLight: [226, 214, 196],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [86, 74, 70],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      fleeceVolume: 1,
      // dense extra-fluffed fleece
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP45(a, b, t) {
    return {
      fleeceLight: lerpRGB38(a.fleeceLight, b.fleeceLight, t),
      fleeceShadow: lerpRGB38(a.fleeceShadow, b.fleeceShadow, t),
      faceDark: lerpRGB38(a.faceDark, b.faceDark, t),
      faceLight: lerpRGB38(a.faceLight, b.faceLight, t),
      padGrass: lerpRGB38(a.padGrass, b.padGrass, t),
      soil: lerpRGB38(a.soil, b.soil, t),
      outline: lerpRGB38(a.outline, b.outline, t),
      lightWash: lerpRGB38(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp50(a.lightWashAmt, b.lightWashAmt, t),
      fleeceVolume: lerp50(a.fleeceVolume, b.fleeceVolume, t),
      frostAmt: lerp50(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp50(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp50(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp50(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp50(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp50(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP34(p) {
    return {
      ...p,
      lightWashAmt: clamp0146(p.lightWashAmt),
      fleeceVolume: clamp0146(p.fleeceVolume),
      frostAmt: clamp0146(p.frostAmt),
      backSnowAmt: clamp0146(p.backSnowAmt),
      padSnowAmt: clamp0146(p.padSnowAmt),
      blossomAmt: clamp0146(p.blossomAmt),
      fallenLeafAmt: clamp0146(p.fallenLeafAmt),
      breathFogAmt: clamp0146(p.breathFogAmt)
    };
  }
  function paintPad13(ctx, p) {
    ctx.fillStyle = rgb45([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb45(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb45([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb45([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb45([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb45([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb45([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb45(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb45([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg11(ctx, p, x, topY, baseY, dim = false) {
    ctx.save();
    if (dim) ctx.globalAlpha = 0.85;
    ctx.strokeStyle = rgb45(p.faceDark);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb45([54, 40, 30]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.6, 1.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
    ctx.restore();
  }
  function paintFleeceBody2(ctx, p, cx, cy, fill, scallop) {
    const vol = 0.86 + p.fleeceVolume * 0.34;
    const rx = 11.5 * vol;
    const ry = 8.4 * vol;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
    const lumps = 11;
    const lumpR = (2 + p.fleeceVolume * 1.5) * scallop;
    for (let i = 0; i < lumps; i++) {
      const a = i / lumps * Math.PI * 2 - Math.PI / 2;
      const lx = cx + Math.cos(a) * rx * 0.98;
      const ly = cy + Math.sin(a) * ry * 0.98;
      ctx.beginPath();
      ctx.arc(lx, ly, lumpR, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintNeckHead(ctx, p, baseX, baseY, swing, earFlick) {
    ctx.save();
    ctx.translate(baseX, baseY);
    ctx.rotate(swing);
    const neckTopX = -2.4;
    const neckTopY = -16;
    ctx.strokeStyle = rgb45(p.outline);
    ctx.lineCap = "round";
    ctx.lineWidth = 8 + p.fleeceVolume * 2.4;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-3.2, -8, neckTopX, neckTopY);
    ctx.stroke();
    ctx.strokeStyle = rgb45(p.fleeceShadow);
    ctx.lineWidth = 6.4 + p.fleeceVolume * 2.4;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(-3.2, -8, neckTopX, neckTopY);
    ctx.stroke();
    ctx.strokeStyle = rgb45(p.fleeceLight);
    ctx.lineWidth = 4 + p.fleeceVolume * 2;
    ctx.beginPath();
    ctx.moveTo(-1, -1);
    ctx.quadraticCurveTo(-4.2, -9, neckTopX - 1, neckTopY - 1);
    ctx.stroke();
    ctx.lineCap = "butt";
    const hx = neckTopX - 1.6;
    const hy = neckTopY - 3.4;
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 2.2, hy - 3);
      const flick = side === 1 ? earFlick : 0;
      ctx.rotate(side * 0.34 + flick * 0.5);
      ctx.fillStyle = rgb45(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, -2.6, 1.8, 3.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb45(p.faceLight);
      ctx.beginPath();
      ctx.ellipse(0, -2.6, 1.2, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb45(p.faceDark);
      ctx.beginPath();
      ctx.ellipse(0, -1.6, 0.6, 1.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb45(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 4.4, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45(p.faceLight);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 3.7, 3.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45(p.fleeceLight);
    for (const [tx, ty, tr] of [[-1.4, -3.6, 1.9], [1.2, -4, 1.7], [-0.2, -4.6, 1.5]]) {
      ctx.beginPath();
      ctx.arc(hx + tx, hy + ty, tr, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb45(p.faceDark);
    ctx.beginPath();
    ctx.ellipse(hx - 2.4, hy + 2.2, 2.4, 1.8, -0.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45([255, 255, 255], 0.12);
    ctx.beginPath();
    ctx.ellipse(hx - 1.4, hy - 1.2, 1.6, 1.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb45([36, 28, 24]);
    for (const [ex, ey] of [[-2.1, -0.2], [0.6, -0.4]]) {
      ctx.beginPath();
      ctx.arc(hx + ex, hy + ey, 0.78, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb45([255, 255, 255], 0.85);
    for (const [ex, ey] of [[-2.3, -0.45], [0.4, -0.65]]) {
      ctx.beginPath();
      ctx.arc(hx + ex, hy + ey, 0.3, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb45([24, 18, 16]);
    for (const ex of [-3.2, -2]) {
      ctx.beginPath();
      ctx.ellipse(hx + ex, hy + 2.8, 0.42, 0.55, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
  }
  function paintAlpaca(ctx, p, bob, neckSwing, earFlick) {
    const bx = 1;
    const by = 6 - bob;
    paintLeg11(ctx, p, bx + 6, by + 5, 18.6, true);
    paintLeg11(ctx, p, bx - 5.5, by + 5, 18.9, true);
    paintLeg11(ctx, p, bx + 3, by + 5, 19.2);
    paintLeg11(ctx, p, bx - 3, by + 5, 19.4);
    paintFleeceBody2(ctx, p, bx, by, rgb45(p.outline), 1.18);
    paintFleeceBody2(ctx, p, bx, by, rgb45(p.fleeceShadow), 1);
    ctx.save();
    ctx.translate(-1.4, -1.6);
    paintFleeceBody2(ctx, p, bx, by, rgb45(p.fleeceLight), 0.82);
    ctx.restore();
    ctx.fillStyle = rgb45(p.fleeceLight);
    ctx.beginPath();
    ctx.arc(bx + 11, by - 2, 2.2 + p.fleeceVolume * 0.6, 0, Math.PI * 2);
    ctx.fill();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb45([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 7, 8 * (0.9 + p.fleeceVolume * 0.3), 3.1, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb45([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-4, -7.8], [1, -8.2], [5, -7.2]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + p.fleeceVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb45([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-7, 0],
        [-3, 4],
        [4, -2],
        [7, 2],
        [0, -3],
        [-4, -5],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const neckBaseX = bx - 6;
    const neckBaseY = by - 4;
    paintNeckHead(ctx, p, neckBaseX, neckBaseY, neckSwing, earFlick);
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb45([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(neckBaseX - 8, neckBaseY - 17, 3, 2.1, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash12(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb45(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 2, 26, 27, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint45(ctx, pIn, bob, neckSwing = 0, earFlick = 0) {
    const p = clampP34(pIn);
    ctx.save();
    try {
      paintPad13(ctx, p);
      paintAlpaca(ctx, p, bob, neckSwing, earFlick);
      paintLightWash12(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw35(season) {
    return (ctx) => paint45(ctx, SP45[season], 0);
  }
  function anim35(season) {
    return (ctx, t) => {
      const p = clampP34(SP45[season]);
      const bob = bobAt45(t);
      const neckSwing = 0.06 * (1 - Math.cos(t * 0.9)) * 0.5 * Math.sin(t * 0.45);
      const loop = t % 5 / 5;
      const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
      const earFlick = Math.sin(t * 9) * flickGate;
      paint45(ctx, SP45[season], bob, neckSwing, earFlick);
      ctx.save();
      try {
        const bx = 1;
        const by = 6 - bob;
        const neckBaseX = bx - 6;
        const neckBaseY = by - 4;
        const headX = neckBaseX - 4 - neckSwing * 16;
        const headY = neckBaseY - 19;
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb45([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 8 + s * 16;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb45([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 1, 3, 5.5, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb45([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb45([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(headX - reach, headY + 2, 2.4 + breathe * 1.7, 1.7 + breathe * 1.1, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb45([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb45([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 11.5, 8.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition42(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother45(clamp0146(pp));
      paint45(ctx, lerpP45(SP45[from], SP45[to], k), 0);
    };
  }
  var springToSummer39 = makeTransition42(0);
  var summerToAutumn39 = makeTransition42(1);
  var autumnToWinter39 = makeTransition42(2);
  var VARIANTS47 = {
    Spring: { draw: draw35("Spring"), anim: anim35("Spring") },
    Summer: { draw: draw35("Summer"), anim: anim35("Summer") },
    Autumn: { draw: draw35("Autumn"), anim: anim35("Autumn") },
    Winter: { draw: draw35("Winter"), anim: anim35("Winter") }
  };
  var TRANSITIONS47 = {
    0: springToSummer39,
    1: summerToAutumn39,
    2: autumnToWinter39
  };

  // src/textures/seasonal/herd/goat.ts
  function clamp0147(x) {
    if (!(x >= 0)) return 0;
    return x > 1 ? 1 : x;
  }
  function lerp51(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB39(a, b, t) {
    return [lerp51(a[0], b[0], t), lerp51(a[1], b[1], t), lerp51(a[2], b[2], t)];
  }
  function rgb46(c, a = 1) {
    const r = Math.round(clamp0147(c[0] / 255) * 255);
    const g = Math.round(clamp0147(c[1] / 255) * 255);
    const b = Math.round(clamp0147(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0147(a)})`;
  }
  function smoother46(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt46(t, amp = 1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP46 = {
    // Spring — shedding patchy-thin coat; dewy lime pad + blossom; cool-bright.
    Spring: {
      coatLight: [248, 246, 238],
      coatShadow: [206, 200, 186],
      saddleTan: [206, 168, 116],
      horn: [216, 200, 168],
      hoofDark: [60, 52, 46],
      beard: [232, 226, 212],
      padGrass: [128, 206, 88],
      // bright lime dewy grass
      soil: [86, 134, 54],
      outline: [62, 54, 46],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.3,
      // thinned out
      coatPatchy: 0.8,
      // shedding tufts
      coatSheen: 0.18,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — normal sleek coat (PEAK); saturated mid-green pad; warm light.
    Summer: {
      coatLight: [250, 248, 240],
      coatShadow: [202, 196, 182],
      saddleTan: [200, 158, 102],
      horn: [214, 196, 160],
      hoofDark: [56, 48, 42],
      beard: [236, 230, 218],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [56, 48, 42],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.15,
      coatVolume: 0.5,
      // normal coat
      coatPatchy: 0,
      coatSheen: 0.9,
      // sleek
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — fuller coat; olive-tan browning pad + fallen leaves; low amber.
    Autumn: {
      coatLight: [244, 238, 226],
      coatShadow: [198, 188, 170],
      saddleTan: [190, 142, 84],
      horn: [206, 184, 144],
      hoofDark: [58, 46, 40],
      beard: [228, 220, 204],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [58, 46, 38],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.72,
      // fuller, thicker
      coatPatchy: 0,
      coatSheen: 0.35,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — shaggy thick coat + back snow + breath-fog; snowy pad; cool light.
    Winter: {
      coatLight: [252, 251, 248],
      coatShadow: [208, 210, 218],
      saddleTan: [186, 154, 110],
      horn: [206, 196, 176],
      hoofDark: [62, 58, 64],
      beard: [236, 234, 230],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [70, 70, 82],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // shaggy thick winter coat
      coatPatchy: 0,
      coatSheen: 0.1,
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP46(a, b, t) {
    return {
      coatLight: lerpRGB39(a.coatLight, b.coatLight, t),
      coatShadow: lerpRGB39(a.coatShadow, b.coatShadow, t),
      saddleTan: lerpRGB39(a.saddleTan, b.saddleTan, t),
      horn: lerpRGB39(a.horn, b.horn, t),
      hoofDark: lerpRGB39(a.hoofDark, b.hoofDark, t),
      beard: lerpRGB39(a.beard, b.beard, t),
      padGrass: lerpRGB39(a.padGrass, b.padGrass, t),
      soil: lerpRGB39(a.soil, b.soil, t),
      outline: lerpRGB39(a.outline, b.outline, t),
      lightWash: lerpRGB39(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp51(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp51(a.coatVolume, b.coatVolume, t),
      coatPatchy: lerp51(a.coatPatchy, b.coatPatchy, t),
      coatSheen: lerp51(a.coatSheen, b.coatSheen, t),
      frostAmt: lerp51(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp51(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp51(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp51(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp51(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp51(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP35(p) {
    return {
      ...p,
      lightWashAmt: clamp0147(p.lightWashAmt),
      coatVolume: clamp0147(p.coatVolume),
      coatPatchy: clamp0147(p.coatPatchy),
      coatSheen: clamp0147(p.coatSheen),
      frostAmt: clamp0147(p.frostAmt),
      backSnowAmt: clamp0147(p.backSnowAmt),
      padSnowAmt: clamp0147(p.padSnowAmt),
      blossomAmt: clamp0147(p.blossomAmt),
      fallenLeafAmt: clamp0147(p.fallenLeafAmt),
      breathFogAmt: clamp0147(p.breathFogAmt)
    };
  }
  function paintPad14(ctx, p) {
    ctx.fillStyle = rgb46([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb46(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb46([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb46([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb46([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb46([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb46([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb46(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb46([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg12(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb46(p.coatShadow);
    ctx.lineWidth = 2.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 2.4);
    ctx.stroke();
    ctx.strokeStyle = rgb46(p.hoofDark);
    ctx.lineWidth = 2.2;
    ctx.beginPath();
    ctx.moveTo(x, baseY - 3);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb46([30, 26, 28]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.6, 1.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function paintBody3(ctx, p, cx, cy, fill, grow) {
    const vol = 0.9 + p.coatVolume * 0.22;
    const rx = 12.5 * vol * grow;
    const ry = 8 * vol * grow;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.06, 0, Math.PI * 2);
    ctx.fill();
  }
  function paintCoatShag(ctx, p, cx, cy) {
    if (p.coatVolume <= 0.34) return;
    const amt = (p.coatVolume - 0.34) / 0.66;
    ctx.fillStyle = rgb46(p.coatShadow, 0.9);
    const rx = 12.5 * (0.9 + p.coatVolume * 0.22);
    const ry = 8 * (0.9 + p.coatVolume * 0.22);
    const tufts = 9;
    for (let i = 0; i < tufts; i++) {
      const f = i / (tufts - 1);
      const ang = lerp51(Math.PI * 0.18, Math.PI * 0.92, f);
      const lx = cx + Math.cos(ang) * rx * 0.96;
      const ly = cy + Math.sin(ang) * ry * 0.96;
      const drop = (2 + amt * 3.2) * (0.6 + 0.4 * Math.sin(i * 1.7));
      ctx.beginPath();
      ctx.moveTo(lx - 1.5, ly - 1);
      ctx.lineTo(lx, ly + drop);
      ctx.lineTo(lx + 1.5, ly - 1);
      ctx.closePath();
      ctx.fill();
    }
  }
  function paintGoat(ctx, p, bob) {
    const bx = 1;
    const by = 4 - bob;
    ctx.save();
    ctx.globalAlpha = 0.85;
    paintLeg12(ctx, p, bx + 7, by + 5, 18.6);
    paintLeg12(ctx, p, bx - 6, by + 5, 18.9);
    ctx.restore();
    paintLeg12(ctx, p, bx + 4.5, by + 5.5, 19.3);
    paintLeg12(ctx, p, bx - 3.5, by + 5.5, 19.6);
    paintCoatShag(ctx, p, bx, by + 1);
    paintBody3(ctx, p, bx, by, rgb46(p.outline), 1.1);
    paintBody3(ctx, p, bx, by, rgb46(p.coatShadow), 1);
    ctx.save();
    ctx.translate(-1.2, -1.4);
    paintBody3(ctx, p, bx, by, rgb46(p.coatLight), 0.82);
    ctx.restore();
    ctx.save();
    ctx.beginPath();
    ctx.ellipse(bx, by, 12.5 * (0.9 + p.coatVolume * 0.22), 8 * (0.9 + p.coatVolume * 0.22), -0.06, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb46(p.saddleTan, 0.92);
    ctx.beginPath();
    ctx.ellipse(bx + 4.5, by - 1.5, 7.5, 5.5, -0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.saddleTan, 0.7);
    ctx.beginPath();
    ctx.ellipse(bx - 6, by + 3, 4, 3, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.coatPatchy > 1e-3) {
      ctx.fillStyle = rgb46(p.coatLight, 0.55 * p.coatPatchy);
      for (const [dx, dy] of [[-6, -5], [2, -6], [6, -3], [-2, -2], [8, 1]]) {
        ctx.beginPath();
        ctx.ellipse(bx + dx, by + dy, 1.8, 1.1, 0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.strokeStyle = rgb46(p.coatShadow, 0.4 * p.coatPatchy);
      ctx.lineWidth = 0.8;
      for (const [wx, wy] of [[-4, -8], [5, -9]]) {
        ctx.beginPath();
        ctx.moveTo(bx + wx, by + wy);
        ctx.lineTo(bx + wx + 1.4, by + wy - 2.4);
        ctx.stroke();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb46([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx + 1, by - 6.6, 9.5 * (0.9 + p.coatVolume * 0.2), 3.2, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb46([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-4, -7.4], [2, -8], [6, -7]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + p.coatVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb46([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-3, 4],
        [4, -2],
        [8, 2],
        [0, -3],
        [-5, -4],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.strokeStyle = rgb46(p.coatShadow);
    ctx.lineWidth = 2.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx + 11, by - 2);
    ctx.lineTo(bx + 13.5, by - 5);
    ctx.stroke();
    ctx.strokeStyle = rgb46(p.coatLight);
    ctx.lineWidth = 1.2;
    ctx.beginPath();
    ctx.moveTo(bx + 11, by - 2);
    ctx.lineTo(bx + 13.4, by - 4.8);
    ctx.stroke();
    ctx.lineCap = "butt";
    const hx = bx - 11;
    const hy = by + 1.5;
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 3.4, hy - 3.2);
      ctx.rotate(side * 0.5 - 0.1);
      ctx.fillStyle = rgb46(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, 0, 1.9, 3.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb46(p.coatLight);
      ctx.beginPath();
      ctx.ellipse(0, 0, 1.4, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb46(p.saddleTan, 0.6);
      ctx.beginPath();
      ctx.ellipse(0, 0.4, 0.7, 1.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    for (const side of [-1, 1]) {
      const baseX = hx + side * 1.6;
      const baseY = hy - 4.2;
      ctx.strokeStyle = rgb46(p.outline);
      ctx.lineWidth = 3;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + side * 0.5 + 2.2, baseY - 6.5, baseX + 4.5, baseY - 4.5);
      ctx.stroke();
      ctx.strokeStyle = rgb46(p.horn);
      ctx.lineWidth = 1.8;
      ctx.beginPath();
      ctx.moveTo(baseX, baseY);
      ctx.quadraticCurveTo(baseX + side * 0.5 + 2.2, baseY - 6.5, baseX + 4.5, baseY - 4.5);
      ctx.stroke();
      ctx.strokeStyle = rgb46([255, 255, 255], 0.3);
      ctx.lineWidth = 0.7;
      ctx.beginPath();
      ctx.moveTo(baseX, baseY - 0.4);
      ctx.quadraticCurveTo(baseX + side * 0.5 + 2, baseY - 6.4, baseX + 4.2, baseY - 4.6);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb46(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(hx, hy - 3.8, 2.4, 1.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.34);
    ctx.fillStyle = rgb46(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0.4, 4.2, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(0, 0.4, 3.6, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.saddleTan, 0.45);
    ctx.beginPath();
    ctx.ellipse(0.4, -1.6, 1.8, 2.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(0.2, 4.2, 1.9, 1.5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46([255, 255, 255], 0.16);
    ctx.beginPath();
    ctx.ellipse(-1.4, -1.4, 1.6, 2.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb46([245, 245, 240]);
    for (const ex of [-1.7, 1.5]) {
      ctx.beginPath();
      ctx.arc(ex, -0.6, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb46([26, 22, 24]);
    for (const ex of [-1.6, 1.6]) {
      ctx.beginPath();
      ctx.ellipse(ex, -0.4, 0.5, 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb46([16, 14, 16]);
    for (const ex of [-0.7, 1]) {
      ctx.beginPath();
      ctx.ellipse(ex, 4.2, 0.4, 0.6, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    {
      const cxb = hx - 1.6;
      const cyb = hy + 5.6;
      ctx.fillStyle = rgb46(p.outline);
      ctx.beginPath();
      ctx.moveTo(cxb - 1.8, cyb - 1.6);
      ctx.lineTo(cxb - 2.4, cyb + 4.2);
      ctx.lineTo(cxb, cyb + 2.4);
      ctx.lineTo(cxb + 2, cyb + 4);
      ctx.lineTo(cxb + 1.6, cyb - 1.6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb46(p.beard);
      ctx.beginPath();
      ctx.moveTo(cxb - 1.3, cyb - 1.4);
      ctx.lineTo(cxb - 1.8, cyb + 3.6);
      ctx.lineTo(cxb, cyb + 2);
      ctx.lineTo(cxb + 1.5, cyb + 3.4);
      ctx.lineTo(cxb + 1.2, cyb - 1.4);
      ctx.closePath();
      ctx.fill();
    }
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb46([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 4.4, hy + 4.6, 3, 2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash13(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb46(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint46(ctx, pIn, bob) {
    const p = clampP35(pIn);
    ctx.save();
    try {
      paintPad14(ctx, p);
      paintGoat(ctx, p, bob);
      paintLightWash13(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw36(season) {
    return (ctx) => paint46(ctx, SP46[season], 0);
  }
  function anim36(season) {
    return (ctx, t) => {
      const p = clampP35(SP46[season]);
      const bob = bobAt46(t);
      paint46(ctx, SP46[season], bob);
      ctx.save();
      try {
        const bx = 1;
        const by = 4 - bob;
        const hx = bx - 11;
        const hy = by + 1.5;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.save();
        ctx.translate(hx - 3.4, hy - 3.2);
        ctx.rotate(-0.6 + flick * 0.5);
        ctx.fillStyle = rgb46(p.outline);
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.9, 3.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb46(p.coatLight);
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.4, 3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        const beardSway = flickGate * 0.9 + Math.sin(t * 1.5) * 0.18 * flickGate;
        const cxb = hx - 1.6 + beardSway;
        const cyb = hy + 5.6;
        ctx.fillStyle = rgb46(p.beard);
        ctx.beginPath();
        ctx.moveTo(cxb - 1.3, cyb - 1.4);
        ctx.lineTo(cxb - 1.8 + beardSway, cyb + 3.6);
        ctx.lineTo(cxb + beardSway, cyb + 2);
        ctx.lineTo(cxb + 1.5 + beardSway, cyb + 3.4);
        ctx.lineTo(cxb + 1.2, cyb - 1.4);
        ctx.closePath();
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb46([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb46([255, 255, 255], 0.3 + 0.2 * p.coatSheen);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb46([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 3 + breathe * 3;
          ctx.fillStyle = rgb46([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach, hy + 4.6, 2.4 + breathe * 1.6, 1.6 + breathe * 1, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb46([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb46([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 12.5, 8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition43(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother46(clamp0147(pp));
      paint46(ctx, lerpP46(SP46[from], SP46[to], k), 0);
    };
  }
  var springToSummer40 = makeTransition43(0);
  var summerToAutumn40 = makeTransition43(1);
  var autumnToWinter40 = makeTransition43(2);
  var VARIANTS48 = {
    Spring: { draw: draw36("Spring"), anim: anim36("Spring") },
    Summer: { draw: draw36("Summer"), anim: anim36("Summer") },
    Autumn: { draw: draw36("Autumn"), anim: anim36("Autumn") },
    Winter: { draw: draw36("Winter"), anim: anim36("Winter") }
  };
  var TRANSITIONS48 = {
    0: springToSummer40,
    1: summerToAutumn40,
    2: autumnToWinter40
  };

  // src/textures/seasonal/herd/ram.ts
  function clamp0148(x) {
    return x < 0 ? 0 : x > 1 ? 1 : x;
  }
  function lerp52(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB40(a, b, t) {
    return [lerp52(a[0], b[0], t), lerp52(a[1], b[1], t), lerp52(a[2], b[2], t)];
  }
  function rgb47(c, a = 1) {
    const r = Math.round(clamp0148(c[0] / 255) * 255);
    const g = Math.round(clamp0148(c[1] / 255) * 255);
    const b = Math.round(clamp0148(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0148(a)})`;
  }
  function smoother47(x) {
    return x * x * x * (x * (6 * x - 15) + 10);
  }
  function bobAt47(t, amp = 1.1, w = 1.5) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP47 = {
    Spring: {
      fleeceLight: [248, 244, 232],
      // cream
      fleeceShadow: [212, 204, 184],
      faceCoat: [216, 196, 162],
      faceShade: [176, 154, 120],
      hornLight: [224, 206, 168],
      hornShade: [168, 144, 104],
      legDark: [92, 78, 64],
      padGrass: [126, 198, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [62, 52, 42],
      lightWash: [212, 238, 255],
      // cool-bright, dewy
      lightWashAmt: 0.16,
      fleeceVolume: 0.16,
      // recently-shorn: lighter, slimmer fleece
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.6,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Summer: {
      fleeceLight: [250, 246, 232],
      // cream, richest
      fleeceShadow: [210, 200, 178],
      faceCoat: [220, 198, 160],
      faceShade: [178, 154, 116],
      hornLight: [226, 206, 166],
      hornShade: [166, 142, 100],
      legDark: [88, 74, 60],
      padGrass: [86, 168, 64],
      // saturated mid-green
      soil: [58, 110, 40],
      outline: [58, 48, 40],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      fleeceVolume: 0.52,
      // full fleece (PEAK)
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    Autumn: {
      fleeceLight: [244, 236, 218],
      // cream warming
      fleeceShadow: [204, 192, 168],
      faceCoat: [212, 188, 148],
      faceShade: [170, 144, 104],
      hornLight: [220, 198, 154],
      hornShade: [160, 134, 92],
      legDark: [86, 70, 56],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [60, 48, 38],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      fleeceVolume: 0.74,
      // fuller, thicker
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    Winter: {
      fleeceLight: [250, 250, 246],
      // cream, cooled toward white by the light
      fleeceShadow: [212, 214, 214],
      faceCoat: [206, 192, 168],
      faceShade: [164, 150, 132],
      hornLight: [216, 204, 184],
      // keratin reads cooler under cold light
      hornShade: [156, 142, 120],
      legDark: [80, 72, 70],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [70, 66, 72],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      fleeceVolume: 1,
      // heavy thick puffed wool
      frostAmt: 0.7,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP47(a, b, t) {
    return {
      fleeceLight: lerpRGB40(a.fleeceLight, b.fleeceLight, t),
      fleeceShadow: lerpRGB40(a.fleeceShadow, b.fleeceShadow, t),
      faceCoat: lerpRGB40(a.faceCoat, b.faceCoat, t),
      faceShade: lerpRGB40(a.faceShade, b.faceShade, t),
      hornLight: lerpRGB40(a.hornLight, b.hornLight, t),
      hornShade: lerpRGB40(a.hornShade, b.hornShade, t),
      legDark: lerpRGB40(a.legDark, b.legDark, t),
      padGrass: lerpRGB40(a.padGrass, b.padGrass, t),
      soil: lerpRGB40(a.soil, b.soil, t),
      outline: lerpRGB40(a.outline, b.outline, t),
      lightWash: lerpRGB40(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp52(a.lightWashAmt, b.lightWashAmt, t),
      fleeceVolume: lerp52(a.fleeceVolume, b.fleeceVolume, t),
      frostAmt: lerp52(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp52(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp52(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp52(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp52(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp52(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP36(p) {
    return {
      ...p,
      lightWashAmt: clamp0148(p.lightWashAmt),
      fleeceVolume: clamp0148(p.fleeceVolume),
      frostAmt: clamp0148(p.frostAmt),
      backSnowAmt: clamp0148(p.backSnowAmt),
      padSnowAmt: clamp0148(p.padSnowAmt),
      blossomAmt: clamp0148(p.blossomAmt),
      fallenLeafAmt: clamp0148(p.fallenLeafAmt),
      breathFogAmt: clamp0148(p.breathFogAmt)
    };
  }
  function paintPad15(ctx, p) {
    ctx.fillStyle = rgb47([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb47(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb47([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb47([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb47([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-12, 18.5], [9, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb47([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb47([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-11, 20, -0.5, [196, 96, 36]],
        [10, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb47(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb47([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg13(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb47(p.legDark);
    ctx.lineWidth = 3.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.fillStyle = rgb47([30, 26, 26]);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.9, 1.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function paintFleeceBody3(ctx, p, cx, cy, fill, scallop) {
    const vol = 0.86 + p.fleeceVolume * 0.42;
    const rx = 13.6 * vol;
    const ry = 9.8 * vol;
    ctx.fillStyle = fill;
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
    ctx.fill();
    const lumps = 11;
    const lumpR = (2.3 + p.fleeceVolume * 1.7) * scallop;
    for (let i = 0; i < lumps; i++) {
      const a = i / lumps * Math.PI * 2 - Math.PI / 2;
      const lx = cx + Math.cos(a) * rx * 0.98;
      const ly = cy + Math.sin(a) * ry * 0.98;
      ctx.beginPath();
      ctx.arc(lx, ly, lumpR, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintHorn(ctx, p, ox, oy, side) {
    const turns = 1.55;
    const steps = 22;
    const a0 = side === 1 ? -1.1 : Math.PI + 1.1;
    const r0 = 5.6;
    const discs = [];
    for (let i = 0; i <= steps; i++) {
      const f = i / steps;
      const ang = a0 + side * f * turns * Math.PI * 2;
      const rad = r0 * (1 - f * 0.78);
      const cx = ox + side * 4.2 + Math.cos(ang) * rad;
      const cy = oy + 0.5 + Math.sin(ang) * rad * 0.9;
      const thick = 2.9 * (1 - f * 0.55);
      discs.push({ x: cx, y: cy, r: thick });
    }
    ctx.fillStyle = rgb47(p.outline);
    for (const d of discs) {
      ctx.beginPath();
      ctx.arc(d.x, d.y, d.r + 0.9, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb47(p.hornLight);
    for (const d of discs) {
      ctx.beginPath();
      ctx.arc(d.x, d.y, d.r, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb47(p.hornShade);
    for (let i = 0; i < discs.length; i++) {
      const d = discs[i];
      ctx.beginPath();
      ctx.arc(d.x + 0.8, d.y + 0.8, d.r * 0.5, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.strokeStyle = rgb47(p.hornShade, 0.7);
    ctx.lineWidth = 0.7;
    for (let i = 2; i < discs.length - 1; i += 2) {
      const d = discs[i];
      ctx.beginPath();
      ctx.moveTo(d.x - d.r, d.y - d.r * 0.4);
      ctx.lineTo(d.x + d.r, d.y + d.r * 0.4);
      ctx.stroke();
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb47([255, 255, 255], 0.8 * p.frostAmt);
      for (let i = 0; i < discs.length; i += 3) {
        const d = discs[i];
        ctx.beginPath();
        ctx.arc(d.x - 0.6, d.y - d.r * 0.6, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
  }
  function paintRam(ctx, p, bob) {
    const bx = -1;
    const by = 4 - bob;
    ctx.save();
    ctx.globalAlpha = 0.85;
    paintLeg13(ctx, p, bx + 6.8, by + 7, 18.6);
    paintLeg13(ctx, p, bx - 7.8, by + 7, 18.9);
    ctx.restore();
    paintLeg13(ctx, p, bx + 3.6, by + 7, 19.2);
    paintLeg13(ctx, p, bx - 4.6, by + 7, 19.4);
    paintFleeceBody3(ctx, p, bx, by, rgb47(p.outline), 1.18);
    paintFleeceBody3(ctx, p, bx, by, rgb47(p.fleeceShadow), 1);
    ctx.save();
    ctx.translate(-1.4, -1.6);
    paintFleeceBody3(ctx, p, bx, by, rgb47(p.fleeceLight), 0.82);
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb47([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 8, 9.4 * (0.9 + p.fleeceVolume * 0.3), 3.6, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb47([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -8.8], [1, -9.4], [5, -8.4]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.6 + p.fleeceVolume * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb47([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-3, 4],
        [4, -2],
        [8, 2],
        [0, -3],
        [-5, -5],
        [6, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 11;
    const hy = by + 3;
    ctx.fillStyle = rgb47(p.faceShade);
    for (const sideE of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + sideE * 4.4, hy - 1.5);
      ctx.rotate(sideE * 0.7 + 0.1);
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.2, 1.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb47(p.fleeceLight);
    ctx.beginPath();
    ctx.ellipse(hx, hy - 5, 3.2, 2.3, 0, 0, Math.PI * 2);
    ctx.fill();
    paintHorn(ctx, p, hx, hy - 2.2, -1);
    paintHorn(ctx, p, hx, hy - 2.2, 1);
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.22);
    ctx.fillStyle = rgb47(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 6, 5.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47(p.faceCoat);
    ctx.beginPath();
    ctx.ellipse(0, 0, 5.3, 4.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47(p.faceShade);
    ctx.beginPath();
    ctx.ellipse(1.8, 0.6, 3.4, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47(p.fleeceLight, 0.55);
    ctx.beginPath();
    ctx.ellipse(-0.6, 1.4, 2, 3.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47([255, 255, 255], 0.14);
    ctx.beginPath();
    ctx.ellipse(-2, -1.8, 2, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb47([245, 244, 236]);
    for (const ex of [-2, 2]) {
      ctx.beginPath();
      ctx.arc(ex, -1.3, 1.1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb47([26, 22, 22]);
    for (const ex of [-1.8, 2.2]) {
      ctx.beginPath();
      ctx.ellipse(ex, -1.1, 0.7, 0.5, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb47([24, 20, 20]);
    for (const ex of [-1, 1]) {
      ctx.beginPath();
      ctx.ellipse(ex, 3.6, 0.5, 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    ctx.fillStyle = rgb47(p.fleeceLight);
    ctx.beginPath();
    ctx.arc(bx + 13, by - 1, 2.5 + p.fleeceVolume * 0.6, 0, Math.PI * 2);
    ctx.fill();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb47([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 6.5, hy + 4, 3.4, 2.3, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash14(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb47(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint47(ctx, pIn, bob) {
    const p = clampP36(pIn);
    ctx.save();
    try {
      paintPad15(ctx, p);
      paintRam(ctx, p, bob);
      paintLightWash14(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw37(season) {
    return (ctx) => paint47(ctx, SP47[season], 0);
  }
  function anim37(season) {
    return (ctx, t) => {
      const p = clampP36(SP47[season]);
      const bob = bobAt47(t);
      const loop = t % 6 / 6;
      const dipGate = Math.max(0, Math.sin(loop * Math.PI)) ** 2;
      const headDip = dipGate * 1.4;
      paint47(ctx, SP47[season], bob);
      ctx.save();
      try {
        const bx = -1;
        const by = 4 - bob;
        const hx = bx - 11;
        const hy = by + 3 + headDip;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        if (headDip > 0.05) {
          ctx.save();
          ctx.translate(hx, hy);
          ctx.rotate(0.22);
          ctx.fillStyle = rgb47(p.outline);
          ctx.beginPath();
          ctx.ellipse(0, 0, 6, 5.6, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb47(p.faceCoat);
          ctx.beginPath();
          ctx.ellipse(0, 0, 5.3, 4.9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb47(p.faceShade);
          ctx.beginPath();
          ctx.ellipse(1.8, 0.6, 3.4, 4.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgb47([245, 244, 236]);
          for (const ex of [-2, 2]) {
            ctx.beginPath();
            ctx.arc(ex, -1.3, 1.1, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgb47([26, 22, 22]);
          for (const ex of [-1.8, 2.2]) {
            ctx.beginPath();
            ctx.ellipse(ex, -1.1, 0.7, 0.5, 0, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgb47([24, 20, 20]);
          for (const ex of [-1, 1]) {
            ctx.beginPath();
            ctx.ellipse(ex, 3.6, 0.5, 0.8, 0, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.restore();
        }
        ctx.fillStyle = rgb47(p.faceShade);
        ctx.save();
        ctx.translate(hx + 4.4, hy - 1.5);
        ctx.rotate(0.6 + flick * 0.5);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.2, 1.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        ctx.fillStyle = rgb47(p.fleeceLight);
        ctx.beginPath();
        ctx.arc(bx + 13 + flick * 1.4, by - 1, 2.5 + p.fleeceVolume * 0.6, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb47([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb47([255, 255, 255], 0.35);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(10 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb47([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.5);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb47([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - 2 - reach, hy + 4, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb47([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb47([206, 224, 255], 0.12 + sheen2 * 0.14);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13.6, 9.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition44(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother47(clamp0148(pp));
      paint47(ctx, lerpP47(SP47[from], SP47[to], k), 0);
    };
  }
  var springToSummer41 = makeTransition44(0);
  var summerToAutumn41 = makeTransition44(1);
  var autumnToWinter41 = makeTransition44(2);
  var VARIANTS49 = {
    Spring: { draw: draw37("Spring"), anim: anim37("Spring") },
    Summer: { draw: draw37("Summer"), anim: anim37("Summer") },
    Autumn: { draw: draw37("Autumn"), anim: anim37("Autumn") },
    Winter: { draw: draw37("Winter"), anim: anim37("Winter") }
  };
  var TRANSITIONS49 = {
    0: springToSummer41,
    1: summerToAutumn41,
    2: autumnToWinter41
  };

  // src/textures/seasonal/cattle/cow.ts
  function clamp0149(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp53(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB41(a, b, t) {
    return [lerp53(a[0], b[0], t), lerp53(a[1], b[1], t), lerp53(a[2], b[2], t)];
  }
  function rgb48(c, a = 1) {
    const r = Math.round(clamp0149(c[0] / 255) * 255);
    const g = Math.round(clamp0149(c[1] / 255) * 255);
    const b = Math.round(clamp0149(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0149(a)})`;
  }
  var smoother48 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt48(t, amp = 1, w = 1.35) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP48 = {
    // Spring — sleek coat, dewy lime pad + blossom, cool-bright light.
    Spring: {
      hideLight: [250, 250, 248],
      hideShadow: [208, 210, 210],
      patch: [44, 40, 44],
      muzzle: [238, 168, 176],
      hornDark: [60, 54, 50],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [54, 48, 46],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.12,
      // sleek
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — normal coat (PEAK), saturated mid-green pad, warm light.
    Summer: {
      hideLight: [252, 251, 247],
      hideShadow: [204, 202, 198],
      patch: [40, 36, 40],
      muzzle: [240, 162, 170],
      hornDark: [56, 50, 46],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [50, 44, 42],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.42,
      // normal full coat
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — slightly fuller coat, olive-tan browning pad, fallen leaves.
    Autumn: {
      hideLight: [246, 242, 234],
      hideShadow: [200, 194, 184],
      patch: [42, 36, 36],
      muzzle: [232, 156, 160],
      hornDark: [58, 48, 42],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [54, 44, 38],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.62,
      // slightly fuller
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — thick shaggy coat + back-snow + breath fog, snowy pad, cool light.
    Winter: {
      hideLight: [253, 254, 255],
      hideShadow: [206, 216, 228],
      patch: [50, 46, 54],
      muzzle: [226, 150, 162],
      hornDark: [62, 58, 66],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [64, 62, 74],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick & shaggy
      frostAmt: 0.65,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP48(a, b, t) {
    return {
      hideLight: lerpRGB41(a.hideLight, b.hideLight, t),
      hideShadow: lerpRGB41(a.hideShadow, b.hideShadow, t),
      patch: lerpRGB41(a.patch, b.patch, t),
      muzzle: lerpRGB41(a.muzzle, b.muzzle, t),
      hornDark: lerpRGB41(a.hornDark, b.hornDark, t),
      padGrass: lerpRGB41(a.padGrass, b.padGrass, t),
      soil: lerpRGB41(a.soil, b.soil, t),
      outline: lerpRGB41(a.outline, b.outline, t),
      lightWash: lerpRGB41(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp53(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp53(a.coatVolume, b.coatVolume, t),
      frostAmt: lerp53(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp53(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp53(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp53(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp53(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp53(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP37(p) {
    return {
      ...p,
      lightWashAmt: clamp0149(p.lightWashAmt),
      coatVolume: clamp0149(p.coatVolume),
      frostAmt: clamp0149(p.frostAmt),
      backSnowAmt: clamp0149(p.backSnowAmt),
      padSnowAmt: clamp0149(p.padSnowAmt),
      blossomAmt: clamp0149(p.blossomAmt),
      fallenLeafAmt: clamp0149(p.fallenLeafAmt),
      breathFogAmt: clamp0149(p.breathFogAmt)
    };
  }
  function paintPad16(ctx, p) {
    ctx.fillStyle = rgb48([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb48(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb48([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb48([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb48([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb48([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb48([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb48(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb48([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg14(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb48(p.outline);
    ctx.lineWidth = 4.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb48(p.hideShadow);
    ctx.lineWidth = 3;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1);
    ctx.stroke();
    ctx.fillStyle = rgb48(p.hornDark);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 2, 1.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath5(ctx, cx, cy, vol) {
    const rx = 13.5 * (0.97 + vol * 0.12);
    const ry = 8.8 * (0.97 + vol * 0.14);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
  }
  function paintCow(ctx, p, bob, headDip = 0) {
    const bx = 1;
    const by = 2 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg14(ctx, p, bx + 8, by + 6, 18.6);
    paintLeg14(ctx, p, bx - 6.5, by + 6, 18.9);
    ctx.restore();
    paintLeg14(ctx, p, bx + 4, by + 6.5, 19.4);
    paintLeg14(ctx, p, bx - 9.5, by + 6.5, 19.6);
    ctx.fillStyle = rgb48(p.outline);
    bodyPath5(ctx, bx, by, vol);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.1, 1.14);
    ctx.translate(-bx, -by);
    bodyPath5(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb48(p.hideShadow);
    bodyPath5(ctx, bx, by, vol);
    ctx.fill();
    ctx.save();
    ctx.fillStyle = rgb48(p.hideLight);
    ctx.translate(-1.4, -1.6);
    bodyPath5(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    if (vol > 0.55) {
      const fr = (vol - 0.55) / 0.45;
      ctx.fillStyle = rgb48(p.hideShadow, 0.9);
      const rx = 13.5 * (0.97 + vol * 0.12);
      const ry = 8.8 * (0.97 + vol * 0.14);
      for (let i = 0; i < 9; i++) {
        const a = Math.PI * 0.15 + i / 8 * Math.PI * 0.9;
        const lx = bx + Math.cos(a) * rx * 1.02;
        const ly = by + Math.sin(a) * ry * 1.02;
        ctx.beginPath();
        ctx.arc(lx, ly, (1.2 + 0.9 * fr) * (i % 2 ? 1 : 0.8), 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.save();
    bodyPath5(ctx, bx, by, vol);
    ctx.clip();
    ctx.fillStyle = rgb48(p.patch);
    const patches = [
      [bx - 7, by - 3, 5.2, 4.4, -0.3],
      [bx + 5.5, by - 1.5, 5.8, 5.2, 0.25],
      [bx + 9.5, by + 4, 3.4, 3, 0.1],
      [bx - 2, by + 4.5, 4, 2.8, -0.15]
    ];
    for (const [px, py, prx, pry, rot] of patches) {
      ctx.save();
      ctx.translate(px, py);
      ctx.rotate(rot);
      ctx.beginPath();
      ctx.ellipse(0, 0, prx, pry, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(prx * 0.7, -pry * 0.4, prx * 0.45, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(-prx * 0.5, pry * 0.5, pry * 0.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb48([255, 255, 255], 0.08);
    ctx.beginPath();
    ctx.ellipse(bx - 3, by - 4, 9, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb48([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx, by - 7.4, 10 * (0.9 + vol * 0.2), 3.2, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb48([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-6, -8.2], [0, -8.8], [6, -8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb48([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-3, 4],
        [4, -2],
        [9, 2],
        [0, -3],
        [-5, -5],
        [7, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.strokeStyle = rgb48(p.outline);
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx + 13, by - 2);
    ctx.quadraticCurveTo(bx + 17, by + 3, bx + 15.5, by + 9);
    ctx.stroke();
    ctx.strokeStyle = rgb48(p.hideShadow);
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(bx + 13, by - 2);
    ctx.quadraticCurveTo(bx + 17, by + 3, bx + 15.5, by + 9);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb48(p.patch);
    ctx.beginPath();
    ctx.ellipse(bx + 15.5, by + 10, 1.6, 2.6, 0.15, 0, Math.PI * 2);
    ctx.fill();
    const hx = bx - 14;
    const hy = by + 3 + headDip;
    ctx.strokeStyle = rgb48(p.hornDark);
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    for (const side of [-1, 1]) {
      ctx.beginPath();
      ctx.moveTo(hx + side * 2.6, hy - 5.2);
      ctx.quadraticCurveTo(hx + side * 4.6, hy - 7.6, hx + side * 4, hy - 8.8);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 5.6, hy - 3.2);
      ctx.rotate(side * 0.7);
      ctx.fillStyle = rgb48(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.4, 1.9, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb48(p.hideLight);
      ctx.beginPath();
      ctx.ellipse(0, 0, 2.8, 1.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb48(p.muzzle);
      ctx.beginPath();
      ctx.ellipse(side * -0.3, 0.1, 1.5, 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.22);
    ctx.fillStyle = rgb48(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 5.4, 6.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48(p.hideShadow);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.7, 5.7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48(p.hideLight);
    ctx.beginPath();
    ctx.ellipse(-0.6, -0.8, 4.1, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.7, 5.7, 0, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb48(p.patch);
    ctx.beginPath();
    ctx.ellipse(1.8, -2.2, 2.6, 2.4, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb48([245, 245, 240]);
    for (const ex of [-1.8, 1.8]) {
      ctx.beginPath();
      ctx.arc(ex, -1, 1.05, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb48([20, 18, 20]);
    for (const ex of [-1.6, 2]) {
      ctx.beginPath();
      ctx.arc(ex, -0.8, 0.6, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb48(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 3.6, 4, 2.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48(p.muzzle);
    ctx.beginPath();
    ctx.ellipse(0, 3.4, 3.4, 2.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48([255, 255, 255], 0.18);
    ctx.beginPath();
    ctx.ellipse(-1.2, 2.4, 1.4, 0.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb48([150, 70, 84]);
    for (const ex of [-1.2, 1.2]) {
      ctx.beginPath();
      ctx.ellipse(ex, 3.9, 0.55, 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb48([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 5.5, hy + 4.4, 3.2, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash15(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb48(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint48(ctx, pIn, bob, headDip = 0) {
    const p = clampP37(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad16(ctx, p);
      paintCow(ctx, p, bob, headDip);
      paintLightWash15(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw38(season) {
    return (ctx) => paint48(ctx, SP48[season], 0);
  }
  function anim38(season) {
    return (ctx, t) => {
      const p = clampP37(SP48[season]);
      const bob = bobAt48(t);
      const dipLoop = t % 6 / 6;
      const dip = 0.9 * (1 - Math.cos(dipLoop * Math.PI * 2)) * 0.5;
      paint48(ctx, SP48[season], bob, dip);
      ctx.save();
      try {
        const bx = 1;
        const by = 2 - bob;
        const hx = bx - 14;
        const hy = by + 3 + dip;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.save();
        ctx.translate(hx - 5.6, hy - 3.2);
        ctx.rotate(-0.7 + flick * 0.6);
        ctx.fillStyle = rgb48(p.outline);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.9, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb48(p.hideLight);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.8, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb48(p.muzzle);
        ctx.beginPath();
        ctx.ellipse(0.3, 0.1, 1.5, 0.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        const swish = Math.sin(t * 7) * flickGate * 3.2;
        ctx.strokeStyle = rgb48(p.outline);
        ctx.lineWidth = 1.8;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(bx + 13, by - 2);
        ctx.quadraticCurveTo(bx + 17 + swish, by + 3, bx + 15.5 + swish, by + 9);
        ctx.stroke();
        ctx.lineCap = "butt";
        ctx.fillStyle = rgb48(p.patch);
        ctx.beginPath();
        ctx.ellipse(bx + 15.5 + swish, by + 10, 1.6, 2.6, 0.15, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb48([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 10 + s * 20;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb48([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb48([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.35);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb48([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach - 1, hy + 4.4, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 7 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb48([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb48([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 14, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition45(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother48(clamp0149(pp));
      paint48(ctx, lerpP48(SP48[from], SP48[to], k), 0);
    };
  }
  var springToSummer42 = makeTransition45(0);
  var summerToAutumn42 = makeTransition45(1);
  var autumnToWinter42 = makeTransition45(2);
  var VARIANTS50 = {
    Spring: { draw: draw38("Spring"), anim: anim38("Spring") },
    Summer: { draw: draw38("Summer"), anim: anim38("Summer") },
    Autumn: { draw: draw38("Autumn"), anim: anim38("Autumn") },
    Winter: { draw: draw38("Winter"), anim: anim38("Winter") }
  };
  var TRANSITIONS50 = {
    0: springToSummer42,
    1: summerToAutumn42,
    2: autumnToWinter42
  };

  // src/textures/seasonal/cattle/longhorn.ts
  function clamp0150(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp54(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB42(a, b, t) {
    return [lerp54(a[0], b[0], t), lerp54(a[1], b[1], t), lerp54(a[2], b[2], t)];
  }
  function rgb49(c, a = 1) {
    const r = Math.round(clamp0150(c[0] / 255) * 255);
    const g = Math.round(clamp0150(c[1] / 255) * 255);
    const b = Math.round(clamp0150(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0150(a)})`;
  }
  var smoother49 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt49(t, amp = 1, w = 1.35) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP49 = {
    // Spring — sleek tan coat, dewy lime pad + blossom, cool-bright light.
    Spring: {
      hideLight: [214, 162, 100],
      hideMid: [184, 126, 70],
      hideShadow: [142, 92, 50],
      blaze: [248, 244, 236],
      hornLight: [238, 228, 204],
      hornDark: [88, 72, 56],
      muzzle: [70, 58, 52],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [66, 48, 36],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.12,
      // sleek
      frostAmt: 0,
      hornFrostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — normal tan coat (PEAK), saturated mid-green pad, warm light.
    Summer: {
      hideLight: [222, 162, 92],
      hideMid: [192, 124, 60],
      hideShadow: [146, 86, 40],
      blaze: [250, 246, 238],
      hornLight: [242, 232, 208],
      hornDark: [84, 66, 50],
      muzzle: [66, 54, 48],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [62, 44, 32],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.4,
      // normal full coat
      frostAmt: 0,
      hornFrostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — slightly fuller tan coat, olive-tan browning pad, fallen leaves.
    Autumn: {
      hideLight: [206, 144, 82],
      hideMid: [176, 110, 54],
      hideShadow: [134, 78, 38],
      blaze: [244, 238, 226],
      hornLight: [236, 224, 196],
      hornDark: [80, 62, 46],
      muzzle: [64, 50, 44],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [60, 42, 30],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.62,
      // slightly fuller
      frostAmt: 0,
      hornFrostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — thicker tan coat + frosted horns + back-snow + breath fog, snowy
    // pad, cool light. The tan hide stays clearly visible under the frost.
    Winter: {
      hideLight: [202, 154, 104],
      hideMid: [172, 120, 70],
      hideShadow: [134, 90, 56],
      blaze: [250, 250, 252],
      hornLight: [236, 234, 230],
      hornDark: [96, 84, 78],
      muzzle: [74, 64, 62],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [70, 58, 54],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick
      frostAmt: 0.6,
      hornFrostAmt: 0.8,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP49(a, b, t) {
    return {
      hideLight: lerpRGB42(a.hideLight, b.hideLight, t),
      hideMid: lerpRGB42(a.hideMid, b.hideMid, t),
      hideShadow: lerpRGB42(a.hideShadow, b.hideShadow, t),
      blaze: lerpRGB42(a.blaze, b.blaze, t),
      hornLight: lerpRGB42(a.hornLight, b.hornLight, t),
      hornDark: lerpRGB42(a.hornDark, b.hornDark, t),
      muzzle: lerpRGB42(a.muzzle, b.muzzle, t),
      padGrass: lerpRGB42(a.padGrass, b.padGrass, t),
      soil: lerpRGB42(a.soil, b.soil, t),
      outline: lerpRGB42(a.outline, b.outline, t),
      lightWash: lerpRGB42(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp54(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp54(a.coatVolume, b.coatVolume, t),
      frostAmt: lerp54(a.frostAmt, b.frostAmt, t),
      hornFrostAmt: lerp54(a.hornFrostAmt, b.hornFrostAmt, t),
      backSnowAmt: lerp54(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp54(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp54(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp54(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp54(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP38(p) {
    return {
      ...p,
      lightWashAmt: clamp0150(p.lightWashAmt),
      coatVolume: clamp0150(p.coatVolume),
      frostAmt: clamp0150(p.frostAmt),
      hornFrostAmt: clamp0150(p.hornFrostAmt),
      backSnowAmt: clamp0150(p.backSnowAmt),
      padSnowAmt: clamp0150(p.padSnowAmt),
      blossomAmt: clamp0150(p.blossomAmt),
      fallenLeafAmt: clamp0150(p.fallenLeafAmt),
      breathFogAmt: clamp0150(p.breathFogAmt)
    };
  }
  function paintPad17(ctx, p) {
    ctx.fillStyle = rgb49([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb49(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb49([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb49([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb49([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb49([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb49([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb49(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb49([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg15(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb49(p.outline);
    ctx.lineWidth = 4.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb49(p.hideShadow);
    ctx.lineWidth = 3;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1);
    ctx.stroke();
    ctx.fillStyle = rgb49(p.hornDark);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 2, 1.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath6(ctx, cx, cy, vol) {
    const rx = 13.5 * (0.97 + vol * 0.1);
    const ry = 8.6 * (0.97 + vol * 0.12);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, 0, 0, Math.PI * 2);
  }
  function paintHorns(ctx, p, hx, hy) {
    for (const side of [-1, 1]) {
      const x0 = hx + side * 2.2;
      const y0 = hy - 4.4;
      const cx = hx + side * 12.5;
      const cy = hy - 5.6;
      const x1 = hx + side * 17.5;
      const y1 = hy - 8.6;
      ctx.strokeStyle = rgb49(p.outline);
      ctx.lineWidth = 3.4;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(x0, y0);
      ctx.quadraticCurveTo(cx, cy, x1, y1);
      ctx.stroke();
      ctx.strokeStyle = rgb49(p.hornDark);
      ctx.lineWidth = 2.4;
      ctx.beginPath();
      ctx.moveTo(x0, y0);
      ctx.quadraticCurveTo(cx, cy, x1, y1);
      ctx.stroke();
      ctx.strokeStyle = rgb49(p.hornLight);
      ctx.lineWidth = 1.2;
      ctx.beginPath();
      ctx.moveTo(x0, y0 - 0.7);
      ctx.quadraticCurveTo(cx, cy - 0.9, x1, y1 - 0.4);
      ctx.stroke();
      if (p.hornFrostAmt > 1e-3) {
        ctx.fillStyle = rgb49([236, 246, 255], 0.85 * p.hornFrostAmt);
        for (let k = 0; k <= 5; k++) {
          const tt = k / 5;
          const bxp = (1 - tt) * (1 - tt) * x0 + 2 * (1 - tt) * tt * cx + tt * tt * x1;
          const byp = (1 - tt) * (1 - tt) * y0 + 2 * (1 - tt) * tt * cy + tt * tt * y1;
          ctx.beginPath();
          ctx.arc(bxp, byp - 0.4, 0.7, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.lineCap = "butt";
    }
  }
  function paintLonghorn(ctx, p, bob, headDip = 0) {
    const bx = 1;
    const by = 2 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg15(ctx, p, bx + 8, by + 6, 18.6);
    paintLeg15(ctx, p, bx - 6.5, by + 6, 18.9);
    ctx.restore();
    paintLeg15(ctx, p, bx + 4, by + 6.5, 19.4);
    paintLeg15(ctx, p, bx - 9.5, by + 6.5, 19.6);
    ctx.fillStyle = rgb49(p.outline);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.1, 1.14);
    ctx.translate(-bx, -by);
    bodyPath6(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb49(p.hideShadow);
    bodyPath6(ctx, bx, by, vol);
    ctx.fill();
    ctx.fillStyle = rgb49(p.hideMid);
    ctx.save();
    ctx.translate(-0.6, -0.8);
    bodyPath6(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.fillStyle = rgb49(p.hideLight);
    ctx.translate(-1.6, -1.8);
    ctx.scale(0.84, 0.78);
    ctx.translate(bx * 0.16, by * 0.22);
    bodyPath6(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    if (vol > 0.55) {
      const fr = (vol - 0.55) / 0.45;
      ctx.fillStyle = rgb49(p.hideShadow, 0.9);
      const rx = 13.5 * (0.97 + vol * 0.1);
      const ry = 8.6 * (0.97 + vol * 0.12);
      for (let i = 0; i < 9; i++) {
        const a = Math.PI * 0.15 + i / 8 * Math.PI * 0.9;
        const lx = bx + Math.cos(a) * rx * 1.02;
        const ly = by + Math.sin(a) * ry * 1.02;
        ctx.beginPath();
        ctx.arc(lx, ly, (1.2 + 0.9 * fr) * (i % 2 ? 1 : 0.8), 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.save();
    bodyPath6(ctx, bx, by, vol);
    ctx.clip();
    ctx.fillStyle = rgb49(p.hideShadow, 0.5);
    ctx.beginPath();
    ctx.ellipse(bx, by + 5.5, 12, 4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49([255, 255, 255], 0.08);
    ctx.beginPath();
    ctx.ellipse(bx - 3, by - 4, 9, 3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb49([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx, by - 7.2, 10 * (0.9 + vol * 0.2), 3.2, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb49([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-6, -8], [0, -8.6], [6, -7.8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb49([255, 255, 255], 0.8 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-3, 4],
        [4, -2],
        [9, 2],
        [0, -3],
        [-5, -5],
        [7, 5]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.strokeStyle = rgb49(p.outline);
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx + 13, by - 2);
    ctx.quadraticCurveTo(bx + 17, by + 3, bx + 15.5, by + 9);
    ctx.stroke();
    ctx.strokeStyle = rgb49(p.hideMid);
    ctx.lineWidth = 1;
    ctx.beginPath();
    ctx.moveTo(bx + 13, by - 2);
    ctx.quadraticCurveTo(bx + 17, by + 3, bx + 15.5, by + 9);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb49(p.hornDark);
    ctx.beginPath();
    ctx.ellipse(bx + 15.5, by + 10, 1.6, 2.6, 0.15, 0, Math.PI * 2);
    ctx.fill();
    const hx = bx - 14;
    const hy = by + 3 + headDip;
    paintHorns(ctx, p, hx, hy);
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + side * 5.4, hy - 1.6);
      ctx.rotate(side * 0.85);
      ctx.fillStyle = rgb49(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb49(p.hideMid);
      ctx.beginPath();
      ctx.ellipse(0, 0, 2.6, 1.3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb49(p.hideShadow);
      ctx.beginPath();
      ctx.ellipse(side * -0.3, 0.1, 1.4, 0.7, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.22);
    ctx.fillStyle = rgb49(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 5.2, 6.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49(p.hideShadow);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.6, 5.7, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49(p.hideMid);
    ctx.beginPath();
    ctx.ellipse(-0.5, -0.6, 4.2, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.save();
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.6, 5.7, 0, 0, Math.PI * 2);
    ctx.clip();
    ctx.fillStyle = rgb49(p.blaze);
    ctx.beginPath();
    ctx.moveTo(-1.3, -5.4);
    ctx.quadraticCurveTo(-2, 0, -1.4, 4.4);
    ctx.quadraticCurveTo(0, 5.4, 1.4, 4.4);
    ctx.quadraticCurveTo(2, 0, 1.3, -5.4);
    ctx.quadraticCurveTo(0, -6.2, -1.3, -5.4);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb49(p.hideLight, 0.55);
    ctx.beginPath();
    ctx.ellipse(-2.6, -1.6, 1.8, 2.6, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb49([245, 243, 238]);
    for (const ex of [-2.4, 2.4]) {
      ctx.beginPath();
      ctx.arc(ex, -1.2, 1, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb49([22, 18, 18]);
    for (const ex of [-2.2, 2.6]) {
      ctx.beginPath();
      ctx.arc(ex, -1, 0.58, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.fillStyle = rgb49(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 3.8, 3.8, 2.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49(p.muzzle);
    ctx.beginPath();
    ctx.ellipse(0, 3.6, 3.2, 2.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49([255, 255, 255], 0.12);
    ctx.beginPath();
    ctx.ellipse(-1.1, 2.6, 1.3, 0.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb49([18, 14, 14]);
    for (const ex of [-1.1, 1.1]) {
      ctx.beginPath();
      ctx.ellipse(ex, 4, 0.55, 0.8, 0, 0, Math.PI * 2);
      ctx.fill();
    }
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb49([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 5.5, hy + 4.6, 3.2, 2.2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash16(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb49(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint49(ctx, pIn, bob, headDip = 0) {
    const p = clampP38(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad17(ctx, p);
      paintLonghorn(ctx, p, bob, headDip);
      paintLightWash16(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw39(season) {
    return (ctx) => paint49(ctx, SP49[season], 0);
  }
  function anim39(season) {
    return (ctx, t) => {
      const p = clampP38(SP49[season]);
      const bob = bobAt49(t);
      const dipLoop = t % 6 / 6;
      const dip = 0.9 * (1 - Math.cos(dipLoop * Math.PI * 2)) * 0.5;
      paint49(ctx, SP49[season], bob, dip);
      ctx.save();
      try {
        const bx = 1;
        const by = 2 - bob;
        const hx = bx - 14;
        const hy = by + 3 + dip;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.save();
        ctx.translate(hx - 5.4, hy - 1.6);
        ctx.rotate(-0.85 + flick * 0.6);
        ctx.fillStyle = rgb49(p.outline);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb49(p.hideMid);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb49(p.hideShadow);
        ctx.beginPath();
        ctx.ellipse(0.3, 0.1, 1.4, 0.7, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        const swish = Math.sin(t * 7) * flickGate * 3.2;
        ctx.strokeStyle = rgb49(p.outline);
        ctx.lineWidth = 1.8;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(bx + 13, by - 2);
        ctx.quadraticCurveTo(bx + 17 + swish, by + 3, bx + 15.5 + swish, by + 9);
        ctx.stroke();
        ctx.lineCap = "butt";
        ctx.fillStyle = rgb49(p.hornDark);
        ctx.beginPath();
        ctx.ellipse(bx + 15.5 + swish, by + 10, 1.6, 2.6, 0.15, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb49([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 10 + s * 20;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb49([255, 248, 224], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb49([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.35);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb49([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach - 1, hy + 4.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 7 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb49([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb49([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 14, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition46(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother49(clamp0150(pp));
      paint49(ctx, lerpP49(SP49[from], SP49[to], k), 0);
    };
  }
  var springToSummer43 = makeTransition46(0);
  var summerToAutumn43 = makeTransition46(1);
  var autumnToWinter43 = makeTransition46(2);
  var VARIANTS51 = {
    Spring: { draw: draw39("Spring"), anim: anim39("Spring") },
    Summer: { draw: draw39("Summer"), anim: anim39("Summer") },
    Autumn: { draw: draw39("Autumn"), anim: anim39("Autumn") },
    Winter: { draw: draw39("Winter"), anim: anim39("Winter") }
  };
  var TRANSITIONS51 = {
    0: springToSummer43,
    1: summerToAutumn43,
    2: autumnToWinter43
  };

  // src/textures/seasonal/cattle/triceratops.ts
  function clamp0151(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp55(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB43(a, b, t) {
    return [lerp55(a[0], b[0], t), lerp55(a[1], b[1], t), lerp55(a[2], b[2], t)];
  }
  function rgb50(c, a = 1) {
    const r = Math.round(clamp0151(c[0] / 255) * 255);
    const g = Math.round(clamp0151(c[1] / 255) * 255);
    const b = Math.round(clamp0151(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0151(a)})`;
  }
  var smoother50 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt50(t, amp = 1, w = 1.15) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP50 = {
    // Spring — fresh green hide; dewy lime pad + blossom; cool-bright light.
    Spring: {
      hideLight: [142, 200, 96],
      hideMid: [96, 162, 66],
      hideShadow: [58, 110, 48],
      frill: [120, 168, 84],
      frillRim: [176, 214, 124],
      beak: [226, 220, 188],
      hornDark: [70, 70, 56],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [40, 60, 34],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      hideSheen: 0.22,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.7,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — rich green hide (PEAK), saturated mid-green pad, warm light.
    Summer: {
      hideLight: [128, 196, 78],
      hideMid: [78, 156, 56],
      hideShadow: [44, 102, 42],
      frill: [104, 158, 70],
      frillRim: [162, 206, 110],
      beak: [232, 224, 188],
      hornDark: [64, 64, 50],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [64, 116, 44],
      outline: [34, 58, 30],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.2,
      hideSheen: 0.55,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — duller olive-green hide; olive-tan pad; a couple of fallen leaves.
    Autumn: {
      hideLight: [148, 168, 86],
      hideMid: [110, 130, 60],
      hideShadow: [72, 88, 42],
      frill: [126, 138, 74],
      frillRim: [170, 178, 104],
      beak: [220, 206, 162],
      hornDark: [70, 62, 44],
      padGrass: [156, 150, 90],
      // olive-tan browning grass
      soil: [112, 96, 52],
      outline: [54, 50, 28],
      lightWash: [248, 210, 150],
      // low amber
      lightWashAmt: 0.2,
      hideSheen: 0.28,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.8,
      breathFogAmt: 0
    },
    // Winter — green hide still clearly visible, snow on frill + back, frost
    // dusting, breath-fog at the beak, snowy pad, cool light.
    Winter: {
      hideLight: [118, 168, 92],
      hideMid: [80, 134, 64],
      hideShadow: [50, 96, 50],
      frill: [104, 142, 84],
      frillRim: [156, 188, 122],
      beak: [216, 216, 196],
      hornDark: [62, 64, 58],
      padGrass: [182, 200, 216],
      // muted grey-green under snow
      soil: [120, 142, 164],
      outline: [44, 56, 50],
      lightWash: [206, 226, 252],
      // cool blue-grey
      lightWashAmt: 0.3,
      hideSheen: 0.18,
      frostAmt: 0.62,
      backSnowAmt: 0.85,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.8
    }
  };
  function lerpP50(a, b, t) {
    return {
      hideLight: lerpRGB43(a.hideLight, b.hideLight, t),
      hideMid: lerpRGB43(a.hideMid, b.hideMid, t),
      hideShadow: lerpRGB43(a.hideShadow, b.hideShadow, t),
      frill: lerpRGB43(a.frill, b.frill, t),
      frillRim: lerpRGB43(a.frillRim, b.frillRim, t),
      beak: lerpRGB43(a.beak, b.beak, t),
      hornDark: lerpRGB43(a.hornDark, b.hornDark, t),
      padGrass: lerpRGB43(a.padGrass, b.padGrass, t),
      soil: lerpRGB43(a.soil, b.soil, t),
      outline: lerpRGB43(a.outline, b.outline, t),
      lightWash: lerpRGB43(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp55(a.lightWashAmt, b.lightWashAmt, t),
      hideSheen: lerp55(a.hideSheen, b.hideSheen, t),
      frostAmt: lerp55(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp55(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp55(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp55(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp55(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp55(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP39(p) {
    return {
      ...p,
      lightWashAmt: clamp0151(p.lightWashAmt),
      hideSheen: clamp0151(p.hideSheen),
      frostAmt: clamp0151(p.frostAmt),
      backSnowAmt: clamp0151(p.backSnowAmt),
      padSnowAmt: clamp0151(p.padSnowAmt),
      blossomAmt: clamp0151(p.blossomAmt),
      fallenLeafAmt: clamp0151(p.fallenLeafAmt),
      breathFogAmt: clamp0151(p.breathFogAmt)
    };
  }
  function drawPad4(ctx, p) {
    ctx.fillStyle = rgb50(p.soil, 0.4);
    ctx.beginPath();
    ctx.ellipse(3, 21.6, 16, 4.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb50(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb50(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb50(p.soil);
    ctx.lineWidth = 1.1;
    for (let i = -7; i <= 7; i++) {
      const tx = i * 2.4;
      const ty = 19 - Math.sqrt(Math.max(0, 1 - (tx / 18) ** 2)) * 5.2;
      ctx.beginPath();
      ctx.moveTo(tx, ty + 0.4);
      ctx.lineTo(tx - 0.8, ty - 2.4);
      ctx.stroke();
    }
    if (p.padSnowAmt > 0.01) {
      ctx.fillStyle = rgb50([244, 250, 255], 0.92 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb50([210, 226, 244], 0.5 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb50([255, 255, 255], 0.8 * p.padSnowAmt);
      [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.blossomAmt > 0.01) {
      const a = p.blossomAmt;
      const spots = [[-13, 18.5], [12, 17.8], [-4, 21]];
      spots.forEach(([bx, by], idx) => {
        ctx.fillStyle = rgb50(idx === 1 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
        for (let k = 0; k < 5; k++) {
          const ang = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.ellipse(bx + Math.cos(ang) * 1.5, by + Math.sin(ang) * 1, 1.1, 0.8, ang, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb50([255, 214, 90], a);
        ctx.beginPath();
        ctx.arc(bx, by, 0.9, 0, Math.PI * 2);
        ctx.fill();
      });
    }
    if (p.fallenLeafAmt > 0.01) {
      const a = p.fallenLeafAmt;
      const leaves = [
        [-12, 19.6, -0.5, [196, 120, 40]],
        [12, 18.6, 0.7, [176, 72, 32]]
      ];
      leaves.forEach(([lx, ly, rot, col]) => {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb50(col, a);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb50([90, 44, 16], a);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-3, 0);
        ctx.lineTo(3, 0);
        ctx.stroke();
        ctx.restore();
      });
    }
  }
  function bodyPath7(ctx, bob) {
    ctx.beginPath();
    ctx.moveTo(-9, 2 + bob);
    ctx.quadraticCurveTo(-4, -6 + bob, 5, -5 + bob);
    ctx.quadraticCurveTo(14, -4 + bob, 16, 4 + bob);
    ctx.quadraticCurveTo(17, 11 + bob, 11, 13 + bob);
    ctx.quadraticCurveTo(2, 16 + bob, -7, 13 + bob);
    ctx.quadraticCurveTo(-12, 11 + bob, -10, 5 + bob);
    ctx.closePath();
  }
  function paint50(ctx, raw, bob, headSway = 0, tailSwish = 0) {
    const p = clampP39(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      drawPad4(ctx, p);
      ctx.fillStyle = rgb50(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(2.5, 16.5, 15, 3.2, 0, 0, Math.PI * 2);
      ctx.fill();
      {
        const tx = tailSwish;
        ctx.strokeStyle = rgb50(p.outline);
        ctx.lineWidth = 8.5;
        ctx.beginPath();
        ctx.moveTo(13, 6 + bob);
        ctx.quadraticCurveTo(20, 7 + bob, 21 + tx, 11 + bob);
        ctx.stroke();
        ctx.strokeStyle = rgb50(p.hideMid);
        ctx.lineWidth = 6;
        ctx.beginPath();
        ctx.moveTo(13, 6 + bob);
        ctx.quadraticCurveTo(20, 7 + bob, 21 + tx, 11 + bob);
        ctx.stroke();
        ctx.strokeStyle = rgb50(p.hideLight, 0.6);
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.moveTo(13, 4.5 + bob);
        ctx.quadraticCurveTo(19, 5.5 + bob, 20.4 + tx, 9.5 + bob);
        ctx.stroke();
      }
      const drawLeg = (x, top, len, w, far) => {
        ctx.strokeStyle = rgb50(p.outline);
        ctx.lineWidth = w + 2;
        ctx.beginPath();
        ctx.moveTo(x, top + bob);
        ctx.lineTo(x, top + len);
        ctx.stroke();
        ctx.strokeStyle = far ? rgb50(p.hideShadow) : rgb50(p.hideMid);
        ctx.lineWidth = w;
        ctx.beginPath();
        ctx.moveTo(x, top + bob);
        ctx.lineTo(x, top + len);
        ctx.stroke();
        ctx.fillStyle = rgb50(p.hornDark, far ? 0.7 : 1);
        ctx.beginPath();
        ctx.ellipse(x, top + len, w * 0.62, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
      };
      drawLeg(-3, 11, 5, 4.6, true);
      drawLeg(9, 11.5, 4.6, 5.4, true);
      {
        ctx.save();
        ctx.translate(-4 + headSway * 0.5, bob);
        ctx.fillStyle = rgb50(p.outline);
        ctx.beginPath();
        ctx.moveTo(-1, 7);
        ctx.quadraticCurveTo(-13, 4, -11, -9);
        ctx.quadraticCurveTo(-7, -15, 1, -13);
        ctx.quadraticCurveTo(9, -12, 9, -3);
        ctx.quadraticCurveTo(8, 4, 2, 7);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb50(p.frill);
        ctx.beginPath();
        ctx.moveTo(-0.5, 5.6);
        ctx.quadraticCurveTo(-11, 3, -9.2, -8);
        ctx.quadraticCurveTo(-5.6, -13, 1, -11.4);
        ctx.quadraticCurveTo(7.4, -10.4, 7.4, -2.6);
        ctx.quadraticCurveTo(6.6, 3, 1.6, 5.6);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb50(p.frillRim);
        const bumps = [
          [-8.6, -7.4],
          [-5.4, -11],
          [-1, -12.4],
          [3.2, -11.4],
          [6.6, -7.8]
        ];
        bumps.forEach(([bx, by]) => {
          ctx.beginPath();
          ctx.arc(bx, by, 1.6, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.strokeStyle = rgb50(p.hideShadow, 0.55);
        ctx.lineWidth = 1;
        [-7.5, -3.5, 0.5, 4.5].forEach((ex, i) => {
          ctx.beginPath();
          ctx.moveTo(0.5, 4);
          ctx.lineTo(ex, -9 + i * 0.4);
          ctx.stroke();
        });
        ctx.restore();
      }
      bodyPath7(ctx, bob);
      ctx.fillStyle = rgb50(p.outline);
      ctx.fill();
      ctx.save();
      bodyPath7(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb50(p.hideMid);
      ctx.fillRect(-14, -8 + bob, 34, 28);
      const litGrad = ctx.createLinearGradient(-8, -6 + bob, 10, 14 + bob);
      litGrad.addColorStop(0, rgb50(p.hideLight));
      litGrad.addColorStop(0.5, rgb50(p.hideMid));
      litGrad.addColorStop(1, rgb50(p.hideShadow));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.9;
      ctx.beginPath();
      ctx.ellipse(1, 2 + bob, 16, 11, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.fillStyle = rgb50(p.hideShadow, 0.7);
      ctx.beginPath();
      ctx.ellipse(0, 12 + bob, 13, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb50(p.hideShadow, 0.5);
      [-6, -1, 4, 9].forEach((rx, i) => {
        ctx.beginPath();
        ctx.arc(rx, -4 + i * 0.5 + bob, 1.4, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.hideSheen > 0.02) {
        ctx.fillStyle = rgb50([255, 255, 255], 0.12 + 0.32 * p.hideSheen);
        ctx.beginPath();
        ctx.ellipse(-3, -1 + bob, 5.5, 3.4, -0.3, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgb50([222, 238, 252], 0.24 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(1, -3 + bob, 15, 6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb50([240, 248, 255], 0.7 * p.frostAmt);
        [[-7, -4], [-1, -5], [5, -4], [10, -2], [2, -1]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy + bob, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      drawLeg(-6, 10.5, 5.4, 5, false);
      drawLeg(6, 11.5, 4.8, 6, false);
      if (p.backSnowAmt > 0.02) {
        const a = p.backSnowAmt;
        ctx.fillStyle = rgb50([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-8, 1 + bob);
        ctx.quadraticCurveTo(-3, -7 + bob, 5, -6 + bob);
        ctx.quadraticCurveTo(13, -5 + bob, 15, 2 + bob);
        ctx.quadraticCurveTo(10, -2 + bob, 4, -3 + bob);
        ctx.quadraticCurveTo(-3, -3.5 + bob, -8, 1 + bob);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb50([214, 230, 246], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(2, -4 + bob, 9, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.save();
        ctx.translate(-4 + headSway * 0.5, bob);
        ctx.fillStyle = rgb50([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-9.2, -8);
        ctx.quadraticCurveTo(-5.6, -14, 1, -12.6);
        ctx.quadraticCurveTo(7, -11.6, 7.4, -5);
        ctx.quadraticCurveTo(4, -9.4, -1, -10);
        ctx.quadraticCurveTo(-6, -10.2, -9.2, -8);
        ctx.closePath();
        ctx.fill();
        ctx.restore();
      }
      {
        ctx.save();
        ctx.translate(-12 + headSway, 5 + bob);
        ctx.rotate(0.12);
        ctx.fillStyle = rgb50(p.outline);
        ctx.beginPath();
        ctx.moveTo(8, -5);
        ctx.quadraticCurveTo(2, -7, -4, -3);
        ctx.quadraticCurveTo(-9, -1, -8.5, 2.5);
        ctx.quadraticCurveTo(-6, 5.5, 0, 5);
        ctx.quadraticCurveTo(6, 5, 8, 1);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb50(p.hideMid);
        ctx.beginPath();
        ctx.moveTo(7, -3.6);
        ctx.quadraticCurveTo(2, -5.4, -3.4, -1.8);
        ctx.quadraticCurveTo(-7.4, -0.2, -7, 2.2);
        ctx.quadraticCurveTo(-5, 4.4, 0, 3.8);
        ctx.quadraticCurveTo(5, 3.8, 6.6, 0.2);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb50(p.hideLight, 0.8);
        ctx.beginPath();
        ctx.ellipse(0, -1.4, 4.2, 2.2, -0.2, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb50(p.beak);
        ctx.beginPath();
        ctx.moveTo(-3.4, -1.6);
        ctx.quadraticCurveTo(-9.4, -0.4, -8.6, 2.6);
        ctx.quadraticCurveTo(-6.4, 4.2, -3, 3.2);
        ctx.quadraticCurveTo(-4.4, 0.8, -3.4, -1.6);
        ctx.closePath();
        ctx.fill();
        ctx.strokeStyle = rgb50(p.hornDark, 0.7);
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(-7.6, 1);
        ctx.lineTo(-3.6, 0.8);
        ctx.stroke();
        ctx.fillStyle = rgb50(p.beak);
        ctx.strokeStyle = rgb50(p.hornDark, 0.6);
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(-3.6, -2.2);
        ctx.quadraticCurveTo(-5.2, -6.2, -2.6, -7.2);
        ctx.quadraticCurveTo(-1.6, -4.4, -1.6, -2);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
        const brow = (sx, dx, dy, _len) => {
          ctx.fillStyle = rgb50(p.beak);
          ctx.beginPath();
          ctx.moveTo(sx, -3.4);
          ctx.quadraticCurveTo(sx + dx * 0.5, -3.4 + dy * 0.55, sx + dx, -3.4 + dy);
          ctx.quadraticCurveTo(sx + dx + 1.1, -3.4 + dy + 1.2, sx + 1.8, -2.6);
          ctx.closePath();
          ctx.fill();
          ctx.strokeStyle = rgb50(p.hornDark, 0.55);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(sx + 0.6, -3.2);
          ctx.quadraticCurveTo(sx + dx * 0.55, -3.4 + dy * 0.55, sx + dx, -3.4 + dy);
          ctx.stroke();
        };
        brow(1.8, -3.4, -9.6, 0);
        brow(5.4, -2.4, -10.6, 0);
        ctx.fillStyle = rgb50(p.hornDark);
        ctx.beginPath();
        ctx.arc(1.6, -0.6, 1.15, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb50([255, 255, 255], 0.85);
        ctx.beginPath();
        ctx.arc(1.2, -1, 0.42, 0, Math.PI * 2);
        ctx.fill();
        if (p.breathFogAmt > 0.02) {
          const a = p.breathFogAmt;
          ctx.fillStyle = rgb50([255, 255, 255], 0.34 * a);
          [[-10.5, 2.4, 2], [-12.6, 1.2, 1.5], [-12.4, 3.4, 1.3]].forEach(([fx, fy, fr]) => {
            ctx.beginPath();
            ctx.arc(fx, fy, fr, 0, Math.PI * 2);
            ctx.fill();
          });
        }
        ctx.restore();
      }
      if (p.lightWashAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 48);
        lg.addColorStop(0, rgb50(p.lightWash, p.lightWashAmt));
        lg.addColorStop(1, rgb50(p.lightWash, p.lightWashAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function draw40(season) {
    return (ctx) => paint50(ctx, SP50[season], 0, 0, 0);
  }
  function anim40(season) {
    return (ctx, t) => {
      const bob = bobAt50(t, 1, 1.15);
      const headSway = (1 - Math.cos(t * 0.85)) * 0.5 * 1.4;
      const tailSwish = (1 - Math.cos(t * 0.7)) * 0.5 * 3;
      paint50(ctx, SP50[season], bob, headSway, tailSwish);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.22 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          const gy = -3 + bob + Math.sin(t * 1) * 1.2;
          ctx.beginPath();
          ctx.arc(-2, gy, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.45 % 1;
          const gx = lerp55(-6, 8, prog);
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(gx, -2 + bob, 2, 3.4, -0.4, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const prog = t * 0.3 % 1;
          const lx = -13 + prog * 8;
          const ly = 19.6 - Math.sin(prog * Math.PI) * 2.2;
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(prog * Math.PI * 2);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.8, 1.5, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const fogPulse = 0.3 + 0.5 * (0.5 + 0.5 * Math.sin(t * 1.6));
          ctx.fillStyle = `rgba(255,255,255,${0.3 * fogPulse})`;
          const fx0 = -22 - 1.5 * fogPulse + headSway;
          [[fx0, 10, 2.4 * fogPulse], [fx0 - 2.4, 8.6, 1.8 * fogPulse]].forEach(
            ([fx, fy, fr]) => {
              ctx.beginPath();
              ctx.arc(fx, fy, Math.max(0.1, fr), 0, Math.PI * 2);
              ctx.fill();
            }
          );
          const prog = t / 3.4 % 1;
          const sy = -22 + prog * 40;
          const sx = -4 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(sx, sy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition47(fromIdx) {
    const from = SP50[SEASON_NAMES[fromIdx]];
    const to = SP50[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother50(clamp0151(pp));
      paint50(ctx, lerpP50(from, to, k), 0, 0, 0);
    };
  }
  var springToSummer44 = makeTransition47(0);
  var summerToAutumn44 = makeTransition47(1);
  var autumnToWinter44 = makeTransition47(2);
  var VARIANTS52 = {
    Spring: { draw: draw40("Spring"), anim: anim40("Spring") },
    Summer: { draw: draw40("Summer"), anim: anim40("Summer") },
    Autumn: { draw: draw40("Autumn"), anim: anim40("Autumn") },
    Winter: { draw: draw40("Winter"), anim: anim40("Winter") }
  };
  var TRANSITIONS52 = {
    0: springToSummer44,
    1: summerToAutumn44,
    2: autumnToWinter44
  };

  // src/textures/seasonal/mount/horse.ts
  function clamp0152(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp56(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB44(a, b, t) {
    return [lerp56(a[0], b[0], t), lerp56(a[1], b[1], t), lerp56(a[2], b[2], t)];
  }
  function rgb51(c, a = 1) {
    const r = Math.round(clamp0152(c[0] / 255) * 255);
    const g = Math.round(clamp0152(c[1] / 255) * 255);
    const b = Math.round(clamp0152(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0152(a)})`;
  }
  var smoother51 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt51(t, amp = 1, w = 1.35) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP51 = {
    // Spring — sleek brown coat, dewy lime pad + blossom, cool-bright light.
    Spring: {
      coatLight: [168, 116, 70],
      coatMid: [134, 86, 48],
      coatShadow: [92, 56, 30],
      mane: [54, 34, 22],
      maneShade: [34, 20, 12],
      muzzle: [128, 84, 70],
      hoofDark: [44, 34, 28],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [50, 30, 18],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.12,
      // sleek
      gloss: 0.3,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — glossy brown coat (PEAK), saturated mid-green pad, warm light.
    Summer: {
      coatLight: [184, 124, 72],
      coatMid: [146, 90, 46],
      coatShadow: [96, 56, 26],
      mane: [50, 30, 18],
      maneShade: [30, 17, 10],
      muzzle: [132, 86, 70],
      hoofDark: [40, 30, 24],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [46, 28, 16],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.42,
      // normal full coat
      gloss: 0.95,
      // glossiest peak
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — slightly fuller coat, olive-tan browning pad, fallen leaves.
    Autumn: {
      coatLight: [172, 112, 64],
      coatMid: [138, 84, 44],
      coatShadow: [92, 54, 28],
      mane: [52, 32, 20],
      maneShade: [32, 19, 11],
      muzzle: [126, 82, 68],
      hoofDark: [44, 33, 26],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [50, 30, 18],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.62,
      // slightly fuller
      gloss: 0.4,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — thick shaggy coat + back-snow + breath fog, snowy pad, cool light.
    Winter: {
      coatLight: [150, 108, 74],
      coatMid: [118, 80, 50],
      coatShadow: [84, 54, 34],
      mane: [50, 34, 26],
      maneShade: [30, 20, 14],
      muzzle: [124, 86, 76],
      hoofDark: [48, 40, 38],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [52, 38, 30],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick & shaggy
      gloss: 0.2,
      frostAmt: 0.65,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP51(a, b, t) {
    return {
      coatLight: lerpRGB44(a.coatLight, b.coatLight, t),
      coatMid: lerpRGB44(a.coatMid, b.coatMid, t),
      coatShadow: lerpRGB44(a.coatShadow, b.coatShadow, t),
      mane: lerpRGB44(a.mane, b.mane, t),
      maneShade: lerpRGB44(a.maneShade, b.maneShade, t),
      muzzle: lerpRGB44(a.muzzle, b.muzzle, t),
      hoofDark: lerpRGB44(a.hoofDark, b.hoofDark, t),
      padGrass: lerpRGB44(a.padGrass, b.padGrass, t),
      soil: lerpRGB44(a.soil, b.soil, t),
      outline: lerpRGB44(a.outline, b.outline, t),
      lightWash: lerpRGB44(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp56(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp56(a.coatVolume, b.coatVolume, t),
      gloss: lerp56(a.gloss, b.gloss, t),
      frostAmt: lerp56(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp56(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp56(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp56(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp56(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp56(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP40(p) {
    return {
      ...p,
      lightWashAmt: clamp0152(p.lightWashAmt),
      coatVolume: clamp0152(p.coatVolume),
      gloss: clamp0152(p.gloss),
      frostAmt: clamp0152(p.frostAmt),
      backSnowAmt: clamp0152(p.backSnowAmt),
      padSnowAmt: clamp0152(p.padSnowAmt),
      blossomAmt: clamp0152(p.blossomAmt),
      fallenLeafAmt: clamp0152(p.fallenLeafAmt),
      breathFogAmt: clamp0152(p.breathFogAmt)
    };
  }
  function paintPad18(ctx, p) {
    ctx.fillStyle = rgb51([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb51(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb51([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb51([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb51([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb51([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb51([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb51(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb51([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg16(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb51(p.outline);
    ctx.lineWidth = 3.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb51(p.coatShadow);
    ctx.lineWidth = 2.2;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1.4);
    ctx.stroke();
    ctx.fillStyle = rgb51(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.8, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath8(ctx, cx, cy, vol) {
    const rx = 12.5 * (0.97 + vol * 0.12);
    const ry = 7.8 * (0.97 + vol * 0.14);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.06, 0, Math.PI * 2);
  }
  function paintHorse(ctx, p, bob, maneSway = 0, tailSway = 0, headToss = 0) {
    const bx = 2;
    const by = 2 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg16(ctx, p, bx + 8.5, by + 5.5, 18.8);
    paintLeg16(ctx, p, bx - 4, by + 5.5, 19);
    ctx.restore();
    paintLeg16(ctx, p, bx + 5.5, by + 6, 19.6);
    paintLeg16(ctx, p, bx - 7, by + 6, 19.8);
    {
      const tx0 = bx + 11;
      const ty0 = by - 4;
      ctx.strokeStyle = rgb51(p.maneShade);
      ctx.lineWidth = 4.4 * (0.9 + vol * 0.25);
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(tx0, ty0);
      ctx.quadraticCurveTo(tx0 + 5 + tailSway, by + 4, tx0 + 3.5 + tailSway * 1.6, by + 13);
      ctx.stroke();
      ctx.strokeStyle = rgb51(p.mane);
      ctx.lineWidth = 2.6 * (0.9 + vol * 0.25);
      ctx.beginPath();
      ctx.moveTo(tx0, ty0);
      ctx.quadraticCurveTo(tx0 + 5 + tailSway, by + 4, tx0 + 3.5 + tailSway * 1.6, by + 13);
      ctx.stroke();
      ctx.strokeStyle = rgb51(p.maneShade, 0.8);
      ctx.lineWidth = 1.1;
      for (const off of [-1.6, 0, 1.8]) {
        ctx.beginPath();
        ctx.moveTo(tx0 + off * 0.4, ty0 + 2);
        ctx.quadraticCurveTo(tx0 + 4 + tailSway + off, by + 5, tx0 + 2.5 + tailSway * 1.6 + off, by + 13.5);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    ctx.fillStyle = rgb51(p.outline);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.1, 1.14);
    ctx.translate(-bx, -by);
    bodyPath8(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb51(p.coatShadow);
    bodyPath8(ctx, bx, by, vol);
    ctx.fill();
    ctx.fillStyle = rgb51(p.coatMid);
    ctx.save();
    ctx.translate(-0.4, -0.4);
    bodyPath8(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.fillStyle = rgb51(p.coatLight);
    ctx.translate(-1.6, -1.8);
    bodyPath8(ctx, bx, by, vol * 0.7);
    ctx.fill();
    ctx.restore();
    if (p.gloss > 0.02) {
      ctx.save();
      bodyPath8(ctx, bx, by, vol);
      ctx.clip();
      ctx.fillStyle = rgb51([255, 255, 255], 0.12 + 0.18 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 4.4, 9, 2.4, -0.12, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (vol > 0.55) {
      const fr = (vol - 0.55) / 0.45;
      ctx.fillStyle = rgb51(p.coatShadow, 0.9);
      const rx = 12.5 * (0.97 + vol * 0.12);
      const ry = 7.8 * (0.97 + vol * 0.14);
      for (let i = 0; i < 9; i++) {
        const a = Math.PI * 0.15 + i / 8 * Math.PI * 0.9;
        const lx = bx + Math.cos(a) * rx * 1.02;
        const ly = by + Math.sin(a) * ry * 1.02;
        ctx.beginPath();
        ctx.arc(lx, ly, (1.2 + 0.9 * fr) * (i % 2 ? 1 : 0.8), 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb51([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx, by - 6.6, 9 * (0.9 + vol * 0.2), 3, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb51([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -7.4], [1, -8], [6, -7]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.4 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb51([255, 255, 255], 0.75 * p.frostAmt);
      const pts = [
        [-7, 0],
        [-2, 4],
        [4, -2],
        [8, 2],
        [0, -3],
        [-4, -5],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const neckTopX = bx - 8;
    const neckTopY = by - 6;
    const hx = bx - 16;
    const hy = by - 1 - headToss;
    ctx.fillStyle = rgb51(p.outline);
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 6);
    ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 1, hx + 2.5, hy - 3.5);
    ctx.lineTo(hx + 4.5, hy + 3.5);
    ctx.quadraticCurveTo(neckTopX + 3, by + 1, bx - 1, by + 3);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb51(p.coatMid);
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 5.6);
    ctx.quadraticCurveTo(neckTopX - 0.6, neckTopY - 0.4, hx + 2.6, hy - 3);
    ctx.lineTo(hx + 4.2, hy + 3);
    ctx.quadraticCurveTo(neckTopX + 3, by + 0.6, bx - 1, by + 2.4);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb51(p.coatLight, 0.8);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(hx + 4, hy + 2.6);
    ctx.quadraticCurveTo(neckTopX + 3, by + 0.4, bx - 1, by + 2);
    ctx.stroke();
    ctx.lineCap = "butt";
    {
      ctx.strokeStyle = rgb51(p.maneShade);
      ctx.lineWidth = 3 * (0.9 + vol * 0.2);
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(bx - 3, by - 6.2);
      ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 1.8, hx + 2.2, hy - 4.6);
      ctx.stroke();
      ctx.lineWidth = 1.3;
      const strands2 = [
        [bx - 4, by - 6, 0.2],
        [bx - 7.5, by - 6.8, 0.5],
        [neckTopX - 1, neckTopY - 1.2, 0.8],
        [hx + 1, hy - 4.4, 1]
      ];
      for (let i = 0; i < strands2.length; i++) {
        const [sx, sy, ph] = strands2[i];
        const sway = maneSway * (0.4 + ph);
        ctx.strokeStyle = rgb51(i % 2 ? p.mane : p.maneShade, 0.92);
        ctx.beginPath();
        ctx.moveTo(sx, sy);
        ctx.quadraticCurveTo(sx - 1.4 + sway, sy + 2.4, sx - 0.8 + sway * 1.6, sy + 5.2);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + 1.4 + side * 1.6, hy - 5.2);
      ctx.rotate(-0.15 + side * 0.34);
      ctx.fillStyle = rgb51(p.outline);
      ctx.beginPath();
      ctx.moveTo(0, 2.2);
      ctx.quadraticCurveTo(-1, -2.6, 0.2, -3.4);
      ctx.quadraticCurveTo(1.4, -2.4, 1.2, 2);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb51(p.coatMid);
      ctx.beginPath();
      ctx.moveTo(0.2, 1.8);
      ctx.quadraticCurveTo(-0.5, -2, 0.3, -2.8);
      ctx.quadraticCurveTo(1, -2, 0.9, 1.6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb51(p.muzzle, 0.8);
      ctx.beginPath();
      ctx.ellipse(0.4, -0.2, 0.5, 1.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.34);
    ctx.fillStyle = rgb51(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.2, 6.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(0, 0, 3.5, 5.5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.coatMid);
    ctx.beginPath();
    ctx.ellipse(-0.4, -0.4, 3.1, 5.1, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(-0.8, -0.9, 2.3, 3.8, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.maneShade);
    ctx.beginPath();
    ctx.moveTo(-1.4, -5);
    ctx.quadraticCurveTo(0.4, -6.2, 1.4, -4.6);
    ctx.quadraticCurveTo(0.2, -3.4, -1, -3.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb51(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(1.4, -1.6, 1, 1.3, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51([255, 255, 255], 0.5);
    ctx.beginPath();
    ctx.arc(1.1, -2, 0.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.muzzle);
    ctx.beginPath();
    ctx.ellipse(0.4, 4.4, 2.4, 2, 0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb51(p.hoofDark, 0.85);
    ctx.beginPath();
    ctx.ellipse(0.9, 4.8, 0.6, 0.9, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb51([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 4.4, hy + 4.6, 3, 2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash17(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb51(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint51(ctx, pIn, bob, maneSway = 0, tailSway = 0, headToss = 0) {
    const p = clampP40(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad18(ctx, p);
      paintHorse(ctx, p, bob, maneSway, tailSway, headToss);
      paintLightWash17(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw41(season) {
    return (ctx) => paint51(ctx, SP51[season], 0);
  }
  function anim41(season) {
    return (ctx, t) => {
      const p = clampP40(SP51[season]);
      const bob = bobAt51(t);
      const maneSway = Math.sin(t * 1.6) * 0.7;
      const tailSway = Math.sin(t * 1.15 + 0.6) * 1.6;
      const tossLoop = t % 6 / 6;
      const headToss = 1.4 * (1 - Math.cos(tossLoop * Math.PI * 2)) * 0.5;
      paint51(ctx, SP51[season], bob, maneSway, tailSway, headToss);
      ctx.save();
      try {
        const bx = 2;
        const by = 2 - bob;
        const hx = bx - 16;
        const hy = by - 1 - headToss;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.save();
        ctx.translate(hx + 1.4 - 1.6, hy - 5.2);
        ctx.rotate(-0.15 - 0.34 + flick * 0.5);
        ctx.fillStyle = rgb51(p.outline);
        ctx.beginPath();
        ctx.moveTo(0, 2.2);
        ctx.quadraticCurveTo(-1, -2.6, 0.2, -3.4);
        ctx.quadraticCurveTo(1.4, -2.4, 1.2, 2);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb51(p.coatMid);
        ctx.beginPath();
        ctx.moveTo(0.2, 1.8);
        ctx.quadraticCurveTo(-0.5, -2, 0.3, -2.8);
        ctx.quadraticCurveTo(1, -2, 0.9, 1.6);
        ctx.closePath();
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb51([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb51([255, 255, 255], 0.34);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 2.8, 5.4, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb51([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.35);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb51([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach + 0.5, hy + 4.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 7 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb51([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb51([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13, 8, -0.06, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition48(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother51(clamp0152(pp));
      paint51(ctx, lerpP51(SP51[from], SP51[to], k), 0);
    };
  }
  var springToSummer45 = makeTransition48(0);
  var summerToAutumn45 = makeTransition48(1);
  var autumnToWinter45 = makeTransition48(2);
  var VARIANTS53 = {
    Spring: { draw: draw41("Spring"), anim: anim41("Spring") },
    Summer: { draw: draw41("Summer"), anim: anim41("Summer") },
    Autumn: { draw: draw41("Autumn"), anim: anim41("Autumn") },
    Winter: { draw: draw41("Winter"), anim: anim41("Winter") }
  };
  var TRANSITIONS53 = {
    0: springToSummer45,
    1: summerToAutumn45,
    2: autumnToWinter45
  };

  // src/textures/seasonal/mount/donkey.ts
  function clamp0153(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp57(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB45(a, b, t) {
    return [lerp57(a[0], b[0], t), lerp57(a[1], b[1], t), lerp57(a[2], b[2], t)];
  }
  function rgb52(c, a = 1) {
    const r = Math.round(clamp0153(c[0] / 255) * 255);
    const g = Math.round(clamp0153(c[1] / 255) * 255);
    const b = Math.round(clamp0153(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0153(a)})`;
  }
  var smoother52 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt52(t, amp = 1, w = 1.35) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP52 = {
    // Spring — sleek grey coat, dewy lime pad + blossom, cool-bright light.
    Spring: {
      coatLight: [176, 176, 180],
      coatMid: [140, 140, 146],
      coatShadow: [98, 98, 106],
      belly: [216, 212, 206],
      mane: [60, 56, 56],
      maneShade: [38, 36, 38],
      hoofDark: [46, 42, 44],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [54, 50, 52],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.12,
      // sleek
      gloss: 0.3,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — normal grey coat (PEAK), saturated mid-green pad, warm light.
    Summer: {
      coatLight: [186, 184, 186],
      coatMid: [148, 146, 150],
      coatShadow: [100, 98, 104],
      belly: [222, 218, 210],
      mane: [56, 52, 52],
      maneShade: [34, 32, 34],
      hoofDark: [42, 38, 40],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [50, 46, 48],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.4,
      // normal full coat
      gloss: 0.95,
      // glossiest peak
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — slightly fuller coat, olive-tan browning pad, fallen leaves.
    Autumn: {
      coatLight: [174, 170, 168],
      coatMid: [138, 134, 134],
      coatShadow: [96, 92, 94],
      belly: [216, 210, 198],
      mane: [58, 54, 52],
      maneShade: [36, 33, 33],
      hoofDark: [46, 41, 42],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [52, 48, 48],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.62,
      // slightly fuller
      gloss: 0.4,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — thick fluffed coat + back-snow + breath fog, snowy pad, cool light.
    Winter: {
      coatLight: [184, 188, 196],
      coatMid: [146, 150, 158],
      coatShadow: [102, 106, 116],
      belly: [224, 226, 228],
      mane: [60, 58, 60],
      maneShade: [40, 38, 42],
      hoofDark: [50, 46, 48],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [56, 52, 56],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick & fluffed
      gloss: 0.2,
      frostAmt: 0.65,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP52(a, b, t) {
    return {
      coatLight: lerpRGB45(a.coatLight, b.coatLight, t),
      coatMid: lerpRGB45(a.coatMid, b.coatMid, t),
      coatShadow: lerpRGB45(a.coatShadow, b.coatShadow, t),
      belly: lerpRGB45(a.belly, b.belly, t),
      mane: lerpRGB45(a.mane, b.mane, t),
      maneShade: lerpRGB45(a.maneShade, b.maneShade, t),
      hoofDark: lerpRGB45(a.hoofDark, b.hoofDark, t),
      padGrass: lerpRGB45(a.padGrass, b.padGrass, t),
      soil: lerpRGB45(a.soil, b.soil, t),
      outline: lerpRGB45(a.outline, b.outline, t),
      lightWash: lerpRGB45(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp57(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp57(a.coatVolume, b.coatVolume, t),
      gloss: lerp57(a.gloss, b.gloss, t),
      frostAmt: lerp57(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp57(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp57(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp57(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp57(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp57(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP41(p) {
    return {
      ...p,
      lightWashAmt: clamp0153(p.lightWashAmt),
      coatVolume: clamp0153(p.coatVolume),
      gloss: clamp0153(p.gloss),
      frostAmt: clamp0153(p.frostAmt),
      backSnowAmt: clamp0153(p.backSnowAmt),
      padSnowAmt: clamp0153(p.padSnowAmt),
      blossomAmt: clamp0153(p.blossomAmt),
      fallenLeafAmt: clamp0153(p.fallenLeafAmt),
      breathFogAmt: clamp0153(p.breathFogAmt)
    };
  }
  function paintPad19(ctx, p) {
    ctx.fillStyle = rgb52([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb52(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb52([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb52([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb52([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb52([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb52([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb52(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb52([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg17(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb52(p.outline);
    ctx.lineWidth = 3.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb52(p.coatShadow);
    ctx.lineWidth = 2;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1.4);
    ctx.stroke();
    ctx.fillStyle = rgb52(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 1.7, 1.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath9(ctx, cx, cy, vol) {
    const rx = 12 * (0.97 + vol * 0.12);
    const ry = 8.2 * (0.97 + vol * 0.14);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.05, 0, Math.PI * 2);
  }
  function paintDonkey(ctx, p, bob, earSwivel = 0, tailSway = 0, headToss = 0) {
    const bx = 2;
    const by = 2 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg17(ctx, p, bx + 8, by + 5.8, 18.8);
    paintLeg17(ctx, p, bx - 4, by + 5.8, 19);
    ctx.restore();
    paintLeg17(ctx, p, bx + 5.5, by + 6.2, 19.6);
    paintLeg17(ctx, p, bx - 6.5, by + 6.2, 19.8);
    {
      const tx0 = bx + 10.5;
      const ty0 = by - 3.5;
      const tipX = tx0 + 3 + tailSway * 1.6;
      const tipY = by + 12.5;
      ctx.strokeStyle = rgb52(p.coatShadow);
      ctx.lineWidth = 2.6 * (0.9 + vol * 0.2);
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(tx0, ty0);
      ctx.quadraticCurveTo(tx0 + 4 + tailSway, by + 4, tipX, tipY - 2);
      ctx.stroke();
      ctx.fillStyle = rgb52(p.maneShade);
      ctx.beginPath();
      ctx.ellipse(tipX, tipY, 2.2, 3.2, 0.1 + tailSway * 0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgb52(p.mane, 0.9);
      ctx.lineWidth = 1;
      for (const off of [-1.4, 0, 1.6]) {
        ctx.beginPath();
        ctx.moveTo(tipX + off * 0.5, tipY - 2);
        ctx.quadraticCurveTo(tipX + off, tipY + 1, tipX + off * 1.2, tipY + 3.4);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    ctx.fillStyle = rgb52(p.outline);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.1, 1.14);
    ctx.translate(-bx, -by);
    bodyPath9(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb52(p.coatShadow);
    bodyPath9(ctx, bx, by, vol);
    ctx.fill();
    ctx.fillStyle = rgb52(p.coatMid);
    ctx.save();
    ctx.translate(-0.4, -0.4);
    bodyPath9(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.fillStyle = rgb52(p.coatLight);
    ctx.translate(-1.6, -1.8);
    bodyPath9(ctx, bx, by, vol * 0.7);
    ctx.fill();
    ctx.restore();
    ctx.save();
    bodyPath9(ctx, bx, by, vol);
    ctx.clip();
    ctx.fillStyle = rgb52(p.belly, 0.92);
    ctx.beginPath();
    ctx.ellipse(bx - 1, by + 4.4, 9.5, 4.2, -0.04, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    ctx.strokeStyle = rgb52(p.mane, 0.85);
    ctx.lineWidth = 1.8;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx - 9, by - 5.4);
    ctx.quadraticCurveTo(bx, by - 8.4, bx + 10.5, by - 3.6);
    ctx.stroke();
    ctx.lineCap = "butt";
    if (p.gloss > 0.02) {
      ctx.save();
      bodyPath9(ctx, bx, by, vol);
      ctx.clip();
      ctx.fillStyle = rgb52([255, 255, 255], 0.12 + 0.18 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 4.6, 8.6, 2.4, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (vol > 0.55) {
      const fr = (vol - 0.55) / 0.45;
      ctx.fillStyle = rgb52(p.coatShadow, 0.9);
      const rx = 12 * (0.97 + vol * 0.12);
      const ry = 8.2 * (0.97 + vol * 0.14);
      for (let i = 0; i < 9; i++) {
        const a = Math.PI * 0.15 + i / 8 * Math.PI * 0.9;
        const lx = bx + Math.cos(a) * rx * 1.02;
        const ly = by + Math.sin(a) * ry * 1.02;
        ctx.beginPath();
        ctx.arc(lx, ly, (1.2 + 0.9 * fr) * (i % 2 ? 1 : 0.8), 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb52([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx, by - 7, 8.6 * (0.9 + vol * 0.2), 3, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb52([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-5, -7.6], [1, -8.2], [6, -7.2]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.4 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb52([255, 255, 255], 0.75 * p.frostAmt);
      const pts = [
        [-7, 0],
        [-2, 4],
        [4, -2],
        [8, 2],
        [0, -3],
        [-4, -5],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const neckTopX = bx - 8;
    const neckTopY = by - 5;
    const hx = bx - 15;
    const hy = by - 1 - headToss;
    ctx.fillStyle = rgb52(p.outline);
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 6);
    ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 1, hx + 2.5, hy - 3.5);
    ctx.lineTo(hx + 4.5, hy + 3.5);
    ctx.quadraticCurveTo(neckTopX + 3, by + 1, bx - 1, by + 3);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb52(p.coatMid);
    ctx.beginPath();
    ctx.moveTo(bx - 3, by - 5.6);
    ctx.quadraticCurveTo(neckTopX - 0.6, neckTopY - 0.4, hx + 2.6, hy - 3);
    ctx.lineTo(hx + 4.2, hy + 3);
    ctx.quadraticCurveTo(neckTopX + 3, by + 0.6, bx - 1, by + 2.4);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb52(p.coatLight, 0.8);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(hx + 4, hy + 2.6);
    ctx.quadraticCurveTo(neckTopX + 3, by + 0.4, bx - 1, by + 2);
    ctx.stroke();
    ctx.lineCap = "butt";
    {
      ctx.strokeStyle = rgb52(p.maneShade);
      ctx.lineWidth = 3.2 * (0.9 + vol * 0.2);
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(bx - 3, by - 6.4);
      ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 1.8, hx + 2.2, hy - 4.6);
      ctx.stroke();
      ctx.lineWidth = 1.2;
      const tufts = [
        [bx - 3.5, by - 6.6, 0.1],
        [bx - 6, by - 6.9, 0.35],
        [neckTopX - 0.6, neckTopY - 2, 0.6],
        [hx + 1, hy - 4.8, 0.85]
      ];
      for (let i = 0; i < tufts.length; i++) {
        const [sx, sy, ph] = tufts[i];
        const sw = earSwivel * 0.25 * (0.4 + ph);
        ctx.strokeStyle = rgb52(i % 2 ? p.mane : p.maneShade, 0.95);
        ctx.beginPath();
        ctx.moveTo(sx, sy);
        ctx.quadraticCurveTo(sx + 0.4 + sw, sy - 1.8, sx + 0.9 + sw, sy - 3.4);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
    }
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(hx + 1.2 + side * 1.7, hy - 5.6);
      ctx.rotate(-0.06 + side * 0.22 + earSwivel * (side > 0 ? 0 : 1));
      ctx.fillStyle = rgb52(p.outline);
      ctx.beginPath();
      ctx.moveTo(0, 3.2);
      ctx.quadraticCurveTo(-1.4, -4.6, 0.3, -7.2);
      ctx.quadraticCurveTo(1.9, -4.4, 1.5, 3);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb52(p.coatMid);
      ctx.beginPath();
      ctx.moveTo(0.2, 2.8);
      ctx.quadraticCurveTo(-0.8, -4, 0.4, -6.2);
      ctx.quadraticCurveTo(1.4, -4, 1.1, 2.6);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb52(p.belly, 0.9);
      ctx.beginPath();
      ctx.ellipse(0.5, -1.4, 0.55, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(0.34);
    ctx.fillStyle = rgb52(p.outline);
    ctx.beginPath();
    ctx.ellipse(0, 0, 4.4, 6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(0, 0, 3.7, 5.3, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.coatMid);
    ctx.beginPath();
    ctx.ellipse(-0.4, -0.4, 3.3, 4.9, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(-0.9, -0.9, 2.4, 3.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.maneShade);
    ctx.beginPath();
    ctx.moveTo(-1.4, -4.8);
    ctx.quadraticCurveTo(0.4, -6, 1.4, -4.4);
    ctx.quadraticCurveTo(0.2, -3.4, -1, -3.6);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb52(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(1.4, -1.6, 1, 1.3, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52([255, 255, 255], 0.5);
    ctx.beginPath();
    ctx.arc(1.1, -2, 0.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.belly);
    ctx.beginPath();
    ctx.ellipse(0.4, 4.3, 2.7, 2.3, 0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb52(p.hoofDark, 0.85);
    ctx.beginPath();
    ctx.ellipse(0.9, 4.7, 0.6, 0.9, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb52([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 4.6, hy + 4.6, 3, 2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash18(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb52(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint52(ctx, pIn, bob, earSwivel = 0, tailSway = 0, headToss = 0) {
    const p = clampP41(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad19(ctx, p);
      paintDonkey(ctx, p, bob, earSwivel, tailSway, headToss);
      paintLightWash18(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw42(season) {
    return (ctx) => paint52(ctx, SP52[season], 0);
  }
  function anim42(season) {
    return (ctx, t) => {
      const p = clampP41(SP52[season]);
      const bob = bobAt52(t);
      const tailSway = Math.sin(t * 1.1 + 0.4) * 1.4;
      const swishLoop = t % 6 / 6;
      const swishGate = Math.max(0, Math.sin(swishLoop * Math.PI * 2)) ** 4;
      const tailSwish = Math.sin(t * 5) * swishGate * 2;
      const tossLoop = t % 6 / 6;
      const headToss = 1.2 * (1 - Math.cos(tossLoop * Math.PI * 2)) * 0.5;
      paint52(ctx, SP52[season], bob, 0, tailSway + tailSwish, headToss);
      ctx.save();
      try {
        const bx = 2;
        const by = 2 - bob;
        const hx = bx - 15;
        const hy = by - 1 - headToss;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const earSwivel = Math.sin(t * 7) * flickGate * 0.5;
        ctx.save();
        ctx.translate(hx + 1.2 - 1.7, hy - 5.6);
        ctx.rotate(-0.06 - 0.22 + earSwivel);
        ctx.fillStyle = rgb52(p.outline);
        ctx.beginPath();
        ctx.moveTo(0, 3.2);
        ctx.quadraticCurveTo(-1.4, -4.6, 0.3, -7.2);
        ctx.quadraticCurveTo(1.9, -4.4, 1.5, 3);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb52(p.coatMid);
        ctx.beginPath();
        ctx.moveTo(0.2, 2.8);
        ctx.quadraticCurveTo(-0.8, -4, 0.4, -6.2);
        ctx.quadraticCurveTo(1.4, -4, 1.1, 2.6);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgb52(p.belly, 0.9);
        ctx.beginPath();
        ctx.ellipse(0.5, -1.4, 0.55, 3, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb52([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.1);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb52([255, 255, 255], 0.34);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 2.8, 5.4, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb52([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.35);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb52([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach + 0.5, hy + 4.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 7 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb52([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb52([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 12.5, 8.2, -0.05, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition49(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother52(clamp0153(pp));
      paint52(ctx, lerpP52(SP52[from], SP52[to], k), 0);
    };
  }
  var springToSummer46 = makeTransition49(0);
  var summerToAutumn46 = makeTransition49(1);
  var autumnToWinter46 = makeTransition49(2);
  var VARIANTS54 = {
    Spring: { draw: draw42("Spring"), anim: anim42("Spring") },
    Summer: { draw: draw42("Summer"), anim: anim42("Summer") },
    Autumn: { draw: draw42("Autumn"), anim: anim42("Autumn") },
    Winter: { draw: draw42("Winter"), anim: anim42("Winter") }
  };
  var TRANSITIONS54 = {
    0: springToSummer46,
    1: summerToAutumn46,
    2: autumnToWinter46
  };

  // src/textures/seasonal/mount/moose.ts
  function clamp0154(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp58(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB46(a, b, t) {
    return [lerp58(a[0], b[0], t), lerp58(a[1], b[1], t), lerp58(a[2], b[2], t)];
  }
  function rgb53(c, a = 1) {
    const r = Math.round(clamp0154(c[0] / 255) * 255);
    const g = Math.round(clamp0154(c[1] / 255) * 255);
    const b = Math.round(clamp0154(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0154(a)})`;
  }
  var smoother53 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt53(t, amp = 1.1, w = 1.15) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP53 = {
    // Spring — sleek coat, soft VELVET antlers, dewy lime pad + blossom, cool light.
    Spring: {
      coatLight: [110, 78, 52],
      coatMid: [82, 56, 36],
      coatShadow: [54, 36, 24],
      mane: [44, 28, 18],
      maneShade: [28, 18, 12],
      muzzle: [96, 72, 62],
      hoofDark: [34, 26, 22],
      antler: [206, 184, 138],
      // velvety warm bone
      antlerShade: [150, 128, 88],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [38, 24, 16],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.16,
      // sleek
      gloss: 0.28,
      velvetAmt: 1,
      // full fuzzy velvet
      antlerSnowAmt: 0,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — full HARD antlers, normal coat (PEAK), saturated pad, warm light.
    Summer: {
      coatLight: [120, 84, 54],
      coatMid: [88, 58, 36],
      coatShadow: [56, 36, 22],
      mane: [42, 26, 16],
      maneShade: [26, 16, 10],
      muzzle: [98, 72, 62],
      hoofDark: [32, 24, 20],
      antler: [224, 206, 162],
      // clean hard bone
      antlerShade: [164, 142, 100],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [36, 22, 14],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.44,
      // normal full coat
      gloss: 0.9,
      // glossiest peak
      velvetAmt: 0,
      // shed velvet — hard antler
      antlerSnowAmt: 0,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — hard POLISHED antlers (rich), fuller coat, olive-tan pad, leaves.
    Autumn: {
      coatLight: [112, 76, 48],
      coatMid: [84, 54, 32],
      coatShadow: [54, 34, 20],
      mane: [44, 28, 18],
      maneShade: [28, 17, 10],
      muzzle: [94, 70, 60],
      hoofDark: [34, 26, 22],
      antler: [200, 168, 116],
      // rich polished bone
      antlerShade: [142, 112, 70],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [38, 24, 16],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.66,
      // fuller coat
      gloss: 0.4,
      velvetAmt: 0,
      // hard polished antler
      antlerSnowAmt: 0,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.75,
      breathFogAmt: 0
    },
    // Winter — thick coat + back-snow + antler-snow + breath fog, snowy pad, cool.
    Winter: {
      coatLight: [98, 72, 52],
      coatMid: [74, 52, 36],
      coatShadow: [50, 34, 24],
      mane: [44, 30, 22],
      maneShade: [28, 18, 14],
      muzzle: [96, 74, 66],
      hoofDark: [40, 32, 28],
      antler: [196, 180, 150],
      // cool muted bone
      antlerShade: [140, 124, 100],
      padGrass: [222, 232, 244],
      // snow on the pad
      soil: [150, 168, 190],
      outline: [40, 28, 20],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.24,
      coatVolume: 1,
      // thick & shaggy
      gloss: 0.18,
      velvetAmt: 0,
      // hard antler, but snow-capped
      antlerSnowAmt: 0.8,
      frostAmt: 0.6,
      backSnowAmt: 0.7,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.7
    }
  };
  function lerpP53(a, b, t) {
    return {
      coatLight: lerpRGB46(a.coatLight, b.coatLight, t),
      coatMid: lerpRGB46(a.coatMid, b.coatMid, t),
      coatShadow: lerpRGB46(a.coatShadow, b.coatShadow, t),
      mane: lerpRGB46(a.mane, b.mane, t),
      maneShade: lerpRGB46(a.maneShade, b.maneShade, t),
      muzzle: lerpRGB46(a.muzzle, b.muzzle, t),
      hoofDark: lerpRGB46(a.hoofDark, b.hoofDark, t),
      antler: lerpRGB46(a.antler, b.antler, t),
      antlerShade: lerpRGB46(a.antlerShade, b.antlerShade, t),
      padGrass: lerpRGB46(a.padGrass, b.padGrass, t),
      soil: lerpRGB46(a.soil, b.soil, t),
      outline: lerpRGB46(a.outline, b.outline, t),
      lightWash: lerpRGB46(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp58(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp58(a.coatVolume, b.coatVolume, t),
      gloss: lerp58(a.gloss, b.gloss, t),
      velvetAmt: lerp58(a.velvetAmt, b.velvetAmt, t),
      antlerSnowAmt: lerp58(a.antlerSnowAmt, b.antlerSnowAmt, t),
      frostAmt: lerp58(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp58(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp58(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp58(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp58(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp58(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP42(p) {
    return {
      ...p,
      lightWashAmt: clamp0154(p.lightWashAmt),
      coatVolume: clamp0154(p.coatVolume),
      gloss: clamp0154(p.gloss),
      velvetAmt: clamp0154(p.velvetAmt),
      antlerSnowAmt: clamp0154(p.antlerSnowAmt),
      frostAmt: clamp0154(p.frostAmt),
      backSnowAmt: clamp0154(p.backSnowAmt),
      padSnowAmt: clamp0154(p.padSnowAmt),
      blossomAmt: clamp0154(p.blossomAmt),
      fallenLeafAmt: clamp0154(p.fallenLeafAmt),
      breathFogAmt: clamp0154(p.breathFogAmt)
    };
  }
  function paintPad20(ctx, p) {
    ctx.fillStyle = rgb53([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb53(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb53([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb53([244, 250, 255], 0.85 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.6, 17, 4.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53([255, 255, 255], 0.7 * p.padSnowAmt);
      for (const [sx, sy] of [[-9, 19], [6, 20], [12, 18]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb53([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb53([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb53(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb53([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg18(ctx, p, x, topY, baseY) {
    ctx.strokeStyle = rgb53(p.outline);
    ctx.lineWidth = 3.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb53(p.mane);
    ctx.lineWidth = 2.3;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1.4);
    ctx.stroke();
    ctx.fillStyle = rgb53(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(x, baseY, 2, 1.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath10(ctx, cx, cy, vol) {
    const rx = 13 * (0.97 + vol * 0.12);
    const ry = 8.6 * (0.97 + vol * 0.14);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.04, 0, Math.PI * 2);
  }
  function paintAntler(ctx, p, baseX, baseY, side, velvet, snow) {
    ctx.save();
    ctx.translate(baseX, baseY);
    ctx.scale(side, 1);
    const palmX = 9.5;
    const palmY = -7.5;
    ctx.strokeStyle = rgb53(p.outline, 0.95);
    ctx.lineJoin = "round";
    ctx.lineCap = "round";
    ctx.lineWidth = 4.4 + velvet * 1;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(4.5, -3.5, palmX, palmY);
    ctx.stroke();
    const fingers = [
      [4, -7.2],
      [7.6, -8.6],
      [11, -8.4],
      [13.6, -6.4],
      [14.4, -3.4]
    ];
    const palmPath = () => {
      ctx.beginPath();
      ctx.moveTo(palmX - 4.5, palmY + 2.2);
      ctx.quadraticCurveTo(palmX - 5, palmY - 3, palmX - 1.8, palmY - 4.4);
      for (const [fx, fy] of fingers) {
        ctx.lineTo(palmX + fx - 5.5, palmY + fy + 4);
      }
      ctx.quadraticCurveTo(palmX + 8, palmY + 4.4, palmX + 3.5, palmY + 4.8);
      ctx.quadraticCurveTo(palmX - 1, palmY + 4.6, palmX - 4.5, palmY + 2.2);
      ctx.closePath();
    };
    ctx.fillStyle = rgb53(p.outline, 0.95);
    ctx.save();
    ctx.translate(palmX, palmY);
    ctx.scale(1.16, 1.18);
    ctx.translate(-palmX, -palmY);
    palmPath();
    ctx.fill();
    ctx.restore();
    ctx.strokeStyle = rgb53(p.antlerShade);
    ctx.lineWidth = 3 + velvet * 0.8;
    ctx.beginPath();
    ctx.moveTo(0, 0);
    ctx.quadraticCurveTo(4.5, -3.5, palmX, palmY);
    ctx.stroke();
    ctx.strokeStyle = rgb53(p.antler);
    ctx.lineWidth = 1.7 + velvet * 0.5;
    ctx.beginPath();
    ctx.moveTo(0.4, 0);
    ctx.quadraticCurveTo(4.8, -3.6, palmX, palmY - 0.4);
    ctx.stroke();
    ctx.fillStyle = rgb53(p.antlerShade);
    palmPath();
    ctx.fill();
    ctx.save();
    ctx.fillStyle = rgb53(p.antler);
    ctx.translate(-1.2, -1.4);
    ctx.scale(0.92, 0.92);
    ctx.translate(palmX, palmY);
    ctx.translate(-palmX, -palmY);
    palmPath();
    ctx.fill();
    ctx.restore();
    ctx.strokeStyle = rgb53(p.antlerShade, 0.85);
    ctx.lineWidth = 1;
    for (const [fx, fy] of fingers) {
      ctx.beginPath();
      ctx.moveTo(palmX - 1.5, palmY + 1.5);
      ctx.lineTo(palmX + fx - 5.5, palmY + fy + 4.4);
      ctx.stroke();
    }
    if (velvet > 0.02) {
      ctx.fillStyle = rgb53([214, 196, 156], 0.5 * velvet);
      for (const [fx, fy] of fingers) {
        ctx.beginPath();
        ctx.arc(palmX + fx - 5.5, palmY + fy + 4, 1.5, 0, Math.PI * 2);
        ctx.fill();
      }
      ctx.save();
      palmPath();
      ctx.clip();
      ctx.fillStyle = rgb53([236, 222, 188], 0.28 * velvet);
      ctx.beginPath();
      ctx.ellipse(palmX, palmY - 1, 8, 4, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    } else {
      ctx.fillStyle = rgb53([255, 255, 255], 0.3);
      ctx.beginPath();
      ctx.ellipse(palmX - 1, palmY - 2, 2.2, 1.2, -0.2, 0, Math.PI * 2);
      ctx.fill();
    }
    if (snow > 0.02) {
      ctx.save();
      palmPath();
      ctx.clip();
      ctx.fillStyle = rgb53([248, 252, 255], 0.92 * snow);
      ctx.beginPath();
      ctx.ellipse(palmX + 1, palmY - 2.4, 8.4, 3.4, -0.08, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.fillStyle = rgb53([255, 255, 255], 0.85 * snow);
      for (const [fx, fy] of fingers) {
        ctx.beginPath();
        ctx.arc(palmX + fx - 5.5, palmY + fy + 3, 1.3, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    ctx.lineCap = "butt";
    ctx.restore();
  }
  function paintMoose(ctx, p, bob, headSway = 0) {
    const bx = 2;
    const by = 3 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.9;
    paintLeg18(ctx, p, bx + 8.5, by + 5.5, 18.6);
    paintLeg18(ctx, p, bx - 3.5, by + 5.5, 18.8);
    ctx.restore();
    paintLeg18(ctx, p, bx + 5.5, by + 6, 19.6);
    paintLeg18(ctx, p, bx - 6.5, by + 6, 19.8);
    ctx.fillStyle = rgb53(p.outline);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.1, 1.14);
    ctx.translate(-bx, -by);
    bodyPath10(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb53(p.coatShadow);
    bodyPath10(ctx, bx, by, vol);
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatMid);
    ctx.save();
    ctx.translate(-0.4, -0.4);
    bodyPath10(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.fillStyle = rgb53(p.coatLight);
    ctx.translate(-1.6, -1.8);
    bodyPath10(ctx, bx, by, vol * 0.7);
    ctx.fill();
    ctx.restore();
    {
      const humpX = bx - 4.5;
      const humpY = by - 7.4;
      ctx.fillStyle = rgb53(p.outline);
      ctx.beginPath();
      ctx.ellipse(humpX, humpY, 7.4, 5.4, -0.35, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53(p.coatShadow);
      ctx.beginPath();
      ctx.ellipse(humpX, humpY, 6.4, 4.6, -0.35, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53(p.coatMid);
      ctx.beginPath();
      ctx.ellipse(humpX - 0.6, humpY - 0.8, 5.6, 3.9, -0.35, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53(p.coatLight);
      ctx.beginPath();
      ctx.ellipse(humpX - 1.4, humpY - 1.6, 4, 2.6, -0.35, 0, Math.PI * 2);
      ctx.fill();
    }
    if (p.gloss > 0.02) {
      ctx.save();
      bodyPath10(ctx, bx, by, vol);
      ctx.clip();
      ctx.fillStyle = rgb53([255, 255, 255], 0.1 + 0.16 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 4.6, 9, 2.4, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (vol > 0.55) {
      const fr = (vol - 0.55) / 0.45;
      ctx.fillStyle = rgb53(p.coatShadow, 0.9);
      const rx = 13 * (0.97 + vol * 0.12);
      const ry = 8.6 * (0.97 + vol * 0.14);
      for (let i = 0; i < 10; i++) {
        const a = Math.PI * 0.12 + i / 9 * Math.PI * 0.95;
        const lx = bx + Math.cos(a) * rx * 1.02;
        const ly = by + Math.sin(a) * ry * 1.02;
        ctx.beginPath();
        ctx.arc(lx, ly, (1.3 + 0.9 * fr) * (i % 2 ? 1 : 0.8), 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb53([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 8.2, 9.5 * (0.9 + vol * 0.2), 3.2, -0.18, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-7, -9.4], [-1, -9], [5, -7.8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.5 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb53([255, 255, 255], 0.7 * p.frostAmt);
      const pts = [
        [-7, 0],
        [-2, 4],
        [4, -1],
        [8, 2],
        [0, -3],
        [-4, -5],
        [6, 4]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const neckTopX = bx - 8;
    const neckTopY = by - 4;
    const hx = bx - 16;
    const hy = by + 1;
    ctx.fillStyle = rgb53(p.outline);
    ctx.beginPath();
    ctx.moveTo(bx - 2, by - 7);
    ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 2, hx + 3, hy - 4.5);
    ctx.lineTo(hx + 5, hy + 3.5);
    ctx.quadraticCurveTo(neckTopX + 3, by + 2, bx, by + 4);
    ctx.closePath();
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatMid);
    ctx.beginPath();
    ctx.moveTo(bx - 2, by - 6.6);
    ctx.quadraticCurveTo(neckTopX - 0.6, neckTopY - 1.4, hx + 3.1, hy - 4);
    ctx.lineTo(hx + 4.6, hy + 3);
    ctx.quadraticCurveTo(neckTopX + 3, by + 1.6, bx, by + 3.4);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb53(p.coatLight, 0.7);
    ctx.lineWidth = 1.6;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(hx + 4.4, hy + 2.6);
    ctx.quadraticCurveTo(neckTopX + 3, by + 1.4, bx, by + 3);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.strokeStyle = rgb53(p.maneShade);
    ctx.lineWidth = 2.6 * (0.9 + vol * 0.2);
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx - 2, by - 7.4);
    ctx.quadraticCurveTo(neckTopX - 1, neckTopY - 2.6, hx + 2.6, hy - 5);
    ctx.stroke();
    ctx.lineCap = "butt";
    ctx.save();
    ctx.translate(hx, hy);
    ctx.rotate(headSway);
    paintAntler(ctx, p, 1.6, -5.4, 1, p.velvetAmt, p.antlerSnowAmt);
    paintAntler(ctx, p, -1.6, -5.4, -1, p.velvetAmt, p.antlerSnowAmt);
    for (const side of [-1, 1]) {
      ctx.save();
      ctx.translate(side * 3, -3.6);
      ctx.rotate(-0.1 + side * 0.5);
      ctx.fillStyle = rgb53(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, 0, 1.5, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53(p.coatMid);
      ctx.beginPath();
      ctx.ellipse(0, 0.2, 1, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb53(p.muzzle, 0.8);
      ctx.beginPath();
      ctx.ellipse(0, 0.2, 0.5, 1.3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.save();
    ctx.rotate(0.28);
    ctx.fillStyle = rgb53(p.outline);
    ctx.beginPath();
    ctx.ellipse(-1, 2, 4.6, 7.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(-1, 2, 3.9, 6.5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatMid);
    ctx.beginPath();
    ctx.ellipse(-1.4, 1.4, 3.4, 6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(-1.8, 0.6, 2.4, 4.2, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.outline);
    ctx.beginPath();
    ctx.ellipse(-1.2, 8.4, 3.6, 3.2, -0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.muzzle);
    ctx.beginPath();
    ctx.ellipse(-1.4, 8, 2.9, 2.6, -0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.coatShadow, 0.6);
    ctx.beginPath();
    ctx.ellipse(-0.6, 9.2, 2.2, 1.5, -0.15, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.hoofDark, 0.85);
    ctx.beginPath();
    ctx.ellipse(-1.8, 8.6, 0.7, 1, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53(p.hoofDark);
    ctx.beginPath();
    ctx.ellipse(0.6, 1.6, 1, 1.3, 0.2, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb53([255, 255, 255], 0.5);
    ctx.beginPath();
    ctx.arc(0.3, 1.1, 0.4, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
    {
      const dlx = -2.4;
      const dly = 8;
      ctx.fillStyle = rgb53(p.outline);
      ctx.beginPath();
      ctx.moveTo(dlx - 1.6, dly);
      ctx.quadraticCurveTo(dlx - 2.6, dly + 6.5, dlx - 0.4, dly + 8.4);
      ctx.quadraticCurveTo(dlx + 1.8, dly + 6.5, dlx + 1.4, dly);
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb53(p.maneShade);
      ctx.beginPath();
      ctx.moveTo(dlx - 1.1, dly);
      ctx.quadraticCurveTo(dlx - 2, dly + 6, dlx - 0.4, dly + 7.6);
      ctx.quadraticCurveTo(dlx + 1.3, dly + 6, dlx + 1, dly);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb53(p.mane, 0.7);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(dlx - 1, dly + 1);
      ctx.quadraticCurveTo(dlx - 1.8, dly + 5.4, dlx - 0.5, dly + 7);
      ctx.stroke();
    }
    ctx.restore();
    if (p.breathFogAmt > 1e-3) {
      ctx.fillStyle = rgb53([235, 244, 255], 0.4 * p.breathFogAmt);
      ctx.beginPath();
      ctx.ellipse(hx - 5.6, hy + 8.6, 3, 2, 0.2, 0, Math.PI * 2);
      ctx.fill();
    }
  }
  function paintLightWash19(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb53(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint53(ctx, pIn, bob, headSway = 0) {
    const p = clampP42(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad20(ctx, p);
      paintMoose(ctx, p, bob, headSway);
      paintLightWash19(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw43(season) {
    return (ctx) => paint53(ctx, SP53[season], 0);
  }
  function anim43(season) {
    return (ctx, t) => {
      const p = clampP42(SP53[season]);
      const bob = bobAt53(t);
      const headSway = Math.sin(t * 0.85) * 0.05;
      paint53(ctx, SP53[season], bob, headSway);
      ctx.save();
      try {
        const bx = 2;
        const by = 3 - bob;
        const hx = bx - 16;
        const hy = by + 1;
        const loop = t % 5 / 5;
        const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
        const flick = Math.sin(t * 9) * flickGate;
        ctx.save();
        ctx.translate(hx, hy);
        ctx.rotate(headSway);
        ctx.translate(-3, -3.6);
        ctx.rotate(-0.1 - 0.5 + flick * 0.5);
        ctx.fillStyle = rgb53(p.outline);
        ctx.beginPath();
        ctx.ellipse(0, 0, 1.5, 2.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb53(p.coatMid);
        ctx.beginPath();
        ctx.ellipse(0, 0.2, 1, 2.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb53([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(hx - 8, hy - 12, 0.9 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(hx + 9, hy - 12, 0.8 + g * 0.6, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(-8, 18.4, 1 + g, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1.05);
          const sx = bx - 9 + s * 18;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb53([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 2.8, 5.6, 0.3, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb53([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.15);
          const reach = 4 + breathe * 3;
          ctx.fillStyle = rgb53([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(hx - reach - 1.6, hy + 8.6, 2.6 + breathe * 1.8, 1.8 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fxx = 7 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = rgb53([255, 255, 255], 0.85);
          ctx.beginPath();
          ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
          ctx.fill();
          const tw = 0.4 + 0.5 * Math.abs(Math.sin(t * 2));
          ctx.fillStyle = rgb53([255, 255, 255], tw * p.antlerSnowAmt);
          for (const [gx, gy] of [[hx - 7, hy - 13], [hx + 8, hy - 13], [hx - 3, hy - 14]]) {
            ctx.beginPath();
            ctx.arc(gx, gy, 0.8, 0, Math.PI * 2);
            ctx.fill();
          }
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb53([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 13, 8.6, -0.04, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition50(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother53(clamp0154(pp));
      paint53(ctx, lerpP53(SP53[from], SP53[to], k), 0);
    };
  }
  var springToSummer47 = makeTransition50(0);
  var summerToAutumn47 = makeTransition50(1);
  var autumnToWinter47 = makeTransition50(2);
  var VARIANTS55 = {
    Spring: { draw: draw43("Spring"), anim: anim43("Spring") },
    Summer: { draw: draw43("Summer"), anim: anim43("Summer") },
    Autumn: { draw: draw43("Autumn"), anim: anim43("Autumn") },
    Winter: { draw: draw43("Winter"), anim: anim43("Winter") }
  };
  var TRANSITIONS55 = {
    0: springToSummer47,
    1: summerToAutumn47,
    2: autumnToWinter47
  };

  // src/textures/seasonal/mount/mammoth.ts
  function clamp0155(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  function lerp59(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB47(a, b, t) {
    return [lerp59(a[0], b[0], t), lerp59(a[1], b[1], t), lerp59(a[2], b[2], t)];
  }
  function rgb54(c, a = 1) {
    const r = Math.round(clamp0155(c[0] / 255) * 255);
    const g = Math.round(clamp0155(c[1] / 255) * 255);
    const b = Math.round(clamp0155(c[2] / 255) * 255);
    return `rgba(${r},${g},${b},${clamp0155(a)})`;
  }
  var smoother54 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function bobAt54(t, amp = 1.1, w = 1.05) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var SP54 = {
    // Spring — patchy SHEDDING coat (tufts loose), dewy lime pad + blossom, cool light.
    Spring: {
      coatLight: [176, 110, 70],
      coatMid: [138, 78, 46],
      coatShadow: [94, 50, 28],
      tuskLight: [244, 236, 214],
      tuskShade: [204, 190, 156],
      trunkShade: [104, 58, 34],
      darkPart: [40, 28, 22],
      padGrass: [128, 206, 86],
      // bright lime dewy grass
      soil: [86, 132, 54],
      outline: [48, 28, 18],
      lightWash: [214, 240, 255],
      // cool-bright
      lightWashAmt: 0.16,
      coatVolume: 0.4,
      // patchy / mid, mid-shed
      shedAmt: 0.85,
      // tufts coming loose
      gloss: 0.28,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.65,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Summer — lighter THINNER coat (PEAK warmth), saturated mid-green pad, warm light.
    Summer: {
      coatLight: [192, 120, 76],
      coatMid: [150, 84, 50],
      coatShadow: [100, 54, 30],
      tuskLight: [248, 240, 220],
      tuskShade: [206, 192, 158],
      trunkShade: [112, 62, 38],
      darkPart: [38, 26, 20],
      padGrass: [86, 170, 64],
      // saturated mid-green
      soil: [58, 112, 40],
      outline: [46, 28, 18],
      lightWash: [255, 244, 206],
      // warm
      lightWashAmt: 0.14,
      coatVolume: 0.18,
      // thinnest — peak warmth
      shedAmt: 0,
      gloss: 0.9,
      // glossiest peak
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0
    },
    // Autumn — full THICK coat, olive-tan browning pad, fallen leaves, amber light.
    Autumn: {
      coatLight: [180, 108, 64],
      coatMid: [142, 78, 44],
      coatShadow: [96, 50, 28],
      tuskLight: [242, 230, 204],
      tuskShade: [200, 184, 148],
      trunkShade: [106, 56, 34],
      darkPart: [42, 30, 22],
      padGrass: [156, 142, 78],
      // olive-tan
      soil: [104, 84, 44],
      outline: [48, 28, 18],
      lightWash: [255, 206, 138],
      // low amber
      lightWashAmt: 0.2,
      coatVolume: 0.74,
      // full thick coat
      shedAmt: 0,
      gloss: 0.36,
      frostAmt: 0,
      backSnowAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.7,
      breathFogAmt: 0
    },
    // Winter — heavy FROSTED shaggy coat + back-snow + breath fog, deep snowy pad
    // (its element), cool blue-grey light, tusks bright.
    Winter: {
      coatLight: [158, 104, 72],
      coatMid: [124, 76, 48],
      coatShadow: [86, 52, 32],
      tuskLight: [252, 250, 242],
      // tusks bright in winter
      tuskShade: [212, 204, 184],
      trunkShade: [98, 56, 36],
      darkPart: [46, 36, 32],
      padGrass: [224, 234, 246],
      // deep snow on the pad
      soil: [150, 168, 190],
      outline: [50, 36, 28],
      lightWash: [206, 222, 248],
      // cool blue-grey
      lightWashAmt: 0.26,
      coatVolume: 1,
      // heaviest shaggy coat (its element)
      shedAmt: 0,
      gloss: 0.18,
      frostAmt: 0.7,
      backSnowAmt: 0.78,
      padSnowAmt: 0.95,
      // deeper snow
      blossomAmt: 0,
      fallenLeafAmt: 0,
      breathFogAmt: 0.72
    }
  };
  function lerpP54(a, b, t) {
    return {
      coatLight: lerpRGB47(a.coatLight, b.coatLight, t),
      coatMid: lerpRGB47(a.coatMid, b.coatMid, t),
      coatShadow: lerpRGB47(a.coatShadow, b.coatShadow, t),
      tuskLight: lerpRGB47(a.tuskLight, b.tuskLight, t),
      tuskShade: lerpRGB47(a.tuskShade, b.tuskShade, t),
      trunkShade: lerpRGB47(a.trunkShade, b.trunkShade, t),
      darkPart: lerpRGB47(a.darkPart, b.darkPart, t),
      padGrass: lerpRGB47(a.padGrass, b.padGrass, t),
      soil: lerpRGB47(a.soil, b.soil, t),
      outline: lerpRGB47(a.outline, b.outline, t),
      lightWash: lerpRGB47(a.lightWash, b.lightWash, t),
      lightWashAmt: lerp59(a.lightWashAmt, b.lightWashAmt, t),
      coatVolume: lerp59(a.coatVolume, b.coatVolume, t),
      shedAmt: lerp59(a.shedAmt, b.shedAmt, t),
      gloss: lerp59(a.gloss, b.gloss, t),
      frostAmt: lerp59(a.frostAmt, b.frostAmt, t),
      backSnowAmt: lerp59(a.backSnowAmt, b.backSnowAmt, t),
      padSnowAmt: lerp59(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp59(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp59(a.fallenLeafAmt, b.fallenLeafAmt, t),
      breathFogAmt: lerp59(a.breathFogAmt, b.breathFogAmt, t)
    };
  }
  function clampP43(p) {
    return {
      ...p,
      lightWashAmt: clamp0155(p.lightWashAmt),
      coatVolume: clamp0155(p.coatVolume),
      shedAmt: clamp0155(p.shedAmt),
      gloss: clamp0155(p.gloss),
      frostAmt: clamp0155(p.frostAmt),
      backSnowAmt: clamp0155(p.backSnowAmt),
      padSnowAmt: clamp0155(p.padSnowAmt),
      blossomAmt: clamp0155(p.blossomAmt),
      fallenLeafAmt: clamp0155(p.fallenLeafAmt),
      breathFogAmt: clamp0155(p.breathFogAmt)
    };
  }
  function paintPad21(ctx, p) {
    ctx.fillStyle = rgb54([0, 0, 0], 0.18);
    ctx.beginPath();
    ctx.ellipse(3, 21, 17, 4.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.soil);
    ctx.beginPath();
    ctx.ellipse(0, 20.5, 18, 5.4, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.padGrass);
    ctx.beginPath();
    ctx.ellipse(0, 19, 18, 5, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.strokeStyle = rgb54(p.padGrass);
    ctx.lineWidth = 1.4;
    ctx.lineCap = "round";
    for (let i = -16; i <= 16; i += 4) {
      const h = 1.6 + (i % 8 === 0 ? 1.4 : 0);
      const yEdge = 19 - Math.sqrt(Math.max(0, 1 - (i / 18) ** 2)) * 5;
      ctx.beginPath();
      ctx.moveTo(i, yEdge);
      ctx.lineTo(i + 1, yEdge - h);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    ctx.fillStyle = rgb54([255, 255, 255], 0.1);
    ctx.beginPath();
    ctx.ellipse(-5, 17.5, 9, 2, 0, 0, Math.PI * 2);
    ctx.fill();
    if (p.padSnowAmt > 1e-3) {
      ctx.fillStyle = rgb54([244, 250, 255], 0.88 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(0, 18.2, 17.4, 5.2 * (0.7 + 0.3 * p.padSnowAmt), 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb54([255, 255, 255], 0.6 * p.padSnowAmt);
      ctx.beginPath();
      ctx.ellipse(-2, 17.2, 12, 2.8, -0.04, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb54([255, 255, 255], 0.75 * p.padSnowAmt);
      for (const [sx, sy] of [[-10, 19], [6, 20], [12, 18], [0, 17]]) {
        ctx.beginPath();
        ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.blossomAmt > 1e-3) {
      for (const [bx, by] of [[-13, 18.5], [10, 20], [0, 21], [14, 19]]) {
        ctx.fillStyle = rgb54([255, 250, 252], 0.9 * p.blossomAmt);
        for (let k = 0; k < 5; k++) {
          const a = k / 5 * Math.PI * 2;
          ctx.beginPath();
          ctx.arc(bx + Math.cos(a) * 1.1, by + Math.sin(a) * 1.1, 0.8, 0, Math.PI * 2);
          ctx.fill();
        }
        ctx.fillStyle = rgb54([252, 214, 110], p.blossomAmt);
        ctx.beginPath();
        ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.fallenLeafAmt > 1e-3) {
      const leaves = [
        [-12, 20, -0.5, [196, 96, 36]],
        [11, 20.5, 0.7, [212, 150, 52]],
        [2, 21, 0.2, [168, 80, 40]]
      ];
      for (const [lx, ly, rot, col] of leaves) {
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(rot);
        ctx.fillStyle = rgb54(col, p.fallenLeafAmt);
        ctx.beginPath();
        ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgb54([110, 60, 26], p.fallenLeafAmt);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(-2.4, 0);
        ctx.lineTo(2.4, 0);
        ctx.stroke();
        ctx.restore();
      }
    }
  }
  function paintLeg19(ctx, p, x, topY, baseY, vol) {
    const w = 4.6 * (0.95 + vol * 0.18);
    ctx.strokeStyle = rgb54(p.outline);
    ctx.lineWidth = w + 1.4;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY);
    ctx.stroke();
    ctx.strokeStyle = rgb54(p.coatShadow);
    ctx.lineWidth = w;
    ctx.beginPath();
    ctx.moveTo(x, topY);
    ctx.lineTo(x, baseY - 1.2);
    ctx.stroke();
    ctx.strokeStyle = rgb54(p.coatMid);
    ctx.lineWidth = w * 0.6;
    ctx.beginPath();
    ctx.moveTo(x - w * 0.18, topY + 1);
    ctx.lineTo(x - w * 0.18, baseY - 2);
    ctx.stroke();
    ctx.fillStyle = rgb54(p.darkPart);
    ctx.beginPath();
    ctx.ellipse(x, baseY, w * 0.6, 1.6, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
  }
  function bodyPath11(ctx, cx, cy, vol) {
    const rx = 13.5 * (0.97 + vol * 0.14);
    const ry = 9.2 * (0.97 + vol * 0.16);
    ctx.beginPath();
    ctx.ellipse(cx, cy, rx, ry, -0.05, 0, Math.PI * 2);
  }
  function paintShag(ctx, cx, cy, vol, col) {
    if (vol < 0.3) return;
    const fr = (vol - 0.3) / 0.7;
    const rx = 13.5 * (0.97 + vol * 0.14);
    const ry = 9.2 * (0.97 + vol * 0.16);
    ctx.fillStyle = rgb54(col, 0.9);
    for (let i = 0; i < 14; i++) {
      const a = Math.PI * 0.12 + i / 13 * Math.PI * 1;
      const lx = cx + Math.cos(a) * rx * 1.02;
      const ly = cy + Math.sin(a) * ry * 1.02;
      const drop = (1.4 + 2.6 * fr) * (i % 2 ? 1 : 0.78);
      ctx.beginPath();
      ctx.moveTo(lx - 1.2, ly - 0.6);
      ctx.quadraticCurveTo(lx, ly + drop, lx + 1.2, ly - 0.6);
      ctx.closePath();
      ctx.fill();
    }
  }
  function paintMammoth(ctx, p, bob, trunkSway = 0, earFlick = 0) {
    const bx = 3;
    const by = 1 - bob;
    const vol = p.coatVolume;
    ctx.save();
    ctx.globalAlpha = 0.92;
    paintLeg19(ctx, p, bx + 9, by + 6, 19, vol);
    paintLeg19(ctx, p, bx - 3, by + 6.5, 19.2, vol);
    ctx.restore();
    paintLeg19(ctx, p, bx + 6, by + 7, 19.8, vol);
    paintLeg19(ctx, p, bx - 8, by + 7, 20, vol);
    ctx.strokeStyle = rgb54(p.coatShadow);
    ctx.lineWidth = 2.2;
    ctx.lineCap = "round";
    ctx.beginPath();
    ctx.moveTo(bx + 12.5, by + 1);
    ctx.quadraticCurveTo(bx + 14.5, by + 5, bx + 13.5, by + 9);
    ctx.stroke();
    ctx.fillStyle = rgb54(p.darkPart, 0.9);
    ctx.beginPath();
    ctx.arc(bx + 13.5, by + 9.6, 1.3, 0, Math.PI * 2);
    ctx.fill();
    ctx.lineCap = "butt";
    paintShag(ctx, bx, by, vol, p.coatShadow);
    ctx.fillStyle = rgb54(p.outline);
    ctx.save();
    ctx.translate(bx, by);
    ctx.scale(1.09, 1.12);
    ctx.translate(-bx, -by);
    bodyPath11(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.fillStyle = rgb54(p.coatShadow);
    bodyPath11(ctx, bx, by, vol);
    ctx.fill();
    ctx.fillStyle = rgb54(p.coatMid);
    ctx.save();
    ctx.translate(-0.5, -0.5);
    bodyPath11(ctx, bx, by, vol);
    ctx.fill();
    ctx.restore();
    ctx.save();
    ctx.fillStyle = rgb54(p.coatLight);
    ctx.translate(-1.8, -2);
    bodyPath11(ctx, bx, by, vol * 0.7);
    ctx.fill();
    ctx.restore();
    {
      ctx.save();
      bodyPath11(ctx, bx, by, vol);
      ctx.clip();
      ctx.strokeStyle = rgb54(p.coatShadow, 0.55 + 0.3 * vol);
      ctx.lineWidth = 1;
      ctx.lineCap = "round";
      const n = 5 + Math.round(vol * 6);
      for (let i = 0; i < n; i++) {
        const sx = bx - 11 + i / Math.max(1, n - 1) * 22;
        ctx.beginPath();
        ctx.moveTo(sx, by - 7);
        ctx.quadraticCurveTo(sx - 1.2, by, sx - 0.4, by + 8);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
      ctx.restore();
    }
    if (p.gloss > 0.02) {
      ctx.save();
      bodyPath11(ctx, bx, by, vol);
      ctx.clip();
      ctx.fillStyle = rgb54([255, 255, 255], 0.1 + 0.16 * p.gloss);
      ctx.beginPath();
      ctx.ellipse(bx - 2, by - 5.2, 10, 2.6, -0.1, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    if (p.shedAmt > 1e-3) {
      ctx.fillStyle = rgb54(p.coatLight, 0.85 * p.shedAmt);
      const tufts = [
        [bx - 8, by - 7, 0.3],
        [bx - 1, by - 8.4, -0.2],
        [bx + 7, by - 6.6, 0.5],
        [bx + 11, by - 1, 0.1]
      ];
      for (const [tx, ty, rot] of tufts) {
        ctx.save();
        ctx.translate(tx, ty);
        ctx.rotate(rot);
        ctx.beginPath();
        ctx.ellipse(-1, 0, 1.6, 1.1, 0, 0, Math.PI * 2);
        ctx.ellipse(1.2, -0.4, 1.3, 0.9, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
      }
    }
    if (p.backSnowAmt > 1e-3) {
      ctx.fillStyle = rgb54([248, 252, 255], 0.92 * p.backSnowAmt);
      ctx.beginPath();
      ctx.ellipse(bx - 1, by - 7.6, 10 * (0.9 + vol * 0.2), 3.2, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb54([255, 255, 255], 0.85 * p.backSnowAmt);
      for (const [dx, dy] of [[-6, -8.4], [0, -9], [6, -8]]) {
        ctx.beginPath();
        ctx.arc(bx + dx, by + dy, 1.4 + vol * 0.4, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    if (p.frostAmt > 1e-3) {
      ctx.fillStyle = rgb54([255, 255, 255], 0.72 * p.frostAmt);
      const pts = [
        [-8, 0],
        [-2, 4],
        [4, -2],
        [9, 2],
        [0, -4],
        [-5, -6],
        [7, 4],
        [12, -1]
      ];
      for (const [fx, fy] of pts) {
        ctx.beginPath();
        ctx.arc(bx + fx, by + fy, 0.7, 0, Math.PI * 2);
        ctx.fill();
      }
    }
    const hx = bx - 13;
    const hy = by - 4;
    ctx.fillStyle = rgb54(p.outline);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 7.6, 8.8, -0.05, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.coatShadow);
    ctx.beginPath();
    ctx.ellipse(hx, hy, 6.8, 8, -0.05, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.coatMid);
    ctx.beginPath();
    ctx.ellipse(hx - 0.5, hy - 0.6, 6.2, 7.4, -0.05, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.coatLight);
    ctx.beginPath();
    ctx.ellipse(hx - 1.8, hy - 2.6, 4, 4.8, -0.05, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54(p.coatMid);
    ctx.beginPath();
    ctx.moveTo(hx - 3.4, hy - 6.4);
    ctx.quadraticCurveTo(hx - 1, hy - 10.4, hx + 1.4, hy - 6);
    ctx.quadraticCurveTo(hx - 1, hy - 7.2, hx - 3.4, hy - 6.4);
    ctx.closePath();
    ctx.fill();
    ctx.strokeStyle = rgb54(p.coatShadow, 0.8);
    ctx.lineWidth = 0.9;
    ctx.lineCap = "round";
    for (const ox of [-2.4, -0.6, 1.2]) {
      ctx.beginPath();
      ctx.moveTo(hx + ox, hy - 6.6);
      ctx.lineTo(hx + ox - 0.4, hy - 9 + Math.abs(ox) * 0.5);
      ctx.stroke();
    }
    ctx.lineCap = "butt";
    {
      ctx.save();
      ctx.translate(hx + 4.2, hy - 0.4);
      ctx.rotate(0.25 + earFlick * 0.4);
      ctx.fillStyle = rgb54(p.outline);
      ctx.beginPath();
      ctx.ellipse(0, 0, 2.4, 3, 0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb54(p.coatShadow);
      ctx.beginPath();
      ctx.ellipse(-0.2, 0, 1.8, 2.4, 0.2, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
    }
    ctx.fillStyle = rgb54(p.darkPart);
    ctx.beginPath();
    ctx.ellipse(hx - 2.4, hy - 1.2, 1, 1.2, 0.1, 0, Math.PI * 2);
    ctx.fill();
    ctx.fillStyle = rgb54([255, 255, 255], 0.5);
    ctx.beginPath();
    ctx.arc(hx - 2.7, hy - 1.6, 0.4, 0, Math.PI * 2);
    ctx.fill();
    const drawTusk = (rootX, rootY, spread, dim) => {
      ctx.strokeStyle = rgb54(p.outline, dim);
      ctx.lineWidth = 3.6;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(rootX, rootY);
      ctx.quadraticCurveTo(rootX - 3 - spread, rootY + 9, rootX - 1 - spread, rootY + 15);
      ctx.quadraticCurveTo(rootX + 1 - spread, rootY + 19, rootX + 5 - spread * 0.5, rootY + 18);
      ctx.stroke();
      ctx.strokeStyle = rgb54(p.tuskShade, dim);
      ctx.lineWidth = 2.6;
      ctx.beginPath();
      ctx.moveTo(rootX, rootY);
      ctx.quadraticCurveTo(rootX - 3 - spread, rootY + 9, rootX - 1 - spread, rootY + 15);
      ctx.quadraticCurveTo(rootX + 1 - spread, rootY + 19, rootX + 5 - spread * 0.5, rootY + 18);
      ctx.stroke();
      ctx.strokeStyle = rgb54(p.tuskLight, dim);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.moveTo(rootX - 0.6, rootY + 1);
      ctx.quadraticCurveTo(rootX - 3.6 - spread, rootY + 9, rootX - 1.6 - spread, rootY + 14.5);
      ctx.stroke();
    };
    drawTusk(hx + 3.2, hy + 4.4, -1.6, 0.85);
    drawTusk(hx - 1.4, hy + 4.8, 2, 1);
    {
      const t0x = hx - 0.4;
      const t0y = hy + 4.2;
      const tipX = hx - 6 + trunkSway;
      const tipY = hy + 16 + trunkSway * 0.4;
      const midX = hx - 5 + trunkSway * 0.6;
      const midY = hy + 11;
      ctx.strokeStyle = rgb54(p.outline);
      ctx.lineWidth = 5.2;
      ctx.lineCap = "round";
      ctx.beginPath();
      ctx.moveTo(t0x, t0y);
      ctx.quadraticCurveTo(midX, midY, tipX, tipY);
      ctx.quadraticCurveTo(tipX + 2.6, tipY + 2.6, tipX + 3.8 + trunkSway * 0.5, tipY - 0.4);
      ctx.stroke();
      ctx.strokeStyle = rgb54(p.trunkShade);
      ctx.lineWidth = 4;
      ctx.beginPath();
      ctx.moveTo(t0x, t0y);
      ctx.quadraticCurveTo(midX, midY, tipX, tipY);
      ctx.quadraticCurveTo(tipX + 2.6, tipY + 2.6, tipX + 3.8 + trunkSway * 0.5, tipY - 0.4);
      ctx.stroke();
      ctx.strokeStyle = rgb54(p.coatLight, 0.85);
      ctx.lineWidth = 1.8;
      ctx.beginPath();
      ctx.moveTo(t0x - 1.2, t0y + 1);
      ctx.quadraticCurveTo(midX - 1.6, midY, tipX - 1, tipY - 1);
      ctx.stroke();
      ctx.strokeStyle = rgb54(p.outline, 0.5);
      ctx.lineWidth = 0.9;
      for (let i = 1; i <= 3; i++) {
        const tt = i / 4;
        const rxp = lerp59(t0x, tipX, tt) + tt * (1 - tt) * (midX - (t0x + tipX) / 2) * 2;
        const ryp = lerp59(t0y, tipY, tt) + tt * (1 - tt) * (midY - (t0y + tipY) / 2) * 2;
        ctx.beginPath();
        ctx.moveTo(rxp - 2, ryp);
        ctx.lineTo(rxp + 2, ryp);
        ctx.stroke();
      }
      ctx.lineCap = "butt";
      if (p.breathFogAmt > 1e-3) {
        ctx.fillStyle = rgb54([235, 244, 255], 0.4 * p.breathFogAmt);
        ctx.beginPath();
        ctx.ellipse(tipX + 5.4 + trunkSway * 0.5, tipY - 1.4, 3, 2, 0.2, 0, Math.PI * 2);
        ctx.fill();
      }
    }
  }
  function paintLightWash20(ctx, p) {
    if (p.lightWashAmt <= 1e-3) return;
    ctx.save();
    ctx.globalCompositeOperation = "soft-light";
    ctx.fillStyle = rgb54(p.lightWash, p.lightWashAmt);
    ctx.beginPath();
    ctx.ellipse(0, 4, 26, 26, 0, 0, Math.PI * 2);
    ctx.fill();
    ctx.restore();
  }
  function paint54(ctx, pIn, bob, trunkSway = 0, earFlick = 0) {
    const p = clampP43(pIn);
    ctx.save();
    try {
      ctx.lineJoin = "round";
      paintPad21(ctx, p);
      paintMammoth(ctx, p, bob, trunkSway, earFlick);
      paintLightWash20(ctx, p);
    } finally {
      ctx.globalAlpha = 1;
      ctx.globalCompositeOperation = "source-over";
      ctx.restore();
    }
  }
  function draw44(season) {
    return (ctx) => paint54(ctx, SP54[season], 0);
  }
  function anim44(season) {
    return (ctx, t) => {
      const p = clampP43(SP54[season]);
      const bob = bobAt54(t);
      const trunkSway = Math.sin(t * 0.9) * 1.5;
      const loop = t % 5 / 5;
      const flickGate = Math.max(0, Math.sin(loop * Math.PI * 2)) ** 6;
      const earFlick = Math.sin(t * 9) * flickGate;
      paint54(ctx, SP54[season], bob, trunkSway, earFlick);
      ctx.save();
      try {
        const bx = 3;
        const by = 1 - bob;
        const hx = bx - 13;
        const hy = by - 4;
        if (season === "Spring") {
          const prog = (t / 3.6 % 1 + 1) % 1;
          const tx = bx - 2 + prog * 6 + Math.sin(prog * Math.PI * 2) * 2;
          const ty = by - 6 + prog * 18;
          ctx.save();
          ctx.translate(tx, ty);
          ctx.rotate(prog * 3);
          ctx.fillStyle = rgb54(p.coatLight, (1 - prog) * 0.8);
          ctx.beginPath();
          ctx.ellipse(-1, 0, 1.6, 1, 0, 0, Math.PI * 2);
          ctx.ellipse(1.1, -0.3, 1.2, 0.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
          const g = 0.2 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.4));
          ctx.fillStyle = rgb54([255, 255, 255], g);
          ctx.beginPath();
          ctx.arc(-9, 18.4, 1.1 + g, 0, Math.PI * 2);
          ctx.fill();
          ctx.beginPath();
          ctx.arc(10, 19.6, 0.9 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.5 + 0.5 * Math.sin(t * 1);
          const sx = bx - 10 + s * 20;
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb54([255, 255, 255], 0.32);
          ctx.beginPath();
          ctx.ellipse(sx, by - 2, 3, 6, 0.28, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else if (season === "Autumn") {
          const a = Math.sin(t * 1.3) * 0.5;
          const dx = Math.sin(t * 0.7) * 1.2;
          ctx.save();
          ctx.translate(11 + dx, 20.5);
          ctx.rotate(0.7 + a);
          ctx.fillStyle = rgb54([212, 150, 52], p.fallenLeafAmt);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          const trunkSway2 = Math.sin(t * 0.9) * 1.5;
          const tipX = hx - 6 + trunkSway2;
          const tipY = hy + 16 + trunkSway2 * 0.4;
          const breathe = 0.5 + 0.5 * Math.sin(t * 1.2);
          const reach = 4 + breathe * 3.5;
          ctx.fillStyle = rgb54([235, 244, 255], (0.18 + 0.32 * breathe) * p.breathFogAmt);
          ctx.beginPath();
          ctx.ellipse(tipX + 5 + reach * 0.4, tipY - 1.4, 2.8 + breathe * 1.8, 2 + breathe * 1.2, 0.2, 0, Math.PI * 2);
          ctx.fill();
          const flakes = [[8, 0], [-6, 0.45]];
          ctx.fillStyle = rgb54([255, 255, 255], 0.85);
          for (const [fx, ph] of flakes) {
            const prog = ((t / 3.4 + ph) % 1 + 1) % 1;
            const fy = -20 + prog * 38;
            const fxx = fx + Math.sin(prog * Math.PI * 2 + ph * 6) * 4;
            ctx.globalAlpha = 0.5 + 0.4 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(fxx, fy, 1.1, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.globalAlpha = 1;
          const sheen2 = 0.5 + 0.5 * Math.sin(t * 0.8);
          ctx.save();
          ctx.globalCompositeOperation = "soft-light";
          ctx.fillStyle = rgb54([206, 224, 255], 0.12 + sheen2 * 0.12);
          ctx.beginPath();
          ctx.ellipse(bx, by, 14, 9, -0.05, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.globalCompositeOperation = "source-over";
        ctx.restore();
      }
    };
  }
  function makeTransition51(fromIdx) {
    const from = SEASON_NAMES[fromIdx];
    const to = SEASON_NAMES[fromIdx + 1];
    return (ctx, pp) => {
      const k = smoother54(clamp0155(pp));
      paint54(ctx, lerpP54(SP54[from], SP54[to], k), 0);
    };
  }
  var springToSummer48 = makeTransition51(0);
  var summerToAutumn48 = makeTransition51(1);
  var autumnToWinter48 = makeTransition51(2);
  var VARIANTS56 = {
    Spring: { draw: draw44("Spring"), anim: anim44("Spring") },
    Summer: { draw: draw44("Summer"), anim: anim44("Summer") },
    Autumn: { draw: draw44("Autumn"), anim: anim44("Autumn") },
    Winter: { draw: draw44("Winter"), anim: anim44("Winter") }
  };
  var TRANSITIONS56 = {
    0: springToSummer48,
    1: summerToAutumn48,
    2: autumnToWinter48
  };

  // src/textures/seasonal/fish/clam.ts
  function clamp0156(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother55 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp60(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB48(a, b, t) {
    return [lerp60(a[0], b[0], t), lerp60(a[1], b[1], t), lerp60(a[2], b[2], t)];
  }
  function rgb55(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba23(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0156(a)})`;
  }
  function lerpP55(a, b, t) {
    return {
      shellLight: lerpRGB48(a.shellLight, b.shellLight, t),
      shellMid: lerpRGB48(a.shellMid, b.shellMid, t),
      shellDark: lerpRGB48(a.shellDark, b.shellDark, t),
      bodyCream: lerpRGB48(a.bodyCream, b.bodyCream, t),
      water: lerpRGB48(a.water, b.water, t),
      waterDeep: lerpRGB48(a.waterDeep, b.waterDeep, t),
      ice: lerpRGB48(a.ice, b.ice, t),
      outline: lerpRGB48(a.outline, b.outline, t),
      light: lerpRGB48(a.light, b.light, t),
      lightAmt: lerp60(a.lightAmt, b.lightAmt, t),
      openAmt: lerp60(a.openAmt, b.openAmt, t),
      gloss: lerp60(a.gloss, b.gloss, t),
      frostAmt: lerp60(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp60(a.snowCapAmt, b.snowCapAmt, t),
      iceAmt: lerp60(a.iceAmt, b.iceAmt, t),
      blossomAmt: lerp60(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp60(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP44(p) {
    return {
      ...p,
      lightAmt: clamp0156(p.lightAmt),
      openAmt: clamp0156(p.openAmt),
      gloss: clamp0156(p.gloss),
      frostAmt: clamp0156(p.frostAmt),
      snowCapAmt: clamp0156(p.snowCapAmt),
      iceAmt: clamp0156(p.iceAmt),
      blossomAmt: clamp0156(p.blossomAmt),
      fallenLeafAmt: clamp0156(p.fallenLeafAmt)
    };
  }
  var TAN_LIGHT = [230, 206, 162];
  var TAN_MID = [205, 174, 124];
  var TAN_DARK = [156, 122, 78];
  var CREAM = [244, 232, 214];
  var SP55 = {
    // Spring — fresh bright-blue dewy water + a tiny floating blossom petal.
    Spring: {
      shellLight: TAN_LIGHT,
      shellMid: TAN_MID,
      shellDark: TAN_DARK,
      bodyCream: CREAM,
      water: [120, 196, 230],
      waterDeep: [66, 138, 190],
      ice: [222, 238, 248],
      outline: [78, 60, 40],
      light: [232, 244, 250],
      lightAmt: 0.16,
      openAmt: 0.6,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — bright saturated blue water (PEAK), warm light, strong gloss.
    Summer: {
      shellLight: TAN_LIGHT,
      shellMid: TAN_MID,
      shellDark: TAN_DARK,
      bodyCream: CREAM,
      water: [70, 178, 232],
      waterDeep: [34, 118, 188],
      ice: [222, 238, 248],
      outline: [78, 58, 38],
      light: [255, 242, 208],
      lightAmt: 0.18,
      openAmt: 0.6,
      gloss: 0.95,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — duller olive-tinged water + a floating fallen leaf, low amber light.
    Autumn: {
      shellLight: TAN_LIGHT,
      shellMid: TAN_MID,
      shellDark: TAN_DARK,
      bodyCream: CREAM,
      water: [120, 152, 130],
      waterDeep: [78, 108, 92],
      ice: [222, 238, 248],
      outline: [74, 54, 34],
      light: [248, 210, 150],
      lightAmt: 0.2,
      openAmt: 0.58,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — water FROZEN to pale blue-white ice + frost sparkle; a little
    // snow/frost on the still-clearly-tan shell; cool light.
    Winter: {
      shellLight: TAN_LIGHT,
      shellMid: TAN_MID,
      shellDark: TAN_DARK,
      bodyCream: CREAM,
      water: [182, 210, 226],
      waterDeep: [136, 170, 196],
      ice: [226, 240, 250],
      outline: [70, 62, 60],
      light: [206, 226, 252],
      lightAmt: 0.3,
      openAmt: 0.5,
      gloss: 0.3,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      iceAmt: 0.95,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var HINGE_X = 4;
  var HINGE_Y = 14;
  var SHELL_R = 14.5;
  var RIB_COUNT = 7;
  function valvePath(ctx, tilt, span) {
    ctx.beginPath();
    ctx.moveTo(0, 0);
    const steps = 14;
    for (let i = 0; i <= steps; i++) {
      const a = tilt - span + 2 * span * i / steps;
      const flute = 1 + 0.07 * Math.cos(i / steps * RIB_COUNT * Math.PI);
      const r = SHELL_R * flute;
      ctx.lineTo(Math.cos(a) * r, Math.sin(a) * r);
    }
    ctx.closePath();
  }
  function valveRibs(ctx, tilt, span, dark, light) {
    for (let i = 0; i <= RIB_COUNT; i++) {
      const a = tilt - span + 2 * span * i / RIB_COUNT;
      ctx.strokeStyle = rgba23(dark, 0.8);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.moveTo(Math.cos(a) * 2.5, Math.sin(a) * 2.5);
      ctx.lineTo(Math.cos(a) * (SHELL_R + 0.5), Math.sin(a) * (SHELL_R + 0.5));
      ctx.stroke();
      const a2 = a + span / RIB_COUNT * 0.42;
      ctx.strokeStyle = rgba23(light, 0.5);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(Math.cos(a2) * 3.5, Math.sin(a2) * 3.5);
      ctx.lineTo(Math.cos(a2) * (SHELL_R - 1), Math.sin(a2) * (SHELL_R - 1));
      ctx.stroke();
    }
  }
  function paint55(ctx, raw, bob) {
    const p = clampP44(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba23(p.waterDeep, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb55(p.waterDeep);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      const waterGrad = ctx.createLinearGradient(0, 14, 0, 24);
      waterGrad.addColorStop(0, rgb55(p.water));
      waterGrad.addColorStop(1, rgb55(p.waterDeep));
      ctx.fillStyle = waterGrad;
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.clip();
      ctx.strokeStyle = rgba23([255, 255, 255], 0.22);
      ctx.lineWidth = 1.1;
      [[-9, 17.6, 6], [6, 20, 5], [-2, 21, 4]].forEach(([gx, gy, gw]) => {
        ctx.beginPath();
        ctx.moveTo(gx - gw / 2, gy);
        ctx.lineTo(gx + gw / 2, gy);
        ctx.stroke();
      });
      ctx.restore();
      if (p.iceAmt > 0.01) {
        const a = p.iceAmt;
        ctx.fillStyle = rgba23(p.ice, 0.9 * a);
        ctx.beginPath();
        ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba23(p.waterDeep, 0.5 * a);
        ctx.lineWidth = 1.2;
        ctx.beginPath();
        ctx.ellipse(0, 19, 17.6, 5, 0, 0, Math.PI * 2);
        ctx.stroke();
        ctx.strokeStyle = rgba23([255, 255, 255], 0.5 * a);
        ctx.lineWidth = 0.8;
        [[-12, 18, -3, 20], [-2, 17, 9, 20.5], [4, 21, 13, 18.5]].forEach(([x1, y1, x2, y2]) => {
          ctx.beginPath();
          ctx.moveTo(x1, y1);
          ctx.lineTo(x2, y2);
          ctx.stroke();
        });
        ctx.fillStyle = rgba23([255, 255, 255], 0.85 * a);
        [[-10, 18], [5, 17.6], [11, 19.2], [-4, 20.4], [1, 18.2]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.5, 0.4], [12, 17.8, -0.6]];
        spots.forEach(([bx, by, rot]) => {
          ctx.save();
          ctx.translate(bx, by);
          ctx.rotate(rot);
          ctx.fillStyle = rgba23([255, 232, 246], 0.95 * a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.5, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgba23([255, 250, 252], 0.8 * a);
          ctx.beginPath();
          ctx.ellipse(-0.6, -0.3, 1.4, 0.9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.6, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba23(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba23([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      const ox = -2;
      const oy = -1 + bob;
      ctx.fillStyle = rgba23(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(ox + 2, HINGE_Y + bob + 2.5, 13, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.translate(ox + HINGE_X, oy + HINGE_Y);
      const baseTilt = Math.PI * 0.92;
      const span = 0.66;
      const lowerTilt = baseTilt + 0.34;
      const upperTilt = baseTilt - 0.34 - p.openAmt * 0.22;
      ctx.save();
      valvePath(ctx, lowerTilt, span);
      ctx.fillStyle = rgb55(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      valvePath(ctx, lowerTilt, span * 0.97);
      ctx.clip();
      ctx.fillStyle = rgb55(p.shellDark);
      ctx.fillRect(-SHELL_R - 4, -SHELL_R - 4, (SHELL_R + 4) * 2, (SHELL_R + 4) * 2);
      const lg = ctx.createLinearGradient(-SHELL_R, -SHELL_R, SHELL_R, SHELL_R);
      lg.addColorStop(0, rgb55(p.shellMid));
      lg.addColorStop(1, rgb55(p.shellDark));
      ctx.fillStyle = lg;
      ctx.fillRect(-SHELL_R - 4, -SHELL_R - 4, (SHELL_R + 4) * 2, (SHELL_R + 4) * 2);
      valveRibs(ctx, lowerTilt, span, p.shellDark, p.shellMid);
      ctx.restore();
      ctx.save();
      ctx.fillStyle = rgb55(p.bodyCream);
      ctx.beginPath();
      const bodyA = baseTilt;
      const bdx = Math.cos(bodyA);
      const bdy = Math.sin(bodyA);
      ctx.ellipse(bdx * 6.5, bdy * 6.5, 5.5, 2.6, bodyA + Math.PI / 2, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba23(p.shellDark, 0.4);
      ctx.lineWidth = 0.9;
      ctx.beginPath();
      ctx.moveTo(bdx * 3, bdy * 3);
      ctx.lineTo(bdx * 10, bdy * 10);
      ctx.stroke();
      ctx.restore();
      ctx.save();
      valvePath(ctx, upperTilt, span);
      ctx.fillStyle = rgb55(p.outline);
      ctx.fill();
      ctx.restore();
      ctx.save();
      valvePath(ctx, upperTilt, span * 0.97);
      ctx.clip();
      ctx.fillStyle = rgb55(p.shellMid);
      ctx.fillRect(-SHELL_R - 4, -SHELL_R - 4, (SHELL_R + 4) * 2, (SHELL_R + 4) * 2);
      const ug = ctx.createLinearGradient(-SHELL_R, -SHELL_R, SHELL_R * 0.4, SHELL_R);
      ug.addColorStop(0, rgb55(p.shellLight));
      ug.addColorStop(0.55, rgb55(p.shellMid));
      ug.addColorStop(1, rgb55(p.shellDark));
      ctx.fillStyle = ug;
      ctx.globalAlpha = 0.95;
      ctx.fillRect(-SHELL_R - 4, -SHELL_R - 4, (SHELL_R + 4) * 2, (SHELL_R + 4) * 2);
      ctx.globalAlpha = 1;
      valveRibs(ctx, upperTilt, span, p.shellDark, p.shellLight);
      if (p.gloss > 0.02) {
        const ga = baseTilt - 0.2;
        ctx.fillStyle = rgba23([255, 255, 255], 0.12 + 0.5 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(Math.cos(ga) * 7, Math.sin(ga) * 7, 4.4, 1.7, ga + Math.PI / 2, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba23([210, 230, 250], 0.22 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(Math.cos(baseTilt) * 8, Math.sin(baseTilt) * 8, SHELL_R * 0.7, 3.4, baseTilt + Math.PI / 2, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba23([235, 246, 255], 0.7 * p.frostAmt);
        [4, 7, 10, 13].forEach((rr, i) => {
          const fa = baseTilt - span + i / 3 * span * 2;
          ctx.beginPath();
          ctx.arc(Math.cos(fa) * rr, Math.sin(fa) * rr, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba23([246, 251, 255], 0.92 * a);
        ctx.beginPath();
        const c1 = upperTilt - span * 0.8;
        const c2 = upperTilt + span * 0.8;
        ctx.moveTo(Math.cos(c1) * (SHELL_R - 1), Math.sin(c1) * (SHELL_R - 1));
        ctx.quadraticCurveTo(
          Math.cos(upperTilt) * (SHELL_R + 2.5),
          Math.sin(upperTilt) * (SHELL_R + 2.5),
          Math.cos(c2) * (SHELL_R - 1),
          Math.sin(c2) * (SHELL_R - 1)
        );
        ctx.quadraticCurveTo(
          Math.cos(upperTilt) * (SHELL_R - 4),
          Math.sin(upperTilt) * (SHELL_R - 4),
          Math.cos(c1) * (SHELL_R - 1),
          Math.sin(c1) * (SHELL_R - 1)
        );
        ctx.closePath();
        ctx.fill();
      }
      ctx.restore();
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lgrad = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lgrad.addColorStop(0, rgba23(p.light, p.lightAmt));
        lgrad.addColorStop(1, rgba23(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lgrad;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt55(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function breatheAt(t, amp = 0.12, w = 1) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw45(season) {
    return (ctx) => paint55(ctx, SP55[season], 0);
  }
  function anim45(season) {
    return (ctx, t) => {
      const bob = bobAt55(t);
      const sp = SP55[season];
      const breathing = { ...sp, openAmt: clamp0156(sp.openAmt + breatheAt(t)) };
      paint55(ctx, breathing, bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const rippleY = 19 + Math.sin(t * 1.6) * 0.4;
          ctx.strokeStyle = `rgba(255,255,255,${0.16 + 0.12 * (0.5 + 0.5 * Math.sin(t * 1.6))})`;
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.ellipse(0, rippleY, 12 + Math.sin(t * 1.1) * 1.5, 3.2, 0, Math.PI * 0.15, Math.PI * 0.85);
          ctx.stroke();
          const g = 0.25 + 0.3 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(-4, -2 + bob, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const rippleY = 19 + Math.sin(t * 1.8) * 0.5;
          ctx.strokeStyle = `rgba(255,255,255,${0.2 + 0.14 * (0.5 + 0.5 * Math.sin(t * 1.8))})`;
          ctx.lineWidth = 1.1;
          ctx.beginPath();
          ctx.ellipse(0, rippleY, 13 + Math.sin(t * 1.3) * 1.6, 3.4, 0, Math.PI * 0.1, Math.PI * 0.9);
          ctx.stroke();
          const prog = t * 0.5 % 1;
          const ga = Math.PI * 0.92 - 0.4 + prog * 0.8;
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(-2 + Math.cos(ga) * 8, -1 + bob + Math.sin(ga) * 8, 1.7, 1.1, ga, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const rippleY = 19 + Math.sin(t * 1.3) * 0.35;
          ctx.strokeStyle = `rgba(255,255,255,${0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 1.3))})`;
          ctx.lineWidth = 1;
          ctx.beginPath();
          ctx.ellipse(0, rippleY, 12, 3, 0, Math.PI * 0.15, Math.PI * 0.85);
          ctx.stroke();
          const dx = Math.sin(t * 0.6) * 2.5;
          const dy = 19.4 + Math.sin(t * 0.9) * 0.5;
          ctx.save();
          ctx.translate(-12 + dx, dy);
          ctx.rotate(-0.5 + Math.sin(t * 0.5) * 0.15);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        } else {
          ctx.globalAlpha = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 19, 10, 3, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
          const seeds = [
            [-9, 0, 0.9],
            [7, 0.5, 0.8],
            [-2, 0.25, 0.85]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3.2 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const fdx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(fdx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition52(fromIdx) {
    const from = SP55[SEASON_NAMES[fromIdx]];
    const to = SP55[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother55(clamp0156(pp));
      paint55(ctx, lerpP55(from, to, k), 0);
    };
  }
  var springToSummer49 = makeTransition52(0);
  var summerToAutumn49 = makeTransition52(1);
  var autumnToWinter49 = makeTransition52(2);
  var VARIANTS57 = {
    Spring: { draw: draw45("Spring"), anim: anim45("Spring") },
    Summer: { draw: draw45("Summer"), anim: anim45("Summer") },
    Autumn: { draw: draw45("Autumn"), anim: anim45("Autumn") },
    Winter: { draw: draw45("Winter"), anim: anim45("Winter") }
  };
  var TRANSITIONS57 = {
    0: springToSummer49,
    1: summerToAutumn49,
    2: autumnToWinter49
  };

  // src/textures/seasonal/fish/oyster.ts
  function clamp0157(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother56 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp61(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB49(a, b, t) {
    return [lerp61(a[0], b[0], t), lerp61(a[1], b[1], t), lerp61(a[2], b[2], t)];
  }
  function rgb56(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba24(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0157(a)})`;
  }
  function lerpP56(a, b, t) {
    return {
      shellLight: lerpRGB49(a.shellLight, b.shellLight, t),
      shellMid: lerpRGB49(a.shellMid, b.shellMid, t),
      shellDark: lerpRGB49(a.shellDark, b.shellDark, t),
      mantle: lerpRGB49(a.mantle, b.mantle, t),
      pearlLight: lerpRGB49(a.pearlLight, b.pearlLight, t),
      pearlMid: lerpRGB49(a.pearlMid, b.pearlMid, t),
      pearlDark: lerpRGB49(a.pearlDark, b.pearlDark, t),
      water: lerpRGB49(a.water, b.water, t),
      waterDark: lerpRGB49(a.waterDark, b.waterDark, t),
      soil: lerpRGB49(a.soil, b.soil, t),
      outline: lerpRGB49(a.outline, b.outline, t),
      light: lerpRGB49(a.light, b.light, t),
      lightAmt: lerp61(a.lightAmt, b.lightAmt, t),
      waterSat: lerp61(a.waterSat, b.waterSat, t),
      pearlGloss: lerp61(a.pearlGloss, b.pearlGloss, t),
      frostAmt: lerp61(a.frostAmt, b.frostAmt, t),
      iceAmt: lerp61(a.iceAmt, b.iceAmt, t),
      padSnowAmt: lerp61(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp61(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp61(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP45(p) {
    return {
      ...p,
      lightAmt: clamp0157(p.lightAmt),
      waterSat: clamp0157(p.waterSat),
      pearlGloss: clamp0157(p.pearlGloss),
      frostAmt: clamp0157(p.frostAmt),
      iceAmt: clamp0157(p.iceAmt),
      padSnowAmt: clamp0157(p.padSnowAmt),
      blossomAmt: clamp0157(p.blossomAmt),
      fallenLeafAmt: clamp0157(p.fallenLeafAmt)
    };
  }
  var SHELL_LIGHT = [196, 200, 206];
  var SHELL_MID = [148, 152, 160];
  var SHELL_DARK = [96, 100, 110];
  var PEARL_LIGHT = [250, 248, 244];
  var PEARL_MID = [226, 224, 222];
  var PEARL_DARK = [184, 184, 190];
  var SP56 = {
    // Spring — grey shell + pearl; fresh bright-blue water + a tiny blossom petal.
    Spring: {
      shellLight: SHELL_LIGHT,
      shellMid: SHELL_MID,
      shellDark: SHELL_DARK,
      mantle: [232, 206, 200],
      pearlLight: PEARL_LIGHT,
      pearlMid: PEARL_MID,
      pearlDark: PEARL_DARK,
      water: [120, 196, 224],
      waterDark: [72, 144, 184],
      soil: [96, 150, 168],
      outline: [52, 58, 66],
      light: [232, 244, 248],
      lightAmt: 0.16,
      waterSat: 0.7,
      pearlGloss: 0.6,
      frostAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — grey shell; bright saturated blue water (PEAK); pearl glints
    // brightest; warm light.
    Summer: {
      shellLight: SHELL_LIGHT,
      shellMid: SHELL_MID,
      shellDark: SHELL_DARK,
      mantle: [236, 200, 196],
      pearlLight: PEARL_LIGHT,
      pearlMid: PEARL_MID,
      pearlDark: PEARL_DARK,
      water: [60, 168, 224],
      waterDark: [28, 110, 184],
      soil: [70, 140, 176],
      outline: [44, 52, 62],
      light: [255, 244, 214],
      lightAmt: 0.18,
      waterSat: 1,
      pearlGloss: 1,
      frostAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — grey shell; duller olive-tinged water + a floating fallen leaf;
    // low amber light.
    Autumn: {
      shellLight: SHELL_LIGHT,
      shellMid: SHELL_MID,
      shellDark: SHELL_DARK,
      mantle: [220, 192, 184],
      pearlLight: [246, 242, 234],
      pearlMid: PEARL_MID,
      pearlDark: PEARL_DARK,
      water: [108, 150, 138],
      waterDark: [70, 104, 96],
      soil: [96, 116, 96],
      outline: [50, 50, 50],
      light: [248, 212, 156],
      lightAmt: 0.2,
      waterSat: 0.5,
      pearlGloss: 0.5,
      frostAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — water FROZEN to pale blue-white ICE + frost sparkle; a little frost
    // on the shell rim; grey shell + pale pearl still clearly visible; cool light.
    Winter: {
      shellLight: SHELL_LIGHT,
      shellMid: [142, 148, 158],
      shellDark: [92, 98, 110],
      mantle: [214, 196, 198],
      pearlLight: PEARL_LIGHT,
      pearlMid: PEARL_MID,
      pearlDark: [186, 190, 198],
      water: [216, 232, 244],
      waterDark: [168, 192, 214],
      soil: [180, 200, 218],
      outline: [60, 64, 76],
      light: [212, 230, 252],
      lightAmt: 0.3,
      waterSat: 0.2,
      pearlGloss: 0.55,
      frostAmt: 0.7,
      iceAmt: 0.92,
      padSnowAmt: 0.85,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var SHELL_CY = 8;
  var SHELL_HALF = 15;
  function bottomValvePath(ctx, bob) {
    const cy = SHELL_CY + bob;
    const h = SHELL_HALF;
    ctx.beginPath();
    ctx.moveTo(-h, cy - 1);
    ctx.quadraticCurveTo(-h * 0.8, cy - 5, -h * 0.42, cy - 5.6);
    ctx.quadraticCurveTo(-h * 0.18, cy - 7, 0, cy - 6.6);
    ctx.quadraticCurveTo(h * 0.2, cy - 7, h * 0.44, cy - 5.6);
    ctx.quadraticCurveTo(h * 0.82, cy - 5, h, cy - 1);
    ctx.quadraticCurveTo(h * 0.9, cy + 6.5, h * 0.34, cy + 7.6);
    ctx.quadraticCurveTo(0, cy + 8.4, -h * 0.34, cy + 7.6);
    ctx.quadraticCurveTo(-h * 0.9, cy + 6.5, -h, cy - 1);
    ctx.closePath();
  }
  function topValvePath(ctx, bob) {
    const cy = SHELL_CY + bob;
    const h = SHELL_HALF;
    ctx.beginPath();
    ctx.moveTo(-h + 1, cy - 2);
    ctx.quadraticCurveTo(-h - 1, cy - 13, -3, cy - 17.5);
    ctx.quadraticCurveTo(h * 0.55, cy - 18.5, h - 1, cy - 12);
    ctx.quadraticCurveTo(h + 1, cy - 7, h - 1, cy - 4);
    ctx.quadraticCurveTo(h * 0.4, cy - 8, 0, cy - 8.2);
    ctx.quadraticCurveTo(-h * 0.5, cy - 8, -h + 1, cy - 2);
    ctx.closePath();
  }
  function craggyRidges(ctx, cy, dark, light) {
    ctx.strokeStyle = rgba24(dark, 0.7);
    ctx.lineWidth = 1.3;
    for (let i = -3; i <= 3; i++) {
      const x0 = i * (SHELL_HALF * 0.26);
      ctx.beginPath();
      ctx.moveTo(x0, cy - 5.2);
      ctx.quadraticCurveTo(x0 * 1.05, cy + 1.5, x0 * 1.1, cy + 6.4);
      ctx.stroke();
    }
    ctx.strokeStyle = rgba24(light, 0.55);
    ctx.lineWidth = 0.9;
    [-SHELL_HALF * 0.5, -SHELL_HALF * 0.08, SHELL_HALF * 0.42].forEach((x0) => {
      ctx.beginPath();
      ctx.moveTo(x0 - 0.6, cy - 4.6);
      ctx.quadraticCurveTo(x0 - 0.4, cy + 1, x0 - 0.2, cy + 5.4);
      ctx.stroke();
    });
  }
  function paint56(ctx, raw, bob) {
    const p = clampP45(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba24(p.waterDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb56(p.waterDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      const surf = lerpRGB49(p.waterDark, p.water, 0.4 + 0.6 * p.waterSat);
      ctx.fillStyle = rgb56(surf);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba24([255, 255, 255], 0.16 * (1 - p.iceAmt) * (0.4 + 0.6 * p.waterSat));
      ctx.beginPath();
      ctx.ellipse(-4, 17.6, 11, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      if (p.iceAmt < 0.5) {
        ctx.strokeStyle = rgba24([255, 255, 255], 0.22 * (1 - p.iceAmt));
        ctx.lineWidth = 0.9;
        [-2.6, 0.4, -1].forEach((ry, i) => {
          ctx.beginPath();
          ctx.moveTo(-12 + i * 2, 18.6 + ry);
          ctx.quadraticCurveTo(0, 17.4 + ry, 12 - i * 2, 18.6 + ry);
          ctx.stroke();
        });
      }
      if (p.iceAmt > 0.02) {
        const a = p.iceAmt;
        ctx.fillStyle = rgba24([232, 244, 252], 0.85 * a);
        ctx.beginPath();
        ctx.ellipse(0, 18.8, 17.6, 5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba24([200, 222, 240], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba24([170, 198, 224], 0.7 * a);
        ctx.lineWidth = 0.8;
        [[-10, 18, -4, 20.5], [3, 17.5, 9, 20], [-2, 21, 4, 18.5]].forEach(([x0, y0, x1, y1]) => {
          ctx.beginPath();
          ctx.moveTo(x0, y0);
          ctx.lineTo(x1, y1);
          ctx.stroke();
        });
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba24([255, 255, 255], 0.85 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.8], [-3, 20.2], [8, 20.4]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.6], [12, 18]];
        spots.forEach(([bx, by], idx) => {
          ctx.save();
          ctx.translate(bx, by);
          ctx.rotate(idx === 0 ? -0.4 : 0.5);
          ctx.fillStyle = rgba24(idx === 0 ? [255, 232, 246] : [255, 248, 252], 0.95 * a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.4, 1.3, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgba24([255, 214, 90], 0.85 * a);
          ctx.beginPath();
          ctx.arc(-0.6, 0, 0.6, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.4, -0.5, [196, 120, 40]],
          [12, 18.8, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba24(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba24([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba24(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(2.5, SHELL_CY + bob + 8.5, 13, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba24(p.soil, 0.45);
      ctx.beginPath();
      ctx.ellipse(0, SHELL_CY + bob + 7.5, 10, 2.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const cy = SHELL_CY + bob;
      topValvePath(ctx, bob);
      ctx.fillStyle = rgb56(p.outline);
      ctx.fill();
      ctx.save();
      topValvePath(ctx, bob);
      ctx.clip();
      const topGrad = ctx.createLinearGradient(-SHELL_HALF, cy - 18, SHELL_HALF, cy - 2);
      topGrad.addColorStop(0, rgb56(p.shellLight));
      topGrad.addColorStop(0.5, rgb56(p.shellMid));
      topGrad.addColorStop(1, rgb56(p.shellDark));
      ctx.fillStyle = topGrad;
      ctx.fillRect(-SHELL_HALF - 3, cy - 20, (SHELL_HALF + 3) * 2, 22);
      ctx.strokeStyle = rgba24(p.shellDark, 0.6);
      ctx.lineWidth = 1.1;
      for (let i = -3; i <= 3; i++) {
        ctx.beginPath();
        ctx.moveTo(-SHELL_HALF + 1, cy - 2);
        ctx.quadraticCurveTo(i * 3, cy - 12, i * 4.5, cy - 16.5);
        ctx.stroke();
      }
      ctx.fillStyle = rgba24(p.shellDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(0, cy - 8.5, SHELL_HALF * 0.7, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      ctx.fillStyle = rgb56(p.mantle);
      ctx.beginPath();
      ctx.ellipse(0, cy - 1.5, SHELL_HALF * 0.72, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba24(p.shellDark, 0.3);
      ctx.beginPath();
      ctx.ellipse(0, cy + 0.4, SHELL_HALF * 0.72, 3.2, 0, 0, Math.PI * 2);
      ctx.fill();
      const pearlX = 0.5;
      const pearlY = cy - 2.6;
      const pearlR = 4.6;
      ctx.fillStyle = rgb56(p.outline);
      ctx.beginPath();
      ctx.arc(pearlX, pearlY, pearlR + 0.7, 0, Math.PI * 2);
      ctx.fill();
      const pGrad = ctx.createRadialGradient(
        pearlX - 1.8,
        pearlY - 1.8,
        0.5,
        pearlX,
        pearlY,
        pearlR
      );
      pGrad.addColorStop(0, rgb56(p.pearlLight));
      pGrad.addColorStop(0.55, rgb56(p.pearlMid));
      pGrad.addColorStop(1, rgb56(p.pearlDark));
      ctx.fillStyle = pGrad;
      ctx.beginPath();
      ctx.arc(pearlX, pearlY, pearlR, 0, Math.PI * 2);
      ctx.fill();
      if (p.pearlGloss > 0.02) {
        ctx.fillStyle = rgba24([255, 255, 255], 0.3 + 0.45 * p.pearlGloss);
        ctx.beginPath();
        ctx.ellipse(pearlX - 1.6, pearlY - 1.8, 1.2, 1.5, -0.4, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba24([244, 240, 250], 0.3 * p.pearlGloss);
        ctx.beginPath();
        ctx.arc(pearlX + 1.4, pearlY + 2, 1.1, 0, Math.PI * 2);
        ctx.fill();
      }
      bottomValvePath(ctx, bob);
      ctx.fillStyle = rgb56(p.outline);
      ctx.fill();
      ctx.save();
      bottomValvePath(ctx, bob);
      ctx.clip();
      ctx.fillStyle = rgb56(p.shellMid);
      ctx.fillRect(-SHELL_HALF - 3, cy - 8, (SHELL_HALF + 3) * 2, 18);
      const cupGrad = ctx.createLinearGradient(-SHELL_HALF, cy - 6, SHELL_HALF, cy + 8);
      cupGrad.addColorStop(0, rgb56(p.shellLight));
      cupGrad.addColorStop(0.5, rgb56(p.shellMid));
      cupGrad.addColorStop(1, rgb56(p.shellDark));
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-1.5, cy + 1.5, SHELL_HALF + 1, 8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.fillStyle = rgba24(p.shellDark, 0.55);
      ctx.beginPath();
      ctx.ellipse(0, cy - 4.4, SHELL_HALF * 0.78, 2.4, 0, 0, Math.PI * 2);
      ctx.fill();
      craggyRidges(ctx, cy, p.shellDark, p.shellLight);
      ctx.restore();
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba24([222, 238, 250], 0.7 * p.frostAmt);
        const speck = [
          [-SHELL_HALF * 0.8, cy - 4],
          [-SHELL_HALF * 0.3, cy - 5.4],
          [SHELL_HALF * 0.3, cy - 5.2],
          [SHELL_HALF * 0.78, cy - 4],
          [-4, cy - 16],
          [4, cy - 16.5],
          [0, cy - 17],
          [-SHELL_HALF * 0.9, cy - 11],
          [SHELL_HALF * 0.9, cy - 11]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.strokeStyle = rgba24([236, 248, 255], 0.5 * p.frostAmt);
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(-SHELL_HALF * 0.78, cy - 5.4);
        ctx.quadraticCurveTo(0, cy - 7.4, SHELL_HALF * 0.78, cy - 5.4);
        ctx.stroke();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba24(p.light, p.lightAmt));
        lg.addColorStop(1, rgba24(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt56(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw46(season) {
    return (ctx) => paint56(ctx, SP56[season], 0);
  }
  function rippleShimmer(ctx, t, iceAmt) {
    const live = 1 - iceAmt;
    if (live <= 0.01) return;
    ctx.save();
    ctx.globalAlpha = 1;
    ctx.strokeStyle = `rgba(255,255,255,${0.12 + 0.12 * (0.5 + 0.5 * Math.sin(t * 1.6)) * live})`;
    ctx.lineWidth = 0.8;
    for (let i = 0; i < 3; i++) {
      const ph = t * 1.3 + i * 1.4;
      const yo = -1.5 + i * 1.6 + Math.sin(ph) * 0.6;
      ctx.beginPath();
      ctx.moveTo(-12, 18.6 + yo);
      ctx.quadraticCurveTo(Math.sin(ph) * 2, 17.6 + yo, 12, 18.6 + yo);
      ctx.stroke();
    }
    ctx.restore();
  }
  function anim46(season) {
    return (ctx, t) => {
      const bob = bobAt56(t);
      const p = clampP45(SP56[season]);
      paint56(ctx, SP56[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        rippleShimmer(ctx, t, p.iceAmt);
        const cy = SHELL_CY + bob;
        const pearlX = 0.5;
        const pearlY = cy - 2.6;
        if (season === "Spring") {
          const g = 0.2 + 0.25 * (0.5 + 0.5 * Math.sin(t * 1.4));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.ellipse(pearlX - 1.6, pearlY - 1.8, 0.9 + g * 0.7, 1.1 + g * 0.7, -0.4, 0, Math.PI * 2);
          ctx.fill();
          const dy = cy - 5 + Math.sin(t * 1.1) * 0.8;
          ctx.fillStyle = `rgba(255,255,255,${0.25 + 0.25 * (0.5 + 0.5 * Math.sin(t * 2))})`;
          ctx.beginPath();
          ctx.arc(-5, dy, 0.9, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const ang = prog * Math.PI * 2;
          const gx = pearlX + Math.cos(ang) * 1.7;
          const gy = pearlY + Math.sin(ang) * 1.7;
          ctx.fillStyle = "rgba(255,255,255,0.9)";
          ctx.beginPath();
          ctx.ellipse(gx, gy, 1.1, 1.3, ang, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = "rgba(255,255,255,0.4)";
          ctx.beginPath();
          ctx.arc(pearlX - 1.4, pearlY - 1.6, 1, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const drift = Math.sin(t * 0.7) * 4;
          const rot = -0.5 + Math.sin(t * 0.7) * 0.3;
          ctx.save();
          ctx.translate(-12 + drift, 19.4 + Math.cos(t * 0.7) * 0.6);
          ctx.rotate(rot);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
          const g = 0.18 + 0.2 * (0.5 + 0.5 * Math.sin(t * 1));
          ctx.fillStyle = `rgba(255,248,235,${g})`;
          ctx.beginPath();
          ctx.arc(pearlX - 1.4, pearlY - 1.6, 1, 0, Math.PI * 2);
          ctx.fill();
        } else {
          ctx.globalAlpha = 0.12 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(214,234,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 18.6, 9, 2.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
          const prog = (t / 3.2 % 1 + 1) % 1;
          const fy = -20 + prog * 36;
          const fx = -4 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
          const g = 0.18 + 0.2 * (0.5 + 0.5 * Math.sin(t * 1));
          ctx.fillStyle = `rgba(245,248,255,${g})`;
          ctx.beginPath();
          ctx.arc(pearlX - 1.5, pearlY - 1.7, 1, 0, Math.PI * 2);
          ctx.fill();
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition53(fromIdx) {
    const from = SP56[SEASON_NAMES[fromIdx]];
    const to = SP56[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother56(clamp0157(pp));
      paint56(ctx, lerpP56(from, to, k), 0);
    };
  }
  var springToSummer50 = makeTransition53(0);
  var summerToAutumn50 = makeTransition53(1);
  var autumnToWinter50 = makeTransition53(2);
  var VARIANTS58 = {
    Spring: { draw: draw46("Spring"), anim: anim46("Spring") },
    Summer: { draw: draw46("Summer"), anim: anim46("Summer") },
    Autumn: { draw: draw46("Autumn"), anim: anim46("Autumn") },
    Winter: { draw: draw46("Winter"), anim: anim46("Winter") }
  };
  var TRANSITIONS58 = {
    0: springToSummer50,
    1: summerToAutumn50,
    2: autumnToWinter50
  };

  // src/textures/seasonal/fish/mackerel.ts
  function clamp0158(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother57 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp62(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB50(a, b, t) {
    return [lerp62(a[0], b[0], t), lerp62(a[1], b[1], t), lerp62(a[2], b[2], t)];
  }
  function rgb57(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba25(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0158(a)})`;
  }
  function lerpP57(a, b, t) {
    return {
      backDark: lerpRGB50(a.backDark, b.backDark, t),
      backMid: lerpRGB50(a.backMid, b.backMid, t),
      stripe: lerpRGB50(a.stripe, b.stripe, t),
      belly: lerpRGB50(a.belly, b.belly, t),
      bellyLit: lerpRGB50(a.bellyLit, b.bellyLit, t),
      fin: lerpRGB50(a.fin, b.fin, t),
      eye: lerpRGB50(a.eye, b.eye, t),
      water: lerpRGB50(a.water, b.water, t),
      waterDeep: lerpRGB50(a.waterDeep, b.waterDeep, t),
      outline: lerpRGB50(a.outline, b.outline, t),
      light: lerpRGB50(a.light, b.light, t),
      lightAmt: lerp62(a.lightAmt, b.lightAmt, t),
      vividness: lerp62(a.vividness, b.vividness, t),
      gloss: lerp62(a.gloss, b.gloss, t),
      iceAmt: lerp62(a.iceAmt, b.iceAmt, t),
      frostAmt: lerp62(a.frostAmt, b.frostAmt, t),
      blossomAmt: lerp62(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp62(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP46(p) {
    return {
      ...p,
      lightAmt: clamp0158(p.lightAmt),
      vividness: clamp0158(p.vividness),
      gloss: clamp0158(p.gloss),
      iceAmt: clamp0158(p.iceAmt),
      frostAmt: clamp0158(p.frostAmt),
      blossomAmt: clamp0158(p.blossomAmt),
      fallenLeafAmt: clamp0158(p.fallenLeafAmt)
    };
  }
  var SP57 = {
    // Spring — bright fish; fresh blue dewy water; a tiny blossom petal.
    Spring: {
      backDark: [44, 96, 110],
      backMid: [70, 144, 152],
      stripe: [26, 58, 70],
      belly: [214, 224, 226],
      bellyLit: [245, 250, 250],
      fin: [96, 150, 158],
      eye: [30, 46, 54],
      water: [120, 192, 220],
      waterDeep: [70, 134, 170],
      outline: [26, 50, 58],
      light: [232, 246, 248],
      lightAmt: 0.16,
      vividness: 0.6,
      gloss: 0.45,
      iceAmt: 0,
      frostAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — PEAK: vivid iridescent blue-green stripes; bright saturated water.
    Summer: {
      backDark: [30, 112, 120],
      backMid: [48, 178, 168],
      stripe: [16, 56, 72],
      belly: [226, 236, 234],
      bellyLit: [252, 255, 255],
      fin: [70, 168, 168],
      eye: [22, 40, 50],
      water: [78, 178, 226],
      waterDeep: [40, 122, 180],
      outline: [18, 46, 56],
      light: [255, 244, 214],
      lightAmt: 0.18,
      vividness: 1,
      gloss: 0.95,
      iceAmt: 0,
      frostAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — fish a touch duller; olive-tinged water + a floating fallen leaf.
    Autumn: {
      backDark: [56, 92, 92],
      backMid: [84, 138, 126],
      stripe: [34, 56, 56],
      belly: [206, 210, 198],
      bellyLit: [238, 240, 226],
      fin: [104, 140, 122],
      eye: [36, 44, 44],
      water: [120, 158, 150],
      waterDeep: [82, 116, 104],
      outline: [40, 52, 48],
      light: [248, 214, 156],
      lightAmt: 0.2,
      vividness: 0.5,
      gloss: 0.4,
      iceAmt: 0,
      frostAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — water FROZEN to pale blue-white ICE + frost sparkle; a little frost
    // on the fish; blue-green stripes still clearly visible; cool light.
    Winter: {
      backDark: [52, 100, 110],
      backMid: [82, 150, 152],
      stripe: [30, 60, 70],
      belly: [218, 230, 236],
      bellyLit: [248, 252, 255],
      fin: [110, 156, 162],
      eye: [34, 50, 58],
      water: [216, 234, 244],
      waterDeep: [168, 198, 218],
      outline: [40, 58, 68],
      light: [212, 230, 252],
      lightAmt: 0.3,
      vividness: 0.55,
      gloss: 0.3,
      iceAmt: 0.92,
      frostAmt: 0.7,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var FISH_CY = 10;
  var HEAD_X2 = -16.5;
  var TAIL_X2 = 17;
  var BODY_HALF = 6.2;
  function spineY(u, bob, flex) {
    const arch = Math.sin(u * Math.PI) * 1.4;
    const wave = Math.sin(u * Math.PI * 1.6 - 0.6) * flex;
    return FISH_CY + bob - arch + wave;
  }
  function bodyHalf(u) {
    const shape = Math.sin(Math.pow(clamp0158(u), 0.85) * Math.PI);
    const taper = 1 - clamp0158((u - 0.55) / 0.45) * 0.72;
    return BODY_HALF * shape * Math.max(0.28, taper);
  }
  function xAt(u) {
    return lerp62(HEAD_X2, TAIL_X2, clamp0158(u));
  }
  function fishBodyPath(ctx, bob, flex) {
    const N = 14;
    ctx.beginPath();
    ctx.moveTo(HEAD_X2, spineY(0, bob, flex));
    for (let i = 1; i <= N; i++) {
      const u = i / N;
      ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u));
    }
    for (let i = N; i >= 0; i--) {
      const u = i / N;
      ctx.lineTo(xAt(u), spineY(u, bob, flex) + bodyHalf(u));
    }
    ctx.closePath();
  }
  function tailFin(ctx, bob, flex, col, oc) {
    const wristY = spineY(1, bob, flex);
    const wx = TAIL_X2;
    ctx.fillStyle = rgb57(col);
    ctx.strokeStyle = rgb57(oc);
    ctx.lineWidth = 1.1;
    ctx.beginPath();
    ctx.moveTo(wx - 1, wristY - 1.4);
    ctx.quadraticCurveTo(wx + 5, wristY - 6.5, wx + 7.4, wristY - 6.6);
    ctx.quadraticCurveTo(wx + 4.6, wristY - 2.6, wx + 4.2, wristY);
    ctx.quadraticCurveTo(wx + 4.6, wristY + 2.6, wx + 7.4, wristY + 6.6);
    ctx.quadraticCurveTo(wx + 5, wristY + 6.5, wx - 1, wristY + 1.4);
    ctx.closePath();
    ctx.fill();
    ctx.stroke();
  }
  function paint57(ctx, raw, bob, flex = 0) {
    const p = clampP46(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba25(p.waterDeep, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb57(p.waterDeep);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.6, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb57(p.water);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.save();
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.clip();
      const wg = ctx.createLinearGradient(-14, 14, 10, 22);
      wg.addColorStop(0, rgba25([255, 255, 255], 0.22 * (1 - 0.4 * p.iceAmt)));
      wg.addColorStop(0.5, rgba25([255, 255, 255], 0.04));
      wg.addColorStop(1, rgba25(p.waterDeep, 0.18));
      ctx.fillStyle = wg;
      ctx.fillRect(-18, 13, 36, 12);
      ctx.strokeStyle = rgba25([255, 255, 255], 0.22 * (1 - 0.5 * p.iceAmt));
      ctx.lineWidth = 0.9;
      [16.4, 18.6, 20.6].forEach((ry, i) => {
        ctx.beginPath();
        ctx.moveTo(-13 + i, ry);
        ctx.quadraticCurveTo(-2, ry - 0.8, 12 - i, ry + 0.4);
        ctx.stroke();
      });
      ctx.restore();
      if (p.iceAmt > 0.01) {
        const a = p.iceAmt;
        ctx.fillStyle = rgba25([236, 246, 252], 0.8 * a);
        ctx.beginPath();
        ctx.ellipse(0, 18.8, 17.6, 5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba25([200, 222, 240], 0.45 * a);
        ctx.beginPath();
        ctx.ellipse(2, 20.2, 16, 3.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba25([255, 255, 255], 0.55 * a);
        ctx.lineWidth = 0.8;
        const facets = [
          [-12, 18, -2, 21],
          [3, 16.6, 9, 20],
          [-6, 21, 6, 17.4],
          [10, 17.8, 14, 20.4]
        ];
        facets.forEach(([x0, y0, x1, y1]) => {
          ctx.beginPath();
          ctx.moveTo(x0, y0);
          ctx.lineTo(x1, y1);
          ctx.stroke();
        });
        ctx.fillStyle = rgba25([255, 255, 255], 0.85 * a);
        [[-10, 18], [4, 19.4], [12, 17.6], [-3, 20.6], [8, 21]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const px = -12.5;
        const py = 18.6;
        ctx.fillStyle = rgba25([255, 246, 250], 0.95 * a);
        ctx.beginPath();
        ctx.ellipse(px, py, 2.3, 1.3, -0.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba25([255, 198, 222], 0.8 * a);
        ctx.lineWidth = 0.6;
        ctx.beginPath();
        ctx.moveTo(px - 1.8, py + 0.4);
        ctx.quadraticCurveTo(px, py - 0.6, px + 1.8, py - 0.2);
        ctx.stroke();
        ctx.strokeStyle = rgba25([255, 255, 255], 0.35 * a);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.ellipse(px, py + 0.2, 3.6, 1.6, 0, 0, Math.PI * 2);
        ctx.stroke();
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const lx = 12.5;
        const ly = 18.4;
        ctx.save();
        ctx.translate(lx, ly);
        ctx.rotate(0.5);
        ctx.fillStyle = rgba25([196, 116, 40], a);
        ctx.beginPath();
        ctx.ellipse(0, 0, 3.4, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba25([110, 56, 18], a);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.moveTo(-3.2, 0);
        ctx.lineTo(3.2, 0);
        ctx.stroke();
        ctx.restore();
        ctx.strokeStyle = rgba25([255, 255, 255], 0.25 * a);
        ctx.lineWidth = 0.7;
        ctx.beginPath();
        ctx.ellipse(lx, ly + 0.4, 4.4, 1.9, 0, 0, Math.PI * 2);
        ctx.stroke();
      }
      ctx.fillStyle = rgba25(p.outline, 0.26 * (1 - 0.5 * p.iceAmt));
      ctx.beginPath();
      ctx.ellipse(1.5, FISH_CY + bob + 5.5, 16, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      tailFin(ctx, bob, flex, p.fin, p.outline);
      ctx.save();
      ctx.lineWidth = 2.2;
      ctx.strokeStyle = rgb57(p.outline);
      fishBodyPath(ctx, bob, flex);
      ctx.stroke();
      ctx.restore();
      ctx.save();
      fishBodyPath(ctx, bob, flex);
      ctx.clip();
      ctx.fillStyle = rgb57(p.belly);
      ctx.fillRect(HEAD_X2 - 4, FISH_CY + bob - 14, TAIL_X2 - HEAD_X2 + 10, 28);
      ctx.fillStyle = rgb57(p.bellyLit);
      ctx.beginPath();
      ctx.moveTo(HEAD_X2, FISH_CY + bob + 1);
      for (let i = 0; i <= 14; i++) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) + bodyHalf(u));
      }
      for (let i = 14; i >= 0; i--) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) + bodyHalf(u) * 0.2);
      }
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb57(p.backMid);
      ctx.beginPath();
      for (let i = 0; i <= 14; i++) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u));
      }
      for (let i = 14; i >= 0; i--) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u) * 0.05);
      }
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgb57(p.backDark);
      ctx.beginPath();
      for (let i = 0; i <= 14; i++) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u));
      }
      for (let i = 14; i >= 0; i--) {
        const u = i / 14;
        ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u) * 0.55);
      }
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgba25(p.stripe, 0.85 + 0.1 * p.vividness);
      ctx.lineWidth = 1.5;
      const nStripes = 9;
      for (let s = 0; s < nStripes; s++) {
        const u = 0.1 + s / nStripes * 0.78;
        const cx = xAt(u);
        const topY = spineY(u, bob, flex) - bodyHalf(u);
        const midY = spineY(u, bob, flex) - bodyHalf(u) * 0.08;
        ctx.beginPath();
        ctx.moveTo(cx - 0.6, topY + 0.4);
        ctx.quadraticCurveTo(cx + 1.8, lerp62(topY, midY, 0.5), cx - 0.8, midY);
        ctx.stroke();
      }
      if (p.vividness > 0.02) {
        const sg = ctx.createLinearGradient(HEAD_X2, FISH_CY + bob - BODY_HALF, TAIL_X2, FISH_CY + bob);
        sg.addColorStop(0, rgba25(p.backMid, 0));
        sg.addColorStop(0.45, rgba25([140, 240, 220], 0.18 * p.vividness));
        sg.addColorStop(0.7, rgba25([180, 240, 255], 0.12 * p.vividness));
        sg.addColorStop(1, rgba25(p.backMid, 0));
        ctx.fillStyle = sg;
        ctx.beginPath();
        for (let i = 0; i <= 14; i++) {
          const u = i / 14;
          ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u));
        }
        for (let i = 14; i >= 0; i--) {
          const u = i / 14;
          ctx.lineTo(xAt(u), spineY(u, bob, flex) - bodyHalf(u) * 0.1);
        }
        ctx.closePath();
        ctx.fill();
      }
      if (p.gloss > 0.02) {
        ctx.strokeStyle = rgba25([255, 255, 255], 0.2 + 0.5 * p.gloss);
        ctx.lineWidth = 1.3;
        ctx.beginPath();
        for (let i = 1; i <= 13; i++) {
          const u = i / 14;
          const x = xAt(u);
          const y = spineY(u, bob, flex) - bodyHalf(u) * 0.78;
          if (i === 1) ctx.moveTo(x, y);
          else ctx.lineTo(x, y);
        }
        ctx.stroke();
      }
      if (p.frostAmt > 0.02) {
        ctx.strokeStyle = rgba25([226, 240, 252], 0.5 * p.frostAmt);
        ctx.lineWidth = 1.6;
        ctx.beginPath();
        for (let i = 1; i <= 13; i++) {
          const u = i / 14;
          const x = xAt(u);
          const y = spineY(u, bob, flex) - bodyHalf(u) * 0.92;
          if (i === 1) ctx.moveTo(x, y);
          else ctx.lineTo(x, y);
        }
        ctx.stroke();
        ctx.fillStyle = rgba25([240, 248, 255], 0.8 * p.frostAmt);
        for (let s = 0; s < 6; s++) {
          const u = 0.18 + s / 6 * 0.62;
          const x = xAt(u) + Math.sin(s * 1.7) * 1.2;
          const y = spineY(u, bob, flex) - bodyHalf(u) * 0.7;
          ctx.beginPath();
          ctx.arc(x, y, 0.7, 0, Math.PI * 2);
          ctx.fill();
        }
      }
      ctx.restore();
      {
        const u = 0.42;
        const bx = xAt(u);
        const by = spineY(u, bob, flex) - bodyHalf(u) + 0.6;
        ctx.fillStyle = rgb57(p.fin);
        ctx.strokeStyle = rgb57(p.outline);
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.moveTo(bx - 3, by);
        ctx.quadraticCurveTo(bx - 1, by - 5, bx + 1.4, by - 4.4);
        ctx.lineTo(bx + 3.4, by);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
      }
      {
        const u = 0.34;
        const fx = xAt(u);
        const fy = spineY(u, bob, flex) + bodyHalf(u) * 0.55;
        ctx.fillStyle = rgba25(p.fin, 0.92);
        ctx.strokeStyle = rgb57(p.outline);
        ctx.lineWidth = 0.9;
        ctx.beginPath();
        ctx.moveTo(fx - 1, fy - 1);
        ctx.quadraticCurveTo(fx + 2.4, fy + 2.6, fx + 4.6, fy + 4);
        ctx.quadraticCurveTo(fx + 1.6, fy + 3, fx - 1, fy + 1.4);
        ctx.closePath();
        ctx.fill();
        ctx.stroke();
      }
      {
        const u = 0.18;
        const gx = xAt(u);
        const gTop = spineY(u, bob, flex) - bodyHalf(u);
        const gBot = spineY(u, bob, flex) + bodyHalf(u);
        ctx.strokeStyle = rgba25(p.outline, 0.7);
        ctx.lineWidth = 1.1;
        ctx.beginPath();
        ctx.moveTo(gx, gTop + 0.6);
        ctx.quadraticCurveTo(gx - 2.4, FISH_CY + bob, gx, gBot - 0.6);
        ctx.stroke();
        const ex = HEAD_X2 + 4.2;
        const ey = spineY(0.06, bob, flex) - 1.2;
        ctx.fillStyle = rgb57(p.bellyLit);
        ctx.beginPath();
        ctx.arc(ex, ey, 1.9, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb57(p.eye);
        ctx.beginPath();
        ctx.arc(ex, ey, 1.25, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = "rgba(255,255,255,0.9)";
        ctx.beginPath();
        ctx.arc(ex - 0.45, ey - 0.45, 0.5, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba25(p.light, p.lightAmt));
        lg.addColorStop(1, rgba25(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt57(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function flexAt(t, amp = 1.1, w = 1.4) {
    return amp * Math.sin(w * t);
  }
  function draw47(season) {
    return (ctx) => paint57(ctx, SP57[season], 0, 0);
  }
  function anim47(season) {
    return (ctx, t) => {
      const bob = bobAt57(t);
      const flex = flexAt(t);
      paint57(ctx, SP57[season], bob, flex);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const ripPhase = t * 0.35 % 1;
        const iceMute = 1 - 0.55 * clamp0158(SP57[season].iceAmt);
        ctx.strokeStyle = `rgba(255,255,255,${(0.12 + 0.12 * (0.5 + 0.5 * Math.sin(t * 1.3))) * iceMute})`;
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        const ry = 16.5 + ripPhase * 5.5;
        ctx.moveTo(-13, ry);
        ctx.quadraticCurveTo(-1, ry - 0.9 - Math.sin(t * 1.7) * 0.4, 12, ry + 0.4);
        ctx.stroke();
        const exg = HEAD_X2 + 4.2;
        const eyg = spineY(0.06, bob, flex) - 1.2;
        const gl = 0.4 + 0.5 * (0.5 + 0.5 * Math.sin(t * 2.4));
        ctx.fillStyle = `rgba(255,255,255,${gl * 0.7})`;
        ctx.beginPath();
        ctx.arc(exg - 0.45, eyg - 0.45, 0.5 + 0.25 * gl, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.22 + 0.26 * (0.5 + 0.5 * Math.sin(t * 2));
          const u = 0.3 + 0.4 * (0.5 + 0.5 * Math.sin(t * 0.6));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(xAt(u), spineY(u, bob, flex) - 1.5, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const prog = t * 0.5 % 1;
          const u = lerp62(0.12, 0.86, prog);
          const x = xAt(u);
          const y = spineY(u, bob, flex) - bodyHalf(u) * 0.6;
          const sh = ctx.createRadialGradient(x, y, 0.4, x, y, 6);
          sh.addColorStop(0, "rgba(190,255,238,0.7)");
          sh.addColorStop(0.5, "rgba(150,225,255,0.3)");
          sh.addColorStop(1, "rgba(150,225,255,0)");
          ctx.fillStyle = sh;
          ctx.beginPath();
          ctx.ellipse(x, y, 6, 3.2, 0, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const dx = 12.5 + Math.sin(t * 0.5) * 2.2;
          const dy = 18.4 + Math.sin(t * 0.7 + 1) * 0.6;
          ctx.save();
          ctx.translate(dx, dy);
          ctx.rotate(0.5 + Math.sin(t * 0.4) * 0.25);
          ctx.fillStyle = "rgba(196,116,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.4, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(110,56,18,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3.2, 0);
          ctx.lineTo(3.2, 0);
          ctx.stroke();
          ctx.restore();
        } else {
          const prog = (t / 3.2 % 1 + 1) % 1;
          const fy = -22 + prog * 40;
          const fx = -4 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.globalAlpha = 0.5 + 0.4 * Math.sin(prog * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 0.9));
          ctx.fillStyle = "rgba(214,234,252,1)";
          ctx.beginPath();
          ctx.ellipse(0, 19, 15, 4.2, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition54(fromIdx) {
    const from = SP57[SEASON_NAMES[fromIdx]];
    const to = SP57[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother57(clamp0158(pp));
      paint57(ctx, lerpP57(from, to, k), 0, 0);
    };
  }
  var springToSummer51 = makeTransition54(0);
  var summerToAutumn51 = makeTransition54(1);
  var autumnToWinter51 = makeTransition54(2);
  var VARIANTS59 = {
    Spring: { draw: draw47("Spring"), anim: anim47("Spring") },
    Summer: { draw: draw47("Summer"), anim: anim47("Summer") },
    Autumn: { draw: draw47("Autumn"), anim: anim47("Autumn") },
    Winter: { draw: draw47("Winter"), anim: anim47("Winter") }
  };
  var TRANSITIONS59 = {
    0: springToSummer51,
    1: summerToAutumn51,
    2: autumnToWinter51
  };

  // src/textures/seasonal/fish/kelp.ts
  function clamp0159(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother58 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp63(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB51(a, b, t) {
    return [lerp63(a[0], b[0], t), lerp63(a[1], b[1], t), lerp63(a[2], b[2], t)];
  }
  function rgb58(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba26(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0159(a)})`;
  }
  function lerpP58(a, b, t) {
    return {
      bladeLight: lerpRGB51(a.bladeLight, b.bladeLight, t),
      bladeMid: lerpRGB51(a.bladeMid, b.bladeMid, t),
      bladeDark: lerpRGB51(a.bladeDark, b.bladeDark, t),
      float: lerpRGB51(a.float, b.float, t),
      holdfast: lerpRGB51(a.holdfast, b.holdfast, t),
      padWater: lerpRGB51(a.padWater, b.padWater, t),
      padDark: lerpRGB51(a.padDark, b.padDark, t),
      outline: lerpRGB51(a.outline, b.outline, t),
      light: lerpRGB51(a.light, b.light, t),
      lightAmt: lerp63(a.lightAmt, b.lightAmt, t),
      ripeness: lerp63(a.ripeness, b.ripeness, t),
      gloss: lerp63(a.gloss, b.gloss, t),
      frostAmt: lerp63(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp63(a.snowCapAmt, b.snowCapAmt, t),
      iceAmt: lerp63(a.iceAmt, b.iceAmt, t),
      padSnowAmt: lerp63(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp63(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp63(a.fallenLeafAmt, b.fallenLeafAmt, t),
      frayAmt: lerp63(a.frayAmt, b.frayAmt, t)
    };
  }
  function clampP47(p) {
    return {
      ...p,
      lightAmt: clamp0159(p.lightAmt),
      ripeness: clamp0159(p.ripeness),
      gloss: clamp0159(p.gloss),
      frostAmt: clamp0159(p.frostAmt),
      snowCapAmt: clamp0159(p.snowCapAmt),
      iceAmt: clamp0159(p.iceAmt),
      padSnowAmt: clamp0159(p.padSnowAmt),
      blossomAmt: clamp0159(p.blossomAmt),
      fallenLeafAmt: clamp0159(p.fallenLeafAmt),
      frayAmt: clamp0159(p.frayAmt)
    };
  }
  var SP58 = {
    // Spring — bright fresh-green young kelp; fresh blue water; a tiny blossom.
    Spring: {
      bladeLight: [150, 206, 96],
      bladeMid: [86, 156, 64],
      bladeDark: [48, 104, 50],
      float: [120, 176, 80],
      holdfast: [96, 78, 44],
      padWater: [126, 196, 222],
      padDark: [70, 134, 168],
      outline: [34, 62, 40],
      light: [228, 244, 230],
      lightAmt: 0.16,
      ripeness: 0.3,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0,
      frayAmt: 0
    },
    // Summer — lush full green-brown kelp (PEAK); bright saturated blue water.
    Summer: {
      bladeLight: [126, 178, 70],
      bladeMid: [78, 132, 52],
      bladeDark: [44, 92, 42],
      float: [108, 150, 60],
      holdfast: [102, 80, 42],
      padWater: [70, 168, 220],
      padDark: [34, 108, 168],
      outline: [28, 54, 32],
      light: [255, 240, 206],
      lightAmt: 0.18,
      ripeness: 1,
      gloss: 0.95,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      frayAmt: 0
    },
    // Autumn — browning/amber kelp, frayed tips; olive-tinged water; a fallen leaf.
    Autumn: {
      bladeLight: [176, 150, 70],
      bladeMid: [134, 104, 50],
      bladeDark: [88, 66, 36],
      float: [150, 118, 56],
      holdfast: [104, 76, 40],
      padWater: [120, 150, 130],
      padDark: [80, 104, 84],
      outline: [58, 44, 26],
      light: [248, 208, 148],
      lightAmt: 0.2,
      ripeness: 0.75,
      gloss: 0.4,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85,
      frayAmt: 0.85
    },
    // Winter — water FROZEN to pale blue-white ICE; frost + snow on the blades;
    // green-brown kelp still clearly visible underneath; cool light.
    Winter: {
      bladeLight: [110, 150, 78],
      bladeMid: [72, 112, 54],
      bladeDark: [46, 80, 44],
      float: [96, 130, 62],
      holdfast: [92, 78, 56],
      padWater: [206, 226, 240],
      padDark: [150, 178, 204],
      outline: [40, 52, 52],
      light: [206, 226, 252],
      lightAmt: 0.3,
      ripeness: 0.6,
      gloss: 0.3,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      iceAmt: 0.9,
      padSnowAmt: 0.8,
      blossomAmt: 0,
      fallenLeafAmt: 0,
      frayAmt: 0.2
    }
  };
  var KELP_BASE_Y = 17;
  var KELP_TOP_Y = -22;
  function stipePoint(s, wave) {
    const y = lerp63(KELP_BASE_Y, KELP_TOP_Y, s);
    const baseBend = Math.sin(s * Math.PI * 1.15) * 3.4 - s * 2;
    const x = baseBend + wave * s * s;
    return [x, y];
  }
  var BLADES3 = [
    // s, side, length
    [0.24, -1, 12],
    [0.42, 1, 13],
    [0.6, -1, 12.5],
    [0.78, 1, 11],
    [0.92, -1, 9]
  ];
  var FLOATS = [
    // s, side
    [0.36, 1],
    [0.55, -1],
    [0.72, 1],
    [0.88, -1]
  ];
  function paint58(ctx, raw, bob, wave = 0) {
    const p = clampP47(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba26(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb58(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb58(p.padWater);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba26([255, 255, 255], 0.16 * (1 - 0.5 * p.iceAmt));
      ctx.beginPath();
      ctx.ellipse(-4, 17.6, 11, 2, -0.06, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba26([255, 255, 255], 0.18 * (1 - 0.6 * p.iceAmt));
      ctx.lineWidth = 0.8;
      [[-6, 18.6, 7], [6, 20, 6]].forEach(([rx, ry, rr]) => {
        ctx.beginPath();
        ctx.ellipse(rx, ry, rr, rr * 0.28, 0, Math.PI * 1.05, Math.PI * 1.95);
        ctx.stroke();
      });
      if (p.iceAmt > 0.01) {
        const a = p.iceAmt;
        ctx.fillStyle = rgba26([232, 244, 252], 0.85 * a);
        ctx.beginPath();
        ctx.ellipse(0, 18.6, 17.6 * (0.6 + 0.4 * a), 4.8, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba26([198, 222, 240], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba26([170, 198, 222], 0.6 * a);
        ctx.lineWidth = 0.7;
        [[-10, 18, -3, 20], [4, 17.6, 11, 19.4], [-2, 21, 5, 18.6]].forEach(([x1, y1, x2, y2]) => {
          ctx.beginPath();
          ctx.moveTo(x1, y1);
          ctx.lineTo(x2, y2);
          ctx.stroke();
        });
        if (p.padSnowAmt > 0.01) {
          ctx.fillStyle = rgba26([255, 255, 255], 0.8 * clamp0159(p.padSnowAmt));
          [[-9, 17.6], [5, 19], [11, 17.6], [-3, 20.2]].forEach(([sx, sy]) => {
            ctx.beginPath();
            ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
            ctx.fill();
          });
        }
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-12.5, 18.6], [11.5, 18]];
        spots.forEach(([bx, by], idx) => {
          ctx.fillStyle = rgba26(idx === 0 ? [255, 232, 246] : [255, 250, 252], 0.95 * a);
          for (let k = 0; k < 5; k++) {
            const ang = k / 5 * Math.PI * 2;
            ctx.beginPath();
            ctx.ellipse(bx + Math.cos(ang) * 1.4, by + Math.sin(ang) * 0.9, 1, 0.7, ang, 0, Math.PI * 2);
            ctx.fill();
          }
          ctx.fillStyle = rgba26([255, 214, 90], a);
          ctx.beginPath();
          ctx.arc(bx, by, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.4, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba26(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba26([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba26(p.outline, 0.22);
      ctx.beginPath();
      ctx.ellipse(2.5, KELP_BASE_Y + 2, 8, 2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb58(p.holdfast);
      ctx.beginPath();
      ctx.moveTo(-6, KELP_BASE_Y + 1.5);
      ctx.quadraticCurveTo(-3, KELP_BASE_Y - 3, 0, KELP_BASE_Y - 2.5);
      ctx.quadraticCurveTo(3, KELP_BASE_Y - 3, 6, KELP_BASE_Y + 1.5);
      ctx.quadraticCurveTo(3, KELP_BASE_Y + 3, 0, KELP_BASE_Y + 2.4);
      ctx.quadraticCurveTo(-3, KELP_BASE_Y + 3, -6, KELP_BASE_Y + 1.5);
      ctx.closePath();
      ctx.fill();
      ctx.strokeStyle = rgb58(p.holdfast);
      ctx.lineWidth = 1.6;
      [-5, -2, 2, 5].forEach((rx) => {
        ctx.beginPath();
        ctx.moveTo(rx * 0.6, KELP_BASE_Y - 1);
        ctx.quadraticCurveTo(rx, KELP_BASE_Y + 2, rx, KELP_BASE_Y + 3.2);
        ctx.stroke();
      });
      const STEPS = 14;
      const spine = [];
      for (let i = 0; i <= STEPS; i++) spine.push(stipePoint(i / STEPS, wave));
      const spineBob = spine.map(([x, y], i) => [x, y - bob * (i / STEPS)]);
      const drawBlade = (s, side, len, fill, w) => {
        const [ax, ay] = stipePoint(s, wave);
        const bobY = ay - bob * s;
        const dir = side;
        const eff = len * (1 - 0.18 * p.frayAmt);
        const tipX = ax + dir * eff * 0.62;
        const tipY = bobY - eff * 0.5;
        const midX = ax + dir * eff * 0.42;
        const midY = bobY - eff * 0.12;
        ctx.strokeStyle = fill;
        ctx.lineWidth = w;
        ctx.lineCap = "round";
        ctx.beginPath();
        ctx.moveTo(ax, bobY);
        ctx.quadraticCurveTo(midX, midY, tipX, tipY);
        ctx.stroke();
      };
      BLADES3.forEach(([s, side, len]) => drawBlade(s, side, len + 1, rgb58(p.outline), 5.2));
      ctx.strokeStyle = rgb58(p.outline);
      ctx.lineWidth = 5.4;
      ctx.beginPath();
      ctx.moveTo(spineBob[0][0], spineBob[0][1]);
      for (let i = 1; i < spineBob.length; i++) ctx.lineTo(spineBob[i][0], spineBob[i][1]);
      ctx.stroke();
      BLADES3.forEach(([s, side, len]) => drawBlade(s, side, len, rgb58(p.bladeMid), 3.6));
      BLADES3.forEach(([s, side, len]) => {
        if (side < 0) drawBlade(s, side, len * 0.92, rgb58(p.bladeLight), 1.5);
      });
      ctx.strokeStyle = rgb58(p.bladeMid);
      ctx.lineWidth = 3.4;
      ctx.beginPath();
      ctx.moveTo(spineBob[0][0], spineBob[0][1]);
      for (let i = 1; i < spineBob.length; i++) ctx.lineTo(spineBob[i][0], spineBob[i][1]);
      ctx.stroke();
      ctx.strokeStyle = rgba26(p.bladeLight, 0.7);
      ctx.lineWidth = 1.3;
      ctx.beginPath();
      ctx.moveTo(spineBob[0][0] - 1, spineBob[0][1]);
      for (let i = 1; i < spineBob.length; i++) ctx.lineTo(spineBob[i][0] - 1, spineBob[i][1]);
      ctx.stroke();
      FLOATS.forEach(([s, side]) => {
        const [ax, ay] = stipePoint(s, wave);
        const fy = ay - bob * s;
        const fx = ax + side * 1.6;
        ctx.fillStyle = rgb58(p.outline);
        ctx.beginPath();
        ctx.arc(fx, fy, 2.6, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgb58(p.float);
        ctx.beginPath();
        ctx.arc(fx, fy, 2, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba26([255, 255, 255], 0.4 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.arc(fx - 0.7, fy - 0.8, 0.7, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.frayAmt > 0.02) {
        ctx.strokeStyle = rgba26(p.bladeDark, 0.8 * p.frayAmt);
        ctx.lineWidth = 1;
        BLADES3.forEach(([s, side, len]) => {
          const [ax, ay] = stipePoint(s, wave);
          const bobY = ay - bob * s;
          const eff = len * (1 - 0.18 * p.frayAmt);
          const tipX = ax + side * eff * 0.62;
          const tipY = bobY - eff * 0.5;
          for (let k = 0; k < 3; k++) {
            const ang = -0.6 + k * 0.5;
            ctx.beginPath();
            ctx.moveTo(tipX, tipY);
            ctx.lineTo(tipX + Math.cos(ang) * 2.6 * side, tipY + Math.sin(ang) * 2.6);
            ctx.stroke();
          }
        });
      }
      if (p.gloss > 0.02) {
        ctx.strokeStyle = rgba26([255, 255, 255], 0.12 + 0.4 * p.gloss);
        ctx.lineWidth = 1;
        BLADES3.forEach(([s, side, len]) => {
          if (side >= 0) return;
          const [ax, ay] = stipePoint(s, wave);
          const bobY = ay - bob * s;
          const tipX = ax + side * len * 0.5;
          const tipY = bobY - len * 0.38;
          ctx.beginPath();
          ctx.moveTo(ax + side * 1.5, bobY - 1);
          ctx.lineTo(tipX, tipY);
          ctx.stroke();
        });
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba26([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-7, -2],
          [4, -8],
          [-3, 4],
          [6, -1],
          [-9, 8],
          [2, -14],
          [8, 5]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy - bob * 0.4, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba26([246, 251, 255], 0.92 * a);
        BLADES3.forEach(([s, side, len]) => {
          if (side >= 0 && s < 0.5) return;
          const [ax, ay] = stipePoint(s, wave);
          const bobY = ay - bob * s;
          const tipX = ax + side * len * 0.58;
          const tipY = bobY - len * 0.46;
          ctx.beginPath();
          ctx.ellipse(tipX, tipY, 2.2, 1.3, side * 0.4, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.fillStyle = rgba26([246, 251, 255], 0.9 * a);
        const crown2 = spineBob[spineBob.length - 1];
        ctx.beginPath();
        ctx.ellipse(crown2[0], crown2[1] + 0.5, 2.6, 1.6, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba26(p.light, p.lightAmt));
        lg.addColorStop(1, rgba26(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt58(t, amp = 0.7, w = 1.3) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function waveAt(t, amp = 2.2, w = 0.9) {
    return amp * (1 - Math.cos(w * t)) * 0.5 - amp * (1 - Math.cos(w * 2 * t)) * 0.5 * 0.25;
  }
  function draw48(season) {
    return (ctx) => paint58(ctx, SP58[season], 0, 0);
  }
  function anim48(season) {
    return (ctx, t) => {
      const bob = bobAt58(t);
      const swayScale = season === "Winter" ? 0.35 : 1;
      const wave = waveAt(t) * swayScale;
      paint58(ctx, SP58[season], bob, wave);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        if (season === "Spring") {
          const g = 0.2 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2));
          ctx.strokeStyle = `rgba(255,255,255,${0.14 + 0.12 * Math.sin(t * 1.3)})`;
          ctx.lineWidth = 0.8;
          const rp = t * 0.4 % 1;
          ctx.beginPath();
          ctx.ellipse(-2, 18.6, 4 + rp * 9, (4 + rp * 9) * 0.26, 0, 0, Math.PI * 2);
          ctx.stroke();
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(-5 + wave, -4 - bob, 1 + g * 0.7, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          ctx.strokeStyle = `rgba(255,255,255,${0.16 + 0.12 * Math.sin(t * 1.6)})`;
          ctx.lineWidth = 0.9;
          const rp = t * 0.5 % 1;
          ctx.beginPath();
          ctx.ellipse(0, 19, 4 + rp * 10, (4 + rp * 10) * 0.26, 0, 0, Math.PI * 2);
          ctx.stroke();
          const prog = t * 0.45 % 1;
          const sy = lerp63(-2, -18, prog);
          ctx.fillStyle = "rgba(255,255,255,0.7)";
          ctx.beginPath();
          ctx.ellipse(-5 + wave * 0.6, sy - bob * 0.5, 1.3, 2.4, -0.1, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          ctx.strokeStyle = `rgba(255,255,255,${0.1 + 0.08 * Math.sin(t * 1.2)})`;
          ctx.lineWidth = 0.8;
          const rp = t * 0.35 % 1;
          ctx.beginPath();
          ctx.ellipse(2, 19.4, 4 + rp * 9, (4 + rp * 9) * 0.26, 0, 0, Math.PI * 2);
          ctx.stroke();
          const dp = t / 4 % 1;
          const dx = 6 + dp * 12;
          const dy = -4 + dp * 22 + Math.sin(dp * Math.PI * 3) * 1.5;
          ctx.globalAlpha = 0.7 * Math.sin(dp * Math.PI);
          ctx.fillStyle = "rgba(150,118,56,1)";
          ctx.save();
          ctx.translate(dx, dy);
          ctx.rotate(dp * 4);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.4, 1, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
          ctx.globalAlpha = 1;
        } else {
          ctx.globalAlpha = 0.12 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 18.8, 8, 2.2, -0.05, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
          const seeds = [
            [-9, 0, 1],
            [6, 0.4, 0.9],
            [11, 0.7, 0.8],
            [-2, 0.25, 0.9]
          ];
          ctx.fillStyle = "#ffffff";
          seeds.forEach(([fx, phase, r]) => {
            const prog = ((t / 3.2 + phase) % 1 + 1) % 1;
            const fy = -22 + prog * 40;
            const dx = fx + Math.sin(prog * Math.PI * 2 + phase * 6) * 3;
            ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
            ctx.beginPath();
            ctx.arc(dx, fy, r, 0, Math.PI * 2);
            ctx.fill();
          });
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition55(fromIdx) {
    const from = SP58[SEASON_NAMES[fromIdx]];
    const to = SP58[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother58(clamp0159(pp));
      paint58(ctx, lerpP58(from, to, k), 0, 0);
    };
  }
  var springToSummer52 = makeTransition55(0);
  var summerToAutumn52 = makeTransition55(1);
  var autumnToWinter52 = makeTransition55(2);
  var VARIANTS60 = {
    Spring: { draw: draw48("Spring"), anim: anim48("Spring") },
    Summer: { draw: draw48("Summer"), anim: anim48("Summer") },
    Autumn: { draw: draw48("Autumn"), anim: anim48("Autumn") },
    Winter: { draw: draw48("Winter"), anim: anim48("Winter") }
  };
  var TRANSITIONS60 = {
    0: springToSummer52,
    1: summerToAutumn52,
    2: autumnToWinter52
  };

  // src/textures/seasonal/mine/gem.ts
  function clamp0160(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother59 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp64(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB52(a, b, t) {
    return [lerp64(a[0], b[0], t), lerp64(a[1], b[1], t), lerp64(a[2], b[2], t)];
  }
  function rgb59(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba27(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0160(a)})`;
  }
  function lerpP59(a, b, t) {
    return {
      rockLight: lerpRGB52(a.rockLight, b.rockLight, t),
      rockMid: lerpRGB52(a.rockMid, b.rockMid, t),
      rockDark: lerpRGB52(a.rockDark, b.rockDark, t),
      gemLight: lerpRGB52(a.gemLight, b.gemLight, t),
      gemMid: lerpRGB52(a.gemMid, b.gemMid, t),
      gemDark: lerpRGB52(a.gemDark, b.gemDark, t),
      padRock: lerpRGB52(a.padRock, b.padRock, t),
      padDark: lerpRGB52(a.padDark, b.padDark, t),
      soil: lerpRGB52(a.soil, b.soil, t),
      outline: lerpRGB52(a.outline, b.outline, t),
      light: lerpRGB52(a.light, b.light, t),
      lightAmt: lerp64(a.lightAmt, b.lightAmt, t),
      gemSat: lerp64(a.gemSat, b.gemSat, t),
      gloss: lerp64(a.gloss, b.gloss, t),
      mossAmt: lerp64(a.mossAmt, b.mossAmt, t),
      frostAmt: lerp64(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp64(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp64(a.padSnowAmt, b.padSnowAmt, t),
      dewAmt: lerp64(a.dewAmt, b.dewAmt, t),
      fallenLeafAmt: lerp64(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP48(p) {
    return {
      ...p,
      lightAmt: clamp0160(p.lightAmt),
      gemSat: clamp0160(p.gemSat),
      gloss: clamp0160(p.gloss),
      mossAmt: clamp0160(p.mossAmt),
      frostAmt: clamp0160(p.frostAmt),
      snowCapAmt: clamp0160(p.snowCapAmt),
      padSnowAmt: clamp0160(p.padSnowAmt),
      dewAmt: clamp0160(p.dewAmt),
      fallenLeafAmt: clamp0160(p.fallenLeafAmt)
    };
  }
  var SP59 = {
    // Spring — cool-bright light; fresh moss tint at the rock base; dewy pad.
    Spring: {
      rockLight: [176, 180, 188],
      rockMid: [128, 132, 142],
      rockDark: [84, 88, 98],
      gemLight: [150, 206, 255],
      gemMid: [70, 138, 240],
      gemDark: [34, 78, 176],
      padRock: [150, 150, 142],
      padDark: [96, 94, 88],
      soil: [120, 102, 78],
      outline: [42, 46, 56],
      light: [228, 240, 246],
      lightAmt: 0.15,
      gemSat: 0.78,
      gloss: 0.55,
      mossAmt: 0.55,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0.6,
      fallenLeafAmt: 0
    },
    // Summer — richest saturated blue gem (PEAK); warm light; dry rocky pad.
    Summer: {
      rockLight: [184, 186, 190],
      rockMid: [134, 136, 140],
      rockDark: [86, 88, 94],
      gemLight: [168, 218, 255],
      gemMid: [54, 130, 252],
      gemDark: [24, 70, 198],
      padRock: [168, 158, 132],
      padDark: [110, 102, 80],
      soil: [128, 104, 74],
      outline: [40, 44, 54],
      light: [255, 242, 210],
      lightAmt: 0.17,
      gemSat: 1,
      gloss: 0.95,
      mossAmt: 0.32,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — low amber light; olive-tan earthy pad; a couple fallen leaves.
    Autumn: {
      rockLight: [178, 174, 168],
      rockMid: [128, 124, 120],
      rockDark: [82, 80, 78],
      gemLight: [150, 200, 244],
      gemMid: [62, 126, 224],
      gemDark: [30, 72, 168],
      padRock: [156, 140, 96],
      padDark: [108, 94, 60],
      soil: [120, 92, 56],
      outline: [50, 42, 38],
      light: [248, 208, 146],
      lightAmt: 0.2,
      gemSat: 0.82,
      gloss: 0.45,
      mossAmt: 0.1,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; snow cap on the rock + frost on the facets;
    // gem still bright underneath; snowy pad.
    Winter: {
      rockLight: [170, 178, 192],
      rockMid: [120, 128, 142],
      rockDark: [78, 86, 100],
      gemLight: [156, 208, 255],
      gemMid: [66, 134, 238],
      gemDark: [30, 76, 178],
      padRock: [178, 196, 214],
      padDark: [120, 144, 170],
      soil: [126, 118, 110],
      outline: [48, 52, 66],
      light: [208, 228, 252],
      lightAmt: 0.3,
      gemSat: 0.8,
      gloss: 0.4,
      mossAmt: 0,
      frostAmt: 0.7,
      snowCapAmt: 0.9,
      padSnowAmt: 0.9,
      dewAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var ROCK_BASE_Y = 16;
  var ROCK_TOP_Y = 0;
  function rockPath(ctx, bob) {
    const b = ROCK_BASE_Y + bob;
    const t = ROCK_TOP_Y + bob;
    ctx.beginPath();
    ctx.moveTo(-15, b - 1);
    ctx.lineTo(-16, b - 6);
    ctx.lineTo(-12, t + 5);
    ctx.lineTo(-5, t + 1);
    ctx.lineTo(2, t + 4);
    ctx.lineTo(9, t - 1);
    ctx.lineTo(15, t + 7);
    ctx.lineTo(16, b - 5);
    ctx.lineTo(13, b);
    ctx.lineTo(-12, b);
    ctx.closePath();
  }
  var GEM_OX = -1.5;
  var GEM_OY = -2;
  var GEM_OUTLINE = [
    [-6, 2],
    // base-left on the rock
    [-7, -8],
    // left mid edge
    [-3, -20],
    // tall left tip
    [0, -10],
    // inner valley
    [3, -22],
    // tallest centre tip
    [6, -9],
    // right valley
    [8, -14],
    // right tip
    [7, 1],
    // base-right
    [1, 4]
    // base centre
  ];
  function gemOutlinePath(ctx, bob) {
    ctx.beginPath();
    GEM_OUTLINE.forEach(([x, y], i) => {
      const px = GEM_OX + x;
      const py = GEM_OY + y + bob;
      if (i === 0) ctx.moveTo(px, py);
      else ctx.lineTo(px, py);
    });
    ctx.closePath();
  }
  var GEM_FACETS = [
    // left tall shard — lit front face
    { pts: [[-6, 2], [-3, -20], [0, -10]], tone: 0 },
    // left tall shard — shaded right side
    { pts: [[-3, -20], [0, -10], [1, 4]], tone: 2 },
    // left base under shard
    { pts: [[-6, 2], [0, -10], [1, 4]], tone: 1 },
    // centre tallest shard — lit
    { pts: [[1, 4], [3, -22], [0, -10]], tone: 0 },
    // centre shard — mid
    { pts: [[1, 4], [3, -22], [6, -9]], tone: 1 },
    // right shard — lit upper
    { pts: [[3, -22], [6, -9], [8, -14]], tone: 0 },
    // right shard — dark side
    { pts: [[6, -9], [8, -14], [7, 1]], tone: 2 },
    // right base
    { pts: [[1, 4], [6, -9], [7, 1]], tone: 1 }
  ];
  var GEM_EDGES = [
    [[-3, -20], [0, -10]],
    [[3, -22], [0, -10]],
    [[3, -22], [6, -9]],
    [[8, -14], [6, -9]]
  ];
  function paint59(ctx, raw, bob) {
    const p = clampP48(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba27(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb59(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb59(p.padRock);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba27(p.padDark, 0.55);
      const pebbles = [
        [-12, 18.4, 1.2],
        [-6, 20.4, 0.9],
        [7, 18.2, 1.1],
        [13, 19.8, 0.9],
        [1, 21, 0.8]
      ];
      pebbles.forEach(([px, py, r]) => {
        ctx.beginPath();
        ctx.ellipse(px, py, r, r * 0.62, 0, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.dewAmt > 0.01) {
        ctx.fillStyle = rgba27([235, 246, 252], 0.3 * p.dewAmt);
        ctx.beginPath();
        ctx.ellipse(-4, 18.4, 12, 2.8, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.mossAmt > 0.01) {
        const a = p.mossAmt;
        ctx.fillStyle = rgba27([108, 168, 78], 0.5 * a);
        const moss = [
          [-13, 18, 3.4],
          [12, 18, 3],
          [-2, 20.4, 4.2]
        ];
        moss.forEach(([mx, my, r]) => {
          ctx.beginPath();
          ctx.ellipse(mx, my, r, r * 0.5, 0, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.strokeStyle = rgba27([86, 150, 62], 0.8 * a);
        ctx.lineWidth = 1;
        [-14, -10, 11, 14].forEach((gx, i) => {
          ctx.beginPath();
          ctx.moveTo(gx, 18.5);
          ctx.lineTo(gx + (i % 2 ? 1 : -1), 14.5);
          ctx.stroke();
        });
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba27([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba27([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba27([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.fillStyle = rgb59(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, ROCK_BASE_Y + bob + 1.5, 13, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba27(p.outline, 0.28);
      ctx.beginPath();
      ctx.ellipse(3, ROCK_BASE_Y + bob + 2, 14, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      rockPath(ctx, bob);
      ctx.fillStyle = rgb59(p.outline);
      ctx.fill();
      ctx.save();
      rockPath(ctx, bob);
      ctx.clip();
      const rb = ROCK_BASE_Y + bob;
      const rt = ROCK_TOP_Y + bob;
      ctx.fillStyle = rgb59(p.rockMid);
      ctx.fillRect(-18, rt - 6, 36, rb - rt + 14);
      const litGrad = ctx.createLinearGradient(-14, rt - 2, 14, rb);
      litGrad.addColorStop(0, rgb59(p.rockLight));
      litGrad.addColorStop(0.5, rgb59(p.rockMid));
      litGrad.addColorStop(1, rgb59(p.rockDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-3, lerp64(rt, rb, 0.5), 16, (rb - rt) * 0.72, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba27(p.rockDark, 0.8);
      ctx.lineWidth = 1.4;
      ctx.beginPath();
      ctx.moveTo(-9, rt + 6);
      ctx.lineTo(-6, lerp64(rt, rb, 0.6));
      ctx.lineTo(-9, rb - 2);
      ctx.stroke();
      ctx.beginPath();
      ctx.moveTo(11, rt + 4);
      ctx.lineTo(8, lerp64(rt, rb, 0.5));
      ctx.stroke();
      ctx.fillStyle = rgba27(p.rockLight, 0.5);
      ctx.beginPath();
      ctx.moveTo(-12, rt + 6);
      ctx.lineTo(-6, rt + 2);
      ctx.lineTo(-7, lerp64(rt, rb, 0.45));
      ctx.closePath();
      ctx.fill();
      ctx.fillStyle = rgba27(p.rockDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(2, rb - 1, 15, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba27([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-12, rt + 5);
        ctx.quadraticCurveTo(-9, rt - 1, -5, rt + 1);
        ctx.quadraticCurveTo(-1, rt + 5, 9, rt - 1);
        ctx.quadraticCurveTo(13, rt + 2, 15, rt + 7);
        ctx.quadraticCurveTo(8, rt + 4, 2, rt + 6);
        ctx.quadraticCurveTo(-5, rt + 8, -12, rt + 5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba27([210, 226, 244], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(-3, rt + 4, 9, 2, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      gemOutlinePath(ctx, bob);
      ctx.fillStyle = rgb59(p.outline);
      ctx.fill();
      ctx.save();
      gemOutlinePath(ctx, bob);
      ctx.clip();
      GEM_FACETS.forEach(({ pts, tone }) => {
        const col = tone === 0 ? p.gemLight : tone === 1 ? p.gemMid : p.gemDark;
        ctx.fillStyle = rgb59(col);
        ctx.beginPath();
        pts.forEach(([x, y], i) => {
          const px = GEM_OX + x;
          const py = GEM_OY + y + bob;
          if (i === 0) ctx.moveTo(px, py);
          else ctx.lineTo(px, py);
        });
        ctx.closePath();
        ctx.fill();
      });
      if (p.gemSat > 0.5) {
        ctx.fillStyle = rgba27([120, 190, 255], 0.18 * (p.gemSat - 0.5) * 2);
        ctx.fillRect(GEM_OX - 8, GEM_OY - 24 + bob, 18, 30);
      }
      ctx.strokeStyle = rgba27([220, 240, 255], 0.5 + 0.4 * p.gloss);
      ctx.lineWidth = 1;
      GEM_EDGES.forEach(([[ax, ay], [bx, by]]) => {
        ctx.beginPath();
        ctx.moveTo(GEM_OX + ax, GEM_OY + ay + bob);
        ctx.lineTo(GEM_OX + bx, GEM_OY + by + bob);
        ctx.stroke();
      });
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba27([255, 255, 255], 0.3 + 0.5 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(GEM_OX + 1.5, GEM_OY - 16 + bob, 1, 2.2, -0.25, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba27([214, 232, 250], 0.22 * p.frostAmt);
        ctx.fillRect(GEM_OX - 8, GEM_OY - 24 + bob, 18, 16);
        ctx.fillStyle = rgba27([236, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-3, -18],
          [1, -20],
          [4, -16],
          [-1, -12],
          [6, -11],
          [-5, -9],
          [2, -6]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(GEM_OX + sx, GEM_OY + sy + bob, 0.6, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [9, rt + 6, 0.5, [196, 120, 40]],
          [-10, rb - 4, -0.6, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba27(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba27([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba27(p.light, p.lightAmt));
        lg.addColorStop(1, rgba27(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt59(t, amp = 0.7, w = 1.4) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw49(season) {
    return (ctx) => paint59(ctx, SP59[season], 0);
  }
  function glintPoint(prog, bob) {
    const x = GEM_OX + lerp64(1.5, 1.5, prog);
    const y = GEM_OY + lerp64(2, -20, prog) + bob;
    return [x, y];
  }
  function anim49(season) {
    return (ctx, t) => {
      const bob = bobAt59(t);
      paint59(ctx, SP59[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const prog = t * 0.4 % 1;
        const [gx, gy] = glintPoint(prog, bob);
        let glintAlpha = 0.4;
        if (season === "Summer") glintAlpha = 0.85;
        else if (season === "Spring") glintAlpha = 0.5;
        else if (season === "Autumn") glintAlpha = 0.4;
        else glintAlpha = 0.45;
        const fade = Math.sin(prog * Math.PI);
        const glintCol = season === "Winter" ? "210,232,255" : "255,255,255";
        ctx.fillStyle = `rgba(${glintCol},${glintAlpha * fade})`;
        ctx.beginPath();
        ctx.ellipse(gx, gy, 1.2, 2.4, -0.2, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.25 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(-8, 18 + Math.sin(t * 1.1) * 0.6, 1 + g * 0.6, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          ctx.save();
          const lrx = 9, lry = ROCK_TOP_Y + bob + 6;
          ctx.translate(lrx, lry);
          ctx.rotate(0.5 + Math.sin(t * 1.3) * 0.18);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        } else if (season === "Winter") {
          const flProg = t / 3.2 % 1;
          const fy = -22 + flProg * 38;
          const fx = -2 + Math.sin(flProg * Math.PI * 2) * 3;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(flProg * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 0.08 + 0.08 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(GEM_OX, GEM_OY - 12 + bob, 7, 9, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition56(fromIdx) {
    const from = SP59[SEASON_NAMES[fromIdx]];
    const to = SP59[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother59(clamp0160(pp));
      paint59(ctx, lerpP59(from, to, k), 0);
    };
  }
  var springToSummer53 = makeTransition56(0);
  var summerToAutumn53 = makeTransition56(1);
  var autumnToWinter53 = makeTransition56(2);
  var VARIANTS61 = {
    Spring: { draw: draw49("Spring"), anim: anim49("Spring") },
    Summer: { draw: draw49("Summer"), anim: anim49("Summer") },
    Autumn: { draw: draw49("Autumn"), anim: anim49("Autumn") },
    Winter: { draw: draw49("Winter"), anim: anim49("Winter") }
  };
  var TRANSITIONS61 = {
    0: springToSummer53,
    1: summerToAutumn53,
    2: autumnToWinter53
  };

  // src/textures/seasonal/mine/gold.ts
  function clamp0161(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother60 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp65(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB53(a, b, t) {
    return [lerp65(a[0], b[0], t), lerp65(a[1], b[1], t), lerp65(a[2], b[2], t)];
  }
  function rgb60(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba28(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0161(a)})`;
  }
  function lerpP60(a, b, t) {
    return {
      rockLight: lerpRGB53(a.rockLight, b.rockLight, t),
      rockMid: lerpRGB53(a.rockMid, b.rockMid, t),
      rockDark: lerpRGB53(a.rockDark, b.rockDark, t),
      goldLight: lerpRGB53(a.goldLight, b.goldLight, t),
      goldMid: lerpRGB53(a.goldMid, b.goldMid, t),
      goldDark: lerpRGB53(a.goldDark, b.goldDark, t),
      padTop: lerpRGB53(a.padTop, b.padTop, t),
      padDark: lerpRGB53(a.padDark, b.padDark, t),
      soil: lerpRGB53(a.soil, b.soil, t),
      outline: lerpRGB53(a.outline, b.outline, t),
      light: lerpRGB53(a.light, b.light, t),
      lightAmt: lerp65(a.lightAmt, b.lightAmt, t),
      goldShine: lerp65(a.goldShine, b.goldShine, t),
      mossAmt: lerp65(a.mossAmt, b.mossAmt, t),
      frostAmt: lerp65(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp65(a.snowCapAmt, b.snowCapAmt, t),
      padSnowAmt: lerp65(a.padSnowAmt, b.padSnowAmt, t),
      dewAmt: lerp65(a.dewAmt, b.dewAmt, t),
      fallenLeafAmt: lerp65(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP49(p) {
    return {
      ...p,
      lightAmt: clamp0161(p.lightAmt),
      goldShine: clamp0161(p.goldShine),
      mossAmt: clamp0161(p.mossAmt),
      frostAmt: clamp0161(p.frostAmt),
      snowCapAmt: clamp0161(p.snowCapAmt),
      padSnowAmt: clamp0161(p.padSnowAmt),
      dewAmt: clamp0161(p.dewAmt),
      fallenLeafAmt: clamp0161(p.fallenLeafAmt)
    };
  }
  var SP60 = {
    // Spring — cool-bright light; grey rock + bright gold; fresh moss at the base,
    // dewy pad.
    Spring: {
      rockLight: [186, 190, 196],
      rockMid: [140, 144, 152],
      rockDark: [88, 92, 100],
      goldLight: [252, 222, 124],
      goldMid: [232, 182, 60],
      goldDark: [172, 120, 28],
      padTop: [150, 142, 122],
      padDark: [96, 88, 72],
      soil: [120, 100, 76],
      outline: [54, 56, 62],
      light: [232, 242, 248],
      lightAmt: 0.15,
      goldShine: 0.55,
      mossAmt: 0.55,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0.7,
      fallenLeafAmt: 0
    },
    // Summer — richest warm gold (PEAK); warm light, dry rocky pad, light moss.
    Summer: {
      rockLight: [192, 192, 190],
      rockMid: [146, 146, 146],
      rockDark: [92, 92, 94],
      goldLight: [255, 230, 130],
      goldMid: [240, 190, 58],
      goldDark: [180, 126, 24],
      padTop: [162, 144, 110],
      padDark: [108, 92, 64],
      soil: [134, 108, 74],
      outline: [56, 54, 54],
      light: [255, 242, 206],
      lightAmt: 0.18,
      goldShine: 0.9,
      mossAmt: 0.3,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — low amber light; olive-tan earthy pad; a couple fallen leaves.
    Autumn: {
      rockLight: [184, 182, 178],
      rockMid: [140, 138, 136],
      rockDark: [88, 86, 86],
      goldLight: [252, 224, 122],
      goldMid: [234, 184, 56],
      goldDark: [174, 122, 24],
      padTop: [158, 142, 96],
      padDark: [110, 96, 58],
      soil: [128, 100, 60],
      outline: [58, 50, 42],
      light: [248, 212, 150],
      lightAmt: 0.2,
      goldShine: 0.55,
      mossAmt: 0.12,
      frostAmt: 0,
      snowCapAmt: 0,
      padSnowAmt: 0,
      dewAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — cool blue-grey light; snow cap + frost on the rock, snowy pad; the
    // gold veins still glint clearly underneath.
    Winter: {
      rockLight: [188, 196, 208],
      rockMid: [140, 148, 162],
      rockDark: [90, 98, 114],
      goldLight: [250, 224, 138],
      goldMid: [230, 184, 70],
      goldDark: [168, 122, 36],
      padTop: [172, 186, 204],
      padDark: [118, 134, 156],
      soil: [124, 118, 110],
      outline: [50, 52, 62],
      light: [208, 228, 252],
      lightAmt: 0.3,
      goldShine: 0.6,
      mossAmt: 0,
      frostAmt: 0.7,
      snowCapAmt: 0.85,
      padSnowAmt: 0.9,
      dewAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var ROCK_TOP = -14;
  var ROCK_BOT = 17;
  var ROCK_HALF = 15;
  function rockBodyPath(ctx, bob) {
    const t = ROCK_TOP + bob;
    const b = ROCK_BOT + bob;
    ctx.beginPath();
    ctx.moveTo(-ROCK_HALF, b - 1);
    ctx.lineTo(-ROCK_HALF + 1.5, b - 11);
    ctx.lineTo(-11.5, t + 12);
    ctx.lineTo(-9, t + 4);
    ctx.lineTo(-4.5, t + 1);
    ctx.lineTo(-1, t);
    ctx.lineTo(4, t + 3);
    ctx.lineTo(8.5, t + 1.5);
    ctx.lineTo(12, t + 11);
    ctx.lineTo(ROCK_HALF - 1, b - 10);
    ctx.lineTo(ROCK_HALF, b - 1);
    ctx.lineTo(8, b + 1.5);
    ctx.lineTo(0, b + 0.5);
    ctx.lineTo(-8, b + 1.5);
    ctx.closePath();
  }
  var VEINS = [
    [[-12, 12], [-6, 4], [-2, -2], [3, -8]],
    [[-9, -6], [-3, -1], [4, 3], [11, 8]],
    [[-1, 14], [1, 6], [-2, 0], [2, -10]]
  ];
  var NUGGETS = [
    // [x, y, radius]
    [-7, 2, 2.4],
    [5, -3, 2],
    [9, 6, 1.7],
    [-3, 9, 1.5]
  ];
  function paint60(ctx, raw, bob) {
    const p = clampP49(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba28(p.padDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb60(p.padDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb60(p.padTop);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba28(p.padDark, 0.55);
      [[-13, 19.5], [12, 18.6], [-6, 21], [7, 20.6], [0, 18.4]].forEach(([sx, sy]) => {
        ctx.beginPath();
        ctx.ellipse(sx, sy, 1.5, 0.9, 0, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.dewAmt > 0.01) {
        ctx.fillStyle = rgba28([235, 248, 255], 0.28 * p.dewAmt);
        ctx.beginPath();
        ctx.ellipse(-3, 18.2, 14, 2.8, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.mossAmt > 0.01) {
        const a = p.mossAmt;
        ctx.fillStyle = rgba28([110, 168, 78], 0.65 * a);
        [[-12, 18.5], [10, 18.2], [-3, 20], [5, 19.4]].forEach(([mx, my], idx) => {
          ctx.beginPath();
          ctx.ellipse(mx, my, 3.2 + idx % 2, 1.6, 0, 0, Math.PI * 2);
          ctx.fill();
        });
        ctx.strokeStyle = rgba28([86, 150, 64], 0.8 * a);
        ctx.lineWidth = 1;
        [-10, -2, 6, 11].forEach((gx) => {
          ctx.beginPath();
          ctx.moveTo(gx, 18.6);
          ctx.lineTo(gx - 0.8, 15.6);
          ctx.stroke();
        });
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba28([244, 250, 255], 0.92 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4 * (0.6 + 0.4 * p.padSnowAmt), 4.6, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba28([210, 226, 244], 0.5 * p.padSnowAmt);
        ctx.beginPath();
        ctx.ellipse(2, 20, 16, 3.4, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba28([255, 255, 255], 0.8 * p.padSnowAmt);
        [[-9, 17.6], [5, 19], [11, 17.4], [-3, 20]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-13, 19.6, -0.5, [196, 120, 40]],
          [13, 18.4, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba28(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba28([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgb60(p.soil);
      ctx.beginPath();
      ctx.ellipse(0, ROCK_BOT + bob + 1.5, 12, 2.8, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgba28(p.outline, 0.3);
      ctx.beginPath();
      ctx.ellipse(3, ROCK_BOT + bob + 2, 13, 2.6, 0, 0, Math.PI * 2);
      ctx.fill();
      rockBodyPath(ctx, bob);
      ctx.strokeStyle = rgb60(p.outline);
      ctx.lineWidth = 2.4;
      ctx.stroke();
      ctx.fillStyle = rgb60(p.outline);
      ctx.fill();
      ctx.save();
      rockBodyPath(ctx, bob);
      ctx.clip();
      const top = ROCK_TOP + bob;
      const bot = ROCK_BOT + bob;
      ctx.fillStyle = rgb60(p.rockMid);
      ctx.fillRect(-ROCK_HALF - 3, top - 4, (ROCK_HALF + 3) * 2, bot - top + 10);
      const litGrad = ctx.createLinearGradient(-ROCK_HALF, top, ROCK_HALF, bot);
      litGrad.addColorStop(0, rgb60(p.rockLight));
      litGrad.addColorStop(0.5, rgb60(p.rockMid));
      litGrad.addColorStop(1, rgb60(p.rockDark));
      ctx.fillStyle = litGrad;
      ctx.globalAlpha = 0.92;
      ctx.beginPath();
      ctx.ellipse(-2, lerp65(top, bot, 0.45), ROCK_HALF + 3, (bot - top) * 0.62, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.globalAlpha = 1;
      ctx.strokeStyle = rgba28(p.rockDark, 0.8);
      ctx.lineWidth = 1.8;
      [
        [[-9, top + 4], [-4, lerp65(top, bot, 0.5)], [-6, bot - 2]],
        [[6, top + 5], [9, lerp65(top, bot, 0.55)], [11, bot - 3]]
      ].forEach((seg) => {
        ctx.beginPath();
        ctx.moveTo(seg[0][0], seg[0][1]);
        ctx.lineTo(seg[1][0], seg[1][1]);
        ctx.lineTo(seg[2][0], seg[2][1]);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba28(p.rockLight, 0.5);
      ctx.lineWidth = 1.1;
      [
        [[-10, top + 6], [-7, lerp65(top, bot, 0.4)]],
        [[-2, top + 2], [0, lerp65(top, bot, 0.3)]]
      ].forEach((seg) => {
        ctx.beginPath();
        ctx.moveTo(seg[0][0], seg[0][1]);
        ctx.lineTo(seg[1][0], seg[1][1]);
        ctx.stroke();
      });
      ctx.fillStyle = rgba28(p.rockDark, 0.6);
      ctx.beginPath();
      ctx.ellipse(0, bot - 2, ROCK_HALF, 4, 0, 0, Math.PI * 2);
      ctx.fill();
      VEINS.forEach((pts) => {
        ctx.strokeStyle = rgb60(p.goldDark);
        ctx.lineWidth = 3;
        ctx.beginPath();
        ctx.moveTo(pts[0][0], pts[0][1] + bob);
        for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0], pts[i][1] + bob);
        ctx.stroke();
        ctx.strokeStyle = rgb60(p.goldMid);
        ctx.lineWidth = 1.8;
        ctx.beginPath();
        ctx.moveTo(pts[0][0], pts[0][1] + bob);
        for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0], pts[i][1] + bob);
        ctx.stroke();
        ctx.strokeStyle = rgba28(p.goldLight, 0.85);
        ctx.lineWidth = 0.8;
        ctx.beginPath();
        ctx.moveTo(pts[0][0] - 0.4, pts[0][1] - 0.5 + bob);
        for (let i = 1; i < pts.length; i++) ctx.lineTo(pts[i][0] - 0.4, pts[i][1] - 0.5 + bob);
        ctx.stroke();
      });
      NUGGETS.forEach(([nx, ny, nr]) => {
        const ng = ctx.createRadialGradient(nx - nr * 0.4, ny - nr * 0.4 + bob, nr * 0.2, nx, ny + bob, nr);
        ng.addColorStop(0, rgb60(p.goldLight));
        ng.addColorStop(0.6, rgb60(p.goldMid));
        ng.addColorStop(1, rgb60(p.goldDark));
        ctx.fillStyle = ng;
        ctx.beginPath();
        ctx.arc(nx, ny + bob, nr, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba28(p.outline, 0.6);
        ctx.lineWidth = 0.7;
        ctx.stroke();
        ctx.fillStyle = rgba28([255, 255, 255], 0.4 + 0.45 * p.goldShine);
        ctx.beginPath();
        ctx.arc(nx - nr * 0.35, ny - nr * 0.35 + bob, nr * 0.32, 0, Math.PI * 2);
        ctx.fill();
      });
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba28([210, 230, 250], 0.26 * p.frostAmt);
        ctx.beginPath();
        ctx.ellipse(-1, lerp65(top, bot, 0.28), ROCK_HALF, (bot - top) * 0.32, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba28([235, 246, 255], 0.65 * p.frostAmt);
        const speck = [
          [-9, top + 4],
          [-3, top + 2],
          [4, top + 4],
          [9, top + 5],
          [-6, lerp65(top, bot, 0.4)],
          [6, lerp65(top, bot, 0.45)],
          [0, lerp65(top, bot, 0.32)]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba28([246, 251, 255], 0.95 * a);
        ctx.beginPath();
        ctx.moveTo(-10, top + 5);
        ctx.quadraticCurveTo(-9, top - 2, -4.5, top - 1);
        ctx.quadraticCurveTo(-1, top - 3, 4, top - 0.5);
        ctx.quadraticCurveTo(8.5, top - 2, 11, top + 4);
        ctx.quadraticCurveTo(6, top + 6, 4, top + 4);
        ctx.quadraticCurveTo(-1, top + 3, -4, top + 5);
        ctx.quadraticCurveTo(-7, top + 6.5, -10, top + 5);
        ctx.closePath();
        ctx.fill();
        ctx.fillStyle = rgba28([205, 222, 242], 0.5 * a);
        ctx.beginPath();
        ctx.ellipse(0, top + 4.5, 9.5, 1.8, 0, 0, Math.PI * 2);
        ctx.fill();
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba28(p.light, p.lightAmt));
        lg.addColorStop(1, rgba28(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt60(t, amp = 0.7, w = 1.3) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  var GLINT_PATH = VEINS.flat();
  function glintAt(u) {
    const n = GLINT_PATH.length;
    const f = clamp0161(u) * (n - 1);
    const i = Math.min(n - 2, Math.floor(f));
    const k = f - i;
    const a = GLINT_PATH[i];
    const b = GLINT_PATH[i + 1];
    return [lerp65(a[0], b[0], k), lerp65(a[1], b[1], k)];
  }
  function draw50(season) {
    return (ctx) => paint60(ctx, SP60[season], 0);
  }
  function anim50(season) {
    return (ctx, t) => {
      const bob = bobAt60(t);
      paint60(ctx, SP60[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const prog = t * 0.4 % 1;
        const [gx, gy] = glintAt(prog);
        const base = season === "Summer" ? 0.7 : season === "Winter" ? 0.45 : 0.55;
        const warm = season === "Summer" ? "255,242,196" : season === "Winter" ? "224,238,255" : "255,250,224";
        const pulse = base * (0.6 + 0.4 * Math.sin(prog * Math.PI));
        ctx.fillStyle = `rgba(${warm},${pulse})`;
        ctx.beginPath();
        ctx.ellipse(gx, gy + bob, 1.6, 1.2, -0.2, 0, Math.PI * 2);
        ctx.fill();
        if (season === "Spring") {
          const g = 0.2 + 0.28 * (0.5 + 0.5 * Math.sin(t * 2.2));
          ctx.fillStyle = `rgba(235,248,255,${g})`;
          ctx.beginPath();
          ctx.arc(-6, 18.4, 1.1 + g * 0.8, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Summer") {
          const s = 0.16 + 0.2 * (0.5 + 0.5 * Math.sin(t * 1.1));
          ctx.fillStyle = `rgba(255,236,170,${s})`;
          ctx.beginPath();
          ctx.ellipse(-7, 2 + bob, 3.4, 2.6, -0.2, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const sway = Math.sin(t * 1.4) * 0.25;
          ctx.save();
          ctx.translate(13, 18.4);
          ctx.rotate(0.7 + sway);
          ctx.fillStyle = "rgba(176,72,32,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.8, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        } else {
          const prog2 = t / 3.2 % 1;
          const fy = -20 + prog2 * 36;
          const dx = 4 + Math.sin(prog2 * Math.PI * 2) * 3;
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog2 * Math.PI);
          ctx.fillStyle = "#ffffff";
          ctx.beginPath();
          ctx.arc(dx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, -2 + bob, 7, 4.5, -0.2, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition57(fromIdx) {
    const from = SP60[SEASON_NAMES[fromIdx]];
    const to = SP60[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother60(clamp0161(pp));
      paint60(ctx, lerpP60(from, to, k), 0);
    };
  }
  var springToSummer54 = makeTransition57(0);
  var summerToAutumn54 = makeTransition57(1);
  var autumnToWinter54 = makeTransition57(2);
  var VARIANTS62 = {
    Spring: { draw: draw50("Spring"), anim: anim50("Spring") },
    Summer: { draw: draw50("Summer"), anim: anim50("Summer") },
    Autumn: { draw: draw50("Autumn"), anim: anim50("Autumn") },
    Winter: { draw: draw50("Winter"), anim: anim50("Winter") }
  };
  var TRANSITIONS62 = {
    0: springToSummer54,
    1: summerToAutumn54,
    2: autumnToWinter54
  };

  // src/textures/seasonal/special/giantPearl.ts
  function clamp0162(x) {
    if (!(x >= 0)) return 0;
    if (x > 1) return 1;
    return x;
  }
  var smoother61 = (x) => x * x * x * (x * (6 * x - 15) + 10);
  function lerp66(a, b, t) {
    return a + (b - a) * t;
  }
  function lerpRGB54(a, b, t) {
    return [lerp66(a[0], b[0], t), lerp66(a[1], b[1], t), lerp66(a[2], b[2], t)];
  }
  function rgb61(c) {
    return `rgb(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])})`;
  }
  function rgba29(c, a) {
    return `rgba(${Math.round(c[0])},${Math.round(c[1])},${Math.round(c[2])},${clamp0162(a)})`;
  }
  function lerpP61(a, b, t) {
    return {
      pearlLight: lerpRGB54(a.pearlLight, b.pearlLight, t),
      pearlMid: lerpRGB54(a.pearlMid, b.pearlMid, t),
      pearlDark: lerpRGB54(a.pearlDark, b.pearlDark, t),
      shellLight: lerpRGB54(a.shellLight, b.shellLight, t),
      shellMid: lerpRGB54(a.shellMid, b.shellMid, t),
      shellDark: lerpRGB54(a.shellDark, b.shellDark, t),
      water: lerpRGB54(a.water, b.water, t),
      waterDark: lerpRGB54(a.waterDark, b.waterDark, t),
      outline: lerpRGB54(a.outline, b.outline, t),
      light: lerpRGB54(a.light, b.light, t),
      lightAmt: lerp66(a.lightAmt, b.lightAmt, t),
      lustre: lerp66(a.lustre, b.lustre, t),
      gloss: lerp66(a.gloss, b.gloss, t),
      frostAmt: lerp66(a.frostAmt, b.frostAmt, t),
      snowCapAmt: lerp66(a.snowCapAmt, b.snowCapAmt, t),
      iceAmt: lerp66(a.iceAmt, b.iceAmt, t),
      padSnowAmt: lerp66(a.padSnowAmt, b.padSnowAmt, t),
      blossomAmt: lerp66(a.blossomAmt, b.blossomAmt, t),
      fallenLeafAmt: lerp66(a.fallenLeafAmt, b.fallenLeafAmt, t)
    };
  }
  function clampP50(p) {
    return {
      ...p,
      lightAmt: clamp0162(p.lightAmt),
      lustre: clamp0162(p.lustre),
      gloss: clamp0162(p.gloss),
      frostAmt: clamp0162(p.frostAmt),
      snowCapAmt: clamp0162(p.snowCapAmt),
      iceAmt: clamp0162(p.iceAmt),
      padSnowAmt: clamp0162(p.padSnowAmt),
      blossomAmt: clamp0162(p.blossomAmt),
      fallenLeafAmt: clamp0162(p.fallenLeafAmt)
    };
  }
  var SP61 = {
    // Spring — lustrous pearl in the shell; fresh bright-blue water + a blossom petal.
    Spring: {
      pearlLight: [255, 255, 255],
      pearlMid: [232, 234, 242],
      pearlDark: [186, 192, 210],
      shellLight: [236, 224, 232],
      shellMid: [206, 188, 200],
      shellDark: [150, 130, 148],
      water: [120, 196, 230],
      waterDark: [66, 138, 184],
      outline: [60, 62, 84],
      light: [232, 244, 248],
      lightAmt: 0.16,
      lustre: 0.62,
      gloss: 0.7,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0.85,
      fallenLeafAmt: 0
    },
    // Summer — pearl at its most brilliant lustre (PEAK); bright saturated blue water.
    Summer: {
      pearlLight: [255, 255, 255],
      pearlMid: [240, 242, 250],
      pearlDark: [196, 202, 222],
      shellLight: [244, 230, 238],
      shellMid: [214, 194, 208],
      shellDark: [156, 134, 154],
      water: [70, 176, 232],
      waterDark: [30, 116, 184],
      outline: [50, 54, 80],
      light: [255, 246, 220],
      lightAmt: 0.2,
      lustre: 1,
      gloss: 1,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0
    },
    // Autumn — pearl a touch warmer/cream; duller olive-tinged water + a fallen leaf.
    Autumn: {
      pearlLight: [252, 248, 236],
      pearlMid: [232, 224, 200],
      pearlDark: [184, 174, 146],
      shellLight: [234, 216, 200],
      shellMid: [200, 176, 158],
      shellDark: [144, 118, 102],
      water: [120, 156, 144],
      waterDark: [78, 110, 96],
      outline: [62, 56, 50],
      light: [248, 214, 158],
      lightAmt: 0.22,
      lustre: 0.5,
      gloss: 0.55,
      frostAmt: 0,
      snowCapAmt: 0,
      iceAmt: 0,
      padSnowAmt: 0,
      blossomAmt: 0,
      fallenLeafAmt: 0.85
    },
    // Winter — water FROZEN to pale blue-white ice + frost; frost on the shell rim;
    // the lustrous white pearl still the bright hero; cool light.
    Winter: {
      pearlLight: [255, 255, 255],
      pearlMid: [236, 240, 248],
      pearlDark: [190, 200, 220],
      shellLight: [224, 216, 228],
      shellMid: [188, 180, 198],
      shellDark: [134, 128, 150],
      water: [214, 230, 244],
      waterDark: [160, 188, 214],
      outline: [62, 66, 92],
      light: [212, 230, 252],
      lightAmt: 0.3,
      lustre: 0.66,
      gloss: 0.7,
      frostAmt: 0.7,
      snowCapAmt: 0.8,
      iceAmt: 0.95,
      padSnowAmt: 0.9,
      blossomAmt: 0,
      fallenLeafAmt: 0
    }
  };
  var PEARL_CX = 0;
  var PEARL_CY = 4;
  var PEARL_R = 11.5;
  var SHELL_BASE_Y = 15;
  function shellValvePath(ctx, dir, bob) {
    const hingeY = SHELL_BASE_Y + bob;
    const open = dir > 0 ? 1 : 0.78;
    ctx.beginPath();
    ctx.moveTo(-15.5, hingeY - dir * 1.5);
    ctx.quadraticCurveTo(
      -16.5,
      hingeY - dir * (8 + 5 * open),
      -8,
      hingeY - dir * (12.5 + 6 * open)
    );
    ctx.quadraticCurveTo(
      0,
      hingeY - dir * (15.5 + 7 * open),
      8,
      hingeY - dir * (12.5 + 6 * open)
    );
    ctx.quadraticCurveTo(
      16.5,
      hingeY - dir * (8 + 5 * open),
      15.5,
      hingeY - dir * 1.5
    );
    ctx.quadraticCurveTo(8, hingeY + dir * 1.2, 0, hingeY + dir * 1.8);
    ctx.quadraticCurveTo(-8, hingeY + dir * 1.2, -15.5, hingeY - dir * 1.5);
    ctx.closePath();
  }
  function paint61(ctx, raw, bob) {
    const p = clampP50(raw);
    ctx.save();
    try {
      ctx.globalAlpha = 1;
      ctx.lineJoin = "round";
      ctx.lineCap = "round";
      ctx.fillStyle = rgba29(p.waterDark, 0.4);
      ctx.beginPath();
      ctx.ellipse(3, 21.5, 16, 4.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb61(p.waterDark);
      ctx.beginPath();
      ctx.ellipse(0, 20.4, 18, 5.4, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.fillStyle = rgb61(p.water);
      ctx.beginPath();
      ctx.ellipse(0, 19, 18, 5.2, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.strokeStyle = rgba29([255, 255, 255], 0.22);
      ctx.lineWidth = 1.1;
      for (let i = -2; i <= 2; i++) {
        const ry = 18 + i * 1.3;
        const rw = 14 - Math.abs(i) * 2.4;
        ctx.beginPath();
        ctx.ellipse(-2 + i * 1.2, ry, rw, 0.7, 0, 0, Math.PI);
        ctx.stroke();
      }
      if (p.iceAmt > 0.01) {
        const a = p.iceAmt;
        ctx.fillStyle = rgba29([224, 238, 250], 0.9 * a);
        ctx.beginPath();
        ctx.ellipse(0, 19, 17.6, 5, 0, 0, Math.PI * 2);
        ctx.fill();
        ctx.strokeStyle = rgba29([170, 200, 226], 0.55 * a);
        ctx.lineWidth = 0.9;
        const cracks = [
          [-12, 18, -2, 20],
          [-2, 20, 9, 17.5],
          [9, 17.5, 15, 20],
          [-6, 16.5, 1, 21]
        ];
        cracks.forEach(([x0, y0, x1, y1]) => {
          ctx.beginPath();
          ctx.moveTo(x0, y0);
          ctx.lineTo(x1, y1);
          ctx.stroke();
        });
        ctx.strokeStyle = rgba29([245, 252, 255], 0.6 * a);
        ctx.lineWidth = 1;
        ctx.beginPath();
        ctx.ellipse(0, 18.4, 17.4, 4.6, 0, Math.PI * 1.05, Math.PI * 1.95);
        ctx.stroke();
      }
      if (p.padSnowAmt > 0.01) {
        ctx.fillStyle = rgba29([255, 255, 255], 0.85 * p.padSnowAmt);
        [[-10, 18.4], [6, 19.6], [12, 17.8], [-3, 20.6], [2, 17.4]].forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.8, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.blossomAmt > 0.01) {
        const a = p.blossomAmt;
        const spots = [[-13, 18.6], [12, 18]];
        spots.forEach(([bx, by], idx) => {
          ctx.save();
          ctx.translate(bx, by);
          ctx.rotate(idx === 0 ? -0.3 : 0.4);
          ctx.fillStyle = rgba29(idx === 0 ? [255, 232, 246] : [255, 248, 252], 0.95 * a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 2.6, 1.4, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.fillStyle = rgba29([255, 214, 220], 0.7 * a);
          ctx.beginPath();
          ctx.ellipse(-0.6, 0, 1, 0.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.restore();
        });
      }
      if (p.fallenLeafAmt > 0.01) {
        const a = p.fallenLeafAmt;
        const leaves = [
          [-12, 19.4, -0.5, [196, 120, 40]],
          [12, 18.6, 0.7, [176, 72, 32]]
        ];
        leaves.forEach(([lx, ly, rot, col]) => {
          ctx.save();
          ctx.translate(lx, ly);
          ctx.rotate(rot);
          ctx.fillStyle = rgba29(col, a);
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = rgba29([90, 44, 16], a);
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        });
      }
      ctx.fillStyle = rgba29(p.outline, 0.26);
      ctx.beginPath();
      ctx.ellipse(2.5, SHELL_BASE_Y + bob + 2.5, 15, 3, 0, 0, Math.PI * 2);
      ctx.fill();
      shellValvePath(ctx, -1, bob);
      ctx.fillStyle = rgb61(p.outline);
      ctx.lineWidth = 2.4;
      ctx.strokeStyle = rgb61(p.outline);
      ctx.stroke();
      ctx.fill();
      ctx.save();
      shellValvePath(ctx, -1, bob);
      ctx.clip();
      const upGrad = ctx.createLinearGradient(0, SHELL_BASE_Y - 22 + bob, 0, SHELL_BASE_Y + bob);
      upGrad.addColorStop(0, rgb61(p.shellLight));
      upGrad.addColorStop(0.6, rgb61(p.shellMid));
      upGrad.addColorStop(1, rgb61(p.shellDark));
      ctx.fillStyle = upGrad;
      ctx.fillRect(-18, SHELL_BASE_Y - 24 + bob, 36, 26);
      ctx.strokeStyle = rgba29(p.shellDark, 0.7);
      ctx.lineWidth = 1.2;
      [-9, -4.5, 0, 4.5, 9].forEach((rx) => {
        ctx.beginPath();
        ctx.moveTo(rx * 0.18, SHELL_BASE_Y - 0.5 + bob);
        ctx.lineTo(rx * 1.5, SHELL_BASE_Y - 18 + bob);
        ctx.stroke();
      });
      ctx.restore();
      const pcx = PEARL_CX;
      const pcy = PEARL_CY + bob;
      ctx.fillStyle = rgba29(p.outline, 0.3);
      ctx.beginPath();
      ctx.ellipse(pcx + 1.5, pcy + PEARL_R - 1.5, PEARL_R * 0.82, PEARL_R * 0.3, 0, 0, Math.PI * 2);
      ctx.fill();
      ctx.beginPath();
      ctx.arc(pcx, pcy, PEARL_R + 0.6, 0, Math.PI * 2);
      ctx.fillStyle = rgb61(p.outline);
      ctx.fill();
      const pGrad = ctx.createRadialGradient(
        pcx - PEARL_R * 0.42,
        pcy - PEARL_R * 0.46,
        PEARL_R * 0.1,
        pcx,
        pcy,
        PEARL_R
      );
      pGrad.addColorStop(0, rgb61(p.pearlLight));
      pGrad.addColorStop(0.5, rgb61(p.pearlMid));
      pGrad.addColorStop(1, rgb61(p.pearlDark));
      ctx.fillStyle = pGrad;
      ctx.beginPath();
      ctx.arc(pcx, pcy, PEARL_R, 0, Math.PI * 2);
      ctx.fill();
      if (p.lustre > 0.02) {
        ctx.save();
        ctx.beginPath();
        ctx.arc(pcx, pcy, PEARL_R, 0, Math.PI * 2);
        ctx.clip();
        ctx.strokeStyle = rgba29([214, 226, 255], 0.22 + 0.34 * p.lustre);
        ctx.lineWidth = 2.6;
        ctx.beginPath();
        ctx.ellipse(pcx - 1.5, pcy + 1, PEARL_R * 0.78, PEARL_R * 0.5, -0.5, Math.PI * 0.05, Math.PI * 0.95);
        ctx.stroke();
        ctx.strokeStyle = rgba29([255, 232, 244], 0.14 + 0.22 * p.lustre);
        ctx.lineWidth = 2;
        ctx.beginPath();
        ctx.ellipse(pcx + 1, pcy + 3, PEARL_R * 0.66, PEARL_R * 0.4, -0.4, Math.PI * 0.1, Math.PI * 0.9);
        ctx.stroke();
        ctx.restore();
      }
      ctx.save();
      ctx.beginPath();
      ctx.arc(pcx, pcy, PEARL_R, 0, Math.PI * 2);
      ctx.clip();
      ctx.fillStyle = rgba29(p.pearlDark, 0.5);
      ctx.beginPath();
      ctx.ellipse(pcx + PEARL_R * 0.5, pcy + PEARL_R * 0.55, PEARL_R * 0.8, PEARL_R * 0.75, -0.5, 0, Math.PI * 2);
      ctx.fill();
      ctx.restore();
      if (p.gloss > 0.02) {
        ctx.fillStyle = rgba29([255, 255, 255], 0.55 + 0.4 * p.gloss);
        ctx.beginPath();
        ctx.ellipse(pcx - PEARL_R * 0.4, pcy - PEARL_R * 0.42, PEARL_R * 0.26, PEARL_R * 0.34, -0.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.fillStyle = rgba29([255, 255, 255], 0.85);
        ctx.beginPath();
        ctx.arc(pcx - PEARL_R * 0.52, pcy - PEARL_R * 0.52, 1.1, 0, Math.PI * 2);
        ctx.fill();
      }
      shellValvePath(ctx, 1, bob);
      ctx.fillStyle = rgb61(p.outline);
      ctx.lineWidth = 2.4;
      ctx.strokeStyle = rgb61(p.outline);
      ctx.stroke();
      ctx.fill();
      ctx.save();
      shellValvePath(ctx, 1, bob);
      ctx.clip();
      const loGrad = ctx.createLinearGradient(0, SHELL_BASE_Y - 4 + bob, 0, SHELL_BASE_Y + 20 + bob);
      loGrad.addColorStop(0, rgb61(p.shellLight));
      loGrad.addColorStop(0.55, rgb61(p.shellMid));
      loGrad.addColorStop(1, rgb61(p.shellDark));
      ctx.fillStyle = loGrad;
      ctx.fillRect(-18, SHELL_BASE_Y - 4 + bob, 36, 26);
      ctx.strokeStyle = rgba29(p.shellDark, 0.7);
      ctx.lineWidth = 1.2;
      [-9, -4.5, 0, 4.5, 9].forEach((rx) => {
        ctx.beginPath();
        ctx.moveTo(rx * 0.18, SHELL_BASE_Y + 0.5 + bob);
        ctx.lineTo(rx * 1.5, SHELL_BASE_Y + 14 + bob);
        ctx.stroke();
      });
      ctx.strokeStyle = rgba29(p.shellLight, 0.6);
      ctx.lineWidth = 1.3;
      ctx.beginPath();
      ctx.moveTo(-14, SHELL_BASE_Y - 0.5 + bob);
      ctx.quadraticCurveTo(0, SHELL_BASE_Y + 2 + bob, 14, SHELL_BASE_Y - 0.5 + bob);
      ctx.stroke();
      ctx.restore();
      if (p.snowCapAmt > 0.02) {
        const a = p.snowCapAmt;
        ctx.fillStyle = rgba29([246, 251, 255], 0.92 * a);
        ctx.beginPath();
        ctx.moveTo(-14, SHELL_BASE_Y - 8.5 + bob);
        ctx.quadraticCurveTo(-8, SHELL_BASE_Y - 12 + bob, -1, SHELL_BASE_Y - 11.5 + bob);
        ctx.quadraticCurveTo(-9, SHELL_BASE_Y - 9 + bob, -14, SHELL_BASE_Y - 8.5 + bob);
        ctx.closePath();
        ctx.fill();
        ctx.beginPath();
        ctx.moveTo(14, SHELL_BASE_Y - 8.5 + bob);
        ctx.quadraticCurveTo(8, SHELL_BASE_Y - 12 + bob, 1, SHELL_BASE_Y - 11.5 + bob);
        ctx.quadraticCurveTo(9, SHELL_BASE_Y - 9 + bob, 14, SHELL_BASE_Y - 8.5 + bob);
        ctx.closePath();
        ctx.fill();
      }
      if (p.frostAmt > 0.02) {
        ctx.fillStyle = rgba29([235, 246, 255], 0.7 * p.frostAmt);
        const speck = [
          [-13, SHELL_BASE_Y - 9 + bob],
          [13, SHELL_BASE_Y - 9 + bob],
          [-7, SHELL_BASE_Y - 11.5 + bob],
          [7, SHELL_BASE_Y - 11.5 + bob],
          [-15, SHELL_BASE_Y + 13 + bob],
          [15, SHELL_BASE_Y + 13 + bob]
        ];
        speck.forEach(([sx, sy]) => {
          ctx.beginPath();
          ctx.arc(sx, sy, 0.7, 0, Math.PI * 2);
          ctx.fill();
        });
      }
      if (p.lightAmt > 1e-3) {
        ctx.globalAlpha = 1;
        const lg = ctx.createRadialGradient(-10, -14, 2, -10, -14, 46);
        lg.addColorStop(0, rgba29(p.light, p.lightAmt));
        lg.addColorStop(1, rgba29(p.light, p.lightAmt * 0.25));
        ctx.fillStyle = lg;
        ctx.fillRect(-24, -24, 48, 48);
      }
    } finally {
      ctx.globalAlpha = 1;
      ctx.restore();
    }
  }
  function bobAt61(t, amp = 0.6, w = 1.3) {
    return amp * (1 - Math.cos(w * t)) * 0.5;
  }
  function draw51(season) {
    return (ctx) => paint61(ctx, SP61[season], 0);
  }
  function anim51(season) {
    return (ctx, t) => {
      const bob = bobAt61(t);
      paint61(ctx, SP61[season], bob);
      ctx.save();
      try {
        ctx.globalAlpha = 1;
        const rippleA = 0.1 + 0.12 * (0.5 + 0.5 * Math.sin(t * 1.4));
        ctx.strokeStyle = `rgba(255,255,255,${rippleA})`;
        ctx.lineWidth = 1;
        const rPhase = t * 0.5 % 1;
        ctx.beginPath();
        ctx.ellipse(-2, 19, 9 + rPhase * 7, 1.6 + rPhase * 1, 0, 0, Math.PI * 2);
        ctx.stroke();
        const pcx = PEARL_CX;
        const pcy = PEARL_CY + bob;
        const glintBright = season === "Summer" ? 0.5 : season === "Autumn" ? 0.26 : 0.34;
        const ga = (t * 0.18 % 1 + 1) % 1;
        const gang = lerp66(Math.PI * 0.85, Math.PI * 0.15, ga);
        const gx = pcx + Math.cos(gang) * PEARL_R * 0.62;
        const gy = pcy - Math.sin(gang) * PEARL_R * 0.62 + 1;
        ctx.save();
        ctx.beginPath();
        ctx.arc(pcx, pcy, PEARL_R - 0.5, 0, Math.PI * 2);
        ctx.clip();
        const gGrad = ctx.createRadialGradient(gx, gy, 0, gx, gy, 5.5);
        gGrad.addColorStop(0, `rgba(255,255,255,${glintBright})`);
        gGrad.addColorStop(1, "rgba(255,255,255,0)");
        ctx.fillStyle = gGrad;
        ctx.beginPath();
        ctx.arc(gx, gy, 5.5, 0, Math.PI * 2);
        ctx.fill();
        ctx.restore();
        if (season === "Spring") {
          const g = 0.18 + 0.22 * (0.5 + 0.5 * Math.sin(t * 2));
          ctx.fillStyle = `rgba(255,255,255,${g})`;
          ctx.beginPath();
          ctx.arc(-9, SHELL_BASE_Y - 8 + bob, 1 + g, 0, Math.PI * 2);
          ctx.fill();
        } else if (season === "Autumn") {
          const dx = Math.sin(t * 0.9) * 2;
          const dy = Math.sin(t * 0.7) * 0.6;
          ctx.save();
          ctx.translate(-12 + dx, 19.4 + dy);
          ctx.rotate(-0.5 + Math.sin(t * 0.8) * 0.12);
          ctx.fillStyle = "rgba(196,120,40,0.9)";
          ctx.beginPath();
          ctx.ellipse(0, 0, 3.2, 1.7, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.strokeStyle = "rgba(90,44,16,0.9)";
          ctx.lineWidth = 0.7;
          ctx.beginPath();
          ctx.moveTo(-3, 0);
          ctx.lineTo(3, 0);
          ctx.stroke();
          ctx.restore();
        } else if (season === "Winter") {
          ctx.globalAlpha = 0.1 + 0.1 * (0.5 + 0.5 * Math.sin(t * 0.8));
          ctx.fillStyle = "rgba(210,232,255,1)";
          ctx.beginPath();
          ctx.ellipse(-3, 19, 10, 3, 0, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
          const prog = (t / 3.4 % 1 + 1) % 1;
          const fy = -22 + prog * 40;
          const fx = 6 + Math.sin(prog * Math.PI * 2) * 4;
          ctx.fillStyle = "#ffffff";
          ctx.globalAlpha = 0.4 + 0.45 * Math.sin(prog * Math.PI);
          ctx.beginPath();
          ctx.arc(fx, fy, 1, 0, Math.PI * 2);
          ctx.fill();
          ctx.globalAlpha = 1;
        }
      } finally {
        ctx.globalAlpha = 1;
        ctx.restore();
      }
    };
  }
  function makeTransition58(fromIdx) {
    const from = SP61[SEASON_NAMES[fromIdx]];
    const to = SP61[SEASON_NAMES[fromIdx + 1]];
    return (ctx, pp) => {
      const k = smoother61(clamp0162(pp));
      paint61(ctx, lerpP61(from, to, k), 0);
    };
  }
  var springToSummer55 = makeTransition58(0);
  var summerToAutumn55 = makeTransition58(1);
  var autumnToWinter55 = makeTransition58(2);
  var VARIANTS63 = {
    Spring: { draw: draw51("Spring"), anim: anim51("Spring") },
    Summer: { draw: draw51("Summer"), anim: anim51("Summer") },
    Autumn: { draw: draw51("Autumn"), anim: anim51("Autumn") },
    Winter: { draw: draw51("Winter"), anim: anim51("Winter") }
  };
  var TRANSITIONS63 = {
    0: springToSummer55,
    1: summerToAutumn55,
    2: autumnToWinter55
  };

  // src/textures/seasonal/showcaseTiles.ts
  var SHOWCASE_TILES = {
    tile_tree_oak: VARIANTS,
    tile_tree_birch: VARIANTS2,
    tile_tree_cypress: VARIANTS3,
    tile_tree_fir: VARIANTS4,
    tile_tree_palm: VARIANTS5,
    tile_tree_willow: VARIANTS6,
    tile_fruit_apple: VARIANTS7,
    tile_fruit_pear: VARIANTS8,
    tile_fruit_lemon: VARIANTS9,
    tile_fruit_blackberry: VARIANTS10,
    tile_fruit_coconut: VARIANTS11,
    tile_fruit_golden_apple: VARIANTS12,
    tile_fruit_jackfruit: VARIANTS13,
    tile_fruit_rambutan: VARIANTS14,
    tile_fruit_starfruit: VARIANTS15,
    tile_grain_corn: VARIANTS16,
    tile_veg_pepper: VARIANTS17,
    tile_veg_mushroom: VARIANTS18,
    tile_veg_beet: VARIANTS19,
    tile_veg_broccoli: VARIANTS20,
    tile_veg_carrot: VARIANTS21,
    tile_veg_cucumber: VARIANTS22,
    tile_veg_eggplant: VARIANTS23,
    tile_veg_squash: VARIANTS24,
    tile_veg_turnip: VARIANTS25,
    tile_grass_grass: VARIANTS26,
    tile_grass_meadow: VARIANTS27,
    tile_grass_spiky: VARIANTS28,
    tile_bird_clover: VARIANTS29,
    tile_flower_pansy: VARIANTS30,
    tile_grass_heather: VARIANTS31,
    tile_flower_water_lily: VARIANTS32,
    tile_bird_chicken: VARIANTS33,
    tile_bird_rooster: VARIANTS34,
    tile_bird_hen: VARIANTS35,
    tile_bird_turkey: VARIANTS36,
    tile_bird_goose: VARIANTS37,
    tile_bird_pheasant: VARIANTS38,
    tile_bird_parrot: VARIANTS39,
    tile_bird_phoenix: VARIANTS40,
    tile_bird_dodo: VARIANTS41,
    tile_herd_sheep: VARIANTS42,
    tile_herd_pig: VARIANTS43,
    tile_herd_hog: VARIANTS44,
    tile_herd_boar: VARIANTS45,
    tile_herd_warthog: VARIANTS46,
    tile_herd_alpaca: VARIANTS47,
    tile_herd_goat: VARIANTS48,
    tile_herd_ram: VARIANTS49,
    tile_cattle_cow: VARIANTS50,
    tile_cattle_longhorn: VARIANTS51,
    tile_cattle_triceratops: VARIANTS52,
    tile_mount_horse: VARIANTS53,
    tile_mount_donkey: VARIANTS54,
    tile_mount_moose: VARIANTS55,
    tile_mount_mammoth: VARIANTS56,
    tile_fish_clam: VARIANTS57,
    tile_fish_oyster: VARIANTS58,
    tile_fish_mackerel: VARIANTS59,
    tile_fish_kelp: VARIANTS60,
    tile_mine_gem: VARIANTS61,
    tile_mine_gold: VARIANTS62,
    tile_special_giant_pearl: VARIANTS63
  };
  var SHOWCASE_TRANSITIONS = {
    tile_tree_oak: TRANSITIONS,
    tile_tree_birch: TRANSITIONS2,
    tile_tree_cypress: TRANSITIONS3,
    tile_tree_fir: TRANSITIONS4,
    tile_tree_palm: TRANSITIONS5,
    tile_tree_willow: TRANSITIONS6,
    tile_fruit_apple: TRANSITIONS7,
    tile_fruit_pear: TRANSITIONS8,
    tile_fruit_lemon: TRANSITIONS9,
    tile_fruit_blackberry: TRANSITIONS10,
    tile_fruit_coconut: TRANSITIONS11,
    tile_fruit_golden_apple: TRANSITIONS12,
    tile_fruit_jackfruit: TRANSITIONS13,
    tile_fruit_rambutan: TRANSITIONS14,
    tile_fruit_starfruit: TRANSITIONS15,
    tile_grain_corn: TRANSITIONS16,
    tile_veg_pepper: TRANSITIONS17,
    tile_veg_mushroom: TRANSITIONS18,
    tile_veg_beet: TRANSITIONS19,
    tile_veg_broccoli: TRANSITIONS20,
    tile_veg_carrot: TRANSITIONS21,
    tile_veg_cucumber: TRANSITIONS22,
    tile_veg_eggplant: TRANSITIONS23,
    tile_veg_squash: TRANSITIONS24,
    tile_veg_turnip: TRANSITIONS25,
    tile_grass_grass: TRANSITIONS26,
    tile_grass_meadow: TRANSITIONS27,
    tile_grass_spiky: TRANSITIONS28,
    tile_bird_clover: TRANSITIONS29,
    tile_flower_pansy: TRANSITIONS30,
    tile_grass_heather: TRANSITIONS31,
    tile_flower_water_lily: TRANSITIONS32,
    tile_bird_chicken: TRANSITIONS33,
    tile_bird_rooster: TRANSITIONS34,
    tile_bird_hen: TRANSITIONS35,
    tile_bird_turkey: TRANSITIONS36,
    tile_bird_goose: TRANSITIONS37,
    tile_bird_pheasant: TRANSITIONS38,
    tile_bird_parrot: TRANSITIONS39,
    tile_bird_phoenix: TRANSITIONS40,
    tile_bird_dodo: TRANSITIONS41,
    tile_herd_sheep: TRANSITIONS42,
    tile_herd_pig: TRANSITIONS43,
    tile_herd_hog: TRANSITIONS44,
    tile_herd_boar: TRANSITIONS45,
    tile_herd_warthog: TRANSITIONS46,
    tile_herd_alpaca: TRANSITIONS47,
    tile_herd_goat: TRANSITIONS48,
    tile_herd_ram: TRANSITIONS49,
    tile_cattle_cow: TRANSITIONS50,
    tile_cattle_longhorn: TRANSITIONS51,
    tile_cattle_triceratops: TRANSITIONS52,
    tile_mount_horse: TRANSITIONS53,
    tile_mount_donkey: TRANSITIONS54,
    tile_mount_moose: TRANSITIONS55,
    tile_mount_mammoth: TRANSITIONS56,
    tile_fish_clam: TRANSITIONS57,
    tile_fish_oyster: TRANSITIONS58,
    tile_fish_mackerel: TRANSITIONS59,
    tile_fish_kelp: TRANSITIONS60,
    tile_mine_gem: TRANSITIONS61,
    tile_mine_gold: TRANSITIONS62,
    tile_special_giant_pearl: TRANSITIONS63
  };

  // docs/seasonal-vector-tiles/_entry.ts
  var cap = (s) => s.charAt(0).toUpperCase() + s.slice(1);
  var labelFor = (key) => key.split("_").slice(2).map(cap).join(" ");
  var familyFor = (key) => key.split("_")[1] ?? "";
  var tiles = Object.keys(SHOWCASE_TILES).map((key) => ({
    key,
    label: labelFor(key),
    family: familyFor(key),
    V: SHOWCASE_TILES[key],
    T: SHOWCASE_TRANSITIONS[key]
  }));
  window.SEASONAL_DEMO = { tiles };
})();
