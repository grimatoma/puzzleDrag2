/**
 * Keys of `FLAG_CATEGORIES` in `src/flags.ts` — used by story-flag registry rows.
 */

export enum StoryFlagCategoryId {
  Story = "story",
  Frostmaw = "frostmaw",
  Mira = "mira",
  Misc = "misc",
}

export const STORY_FLAG_CATEGORY_ID_VALUES = Object.values(StoryFlagCategoryId);
