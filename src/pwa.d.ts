// Ambient types for vite-plugin-pwa's virtual modules (the `virtual:pwa-register/*`
// imports). Needed because tsconfig `types` lists only `vite/client` + `node`,
// which don't include the PWA register helpers.
/// <reference types="vite-plugin-pwa/react" />
/// <reference types="vite-plugin-pwa/info" />
