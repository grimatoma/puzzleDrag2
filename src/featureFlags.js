// Feature flags — set to true to enable the feature.
// Fire and rats hazards are gated here so they can be turned on independently.

export const FIRE_HAZARD_ENABLED = true;
export const RATS_HAZARD_ENABLED = true;
