// Zones feature — Phase 1 placeholder.
// Slice + data are wired up; the full UI (Start Farming modal, zone selector)
// lands in Phase 2. No viewKey / modalKey is set so this component never
// auto-mounts via the import.meta.glob discovery in src/ui.jsx.

export default function ZonesPanel() {
  return null;
}
